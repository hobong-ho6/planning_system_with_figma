# 게이트 리포트 — (Logged in) 초대페이지(63411-34291) 선물 버튼 2종·정책 업데이트

- 작업일: 2026-07-28
- 대상 위키: https://wiki.workers-hub.com/pages/viewpage.action?pageId=4479306980 (럭키볼 친구에게 선물하기 캠페인)
- 대상 프레임: `(Logged in) 초대페이지` (node-id 63411-34291)
- 담당 FE 팀: **LV** (위키 Screen `LV > 초대자 프로모션 페이지` 하위 — UIT/LV 구분이 위키에 있으므로 사용자 확인 없이 LV 규칙 적용). 프리픽스 `mini_luckyball_`, 치환자 `{0}`(본 건 변수 없음)
- 검증 대상 파일: `xlt/xlt_output_LV_20260727.xlsx` (73키, **2026-07-28 14:19 갱신본** — 병행 세션이 `{0} JPYC` 공백 규칙·`interest_desc`·`benefits_desc`를 반영한 최신본으로 재검증) · 용어집 라이브 **v3.5**(107 terms, 이번 실행에서 API 새로 조회)

## 0. 변경 요약 (Figma 원본 새로 조회 — 캐시 미사용)

`scripts/fetch_comments.py` `fetch_threads`(좌표 정규화 적용) 결과 미해결 루트 4건:

| 통합 No | 종류 | 코멘트 본문 | 매칭 텍스트 |
|---|---|---|---|
| 1 | 정책(순수) | 초대자 프로모션 페이지에 사용자가 Unifi 로그인 된 경우 화면 | (없음 — 화면 전체 정책) |
| 2 | 정책 + `↳ xlt` | **선물하기 링크 복사 버튼**을 눌러 클립 보드에 링크가 복사된 경우 팝업 토스트 | 초대 링크가 복사되었어요! |
| 3 | 정책(순수) | **선택 시, LINE에서 친구 선택 후 정의된 LINE MSG 공유** (신규) | LINE 친구에게 선물하기 |
| 4 | 정책 + `↳ xlt` | 버튼 선택 시, 클립 보드에 사용자 파트너 아이디가 포함된 피초대자 프로모션 페이지 URL이 클립보드에 복사된다. | **선물하기 링크 복사** (신규 텍스트, node 64799:3972) |

**위키 v132 대비 델타:**

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| Description 정책 2 | **친구초대** 링크 복사 버튼을 눌러… | **선물하기** 링크 복사 버튼을 눌러… |
| Description 정책 3 | (없음) | **선택 시, LINE에서 친구 선택 후 정의된 LINE MSG 공유** (신규) |
| Description 정책 3→4 | 버튼 선택 시, 클립 보드에 …URL이 복사된다. | 번호만 4로 이동 (문구 동일) |
| XLT 표 | No2 `link_copied` · No3 `invite_me_too_line` | **No4 `gift_copy_link` 행 추가** (No2·No3 유지) |
| 이미지 | 핀 1~3 | 핀 1~4 재어노테이션 |

- **디자인 변경 실체**: 하단 CTA가 **버튼 1개 → 2개**가 됐다. 기존 `LINE 친구에게 선물하기`(LINE 공유) 아래에 **텍스트 링크 `선물하기 링크복사`(클립보드 복사)** 가 신설됐고, 각 버튼의 동작 정책이 코멘트로 분리됐다.
- **신규 키 없음 / 삭제 키 없음** — 3개 키 모두 이미 전역 다국어 표·엑셀에 5개 언어로 등재돼 있다(`gift_copy_link`는 2026-07-28 25034 작업에서 추가됨). 따라서 **전역 표 73키·XLT 엑셀은 변동 없음(재생성·재첨부 불필요)**.

### ⚠️ No3 판단 근거 (사용자 확인 요청 대상)

이번 Figma에서 **코멘트 3에는 `xlt` 답글이 없다**(순수 정책). 코멘트 선별 모드 규칙을 문자 그대로 적용하면 `LINE 친구에게 선물하기`는 XLT 표에서 빠진다. 그러나 **버튼은 화면에 그대로 있고 `mini_luckyball_invite_me_too_line` 키가 이미 부여·번역·사용 중**이므로, 표에서 제거하면 실재 문구의 번역 행이 사라지는 회귀가 된다. → **No3를 유지**하는 쪽으로 처리했다(비파괴적 선택). 디자이너가 마커를 의도적으로 뗀 것이라면 알려주면 제거한다.

## 1. 한국어 원문 교정 (게이트 1단계) — alias 2건

Figma 원문에 띄어쓰기 오류가 있어 XLT `ko_KR`은 교정본을 유지한다. **디자이너에게 원본 수정 요청 필요.**

| Figma 원문 | XLT Key (교정본 ko_KR) | 교정 사유 |
|---|---|---|
| `초대 링크가 복사 되었어요! ` (끝 공백 포함) | `mini_luckyball_link_copied` → **초대 링크가 복사되었어요!** | `복사되다`는 파생동사라 붙여 쓴다. 문미 불필요 공백 제거 |
| `선물하기 링크복사` | `mini_luckyball_gift_copy_link` → **선물하기 링크 복사** | `링크 복사`는 명사구라 띄어 쓴다 |
| `LINE 친구에게 선물하기` | `mini_luckyball_invite_me_too_line` | 교정 불필요 — 원문 일치 |

※ 같은 프레임의 **XLT 대상이 아닌** 본문에도 기존 오타가 잔존한다(`지갑 생성 포`→`포함`, `정보가변경된`→`정보가 변경된`, `LINE계정`·`LINE공식계정` 띄어쓰기). 핸드오프 P3 항목과 동일 건으로, 이번 작업 범위(코멘트 선별) 밖이라 손대지 않았다.

## 2. 자동 검증 — `scripts/validate_translation.py` (a)

실행: `python3 scripts/validate_translation.py xlt/xlt_output_LV_20260727.xlsx scripts/glossary.json`
→ **전체 파일 73키 전수 실행** (14:19 갱신본 기준)

| 심각도 | 건수 | 비고 |
|---|---|---|
| 🔴 P0 | **0건** | 통과 기준 충족 |
| 🟡 P1 | 101건 | 전건 [용어 불일치] — 용어집 협소 매핑·어형 변화 오탐 |
| 🟢 P2 | 26건 | 전건 [마침표 스타일] |

1단계(한국어) P0 0·P1 0 / 2단계(용어집 v3.5) 완료 / 3단계(다국어·이질 문자체계) 검출 0.

> 참고: 리포트 **파일**은 심각도별 상위 20건만 기록하므로, 아래 건별 판정은 `TranslationValidator.issues` 전량을 열거해 확인했다(`md/check.md` 함정 2-1).

## 3. 수동 3단계 체크표 (b) — 본 변경 관련 키 3종

자동 3단계는 위와 같이 **전체 파일 전수** 실행했고, 의미 기반 수동 검토는 이번 부분 작업의 관련 키 3종을 대상으로 수행했다(신규 생성 문구 없음 — 전부 기존 등재 키의 재배치).

### 1단계 — 한국어(ko_KR)

| 키 | ko_KR | 맞춤법·띄어쓰기 | 조사/받침 | 판정 |
|---|---|---|---|---|
| `mini_luckyball_link_copied` | 초대 링크가 복사되었어요! | 정상(`복사되었어요` 붙임) | `링크`+`가` 정상 | ✅ 정확 |
| `mini_luckyball_invite_me_too_line` | LINE 친구에게 선물하기 | 정상 | `친구`+`에게` 정상 | ✅ 정확 |
| `mini_luckyball_gift_copy_link` | 선물하기 링크 복사 | 정상(`링크 복사` 띄어쓰기) | — | ✅ 정확 |

### 2단계 — 용어집 대조 (라이브 v3.5)

| 용어 | 등장 키 | 용어집 표기 | 실제 표기 | 판정 |
|---|---|---|---|---|
| 복사 | link_copied · gift_copy_link | en `copy` | `copied`(활용형) / `Copy` | ⚠️ 오탐 — 4절 참조 |
| 링크 | 3키 전부 | (v3.5 미등재, v3.6 추가 예정) | en `link` / ja `リンク` / zh `連結` / th `ลิงก์` 일관 | ✅ 일관 |
| 친구 | invite_me_too_line | (v3.5 미등재) | ja `友だち` / zh `好友` / th `เพื่อน` 일관 | ✅ 일관 |
| 선물하기 | invite_me_too_line · gift_copy_link | (v3.5 미등재) | ja `プレゼント` / zh `送禮` / th `มอบ`·`ของขวัญ` | ✅ 일관 |

### 3단계 — 다국어(ja/en/th/zh) 의미·표기

| 키 | ja_JP | en_US | th_TH | zh_TW | 판정 |
|---|---|---|---|---|---|
| `link_copied` | 招待リンクをコピーしました！ | The invite link has been copied! | คัดลอกลิงก์เชิญแล้ว! | 已複製邀請連結！ | ✅ "초대 링크 복사됨" 의미 일치, 완료 시제 일관, 번체 정자 정상 |
| `invite_me_too_line` | LINEの友だちにプレゼントする | Gift to a LINE friend | มอบให้เพื่อนใน LINE | 送禮給 LINE 好友 | ✅ "LINE 친구에게 선물" 의미 일치 |
| `gift_copy_link` | プレゼントリンクをコピー | Copy gift link | คัดลอกลิงก์ของขวัญ | 複製送禮連結 | ✅ "선물 링크 복사" 의미 일치, 버튼 라벨 어조(명령형/명사형) 적절 |

- 이질 문자체계 혼입 없음, placeholder 없음, 줄바꿈 없음 — 3단계 자동 검출과 일치.
- 화면 내 일관성: 두 버튼(`선물하기`)과 토스트(`초대 링크`)의 명명이 갈린다 → 4절 (d-2) 참조.

## 4. 검증기 보고 건별 처리 판정 (c) — `issues` 전량 열거 기준

| 키 | 심각도 | 지적 | 처리 판정 |
|---|---|---|---|
| `mini_luckyball_link_copied` | P1 | en_US: '복사' → 'copy' 권장 | **오탐** — 실제 en은 `…has been copied!`로 **어형 변화**(`copy`→`copied`). 검증기가 부분일치라 활용형을 못 잡는 구조적 한계(`md/check.md` 2-1 (A) 계열). 번역은 정확 |
| `mini_luckyball_invite_me_too_line` | — | 지적 없음 | 해당 없음 |
| `mini_luckyball_gift_copy_link` | — | 지적 없음 | 해당 없음 |

전체 파일 기준 나머지 P1 100건·P2 26건은 이번 변경과 무관한 기존 항목이며, 누적 게이트 리포트에서 **전건 오탐**(용어집 협소 매핑·어형 변화·타이틀 마침표)으로 처리 판정된 이력을 유지한다. 이번 작업으로 새로 발생한 건 **0건**.

## 5. 추가 개선·제안 (d) — 임의 적용하지 않음, 사용자 결정 사항

### (d-1) 용어집 보완 권장

1. **`복사` 어형 변화 오탐** — 용어집 en `copy`가 `copied`/`copying` 활용형에서 반복 미검출된다. `md/check.md` 2-1에 기록한 **구조적 한계 (A)** 와 같은 계열이며, 용어를 고쳐도 해결되지 않는다(검증기 매칭 방식 문제). **용어집 변경을 권장하지 않고 오탐 판정으로 종결**한다.
2. **`링크`·`친구`·`선물하기` 3종** — 이번 화면에서 반복 사용되고 5개 언어 표기가 일관되나, **이미 병행 세션에서 용어집 v3.6에 추가돼 있다**(`glossary_v3.6_full.json`, 라이브 반영 대기). 중복 제안하지 않는다. → 라이브 CMS는 아직 **v3.5(107 terms)** 이므로, v3.6/v3.7 붙여넣기 반영이 남아 있다는 점만 상기한다.

### (d-2) 표현·정책 제안

3. **버튼–토스트 명칭 불일치** — 버튼은 `선물하기 링크 복사`인데 복사 성공 토스트는 `초대 링크가 복사되었어요!`다. 사용자 입장에선 "선물하기 링크"를 눌렀는데 "초대 링크"가 복사됐다고 안내된다. `mini_luckyball_link_copied`를 `선물하기 링크가 복사되었어요!`로 통일하는 안을 권장하나, **`link_copied`는 다른 화면과 공유되는 키**일 수 있으므로 일괄 영향 확인 후 결정이 필요하다. 임의 변경하지 않았다.
4. **Figma 원문 오타 수정 요청(P3 누적)** — 1절 alias 2건(`복사 되었어요`·`링크복사`) + 프레임 본문 잔존 오타(`지갑 생성 포`·`정보가변경된`·`LINE계정`)를 디자이너에게 일괄 전달 권장.

## 6. 통과 판정

- 🔴 **P0 = 0건** (자동 전수 + 수동 모두) → **통과**, 위키 반영 진행 가능
- P1/P2: 4절대로 건별 처리 판정 완료(본 변경 관련 1건 오탐), 나머지는 기존 판정 유지
- (d) 권장 사항은 사용자 결정 전까지 **미반영**

# 번역 검증 게이트 리포트 — LINE App x 팝업 (63706:49595)

- 대상 위키: pageId 4479306980 / 섹션 **피초대자 프로모션 페이지** / 팀 **LV** (`mini_luckyball_` · `{0}`)
- 대상 엑셀: `xlt/xlt_output_20260724163331.xlsx` (75키)
- 모드: 코멘트 선별(팝업 화면 — 육안 검증) / 신규 프레임 행 추가
- 용어집: API v2.6 새로 조회.

## 처리 대상 (통합 번호, 팝업 육안 검증 ✅)

| No | 종류 | 내용/텍스트 | 키 |
|----|------|------------|----|
| 1 | 정책 | LINE 앱이 아닌 환경에서 페이지 진입 시 팝업 안내 | (XLT 없음) |
| 2 | xlt | LINE 앱에서 참여하실 수 있습니다.\nLINE 앱에서 참여하시겠습니까? | **신규** `mini_luckyball_line_app_only` |
| 3 | 정책+xlt | 선택 시, LINE Download 페이지로 이동 → 버튼 `확인` | 기존 `mini_luckyball_confirm` |

- **팝업겹침 육안 검증**: 렌더 확인 결과 팝업은 본문(참여 안내)+`확인` 버튼. #3 정책이 가리키는 버튼은 `확인`이며, 좌표 매칭이 배경 `다음에도 하나더 받아요!`로 잘못 잡은 것을 **수동 교정**(confirm). #2는 팝업 본문으로 정매칭.
- **신규 키 1건**(line_app_only), 나머지(confirm)는 기존 키 재사용.

## 0. 한국어 원문 교정 (게이트 1단계)

- 신규 키 ko_KR `LINE 앱에서 참여하실 수 있습니다.\nLINE 앱에서 참여하시겠습니까?` — 맞춤법·띄어쓰기·표기 이상 없음(마침표·물음표 종결 정상), Figma 원문과 동일. alias 기록.

## Executive Summary (자동 — `scripts/validate_translation.py`, 75키)

| 심각도 | 건수 | 카테고리 |
|--------|------|----------|
| 🔴 P0 (Critical) | **0건** | 빈칸, 오타, 언어 혼입, 용어집 표기 위반 |
| 🟡 P1 (Medium) | 25건 | 용어 불일치(용어집 협소 매핑) |
| 🟢 P2 (Low) | 28건 | 마침표 스타일 |

→ **P0 = 0**.

## 1단계: 한국어 (수동, 전수 75키)

- 신규 키: 정중한 `-실 수 있습니다`/`-시겠습니까?` 존댓말, 오타·띄어쓰기 이상 없음. 자동 P2(마침표)는 `.`·`?` 정상 종결이므로 **오탐**.
- 나머지 74키 재점검: 이상 없음(기존 교정본).

## 2단계: 용어집 (수동, 전수)

- 예외 패턴(LINE, JPYC 등) 표기 통일. `LINE App`(zh)·`แอป LINE`(th)·`LINEアプリ`(ja) 브랜드 표기 일관. zh_TW 번체 정자(參與) 확인. 자동 P1 25건 전건 협소 매핑 오탐.

## 3단계: 다른 언어 (수동, 전수)

- 빈칸 0·언어 혼입 0·컬럼 swap 0. 신규 키 5개 언어 줄바꿈(`\n`) 보존 확인.
- 신규 키 재검토: ja `ご参加いただけます／参加しますか？`, en `participate in the LINE app`, th `เข้าร่วมผ่านแอป LINE`, zh `在 LINE App 中參與` — 자연·정확.

## 각 건 처리 판정

- **P1 25건 — 전건 오탐**(용어집 협소 매핑: 확인/변경/미션/복사/송금/예치). 수정 없음.
- **P2 28건 — 전건 오탐**(배너·타이틀·팝업 무마침표/종결부호 스타일). 유지. 신규 키 P2도 오탐(정상 종결).

## (d) 추가 개선·제안 (권장)

- **(d-1) 용어집 보완 권장**: 다의어 문맥 변형 추가 시 P1 오탐 감소(`md/landpress.md`). 이번 신규 키 무관.
- **(d-2) 브랜드 표기**: `LINE 앱`의 언어별 표기(zh `LINE App`/th `แอป LINE`/ja `LINEアプリ`) — 현행 일관, 유지 권장.

## 통과 판정

- 자동 **P0=0** + 수동 3단계 **P0=0** → **통과**. 신규 키 1건 추가(75키), 엑셀 재생성. P1/P2 전건 오탐. (d) 권장 미반영.

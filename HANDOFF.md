# HANDOFF

<!--
════════════════════════════════════════════════════════════
이 파일은 세션 간 핸드오프 문서입니다. (사람 + Claude 공용)

■ Claude 사용 규칙
1. [세션 시작] 이 파일을 가장 먼저 읽고 "현재 상태"·"다음 할 일" 확인 후 한 줄 브리핑.
2. [세션 종료] "핸드오프 갱신"/"세션 정리"/"handoff" 시 아래 규칙으로 갱신.
   - "현재 상태" 덮어쓰기 / 완료 항목 이동 / 새 할 일 추가 / 세션 기록 맨 위 추가(최대 5개)
3. [작성 원칙] 경로·커밋·명령어 포함, "왜"를 우선, 추측/사실 구분, 300줄 이내.
════════════════════════════════════════════════════════════
-->

## 프로젝트 정보

- **프로젝트명**: planning_system_with_figma — Figma → 다국어 번역(XLT) → Confluence 위키 파이프라인
- **한 줄 설명**: Figma 화면의 XLT 코멘트 문구를 5개 언어로 번역·검증하고 Confluence 위키에 화면/번역/이미지로 반영
- **주요 경로/저장소**: **PC마다 다르다** — `AL02359162.local`은 `/Users/user/Documents/planning_system_with_figma`, `AD03230205ui-iMac.local`은 `/Users/ad03230205/…`. GitHub `hobong-ho6/planning_system_with_figma` (branch main)
- **관련 링크**:
  - 작업 위키(주, 2026-08-04~): https://wiki.workers-hub.com/pages/viewpage.action?pageId=4541588845 (**JPYC 럭키볼 시즌 3**) — **현재 v81**(2026-08-10 — 사용자 편집) · Screen UIT 2 + LV(mini 2 · Promotion Page 6 · Popup 4) + OA MSG 2 = **프레임 16개** · 번역표 **58키** · 엑셀 3종(**ALL v22 · LV v13 · UIT v18**) · OA Flex zip 2종(`Reward_Confirm` v5 · `mission_complete` v4)
    - ⚠️ 엑셀 파일명은 `xlt_output_season3_{ALL,LV,UIT}_20260804.xlsx`로 **고정**이다(첨부 정본 파일명 — 본문 view-file 링크 유지 목적). 날짜를 올리지 않고 **내용만** 갱신한다
  - **마스터 페이지 4종**(**2026-08-06 재정합 완료** — Release History 데이터 행 ↔ Summary `<h3>버전 N` 블록 1:1):
    `4368569057` [Master] Lucky Ball Promotion(**v13, 8↔8**) · `4368568387` [Master] Mission and Reward(v16, 9↔9) · `4386238705` [Master] Unifi mini(**v9, 3↔3**) · `4386238738` [Master] Wallet Mode(v3, 2↔2)
  - 럭키볼 친구에게 선물하기 캠페인: `4479306980` — v149, 전역 번역표 75키
  - **비실시간 지급 2부작**(Lucky Ball 마스터 Release History 6·7행): `4541600637` 럭키볼 친구초대 프로모션 리워드 비실시간 지급 변경(v10, `[PL] Unifi v1.6.4.1`) · `4540065229` **…비실시간 지급 변경 후 추가 개선**(v10, `[PL] Unifi v1.6.6` — Jira 티켓 없음)
  - `4479295382` **K-Pick 노출 정책**(v10) — mini 마스터 Release History 2행의 하위 문서. K-Pick 노출 조건의 **최신 정본**(②To-be)
  - Mission and Reward 정책 + FAQ: `4368569133` — v39, FAQ 10문항 5개 언어(미결정 6건은 '다음 할 일' P2)
  - XLT 시스템 등록값(**값의 정본**): **API 1차 조달**(2026-08-07 세션 #17부터) — `python3 scripts/fetch_xlt_registry.py --service <S>`. **export 수동 요청은 폐지**(VPN 미연결·API 장애 시에만 폴백). 절차 정본 `md/xlt-verify.md`
    - ⛔ **서비스가 3개이고 키스페이스가 다르다** — `Dapp Portal` v2.6.0(1,596키) · `Unifi` **v1.6.7(2,134키)** · `Kaia Wallet` v1.4.3(366키). 공통 335키 중 **151키(45%)가 값 상이**. `UF_`(UIT) 정본은 **`Unifi`**, `mini_`·`unifi_promotion_`(LV) 정본은 **`Dapp Portal`**. **대조 전 타겟 확정 필수**
    - 디바이스는 `WEB BROWSER` 하나뿐(다른 값은 빈 배열) · 언어 5개 외에는 HTTP 500 · **쓰기 API 없음**(업로드는 사용자)
  - 기타 위키: `4394814893` Next bay 미션앤리워드(v12) · `4515188069` Kaia Wallet > Unifi Mobile 전환 유도 · `4515188588` 미션 유형 구분 · `4402623039` 7월 JPYC - 예치 골드럭키볼 · `4251435406` [Screen]Wallet Mode · `4244669494` [Plan] EN/UK/CA/SG 분기
  - 부모 페이지(신규 생성 위치): `3910828993` [Hogeun] (space `UNIFI`)
  - Figma 파일: `GOCHAYBS7hIrmWRGNuJOKV` (Web3) · 시즌3 페이지 `65923:2485`
  - 절차 문서: `CLAUDE.md`, `md/translate.md`, `md/wiki.md`, `md/OA.md`, `md/check.md`, `md/landpress.md`, `md/IA.md`, **`md/xlt-verify.md`**(XLT 등록값 검증 — 세션 #17 신설)
  - 용어집: **라이브 v4.7**(116 terms · `deprecated_terms` 14종) — 2026-08-10 사용자 CMS 반영, **API 재조회로 산출 JSON과 완전 동치 확인**. v4.7 = `포이카츠` zh `點數活動`→`集點活動` + metadata 수치 정정. 이력 `md/glossary-changelog.md` · IA 정본 `md/IA.md`
  - 기획자 가이드: **라이브 v26**(2026-08-10 게시 완료, **태그 `guide-v26`**) — v26=시스템 가이드 탭 ⑦ 가이드 버전 관리(git 정본·태그 규약) · v25=v24+용어집 v4.7 병합. **⛔ 소스 정본은 git 추적 `guide/`다**(2026-08-10 신설, 커밋 `7d1bd3d`·`3075eb6`) — 최신 판별 = `git log -- guide/`, zip은 게시용 빌드 산출물(`.gitignore` 유지), 절차는 `md/landpress.md` §5-1 0단계(git 기반으로 전면 개정). 게시 확인 후 `git tag guide-vN`이 라이브 정본 표식이다.
    - 버전 연혁: v26=⑦ 버전 관리 규약 · v25=v24+용어집 v4.7 병합 · v22~v24=IA #4 3회 발행 · v21=용어집 v4.6 · v20=「🧭 시스템 가이드」 탭
  - IA 주간 점검: 스케줄 태스크 `weekly-unifi-ia-check`(매주 월 10:00) — 프로덕션 + Beta + mini Beta. 리포트 `reports/ia/`
- **토큰**: Figma PAT + Confluence PAT — **사용자가 매 세션 채팅으로 제공**(파일/코드에 하드코딩 금지). 착수 전 유효성 검증 필수.

---

## 프로젝트 스킬 / 에이전트

저장소에 커밋돼 **`git pull`만으로 다른 PC에서도 동일하게 동작**한다. 새로 만들면 이 표에 한 줄 등록하고 **스킬 파일과 같은 커밋으로 푸시**한다.

| 종류 | 이름 | 용도 / 호출 시점 |
|---|---|---|
| 스킬 | `handoff` | 세션 인수인계 정본 절차 — 세션 시작(상태 확인·pull·브리핑), 종료(WIP 보존 → HANDOFF 갱신 → 커밋·푸시) |
| 에이전트 | `figma-source-issues` | 게이트 리포트 전체를 스캔해 **Figma 원문 수정 요청 목록**을 화면별로 취합(읽기 전용) |
| 에이전트 | `translation-reviewer` | XLT 번역 **2차 독립 검토**(분리 컨텍스트 재판정, 수정하지 않음) |
| 에이전트 | `wiki-policy-auditor` | 위키 Description ↔ Figma 코멘트 스레드 **1:1 정합성 감사**(읽기 전용) |
| 에이전트 | `glossary-guide-updater` | 용어집 버전 상승 시 **기획자 가이드 zip의 용어집 탭 갱신**(`md/landpress.md` §5-1) |

- 훅: `.claude/settings.json`의 SessionStart가 `HANDOFF.md`를 자동 주입(git 추적).
- `.claude/settings.local.json`(권한 allowlist·MCP)은 **기기별 설정이라 git 제외**.

---

## 환경 / 머신 노트

- **⚠ Python 의존성이 사라질 수 있다** — 세션 #7 시작 시 `pandas`·`openpyxl`·`requests`·`PIL` 전부 없어 모든 스크립트가 `ModuleNotFoundError`로 실패했다(homebrew python 3.14). 복구:
  ```bash
  pip3 install --break-system-packages -r scripts/requirements.txt
  ```
  → **`Pillow`는 세션 #8에서 `requirements.txt`에 추가됨**(`143134d`) — 이제 별도 설치 불필요. 세션 #8 시작 시점에는 4종 모두 정상이었다.
- `validate_translation.py`는 **용어집을 두 번째 위치 인자로만** 받는다(`--glossary` 플래그 없음). 빠뜨리면 "용어집이 로드되지 않음. 2단계 건너뜀"으로 조용히 통과하니 주의:
  ```bash
  python3 scripts/validate_translation.py <엑셀> scripts/glossary.json
  ```
- `check_wiki_storage.py post --page`는 `CONFLUENCE_PAT` **환경변수**를 요구한다(`--token` 아님). `compare_wiki_xlt.py`도 동일하게 `CONFLUENCE_PAT` 환경변수를 쓴다.
- **XLT 읽기 API는 사내망/VPN 전제다**(무인증이지만 IP 화이트리스트 추정). 실패 유형별 처리는 `md/xlt-verify.md` §2-4 — 특히 **사내 프록시 로그인 페이지가 200 + HTML로 오는 경우**를 `RuntimeError`로 잡는다. VPN 미연결 시 사용자 export로 폴백하며, **레지스트리는 항상 옵셔널**이라 게이트 전체가 실패하지는 않는다.
- `.claude/launch.json`은 **git 제외 대상**이고 기존 항목이 **다른 PC의 절대 경로**(`/Users/user/…`)로 되어 있다. 이 PC에서 미리보기가 필요하면 자기 경로로 항목을 추가한다(세션 #17은 `xlt-audit-site` 8126·`guide-v20` 8127을 추가했다).

---

## 현재 상태

> 마지막 갱신: 2026-08-10 (**세션 #21**) · 작업 PC **`AL02359162.local`** · branch `main` · 마지막 커밋 **`4e2e097`** · IA 주간 점검 #4 + **로그인 보강**

**진행 중 작업(WIP)**: **없음.** (작업 트리 clean — 세션 #20 변경은 `3aab91a`로 그 세션이 직접 커밋함)

> 🔴 **가이드 zip 덮어쓰기 사고 — 게시 전 반드시 읽을 것**
> 세션 #20과 이 세션이 **같은 날 병렬로 각각 `web3_planning_v23.zip`을 만들었고, 이 세션이 11:04에 세션 #20의 v23을 덮어썼다.**
> - 현재 `dropweb/web3_planning_v23.zip` = **이 세션의 IA 보강본**(임베드 용어집 **4.6** · `集點活動` 없음 · `total_deprecated_terms` **13**)
> - **사라진 것** = 세션 #20의 **용어집 v4.7 반영본**. `dropweb/`은 git 미추적이라 복구 불가 — 단, 원본 JSON `reports/glossary_v4.7_for_cms.json`이 커밋돼 있어 **재생성은 가능**하다
> - **원인**: 최신 zip 판별(§5-1 0단계)은 **작업 시작 시점**에만 했고, 30분 뒤 파일을 **쓰기 직전에 다시 확인하지 않았다**. 세션 #20은 같은 상황에서 v22를 발견해 피했는데(그쪽 기록 참조), 이 세션은 반대 방향으로 당했다
> - **규칙 보강**: zip을 **쓰기 직전에** 같은 파일명 존재 여부를 재확인하고, 있으면 내부 버전을 열어 남의 산출물인지 판별한 뒤 파일명을 올린다

**이번 세션(#21) — IA 주간 점검 #4(스케줄 태스크 `weekly-unifi-ia-check`) + 사용자 제공 로그인 세션으로 보강:**

- **🔴 USDT PLUS 이율 5%→7%**(2026-08-06 발효, BASIC 3% 유지, Boost 3% 합산 **최대 연 10%**) — 프로덕션 홈 히어로가 `Up to 10% annual rate`로 교체됐다. **위키·XLT에 구 수치(PLUS 5%·합산 8%)가 남아 있으면 교체 대상.**
- **✅ 캐리오버 ⓓ 해소 — 액션 라벨 개편이 프로덕션에 반영됐다**: 내 자산 액션이 **보내기 / 채우기 / 교환하기 / 은행출금**(구 송금하기·입금하기·은행송금 소멸). **「외부 지갑 연결」(케밥 메뉴)도 프로덕션 반영 확인.** 구 라벨이 남은 XLT·위키 문구는 교체 대상이다.
- **✅ 캐리오버 ⓐ 정정 — 스켈레톤은 프로덕션에서 해소됐다**: 4조합 실측 결과 **프로덕션 비로그인·로그인 모두 정상**, **Beta 웹은 비로그인만**, **Beta mini `/benefits-mini/daily-mission`은 로그인에서도 고착**. 남은 문제는 **Beta 한정**이고 mini Reward가 가장 심각하다(럭키볼 캠페인 핵심 화면).
- **🆕 신규 화면 `/interest/usdt`「나의 USDT 누적 이자」** — IA에 없던 라우트. 어휘 **승인 대기 ⑥** 신설(`asset_interest_usdt_01` 잠정).
- **🆕 Unifi Pay Direct**(공지 8/5, B2B 결제·수수료 1%) — 현재는 파트너 콘솔이라 사용자 IA 밖. Beta 디버그 패널의 **「결제 가맹점 진입」** 버튼이 사용자 화면 준비 신호.
- **mini 변경 2건** — **「매일 출석 체크」 신설**(8/3 "mini는 출석 없음" 기재 정정) · **럭키볼 친구 초대에 비실시간 지급 반영**(2주 이내·「지급될/지급 완료 리워드」·유의사항 6종 — 위키 작업 중인 스펙이 화면에 등장).
- **산출물** — `md/IA.md` 갱신 · 리포트 `reports/ia/ia_check_report_2026-08-10.md`(본문 + **부록 로그인 보강**) · 가이드 **v22→v23→v24**(3회 발행) · 3커밋 `5337758`·`13b02c4`·`402dcc1`.

**작업 중 확인된 상태 변화**: 프로덕션 `/apps` **30→29 Apps** · 부스트 **명칭 「KAIA 부스트」→「부스트」**·운영 **~10/4 연장** · 알림/거래내역 필터 **한국어 라벨 확정**.

**캐리오버 이슈(미해결)**: ⓐ **Beta mini Reward 스켈레톤**(로그인에서도 고착 — 프로덕션은 해소) ⓑ 정책 충돌 1건(mini 이자 배너, 2주 연속) ⓒ 영문 UI에 한국어 원문 노출 2건(이번엔 한국어 UI라 판정 보류) ⓓ **Beta 홈 「최대 0% 보상」 표기**(프로덕션 4.2% — 수치 바인딩 오류 의심) ⓔ 입금 브릿지 배너가 **프로덕션에서도 클릭 무반응**.

---

## 다음 할 일

### 사용자 액션 대기 (Claude가 할 수 없음)
- (추적 종료) **XLT 수정 엑셀 업로드는 사용자가 이후 수행** — `xlt/registry_fix/xlt_fix_Unifi_20260810.xlsx`(1키, zh `網址`→`URL`). 2026-08-10 사용자 결정으로 이 문서에서 추적하지 않는다. 업로드 시점에 `fetch_xlt_registry.py` 재조회 확인 권장(세션 #18 미반영 실측 전례).

### Claude 실행 대기 (승인 시 진행)
- 없음. (v25 병합본은 2026-08-10 세션 #20 후속에서 **생성 완료** — 위 사용자 액션만 남음)

### 차단 요소
- 없음.

### 사용자 결정으로 종결(재작업 금지)
- **⛔ Beta 디버그 패널은 IA 점검에 사용하지 않는다**(2026-08-10 사용자 결정) — 「국가 모킹」·「알림 테스트」·「결제 가맹점 진입」은 **개발자 도구**다. **국가 모킹으로 JP IP·Wallet Mode를 실측 승격하는 방안도 폐기** — 그 축들은 실제 접속 환경(VPN·해외 IP·LIFF 링크·실기기 캡처)으로만 승격한다. **다음 회차에 재제안 금지.** 다만 「결제 가맹점 진입」 버튼의 **존재 자체**는 Unifi Pay 사용자 화면 준비 신호로 기록만 한다(`md/IA.md` §4).
- **⛔ 2026-08-10 미결 전건 종결**(사용자 지시 「남은 건은 진행하지 않고 완결 처리」) — 아래는 **재보고·재제안 금지**다. 근거·권장 문안은 각 게이트/감사 리포트에 보존돼 있으니 필요할 때만 참조한다.
  - **처리 4건** — ① 용어집 예외 위반 5건 중 **#7만 수정**(zh `網址`→`URL`), **#8~#11은 팀 리뷰 시 전체 XLT 조회로 이관** ② 시즌3 감사 후속 6건 **전건 현행 유지**(ⓑ 키 중복·ⓒ Bottom Sheet 참조 오기는 **Figma 수정 요청도 하지 않음** / ⓔ 고정 추천 코드 2종도 그대로) ③ 잔여 번역 개선 M-1·M-4~M-10 **전건 현행 유지** ④ `포이카츠` zh → **용어집 v4.7 `集點活動`으로 종결**
  - **미착수 종결 10건** — XLT 구 표기 49건 · Next bay 릴리즈 버전 갱신 · `[Plan] EN/UK/CA/SG` 미확정 7건 · FAQ 미결정 6건 · IA Screen ID 어휘 승인 5건 · 미점검 축 조합 접근 수단 · `Mini - 일본` NEXT Bay 배너 보상 단위 · 가이드 이력 탭 접이식 · `mini_luckyball_invite_banner_desc` 서비스 간 값 분기 · 용어집 보완 2건(`확인` 다의어·`・` 구분자)
  - **이번 세션 신규 발견분도 함께 종결** — 서비스 간 값 분기 3건(`unifi_promotion_jpyc_desc`는 **두 서비스의 문구 자체가 다름** · `_jpyc_title`은 Unifi 쪽만 zh `中獎率`·th 영어 혼입·**리터럴 `\n`** · `_jpyc_info_oa_title`은 Unifi 쪽 zh `關注`=deprecated 금지어) · `unifi_promotion_jpyc_bonus_desc` DP ko 맞춤법(`지급되요`·`유지시`)
- **감사 리포트의 「위키 결함 확정 1건」은 오판이었다**(2026-08-10) — `Reward Confirm Bottom Sheet` 요약문 `회원가입 완료 후`는 **정책 1번과 모순이 아니다**. 정책 1번은 **진입 경로·전제 조건**을, 요약문은 **노출 시점**을 말하며 화면 XLT(`가입이 완료됐어요!`·`당첨된 {0} JPYC는 2주 이내로 지급됩니다.`)가 요약문을 지지한다. **「상충」으로 재보고 금지.**
- **K-Pick 노출 조건은 ①As-is → ②To-be 이력으로 기록한다**(2026-08-06 세션 #13, 사용자 지시) — `K-Pick 노출 정책`(`4479295382`)이 Guide Kim 통합 시점 정책보다 **나중에 확정된 최신 정본**이다. mini 마스터 버전 2 블록에서 **①**(JP IP만 노출 + KR IP 일부 버튼 비노출·국가 서비스 불가 안내) → **②**(JP IP **또는** KR IP + 언어 설정 non-KR로 확장)로 배치하고 **②가 ①을 대체**했음을 명시했다. 블록 말미에 떨어져 있던 KR IP 문장은 ①로 옮겼다. **「정책 충돌」로 재보고하지 않는다.**
- **`K-Pick 노출 정책` 문서의 UIT 표 3컬럼은 그대로 둔다**(2026-08-06) — `check_wiki_storage.py pre`가 4컬럼 위반으로 잡지만 **편집 전부터 있던 기존 상태**이고(번역 없는 화면이라 XLT 컬럼 부재), `md/wiki.md`의 "기존 페이지는 현행 컬럼 구조를 따른다(소급 변경 금지)"가 우선한다. 이 페이지를 편집할 때는 **exit 0 대신 「편집 전/후 위반 목록 동일 = 신규 위반 0건」을 통과 근거**로 삼는다.
- **Button Case 어노테이션 ⓝ⑤가 `unifi` 로고를 가리는 것은 문제없다**(2026-08-06 사용자 확정) — 로고가 TEXT 노드가 아니라 `collect_frames.py`의 텍스트-회피가 감지하지 못하는 케이스다. **도구 개선·재렌더를 재제안하지 않는다.**
- **당첨금 지급 시점은 「2주 이내」다**(2026-08-06) — 2026-08-05의 「즉시 지급(최대 5분 내)」 확정을 **번복**한 것이며 `info_signup_desc`·`bottomsheet_signup_text2`에 반영 완료. 「즉시 지급」으로 되돌리자는 제안 금지.
- **OA 2건은 병행 운영**(2026-08-06) — `(OA)mission complete`=미션 완료 시 / `(OA)Reward Confirm`=지급 시점. **대체 아님**. 기존 OA 삭제·통합 제안 금지.
- **`(Promotion) Unifi Member`·`(Popup) Unifi member` 화면은 삭제됐다**(2026-08-06, 사용자) — 위키 행·첨부 2건 정리 완료. **복원·재추가 제안 금지.** 「이미 회원」 문구는 `unifi_promotion_info_already_member` **신규 키로 일원화**(기존 팝업 키 재사용 아님 — 사용자 선택). ⚠️ 다만 `mini_luckyball_already_member`가 **시스템에는 아직 남아 있다**(2026-08-07 API 실측). 「이미 회원」 문구가 `unifi_promotion_info_already_member`·`mini_luckyball_already_member`·`mini_luckyball_already_member_go_home` **3키로 중복**된 상태이며, 정리 여부는 **사용자 판단으로 미결 종결**했다(FE 참조 확인이 선행돼야 하고 이 문서에서 추적하지 않는다).
- **OA `altText`에 변수를 쓸 수 없다**(2026-08-05) — 금액 삽입안은 **종결**. 이후 OA 작업에서 altText 변수화를 **재제안하지 않는다**. (부록 7의 "봉투 텍스트에서도 치환 가능" 서술은 무효)
- **OA ⑤ `확인하기`는 유입 추적하지 않는다**(2026-08-05) — utm·referral_code 추가 제안 금지.
- **`unifi_promotion_jpyc_info_oa_desc` 정본**(2026-08-06 사용자 최종) — `다양한 이벤트 소식을 가장 빠르게 받아볼 수 있는 Unifi의 LINE 공식 계정 친구를 유지해주세요.` 5개 언어 반영 완료. **이전 ⓐ안(`이후 연계해 참여할 수 있는…`)으로 되돌리자는 제안 금지.**
- **mini 키 미부여 4건 · `최대 60만엔`은 조치하지 않는다**(2026-08-05) — 기존 키가 있고 **XLT 키의 문구가 정답**. 확인 목록 재생성 금지.
- **`UF_home_skyflag_title` ko는 유보**(2026-08-05) — en·ja만 목적어를 보충하고 **ko 원문(`미션하고`·목적어 누락)은 시스템 등록값이라 건드리지 않는다**. 시스템 갱신 시 함께 처리.
- **`{{amount}}` 소급 치환 금지**(2026-08-05) — `oa/` 기존 캠페인 파일과 위키 History 이력 문장은 **그대로 둔다**. 신규·수정 작업부터 `{{total_amount}}`.
- **`XLT 확인 필요 목록` 섹션 삭제**(2026-08-05) — 26행 전부가 시스템 등록/갱신 항목이라 삭제 결정. **재생성 금지.** 미결 결정 3건은 게이트 리포트 부록 3 보존.
- **OA·화면 문구 정본**(2026-08-06 사용자 최종) — `bottomsheet_signup_text3`와 OA 2화면 모두 `…친구에게도 JPYC 선물하면 JPYC 럭키볼 최대 20개를 더 받을 수 있어요.`(「총 2개」→「최대 20개」로 조건·수치 변경). 5개 언어 + OA Flex JSON 10개·zip 2종 반영 완료. **수치는 고정 문구 — 변수화하지 않는다.**
- **OA `altText`는 bubble JSON에 넣지 않는다**(2026-08-05) — 메시지 봉투 값이고 Flex Simulator가 bubble만 받으므로 봉투로 감싸면 검증이 깨진다.
- **`mini_luckyball_invitee_mission`** 재추가 금지(2026-07-27).
- **`선물` 용어집 등재 보류**(2026-07-30) — `선물하기`(v3.6)로 커버 + zh·th 명사/동사 분기.
- **가이드 용어집 검증 버튼** 현행 유지(2026-07-30) — CORS로 자동 대조 불가, 수동 폴백 동작.
- **에이전트 분업 범위** — 번역·게이트·위키 PUT·엑셀은 **직렬 고정**. 읽기 전용만 팬아웃(손익분기 8~10프레임).

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|------|------|------|
| 2026-08-07 | **의미 동등한 ko 교정에서는 다른 언어를 건드리지 않는다** — 교정 후 ko와 현행 번역을 대조해 **의미가 실제로 바뀐 키만** 재번역 | terms 8키 중 7키는 어순·띄어쓰기·조사·금지어 대체라 의미가 같았다. 4개 언어를 함께 흔들면 이미 검증된 번역이 근거 없이 바뀐다. `early_end`만 「준비된…모두」·「내용이 변경」이 추가돼 5개 언어 재번역했다. 커밋 `eea1fb9` |
| 2026-08-07 | **엑셀 첨부 파일명의 날짜는 올리지 않는다** — `xlt_output_season3_*_20260804.xlsx` 고정, 내용만 갱신 | 본문 `view-file` 매크로가 파일명을 참조하므로 파일명을 바꾸면 링크가 끊긴다. 첨부 API `POST .../attachment/{id}/data`로 같은 파일명에 새 버전을 올린다(ALL v22 · LV v13 · UIT v18) |
| 2026-08-07 | **XLT 등록값은 API 1차 조달** — export 수동 요청 폐지, VPN 장애 시에만 폴백 | 세션 #16이 타당성만 검증하고 멈춘 것을 구현. 같은 PC에서 export 없이 0.5초에 4,092키를 받았고, 사람이 손으로 센 구 표기 수치를 전건 재현했다. 커밋 `847e2de` |
| 2026-08-07 | **서비스별로 각각 대조하고 레지스트리를 병합하지 않는다** | 초기 구현은 "먼저 지정한 쪽 우선"으로 합쳤는데 **시즌3에서 Unifi를 앞에 두자 10키가 「값 상이」로 오보**됐다(실제로는 전부 Dapp Portal 정본과 일치). **지정 순서에 따라 결론이 뒤집혔다.** 공통 335키 중 151키(45%)가 값이 갈리는 구조라 병합 자체가 틀린 모델이다 |
| 2026-08-07 | **검증 로직을 새로 만들지 않고 `validate_translation.py`를 재사용** — 레지스트리를 엑셀 규격 DataFrame으로 바꿔 넣는다 | 등급 의미가 갈리면 과거 게이트 리포트와 A/B 비교가 불가능해진다. 검토 보고서가 권고한 `--registry` 주석 옵션보다 단순하고, 「등록값 유래 P1 강등 기각」도 자동으로 지켜진다 |
| 2026-08-07 | **전수 스윕의 자동 등급은 그대로 읽지 않는다** — 전건 사람 판정 후 실제 위반만 보고 | 3서비스 4,092키에서 P1 4,173건이 나오는데 그중 3,954건이 `용어 불일치` 오탐이다. 등급이 아니라 **용어집 매핑이 좁다는 신호**로 읽어야 한다. 오탐 4,975건은 근거를 명시해 분리했다 |
| 2026-08-06 | **마스터 Summary는 Release History를 따라간다** — 행이 삽입되면 Summary 블록 번호를 밀어 1:1을 복구한다 | 사용자가 Release History에 행을 삽입하는 것이 정상 흐름이고, Summary가 뒤늦게 따라간다. Lucky Ball은 **7행 삽입으로 8행↔7블록**이 됐고(세션 #10에서도 같은 패턴) 시즌3 블록을 버전 8로 옮겨 복구했다 |
| 2026-08-06 | **이미 정합인 마스터에 "업데이트" 요청이 오면 직전 버전과 storage diff를 먼저 본다** | mini는 3↔3 정합이라 반영 대상이 안 보였는데, `?status=historical&version=6`과 diff해 **사용자가 방금 추가한 `K-Pick 노출 정책` 링크**를 특정했다. 추측하거나 되묻기보다 빠르고 정확하다 |
| 2026-08-06 | **구·신 정책이 갈리면 같은 블록에 ①As-is → ②To-be 이력으로 배치**하고 최신 쪽을 명시 | 최신 정책만 남기면 왜 바뀌었는지가 사라지고, 구 서술을 그대로 두면 한 블록에서 상반돼 보인다(mini의 KR IP 차단 vs K-Pick 확장). 사용자 지시로 이력 형태 채택 |
| 2026-08-05 | **용어집에 `oa_variables` 영역 신설**(스키마 확장 — 4번째 최상위 키) · `{{amount}}`→**`{{total_amount}}`** | OA는 XLT 키가 없어 **변수명이 정본으로 남지 않았고** 캠페인마다 이름을 다시 정하는 왕복이 반복됐다. 등재하면 문의 없이 재사용. `validate_translation.py`가 `terminology`/`exceptions`만 읽어 신규 키는 무해(실측). 커밋 `21fc1fa` |
| 2026-08-05 | **용어집 등재 판정은 추정이 아니라 A/B 실측으로** | 2차 검토가 `캠페인` 등재를 "+3 신규 P1"로 추정해 보류를 권고했으나 **실측 0건**. 검증기는 **ko에 그 용어가 든 행만** 검사한다. 커밋 `fe67b80` |
| 2026-08-05 | **위키 storage 정규식 파싱은 중첩표·리스트 마크업을 고려한다** | `<tr>(.*?)</tr>` 비탐욕 매칭이 XLT 셀의 중첩표에서 끊겨 **16프레임 XLT 컬럼이 전부 공란**으로 보였고, `<ol><li>` 자동 번호 소실로 "번호 누락" 오탐. **에이전트 3건이 동일 오판** |
| 2026-08-05 | **문자열 치환은 구간을 한정한다 — History 이력은 보존** | th 음차·`{{amount}}`를 전역 치환했으면 **History의 과거 변경 기록을 훼손**할 뻔했다. 번역표/OA 영역 오프셋으로 한정하고 assert로 강제 |
| 2026-08-05 | **OA MSG 섹션은 UIT·LV와 동급 `h3`** / **mini는 LV 안 첫 `h4`** | OA는 FE 팀이 아니라 LINE OA 콘솔·Messaging API 발송이라 팀 프리픽스·`{0}` 치환자 규칙이 적용되지 않는다(LV 하위에 두면 규칙이 잘못 상속). mini는 LV 담당 surface라 LV 안. 커밋 `c20b332` |
| 2026-08-05 | **`bottomsheet_signup_text2` 변수 1개(금액만)** — 시점은 고정 문구 | 사용자 확정. 시스템은 `{0}`·`{1}` 2개였으나 시즌3는 즉시 지급(최대 5분 내)이라 시점 변수가 불필요 |
| 2026-08-05 | **`UF_floating_jpyc_banner_desc` 날짜 삭제** | 시즌3는 즉시 지급인데 이 배너만 `7월 15일에`(시즌2 잔재)여서 정책이 상충. 사용자가 위키에서 직접 삭제 |
| 2026-08-05 | **Figma와 XLT 시스템이 다르면 시스템 export 우선**(재확인) | `unifi_promotion_unifi_text8d`를 Figma 60만엔으로 반영했다가 시스템 3만엔으로 되돌림. mini 화면도 3만엔이라 Figma가 구값 |
| 2026-08-05 | **접두 변형 키를 먼저 조회한다** | `unifi_promotion_unifi_text8a`를 문구 상이로 오진. 실제로는 `jpyc_unifi_text8a`가 별도 등록돼 있었고 Screen 매핑이 틀렸던 것 |
| 2026-08-04 | **기존 키 값의 정본 = XLT 시스템 export** | FE가 실제 사용하는 값. 기획 문서가 구버전인 항목 7건 확인. 커밋 `b656873` |
| 2026-08-04 | **`채널 팔로우`·`팔로우` → `공식 계정 친구 추가`**(5개 언어 정본) · 용어집 등재는 보류 | 사용자 확정. 실측 일치율 32%로 landpress §4-3 미충족 → 규칙 문서로 강제 후 시스템 정리·재측정. 커밋 `4b186d6` |
| 2026-08-04 | **Policy 섹션은 `구분 \| 설명` 2열 표 1개** | 사용자가 직접 쓴 럭키볼 캠페인 Policy가 정본 포맷 |
| 2026-07-30 | **위키 규격 3건 확정** — Screen 표 **4컬럼 고정**(`Screen ID/Screen/Description/XLT`) · 첨부 참조는 `<ri:attachment ri:filename>` **단독**(`<ri:page>` 금지) · 생성 규격(Related Docs 카테고리 행 + Flow Figma 임베드 `width=1000`) | 전부 `md/wiki.md` 규칙 + `check_wiki_storage.py`로 강제됨. `ri:page`는 space-key/제목이 한 글자만 틀려도 「알 수 없는 첨부파일」로 깨진다 |
| 2026-07-24~30 | **초기 규칙 7건 확정** — History 같은 날 1행 병합 · **같은 문구=같은 키 재사용**(공유 키는 화면 제거 시에도 삭제 금지) · OA는 **XLT 키 미부여**(번역만·`{{이름}}`·언어별 Flex JSON) · 용어집 v3.0 대개편(`거래내역` 붙여쓰기) · IA 정본 `md/IA.md` 신설 + 주간 점검 · **가이드 zip 최신본은 파일명·mtime으로 판별하지 않는다**(푸터 버전+임베드+mtime 3값) · 에이전트는 「넓게 읽고 취합」만 위임(쓰기·번역·게이트는 직렬) | 전부 `CLAUDE.md`·`md/wiki.md`·`md/OA.md`·`md/IA.md`·`md/landpress.md`에 규칙으로 반영 완료. 연혁 참고용 |
| 2026-07-27 | **화면 정의에 없는 orphan 키는 번역표에서 삭제** | 화면 정의(Screen 상세표)가 키의 소스 |

---

## 최근 세션 기록

### 2026-08-10 — 세션 #20 후속: 가이드 소스 git 관리 신설 + v25 병합본 (**커밋 `7d1bd3d`·`3075eb6`**)

- **가이드 소스를 `guide/`에서 git 관리** — 사용자 질문(「어떤 버전이 최신인지 git으로 관리해야 하나?」)에서 출발. `guide/`는 **2026-07-27 v1 시절 커밋 후 23개 버전 방치**돼 있었다(추적만 하고 절차가 없으면 낡는다는 실증). v24로 교체해 베이스라인 커밋.
- **v25 병합본 생성 완료** — 세션 #21이 권장한 그 병합본. v24(IA #4 로그인 보강·디버그 정책) 베이스에 커밋된 `reports/glossary_v4.7_for_cms.json`으로 15곳 재적용. 검증(임베드 JSON 동치·용어 표 117행·태그 균형·**IA #4 내용 보존**) + 브라우저 육안 확인. `dropweb/web3_planning_v25.zip` 채팅 전달.
- **절차 개정** — `md/landpress.md` §5-1 0단계를 **zip 포렌식 → `git pull` 후 `guide/` HEAD**로 전면 교체(쓰기 직전 재확인·게시 태그 `guide-vN` 규약 포함). `glossary-guide-updater` 에이전트 지시문 동일 반영.
- **배운 것**: 산출물이 git 밖에 있으면 병렬 세션의 덮어쓰기가 **무음**이다. git 안으로 들이면 같은 사고가 **push 충돌로 가시화**된다. 그리고 원본 데이터(JSON)가 커밋돼 있었기에 유실본을 15분 만에 재생성했다 — **정본은 항상 재생성 가능한 형태로 커밋해 둘 것.**

### 2026-08-10 — 세션 #21: IA 주간 점검 #4 + **로그인 보강** (3커밋 `5337758`·`13b02c4`·`402dcc1`)

- **완료**
  - **주간 점검 #4** — 프로덕션·Beta·mini Beta 비로그인 17라우트 → 리포트 발행 → 가이드 v22
  - **로그인 보강**(사용자가 프로덕션·Beta 세션 제공) — 13라우트 추가 순회 → 리포트 부록 + 가이드 v23
  - **디버그 패널 미사용 확정**(사용자 결정) → IA.md·리포트·가이드 v24 반영
  - **정본 갱신**: PLUS 이율 7% · 액션 라벨/외부 지갑 연결 프로덕션 반영 · `/interest/usdt` 신설 · mini 출석 체크·비실시간 지급 · 스켈레톤 4조합 매트릭스
- **배운 것**
  - **비로그인 관측으로 세운 가설은 로그인에서 뒤집힌다** — "4주 연속 서비스 이슈"로 보고한 스켈레톤이 실제로는 **프로덕션에서 이미 해소**돼 있었고 **Beta mini만** 남아 있었다. 리포트를 이미 쓴 뒤라 **부록으로 정정**했다. 조건을 다 채우기 전의 판정은 "현재까지의 관측"으로 적어야 한다.
  - **같은 파일명 산출물은 병렬 세션에서 충돌한다** — §5-1 0단계(최신 zip 판별)를 **작업 시작 때만** 하고 30분 뒤 쓰기 직전에 재확인하지 않아 **세션 #20의 v23을 덮어썼다**. 세션 #20은 같은 날 반대 방향에서 이 위험을 피했다(v22 발견 → v23 신규). **판별은 조회 시점이 아니라 쓰기 시점에 유효해야 한다.**
  - **한 번의 점검에서 같은 산출물을 3번 올리면 게시 대상이 헷갈린다** — v22→v23→v24를 각각 "이걸 게시하세요"로 전달했다. 회차 중 추가 발견이 예상되면 **발행을 마지막에 한 번**으로 모으는 편이 낫다.
  - **캐시백률·상품 목록처럼 회전하는 값은 IA에 상수로 적으면 안 된다** — 같은 시각 Web과 mini의 시술 캐시백이 달랐고 K-컬처 상품은 조회마다 바뀌었다. IA.md에 경고를 넣어 "변경 발견"으로 오판하지 않게 했다.

### 2026-08-10 — 세션 #20: 미결 14건 중 4건 처리 후 **전건 종결** + 용어집 v4.7 + 가이드 v23

- **완료**
  - **XLT zh `網址`→`URL` 1키** — 게이트 P0=0 · `check_gate_report.py` exit 0 · `xlt/registry_fix/xlt_fix_Unifi_20260810.xlsx`(업로드 대기)
  - **시즌3 감사 후속 6건 · 잔여 번역 개선 8건 → 변경 0건 종결**(사용자 결정)
  - **용어집 v4.7** — `포이카츠` zh `集點活動` + metadata 수치 정정. **A/B 3개 모집단 실측, 감소만·신규 0**
  - **가이드 v23** — v22(IA #4) 보존하고 신규 생성. §5-1 15곳 갱신 · 검증 19항목 + 브라우저 육안 확인
  - **남은 미결 10건 + 신규 발견 4건 일괄 종결** — 「사용자 결정으로 종결(재작업 금지)」에 기록
- **배운 것**
  - **「미결」이 실제로 미결인지부터 확인해야 한다** — 14건 중 **ⓓ 시즌 2 유의사항·M-10·빈 파라미터 3건이 이미 해결**돼 있었다. 리포트 시점(v41)과 라이브(v81) 사이 40버전 차이를 라이브 재조회로 좁히지 않았으면 해결된 건을 다시 물을 뻔했다.
  - **감사 리포트의 「결함 확정」도 오판일 수 있다** — Reward Confirm 요약문은 정책 1번과 **다른 축**(노출 시점 vs 진입 조건)을 말하고 있었고, **화면 XLT가 요약문을 지지**했다. 원본 데이터로 되짚지 않고 리포트만 믿으면 멀쩡한 문장을 고친다.
  - **등재값이 실사용 0건이면 등재값이 틀린 것이다** — `點數活動`은 3,730키 어디에도 없었다. 「번역이 용어집을 안 지킨다」가 아니라 「용어집이 현실을 모른다」였다.
  - **다른 세션의 산출물을 덮어쓸 뻔했다** — `dropweb/web3_planning_v22.zip`이 작업 도중(10:22) 생겼다. §5-1 0단계(최신 zip 판별)가 이걸 잡았고, **게시 여부를 물어** v22 보존 + v23 신규로 갈랐다. `md/IA.md`의 미커밋 변경도 같은 이유로 커밋에서 제외했다.
  - **리포트 기재도 실측으로 검산해야 한다** — 2026-08-07 리포트의 #9는 「문장 끊김·파트너 누락」이라 했으나 실제로는 **USDT 누락 1건**뿐이었다.

### 2026-08-07 — 세션 #18: 시즌3 유의사항 8키 재번역 + 용어집 v4.6 + mini 프레임 교체 (**1커밋 `eea1fb9` · 위키 v77→v80**)

- **완료**
  - **terms 8키 원문 갱신·재번역** — 3원 대조(Figma·위키·시스템)로 위키 v77이 최신 원문임을 확정 → 원문 교정 9건(`OA 팔로우` 금지어·어순 2·띄어쓰기 3·nbsp 8개) → `early_end` 1키만 5개 언어 재번역
  - **용어집 v4.6** — `사용자` 등재 + `유저`→`사용자` 금지(#14). **사용자 CMS 반영 완료, API 확인**
  - **`(Unifi mini) - Login x` 프레임 교체** — Step 2-1 유사 키 대조로 **신규 키 0건**, `mini_guidekim_daily_mission`만 시스템 등재값 승계로 번역표 추가(57→58키)
  - **가이드 v21 게시**(사용자) — 용어집 v4.6 + 본문 하드코딩 stale 3곳 정정
  - 게이트 P0=0 · P1 67(신규 키 추가에도 증감 0) · `check_gate_report.py` exit 0 · `check_wiki_storage.py` pre/post exit 0 · 첨부 라운드트립 불일치 0건
- **배운 것**
  - **「무엇이 바뀌었나」를 먼저 특정해야 했다** — 사용자가 준 8키는 이미 위키·시스템에 있었고, 실제 변경은 **Screen 표 KR 교체**였다. v76↔v77 diff로 이걸 특정하지 않았으면 전수 재번역으로 갔다.
  - **위키가 Figma도 시스템도 아닌 「제3의 정본」일 수 있다** — v77 문구는 양쪽 어디에도 없는 다듬어진 최종본이었다. 3원 대조 없이 한쪽만 봤으면 최신본을 놓쳤다.
  - **가이드 본문의 하드코딩은 조용히 낡는다** — 히어로 chip이 `112 용어 (v3.9)`로 **v20까지 방치**돼 있었다(라이브는 v4.5). §5-1의 갱신 4곳은 지켰는데 「본문 하드코딩」 항목이 빠져서다. **브라우저 육안 확인이 이걸 잡았다** — DOM 수치 검사만으로는 안 나왔다.
  - **검사 코드의 앵커도 틀릴 수 있다** — `구 표기 금지 목록` 문자열이 업데이트 이력 탭에도 있어 `find()`가 다른 표를 잡아 검증 ④가 오탐 실패했다. 앵커에 버전을 붙여 해결. 세션 #17의 「표는 인덱스가 아니라 헤더 시그니처로」와 같은 부류다.
  - **사용자 반영은 검증해야 한다** — 「업데이트함」을 받고 API로 확인하니 9키 중 1키가 미반영이었다. 자기 보고를 그대로 기록하면 다음 세션이 반영된 줄 알고 진행한다.

### 2026-08-07 — 세션 #17: XLT 검증 체계 신설 + 시스템 전체 감사 + 가이드 v20 (**4커밋, 전부 푸시**)

- **완료 (4커밋 `847e2de`·`a4139eb`·`1ba57bd`·`a48ddfb`)**
  - **`847e2de` 체계 신설** — 스크립트 3종(`fetch_xlt_registry.py`·`verify_xlt_glossary.py`·`compare_wiki_xlt.py`) + `md/xlt-verify.md` 신설 + `md/translate.md` **Step 2-1 유사 키 제안**(ⓐ신규/ⓑ재사용/ⓒ업데이트 3택) + `CLAUDE.md`·`.gitignore`. 사용자 요청 2건(번역 시점 제안 · 정기 용어집 검증)의 구현체.
  - **`a4139eb` 전체 감사** — 3서비스 4,092키. 확정 위반 69 + 치환 제안 49 + 확인 필요 9 + 한국어 14, **오탐 4,975건은 근거 명시해 분리**. 리포트 `reports/audit/xlt_system_glossary_audit_2026-08-07.md`.
  - **`1ba57bd` 교차 오염 매트릭스** — 20조합 주입 실측 → **15개만 탐지**. 사각지대 3종을 `md/check.md` **v3.4**에 영구 기록.
  - **`a48ddfb` 시각화 리포트** — `xlt_audit_site/`(dropweb 규격 단일 페이지 10섹션) + `dropweb/xlt_audit_report_v1.zip`.
  - **가이드 v20** — 「🧭 시스템 가이드」 탭 신설(상황별 라우팅 15행 포함). **사용자 게시 완료**.
- **배운 것**
  - **"어느 서비스를 보는가"가 검증의 전제다** — 세션 #16의 「UIT 치환자 불일치」는 규칙 위반이 아니라 **조회 대상 오류**였다. `UF_` 정본은 `Unifi`이고 거기서 `{{0}}`가 279키다. 공통 335키 중 151키(45%)가 서비스 간에 값이 갈린다는 걸 몰랐기 때문에 #16이 오진했다.
  - **편의를 위한 병합이 결론을 뒤집는다** — 레지스트리를 "먼저 지정한 쪽 우선"으로 합쳤더니 지정 순서에 따라 10키가 오보됐다. 데이터가 실제로 갈려 있으면 **합치지 말고 갈렸다고 보고**하는 것이 맞다.
  - **수치를 손으로 옮기면 틀린다** — 시각화 데이터를 원본에서 재계산하도록 만든 덕에 리포트 오차 3건(13→14 · 13→18 · 336→335)이 드러났다. 손으로 센 숫자는 재계산으로 검산해야 한다.
  - **자동 검사의 "통과"에는 종류가 있다** — 언어 혼입 검사는 문자체계 기반이라 ja↔zh 한자 오염을 **원리적으로** 못 잡는다. `P0=0`이 "문제 없음"이 아니라 "이 검사가 볼 수 있는 범위에 문제 없음"임을 매트릭스로 명시했다.
  - **문구 검증은 키 이름을 보지 않는다** — 최우선 P0 3건(nbsp 접두·제어문자·`?`)은 키 이름 전수 점검을 따로 돌려서만 나왔다. 검사 대상에 **식별자 자체**를 넣어야 한다.

## 아카이브 요약

- **2026-08-07 세션 #15**: 럭키볼 뽑았을때 OA 지급 시점 반영(2커밋, HANDOFF 미갱신) — `f2993ca` OA 「2주 이내 지급 예정」 4개 언어(캠페인 위키 `4479306980` v151) · `7077ff3` OA 3화면 「즉시 지급」 각주는 그대로 둔다(사용자 결정 종결).
- **2026-08-06 세션 #14**: 시즌3 금액 정정 + 용어집 v4.5 + 가이드 v19 게시(5커밋 `e338cbf`~`870fc33`, **HANDOFF 부분 갱신 — 커밋 로그로 복원**) — UIT 2키 15만엔→5만엔 · 용어집 **v4.5**(`월렛`→`지갑`) · 시즌3 변경 5키 + OA 2건 재번역(위키 v76) · 가이드 v19 게시로 P0 전건 해소. 「현재 상태」·세션 기록을 갱신하지 않아 세션 #16이 커밋 로그로 복원했다.
- **2026-08-07 세션 #16**: XLT 시스템 API 타당성 검토(1커밋 `07b0d72`, **코드 미작성**) — 크롬 확장 `~/Documents/dec/` 역공학으로 **무인증 읽기 REST API 발견 + 실호출 검증**. 보고서 `reports/research/xlt_api_feasibility_2026-08-07.md`(327줄). **API 명세는 세션 #17이 `md/xlt-verify.md`로 정본화**했고, 이 세션의 「UIT 치환자 불일치」 발견은 #17에서 **조회 대상 오류로 원인 규명**됐다(문서 규칙이 맞았다). 남은 참조 가치는 §5-2 「등록값 유래 P1 강등 기각」 근거 4가지.
- **2026-08-06 세션 #13**: 마스터 페이지 2종 재정합 + K-Pick 노출 조건 이력화(**위키 3페이지, git 커밋 0건**) — Lucky Ball v13(Release History 8행↔Summary 7블록 해소, 버전 7 신설·시즌3를 버전 8로) · mini v9(K-Pick을 **①As-is→②To-be 이력**으로 재정리해 구·신 상반 해소) · K-Pick v10 오타. **교훈: 정합인데 "업데이트" 요청이 오면 직전 버전 diff가 답이다**(mini 3↔3에서 사용자의 직전 편집을 특정) · **전역 금지 가드는 이력 기록과 충돌한다**(`(버전 미정)` 가드가 History 인용문을 잡음 → h3 제목 대상으로 정교화) · **검사기 exit 1이 항상 내 잘못은 아니다**(편집 전 라이브에도 있던 위반 → `pre`로 찍어 신규 위반 0건을 근거로 진행) · **다른 세션의 uncommitted 변경은 커밋하지 않는다**(WIP 보존 규칙을 그대로 적용하면 그 세션 작업을 가로챈다).
- **2026-08-06 세션 #12**: 용어집 v4.4(1커밋 `081c725`, 병렬 세션·HANDOFF 미갱신) — `deprecated_terms` 신규 2종(ja `友達`→`友だち` · ko `공식계정`→`공식 계정`) + **`context` 필드 신설**로 오탐 8건 제거(zh `關注`/`追蹤`·th `ติดตาม`·en `follow`는 같은 셀에 지정 문자열이 있을 때만 판정). 계기는 사용자가 제공한 XLT export 전수 검토 — v4.3 목록 21건 중 8건이 오탐이고 잡아야 할 2종이 누락돼 있었다. 상세 `md/glossary-changelog.md` v4.4.
- **2026-08-05~06 세션 #11**: 비실시간 추가 개선 위키(`4540065229`) + 시즌3 Promotion page(재사용 6키·신규 5키) + **용어집 v4.2·v4.3**(`deprecated_terms` 영역 신설 9종)(4커밋 `ff14c25`) — 마스터 Lucky Ball v9→v10 정합, 가이드 v14~v18, XLT export 1,596키 전체 검토로 **구 표기 17키 실측**. **교훈: 미추적 파일을 같은 경로에 Write해 직전 세션 기록을 복구 불가로 잃었다**(세션 시작 `git status`의 `??` 확인 필수) · **A/B 실측이 판단을 세 번 뒤집었다**(등재 효과 0→-4, 등재안 +36 기각, 오탐 8→0) — 추정으로 권장 금지 · 위키 표 렌더 ≠ 원본(붙어 보이는 문구가 개행일 수 있다).
- **2026-08-06 세션 #10**: 시즌3 `Updated Fram` 6프레임 지급 시점 「즉시」→「2주 이내」(5커밋 `b6b5f93`) — 위키 v47→v56, 신규 키 1·orphan 삭제 1, **신규 OA `(OA)mission complete`**(Flex 5개 언어+zip), OA `ACTION_URL_1` **2회 갱신**, 어노테이션 4건 재렌더. **교훈: 세션이 병렬로 돈다**(`git fetch`만 하고 pull을 미루면 구버전 도구로 산출물을 만든다) · **게이트 수치는 도구 버전에 매인다**(리포트에 검증기 커밋 해시 명기) · **사용자 편집이 삭제일 수도 있다**(PUT 후 `본문 참조 = 첨부` 대조 습관화) · **`overlaps=없음`이 가림 없음을 뜻하지 않는다**(TEXT 노드만 회피 — 육안 검증이 최종 관문).
- **2026-08-05~06 세션 #9**: 비실시간 지급 위키 OA 3화면 + 스크립트 오탐 개선(7커밋 `8827957`) — **Flex JSON 15개 갱신 누락 복구**·첨부 파일명 복구 → **`md/OA.md` §3-1 「OA 문구 변경 시 위키표·JSON·zip 3곳 동시 갱신」 수칙 신설**. `validate_translation.py` **용어집 최장일치**(P1 232→198) · `collect_frames.py` 핀 글자 회피.
- **2026-08-05 세션 #8**: 시즌3 미결 8건 종결 + 전 16프레임 정책 감사 + **용어집 v4.1**(`oa_variables` 신설, 113→114)(4커밋 `fe67b80`) — 위키 v41→v47, th 럭키볼 5키 통일·ja 소진/종료 어휘 분리·**OA `{{amount}}`→`{{total_amount}}`**(위키 11곳+Flex 5) · `md/wiki.md` 「대상 프레임 지정 방식」 신설 · 가이드 v13. **교훈: 에이전트 결과를 실측으로 걷어낼 것**(감사 3건이 동일 오판, 원인은 파서의 중첩표 처리) · **전역 치환은 History 이력을 훼손**하므로 구간 한정 + assert · heredoc에 `\xa0`를 리터럴로 넣으면 nbsp 151건 오탐.
- **2026-08-05 세션 #7**: 시즌3 화면 전수 대조 + **mini·OA MSG 영역 신설** + **마스터 4종 Summary 최초 작성** + 가이드 v11(3커밋 `0d90f48`) — 위키 v16→v37(사용자 편집 8회를 매 PUT 직전 rebase로 보존), 10키 49셀, P0급 기존 결함 2건 수동 발견(ja `{0}}`·th 매달린 날짜), 번역표 43→47키·엑셀 3종(`LV ∪ UIT == ALL` 검증), `XLT 확인 필요 목록` 삭제. **교훈: 위키 URL 표기가 5~6가지로 섞인다**(`&amp;` 미해제 시 LINE이 URL 거부) · **변수화가 조사 결함을 만든다**(`{{wallet_address}}로` → ` 주소로`로 명사 삽입).
- **2026-08-04 세션 #6**: 시즌3 위키 신규 구축(v16) + **XLT 시스템 export를 값 정본으로 전환** + 저장소 정리(9커밋) — 럭키볼 캠페인 v143, 규칙 3건(Policy 포맷·팔로우 표기·게이트 리포트 위치), 핸드오프 스킬 단일화, 루트 73→6·45M→28M. **교훈: PUT 직전 라이브 rebase 필수**(세션 중에도 사용자 편집이 올라온다) · **엑셀은 반드시 `export_to_xlt.create_xlt_excel`로만**(pandas 직접 생성이 `plurals` 포맷을 깨 업로드 실패).

- **2026-07-24~30 세션 #1~#3 (기반 구축)**: 럭키볼 캠페인 위키 대량 구축(v1→v147) · `md/OA.md`·`md/IA.md` 정본 신설 · 용어집 v2.6→**v4.0**(113 terms) · **규칙 4건**(Screen 표 4컬럼·첨부 `ri:page` 금지·위키 생성 규격·zip 최신 판별) · **도구 2종**(`check_wiki_storage.py`·`collect_frames.py`) · 에이전트 4종 · 주간 IA 점검 도입 · 가이드 v1→v7. **규칙·도구는 전부 `md/`·`scripts/`에 반영 완료**라 이 요약은 연혁 참고용이다.
- **2026-07-29 · 08-03**: Mission and Reward FAQ 신설(`4368569133` v38, Q&A 10건 5개 언어 — **미결정 6건은 「다음 할 일」 P2로 이월**) · IA 주간 점검 #3(`md/IA.md` §0-0-1 화면 변형 3축 신설, 가이드 v10). **교훈: 비로그인 렌더는 로그인 상태의 근거가 아니다.**

---

## 컨텍스트 노트

- **파이프라인**: [1] `md/translate.md` → [2] `md/prototype.md` → [3] `md/wiki.md`. 최근은 단일 프레임/코멘트 선별 + 위키 Mode B, 위키 신규 생성, **마스터 페이지 Summary 취합** 중심.
- **엑셀 생성은 반드시 `scripts/export_to_xlt.create_xlt_excel`로만** — pandas로 직접 만들면 `plurals` 고정 포맷(A1:G2·`one/other`·`Unnamed: 2`)이 깨져 **XLT System 업로드가 실패**한다(2026-08-04 실측).
- **게이트(필수)**: 한국어 원문 교정 → `validate_translation.py <엑셀> scripts/glossary.json`(P0=0) → `md/check.md` 3단계 **전수** 수동 → `reports/gate/` 리포트 → `check_gate_report.py` **exit 0**. P1/P2는 이 데이터에서 **전건 오탐**(용어집 협소 매핑·부분문자열·조사 결합·마침표 정책) — 판정 유지.
- **위키 편집(필수 절차)**: 라이브 재조회 → 균형 `<tr>` surgical 교체 → **버전 가드**(PUT 직전 재확인) → PUT → `check_wiki_storage.py` **pre/post exit 0**. 첨부 갱신은 `POST .../child/attachment/{id}/data`(**같은 파일명 유지 → 본문 링크 그대로 최신본**). History는 **같은 날 1행 병합**.
- **마스터 페이지 정합 규칙**: `Release History` **데이터 행 수(`<td class="numberingColumn">` 개수) = Summary `<h3>버전 N` 블록 수**. 검증 시 앵커는 `<h1>Release History</h1>`를 쓸 것(History 행 본문에 "Release History"가 들어가 오집계된 실측 있음). 릴리즈 버전 미확정이면 제목에 `[PL] Unifi (버전 미정)`.
  - **행이 삽입되면 Summary 번호를 밀어 복구**한다(사용자가 행을 넣는 것이 정상 흐름 — 실측 2회).
  - **이미 정합인데 "업데이트" 요청을 받으면** 라이브와 **직전 버전(`?status=historical&version=N-1`)을 diff**해 사용자의 마지막 편집을 특정한다(mini 실측).
  - 참조 문서가 **구·신으로 갈리면 같은 블록에 ①As-is → ②To-be 이력**으로 배치하고 최신 쪽을 명시한다.
- **핵심 산출물**(2026-08-07 기준): 시즌3 위키 **v80**·번역표 **58키** · 럭키볼 캠페인 75키 · `xlt/xlt_output_season3_{ALL,LV,UIT}_20260804.xlsx`(**ALL v22·LV v13·UIT v18** — 파일명 날짜 고정, 내용만 갱신) · `oa/flex_OA_*`(zip `Reward_Confirm` v5·`mission_complete` v4) · `reports/gate/*.md` · `reports/audit/xlt_system_glossary_audit_2026-08-07.md`(XLT 전수 감사 정본) · `reports/glossary_v4.6_for_cms.json`(라이브 반영 완료) · `reports/xlt/`·`reports/ia/`(git 미추적) · `xlt_audit_site/` · **가이드 라이브 v21**
- **OA 변수**: 용어집 `oa_variables`가 정본(`md/OA.md` §2-1) — **`{{total_amount}}`**(당첨금 총액) · `{{wallet_address}}`. 등재된 변수는 **사용자 문의 없이 재사용**하고, 새 의미만 이름을 묻는다. **altText에는 변수 사용 불가.**
- **화면 이미지는 로컬에 보관하지 않는다** — 어노테이션 이미지는 **위키 첨부가 정본**. 재작업은 `scripts/collect_frames.py`(`assets/`는 실행 시 자동 생성, git 미추적).
- **IA 점검 루틴**: 매주 월 10:00 자동 → 직전 리포트 이월 확인 → 프로덕션 비로그인 → Beta·mini Beta → 로그인(가능할 때) → `md/IA.md` 갱신 → 리포트 발행 → **가이드 zip +1** → 커밋·푸시(`dropweb/`은 `git add` 금지). **상태 변경 액션 절대 미실행, 직접 로그인 금지.**
- **스크립트 13종**(`scripts/`): fetch_glossary · **fetch_xlt_registry** · **verify_xlt_glossary** · **compare_wiki_xlt**(⛔ 레지스트리 병합 금지) · fetch_comments(**`fetch_threads`+`collect_node_boxes` 좌표 정규화 필수**) · validate_translation(**모든 검증의 단일 출처**) · check_gate_report · check_wiki_storage · collect_frames(**Pillow 필요**) · export_to_xlt · patch_translation · build_prototype_data · test_validation(**scripts 수정 후 필수 실행**)
- **XLT 등록값 API**(세션 #17에 스크립트화 완료 — 정본 `md/xlt-verify.md`): `fetch_xlt_registry.py --list-targets`로 유효 조합 확인 → `--service <S>`로 조달. ⛔ **서비스 3개의 키스페이스가 다르다**(`Dapp Portal` 1,596 / `Unifi` 2,130 / `Kaia Wallet` 366, 공통 335 중 **151키 값 상이**) — `UF_` 정본은 `Unifi`, `mini_`·`unifi_promotion_` 정본은 `Dapp Portal`. **대조 전 타겟(서비스·디바이스·버전) 사용자 확정 필수.** 목록 API는 없어 후보 프로브로 판별(무효 조합도 200 + `result: []`). **쓰기 API 없음** — 업로드는 사용자.
- **검증기 사각지대**(`md/check.md` v3.4): 언어 혼입 검사는 문자체계 기반이라 **20조합 중 15개만** 잡는다. ❌ **ja↔zh 한자 상호 오염** · ❌ **영어가 ko/ja/th/zh 칸에** · ❌ ko 칸의 부분 영어. `P0=0`은 "이 검사가 볼 수 있는 범위에 문제 없음"이다. 보완 스캔 코드는 `reports/audit/xlt_system_glossary_audit_2026-08-07.md` §8-1.
- **캐시 금지**: Figma 노드·코멘트, 위키 원문, 용어집, **XLT 등록값** 모두 **매 작업 원본 재조회**. 같은 run 안의 파이프라인 핸드오프만 예외. 📌 세션 #17 감사 도중 **`UF_floating_jpyc_banner_title`이 실제로 삭제**돼 1시간 만에 키 수가 2,131→2,130으로 바뀌었다.
- **어노테이션**: 이미지 ⓝ = Description 통합 번호 = XLT No 1:1. **정책=빨강 / xlt=파랑**, 매칭 텍스트 좌측 10pt, 겹침 0 검증, **렌더 후 육안 확인**. 코멘트가 없는 프레임은 좌표 기반으로 직접 핀을 렌더해야 한다(OA 프레임 실측).
- **담당 FE 팀**: LV(`unifi_promotion_`·`mini_`·`{0}`) / UIT(`UF_`·`{{0}}`) / **OA는 팀이 아님**(키 미부여·`{{이름}}`). 위키에 UIT/LV 구분이 없으면 사용자에게 질문.
- **프레임 지정**: 링크 없이 **페이지/섹션 주소 + 프레임 이름**으로도 특정 가능(직속 자식 재조회 후 이름 필터). 단 **동명 프레임 주의**(`(OA)Reward Confirm` 2개 등) — 후보가 둘 이상이면 사용자에게 확인.
- **Screen ID**: `md/IA.md` 어휘로 `주기능_부기능_세부기능_01`(소문자), **매핑 표 사용자 승인 후에만** 부여. 기존 프레임명 기반 페이지는 소급 금지.
- **용어집**: API 읽기 전용, 갱신은 전체 JSON → 사용자가 CMS 붙여넣기 + **가이드 동반 갱신 필수**(`md/landpress.md` §5-1 — **소스는 git 추적 `guide/`, 0단계 = `git pull` 후 `guide/` HEAD**).
- **드랍웹 제약**: 정적 호스팅이라 서버 역할 불가. 용어집 API가 CORS 헤더를 안 줘 자동 대조는 막히고 수동 폴백 — **사용자가 현행 유지 결정**.

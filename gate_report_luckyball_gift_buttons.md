# 번역 품질 게이트 리포트 — 럭키볼 초대자 화면 하단 선물 버튼 2건

- **대상**: Figma `63411-25034` (초대자 화면 전체) · 위키 pageId `4479306980` (작업 시점 version 106)
- **작업일**: 2026-07-28
- **담당 FE 팀**: **LV** (위키 Screen 섹션 `LV > 초대자 프로모션 페이지` 소속 → 프리픽스 `mini_luckyball_`, 치환자 `{0}`) — 위키에 UIT/LV 구분이 있어 사용자 확인 없이 해당 영역 규칙 적용
- **변경 범위**: 하단 버튼 2개
  - **변경(문구 개편)** `mini_luckyball_invite_me_too` — `친구한테 럭키볼 선물하기` → `LINE으로 친구한테 럭키볼 선물하기` (5개 언어 재번역). 사용자 결정(2026-07-28): 공유 키 문구를 변경하고 신규 키를 만들지 않음
  - **신규** `mini_luckyball_gift_copy_link` — `친구한테 럭키볼 선물하기 링크 복사`
- **검증 대상 파일**: `xlt/xlt_output_LV_20260727.xlsx` (properties 73키 — 기존 72키 + 신규 1키)

---

## (1a) 번역 전 — 한국어 원문 교정

| Figma 원문 | 교정 후(XLT Key 값) | 사유 | 조치 |
|---|---|---|---|
| `친구한테 럭키볼 선물하기 링크복사` | `친구한테 럭키볼 선물하기 링크 복사` | `링크복사` 붙여쓰기 → `링크 복사` 띄어쓰기(명사구, 용어집 `복사` 단독 등재) | **alias 기록**: `{Figma: 친구한테 럭키볼 선물하기 링크복사 → XLT: 친구한테 럭키볼 선물하기 링크 복사}` · 디자이너에게 Figma 원본 수정 요청 필요 |
| `LINE으로 친구한테 럭키볼 선물하기` | 동일(교정 없음) | 조사 `LINE으로` 정상(‘라인’ 종성 ㄴ → `으로`), 띄어쓰기 정상 | 그대로 사용 |

- 참고: `친구한테`는 문어체 기준 `친구에게`가 표준이나, 이 캠페인의 기존 문구(`친구한테 럭키볼 선물하기`)와 **표기 일관성 유지**를 위해 원문 그대로 둔다. 통일 여부는 아래 (d-2) 제안 참조.

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py` 실제 실행

```
파일: xlt/xlt_output_LV_20260727.xlsx (73키)
🔴 P0 (Critical) : 0건
🟡 P1 (Medium)   : 111건  (전부 [용어 불일치])
🟢 P2 (Low)      : 26건   (전부 [마침표 스타일])
```

**직전 첨부본(변경 전 72키) 기준선**: P0 `0` / P1 `110` / P2 `26`
→ 이번 변경으로 늘어난 것은 **P1 1건뿐**(신규 키의 `복사` 매칭, 아래 (c)에서 오탐 판정). **P0 = 0 — 통과 기준 충족.**

---

## (b) 수동 3단계 체크표 (md/check.md) — 신규·변경 2키 전 컬럼

> 부분 작업이므로 체크표는 **신규·변경 키**를 대상으로 하되, 자동 검증기는 **엑셀 전체 행(73키) 전수**로 돌렸고, 아래 (c)에서 **전체 파일**의 P1/P2 전건에 처리 판정을 남긴다. (전수 수동 재검토는 2026-07-27 세션에서 25034 전 문구에 대해 완료된 상태이며, 이번 변경분 외 원문 변경 없음.)

### 1단계: 한국어 맞춤법·띄어쓰기

| 키 | ko_KR | 판정 | 근거 |
|---|---|---|---|
| `invite_me_too` | LINE으로 친구한테 럭키볼 선물하기 | ✅ 정확 | 조사(`으로`)·띄어쓰기 정상, 붙여쓰기 오류 없음, `\xa0` 없음 |
| `gift_copy_link` | 친구한테 럭키볼 선물하기 링크 복사 | ✅ 정확 | `링크복사`→`링크 복사` 교정 반영(위 1a), 문말 마침표 없음(버튼 라벨 규칙 일치) |

### 2단계: 용어집 위반 (라이브 v3.5 · 107 terms, `scripts/fetch_glossary.py`로 새로 조회)

| 용어 | 등재값 | 적용 결과 | 판정 |
|---|---|---|---|
| 럭키볼 | Lucky Ball / ラッキーボール / ลูกบอลนำโชค / 幸運球 | 4개 언어 모두 등재값 사용 | ✅ |
| 복사 | copy / コピー / คัดลอก / 複製 | `Copy` / `コピー` / `คัดลอก` / `複製` | ✅ |
| LINE | (예외 — 미번역 브랜드) | 5개 언어 모두 `LINE` 원문 유지 | ✅ |
| 링크·선물·친구·초대 | **미등재** | 기존 키(`copy_invite_link`·`link_copied`) 표기와 정렬 | ⚠️ (d-1) 용어집 보완 권장 |

### 3단계: 다른 언어 자연스러움·표기

| 키 | 언어 | 번역 | 판정 | 근거 |
|---|---|---|---|---|
| `invite_me_too` | ja_JP | LINEで友だちにラッキーボールをプレゼントする | ✅ | `友だち` 표기 정본, 조사 잔존 없음, 기존 값의 `〜する` 어미 유지 |
| | en_US | Gift a Lucky Ball to a friend via LINE | ✅ | 기존 문구 + `via LINE`, 관사 정상 |
| | th_TH | มอบลูกบอลนำโชคให้เพื่อนผ่าน LINE | ✅ | `ผ่าน LINE`(via) 정확, 영어 잔존은 브랜드명뿐 |
| | zh_TW | 透過 LINE 送好友幸運球 | ✅ | 번체 정자(`透過`·`幸運球`), 간체·`資産`류 이체자 없음 |
| `gift_copy_link` | ja_JP | 友だちにラッキーボールをプレゼントするリンクをコピー | ✅ | 기존 `copy_invite_link`의 `リンクをコピー` 패턴과 일치 |
| | en_US | Copy the link to gift a Lucky Ball to a friend | ✅ | 기존 `Copy the invite link…` 패턴과 일치 |
| | th_TH | คัดลอกลิงก์มอบลูกบอลนำโชคให้เพื่อน | ✅ | 기존 `คัดลอกลิงก์…` 패턴과 일치 |
| | zh_TW | 複製送好友幸運球的連結 | ✅ | 기존 `複製…邀請連結` 패턴과 일치, 번체 정자 |

- 컬럼 정렬 검증(check.md 4-1): 두 키 모두 5개 컬럼에 각 언어가 정확히 들어갔음을 셀 단위 확인 — 회전 어긋남·swap 없음.
- 이질 문자체계 검출(검증기 컬럼별 검사): 0건.

---

## (c) 검증기 보고 전건 처리 판정 (P0/P1/P2 — 전체 파일 73키 기준)

| 심각도 | 건수 | 유형 | 처리 판정 |
|---|---|---|---|
| P0 | 0 | — | 해당 없음 |
| P1 | 111 | [용어 불일치] — 용어집 단일 매핑값과 실제 번역이 다름 | **전건 오탐(정상 의역)**. 내역: 당첨 25 · 최대 12 · 확인 10 · 완료 10 · 뽑기 9 · 가입 7 · 리워드 지급 5 · 공식 계정 4 · 종료 4 · 미션 3 · **복사 3** · 송금 3 · 변경 3 · 유의사항 2 · 가격 2 · 예치 2 · 플레이 2 · 회원가입 2 · 로그인 1 · 출금 1 · 혜택 1. 검증기가 용어집의 **대표 표기 1개**하고만 문자열 비교하므로, 문맥 활용형(`당첨됐어요`→`当選しました`)·대소문자(`복사`→`Copy`)·버튼 관용역(`확인`→`OK`)을 모두 위반으로 집계한다. |
| ↳ 이번 변경분 | 1 | `gift_copy_link` en_US: `복사`→`copy` 권장 | **오탐** — 실제 값은 문두 대문자 `Copy`. 검증기가 대소문자 구분 비교. 수정 불요. |
| P2 | 26 | [마침표 스타일] 문장형 텍스트 중 소수 스타일(마침표 있음) | **전건 오탐/정책 유지**. 이 캠페인은 **설명문=마침표 있음 / 버튼·타이틀=마침표 없음** 정책을 일관 적용 중이며, 검출된 26건은 모두 설명문이다. 이번 신규·변경 2키는 버튼 라벨이라 마침표 없음 — 정책과 일치(검출 대상 아님). |

**통과 판정: P0 = 0 (자동·수동 모두) → 위키 반영·엑셀 생성 진행 가능.**

---

## (d) 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

### (d-1) 용어집 보완 권장 — Landpress CMS 반영 필요 (`md/landpress.md` 절차)

이번·기존 럭키볼 문구에서 반복 사용되지만 용어집(v3.5)에 **미등재**인 도메인 용어입니다. 등재하면 P1 오탐도 함께 줄어듭니다.

| 한국어 | en_US | ja_JP | zh_TW | th_TH | 근거(사용 키) |
|---|---|---|---|---|---|
| 링크 | link | リンク | 連結 | ลิงก์ | `gift_copy_link`, `copy_invite_link`, `link_copied` |
| 선물하기 | gift | プレゼント | 送禮 | มอบของขวัญ | `invite_me_too`, `gift_copy_link`, `invitee_gift_from` |
| 친구 | friend | 友だち | 好友 | เพื่อน | 15개 이상 키에서 사용, ja는 `友達`/`友だち` 혼용 위험 |
| 초대 | invite | 招待 | 邀請 | คำเชิญ | `invite_banner`, `copy_invite_link` 등 |

> 용어집은 API 읽기 전용이라 Claude가 직접 쓰지 못합니다. 승인하시면 `md/landpress.md` 절차로 **전체 JSON**을 산출해 드리고, CMS 붙여넣기는 사용자가 수행합니다.

### (d-2) 그 밖의 개선·확인 권장

1. **`mini_luckyball_interest_title` 키 중복(값 다름)** — 위키 다국어 표에 같은 키가 **2행** 존재하고 값이 다릅니다.
   - 행 A: `최대 5% 이자 제공 및 자산보전!` / `最大5%の利息提供および資産保全！` / `提供最高 5% 利息及資產保全！`
   - 행 B(엑셀 반영본): `최대 5% 이자 제공 및 자산 보전!` / `最大5%の利息と資産保全！` / `最高 5% 利息與資產保全！`
   - check.md 패턴 **“key_id 중복(값 다름) — 시스템 충돌 위험”**에 해당. 이번 작업 범위 밖이라 **손대지 않았습니다**. 어느 값을 정본으로 할지 알려주시면 한 행으로 정리하겠습니다.
2. **`63411-36558`(Unifi mini - 피초대자 OA 팔로우 당첨금 지급) 문구 불일치** — 이 화면은 공유 키 `invite_me_too`를 쓰는데, 현재 Figma 문구는 **`나도 친구 초대하기`** 로 바뀌어 있어 변경 전(`친구한테 럭키볼 선물하기`)·변경 후(`LINE으로 친구한테 럭키볼 선물하기`) 어느 쪽과도 다릅니다. 이번 지시 범위(25034) 밖이라 해당 행은 **그대로 두었습니다**. 별도 키 분리 또는 디자인 확정이 필요합니다.
3. **`친구한테` vs `친구에게` 표기 정책** — 캠페인 전반이 `친구한테`(구어체)로 통일돼 있습니다. 문어체 표준은 `친구에게`이나, 일괄 변경은 5개 언어 영향이 없고 한국어 톤만 바뀌므로 **정책 결정 사항**으로 남깁니다.
4. **엑셀 파일명** — 첨부 파일명(`xlt_output_LV_20260727.xlsx`)을 유지한 채 내용만 갱신(첨부 v8)했습니다. 본문 링크·`ri:filename` 참조를 깨지 않기 위함이며, 날짜 접미사를 최신화하려면 본문 링크도 함께 바꿔야 합니다.

# 기획자 가이드 업데이트 대기 목록

> **기획자 가이드(`guide/` → `dropweb/web3_planning_v*.zip`)는 건건이 갱신하지 않고 모아서 한 번에 반영한다**(2026-09-15 사용자 결정).
> 푸시할 때마다 「가이드를 업데이트할까요?」를 묻지 않는다 — **이 파일에 등재만 하고 넘어간다.**
> 사용자가 **「가이드 업데이트」를 요청한 시점**에 대기 전건을 제시하고 한 번에 반영한다.

## 왜 모아서 하는가

가이드는 갱신할 때마다 ⓐ 본문 전용 섹션 ⓑ 문서 지도 3곳 ⓒ 업데이트 이력 ⓓ 작업 모드 카드 ⓔ 요청 예시 + 네비 2곳 — **5곳을 모두** 손봐야 하고(`md/landpress.md` §5-1), zip 재생성·헤더 렌더 확인·MCP 게시·태그까지 따라붙는다. 변경 1건마다 이걸 돌리면 비용이 과하고 버전만 잘게 올라간다.

## 운영 절차

| 단계 | 하는 일 | 정본 |
|---|---|---|
| **① 등재** | md/ 규칙·스크립트·기능이 바뀌어 가이드와 어긋나면 **이 파일 표에 한 줄 추가**한다. 사용자에게 갱신 여부를 **묻지 않는다** | 이 파일 |
| **①-2 제안** | 대기가 **10건 이상**이거나 **⛔ 차단 규칙 2건 이상**이거나 **마지막 `guide-v*` 태그로부터 2주 경과**면, 푸시 보고 끝에 **한 줄로** 「가이드 업데이트를 할까요?」 제안한다(차단 질문 아님). ⛔ **보류하면 5건 더 쌓일 때까지 재제안 금지** — 제안 시점을 아래 「제안 이력」에 남긴다 | CLAUDE.md 📣 규칙 2-1 |
| **② 리뷰** | 사용자가 「가이드 업데이트」를 요청하면 **대기 전건을 표로 제시**하고 반영 범위를 확정받는다 | 이 파일 |
| **③ 반영** | 베이스 확정(태그가 정본) → 5곳 갱신 → zip 생성 → **headless Chrome 헤더 렌더 확인** | `md/landpress.md` §5-1 |
| **④ 게시** | **Claude가 직접 재배포**(사용자 승인 후 · REST `PUT /api/sites/{siteId}`) → `get_site`·라이브 `<title>` 대조 후 `git tag guide-vN` | `md/dropweb-guide.md` §8 |
| **⑤ 사후** | 이 파일에서 반영분 삭제 · 반영 태그를 비고에 남긴다 | 이 파일 |

> ⛔ **용어집은 예외가 아니라 같은 흐름이다.** 용어집 보완도 `md/glossary-backlog.md`로 모아서 반영하므로(2026-09-14 결정), **용어집 버전이 오르는 그 작업에서 가이드 zip을 함께 갱신**하면 된다(`md/landpress.md` §5-1 「필수 동반」은 그대로 유효). 용어집 반영이 없는 평상시 푸시에서는 가이드를 건드리지 않는다.

> ⛔ **베이스는 `guide-v*` 태그가 정본이다.** 로컬 `guide/index.html`의 버전 표기가 태그보다 낮으면 낡은 베이스다 — 반영 전 `git tag -l 'guide-*' --sort=-v:refname | head -1`로 확인한다(`md/landpress.md` §5-1 0단계).

## 대기 중

> 기준선: **`guide-v44`**(2026-09-21 · IA 주간 점검 #8 회차 발행 — 대기 건 반영분은 없다). 아래는 그 이후 변경 중 가이드에 미반영된 것이다.
>
> ※ 2026-09-21 판단 — `md/ia-check.md` 3단계(점검 창구: Beta·mini 로그인은 인앱 브라우저)는 **내부 점검 절차**라 기획자가 요청하는 방식·결과물이 달라지지 않는다 → **등재 대상 아님**.

| # | 변경 | 종류 | 가이드 갱신 대상 | 근거 | 등재일 |
|---|---|---|---|---|---|
| 1 | **신규 XLT 키 중복 검사 2단계화** — 레지스트리(`fetch_xlt_registry.py --similar`)에 더해 **미등록 대기 키 대조 `check_pending_keys.py --wiki`**가 신규 키 확정 직전 필수가 됐다 | ⓐ 규칙 변경 + ⓑ 신규 스크립트 | ① 가이드 탭 「검증 게이트」 절에 1줄 ② 시스템 가이드 탭 문서 지도 3곳(스크립트 표에 `check_pending_keys.py`) ③ 업데이트 이력 | `CLAUDE.md` 게이트 1-2 · `md/translate.md` §2-1-c (2026-09-21 중복 등록 실측) | 2026-09-21 |

## 반영 이력

| 버전 | 날짜 | 반영한 대기 건 |
|---|---|---|
| **v43** | 2026-09-16 | **7건 전건** — ① LPC 쓰기 규칙(차단 게이트 5번 신설) ② `md/decisions.md` 신설(문서 지도 3곳) ③ 용어집 대기 목록 운영(현재 23건) ④ 신규 스크립트 2종(`check_lpc_nulls.py`·`restore/lpc_safe_put.js`) ⑤ OA LIAM 스냅샷 규칙 ⑥ OA 발송 시간 제한(22~08시 · 예약 확정 예외) ⑦ `put_wiki_storage.py` 등재<br>**추가 반영** — DropWeb **직접 게시** 전환(사용자 결정 2026-09-16 · 종전 「게시는 사용자가 수행」 대체) |

## 제안 이력

> 임계에 걸려 제안한 시점을 남긴다. **보류되면 +5건 규칙의 기준선**이 된다.

| 날짜 | 대기 건수 | 사용자 응답 |
|---|---|---|
| — | — | (아직 제안한 적 없음) |

## 확인 필요 (등재 전 판단)

- ✅ **해소(2026-09-15)** — `scripts/put_wiki_storage.py`를 `CLAUDE.md` 자동화 스크립트 표에 등재했다(사용자 지시). 아래 대기 #7로 올린다.

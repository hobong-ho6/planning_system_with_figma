# 용어집(web3_xlt_json) 변경 이력

> Landpress CMS `web3_xlt_json` 용어집의 버전별 변경 이력.
> 용어집 **원본은 Landpress(API 읽기 전용)** 에 있어 git으로 직접 버전 관리가 안 된다. 그래서 변경의 **무엇을·언제·왜**를 이 파일로 git에서 추적한다 (관리 방법은 [`md/landpress.md`](landpress.md) 참조).
>
> ⛔ **용어집을 갱신할 때마다(전체 JSON 산출 → 사용자가 CMS에 붙여넣기) 이 파일에 항목을 추가한다.** (landpress.md 절차의 필수 단계)
>
> ⛔ **같은 작업에서 기획자 가이드 zip(`dropweb/web3_planning_v*.zip`)의 용어집 탭도 반드시 갱신해 전달한다** — 이 표와 동일한 행이 가이드의 용어집 이력 표에 들어가야 하며, 사용자는 가이드 페이지에서 최신 전체 JSON을 복사해 CMS에 반영한다. 절차: [`md/landpress.md`](landpress.md) §5-1.

| 버전 | 날짜 | terms | exceptions | 변경 요약 |
|---|---|---|---|---|
| 2.1 | 2026-05-13 | 25 | 8 | 초기 (guide.md 기반, 암호화폐 예외 USDT/IDRP/JPYC 포함) |
| 2.2 | 2026-06-26 | 29 | 8 | 도메인 용어 4종 추가: 미션·리워드·캐시백·회원가입 |
| 2.3 | 2026-06-26 | 30 | 9 | 포이카츠(term) 추가 + Unifi mini 브랜드 예외 추가 |
| 2.4 | 2026-06-30 | 33 | 9 | 도메인 용어 3종 추가: 혜택·인기·쇼핑 |
| 2.5 | 2026-07-16 | 34 | 9 | 도메인 용어 1종 추가: 채널 동의 |
| 2.6 | 2026-07-24 | 35 | 9 | 도메인 용어 1종 추가: 럭키볼 |
| 3.0 | 2026-07-27 | 104 | 9 | `XLT_통합_용어집_핵심용어_v3.xlsx` 기반 신규 용어 69종 일괄 추가 (충돌·품질 이슈 4건은 팀 검토 보류) |
| 3.1 | 2026-07-27 | 105 | 9 | 1차 팀 검토 반영 — 간편 비밀번호 en/th PIN→Passcode·รหัสผ่านแบบง่าย 교체, 채우기 추가 (잔여 보류 2건) |
| 3.2 | 2026-07-27 | 105 | 9 | 엑셀 v3_2 반영 — 보내기·보낸 사람 ja 出金/出金元 교체, 거래내역 변형 기각(띄어쓰기 유지), 뽑기 계열 계속 보류 |
| 3.3 | 2026-07-27 | 105 | 9 | `거래 내역` → `거래내역` 키 교체 — 서비스 표기 정본을 붙여쓰기로 확정 (v3.2의 기각 결정 번복) |
| 3.4 | 2026-07-27 | 106 | 9 | `당첨`(win/当選/中獎/ถูกรางวัล) 추가 — 럭키볼 도메인 반복 사용 (invite_banner_desc 게이트 d-1 권장의 사용자 채택) |
| 3.5 | 2026-07-27 | 107 | 9 | `당첨금`(prize/当選金/獎金/เงินรางวัล) 추가 — 럭키볼 도메인 6행 일관 확인, `당첨` 단독 매핑의 P1 오탐 감소 (4키 문구 개편 게이트 d-1 권장의 사용자 채택) |
| 3.6 | 2026-07-28 | 111 | 9 | 친구초대 도메인 4종 추가: 링크·친구·선물하기·초대 (OA MSG·키 분리 게이트 d-1 권장의 사용자 채택) |
| 3.7 | 2026-07-28 | 112 | 9 | `{0} JPYC` 추가 — 치환자와 통화 사이 공백 1칸을 5개 언어 공통 정본으로 고정 (사용자 지시) |
| 3.8 | 2026-07-28 | 112 | 9 | `공식 계정` zh_TW 교정: `公眾號`(WeChat 용어) → `官方帳號`(LINE 대만 공식 표기) |
| 3.9 | 2026-07-29 | 112 | 9 | `변경` zh_TW 교정: `更換`(물건·기기 교체) → `變更`(정보·설정 변경) |
| 4.0 | 2026-07-30 | 113 | 9 | `프로필`(profile/プロフィール/個人檔案/โปรไฟล์) 추가 — LINE 연동 안내 문구 반복 사용 (terms_share_noti 게이트 d-1 권장의 사용자 채택). `선물`은 기존 `선물하기`(v3.6)로 이미 커버돼 별도 등재 보류 |
| 4.1 | 2026-08-05 | 114 | 9 | **`oa_variables` 영역 신설(스키마 확장, OA 변수 2종)** — `{{amount}}` → **`{{total_amount}}` 확정** + `{{wallet_address}}` 등재. **terminology 3건 손질** — `종료` zh `結尾`→**`結束`**(등재값 오역) · `포이카츠` en·th → **`Poi-katsu`**(실사용 채택) · **`캠페인` 신규**. 실측 P1 63→58(**-5**), 신규 P1 0 |
| **4.5** | **2026-08-06** | 115 | 9 | **`월렛` → `지갑` 추가(12 → 13건)** — `terminology`에 `지갑`이 등재돼 있어도 **ko가 `월렛`이면 매칭 자체가 안 돼** 검사 대상 밖이었다(「팔로우」와 같은 구조적 사각지대). `md/guide.md` §5-1 C에 규칙은 있었으나 **용어집에 없어 강제되지 않았다**. XLT 시스템 1,596키 실측 **ko `월렛` 14건 잔존**(`지갑`은 79건 정본) |
| 4.6 | 2026-08-07 | **116** | 9 | **`사용자` 등재 + `deprecated_terms` #14 `유저`→`사용자`(12 → 14건)** — 시즌3 유의사항 8키에서 `terms_existing_user`는 `유저`, `terms_wallet_jp`는 `사용자`로 **같은 화면에서 표기가 갈린 것**이 계기다(사용자 결정: `사용자`로 통일). **A/B 실측** — 레지스트리 1,596키 P1 1,655→**1,661(+6)**: 5건이 ko `유저` 실제 검출(`mini_luckyball_terms_existing_user`·`unifi_promotion_jpyc_bonus_all_title`·`_bonus_gold_title`·`_caution_contents2`·`_reward_info1-1`), **1건은 오탐**(`trade_swap_slippage_setting_custom_title` en — ko `사용자 설정`이 관용적으로 `Custom`, **재판정 금지**). 시즌3 57키는 교정 후 P1 **67 동일**(증감 0). zh 정본은 번체 `用戶`(레지스트리 21키, 간체 `用户` 0건) |
| **4.7** | **2026-08-10** | 116 | 9 | **`포이카츠` zh_TW 교정: `點數活動` → `集點活動`** — 등재값이 XLT 시스템 전체(`Dapp Portal` 1,596 + `Unifi` 2,134키)에서 **실사용 0건**인 반면 실제 번역은 `集點活動`을 쓴다(`unifi_promotion_jpyc_unifi_text8c`·`unifi_promotion_unifi_text8d`). **등재값 쪽이 실사용과 어긋난 경우**이며, v4.1에서 en·th만 `Poi-katsu`로 맞추고 「zh_TW `點數活動`은 범위 밖」으로 남겨둔 **잔여분**이다(2026-08-10 사용자 결정 ⓑ안). **A/B 실측** — 시즌3 58키 P1 67→**65(-2)** · `Dapp Portal` 1,596키 P1 1,721→**1,719(-2)** · `Unifi` 2,134키 **변화 0** · P0 불변 · **신규 P1 0건**. 아울러 **metadata 수치 오류 정정** — `total_deprecated_terms` 13 → **14**(v4.6에서 #14 추가 시 갱신 누락) |
| ~~4.8~~ | 2026-08-12 | — | — | **⛔ 철회 — 팀 내 합의 미도출**(사용자 확인 2026-08-20). 「마켓플레이스」 등재 + 「NEXT Bay」 브랜드 예외를 커밋(`39303c1`)했으나 revert(`3a5a4a3`). **라이브는 v4.7 유지**(2026-08-20 API 실측: v4.7 · 116 terms · 14 deprecated · 두 항목 미등재). 재추진은 **팀 합의 후에만** |
| 4.5 | 2026-08-06 | 115 | 9 | **`deprecated_terms` #13 `월렛`→`지갑`(12 → 13건)** — `terminology`에 `지갑`이 등재돼 있어도 **ko가 `월렛`이면 매칭 자체가 안 돼** 검사 대상 밖이었다(「팔로우」와 같은 구조적 사각지대이며 v4.3에서 `deprecated_terms`를 만들 때 함께 넣었어야 했다). `md/guide.md` §5-1 C에 규칙이 **문서로만** 있던 것을 강제 수단으로 옮긴 것. 실측: XLT 시스템 1,596키 P1 1,650→**1,664(+14)** — ko `월렛` 잔존 14건 정확 검출 · 오탐 0 |
| 4.4 | 2026-08-06 | 115 | 9 | **`deprecated_terms` 보완 (9 → 12건)** — 신규 2종(ja `友達`→`友だち` · ko `공식계정`→`공식 계정`) + **`context` 필드 신설로 오탐 제거**. zh `關注`·`追蹤`, th `ติดตาม`, en `follow`는 같은 셀에 `LINE`/`官方帳號`/`บัญชีทางการ`/`official`이 있을 때만 검출(`關注App`=관심 앱, `โปรดติดตาม`=기대해 주세요, `follow the steps`=~를 따르다 제외). en은 **단어 경계** 매칭. **XLT 시스템 1,596키 실측: 오탐 8 → 0건, 실제 대상 16키 정확 검출** |
| 4.3 | 2026-08-06 | 115 | 9 | **`deprecated_terms` 영역 신설(스키마 확장 — 5번째 최상위 키, 9건)** — `OA 팔로우`·`OA 채널 추가`·`채널 팔로우` 등 구 표기 → **`Unifi LINE 공식 계정 친구 추가`** 매핑을 정본화. ja `フォロー`·zh `關注`/`追蹤`·th `ติดตาม`도 포함. terminology 등재안은 **실측 P1 230→266(+36)으로 기각**. 검증기가 각 언어 셀을 검사해 **P1 「구 표기」**로 경고(최장일치). 실측 회귀 **0건** |
| 4.2 | 2026-08-06 | **115** | 9 | **`지급 완료`(Paid/支給済み/จ่ายแล้ว/已發放) 추가** — 비실시간 일괄 지급 전환으로 「지급 완료된 리워드」가 화면에 상시 노출되는데 등재값 `완료`만 있어 각 언어의 완료상 의역이 P1로 오탐됐다. **검증기 최장일치 개선(커밋 `4bd7fa4`)이 선행돼야 효과가 나는 첫 사례.** 실측 P1 197→193(**-4**), 신규 P1 0 |

---

## v4.8 — ⛔ 철회 (2026-08-12 시도 → 2026-08-14 revert)

- **내용**: 「마켓플레이스」 등재 + 브랜드명 「NEXT Bay」 예외 추가 (커밋 `39303c1`, 가이드 v27 동반)
- **철회 사유**: **팀 내 합의가 이뤄지지 않아** 되돌렸다(`3a5a4a3`). 사용자 확인 2026-08-20.
- **라이브 상태**: **v4.7 유지** — 2026-08-20 API 실측 `version=4.7 · terms=116 · deprecated=14`, 「마켓플레이스」·「NEXT Bay」 **미등재** 확인.
- **가이드**: v27도 함께 철회됐고 다음 유효 버전은 **v28**(2026-08-20 게시, 태그 `guide-v28`).
- ⚠️ **재추진 조건**: 팀 합의가 선행돼야 한다. 합의 없이 같은 등재를 재제안하지 않는다.

## v4.7 — `포이카츠` zh_TW 교정 `點數活動` → `集點活動` (2026-08-10)

**계기**: HANDOFF 미결 P1 「`포이카츠` zh 결정」의 종결. 등재값(`點數活動`)과 실제 번역(`集點活動`)이 갈려 검증기가 정상 번역을 P1로 오탐하고 있었다.

### 어느 쪽이 정본인가 — 실사용 전수 조사

| 위치 | zh 표기 |
|---|---|
| **용어집 등재값** | `點數活動` — ⛔ **시스템 전체에서 실사용 0건** |
| `unifi_promotion_jpyc_unifi_text8c` (DP·위키) | `JPYC集點活動全新登場！` |
| `unifi_promotion_unifi_text8d` (DP·위키) | `參與無限 Poi-katsu 集點活動，` |
| `mini_guidekim_point_mission` (DP) | `完成點數任務…` (`點數`+`任務`) |
| `UF_promotion_banner_title_MINI_rolling2` (Unifi) | `領取無限制集點獎勵` |

**등재값이 실사용 0건**이라는 것이 결정적이었다 — 번역이 아니라 **등재값 쪽이 틀린 경우**다(v4.1 `종료` `結尾`→`結束`과 같은 부류). `集點`은 대만에서 포인트 적립을 가리키는 관용 표현이라 직역투인 `點數活動`보다 자연스럽다.

사용자 결정(2026-08-10): **ⓑ 등재값을 `集點活動`으로** (ⓐ 번역을 등재값에 맞추기 / ⓒ `Poi-katsu` 음차 통일은 기각).

### 변경 내용

1. **`terminology["포이카츠"].zh_TW`** — `點數活動` → **`集點活動`** (terminology 변경 키 **1개뿐**, exceptions·oa_variables·deprecated_terms 전부 무변경 검증)
2. **metadata 수치 오류 정정** — `total_deprecated_terms` **13 → 14**. v4.6에서 `#14 유저→사용자`를 추가하고 수치를 올리지 않아 실제 항목 수(14)와 어긋나 있었다.

### A/B 실측 (등재 전 필수 — 2026-08-05 「등재 판정은 추정이 아니라 실측으로」)

| 모집단 | before(v4.6) | after(v4.7) | 증감 |
|---|---|---|---|
| 시즌3 엑셀 58키 | P1 **67** | P1 **65** | **−2** (P0 0 불변) |
| `Dapp Portal` 1,596키 | P1 **1,721** | P1 **1,719** | **−2** (P0 15 불변 · 치환 제안 44건 불변) |
| `Unifi` 2,134키 | — | — | **변화 0** |

**감소만 있고 신규 P1 0건.** 해소된 2건은 `text8c`·`text8d`의 zh 「포이카츠」다.

### 동반 조치

- 기획자 가이드 갱신 — 당초 **v23**으로 만들었으나 **병렬 세션(IA #4)이 같은 파일명으로 덮어써 유실** → 커밋된 원본 JSON으로 **v24 베이스에 재적용해 v25** 생성(`dropweb/web3_planning_v25.zip`). §5-1 갱신 15곳 + IA #4 내용 보존 검증.
- 이 사고를 계기로 **가이드 소스를 git 추적 `guide/`로 전환**(§5-1 0단계 전면 개정 — zip 포렌식 폐지, `git pull` 후 `guide/` HEAD가 베이스, 게시 확인 후 `guide-vN` 태그).
- v4.1 카드의 「포이카츠 zh_TW는 이번 범위가 아니라 그대로 둡니다」 캡션에 **v4.7 후속 한 줄**을 덧붙였다(이력 서술 자체는 보존).

---

## v4.6 — `사용자` 등재 + `유저` 금지 (2026-08-07)

**계기**: 시즌3 「꼭 확인해 주세요」 유의사항 8키를 재번역하던 중, **같은 화면 안에서 표기가 갈린 것**을 발견해 (d-2) 개선 제안으로 올렸다.

| 키 | ko 표기 |
|---|---|
| `mini_luckyball_terms_existing_user` | 완료한 **유저**의 경우 |
| `mini_luckyball_terms_wallet_jp` | 일본 **사용자**가 아닌 경우 |

사용자가 **「`사용자`로 용어집 등록하고 `사용자`로 사용」** 으로 결정했다.

### 변경 내용

1. **`terminology`에 `사용자` 등재** — `{en_US: user, ja_JP: ユーザー, ko_KR: 사용자, th_TH: ผู้ใช้, zh_TW: 用戶}`
2. **`deprecated_terms` #14** — ko `유저` → `사용자` (note: 외래어 음차. 서비스 표기 정본은 「사용자」)

### A/B 실측 (등재 전 필수 — 2026-08-05 「등재 판정은 추정이 아니라 실측으로」)

두 축을 분리해 측정했다.

| 모집단 | baseline(v4.5) | term만 | dep만 | **둘다** |
|---|---|---|---|---|
| 시즌3 57키 | P1 67 | 67 (+0) | 68 (+1) | **68 (+1)** |
| 레지스트리 1,596키 | P1 1,655 | 1,656 (+1) | 1,660 (+5) | **1,661 (+6)** |

P0·P2는 전 케이스 **불변**.

**신규 6건 판정: 5건 실제 위반 · 1건 오탐**

| 키 | 지적 | 판정 |
|---|---|---|
| `mini_luckyball_terms_existing_user` | ko `유저` | 실제 — 이번에 교정 |
| `unifi_promotion_jpyc_bonus_all_title` (`모든 유저 대상`) | ko `유저` | 실제 — **XLT 시스템 정리 대상** |
| `unifi_promotion_jpyc_bonus_gold_title` | ko `유저` | 실제 — 〃 |
| `unifi_promotion_jpyc_caution_contents2` | ko `유저` | 실제 — 〃 |
| `unifi_promotion_jpyc_reward_info1-1` | ko `유저` | 실제 — 〃 |
| `trade_swap_slippage_setting_custom_title` | en `사용자`→`user` | **오탐 · 재판정 금지** — ko `사용자 설정`이 관용적으로 `Custom`(사용자 정의)이라 en이 정확하다. terminology가 다의어를 구분하지 못하는 부류 |

`deprecated_terms`만 넣으면 오탐 0이지만, `terminology` 등재는 **5개 언어 정본 표기를 문서·가이드에 노출**하는 값이 있어 둘 다 채택했다(2026-08-05 `캠페인` 등재와 동일 판단 구조).

### 교정 결과

`mini_luckyball_terms_existing_user` ko만 `유저`→`사용자`. **4개 언어는 무변경** — ja `ユーザー`·en `Users`·th `ผู้ใช้`·zh `用戶`로 이미 정본이었다. 교정 후 시즌3 P1은 **67로 baseline과 동일**(등급 증감 0)이고 엑셀 3종 `유저` 잔존 0건.

### 부수 발견 (별건)

`trade_swap_slippage_setting_custom_title`은 ko `사용자 설정`이 **Custom**의 뜻인데 **ja `ユーザー設定`·th `การตั้งค่าผู้ใช้`·zh `設定用戶`가 "user settings"로 오역**돼 있다(en `Custom`만 정확, zh는 어순도 `用戶設定`이 맞다). `Dapp Portal` 등록값이며 XLT 검증 흐름(`md/xlt-verify.md`)의 수정 제안 대상이다.

---

## v4.5 — `월렛` → `지갑` 추가 (2026-08-06)

**계기**: 사용자가 4개 키를 지목하며 「월렛은 지갑을 쓰도록 용어집을 업데이트하고, **이미 그렇게 되어 있는데 안 걸렸다면 왜인지 확인해서 보완**하라」고 요청했다.

**왜 안 걸렸나 — terminology의 구조적 한계 (「팔로우」와 동일)**

| 검사 경로 | 결과 |
|---|---|
| `terminology`의 `지갑`(en `wallet`·ja `ウォレット`·th `กระเป๋า`·zh `錢包`) | **작동 안 함** — ko에 `지갑`이 있는 행에서만 5개 언어를 검사한다. ko가 `월렛`이면 **매칭 자체가 되지 않아** 검사 대상에서 빠진다 |
| `deprecated_terms` | **항목 없음** — v4.3에서 이 영역을 만들 때 `팔로우` 계열만 넣고 `월렛`을 빠뜨렸다 |
| `md/guide.md` §5-1 C | 「비수탁 지갑: "월렛" → **"지갑"**」 규칙이 **문서로만** 존재 |

→ **규칙은 있는데 강제 수단이 없던 것**이 원인이다. v4.3에서 「팔로우」로 같은 문제를 겪고 `deprecated_terms`를 신설했으면서 `월렛`을 함께 넣지 않은 누락이다.

**등재**: `{lang: ko_KR, pattern: 월렛, recommend: 지갑}` (context 없음 — 「월렛」은 지갑 외 다른 뜻이 없다)

**실측**

| 대상 | v4.4 | v4.5 |
|---|---|---|
| XLT 시스템 1,596키 | 1,650 | **1,664 (+14)** — ko `월렛` 잔존 14건 정확 검출 |
| 시즌3 ALL / LV | 68 / 57 | 69 / 58 (+1) — `mini_luckyball_terms_existing_user` |
| 그 외 엑셀 6종 | — | **변화 없음**(오탐 0) |

**함께 처리**: 검출된 신규 키 `mini_luckyball_terms_existing_user`의 ko를 정본으로 고쳤다. **사용자가 위키 Screen 표에 이미 `(지갑 생성)`으로 수정해 둔 것을 발견**해 그 표기를 따랐고(제 초안은 `지갑 개설`이었다), 같은 문서 `terms_valid_entry`와도 일치한다. 4개 언어도 「생성」 계열로 통일(ja `ウォレット作成`·th `สร้างกระเป๋า`·zh `建立錢包`, en은 이미 `wallet creation`).

**남은 정리 대상**: XLT 시스템 ko `월렛` **13건**(`terms_existing_user` 제외) — `launching_promotion_*` 6 · `oa_promotion*` 5 · `home_pointreward_my_detail_desc` · `mini_luckyball_*` 계열. 대부분 **Dapp Portal 계열 구 문구**다.

---

## v4.4 — `deprecated_terms` 보완: 신규 2종 + `context` 필드 (2026-08-06)

**계기**: 사용자가 **XLT 시스템 export 전체(v2.6.0 · 2026-08-06 · 1,596키)** 를 제공해 잔존 실태를 검토했다. v4.3 목록으로 21건이 잡혔는데 **8건이 오탐**이었고, 반대로 **잡아야 할 표기 2종이 목록에 없었다.**

### ⓐ 신규 2종

| lang | 금지 | 권장 | 잔존 |
|---|---|---|---|
| ja_JP | `友達` | **`友だち`** | 4키 — LINE 일본 공식 표기는 히라가나다 |
| ko_KR | `공식계정` | **`공식 계정`** | 4키 — 붙여쓰기 오류(용어집 등재값은 띄어쓰기) |

### ⓑ `context` 필드 신설 — 같은 단어의 다른 뜻을 걸러낸다

같은 셀에 지정 문자열 중 하나가 있을 때만 구 표기로 판정한다.

| lang | 패턴 | context | 걸러낸 오탐 |
|---|---|---|---|
| zh_TW | `關注` · `追蹤` | `LINE` · `官方帳號` | `設定關注App`·`我關注的Apps`(**관심 앱**) 2건 |
| th_TH | `ติดตาม` | `LINE` · `บัญชีทางการ` | `โปรดติดตาม…`(**기대해 주세요**)·`โปรดรอติดตาม…`(**기다려 주세요**) 2건 |
| en_US | `follow` | `official` · `LINE` | `points follow the policies`·`follow the steps`(**~를 따르다**) 4건 |

en은 **단어 경계**로만 매칭해 `following`·`follow-up` 파생형으로 번지지 않게 했다.

**실측 (XLT 시스템 1,596키)**

| | 검출 | 오탐 | 실제 대상 |
|---|---|---|---|
| v4.3 | 21건 / 14키 | **8건** | — |
| **v4.4** | 27건 / **16키** | **0건** | 16키(A 공식 계정 문맥 10 + `友達` 4 + `공식계정` 4, 중복 2 제외) |

**회귀**: 보유 엑셀 9종 P1 **230 → 230(0건)**. `test_validation.py` 전체 통과.

**남은 정리 대상 16키**(이번 작업 범위 밖 — 문구 수정은 하지 않았다): zh 9건 · ko 7 · ja 6 · th 3 · en 2. 최다는 zh `關注`/`追蹤`이며, `mypage_oa_add`는 **5개 언어 전부**가 대상이다.

---

## v4.3 — `deprecated_terms` 영역 신설 (2026-08-06)

**무엇을**: 최상위에 `deprecated_terms` 배열을 신설했다(`oa_variables`에 이은 **다섯 번째 영역**). 금지 표기 → 권장 표기 매핑 **9건**.

| lang | 금지 표기 | 권장 |
|---|---|---|
| ko_KR | `OA 팔로우` · `OA 채널 추가` · `채널 팔로우` · `공식 계정 팔로우` · `팔로우` | **Unifi LINE 공식 계정 친구 추가** |
| ja_JP | `フォロー` | `友だち追加` |
| zh_TW | `關注` · `追蹤` | `加入好友` |
| th_TH | `ติดตาม` | `เพิ่มเพื่อน` |

**왜**: 2026-08-04에 「채널 팔로우·팔로우 → 공식 계정 친구 추가」를 정본으로 확정했지만 **용어집에는 남기지 못했다.** `terminology`는 `ko → 5개 언어` **정합**을 보는 자리라 ko 자체의 구 표기를 잡지 못하기 때문이다. 그 결과 규칙이 문서에만 있어 **신규 키에 `OA 팔로우`가 그대로 들어가는 일이 실제로 발생**했다(2026-08-06 `mini_luckyball_terms_existing_user`, 사용자 지적으로 발견).

**⛔ terminology 등재안은 실측으로 기각**

| 안 | P1 |
|---|---|
| 현행(v4.2) | 230 |
| `공식 계정 친구 추가`를 terminology에 등재 | **266 (+36)** |

긴 구(phrase)를 등재하면 5개 언어에서 그 문자열을 통째로 찾는데, 실제 번역은 문맥마다 어순이 달라(`友だち追加`·`added as a friend`·`เป็นเพื่อน`·`好友`) 거의 매칭되지 않는다. 2026-08-04의 「일치율 32%」 판단과 같은 결론이 재확인됐다.

**대신 별도 영역 + 검증기 검사**로 풀었다. `validate_translation.py`가 `deprecated_terms`를 읽어 각 언어 셀을 검사하고 **P1 「구 표기」**로 경고한다. **최장일치**를 적용해 `OA 팔로우`가 잡히면 그 안의 `팔로우`는 중복 보고하지 않는다.

**실측**: 최신 엑셀 8종 P1 **230 → 230(회귀 0건)** — 현행 산출물은 이미 정본 표기라 **예방 장치**로만 동작한다. 구 표기가 남은 파일(`season3_ALL_20260804`)에서는 **2건 정상 검출**(`OA 팔로우`·`공식 계정 팔로우`, 중복 없음).

**함께 확정된 작업 규칙**(`md/guide.md` §5-1 C-1): ⓐ **신규 키·문구 변경은 정본 표기로 바로 적용** ⓑ 기존 XLT 시스템 등록값은 임의 변경 금지·보고만 ⓒ **의미가 모호하면 사용자에게 확인받고 `Unifi LINE 공식 계정 친구 추가` 사용을 제안** ⓓ 4개 언어만 정본으로 옮기고 ko를 구 표기로 남기지 않는다.

---

## v4.2 — `지급 완료` 등재 (2026-08-06)

**무엇을**: terminology에 `지급 완료` 1종을 추가했다(114 → 115).

| 용어 | ko_KR | ja_JP | en_US | th_TH | zh_TW |
|---|---|---|---|---|---|
| 지급 완료 | 지급 완료 | `支給済み` | `Paid` | `จ่ายแล้ว` | `已發放` |

**왜**: 럭키볼 친구초대 프로모션이 **실시간 → 비실시간 일괄 지급**으로 바뀌면서 「지급 완료된 리워드」·「지급될 리워드」가 화면 상시 문구가 됐다. 그런데 용어집에는 `완료`(`Complete`/`完了`/`เสร็จ`/`完成`)만 있어, 각 언어에서 **완료상을 자연스럽게 표현한 정상 번역**(`Paid`·`支給済み`·`จ่ายแล้ว`·`已發放`)이 전부 P1(용어 불일치)로 잡혔다.

**⛔ 등재만으로는 효과가 없었다 — 검증기 개선이 선행 조건이었다**

이 건은 **같은 등재를 두 번 실측해 결과가 갈린 첫 사례**다.

| 시점 | 최장일치 | `지급 완료` 등재 | P1 |
|---|:---:|:---:|---|
| 2026-08-06 오전 (2차 게이트) | ✗ | ✓ | **감소 0건** — 등재 무효 |
| 2026-08-06 오후 (4차 게이트) | ✓ (`4bd7fa4`) | ✓ | **-4건** |

`validate_translation.py`가 용어집 전 항목을 부분문자열로 순회해 **짧은 `완료`가 긴 `지급 완료` 안에 들어 있어도 따로 걸렸기** 때문이다. 최장일치 필터가 들어간 뒤에야 등재가 의미를 갖게 됐다.
→ **교훈: 용어집 보완을 권장하기 전에 검증기의 매칭 방식을 먼저 확인한다.** 등재로 풀리는 문제와 도구로 풀리는 문제가 다르다.

**일관성 검증(landpress §5 3단계·차단 단계)**: ko에 「지급 완료」가 든 행 **전수 1건**(`mini_luckyball_paid_reward_total`), 4개 언어 **전부 제안 표기 포함 ✅**.

**A/B 실측(보유 엑셀 8종 전체)**: P1 **197 → 193건(-4)**, **신규 P1 0건**, P0 **0건 불변**. 대상 파일(`xlt_output_nonrealtime_improve_LV_20260806`)은 **4 → 0건**으로 완전 해소.

**배경**: `gate_report_nonrealtime_improve_2keys.md` 3차 (d-1) 권장을 사용자가 승인(2026-08-06). 전체 JSON은 git 추적본 `reports/glossary_v4.2_for_cms.json`.

---

## v4.1 — `oa_variables` 스키마 확장 + terminology 3건 (2026-08-05)

**무엇을**: 최상위에 `oa_variables` 배열을 신설했다. 기존 `{metadata, exceptions, terminology}` 3개 영역에 **네 번째 영역**이 추가된 첫 사례다.

| 변수 | 의미 | 예시 | 대체 대상 | 등재일 |
|---|---|---|---|---|
| `{{total_amount}}` | 지급된 당첨금 총액(JPYC 단위 숫자) | `25` | **`{{amount}}`** | 2026-08-05 |
| `{{wallet_address}}` | 당첨금이 지급된 지갑 주소(마스킹) | `0x8442...7c8` | — | 2026-07-24 |

**왜**: OA(LINE 공식계정) 메시지는 XLT 키를 부여하지 않아 변수명이 **어디에도 정본으로 남지 않았다.** 그 결과 캠페인마다 Claude가 변수 후보를 제안하고 사용자가 이름을 다시 정하는 왕복이 반복됐다. 용어집에 등재하면 **같은 의미에는 같은 이름을 문의 없이 재사용**할 수 있다.

**주의 — 소급 치환하지 않는다**: `oa/`의 기존 캠페인 산출물(`flex_OA친구_추가시`·`flex_가입_완료시` 등)은 `{{amount}}`를 그대로 둔다. 시즌3 `OA_Reward_Confirm` 5파일만 `{{total_amount}}`로 갱신했다(zip v4). 소급이 필요하면 사용자 지시로 별도 수행한다.

**소비자 호환**: `scripts/validate_translation.py`는 `.get('terminology')`·`.get('exceptions')`만 읽으므로 신규 최상위 키는 무해하다(실측 확인). `metadata.total_oa_variables`도 함께 신설해 개수를 검증할 수 있게 했다.

**함께 확정된 규칙**(`md/OA.md` §2-1): ⓐ 등재된 변수는 사용자 문의 없이 재사용 ⓑ 숫자+통화 한 칸 띄움(`{{total_amount}} JPYC`) ⓒ **`altText`에는 변수를 쓸 수 없다** — 봉투 텍스트는 고정 문구로만 작성하고 금액 삽입을 재제안하지 않는다.

### terminology 3건 (같은 v4.1에 병합, 113 → 114)

| 용어 | 언어 | before | after | 근거 |
|---|---|---|---|---|
| **종료** | zh_TW | `結尾` | **`結束`** | `結尾`는 **글·이야기의 맺음말**이라 UI '종료'에 부적합. **등재값 자체가 오역**이었고 실제 번역(`本活動已結束。`)이 맞았다 |
| **포이카츠** | en_US | `point activity` | **`Poi-katsu`** | 실제 번역이 이미 음차 `Poi-katsu`를 쓰고 있었다. 일본 거주자 대상 캠페인이라 고유명사 취급이 맞다 |
| **포이카츠** | th_TH | `การสะสมแต้ม` | **`Poi-katsu`** | 동일 |
| **포이카츠** | zh_TW | `點數活動` | **변경 없음** | 이번 범위 밖. 실제 번역은 `集點活動`을 써서 여전히 갈린다 → **잔여 과제** |
| **캠페인** | 신규 | — | `campaign` / `キャンペーン` / `แคมเปญ` / `活動` | ko에 쓰이는 1키(`unifi_promotion_info_end`)의 4개 언어가 이미 일치 |
| **4.9** | **2026-09-04** | **117** | **11** | **`LINE MINI App` 브랜드 표기 정본화 + 누적 오탐 원인 5건 교정 + 「리뷰」 등재**. ⓐ LINE Developers 공식 문서를 1차 출처로 확인([문서 허브](https://developers.line.biz/en/docs/line-mini-app/) · [Introducing](https://developers.line.biz/en/docs/line-mini-app/discover/introduction/)) — 제품명은 **`LINE MINI App`**(`MINI` 전부 대문자)이고 **`Mini App`(소문자 `ini`)은 쓰지 않는다**. exceptions 2건 신설(`*LINE MINI App*`·`*MINI App*` · 9→**11**) + deprecated 4건(ko·ja·th·zh `Mini App`→`MINI App`) + `*Unifi mini*`(#9) note 보강. ⚠️ **en_US는 deprecated 제외** — 검증기가 en만 `re.I`라 정상 표기까지 오탐. ⓑ **등재 실사용 실측 기반 5건 교정**: 혜택 en `perks`→**`benefit`**(18:6) · 더보기 ja `すべて見る`→**`もっと見る`**(6:6 동률이나 `すべて見る`는 전부 ko 「전체보기」 번역) · 더보기 zh `查看更多`→**`更多`**(24:7) · 결제 zh `結帳`→**`付款`**(26:3) · 교환 ja `スワップ`→**`交換`**(48:5). **교환 en `swap`은 유지** — `exchange`는 A/B에서 P1 **+24 악화**라 반려. ⓒ **`리뷰` 신규 등재**(`レビュー`/`Reviews`/`รีวิว`/`評論` — ko 4키 일관 확인) + **deprecated ko `후기`→`리뷰`**(deprecated 14→**19**). **A/B(등재값 전체 2,461키): P0 102 불변 · P2 534 불변 · P1 2,437→2,357(−80)** — 기여도 교환 ja −33 · 혜택 en −26 · 결제 zh −23 · 더보기 ja −4 · 더보기 zh −3 · 리뷰 등재 ±0 · `Mini App` deprecated +4(실제 위반) · `후기` deprecated +5(실제 위반 · 7키 재업로드로 해소 예정). 보유 엑셀 7종 합계 P1 83 불변(오탐 −4 / 실검출 +4). ※ **4.8은 철회된 번호**(2026-08-12 시도 → 08-14 revert)라 재사용하지 않고 4.9로 올린다 |

**A/B 실측 (등재 전 필수 검증, landpress §4 기준 3)**: 시즌3 47키 기준 **P1 63 → 58건(-5), 신규 P1 0건.**

- 해소 5건 = `info_end` zh `종료` 1 + `text8c`·`text8d`의 en·th `포이카츠` 4
- `캠페인`은 해소 0·신규 0 — **예방적 등재**
- ⚠️ 2차 검토 에이전트는 `캠페인` 등재가 **`+3` 신규 P1을 만든다고 추정**했으나, 실측 결과 **0건**이었다. ko에 `캠페인`이 쓰인 키가 1개뿐이고 그 4개 언어가 이미 일치했기 때문이다. **추정이 아니라 A/B 실측으로 판정해야 한다**는 사례.

---

## 상세

### v4.0 (2026-07-30)
- ✅ **라이브 반영 완료 (2026-07-30, 사용자 CMS 반영)** — API 실측 확인: `metadata.version` **4.0** · `total_terms` **113** · `terminology` 실제 항목 **113개**(일치) · 신규 `프로필` = `プロフィール` 등재 확인.
- **term 추가** (1): `프로필` — en `profile`, ja `プロフィール`, zh `個人檔案`, th `โปรไฟล์`
- 일관성 검증(landpress.md §5-3, 위키 정본 75키 전수 대조): `프로필` 포함 1행(`terms_share_noti`) **4개 언어 100% 일치**. 등재 후 검증기 재실행 결과 이 term 관련 신규 P1 **0건**.
- 배경: `mini_luckyball_terms_share_noti` 신규 키 게이트(`gate_report_luckyball_terms_share_noti.md`) (d-1) 권장을 사용자가 채택(2026-07-30). LINE 연동 안내 문구에서 반복될 표현인데 미등재라 언어별 표기가 작업자 판단에 맡겨져 있었다.
- **보류 — `선물` 별도 등재 안 함**: 같은 요청에서 사용자가 `선물`도 등재 지시했으나, **v3.6에 이미 `선물하기`(en `gift`·ja `プレゼント`·zh `送`·th `มอบ`)가 등재돼 있어 중복**이다. 추가로 `선물`을 단일 매핑하면 zh·th에서 명사(`禮物`/`ของขวัญ`)와 동사(`送`/`มอบ`)가 문맥별로 갈려 어떤 값을 넣어도 새 P1이 발생함을 실측했다(명사형 5건 / 동사형 2건 / 혼합 2건 — 전부 정상 번역에 대한 오탐). 사용자 확인 후 보류 결정.
  - ⚠️ 참고: 기존 `선물하기` 등재값으로도 P1 1건이 이미 발생 중(`gift_copy_link` th `คัดลอกลิงก์ของขวัญ` — 명사형 사용). 근본 해결은 **검증기의 명사/동사 활용 허용** 개선(v3.9의 최장일치 우선 제안과 같은 계열) — 별도 제안으로 남김.

### v3.9 (2026-07-29)
- **term 수정** (1): `변경` zh_TW `更換` → **`變更`** (en `change`·ja `変更`·th `เปลี่ยน` 변동 없음)
- 사유: `更換`은 **물건·기기를 교체**하는 뜻이라 `지갑 주소 변경`·`정보 변경` 같은 정보/설정 변경 문맥에 맞지 않는다. zh-TW 표준은 `變更`이며, 실제 번역은 이미 전부 `變更`을 사용 중이었다.
- 등재 전 일관성 검증(landpress.md §4 기준 3): LV·UIT 엑셀 **76행 전수 대조** → `변경` 포함 3행(`terms_wallet_fixed`·`terms_info_change`·`terms_early_end`) **3/3 모두 `變更`** 사용, `更換` 사용 **0건**.
- 효과: 등재값 오류가 만들던 zh_TW P1 오탐 해소.
- 배경: 럭키볼 캠페인 3키 번역 패치 게이트(`gate_report_luckyball_terms_3keys.md`) (d-1) 권장을 사용자가 승인(2026-07-29).
- **보류**: 같은 게이트에서 제안한 `당첨` en/th **명사형 병기는 반영하지 않음** — 용어집 스키마가 언어당 단일 값이고 검증기(`_term_in_text`)가 en 외 언어에서 완전 부분문자열 일치를 요구하므로 병기(슬래시) 표기는 전 행 오탐을 유발한다. 추가로 `당첨` 포함 12행 중 다수가 `당첨금` 계열(`prize`/`เงินรางวัล`)을 사용해 일관성 기준(§4-3) 미충족. 근본 해결은 검증기의 **최장일치 우선**(`당첨금`이 매칭되면 `당첨` 검사 생략) 개선 — 별도 제안으로 남김.

### v3.8 (2026-07-28)
- **term 수정** (1): `공식 계정` zh_TW `公眾號` → **`官方帳號`** (en·ja·th 변동 없음)
- 사유: `公眾號`는 **WeChat(중국 대륙)** 용어이고, **LINE 대만의 공식 표기는 `官方帳號`**(`LINE 官方帳號`)다. 등재값만 어긋나 있었고 실제 번역은 이미 전부 `官方帳號`였다 — 위키 11곳·LV 엑셀 8행 사용, `公眾號` 사용 **0건** 확인.
- 효과: 등재값 오류가 만들던 P1 오탐 해소 — 럭키볼 73키 기준 **P1 110 → 102**(공식 계정 zh 5건 소멸). 잔여 1건은 en 어순 변형(`Unifi's official LINE account`)으로 정상 의역.
- 배경: 화면 정의 갱신 15키(팔로우 → 친구 추가/공식 계정 전환) 게이트 (d-1) 권장을 사용자가 채택(2026-07-28).

### v3.7 (2026-07-28)
- **term 추가** (1): `{0} JPYC` — 5개 언어 모두 `{0} JPYC` (치환자+통화 표기 패턴)
- 목적: **변수와 통화 사이 공백 1칸**을 정본으로 고정. 붙임 표기 `{0}JPYC`가 들어오면 검증기 2단계가 **P1(용어 불일치)** 로 검출한다.
- **ja_JP·zh_TW도 동일하게 띄운다** — 해당 언어 관용은 숫자+라틴문자 붙여쓰기이나, 런타임 치환값이 통화에 달라붙어 보이는 것을 막기 위한 **서비스 표기 결정**(사용자 지시, 2026-07-28).
- 적용 실적: `mini_luckyball_prize_won` 5개 언어를 `{0}JPYC` → `{0} JPYC`로 교정(이미 공백을 쓰던 `congrats_won`·`reward_paid_toast`와 일치). 등재 후 전수 재검증 결과 이 term 관련 P1 **0건**.
- 규칙 문서화: [`md/guide.md`](guide.md) '치환자(Placeholder) 보존'에 공백 규칙 명문화.

### v3.6 (2026-07-28)
- **term 추가** (4):
  - `링크` — en `link`, ja `リンク`, zh `連結`, th `ลิงก์`
  - `친구` — en `friend`, ja `友だち`, zh `好友`, th `เพื่อน`
  - `선물하기` — en `gift`, ja `プレゼント`, zh `送`, th `มอบ`
  - `초대` — en `invite`, ja `招待`, zh `邀請`, th `เชิญ`
- 일관성 검증(landpress.md §5-3, LV 엑셀 73키 + OA 3문구 전수 대조):
  - `친구` 15행 / `링크` 2행 — **4개 언어 100% 일치** → 무조건 채택
  - `선물하기` 2행 — 실사용은 **동사형** `送`(zh)·`มอบ`(th)이며 명사형 `禮物`·`ของขวัญ`는 미사용 → 최초 제안값(送禮/มอบของขวัญ)을 **실사용 표기로 교정해 등재**
  - `초대` 9행 — ja `招待`·en `invit-`·zh `邀`(邀請 7 + 受邀 2, 수동형 변형) 일치. **th만 `เชิญ`(5) / `ชวน`(4) 혼용** → 다수·명사형 기준 `เชิญ`로 등재하되 **`ชวน`도 정상 의역으로 허용**(P1 오탐 시 기각 처리)
- 배경: 25034 하단 선물 버튼·`invite_me_too` 키 분리·`LINE으로 공유하기` OA MSG 게이트 리포트의 (d-1) 권장을 사용자가 채택(2026-07-28).
- ⚠️ 후속 권장: th `초대` 표기(`เชิญ` vs `ชวน`) 통일 여부는 별도 정책 결정 사항 — 현재는 혼용 허용.

### v3.5 (2026-07-27)
- **term 추가** (1): `당첨금` — en `prize`, ja `当選金`, zh `獎金`, th `เงินรางวัล`
- 배경: 4개 프레임 문구 개편(더 도착 배너·oa_followed 등) 게이트 리포트 (d-1) 권장을 사용자가 채택. 추가 전 `당첨금` 포함 전 행(oa_followed·payout_5min·signup_line_prize·signup_prize_desc·follow_bonus_desc·invitee_gift_hero 6행)의 5개 언어 표기 일치를 검증(landpress.md §5-3) — 전행 일치 확인. 기존 `당첨`(win) 단독 매핑이 당첨금 문구를 P1로 오탐하던 것을 해소.

### v3.4 (2026-07-27)
- **term 추가** (1): `당첨` — en `win`, ja `当選`, zh `中獎`, th `ถูกรางวัล`
- 배경: `mini_luckyball_invite_banner_desc` 게이트 리포트 (d-1) 용어집 보완 권장을 사용자가 채택. 기존 번역(prize_won 当選/中獎/ถูกรางวัล)과 일관 — 마케팅 배너의 `当たる`류 표현은 정상 의역로 허용.

### v3.3 (2026-07-27)
- **term 키 교체** (1): `거래 내역`(띄어쓰기) 삭제 → `거래내역`(붙여쓰기) 등재 — en `Transaction History`(엑셀 v3_2 값·Title Case), ja `取引履歴`, zh `交易紀錄`, th `ประวัติการทำธุรกรรม`
- 배경: v3.2에서 "띄어쓰기 규칙에 맞게 교정(거래 내역 유지)"으로 기각했으나, **사용자가 서비스 표기 정본을 붙여쓰기 `거래내역`으로 확정**(2026-07-27 2차 결정 — 문법상 띄어쓰기가 표준임을 인지한 상태의 표기 정책 결정)하여 번복 반영.
- ⚠️ 유의: 한국어 문법 표준은 `거래 내역`이나 **서비스 UI·용어집 표기는 `거래내역`로 통일** — 검증 시 붙여쓰기를 오타로 오탐하지 않도록 주의.

### v3.2 (2026-07-27)
- **term 수정** (2, 사용자 확정): `보내기` ja_JP `送金`→`出金`, `보낸 사람` ja_JP `送金元`→`出金元` (`XLT_통합_용어집_핵심용어_v3_2.xlsx` 값)
  - ⚠️ 유의: ja `出金`이 `꺼내기`(Withdraw)와 동일 표기가 됨 — 보내기/꺼내기/출금(액션)/출금(거래내역 라벨)의 ja가 모두 出金 계열. 서비스 화면에서 문맥 구분 필요 시 재검토.
- **보류 2 종결 (기각)**: `거래내역`(붙여쓰기) 미등재 — 사용자 결정 "띄어쓰기 규칙에 맞게 교정" → 기존 `거래 내역` 유지 (v3_2 엑셀의 붙여쓰기 변형은 채택하지 않음).
- **보류 3 유지**: `나중에 뽑기`·`더 뽑기` — v3_2 엑셀도 zh `稍後畫`·`畫更多`/th `วาด...`(그리기 오역 의심) 그대로라 계속 보류.
- **기각 (객관 오류)**: v3_2의 `내 자산` zh `資産`(U+7523 이형자)은 미반영 — v3.0 교정본 `資產` 유지.
- 배경: 사용자가 v3_2 엑셀(100행) 제공 → v3.1과 전수 대조(신규 3·충돌 3·소실 1) → 사용자 확인 후 반영.

### v3.1 (2026-07-27)
- **term 수정** (1): `간편 비밀번호` en_US `PIN`→`Passcode`, th_TH `PIN`→`รหัสผ่านแบบง่าย` (ja/zh 변동 없음)
  - 근거: 공식 룰 `[Rule] XLT Value 작성 가이드`(pageId=4494805808) "PIN/Simple Passcode 금지 → Passcode". exceptions `*PIN*` 패턴은 유지.
- **term 추가** (1): `채우기`(Receive/受け取る/接收/รับ) — 팀 확인: 서비스 UI "채우기" = 수신(Receive) 기능.
- 배경: v3.0 보류 4건 중 1·4를 팀 검토로 채택(2026-07-27). 잔여 보류 2건(거래내역 변형, 나중에/더 뽑기 zh·th 오역)은 [`glossary_pending_review_v3.md`](../glossary_pending_review_v3.md) 참조.

### v3.0 (2026-07-27)
- **term 추가** (69): 사용자 제공 `XLT_통합_용어집_핵심용어_v3.xlsx`(terminology 101행) ↔ v2.6 전수 대조 후, 기존 동일 27건 제외·보류 4건 제외한 신규 전량.
  - UI 라벨: 가격·가입·계속하기·공유하기·다음·다음 단계·더보기·전체·전체보기·완료·취소·시작하기·최대·최신순·과거순·직접 입력·최근 사용·홈으로 이동·여기서·숫자·종료·마이·플레이 등
  - 거래·자산: 거래 유형·거래 ID·결제·결제 페이백·교환 입금·교환 출금·교환하기·구매·구매하기·보내기·받는 사람·보낸 사람·내 자산·락업 자산·넣기·꺼내기·예상 수수료·평균 잔고·외부지갑·토큰 교환·네트워크·네트워크 전환·입금/출금 (거래내역 유형 라벨)
  - 이자·미션: 기본 이자·부스트 이자·플러스 이자·예치 이자·일일 미션·보너스 미션·리워드 지급·뽑기
  - 기타: 공식 계정·공지사항·로그아웃 안내·마켓·메시지·문의하기·알림·언어 설정·통화 설정·연결 계정·연결하기·유의사항·복구 비밀번호·최저가격
- **표기 교정** (1): `내 자산` zh_TW `資産`(U+7523 일본식 이형자) → `資產`(U+7522 대만 표준) — `md/check.md` 2단계-C.
- **⚠️ 보류 (미반영, 팀 검토 대기)** — 상세는 [`glossary_pending_review_v3.md`](../glossary_pending_review_v3.md):
  1. `간편 비밀번호` en/th: 기존 `PIN` vs 엑셀 `Passcode`·`รหัสผ่านแบบง่าย` 충돌 (공식 룰 pageId=4494805808은 Passcode 지지)
  2. `거래내역` 붙여쓰기 변형 이중 등재 여부 (`거래 내역` 기존 등재)
  3. `나중에 뽑기`·`더 뽑기` zh/th "그리기" 오역 의심 (`稍後畫`·`วาด...`)
  4. `채우기`=Receive 의미 대응 확인 필요
- 배경: 사용자가 통합 용어집 엑셀 v3를 일괄 추가 요청(2026-07-27). 보류 4건은 landpress.md §4 기준 3(일관성 검증) 미충족으로 제외, 팀 검토 결과 수령 시 v3.1로 반영 예정.

### v2.6 (2026-07-24)
- **term 추가** (1): `럭키볼`(Lucky Ball/ラッキーボール/幸運球/ลูกบอลนำโชค)
  - 5개 언어 표기는 럭키볼 친구초대 캠페인 위키(pageId 4479306980) XLT 번역에서 도출 — 파일 내 사용 15개 문구 전건 대역 일관성 확인 완료.
- 배경: 럭키볼 캠페인 3개 섹션(Unifi mini·초대자·피초대자) XLT 코멘트 선별 번역 게이트(d-1)에서 용어집 미정의 캠페인 고유 용어로 식별, 사용자 승인(2026-07-24)으로 추가.

### v2.5 (2026-07-16)
- **term 추가** (1): `채널 동의`(channel consent/チャネル同意/頻道同意/การยินยอมแชนแนล)
  - 5개 언어 표기는 Unifi mini → LIFF 예치 안내 배너 위키(pageId 4465962427) `mini_bridge_channel` 실제 번역에서 도출 — 파일 내 사용 행(1건)과 일관성 확인 완료.
  - ⚠️ en/zh/th는 이번 1회 사용에서 도출한 잠정 표기 — LINE 공식 로컬라이제이션 표준 확인 시 갱신.
- 배경: LIFF 예치 안내 배너 XLT 코멘트 선별 번역 게이트(d-1)에서 용어집에 없는 도메인 용어로 식별, 사용자 승인(2026-07-16)으로 추가.

### v2.4 (2026-06-30)
- **term 추가** (3): `혜택`(perks/特典/優惠/สิทธิประโยชน์), `인기`(popular/人気/人氣/ยอดนิยม), `쇼핑`(shopping/ショッピング/購物/ช้อปปิ้ง)
  - 5개 언어 표기는 Guide Kim 위키 실제 번역에서 도출 — 혜택: `bridge_title`·`signup_done_desc`·`popular_benefit`, 인기: `kbeauty_desc`·`popular_benefit`, 쇼핑: `my_shopping`·`popular_benefit`.
  - ⚠️ `혜택` th: 표준 `สิทธิประโยชน์` 채택. `특별한 혜택`(bridge_title)은 문맥상 `สิทธิพิเศษ`(special)로 유지 — 변형이지 위반 아님.
- 배경: Guide Kim `Unifi LIFF(JP - Login x)` XLT 코멘트 번역(`mini_guidekim_popular_benefit`) 중 도메인어 반복 사용 + 자동 검증 P1 오탐 → 용어집 보완(게이트 d-1).
- 비고: `K-컬쳐→K-컬처` 표기 통일(외래어 표기법)은 번역/엑셀/위키에서 직접 반영. `K-컬처`는 en/zh/th에서 `K-Culture`(라틴) 유지라 terminology 항목으로는 추가하지 않음.

### v2.3 (2026-06-26)
- **term 추가** `포이카츠`: ko `포이카츠` / ja `ポイ活` / en `point activity` / zh `點數活動` / th `การสะสมแต้ม`
  - ja `ポイ活` **확정** (`point_mission`의 `ポイ活ミッションで最大100万円` 참고, 2026-06-26 사용자 승인).
  - en `point activity` / zh `點數活動` / th `การสะสมแต้ม` 는 **잠정값으로 채택**(2026-06-26 사용자 승인). 추후 더 적절한 표준 발견 시 갱신.
- **exception 추가** `*Unifi mini*` (id 9, brand): 전 언어 `Unifi mini` 유지
- 배경: `포이가츠→포이카츠` 표기 표준화 + `Unifi Mini→Unifi mini` 브랜드 표기 통일에 맞춘 용어집 반영. (XLT: `point_mission` ko, `bridge_notice` 전 언어 동시 갱신)

### v2.2 (2026-06-26)
- **term 추가** (4): `미션`(mission/ミッション/任務/ภารกิจ), `리워드`(reward/リワード/獎勵/รางวัล), `캐시백`(cashback/キャッシュバック/現金回饋/เงินคืน), `회원가입`(sign up/会員登録/註冊/สมัครสมาชิก)
- 배경: GuideKim 파일 검증 중 도메인 용어가 반복 사용되고 정상 의역이 P1로 반복 오탐 → 용어집 보완(게이트 d-1).

### v2.1 (2026-05-13)
- 초기 버전 (guide.md 기반 JSON). terms 25, exceptions 8 (PIN/API/URL/Apple/Google/USDT/IDRP/JPYC).

---

## 기재 규칙
- 각 항목: **버전·날짜·terms 수·exceptions 수·변경 요약** + 상세(추가/수정/삭제된 term·exception의 ko + 5개 언어, 배경).
- 버전은 의미 변경마다 올린다(term/exception 추가·수정·삭제). 표기만 미세 수정해도 기록.
- 잠정·검토 필요 값은 ⚠️로 표시한다.

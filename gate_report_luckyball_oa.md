# 번역 검증 리포트 — 럭키볼 캠페인 OA 섹션 (LINE 공식계정 메시지, 키 미부여)

- 대상 위키: pageId=4479306980 · 팀 **LV**
- 범위: OA 섹션 4개 프레임(63411-26511·26901·26708·27094)의 XLT 코멘트 문구
- **특이사항(사용자 지시)**: XLT 코멘트 문구에 **XLT 키를 부여하지 않고 다국어 번역만** 진행 → XLT 엑셀·전역 키 표에 넣지 않고, 위키 OA Screen 행에 **키 없는 번역표(No|KR|JA|EN|TH|ZH-TW)**로 문서화. 키가 없으므로 **변수화 미적용**(리터럴 유지).
- 고유 문구: **9개** (중복 문구 통합, 동일 문구 동일 번역 — 당첨금 안내/홈으로 가기/확인하기가 3개 프레임 공통)

## 검토 범위 (전수 점검 명시)
OA 4개 프레임의 XLT 문구 **고유 9개 전체(전 행) × 5개 언어를 한 줄씩 전수 수동 검토**. 신규/변경 한정 아님. 정책 코멘트 없음(4개 프레임 모두 XLT 마커만).

## 한국어 원문 교정 (1a) 및 alias
- 교정: `지급 되었어요`→`지급되었어요`(#1, 지급되다 붙여쓰기), ` `(U+2028)→`\n`(#1·#2).
- alias(Figma 원문→번역표 항목) 9건 기록(scratchpad trans.json orig 필드). Figma 원문 `가입완료`·`친구초대`(붙임)는 라벨 스타일로 유지, 오타 아님.
- 키를 부여하지 않으므로 XLT Key alias는 없음(문구 자체가 식별자).

## Executive Summary (자동 검증 — validate_translation.py, 9항목)
| 심각도 | 건수 | 판정 |
|--------|------|------|
| 🔴 P0 | 0건 | 없음 |
| 🟡 P1 | 3건 | 전건 오탐 (oa `확인하기` → 용어집 `확인`=검증 의미 매핑, 버튼 확인=Confirm/確認/ยืนยัน는 정상) |
| 🟢 P2 | 2건 | 전건 오탐 (완전 문장이라 마침표 있음이 정상 — 안내문·지급 안내) |

## 수동 3단계 체크표 (전수 9 × 5언어)
### 1단계: 한국어
- 오타·띄어쓰기: `지급되었어요` 교정 반영. 나머지 정상. 외래어 음차 오염·붙은 띄어쓰기 누락 없음.
### 2단계: 용어집
- `럭키볼`=Lucky Ball/ラッキーボール/幸運球/ลูกบอลนำโชค 일관(v2.6). JPYC 예외 유지. `0x8442...7c8`(마스킹 지갑주소)·`25JPYC`는 전 언어 원문 유지(번역 대상 아님). zh 번체 정자, 간체 0건.
### 3단계: 다른 언어
- 빈칸 0. 언어 혼입 0(ja/zh/th 잔여 라틴 없음, JPYC/Unifi/LINE/0x8442..는 의도적 유지). 컬럼 swap 없음. ja 직역 없음. 동일 문구(5·6·7) 3개 프레임 간 번역 일치.

## 검증기 보고 각 건 처리 판정
- P0 0건. P1 3건(oa_tmp_7 확인하기): **오탐** — 버튼 "확인하기"는 승인(Confirm/確認/ยืนยัน) 의미로 용어집 검증어(check)와 다름. 현행 유지. P2 2건: **오탐** — 완전 문장의 마침표는 정상. 유지.

## 통과 판정
- **P0 = 0건 (자동 + 수동)** → 위키 OA 섹션 반영 진행 가능. P1/P2 전건 오탐(유지).

## 추가 개선·제안 (권장 — 사용자 결정)
- **(d-1) 용어집**: `확인(버튼)=OK/Confirm` 의미 등록 권장(반복 오탐). `럭키볼` v2.6 반영됨.
- **(d-2)**: OA 메시지의 동적값(당첨금 `25JPYC`, 지갑주소 `0x8442...7c8`, 럭키볼 `1개`, `최대 5분`)은 키 미부여 방침에 따라 리터럴로 문서화. 추후 XLT 키·변수화가 필요해지면 별도 진행. `가입완료`·`친구초대` 붙여쓰기는 라벨 표기로 유지(디자이너 확인 시 조정 가능).

---

## 추가 교정 (2026-07-24) — 사용자 지시 띄어쓰기 교정
- `친구초대 완료!` → `친구 초대 완료!` (#1)
- `친구초대 럭키볼 당첨금` → `친구 초대 럭키볼 당첨금` (#4)
- `가입완료 당첨금` → `가입 완료 당첨금` (#8)
- 재검증(교정 3건): validate_translation.py **P0/P1/P2 = 0/0/0**. 수동 확인 — 붙은 단어 해소, 타 언어 영향 없음(en/ja/zh/th는 이미 분리 표기).
- 참고(전수 점검): 키 부여된 `친구초대 링크 복사`(mini_luckyball_copy_invite_link)도 동일 띄어쓰기 이슈가 있으나 키/엑셀 영향이 있어 사용자 확인 후 별도 처리 대상으로 보고.

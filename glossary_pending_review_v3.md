# 용어집 v3.0 반영 보류 항목 — 팀 검토 대기 리포트

> **작성일**: 2026-07-27
> **출처**: `XLT_통합_용어집_핵심용어_v3.xlsx`(101행) ↔ 용어집 API v2.6(35용어) 전수 대조
> **상태**: 아래 4건은 v3.0 일괄 반영(신규 69개)에서 **제외(보류)**. 팀 검토 후 최종 반영 여부 결정 시 이 문서를 기준으로 처리한다.

---

## 0. 이번에 진행한 작업 (경위 — 다음 세션 히스토리 파악용)

**언제·무엇을**: 2026-07-27, 사용자가 `XLT_통합_용어집_핵심용어_v3.xlsx`(terminology 시트 101행)를 용어집 추가 후보로 제공 → `md/landpress.md` 절차대로 진행.

1. **원본 최신 조회**: `scripts/fetch_glossary.py`로 Landpress API 원본 조회 — v2.6, terminology 35개, exceptions 9개 (last_updated 2026-07-24).
2. **전수 대조**: 엑셀 101행 ↔ v2.6 35용어를 프로그램으로 대조.
   - **27건 완전 동일** (거래·지갑·미션·포이카츠 등) → 추가 불필요.
   - **73건 신규** → 이 중 품질 문제 없는 **69건만 v3.0에 반영**.
   - **1건 값 충돌** (간편 비밀번호) + **3건 품질 이슈** (거래내역 변형·뽑기 계열 오역·채우기 의미) → **아래 보류 4건**.
   - 기존에만 있는 7건(송금·쇼핑·인기·혜택·럭키볼·비밀번호·채널 동의)은 그대로 유지.
3. **왜 보류했나**: `md/landpress.md` §4 기준 3(추가 전 일관성 검증) — 불일치·충돌 상태로 추가하면 새 P1 오탐/용어집 오염을 만들기 때문. 사용자 지시로 "팀 검토 후 최종 반영" 결정.
4. **산출물**: `glossary_v3.0_full.json`(보류 4건 제외 전체 JSON, terminology 104개) — 사용자가 Landpress CMS에 붙여넣어 반영. `md/glossary-changelog.md`에 v3.0 항목 기재.

## 0-1. 이후 반영 절차 (팀 검토 결과 수령 시 — 다음 세션은 이대로 수행)

**입력**: 이 리포트 + 팀 검토 결과(보류 1~4 각각에 대해 채택/기각/수정값).

1. `scripts/fetch_glossary.py`로 **현재 원본 재조회** (v3.0이 CMS에 반영됐는지 확인 — metadata.version 체크. 미반영이면 v3.0 JSON부터 재전달).
2. 검토 결과대로 보류 항목을 terminology에 추가/수정:
   - 보류 1 채택 시: `간편 비밀번호`의 en_US·th_TH **값 교체** (+ exceptions `*PIN*` 패턴 유지 여부도 함께 결정).
   - 보류 2 채택 시: `거래내역` 키 추가 (기각 시 아무것도 안 함 — `거래 내역`은 이미 등재).
   - 보류 3 채택 시: `나중에 뽑기`·`더 뽑기`를 **확정된 zh/th 값**으로 추가 (엑셀 원안·Claude 교정안 중 팀 확정값 사용).
   - 보류 4 채택 시: `채우기` 추가.
3. **metadata 갱신**: version을 v3.1로 올림, total_terms=실제 수, last_updated=반영일, description에 사유.
4. **전체 JSON 산출**(부분 조각 금지) → 사용자에게 Landpress 편집 URL과 함께 전달 → 사용자가 CMS 붙여넣기.
5. `md/glossary-changelog.md`에 v3.1 항목 기재 + git 커밋, 이 리포트의 각 보류 항목에 **처리 결과(채택/기각·날짜)** 추기.

---

## 보류 1 — `간편 비밀번호`: 기존 PIN vs 엑셀 Passcode (값 충돌)

| 언어 | 기존 v2.6 (유지 중) | 엑셀 v3 제안 |
|---|---|---|
| en_US | `PIN` | `Passcode` |
| ja_JP | `簡単パスワード` | `簡単パスワード` (동일) |
| zh_TW | `簡易密碼` | `簡易密碼` (동일) |
| th_TH | `PIN` | `รหัสผ่านแบบง่าย` |

- **판단 근거**: 공식 룰 위키 `[Rule] XLT Value 작성 가이드`(pageId=4494805808)가 **"PIN / Simple Passcode 사용 금지 → Passcode 대체"**를 명시 → **엑셀 값이 공식 룰과 일치** (Claude 권장: Passcode 교체).
- **리스크**: 기존 XLT 산출물에 `PIN` 표기가 있다면 교체 시 일괄 정리 필요. 또한 exceptions에 `*PIN*` 패턴이 있어 (표기 검증용) 함께 재검토 필요.
- **현재 상태**: v3.0에는 **기존 PIN 값 그대로 유지**.

## 보류 2 — `거래내역`(붙여쓰기 변형): 이중 등재 여부

| 키 | en_US | 비고 |
|---|---|---|
| `거래 내역` (기존 등재) | `transaction history` | v2.6부터 존재 |
| `거래내역` (엑셀 신규) | `Transaction History` | 띄어쓰기·대소문자만 다른 변형 |

- **문제**: 같은 용어의 띄어쓰기 변형 이중 등재 → 용어집 오염 + 검증기 이중 매칭 (`md/check.md` B-6 띄어쓰기 일관성).
- **Claude 권장**: `거래 내역`으로 표기 통일, 변형 미등재. 실제 서비스 파일에 두 표기가 별도로 굳어져 있으면 등재 가능(단, 오탐 소지).
- **현재 상태**: v3.0 **미등재**.

## 보류 3 — `나중에 뽑기`·`더 뽑기`: zh/th "그리기" 오역 의심

| 키 | zh_TW 엑셀 | th_TH 엑셀 | 문제 |
|---|---|---|---|
| 나중에 뽑기 | `稍後畫` | `วาดในภายหลัง` | `畫`/`วาด` = "그림을 그리다" — 추첨(draw) 의미 아님 |
| 더 뽑기 | `畫更多` | `วาดเพิ่มเติม` | 동일 오역 |
| (참고) 뽑기 | `抽獎` | `จับรางวัล` | 같은 엑셀 내 **정상** 표기 — 내부 불일치 |

- **Claude 교정안**: 나중에 뽑기 → zh `稍後抽獎` · th `จับรางวัลภายหลัง` / 더 뽑기 → zh `再抽更多` · th `จับรางวัลเพิ่ม` (ja `後で引く`·`もっと引く`는 くじを引く 관용으로 정상).
- **현재 상태**: v3.0 **미등재** (두 키 모두).

## 보류 4 — `채우기` = Receive/受け取る/接收/รับ: 의미 대응 확인 필요

- 엑셀: `채우기` → en `Receive`, ja `受け取る`, zh `接收`, th `รับ`
- **문제**: "채우기"(fill/top-up)와 "받기"(receive)의 의미 불일치 의심. 같은 엑셀의 `넣기`=`Deposit`과의 역할 구분도 불명확.
- **확인 필요**: 실제 서비스 UI에서 "채우기" 버튼이 수신(Receive) 기능인지 서비스 화면 기준으로 확인.
- **현재 상태**: v3.0 **미등재**.

---

## 참고 — v3.0에서 교정 반영한 객관적 오류 (보류 아님)

| 항목 | before | after | 근거 |
|---|---|---|---|
| `내 자산` zh_TW | `資産` (U+7523 일본식 이형자) | `資產` (U+7522 대만 표준) | `md/check.md` 2단계-C. 같은 엑셀의 자산·총 자산·락업 자산은 모두 표준 `產` |

## 참고 — 유의 등재 항목 (반영됨, 특성만 기록)

- `입금 (거래내역 유형 라벨)` = Received / `출금 (거래내역 유형 라벨)` = Sent — 괄호 주석 포함 컨텍스트 키. 일반 `출금`=withdraw(액션)와 다른 맥락(거래내역 유형)을 구분하기 위한 문서용 항목으로, 검증기 자동 매칭은 되지 않는다.

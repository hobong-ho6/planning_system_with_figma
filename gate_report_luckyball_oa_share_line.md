# 번역 품질 게이트 리포트 — OA `LINE으로 공유하기` MSG + 키 정리(삭제·통일) + 용어집 v3.6

- **대상**: 위키 pageId `4479306980` OA 섹션 `LINE으로 공유하기` 행 / 전역 다국어 표 / `xlt/xlt_output_LV_20260727.xlsx`
- **작업일**: 2026-07-28
- **담당 FE 팀**: **LV** (OA 메시지는 `md/OA.md` 규칙 — **XLT 키 미부여, 번역만**. 엑셀·전역 키 표에 넣지 않음)
- **사용자 지시(4건)**: ① `mini_luckyball_copy_invite_link` 삭제 ② `mini_luckyball_interest_title`을 `자산보전!`로 통일 ③ 용어집 보완 진행 ④ OA `LINE으로 공유하기` 한국어 3문구 다국어 번역 + OA MSG(Flex) 생성

---

## (1a) 번역 전 — 한국어 원문 교정

| 대상 | 원문 | 판정 |
|---|---|---|
| OA No.1 | 친구가 보낸 디지털 엔화 선물이 도착했어요!<br>당첨된 럭키볼 금액을 확인해보세요. | 교정 불요 — `확인해보세요`는 보조용언 붙여쓰기(허용 표기)이며 캠페인 기존 표기(`받아보세요`)와 일관 |
| OA No.2 | 가입만 해도 100% 받을 수 있어요<br>최대 50,000엔 상당 JPYC 당첨 기회 놓치지 마세요.<br>* 당첨 보상은 즉시 지급됩니다. 최대 5분까지 소요됩니다.<br>* 보상 지급일까지 Unifi LINE 공식계정 친구를 유지해야 JPYC를 받을 수 있습니다. | 교정 불요 — `놓치지 마세요` 띄어쓰기 정상(기존 OA `가입 완료시` 행의 `놓치지말아요`는 오표기였으나 이번 원문은 정상) |
| OA No.3 | 럭키볼 선물 열기 | 교정 불요 |
| `interest_title` | 최대 5% 이자 제공 및 **자산보전!** | **표기 정책 결정 반영** — 한국어 문법 표준은 `자산 보전`(띄어쓰기)이나, **사용자가 서비스 표기 정본을 붙여쓰기 `자산보전!`으로 확정**(2026-07-28). `거래내역` 사례(v3.3)와 동일한 표기 정책 결정. **alias 기록**: `{자산 보전! → 자산보전!}` — 이후 검증에서 오타로 오탐하지 않도록 유의 |

- **각주 2줄 재사용**: `* 당첨 보상은 즉시 지급됩니다…`·`* 보상 지급일까지…`는 기존 OA `가입 완료시` 행과 **완전히 동일한 문구**이므로 `md/OA.md` 규칙 1(같은 문구=같은 번역)에 따라 **기존 4개 언어 번역을 그대로 재사용**했다(신규 번역 아님).

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py` 실제 실행 (2회)

**① LV 엑셀(키 삭제·통일 반영 후, 73키)**
```
🔴 P0 0건 / 🟡 P1 110건 (전부 [용어 불일치]) / 🟢 P2 26건 (전부 [마침표 스타일])
```
- 직전(74키) 대비 P1 111 → 110: `copy_invite_link` 삭제로 1건 감소. **P0 = 0.**

**② OA 3문구 (전역 표·엑셀에 넣지 않으므로 임시 엑셀로 검증)**
```
🔴 P0 0건 / 🟡 P1 7건 / 🟢 P2 0건
```
- **P0 = 0 — 통과 기준 충족(양쪽 모두).**

---

## (b) 수동 3단계 체크표 (md/check.md) — OA 신규 3문구 전 컬럼

> OA 번역은 엑셀에 들어가지 않으므로 **전체 행 전수 검증은 LV 엑셀(73키) 자동 검증 + 아래 (c) 전건 판정**으로 수행하고, 수동 체크표는 이번 신규 3문구를 전 컬럼 검토한다. `interest_title`은 값 통일이라 5개 컬럼을 아래 별도 표로 검토했다.

### 1단계: 한국어

| No | ko_KR | 판정 |
|---|---|---|
| 1 | 친구가 보낸 디지털 엔화 선물이 도착했어요! / 당첨된 럭키볼 금액을 확인해보세요. | ✅ 맞춤법·띄어쓰기 정상, `\xa0` 없음 |
| 2 | 가입만 해도 100% 받을 수 있어요 / 최대 50,000엔 상당 … (각주 2줄 포함) | ✅ 숫자·단위 표기(`50,000엔`, `5분`) 일관, 각주 `*` 형식 기존 OA와 동일 |
| 3 | 럭키볼 선물 열기 | ✅ 버튼 라벨(마침표 없음) |

### 2단계: 용어집 (라이브 v3.5 → 이번 작업으로 **v3.6** 산출)

| 용어 | 등재값 | 적용 | 판정 |
|---|---|---|---|
| 럭키볼 | Lucky Ball / ラッキーボール / ลูกบอลนำโชค / 幸運球 | 3문구 전부 등재값 | ✅ |
| 당첨 / 당첨금 | win·当選·中獎·ถูกรางวัล / prize·当選金·獎金·เงินรางวัล | `당첨된 …금액`→`当選した…金額`·`中獎金額`·`ถูกรางวัล` | ✅ |
| 친구 / 선물하기 | (v3.6 신규) | `友だち`·`好友`·`เพื่อน`, `プレゼント`·`送`·`มอบ` | ✅ |
| 엔화 | 미등재 (기존 표기 준용) | `円`·`yen`·`เงินเยน`·`日圓` — `jpyc_value`·`jpyc_desc`와 동일 | ✅ |
| JPYC / LINE / Unifi | exceptions(미번역) | 5개 언어 원문 유지 | ✅ |

### 3단계: 다른 언어 자연스러움·표기

| No | 언어 | 판정 | 근거 |
|---|---|---|---|
| 1 | ja_JP | ✅ | `友だちから贈られた…届きました！` 자연, `デジタル円`은 `jpyc_desc`의 `日本円` 표기와 정합 |
| 1 | en_US | ✅ | `A digital yen gift … has arrived!` 관사·시제 정상 |
| 1 | th_TH | ✅ | `เงินเยนดิจิทัล`(디지털 엔화) 정확, `ถูกรางวัลเท่าไร`(얼마 당첨) 자연 |
| 1 | zh_TW | ✅ | 번체 정자(`數位日圓`·`幸運球`·`中獎金額`), 간체·이체자 없음 |
| 2 | ja_JP | ✅ | `登録するだけで100%もらえます` 자연, 각주는 기존 검증본 재사용 |
| 2 | en_US | ✅ | `100% guaranteed to receive one` — ‘가입만 해도 100%’ 의미 보존 |
| 2 | th_TH | ✅ | `แค่สมัครก็รับได้ 100%` 구문 정확, `ลุ้นรับ`(당첨 기회) 자연 |
| 2 | zh_TW | ✅ | `只要註冊就 100% 可領取` 자연, 숫자·단위 사이 공백 규칙 기존과 동일 |
| 3 | 4개 언어 | ✅ | 기존 `럭키볼 선물 오픈하기✨`(가입 완료시) 번역과 동일 계열 — `✨` 없는 원문에 맞춰 이모지 제거 |

**`interest_title` 통일 후 5개 컬럼 검토**

| 언어 | 값 | 판정 |
|---|---|---|
| ko_KR | 최대 5% 이자 제공 및 자산보전! | ✅ (표기 정책 결정) |
| ja_JP | 最大5%の利息提供および資産保全！ | ✅ ko의 ‘제공’ 반영본 |
| en_US | Up to 5% interest and asset protection! | ✅ 두 행 값이 동일했음 |
| th_TH | ดอกเบี้ยสูงสุด 5% พร้อมรักษามูลค่าสินทรัพย์! | ✅ 두 행 값이 동일했음, `สินทรัพย์` 표기 정본 |
| zh_TW | 提供最高 5% 利息及資產保全！ | ✅ `資產`(U+7522 대만 표준) 확인 |

- 컬럼 정렬 검증(check.md 4-1): OA 3문구·`interest_title` 모두 언어 칸 회전 어긋남·swap 없음. 이질 문자체계 검출 0건.
- **키 삭제 무결성**: `copy_invite_link` 삭제 전, 위키 전 화면 Screen 표에서 이 키의 잔존 참조가 **0건**임을 확인(34291 No.3이 `invite_me_too_line`으로 교체되면서 고아화). 다른 키·번역은 손대지 않음.

---

## (c) 검증기 보고 전건 처리 판정

**① OA 3문구 — P1 7건 전건 (P0·P2 0건)**

| # | 키/문구 | 지적 | 처리 판정 |
|---|---|---|---|
| 1 | No.1 en_US | `당첨`→`win` 권장 | **오탐** — 실제 값은 과거형 `won`(“your Lucky Ball won”) |
| 2 | No.1 en_US | `확인`→`check` 권장 | **오탐** — 문두 대문자 `Check`. 검증기 대소문자 구분 |
| 3 | No.1 th_TH | `확인`→`ตรวจสอบ` 권장 | **오탐(정상 의역)** — `มาดูกันว่า…`(직접 보게 하는 구어체)가 푸시 메시지에 자연스러움 |
| 4 | No.1 zh_TW | `확인`→`查看` 권장 | **오탐(정상 의역)** — `確認`은 표준 표기이며 기존 OA `확인하기→確認`과 일관 |
| 5 | No.2 en_US | `가입`→`Sign up` 권장 | **오탐** — 문중 소문자 `sign up`. 대소문자 구분 |
| 6 | No.2 th_TH | `당첨`→`ถูกรางวัล` 권장 | **오탐(정상 의역)** — `ลุ้นรับ`(당첨을 노리다)이 ‘당첨 기회’ 의미에 정확 |
| 7 | No.2 en_US | `최대`→`Max` 권장 | **오탐(정상 의역)** — 문장 내 `up to`가 표준 |

**② LV 엑셀 73키 — P1 110건 / P2 26건**

| 심각도 | 건수 | 처리 판정 |
|---|---|---|
| P1 | 110 | **전건 오탐(정상 의역)** — [용어 불일치] 유형. 당첨·최대·확인·완료·뽑기·가입·리워드 지급·공식 계정·종료·미션·복사·송금·변경 등, 용어집 대표 표기 1개와의 문자열 비교에서 발생하는 문맥 활용형·대소문자·버튼 관용역. `interest_title` 관련 2건(`최대`→`Max`/`最多`)도 동일 오탐(`Up to`/`最高`가 표준). |
| P2 | 26 | **전건 오탐/정책 유지** — [마침표 스타일]. 설명문=마침표 / 버튼·타이틀=마침표 없음 정책에 일치. |

**통과 판정: P0 = 0 (자동·수동, OA·엑셀 양쪽) → 위키 반영·엑셀 재첨부·Flex 첨부 진행.**

---

## (d) 추가 개선·제안

### (d-1) 용어집 보완 — **이번에 진행 완료(산출물 전달)**: v3.5 → **v3.6 (111 terms)**

사용자 승인에 따라 4종을 추가한 **전체 JSON**을 산출했습니다(`glossary_v3.6_full.json`). Landpress CMS 붙여넣기는 사용자가 수행합니다.
편집 URL: `https://landpress-content-v2.linecorp.com/projects/wdmwbfuv10x39bukv58ocevp/content/collections/web3_xlt_json/items?env=main`

| 용어 | en_US | ja_JP | zh_TW | th_TH | 일관성 검증(LV 73키 + OA 3문구 전수) |
|---|---|---|---|---|---|
| 링크 | link | リンク | 連結 | ลิงก์ | 2행 **4개 언어 100% 일치** |
| 친구 | friend | 友だち | 好友 | เพื่อน | 15행 **4개 언어 100% 일치** |
| 선물하기 | gift | プレゼント | **送** | **มอบ** | 2행 100% — 실사용이 동사형(`送`·`มอบ`)이라 최초 제안값(`送禮`·`มอบของขวัญ`)을 **실사용 표기로 교정 후 등재** |
| 초대 | invite | 招待 | 邀請 | **เชิญ** | 9행 — ja/en/zh 일치(zh는 `邀請` 7 + `受邀` 2 수동형). **th만 `เชิญ`(5)/`ชวน`(4) 혼용** → 다수·명사형 기준 등재, `ชวน`도 정상 의역 허용 |

### (d-2) 남은 확인 권장

1. **th `초대` 표기 통일(`เชิญ` vs `ชวน`)** — 현재 혼용. 통일할지, 격식/구어 문맥별 혼용을 유지할지 결정 필요(현재는 혼용 허용으로 등재).
2. **OA `LINE으로 공유하기` 행의 `Flex MSG` 줄 부재** — 다른 OA 3개 행(피초대자 가입완료시·럭키볼 뽑았을때·가입 완료시)에는 번역표 마지막에 `Flex MSG` 행(푸시 미리보기 문구)이 있는데 이 행에는 없습니다. **새 카피를 임의로 만들지 않고 비워 뒀습니다** — 필요하시면 문구를 알려주시거나, No.1 첫 줄(`친구가 보낸 디지털 엔화 선물이 도착했어요!`)을 그대로 쓰도록 지시해 주세요.
3. **OA 행 이미지 셀 비어 있음** — `LINE으로 공유하기` 행의 Screen(이미지) 셀이 비어 있습니다(다른 OA 행은 스크린샷 첨부). Figma 프레임이 있으면 알려주시면 어노테이션해 첨부하겠습니다.
4. **버튼 label 길이** — LINE 문서 기준 uri action label 권장 한도(20자)를 en(25)·th(22)가 넘습니다. 다만 기존 검증 통과본(`가입 완료시` en 26·th 23)과 같은 수준이라 그대로 두었습니다. 잘림이 확인되면 짧은 라벨로 조정하겠습니다.
5. **`친구한테` vs `친구에게`** — 미결(캠페인 전반 `친구한테`로 통일 중).

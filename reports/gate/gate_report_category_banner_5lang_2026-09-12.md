# 게이트 리포트 — category_promotion_banner 5개 언어 (2026-09-12)

- **대상**: LV `category_promotion_banner.contents.title` — 카테고리 1 상단 프로모션 배너 문구
- **원문**: LV beta 실등록값 `바우처 전 상품 최대 15% 할인 진행중!`(ko_KR 1건). 나머지 4개 언어를 신규 번역했다.
- **검증 범위**: **전체 행 1건 전수 점검**. 이 필드의 전부다(`on_off`는 boolean이라 번역 대상 아님).
- **엑셀**: `xlt_output_20260912142129.xlsx`

---

## (1a) 한국어 원문 교정

교정 사항 **없음**. 등록된 원문을 그대로 썼고 alias도 없다.

| 확인 항목 | 판정 |
|---|---|
| 맞춤법·띄어쓰기 | 정상 (`전 상품` · `진행중`) |
| 수치 `15%` | 할인율이며 캐시백(10%)과 **별개 값**이다 — (d-2) 확인 대상 |

---

## (a) 자동 검증 결과

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | 2건 |
| 🟢 P2 | 0건 |

**통과 기준 P0 = 0 충족.**

---

## (b) 수동 3단계 체크표 — 전체 행 1건 전수

### 1단계 · 한국어(ko_KR)

| 키 | 판정 | 근거 |
|---|---|---|
| `contents.title` | 정확 | 붙은 띄어쓰기·음차 오염·다의어 모호 없음 · 느낌표 1개 |

### 2단계 · 용어집(v5.4) 대조

| 용어 | 정본 | 적용 | 판정 |
|---|---|---|---|
| 바우처 | ja `クーポン` · en `Voucher` · th `เวาเชอร์` · zh `優惠券` | 전 언어 정본 적용 | 정확 |
| 최대 | ja `最大` · en `Max` · th `สูงสุด` · zh `最多` | ja `最大` · th `สูงสุด` 정본 / en `Up to` · zh `最高` | (c) 오탐 판정 |

### 3단계 · 언어별

| 언어 | 점검 | 판정 |
|---|---|---|
| ja_JP | 전각 느낌표 `！` 사용(일본어 관행) · `15%` 반각 숫자 | 정확 |
| en_US | 배너 문구라 문장부호 `!` 유지 · 관사 없이 간결 | 정확 |
| th_TH | `ลดสูงสุด 15%` 어순 자연 · 띄어쓰기 규칙 준수 | 정확 |
| zh_TW | **번체** 유지(`優惠券`·`折扣`) · 간체 0 · 숫자 앞뒤 공백 | 정확 |
| 전 언어 | 수치 `15%` 5개 언어 모두 동일 · placeholder 없음 | 정확 |

---

## (c) 건별 처리 판정

### P0 — 0건

### P1 2건

| 키 | 권장 | 판정 |
|---|---|---|
| en_US `최대` → `Max` | — | **오탐** — 배너 문구에서 `Up to 15% off`가 자연스러운 영어다. `Max`는 수치 라벨용 표기 |
| zh_TW `최대` → `最多` | — | **오탐** — 할인율에는 `最高`가 표준이다. `最多`는 수량에 쓴다 |

### P2 — 0건

---

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

### (d-1) 용어집 보완

`할인`이 미등재다. 이번 배너와 가이드 페이지 CTA(`최대 15% 할인`)에서 반복 사용된다. 사용자 지시대로 전체 번역 완료 후 일괄 등재하며, 권장 표기는 실사용 실측 후 제출한다.
또한 `최대`의 zh 정본이 `最多`로만 등재돼 있어 **할인율 문맥에서 반복 오탐**이 난다 — 문맥별 표기(`最高`) 추가를 함께 검토 권장.

### (d-2) 추가 개선 제안

1. **`15%` 리터럴** — 할인율이 문구에 박혀 있다. 캐시백(10%)과 마찬가지로 값이 바뀌면 5개 언어를 모두 고쳐야 한다. 또한 **캐시백 10%와 혼동되지 않는지** 확인 권장
2. **`on_off` 운영** — 현재 `true`다. 배너를 내릴 때 `false`로만 바꾸면 되고 문구는 5개 언어가 그대로 남는다

---

## 결론

- **P0 = 0건**(자동 + 수동) → 산출물 출력 가능
- P1 2건은 모두 오탐 판정, 조치 없음
- (d) 권장 사항은 사용자 결정 전까지 미적용

# 번역 검증 리포트 — 럭키볼 캠페인 3개 섹션 (Unifi mini / 초대자 / 피초대자, LV)

- 대상 위키: pageId=4479306980 (럭키볼 친구에게 선물하기 캠페인) · 팀 **LV** (치환자 `{0}`)
- Figma 파일: GOCHAYBS7hIrmWRGNuJOKV / 프레임 15개(XLT 마커 보유 13개, 마커 0개 2개 제외)
- 모드: 코멘트 선별(XLT 마커, 답글 스레드 포함) + 위키 Mode B(3개 h4 섹션 행 추가)
- 대상 키: **62개 고유 문구** (같은 문구=같은 키 통합, #1은 기존 위키 키 `mini_luckyball_invite_banner` 재사용)
- 변수화(사용자 확정 #5·11·38·39·48): `{0}` 5개 키

## 검토 범위 (전수 점검 명시)
XLT 코멘트가 가리키는 **선별 문구 전체(62개 고유 문구)**가 대상이며, **전체 행 × 5개 언어(ko_KR/ja_JP/en_US/th_TH/zh_TW)를 한 줄씩 전수 수동 검토**했다. 신규/변경 키로 한정하지 않고 전체 대상. XLT 마커 0개 프레임 2개(`63411-34514 뽑기,더뽑기`, `63411-38544 피초대자 화면`)는 대상 제외로 보고.

## 한국어 원문 교정 (1a) 및 alias
`md/check.md` 1단계로 원문 교정 수행. 사용자 승인(교정 후 번역 + alias + 디자이너 보고). 교정 항목:
- 오타: `소셜 게정`→`소셜 계정`(#61), `지갑 생성 포)`→`지갑 생성 포함)`(#29)
- 띄어쓰기: `LINE계정외에`→`LINE 계정 외에`(#21), `정보가변경된`→`정보가 변경된`(#31), `5%이자`→`5% 이자`(#56·57), `스테이블 코인USDT리워드`→`스테이블 코인 USDT 리워드`(#59), `Unifi내`→`Unifi 내`(#59), `Unifi서비스`→`Unifi 서비스`(#61), `추가시`→`추가 시`(#49), `서비스 인가요`→`서비스인가요`(#53), `복사 되었어요`→`복사되었어요`(#37), `20명을 초과 하더라도`→`초과하더라도`(#23)
- 비표준 문자: ` `(LINE SEPARATOR)→`\n`(#1·23·44), `\xa0`(NBSP)→일반 공백/제거(#25·27·29·30·34), 중복 공백 정리(#10·57·61)
- 변수화: #5·11·38·39·48의 리터럴 값 → `{0}`
- **alias 기록**: `translation_data.json`의 `aliases`에 `{Figma 원문(정확) → XLT Key}` 62건 전부 보존. Figma 원문 오타(게정·포)·정보가변경 등)는 **디자이너에게 원본 수정 요청** 대상으로 안내.

## Executive Summary (자동 검증 — validate_translation.py, 62개 항목)
| 심각도 | 건수 | 판정 요약 |
|--------|------|----------|
| 🔴 P0 (Critical) | 0건 | 빈칸·오타·언어혼입·표기위반 없음 |
| 🟡 P1 (Medium) | 24건 | **전건 오탐** — 용어집 매핑이 좁아 정상 의역/문맥/대소문자/활용형을 오검출 |
| 🟢 P2 (Low) | 26건 | **전건 오탐** — 타이틀·버튼·배너·CTA의 마침표 미사용(정상) |

자동 `P0=0`은 시작점. 아래 수동 3단계로 전수 확인.

## 수동 3단계 체크표 (전수 — 62행 × 5언어)

### 1단계: 한국어 맞춤법·띄어쓰기·표현
| 점검 | 결과 |
|---|---|
| 오타 | 게정·포)·정보가변경 등 교정 완료(위 1a) |
| 띄어쓰기(붙은 단어·의존명사) | 5%이자·LINE계정외·추가시·Unifi내 등 교정, 붙은 단어(누리고다양한류) 없음 |
| 비표준 공백/줄바꿈 | `\xa0`·` `·중복공백 전량 제거/정규화 |
| 외래어 음차 오염(포이가츠류) | 없음 |
| 표현 어색(미션하고류) | 없음 — 캠페인 카피로 자연스러움 |
| 조사/종결 일관성 | 안내문 합쇼체(-습니다), CTA/타이틀 명사형 일관 |

### 2단계: 용어집 위반
| 점검 | 결과 |
|---|---|
| 한국어 키워드 매칭 | 24건 P1은 전부 오탐(아래 각 건 판정). JPYC·USDT·PIN 등 예외 표기 정상 유지 |
| zh_TW 번체 정자 | `資產`=`產(U+7522)` 정자 사용, 잘못된 `産(U+7523)` 0건. 邀·惠·注는 정자·간체 공통 문자(간체 아님). 간체/이체자 0건 |
| th_TH 자산 표기 | `สินทรัพย์` 일관 |
| 도메인 용어 일관성 | 럭키볼=Lucky Ball/ラッキーボール/幸運球/ลูกบอลนำโชค 전 문구 일관 |

### 3단계: 다른 언어 검토
| 점검 | 결과 |
|---|---|
| 빈칸 | 62행 × 5언어 전부 채움(0건) |
| 언어 혼입 | ja/zh 잔여 라틴 0건. th의 K-Beauty/K-Culture는 의도된 고유표기. JPYC/USDT/Unifi/LINE/MUSINSA/Dapp Portal/Bot은 브랜드·기술어(허용) |
| placeholder `{0}` | 변수 5개 키(#5·11·38·39·48) 모두 5개 언어에 `{0}` 정합 |
| 컬럼 swap | 각 언어 칸에 올바른 언어 |
| ja 한국어 직역(出席류) | 없음 — 자연 일본어(です·ます/명사형) |
| 마침표 일관성 | 안내 문장 마침표, 타이틀·버튼 미사용으로 유형별 일관 |

## 검증기 보고 각 건 처리 판정
### P1 24건 — 전건 **오탐**(실제 위반 아님)
용어집이 특정 한국어 키워드를 특정 대역어로만 매핑해, 문맥이 다른 정상 번역을 오검출. 유형별:
- **버튼 `확인`→OK/確認/確認/ตกลง**(confirm 등): 용어집 `확인`=검증(check/查看/ตรวจสอบ) 의미. 버튼 확인은 승인(OK) 의미로 대역어가 다름 → 오탐, 현행 유지.
- **동사 `확인`(확인하다)**: en verify / zh 確認 / th ดู 등 정상 동의어 → 오탐, 유지.
- **`변경`→zh `更換` 권장**(info_change·wallet_fixed·fcfs): 정보/주소/정책 변경은 `變更`이 정확(`更換`은 물리적 교체) → 오탐, `變更` 유지.
- **`복사`→copy / `로그인`→log in / `미션`→mission**: 영어 대역어가 이미 존재(대문자 Copy/Log in/Mission). 대소문자 차이 오검출 → 오탐, 유지.
- **`송금`→en send / zh 轉帳 / th ส่ง**(jpyc_desc): 국경간 송금·환전 문맥에서 transfer/`匯款`/โอน는 정상 대역 → 오탐, 유지(용어집 확장 권장, d-1).
- **`예치`→ja `預入`**(interest_desc): ja `預け入れ/預けて`는 `預入` 활용형 → 오탐, 유지.
### P2 26건 — 전건 **오탐**(스타일)
타이틀·버튼·배너·짧은 CTA(확인, 친구초대 링크 복사, 배너 문구 등)의 마침표 미사용은 정상. 유형별 일관 유지. → 정책상 유지.

## 통과 판정
- **P0 = 0건 (자동 + 수동)** → 위키 반영·엑셀 생성 진행 가능
- P1 24건 = 전건 오탐(현행 유지), P2 26건 = 전건 오탐(현행 유지)

## 추가 개선·제안 (권장 — 사용자 결정 사안)
- **(d-1) 용어집 보완 권장**: 반복 오탐은 용어집 매핑이 좁다는 신호. 아래 도메인 용어 5개 언어 표기 추가 권장(반영은 `md/landpress.md` 절차로 Landpress CMS 붙여넣기):
  - `럭키볼`: ko 럭키볼 / en Lucky Ball / ja ラッキーボール / zh_TW 幸運球 / th ลูกบอลนำโชค
  - `확인(버튼)`: 승인 의미 OK/確認/確認/ตกลง를 검증 의미(check)와 구분 등록
  - `변경`: 정보·주소 변경 = 變更(zh) 등록(교체 更換과 구분)
  - `송금`: en transfer/remit, zh 匯款/轉帳 병기 검토
- **(d-2) 추가 개선·제안**:
  - Figma 원문 오타(소셜 게정, 지갑 생성 포), 정보가변경 등)·`\xa0`·` `은 **디자이너 원본 수정 권장**(현재 XLT는 교정본 + alias 반영).
  - 변수화 범위: 이번엔 #5·11·38·39·48만 `{0}`. 마케팅 고정값(50,000JPYC·20명·5% 등)이 실제 런타임 가변이면 추가 변수화 필요 — 확인 시 반영.
  - 마침표/구분자 정책은 유형별(안내=마침표, 타이틀/버튼=없음) 현행 일관 유지 권장.

# 게이트 리포트 — GuideKim 바우처 구매 동선 9프레임 신규 XLT (2026-08-26)

> **2판(21:55) — 최종 71키.** 파일명은 초판(64키) 기준으로 유지한다(위키 History가 이 경로를 참조). 2판 변경점:
> ① 어노테이션 통합 번호를 **Figma 코멘트 핀 순서 → 실제 텍스트 좌표 기준**으로 재산출(사용자 지적) ② **코멘트 마커와 무관하게 실제 화면 문구**를 대상으로 삼아 신규 **7키 추가**(64 → 71키) ③ 반영 대상 위키를 `4618657620` → **`4639704548`(올리브영 5만원 상품상세 Unifi 직접제공)** 으로 변경. 상세는 문서 말미 「2판 — 실제 텍스트 기준 재산출」 참조.

- **대상 Figma**: 파일 `GOCHAYBS7hIrmWRGNuJOKV` · 섹션 `상세화면`(`68594:15129`) 하위 **9프레임**
- **대상 위키(2판 최종)**: [pageId 4639704548](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4639704548) `올리브영 5만원 상품상세 Unifi 직접제공` — Screen·다국어 섹션 (초판은 `4618657620`에 반영 → 주소 오지정으로 이전)
- **담당 FE 팀**: **UIT**(사용자 확정) → XLT Key 프리픽스 `UF_` 고정 · 변수 치환자 `{{0}}`(이중 중괄호)
- **범위**: 초판 — XLT 마커 76건 → 유니크 65건 → 병합 **64키**(320셀) / **2판 — 실제 텍스트 기준으로 신규 7키 추가해 최종 71키**(355셀 전수 수동 검토, 전체 파일 대상 — 신규·변경 키 한정 아님)
- **엑셀(2판 최종)**: `xlt/xlt_fix_guidekim_voucher_71keys_20260826.xlsx`(위키 첨부와 같은 파일명 고정 · 초판 64키본은 대체됨)
- **사용자 확정 사항**: ⓐ 상품별 데이터로 보이는 9건도 XLT에 포함 ⓑ 변수화 6건 적용(캐시백률 4·결제금액 1·결제일시 1) ⓒ Screen ID = Figma 프레임명 ⓓ 프레임명↔코멘트 불일치 2건은 **코멘트 기준으로 이름 정정**

---

## (1a) 한국어 원문 교정 (번역 전 필수)

Figma 원문과 달라진 항목은 아래와 같다. **디자이너에게 원본 수정을 요청할 목록**이기도 하다.

| # | XLT Key | Figma 원문 | 교정 후(등록 기준) | 사유 | alias |
|---|---|---|---|---|---|
| 1 | `UF_voucher_terms_pay_title` | `바우처 수령을 위해`+**U+2028**+` Unifi 친구 추가가 필요해요` | `바우처 수령을 위해\nUnifi 친구 추가가 필요해요` | U+2028(LINE SEPARATOR) 혼입 → 개행(`\n`)으로 정규화 | `{U+2028 → \n}` |
| 2 | `UF_voucher_terms_wish_title` | `관심 상품 소식을 받기위해`+**U+2028**+`…` | `관심 상품 소식을 받기 위해\n…` | U+2028 + **`받기위해` 띄어쓰기 오류** | `{받기위해 → 받기 위해}` |
| 3 | `UF_voucher_terms_wish_desc` | `…보내드리기 위해`+**U+2028**+`…` | `…보내드리기 위해\n…` | U+2028 혼입 | `{U+2028 → \n}` |
| 4 | `UF_voucher_terms_pay_desc` | `바우처 전송을 위해 Unifi **친구추가**가 필요해요` | `… Unifi **친구 추가**가 필요해요` | 같은 화면에서 `친구 추가`/`친구추가` 혼용 — 용어집 `친구`(등재값) 및 등록값 `친구 추가`(실측 38건)에 맞춰 통일 | `{친구추가 → 친구 추가}` |
| 5 | `UF_voucher_info_refund` | 결제페이지 `취소**･**환불`(U+FF65 반각) / 상세페이지 `취소**·**환불`(U+00B7) | `취소·환불`(U+00B7) | 같은 문구가 두 화면에서 다른 구분자 → 가운뎃점으로 통일. **두 화면이 같은 키를 공유**하게 됨 | `{취소･환불 → 취소·환불}` |
| 6 | `UF_voucher_pay_step3` | `구매한 바우처는␣␣**My쇼핑**에서…` | `구매한 바우처는 **MY쇼핑**에서…` | **이중 공백** 제거 + 등록값 `mini_guidekim_my_shopping`=`MY쇼핑` 표기 통일 | `{My쇼핑 → MY쇼핑}` |

**교정하지 않고 보고만 하는 항목(내용 변경이라 디자이너·기획 판단 사안)**
- `UF_voucher_howto_step2_desc`의 `MY탭>바우처`와 `UF_voucher_pay_step3`의 `MY쇼핑`이 **같은 위치를 다르게 지칭**한다. 한쪽으로 통일이 필요하나 위치 명칭 변경은 정책이므로 원문 유지.
- 프레임 코멘트 원문 `약관동의가 **안돼어** 있는 경우`(→ `안 되어`) — 코멘트(정책) 문구라 XLT 대상이 아니며, 위키 Description에는 원문 그대로 인용했다.

## 번역 결과 요약 (64키 × 5개 언어)

전체 표는 위키 「다국어 번역 (XLT Full Translation)」 섹션과 첨부 엑셀 참조. 화면별 배분: 바우처 상세 40키 · 결제 시 약관동의 5 · 찜하기 시 약관동의 2 · 결제 시 친구추가 1 · 결제페이지 14 · 결제 실패 2 · 결제 완료 6(공유 키 6건은 중복 계상).

**표기 기준 선례(실측 근거)** — UIT 키스페이스·레지스트리 실사용 건수로 확정
| 용어 | 채택 | 실측 근거 |
|---|---|---|
| 캐시백 | ja `キャッシュバック` · en `cashback` · th `เงินคืน` · zh `現金回饋` | `UF_max_cashback` 등록값과 동일 · ja `キャッシュバック` 4건 vs `還元` 6건이나 `還元`은 `mini_guidekim_`(LV) 키스페이스 한정 결정 → UF_ 키는 용어집 등재값 채택 |
| 바우처 | ja `バウチャー` · en `voucher` · th `เวาเชอร์` · zh `優惠券` | ja 5건 · th `เวาเชอร์` 3건 vs `บัตรกำนัล` 2건 · zh `優惠券` 4건 vs `憑證` 1건 |
| 포인트 | th **`คะแนน`** | th `พอยต์` **실사용 0건** vs `คะแนน` **29건** → 초안의 `พอยต์`를 정정(아래 (c) 실제 결함 #2) |
| 결제 | ja **`決済`** | ja `決済` **30건** vs `お支払い` 6건 + 용어집 등재값 `決済` → 초안의 `お支払い` 계열 9셀을 정정(실제 결함 #3) |
| 친구 추가 | ja `友だち追加` · th `เพิ่ม … เป็นเพื่อน` · zh `加入 … 好友` | ja 35건 · th `เป็นเพื่อน` 25건 · zh는 `加為好友` 6건이 다수지만 `md/guide.md` §5-1 C 사용자 확정 정본(`加入 … 好友`) 우선 — (d-1)로 등재 검토 |
| 유의사항 | ja `注意事項` · en `Precautions` · th `ข้อควรระวัง` · zh `注意事項` | `UF_lockup_caution_title` 등록값과 동일 · th `ข้อควรระวัง` 6건 vs `หมายเหตุ` 3건 |

## (a) 자동 검증 요약 — `scripts/validate_translation.py` 실제 실행

| 심각도 | 건수 | 판정 결과 |
|---|---|---|
| 🔴 P0 | **4건** | 전건 **오탐**(동일 키 1건의 4개 언어 칸) → **실제 P0 위반 0건** |
| 🟡 P1 | 66건 | 전건 판정 완료 — 실제 결함 0건(수정 3건은 이미 반영 후 재실행 결과), 나머지 오탐 |
| 🟢 P2 | 14건 | 마침표 스타일 — 정책 유지(원문 준수) |

초판(수정 전) P0 4 / P1 75 / P2 14 → **실제 결함 3건 수정 후** P0 4(오탐) / P1 66 / P2 14.

## (b) 수동 3단계 체크표 (320셀 전수)

### 1단계: 한국어 맞춤법·띄어쓰기 (64셀)
| 검사 | 결과 |
|---|---|
| 오타·맞춤법(`되요`·`됬`·`미선` 등 패턴 사전) | 발견 없음 |
| 띄어쓰기 | **`받기위해` 1건** → (1a) #2 교정 |
| 비표준 공백(`\xa0`)·이중 공백·앞뒤 공백 | **이중 공백 1건** → (1a) #6 교정 · `\xa0` 0건 |
| 제어문자(U+2028/2029) | **3건** → (1a) #1·2·3 교정 |
| 구분자 일관성(`·`/`･`) | **1건** → (1a) #5 교정·키 병합 |
| 표기 일관성(`친구 추가`/`My쇼핑`) | **2건** → (1a) #4·#6 교정 |
| `/n` 오타 | 0건 |
| 종결어미·마침표 | 원문 그대로 유지(아래 (c) P2 판정) |

### 2단계: 용어집 (v4.7 · 116용어 라이브 조회)
| 용어 | 판정 |
|---|---|
| 캐시백·바우처·포인트·결제·친구 추가·유의사항 | 위 「표기 기준 선례」 표대로 확정 — 등재값과 등록값이 갈리는 건은 실측 다수·키스페이스 선례로 판정 |
| 예외 패턴(브랜드·기술어) | `Unifi mini`·`LINE`·`MINI App`·`JPYC`·`VISA`/`Master`/`JCB`·`OLIVE YOUNG`·`KTX` 전 언어 원어 유지 ✓ |
| `deprecated_terms` | `팔로우`·`채널 추가`·`月렛`·`유저` 등 금지 표기 **0건** ✓ (친구 추가 정본 표기만 사용) |

### 3단계: 언어별 자연스러움 (256셀)
| 언어 | 점검 항목 | 결과 |
|---|---|---|
| ja_JP | です・ます体 일관 · 띄어쓰기 없음 · 숫자+단위 붙임(`{{0}}%`) · 한국어 조사 잔존 · 조사 앞 공백 | `{{0}} を支払う`의 **조사 앞 공백 1건 제거** · `決済` 통일 9셀 정정 · 그 외 이상 없음 |
| en_US | 원문 오타·겹말 · 관사·단복수 · 브랜드 대소문자 | 이상 없음(`OLIVE YOUNG` 대문자 일관) |
| th_TH | 문말 마침표 미사용 관습 · 의미 단위 띄어쓰기 · 숫자+단위 띄움 | 마침표 0건 ✓ · **포인트 표기 2셀 정정** · 그 외 이상 없음 |
| zh_TW | 번체 정자(`產`/`臺` 이형자) · 간체 혼입 · 단어 중복 | 간체 0건 · 이형자 0건 · **`回饋 {{0}}% 現金` 어순 1셀 정정** |
| ko_KR | (1단계에서 전수 완료) | 교정 6건 반영 |
| 컬럼 정렬 | 이질 문자체계 검출(회전 어긋남·swap) | 매장명 목록 1키(4셀, 의도된 한국어 유지) 외 **0건** |
| 치환자 | `{{0}}` 개수 5개 언어 일치 | 6키 전부 일치 ✓ (스크립트 강제 검사) |
| 개행 | `\n` 개수 5개 언어 일치 | 전 키 일치 ✓ |
| ja==zh 동일 셀(CJK 5자+) | 사각지대 ① 수동 보완 | 0건 |

### 2차 독립 검토 (`translation-reviewer` 서브에이전트, 분리 컨텍스트) — 완료

320셀 전수 재판정 결과: **🔴 실제 결함 12건 / 🟡 확인 필요 21건 / ⚪ 자동 검증 P0 4·P1 66·P2 14 전건 오탐 판정 동의**. 아래 「2차 검토 반영」에 건별 처리를 남겼다.

#### 2차 검토 반영 — 🔴 12건 처리

| # | 지적 | 처리 | 근거 |
|---|---|---|---|
| 1 | 매장 목록 항목 구분자가 쉼표 대신 **마침표**(`NC강남점**.** 현대백화점부산점`) | **수정**(5개 언어 5셀) | 32항목 중 유일한 마침표 — 목록 파싱 시 문장 종결로 오인 |
| 2 | `롯데마트계양점갤러리아타임월드점` 두 매장명 **구분자 없이 결합** | **수정** → `롯데마트계양점, 갤러리아타임월드점` | `누리고다양한`과 동일 유형(자동 검증기 구조적 미검출) |
| 3 | `롯데마트부평점NC강서점` 결합 | **수정** → `롯데마트부평점, NC강서점` | 위와 같음 |
| 4 | `뉴코아아울렛, 평택점`이 `뉴코아아울렛평택점`의 오분리 의심 | **미수정 — 원본 확인 필요** | 두 해석(별개 2항목 / 단일 매장명)이 모두 성립하고 목록에 `평택AK역사점`도 별도로 있다. **추측으로 병합하면 매장 정보를 왜곡**하므로 올리브영 원본 목록 대조 후 결정 |
| 5 | ko `더 많은 상품보기` 붙여쓰기 | **수정** → `더 많은 상품 보기` | 표준 띄어쓰기. 등록값 선례 없음(`상품보기`·`상품 보기` 모두 0건). (1a) alias 추가 |
| 6 | ko `문의주실` 보조용언 띄어쓰기 | **수정** → `문의 주실` | 같은 파일 `확인해 주세요`와 일관 |
| 7 | ja `{{0}} を支払う` 조사 앞 공백 | **이미 반영됨** | 검토자 스냅샷이 수정 전 버전(1차가 실측 단계에서 먼저 교정) |
| 8 | zh `支付 {{0}}` — 결제 계열 내부 불일치 | **수정** → `付款 {{0}}` | 같은 키셋 6키가 모두 `付款`. 실측은 `支付` 17건 vs `付款` 14건로 팽팽하나 **키셋 내부 일관성** 우선 |
| 9 | ja `気になる商品を保存` 번역체 | **수정** → `お気に入りに追加` | **실측 지지**: `お気に入り` 8건 vs `気になる商品` 0건 |
| 10 | zh `儲存感興趣的商品` → `加入收藏` 권장 | **미수정 — 확인 필요로 이월** | 권장값 `加入收藏` **실사용 0건** vs 현행 `儲存` **7건**. `md/check.md` 함정 2-1 「일반 규칙 ②」(실사용 0건 표기 채택 금지)에 걸린다. 버튼 길이(8자) 지적은 타당하므로 사용자 결정 사안 |
| 11 | en `Providing information to Unifi third parties`가 **동의 주체·수령자를 뒤바꿔 읽힘**(법적 의미 파손) | **미수정 — 법무·기획 확정 필요(차단 항목)** | ko 원문 `(필수) Unifi 제3자 정보 제공` 자체가 제공 주체/수령자 불명이다. **번역으로 의미를 확정하면 법적 문구를 임의 해석하는 것**이라 ko 확정 후 5개 언어 재작성해야 한다 |
| 12 | `리뷰 (33)` 리뷰 수 하드코딩 | **미수정 — 추가 변수화 승인 필요** | 승인된 변수화 6건에 미포함. (d-2) #5와 동일 건 |

#### 2차 검토 반영 — 🟡 21건 (전건 사용자 결정 사안으로 이월, (d-2)에 통합)
라벨 붙여쓰기 정책(`수령방법`·`사용방법` 등 5건) · `MY탭`↔`MY쇼핑` 위치 명칭 · 제3자 제공 문구 법무 확정 · 올리브영 약관 원문 모호 2건 · th 어순(`ยอดชำระคืน` 오독 가능) · th `ๆ` 조판 · `Master`→`Mastercard` 브랜드 정식명 · zh 대만 로컬 표기 묶음(`行動商品券`/`電子券`/`發行`/`評論`/`消費金額`) · zh 전각 괄호 · zh `優惠券+優惠` 중복 · ja `お支払い`↔`決済` 톤 · ja 문안 3건 · en 스타일 4건 · 키 리네임 근거 기록.

#### 2차 검토가 지적한 절차 사항
- **P1 66건 중 자동 리포트에 20건만 노출된다**(`validate_translation.py`가 `[:20]` 제한). 이 리포트 (c)에는 검증기 내부 목록을 직접 덤프해 **66건 전건을 유형별로 판정**해 두었다.
- **P0을 오탐 처리한 행은 더 정독해야 한다** — 실제로 그 셀 안에 ko 원문 결함 4건(위 1~4)이 있었다. 유효한 지적이며, P0 오탐 판정 시 셀 내부 정독을 절차로 삼는다.
- 매장명 목록 P0은 **「오탐」이 아니라 「정책 예외(고유명사 원문 유지)」로 기록**하라는 권고를 반영해 (c)에 그 표현을 병기한다.

## (c) 검증기 보고 건별 처리 판정

### 🔴 P0 4건 — 전건 오탐
| 건 | 판정 |
|---|---|
| `UF_voucher_detail_unavailable_value` en/ja/zh/th 칸 한글 포함(컬럼 어긋남 의심) 4건 | **정책 예외(고유명사 원문 유지) — 실제 위반 아님**(2차 검토 권고로 「오탐」 대신 이 표현 사용: 오탐으로 닫으면 다음 run에서 셀 내부를 다시 안 본다) — 올리브영 **이용불가 매장 33곳의 지점명**이다. 검출기의 목적은 컬럼 회전 어긋남 탐지이며 이 키는 정렬 정상(ko와 동일 값 의도적 유지). 현지화하면 이용자가 **현장 간판·앱의 한국어 지점명과 대조할 수 없어** 실사용 가치가 떨어진다. → 구조적 해결은 (d-2) #1(Admin 상품 데이터 이관) |

### 🟡 P1 66건 — 전건 판정
| 유형 | 건수 | 판정 |
|---|---|---|
| ko `결제` → zh `結帳` 권장 | 13 | **오탐** — 등록 실사용 `付款` 14건 vs `結帳` 3건. 채택 `付款`(다수·UI 표준) |
| ko `구매` → en `Buy` 권장 | 12+1 | **오탐** — `Purchase`는 동의어이며 문장형(`Purchase a voucher`)에 적합. 용어집은 라벨형 단일 매핑 |
| ko `결제` → th `การชำระเงิน` 권장 | 11 | **오탐** — 동사형 `ชำระเงิน`(실사용 27건) 사용. 명사/동사 품사 차이 |
| ko `완료` → th `เสร็จ` / en `Complete` 권장 | 4+2 | **오탐** — `สำเร็จ`·`completed` 사용(동일 어근·시제 차이) |
| ko `확인` → en `check` / zh `查看` / th `ตรวจสอบ` 권장 | 3+3+1 | **오탐 확정** — `md/check.md` 함정 2-1의 다의어 유형, **재제안 금지** 항목 |
| ko `혜택` → en `perks` / th `สิทธิประโยชน์` / ja `特典` 권장 | 2+2+1 | **오탐** — `혜택가`=가격 라벨이라 `Special price`·`ราคาพิเศษ`·`特別価格`가 정확. 「혜택(benefit)」 직역은 라벨 의미를 훼손 |
| ko `교환` → en `swap` / ja `スワップ` / th `แลกเปลี่ยน` / zh `交換` 권장 | 4 | **오탐(도메인 충돌)** — 용어집 `교환`은 **암호자산 스왑**이고 여기서는 **상품 교환/반품**(exchange·交換/返品). 문맥 불일치 |
| ko `유의사항` → th `หมายเหตุ` 권장 | 1 | **오탐** — `ข้อควรระวัง` 채택(실사용 6건 vs 3건 · `UF_lockup_caution_title` 선례) |
| ko `쇼핑` → th `ช้อปปิ้ง` 권장 | 1 | **오탐** — `MY Shopping`은 화면 명칭(고유 라벨)이라 원어 유지 |
| ko `결제` → ja `決済` 권장 (`UF_voucher_pay_btn`) | 1 | **오탐** — `{{0}}を支払う`는 버튼의 **동사형**. 명사 `決済`로 바꾸면 버튼 문구가 부자연 |
| `번역 누락 의심`(ja/th/zh 고유 문자 없음) | 1 | **오탐** — P0와 같은 매장명 목록 키. 검출기 설명대로 방향 미판정 항목 |

### 🟢 P2 14건 — 정책 유지
문장형 ko 14건에 마침표가 있고 일부(명사구·짧은 안내)에 없다는 지적. **Figma 원문의 마침표 유무를 그대로 따랐다**(명사구 라벨은 마침표 없음, 완결 문장은 마침표) — `md/check.md` B-7 기준에 부합. 원문 자체의 통일은 (d-2) #4로 디자이너에게 제안.

### 실제 결함으로 수정한 3건 (검증기 재실행으로 해소 확인)
| # | 키 | before → after | 발견 경로 |
|---|---|---|---|
| 1 | `UF_voucher_pay_cashback` zh | `回饋 {{0}}% 現金` → `{{0}}% 現金回饋` | 검증기 P1(용어 `캐시백` 미포함)이 어순 붕괴를 잡음 |
| 2 | `UF_voucher_howto_step3_desc`·`UF_voucher_pay_step4` th | `พอยต์` → `คะแนน` | **레지스트리 실측** — `พอยต์` 0건 / `คะแนน` 29건 |
| 3 | ja 9셀 | `お支払い` 계열 → `決済` 계열 + `{{0}} を支払う` → `{{0}}を支払う` | **레지스트리 실측**(`決済` 30건 vs `お支払い` 6건) + 조사 앞 공백 |

## (d) 추가 개선·제안 (권장 — 사용자 결정 전 임의 반영 금지)

### (d-1) 용어집 보완 권장 — 실측 건수 병기
| 용어 | 권장 표기(실측) | 근거 |
|---|---|---|
| **바우처** | ko `바우처`(5건) · ja `バウチャー`(**실측 5건**) · en `voucher`(3건) · th `เวาเชอร์`(**3건** vs `บัตรกำนัล` 2건) · zh `優惠券`(**4건** vs `憑證` 1건) | 미등재 용어인데 이번 64키 중 15키에서 사용. 표기가 두 갈래로 갈려 있어 등재 시 분기 정리 효과. 이전 게이트(08-24)에서도 이월된 항목 |
| **포인트** | ko `포인트`(36건) · ja `ポイント`(**37건**) · en `points`(17건) · th `คะแนน`(**29건**) · zh `點數`(**32건**) | 이번 작업에서 th 오표기(`พอยต์` 0건)를 실제로 유발했다. 등재하면 같은 사고를 검증기가 자동 차단 |
| **친구 추가 zh 분기** | zh `加為好友`(**실측 6건**) ↔ `加入好友`(**1건**) | `md/guide.md` 정본은 `加入 … 好友`인데 등록 실사용은 `加為好友`가 다수다. **정본과 등록값이 반대**이므로 어느 쪽으로 통일할지 사용자 결정이 필요하다(현재 번역은 정본 채택) |
| **모바일상품권** | ko `모바일상품권`(실측 **0건**) · ja `モバイル商品券`(**0건**) · zh `行動商品券`(**0건**) | 이번 3키에서 처음 등장한 상품 유형 명칭이라 전 언어 선례가 없다. **실사용 0건이므로 지금 등재를 권장하지 않고**, 이 표기가 굳은 뒤(업로드 후 재실측) 등재를 검토한다. 2차 검토가 zh 대만 관용 `禮券`/`電子票券`을 제기했으므로 등재 전 표기 확정이 선행되어야 한다 |
| **올리브영(브랜드)** | ko `올리브영`(**1건**) · ja `オリーブヤング`(**0건**) ↔ `オリヤン`(**1건**) · en/th/zh `OLIVE YOUNG` | ja 표기가 실측 1:0으로 사실상 무근거 수준이다. 이번 번역은 **정식 브랜드명 `オリーブヤング`** 를 썼다(2026-08-26 사용자 피드백: 콜드 오디언스에는 `オリヤン` 은어 대신 정식명이 안전). 브랜드는 용어집 `exceptions` 등재 후보이나 **ja 표기 정본을 먼저 확정**해야 한다 |

> 실측 0건 표기는 권장하지 않았다(`md/check.md` 함정 2-1 「일반 규칙 ②」). 용어집은 API 읽기 전용이므로 반영은 `md/landpress.md` 절차대로 전체 JSON 산출 → 사용자가 Landpress CMS에 붙여넣기. **승인 시 기획자 가이드 zip 동반 갱신 필수**.

### (d-2) 원문·구조 개선 제안
1. **매장명 목록·상품별 콘텐츠 9건은 Admin 상품 데이터로 이관 검토** — 사용자 결정에 따라 XLT 키로 등록했으나, 상품(올리브영·다이소·이마트24…)이 늘어날 때마다 키가 상품에 종속돼 증식한다. 같은 프레임의 다른 코멘트도 「Admin에 등록된 상품명/판매가격」이라고 명시하고 있어 **구조적으로는 상품 데이터의 다국어 필드**가 맞다. 특히 `UF_voucher_detail_unavailable_value`(매장 33곳)는 현지화 자체가 부적절해 P0 오탐을 상시 유발한다.
2. **XLT 마커 누락 의심 — 한글 텍스트 118건에 마커 없음**(중복 제거 기준). 화면 고정 문구인데 마커가 없는 대표 예: `(필수) 이용약관` · `약관 동의하고 계속하기` · `*친구 추가는 바우처 전송/혜택 안내를 위해 필요해요.` · `JPYC로 결제` · `상시 할인가` · `단독 혜택가`. 특히 **(Web) 인터스티셜 페이지는 마커가 0건**이다(51건 미표시). 마커 기준 선별이라 이번 범위에서 제외했으니 디자이너 확인이 필요하다.
3. **프레임명 정정 2건(반영됨)** — `68594:14785`(Figma 이름 `결제 시 친구추가`)의 코멘트·화면은 **찜하기 시 약관동의**이고, `68594:14578`(Figma 이름 `찜하기 시 약관동의`)은 **결제 시 친구추가**다. 두 이름이 서로 바뀐 것으로 보여 위키에는 코멘트 기준으로 기재하고 각 행에 확인 요청을 남겼다. **Figma 원본 수정 필요**.
4. **원문 표기 통일 제안** — ⓐ `MY탭>바우처` ↔ `MY쇼핑`(같은 위치의 두 이름) ⓑ 문장 마침표 유무 혼재 ⓒ 코멘트 원문 `안돼어` → `안 되어`.
5. **추가 변수화 후보(이번 범위 외)** — `구매일로부터 **60일**간 유효`(유효기간) · `리뷰 (**33**)`(리뷰 수) · `**10%** 할인`(할인율, 마커 없음). 상품·시점에 따라 바뀌는 값이라 `{{0}}` 처리가 적합하나 사용자가 승인한 변수화 6건에 포함되지 않아 리터럴로 등록했다.
6. **ja 명사/동사 표기 정책** — 라벨은 `決済`(決済方法·決済金額), 버튼은 동사 `支払う`로 썼다. 한 화면에서 두 어형이 보이므로 통일 정책이 필요하면 알려주기 바란다.

## 통과 판정

- **실제 P0 = 0건** (자동 검증 P0 4건은 전건 오탐 판정 — 고유명사 매장명 · (c) 참조 / 수동 320셀 전수 검토 결과 P0 0건)
- P1 66건·P2 14건 전건 처리 판정 완료. 실제 결함 3건은 수정 후 재검증했다.
- (d) 권장 사항은 **사용자 결정 전까지 임의 반영하지 않는다**.

---

# 2판 (2026-08-26 21:55) — 실제 텍스트 기준 재산출 + 신규 7키

## 1. 어노테이션 통합 번호 재산출 (사용자 지적 반영)

**증상**: 이미지의 ⓝ가 실제 문구와 어긋났다 — `⑫`가 `유효기간` 옆에 찍혔는데 XLT 표의 12번은 `구매일로부터 60일간 유효 (연장불가)`였다.

**원인**: `scripts/collect_frames.py`가 통합 번호를 **Figma 코멘트 핀의 y 좌표 순**으로 매긴다. 같은 행에 라벨·값 핀이 나란히 있으면 y가 2px 차이로 뒤집혀(`핀 12` y=691 x=141 = 값 / `핀 13` y=693 x=35 = 라벨) 번호가 화면의 좌→우 순서와 반대가 된다. 원은 매칭 텍스트 좌측에 찍히므로 **번호와 위치가 서로 다른 문구를 가리켰다**.

**조치**: `annotate_by_text.py`(신규)로 번호 산출 기준을 바꿨다.
- XLT 항목은 **매칭 텍스트 bbox**(좌측 x·세로 중앙 y), 정책 항목은 핀 좌표를 쓴다.
- 정렬은 y로 행을 묶고(허용 오차 10px) **같은 행은 x 오름차순(좌→우)** — 화면 읽기 순서와 일치.
- 렌더 후 육안 검증: `유효기간 ⑫ → 구매일로부터… ⑬ → 사용처 ⑭ → 올리브영 가맹점 ⑮ → 취소·환불 ⑯ → 미사용 시… ⑰ → 수령방법 ⑱ → e-쿠폰 ⑲` 1:1 확인. 9프레임 전부 **겹침 0건**.
- 이미지 ⓝ = Description 정책 번호 = XLT 표 No가 여전히 하나의 통합 번호 체계다(빨강 정책 / 파랑 XLT 문구).

## 2. 대상 집합 변경 — 코멘트 마커 → 실제 텍스트

사용자 지시(「xlt 코멘트랑 상관없이 실제 텍스트 기준」)에 따라 마커 유무로 대상을 가리지 않고 **화면의 실제 문구**를 대상으로 삼았다. 프레임별 XLT 항목 수: 인터스티셜 39 · 바우처 상세 40 · 결제 시 약관동의 17 · 찜하기 시 약관동의 12 · 결제 시 친구추가 14 · 찜하기 시 친구추가 9 · 결제페이지 14 · 결제 실패 2 · 결제 완료 6.

### (1a) 신규 7키 — 한국어 원문 교정 없음(원문 그대로 사용)
| XLT Key | ko_KR | 비고 |
|---|---|---|
| `UF_voucher_friend_add_notice` | `*친구 추가는 바우처 전송/혜택 안내를 위해 필요해요.` | 4프레임 공용 |
| `UF_voucher_pay_method_jpyc` | `JPYC로 결제` | 결제 수단 옵션. `JPYC`는 용어집 예외 패턴이라 전 언어 원어 유지 |
| `UF_voucher_terms_service` | `(필수) 이용약관` | 등록값 선례 실측 — ja `利用規約` 7건 · en `Terms of Service` 5건 · zh `使用條款` 5건 · th `ข้อกำหนด` 11건 |
| `UF_voucher_terms_agree_btn` | `약관 동의하고 계속하기` | ja `同意して続ける` 1건 선례 |
| `UF_voucher_price_regular_discount` | `상시 할인가` | 가격 라벨 |
| `UF_voucher_price_exclusive` | `단독 혜택가` | 가격 라벨 |
| `UF_voucher_review_all` | `리뷰 전체보기 (33개)` | **리뷰 수 하드코딩** — 변수화 미승인분(아래 3-②) |

### 대상에서 제외한 것과 사유
- **리뷰 본문**(`친절하시고 대표 원장님께서…`, 8프레임) — 사용자 생성 콘텐츠(UGC)라 XLT 대상이 아니다.
- **상품 데이터**(`올리브영 바우처 5만원권`·`≈¥5,686 상당`·`10% 할인`·`캐시백 5%`) — Admin 등록 상품 정보(같은 프레임 코멘트도 「Admin에 등록된 상품명/판매가격」으로 명시).
- **더미 데이터**(`홍길동길동이`)·**상태바**(`9:41`).
- **문구 변형 3건은 원본 확인 필요(디자이너)**: `Unifi MINI는 LINE 인증 MINI App이에요`(기존 키는 `Unifi mini는 LINE 공식 인증 MINI App이에요`) · `바우처 전송을 위해 … 약관 동의 후 …` 2종 변형. 같은 의미가 프레임마다 다르게 적혀 있어 하나로 확정해야 한다.

## 3. 2판 검증 (71키 × 5개 언어 = 355셀 전수)

- **자동 검증 재실행**: P0 **4건**(초판과 동일 — 매장명 목록 1키의 정책 예외) / P1 **75건**(초판 66 + 신규 7키에서 9건) / P2 **15건**(+1).
- **신규 9건 P1 전건 판정 — 전부 오탐**: `혜택`→`perks`/`特典`/`สิทธิประโยชน์` 3건(`혜택가`는 가격 라벨이라 `Exclusive price`·`限定特別価格`이 정확) · `결제`→`Payment`/`การชำระเงิน`/`結帳` 3건(초판과 같은 동의어·품사 사유) · `전체보기`→`ดูทั้งหมด`/`全部查看` 2건(`ดูรีวิวทั้งหมด`·`查看全部評論`이 각 언어 어순상 자연스럽다) · P2 마침표 1건(원문 준수).
- **기계 점검(355셀)**: 특수문자(U+2028/2029·nbsp)·이중 공백·앞뒤 공백·`/n` **0건** / 치환자 `{{0}}` 5개 언어 일치 **불일치 0건** / 개행 수 일치 **불일치 0건** / th 문말 마침표 **0건** / zh 간체 **0건**.
- **수동 3단계**: 신규 7키 35셀을 1·2·3단계로 개별 검토(원문 교정 불요 · 용어집 대조 · 언어별 자연스러움). 기존 64키는 초판·2차 검토 결과를 유지한다.
- **실제 P0 = 0건** — 통과 기준 충족.

## 4. 반영 위치 변경

- 초판은 `4618657620`(K-Pick 유저 동선 및 연동 구조 개편)에 반영했으나 **주소 오지정**으로, 2판은 **`4639704548`(올리브영 5만원 상품상세 Unifi 직접제공, UNIFY-10468)** 에 반영했다(v7 → v8).
- 원 페이지는 **사용자가 v39에서 Screen·다국어 섹션을 직접 제거**했다. 남은 잔여물(내가 추가한 History 행 1개 · Related Docs의 Figma 링크 1개 · 첨부 10건)은 **임의로 지우지 않고 보고**한다 — 첨부 삭제는 되돌릴 수 없고 History는 이력이다.

# 번역 품질 게이트 리포트 — K-Pick 바우처 구매 후 Unifi OA 알림 동의 팝업 (4키)

- **작업 유형**: 코멘트 선별 모드(`XLT` 마커) → 위키 Mode B 단일 프레임 행 추가 (번역 + 위키 통합 작업)
- **대상 Figma**: `GOCHAYBS7hIrmWRGNuJOKV` / 페이지 `K-pick(상세페이지, My)`(67735:11021) / 프레임 **`Unfi OA 친구추가 동의 팝업`**(`68080:20746`, 375×812)
- **대상 위키**: [pageId 4637821100](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4637821100) — `K-Pick 바우처 구매 후 Unifi OA 친구추가 및 Unifi OA 발송` · Jira `UNIFY-10309`
- **담당 FE 팀**: **LV** — 위키에 UIT/LV 구분이 없어 **사용자에게 질문 → LV 확정**. 규칙 설명·적용 확인 완료(프리픽스 = 프로젝트 약어, 치환자 `{0}`). **이번 4키는 변수 없음**
- **XLT Key 네임스페이스**: **`mini_guidekim_kpick_oa_consent_*`** (사용자 결정 — 아래 §5 #1 정정 경위)
- **대상 키 (4건 — 전부 신규)**:
  - `mini_guidekim_kpick_oa_consent_purchase_complete`
  - `mini_guidekim_kpick_oa_consent_title`
  - `mini_guidekim_kpick_oa_consent_desc`
  - `mini_guidekim_kpick_oa_consent_agree_btn`
- **등록값 조회 시각**: **2026-08-24 16:58 KST** — `Unifi / WEB BROWSER / v1.6.7.1`(2,143키) + `Dapp Portal / WEB BROWSER / v2.6.1`(1,599키) = **3,742키 전수** (캐시 금지 규칙 — 매 run 새로 조회)
- **Figma 코멘트 조회**: **2026-08-24 실행 시 REST 신규 조회**(`fetch_threads` + `collect_node_boxes` 좌표 정규화). `comments_data.json` 캐시 미사용
- **용어집**: v4.7 (116 용어 / exceptions 9 / oa_variables 2 / deprecated_terms 14) — API 신규 조회
- **검토 범위**: **전수 점검** — 대상 파일 전체 행의 5개 언어를 한 줄씩 수동 검토(**4키 × 5개 언어 = 20셀 전부**). 신규·변경 키로 한정하지 않음(대상 파일이 신규 4키로만 구성됨)
- **산출물 엑셀**: `xlt/xlt_output_20260824181908.xlsx` (위키 첨부 완료)

> **개정 이력 (2판)**
> 1. **초판** — 키 프리픽스 `mini_kpick_*`, th `หากยินยอมรับ…`, en `and…and`, th 제목 물음표 없음, ja `同意いただくと`, ko `구매 완료되었어요!`.
> 2. **2판(현행)** — 2차 독립 검토가 **실제 결함 2건**(th 붙은 결합 · en 수식어 누락)과 **프리픽스 선례 불일치**를 검출. 결함 2건은 즉시 수정하고, 프리픽스·구두점·경어·조사 4건은 **사용자 결정을 받아** 반영. 엑셀·검증·리포트 전부 재실행.
>
> 폐기된 초판 엑셀(`xlt_output_20260824170210.xlsx`)과 그 자동 검증 리포트는 **오업로드를 막기 위해 삭제**했다(`md/xlt-verify.md` §6).

---

## 0. 대상 선별 (코멘트 선별 모드)

미해결 루트 코멘트 **5건**을 y좌표 오름차순으로 **통합 번호**를 매겼다(정책 + `xlt` 마커 공통 번호 체계). 답글은 **0건**(`log_self_check`: `roots=5, replies attached=0`, 루트별 `attached replies: 0`).

| 통합 No | 종류 | 핀(frame-rel) | 코멘트 | 매칭된 텍스트 | 매칭 방식 |
|---|---|---|---|---|---|
| **1** | 정책 | (346, 455) | `바텀 시트 닫기` | — (번역 대상 문구 없음) | 핀 위치 렌더 · **빨강** |
| **2** | XLT 마커 | (194, 568) | `xlt` | `구매 완료 되었어요!` | 포함 · **파랑** |
| **3** | XLT 마커 | (173, 596) | `xlt` | `LINE으로\n바우처를 보내드릴까요?` | 포함 · **파랑** |
| **4** | XLT 마커 | (201, 653) | `xlt` | `Unifi 알림 수신에 동의하시면 \n바우처와 쏠쏠한 캐시백 포인트 정보도 알려드려요.` | **팝업겹침→육안확인**(수동 교정) · **파랑** |
| **5** | XLT 마커 | (142, 742) | `xlt` | `동의하고 알림 받기` | 포함 · **파랑** |

- **⛔ 팝업 겹침 오매칭 실측 (No 4)**: 좌표 매칭이 z-order를 인식하지 못해 **팝업 뒤 배경 바우처 카드의 `5%`**(bbox 22×17, rel=[194,643])를 「포함 중 최소 bbox」로 선택했다. 의도한 문구는 팝업 본문(bbox 327×32, rel=[24,644])이다. `md/translate.md` 엣지 케이스 규칙대로 **어노테이션 렌더 후 육안 확인해 수동 교정**했다.
- **매칭 결과는 번역 전 사용자 승인 완료** (4개 문구 번역 진행 승인).
- 프레임 전체 TEXT 노드 **75개** 중 배경(바우처 목록·화장품 카드·GNB·상태바)의 더미·반복 텍스트는 `xlt` 마커가 가리키지 않아 대상에서 제외했다.
- **어노테이션 검증**: 겹침 검사 `overlaps: 없음`(전 쌍 중심거리 ≥ 지름+1pt) · **렌더 후 육안 확인 완료**(번호 위→아래 증가, ⓝ가 첫 글자를 덮지 않음, 색 구분 = 캡션 문구와 일치) · **번호 3면 일치**(이미지 ⓝ = Description 통합 번호 = XLT 표 No).

---

## 1. 한국어 원문 교정 (게이트 1a — 필수)

Figma 원문에 교정 **2건**을 적용했다.

| 구분 | 내용 |
|---|---|
| Figma 원문 | `구매 완료 되었어요!` / `Unifi 알림 수신에 동의하시면 \n바우처와 쏠쏠한 캐시백 포인트 정보도 알려드려요.` |
| **교정 ①(맞춤법)** | `완료 되었어요` → **`완료되었어요`**. 접미사 `-되다`는 앞말에 붙여 쓴다(한글 맞춤법 제35항 계열). 등록값 선례도 전부 붙임 — `연결이 완료되었어요.`(`UF_asset_external_success_title`)·`처리가 완료되었습니다`(`UF_bridge_refund_error_common_desc`)·`본인 확인이 완료되었습니다`(`payment_complete_verification_desc`) 등 **15건 전부** |
| **교정 ②(조사 보완 — 사용자 결정)** | `구매 완료되었어요!` → **`구매가 완료되었어요!`**. 2차 검토가 「등록값 문장형 선례는 주격 조사를 쓴다」로 제기 → **사용자 결정으로 조사 보완 확정**. 근거 선례: `연결이 완료되었어요.`·`서명 요청은 이미 완료되었어요.` |
| **교정 ③(비표준 공백)** | `동의하시면 \n` → **`동의하시면\n`**. 개행 앞 불필요한 공백 1자 제거 |
| 확정 원문 | `구매가 완료되었어요!` / `Unifi 알림 수신에 동의하시면\n바우처와 쏠쏠한 캐시백 포인트 정보도 알려드려요.` |

- **alias 기록 (필수 — `translation_data.json`의 `aliases`에 보존)**:
  - `구매 완료 되었어요!` → `mini_guidekim_kpick_oa_consent_purchase_complete`
  - `Unifi 알림 수신에 동의하시면 \n바우처와 쏠쏠한 캐시백 포인트 정보도 알려드려요.` → `mini_guidekim_kpick_oa_consent_desc`
- **디자이너에게 Figma 원본 수정 요청 필요** → §5 (d-2) #4.
- 그 외 한국어 문제 없음: 비표준 공백(`\xa0`·`​`·전각 공백·BOM·TAB) **0건**, 연속 공백 0건, `/n` 오타 0건, 오타 없음 (4키 × 5개 언어 = 20셀 전수 코드 스캔).

---

## 2. 자동 검증 결과 (P0 / P1 / P2 요약)

```
python3 scripts/validate_translation.py \
  xlt/xlt_output_20260824181908.xlsx \
  scripts/glossary.json
```

| 심각도 | 건수 | 내용 |
|---|---|---|
| 🔴 **P0 (Critical)** | **0건** | 빈칸·오타·언어 혼입·용어집 표기 위반 — 없음 |
| 🟡 P1 (Medium) | 2건 | 용어 불일치 2건 (키① en·th) → §4에서 각 건 처리 판정 |
| 🟢 P2 (Low) | 1건 | 마침표 스타일 1건 (키③) → §4 |

자동 리포트: `xlt/xlt_output_20260824181908_validation_report.md`

> ⚠️ **`P0=0`은 통과가 아니라 시작점이다.** 자동 검증기는 외래어 음차 오염·단어가 붙은 띄어쓰기 누락·다의어 모호·표현 어색을 구조적으로 못 잡는다. **이번 run이 그 실증이다** — 2차 독립 검토가 자동 P0=0 상태에서 **실제 결함 2건**(th `ยินยอมรับ` 붙은 결합 · en 수식어 누락)을 찾아냈다. 따라서 §3 수동 3단계를 20셀 전수로 수행했다.

**보조 코드 스캔 (20셀 전수 · 확정본)**

| 검사 | 결과 |
|---|---|
| 비표준 공백·제어문자(NBSP·ZWSP·전각·BOM·TAB) | ✅ 0건 |
| 선행/후행 공백 · 연속 공백 · `/n` 오타 | ✅ 0건 |
| 개행 수 언어 간 일치 | ✅ 키①·④ = 0 / 키②·③ = 1 (5개 언어 동일) |
| 이질 문자체계 혼입(한글이 비-ko 칸, 가나가 zh 칸 등) | ✅ 0건 |
| zh_TW 간체·이체자(`产`·`台`·`发`·`国`·`为`·`无`·`个` 등) | ✅ 0건 |
| 치환자·HTML 태그 정합 | ✅ 전 키 0개 (5개 언어 동일) |
| `ยินยอมรับ` 재발 가드 | ✅ 없음 |
| 사용자 결정 4건 반영 가드 | ✅ 전부 확인 |

---

## 3. 수동 3단계 체크표 (전수 — 4키 × 5개 언어 = 20셀 전부)

### 1단계: 한국어 맞춤법·띄어쓰기

| 점검 항목 | 키① purchase_complete | 키② title | 키③ desc | 키④ agree_btn |
|---|---|---|---|---|
| 조사·어미 띄어쓰기 | ✅ `구매가 완료되었어요!` — 교정 ①·② 적용(§1) | ✅ `LINE으로` 정상 | ✅ `동의하시면` 정상 | ✅ `동의하고` 정상 |
| 단어 붙음 누락(`누리고다양한`류) | ✅ 없음 | ✅ 없음 | ✅ 없음 | ✅ 없음 |
| 외래어 음차 오염(`포이가츠`류) | ✅ 없음 | ✅ `바우처`는 등록값 정본 표기 | ✅ `캐시백`·`포인트` 용어집·등록값 표기 | ✅ 없음 |
| 오타·글자 누락 | ✅ 없음 | ✅ 없음 | ✅ 없음 | ✅ 없음 |
| 비표준 공백·제어문자 | ✅ 0건 | ✅ 0건 | ✅ 교정 ③으로 제거 | ✅ 0건 |
| 다의어·모호 표현(`피부결과`류) | ✅ 없음 | ✅ 없음 | ⚠️ `쏠쏠한` = 구어 마케팅 표현. 오류는 아니나 5개 언어 의역 폭이 넓다 → §5 (d-2) #6 | ✅ 없음 |
| 종결 어미 일관성 | ✅ 해요체 | ✅ 해요체 | ✅ 해요체 | ✅ 명사형(버튼) |
| `deprecated_terms` 구 표기(팔로우·공식계정·월렛·유저) | ✅ 미검출 | ✅ 미검출 | ✅ 미검출 | ✅ 미검출 |

### 2단계: 용어집 위반 (v4.7)

| 용어 | 용어집 등재값 | 적용 | 판정 |
|---|---|---|---|
| 알림 | en `Notification` / ja `通知` / th `การแจ้งเตือน` / zh `通知` | 키③·④ 4언어 전부 등재값 | ✅ **8/8 셀 일치** |
| 캐시백 | en `cashback` / ja `キャッシュバック` / th `เงินคืน` / zh `現金回饋` | 키③ 4언어 전부 등재값 | ✅ **4/4 일치** |
| 구매 | en `Buy` / ja `購入` / th `ซื้อ` / zh `購買` | ja `ご購入`·th `ซื้อ`·zh `購買` 일치 / en `Purchase` 문맥 의역 | ⚠️ P1 1건 → §4 #1 |
| 완료 | en `Complete` / ja `完了` / th `เสร็จ` / zh `完成` | ja `完了`·en `complete`·zh `完成` 일치 / th `สำเร็จแล้ว` 문맥 의역 | ⚠️ P1 1건 → §4 #2 |
| 바우처 | **용어집 미등재** — 등록값 정본 `mini_guidekim_cat_voucher`: ja `バウチャー` / en `Voucher` / th `เวาเชอร์` / zh `優惠券` | ja `バウチャー` / en `vouchers`(복수) / th `เวาเชอร์` / zh `優惠券` | ✅ **8/8 셀 정본 일치**(en만 문맥상 복수) → (d-1) 등재 권장 |
| 포인트 | **미등재** — 등록값 36키 일관: ja `ポイント` / en `points` / th `คะแนน` / zh `點數` | 키③ 4언어 전부 일치 | ✅ **4/4 일치** → (d-1) 등재 권장 |
| 공식 계정 | en `Official account` / ja `公式アカウント` / th `บัญชีทางการ` / zh `官方帳號` | 이번 4키 문구에 미등장(Description 설명문에만) | ✅ 해당 없음 |
| 예외 목록(번역 금지) | PIN·API·URL·Apple·Google·USDT·IDRP·JPYC·Unifi mini | `Unifi`·`LINE` 브랜드 원어 유지 | ✅ |
| `deprecated_terms` (14항목 전수) | 팔로우/공식계정/友達/フォロー/關注/追蹤/ติดตาม/follow/월렛/유저 | 20셀 전부 미검출 | ✅ |

**기존 등록값 일관성 대조 (2026-08-24 16:58 실측 · 3,742키 전수 검색)**

| 참조 키 | 기존 번역 | 신규 번역의 정합 |
|---|---|---|
| `UF_signin_tos_next_btn` · `UF_signin_tos_aggregator_next_btn` · `UF_asset_token_wire_consent_accept_btn` · `unifi_promotion_jpyc_btn1` (「동의하고 X」 8건) | ja `同意して報酬を受け取る` / en `Agree and Sign Up`·`Agree and Start` / th `ยอมรับและรับรางวัล`·`ยอมรับและเริ่ม` / zh `同意並開始`·`同意並註冊` | ✅ 키④ `同意して通知を受け取る`·`Agree and Get Notifications`·`ยอมรับและรับการแจ้งเตือน`·`同意並接收通知` — **4/4 언어 어형 완전 일치** |
| `mini_guidekim_cat_voucher` | ko `바우처` / ja `バウチャー` / en `Voucher` / th `เวาเชอร์` / zh `優惠券` | ✅ 키②·③ 8셀 전부 정본 일치 |
| `UF_settings_alarm_event_agree` · `UF_settings_alarm_marketing_policy_title` (「정보 수신 동의」) | ja `マーケティング情報受信の同意` / en `Consent to Receive Marketing Information` / th `ความยินยอมในการรับข้อมูลการตลาด` / zh `同意接收行銷資訊` | ✅ 키③ zh `同意接收 Unifi 通知` **패턴 일치** · ja `通知受信` 무연결 복합명사 방식이 선례와 동일 · **th는 이 선례가 결함 검출 근거**(§4 #4) |
| `UF_asset_token_wire_consent_3rdparty_title` (`ご同意` 1건) | ja `ご同意` | ✅ 키③ `ご同意いただくと` — 사용자 결정으로 선례에 정합 |
| `purchase_complete_title` · `drops_complete_title` · `filter_mypage_txtype_option1` | en `Purchase Successful`·`Drops Purchase Complete` / th `ซื้อสำเร็จ` | ✅ 키① en `Purchase complete!`·th `ซื้อสำเร็จแล้ว!` — **어휘 선택이 선례와 동일**(§4 #1·#2 오탐 근거) |
| `UF_notification_*` (알림 12키) | ja `通知` / en `Notification(s)` / th `การแจ้งเตือน` / zh `通知` | ✅ 키③·④ 8셀 일치 |
| `mini_guidekim_kpick_tab`·`_label`·`_jp_banner_title`·`_jp_banner_desc` | K-Pick 탭 기존 키 = **`mini_guidekim_kpick_*` 네임스페이스** | ✅ 신규 4키를 이 네임스페이스로 확정(§5 #1) |
| zh 용어 선택 | `資訊` 50건(정본) vs `信息` 0건 | ✅ 키③ `資訊` 정본 채택 |
| ja 제안형 종결 | `ましょうか？` 1건(ko `~해볼까요?`) / `しますか？` 29건 | ✅ 키② ko `보내드릴까요?` → `お送りしましょうか？` 어형 정합 |

### 3단계: 다른 언어 번역 검토

| 언어 | 점검 | 결과 |
|---|---|---|
| **ja_JP** | です・ます体 · 겸양 · 한국어 직역 패턴 · 전각 부호 · 개행 앞 구두점 | ✅ 4셀 전부 정중체. `ご購入`·`ご同意`·`お送りしましょうか` 겸양 적정(사용자 결정 반영). 전각 `！`·`？` 사용. `、\n`은 선례 22건 있어 정상. `通知受信` 직역 의심 → 동일 원문의 등록값 정본이 `情報受信の同意`로 **무연결 복합명사가 이미 선례** → 허용(§5 (d-2) #7) |
| **en_US** | 브랜드 대소문자 · 단복수 · 관사 · 접속 구조 · 수식어 누락 | ✅ `Unifi`·`LINE` 공식 표기. `vouchers`·`notifications`·`updates` 복수 정합(카테고리 라벨은 단수 `Voucher`이나 문맥상 복수가 정상). **🔴 초판의 「쏠쏠한」 누락 + `and…and` 이중 접속을 수정**(§4 #5). 느낌표 헤드라인 sentence case = Unifi en 43건 우세 스타일 일치 |
| **th_TH** | 의미 단위 띄어쓰기 · 붙은 결합 · 종결부호 · 영어 미번역 잔존 | ✅ **🔴 초판 `ยินยอมรับ`(레지스트리 0건 · `ยอมรับ`와 시각 충돌) 수정**(§4 #4). `เวาเชอร์`·`คะแนน`·`เงินคืน`·`การแจ้งเตือน` 전부 정본. **키② 물음표 추가**(사용자 결정 — ko가 `?`인 55키 중 th도 `?`가 48건 = 87%). 문장 끝 마침표 미사용 관례 준수. 브랜드 외 영어 잔존 없음 |
| **zh_TW** | 번체 정자 · 라틴 주변 공백 · 전각 부호 · 어휘 | ✅ 간체·신자체 **0건**(`購`·`買`·`傳`·`優`·`點`·`數`·`資`·`訊`·`併`·`與`·`饋`·`現`·`並` 전수 확인). `要透過 LINE\n傳送…`·`同意接收 Unifi 通知` 라틴 앞뒤 공백 = 선례 스타일. 전각 `！`·`？`·`，`·`。`. `超值`는 등록값 선례 0건이나 대만 표준 표현으로 적정 |
| **전 언어 공통** | 빈칸 · 언어 혼입 · placeholder · 태그 · 개행 구조 · 컬럼 정렬 | ✅ 빈칸 0 · 문자체계 혼입 0 · 치환자 0개(5개 언어 동일) · 태그 없음 · 개행 수 언어 간 일치 · 컬럼 misalignment 0(각 언어 칸이 실제 그 언어 내용) |

### 2차 독립 검토 (`translation-reviewer` 에이전트 — 분리 컨텍스트 재판정)

초판(20셀)을 대상으로 실행해 **실제 결함 2건 · 확인 필요 5건 · 1차 오탐 판정 3건 동의**로 판정했다. 검토 소스는 용어집 v4.7 + 레지스트리 3,742키 전수 검색이다.

| 항목 | 2차 판정 | 처리 |
|---|---|---|
| 🔴 키③ th `ยินยอมรับ` | `ยินยอม`+`รับ`가 연결어 없이 붙어 `ยอมรับ`(수락)와 시각 충돌·오독 유발. **레지스트리 3,742키 중 `ยินยอมรับ` 0건**. 동일 원문의 th 정본은 `ยินยอม` + `ในการ` + `รับ` | ✅ **수정 — `หากยอมรับ…`**(선례 3건 · 키④와 「동의」 표기 통일까지 해소) |
| 🔴 키③ en 「쏠쏠한」 누락 + `and…and` | ja `お得な`·th `สุดคุ้ม`·zh `超值`는 전부 반영했는데 en만 수식어 누락 = 언어 간 의미 비대칭. 이중 접속으로 문장 어색 | ✅ **수정 — `to also get vouchers and great cashback point updates.`** |
| ⚪ P1 ① 키① en `구매`=Buy 권장 | **오탐** — 용어집 `Buy`는 CTA 라벨용 단일 매핑. 명사구 「구매 완료」 en 선례는 전부 `Purchase`(`Drops Purchase Complete`·`Purchase Successful`). `Buy`를 명사로 쓰면 비문 | ✅ 기각(§4 #1) |
| ⚪ P1 ② 키① th `완료`=เสร็จ 권장 | **오탐** — 「구매+완료」 th 선례가 정확히 `ซื้อสำเร็จ` 2건. 문장·감탄형 완료상 `สำเร็จแล้ว` 7건 선례. `เสร็จ`는 라벨형 등재값 | ✅ 기각(§4 #2) |
| ⚪ P2 ① 키③ 마침표 | **오탐** — ko `.`/ja `。`/en `.`/zh `。` + th만 미사용 = 언어별 관례대로 정확히 적용 | ✅ 기각(§4 #3) |
| 🟡 키② th 물음표 누락 | ko가 `?`인 55키 중 th도 `?`가 48건(87%). 같은 세트 키① th는 `!`를 유지하므로 구두점 억제 정책도 아님 | ✅ **사용자 결정 → `?` 추가**(§4 #6) |
| 🟡 키③ ja `ご同意` | 레지스트리 `ご同意` 1건 / `同意いただく` 0건 | ✅ **사용자 결정 → `ご同意いただくと`**(§4 #7) |
| 🟡 키① ko 조사 생략 | 등록값 문장형 선례는 조사를 쓴다 — `연결이 완료되었어요.` | ✅ **사용자 결정 → `구매가 완료되었어요!`**(§1 교정 ②) |
| 🟡 키③ ja `通知受信` 직역 의심 | 현행 허용 — 동일 원문 정본이 `情報受信の同意`로 무연결 복합명사가 선례. 자연스러움만 보면 `通知の受信`이 우세 | 미반영 → §5 (d-2) #7 (우선순위 낮음) |
| 🟡 키③/④ th 「동의」 혼용(`ยินยอม`↔`ยอมรับ`) | 레지스트리도 혼용이라 위반은 아니나 같은 바텀시트 내 혼용은 눈에 보인다 | ✅ **해소 — 🔴 수정으로 양쪽 `ยอมรับ` 통일** |
| ⚠️ 키 프리픽스 선례 불일치 | K-Pick 탭 기존 키는 전부 `mini_guidekim_kpick_*`. `mini_kpick_*`는 3,742키에서 선례 0건 | ✅ **사용자 결정 → `mini_guidekim_kpick_oa_consent_*`**(§5 #1) |
| ⚠️ 원문 동의 범위 고지 | 화면 목적은 「친구추가 + 알림 수신 동의」인데 문구는 알림 수신만 말한다. 5개 언어가 ko를 일관되게 따르므로 **번역 결함은 아님** | 미반영 → §5 (d-2) #5 (기획·법무 확인 사안) |
| 기계 점검 20/20 | NBSP 0 · 이중공백 0 · 전후공백 0 · 치환자 0개 동일 · 이질 문자체계 0 · 개행 수 일치 · zh 번체 정자 전수 확인 · deprecated 14항목 미검출 | ✅ 1차 보조 스캔과 동일 결과 |

---

## 4. 검증기·2차 검토 보고 각 건의 처리 판정

| # | 등급 | 건 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | P1(자동) | 키① `en_US`: '구매' → 'Buy' 권장 | **오탐 — 기각(현행 `Purchase complete!` 유지)** | 용어집 `Buy`는 **CTA 라벨용 단일 매핑**. 명사구 「구매 완료」의 en 등록값 선례는 전부 `Purchase` — `Drops Purchase Complete`·`Purchase Successful`. `Buy complete`는 비문. 「용어집 단일 매핑 vs 문맥 의역」 오탐 유형 |
| 2 | P1(자동) | 키① `th_TH`: '완료' → 'เสร็จ' 권장 | **오탐 — 기각(현행 `ซื้อสำเร็จแล้ว!` 유지)** | 「구매+완료」 th 선례가 **정확히 `ซื้อสำเร็จ`** 2건(`purchase_complete_title`·`filter_mypage_txtype_option1`). 문장·감탄형 완료상 `สำเร็จแล้ว` 7건 선례. `เสร็จ`는 라벨형 등재값이고 `เสร็จสิ้น`(45건)·`เสร็จสมบูรณ์`(17건)은 절차 종료 문맥. v4.2 「지급 완료」 등재 사유와 동일 구조 |
| 3 | P2(자동) | 키③: 문장형 중 마침표 있는 소수 스타일 | **오탐 — 기각(현행 유지)** | ko `.`/ja `。`/en `.`/zh `。` 유지 + **th만 마침표 없음** = 언어별 관례를 정확히 적용한 결과. 세트 내 구두점 정합도 확인(키① 감탄부호 5/5, 키② 물음표 5/5, 키④ 버튼 무구두점) |
| 4 | **🔴 실제 위반(2차)** | 키③ `th_TH`: `ยินยอมรับ` 붙은 결합 | **수정 완료** | `ยินยอม`+`รับ`가 연결어 없이 붙어 `ยอมรับ`와 시각 충돌·오독 유발. 레지스트리 3,742키 중 **0건**. → `หากยอมรับการแจ้งเตือนจาก Unifi`(`ยอมรับ` 선례 3건). **부수 효과: 키④와 「동의」 표기 통일**(2차가 제기한 🟡 혼용 항목 동시 해소) |
| 5 | **🔴 실제 위반(2차)** | 키③ `en_US`: 「쏠쏠한」 미반영 + `and…and` | **수정 완료** | ja `お得な`·th `สุดคุ้ม`·zh `超值`가 전부 수식어를 반영했는데 en만 누락 = **언어 간 의미 비대칭**(PRODUCTION_RULES 「모든 언어 완전 번역」 위반). → `to also get vouchers and great cashback point updates.`(수식어 `great` 반영 + 이중 접속 제거) |
| 6 | 🟡 정책(2차) | 키② `th_TH`: 물음표 누락(4:1 비대칭) | **사용자 결정 → `?` 추가** | ko가 `?`로 끝나는 55키 중 th도 `?`인 것이 **48건(87%)**. 프로젝트 관례 「th 문장 끝 마침표 미사용」은 마침표 사안이고 물음표는 대상 아님 |
| 7 | 🟡 톤앤매너(2차) | 키③ `ja_JP`: `ご` 겸양 접두 | **사용자 결정 → `ご同意いただくと`** | 레지스트리 `ご同意` 1건 / `同意いただく` 0건. 일본어 UI 관용 겸양 표현 |
| 8 | 🟡 원문(2차) | 키① `ko_KR`: 주격 조사 생략 | **사용자 결정 → `구매가 완료되었어요!`** | 등록값 문장형 선례가 조사를 쓴다 — `연결이 완료되었어요.`·`서명 요청은 이미 완료되었어요.` (§1 교정 ②) |
| 9 | ⚠️ 키 거버넌스(2차) | 프리픽스 `mini_kpick_*` 선례 0건 | **사용자 결정 → `mini_guidekim_kpick_oa_consent_*`** | K-Pick 탭 기존 키가 전부 `mini_guidekim_kpick_*`. 같은 기능 영역이 두 네임스페이스로 갈리는 것을 방지(§5 #1) |

**실제 위반: 2건(전부 수정 완료) / 오탐: 3건(기각) / 사용자 결정 반영: 4건 / 이관(사용자 결정 사안): §5로**
→ **P0 = 0건(자동 + 수동), 조치 필요 위반 0건.**

오탐 3건은 모두 **용어집의 단일(라벨형) 매핑이 문장형 문맥을 못 덮는** 구조적 오탐이며 → §5 (d-1) 용어집 보완 권장으로 연결한다.

---

## 5. 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

### #1 ⚠️ 프리픽스 정정 경위 (기록 — 재발 방지)

**초판에서 프리픽스 선택지를 제시할 때 「기존 K-Pick 탭 키가 전부 `mini_guidekim_kpick_*` 네임스페이스」라는 실측 사실을 조사하지 않고 물었다.** 2차 독립 검토가 이를 지적해 재실측했고(`mini_guidekim_kpick_tab`·`_label`·`_jp_banner_title`·`_jp_banner_desc` / `mini_kpick_*` 선례 0건), 사용자에게 **정정 보고 후 재결정**을 받아 `mini_guidekim_kpick_oa_consent_*`로 확정했다.

- **교훈**: `md/translate.md` Step 2-1은 「신규 키 후보 **문구**마다 유사 키 검색」을 요구하지만, **프리픽스(네임스페이스) 선례 조사는 명시돼 있지 않다.** 문구가 신규여도 **같은 기능 영역의 기존 네임스페이스**는 확인해야 한다 → **Step 2-1에 「프리픽스 선례 조사」를 추가할 것을 권장**(규칙 보강 제안).
- ⚠️ `kpick_` 프리픽스는 `md/IA.md` §2-7에서 **「잠정」·승인 대기**(IA 미결 #496) 상태다. Screen ID `kpick_voucher_oa_01_01`도 그 잠정 어휘에 의존하므로 프리픽스 확정 시 재확인이 필요하다.

### #2 XLT 시스템 업로드 범위 — FE 확인 필요

4키 **전부 신규 등록**이다(Unifi 2,143키 + Dapp Portal 1,599키 = 3,742키 전수에서 동일·정규화 동일 **0건**). 어느 서비스에 올릴지는 화면이 참조하는 키스페이스에 달렸다.

| 서비스 | `mini_guidekim_` 기존 키 | 판단 |
|---|---|---|
| **Dapp Portal** / WEB BROWSER / v2.6.1 | **59건**(K-Pick 관련 4건 포함) | 기존 `mini_guidekim_kpick_*`가 여기 있으므로 **1차 후보** |
| **Unifi** / WEB BROWSER / v1.6.7.1 | 8건 (K-Pick 관련 0건) | 화면이 Unifi 키스페이스를 참조하면 함께 등록 필요 |

→ **FE에 참조 키스페이스를 확인한 뒤 업로드**한다. 쓰기 API가 없어 **업로드는 사용자가 직접 수행**한다(`md/xlt-verify.md` §7).

### (d-1) 용어집 보완 권장 (v4.7 → 차기)

| 용어 | 권장 근거 | ko_KR | en_US | ja_JP | zh_TW | th_TH |
|---|---|---|---|---|---|---|
| **바우처** | 용어집 미등재라 검증기가 못 잡는다. 등록값 정본은 `mini_guidekim_cat_voucher`로 확립. ⚠️ **th·zh는 기존 키에 표기 이탈이 남아 있다** — th `เวาเชอร์` 2키 vs `บัตรกำนัล` 2키(2:2 균등 혼용), zh `優惠券` 3키 vs `憑證` 1키. 등재로 정본을 고정하고 이탈 키를 정리할 것을 권장 (※ 2026-08-24 GuideKim 배너 리포트에서도 동일 권장 — **미해소 이월**) | 바우처 | voucher | バウチャー | 優惠券 | เวาเชอร์ |
| **포인트** | 등록값 36키에서 표기가 이미 일관(`ポイント`/`points`/`คะแนน`/`點數`)인데 미등재라 검증 사각지대다 | 포인트 | point | ポイント | 點數 | คะแนน |
| **구매 완료**(문장형) | 검증기가 en `Purchase`·th `สำเร็จแล้ว`를 **P1로 오탐**(= `구매`=Buy·`완료`=เสร็จ 라벨형 매핑이 문장형을 못 덮는다는 신호). v4.2 「지급 완료」 등재와 동일 구조의 해법 | 구매 완료 | Purchase complete *(라벨은 Buy)* | 購入完了 | 購買完成 | ซื้อสำเร็จ *(라벨은 เสร็จ)* |

> 용어집은 **API 읽기 전용**이라 Claude가 직접 쓰지 못한다. 승인 시 `md/landpress.md` 절차로 **전체 JSON을 산출**해 전달하고, 사용자가 Landpress CMS(`web3_xlt_json`)에 붙여넣어 반영한다. **용어집 버전이 오르면 같은 작업에서 기획자 가이드 zip도 필수 동반 갱신**(`md/landpress.md` §5-1).

### (d-2) 그 외 개선·확인 권장

| # | 항목 | 권장 내용 |
|---|---|---|
| **3** | ⚠️ **위키 History와 본문 Policy 불일치 (이번 작업 전부터 존재 — 손대지 않음)** | History 2번째 행은 「Policy에 합의 사항 **5개 구분**(우선순위 과제 / 9·11 타겟 내재화 / GuideKim 결제 화면 / 복합결제 / 바우처 구매 후 Unifi OA 동선), **후속 액션·확인 필요 4건 분리**」라고 기재하지만, 현재 본문 Policy 표는 **2행**(우선순위 과제 / 바우처 구매 후 Unifi OA 동선)이고 「후속 액션」 섹션은 없다. **v1 실측**: v1에는 Policy 5행 + 후속 액션 섹션이 모두 있었고 **v2(사용자 편집)에서 3행과 후속 액션 섹션이 삭제**됐다. 의도적 축약이면 History 문구를 맞추고, 실수면 v1에서 복원할지 확인 필요. **판단 사안이라 이번 작업에서 건드리지 않았다** |
| **4** | **Figma 원본 교정 요청** | 디자이너에게 확정본 반영 요청 — ⓐ 맞춤법 `구매 완료 되었어요!` → `구매가 완료되었어요!`(붙여쓰기 + 조사) ⓑ 개행 앞 불필요한 공백 제거(`동의하시면 \n` → `동의하시면\n`). §1 alias 2건 기록됨 |
| **5** | **동의 범위 고지 (기획·법무 확인)** | 화면 목적은 「Unifi LINE 공식 계정 **친구추가** + **알림 수신 동의**」인데 문구는 알림 수신만 말한다(`Unifi 알림 수신에 동의하시면…`). LINE OA 친구 추가가 알림 수신의 전제이므로, 동의 범위 고지가 충분한지 확인이 필요할 수 있다. **5개 언어가 ko를 일관되게 따르므로 번역 결함은 아니다** — 원문 정책 사안 |
| **6** | **`쏠쏠한` 표현의 의역 폭** | 구어 마케팅 표현이라 5개 언어 의역 폭이 넓다(ja `お得な` / en `great` / th `สุดคุ้ม` / zh `超值`). 전부 「좋은 가치」로 수렴시켰으나, 원문을 `유용한`·`알찬` 등 표준어로 명확화하면 언어 간 일관성이 올라간다. **원문 변경이라 승인 필요** |
| **7** | **ja `通知受信` 자연스러움** | `通知の受信`이 문법적으로 더 자연스럽다. 다만 동일 원문의 등록값 정본이 `マーケティング情報受信の同意`로 **무연결 복합명사가 이미 선례**라 현행도 선례 일치 — 오류가 아니라 **정책 선택**. 우선순위 낮음 |
| **8** | **키 이름과 내용의 길이** | `mini_guidekim_kpick_oa_consent_purchase_complete`는 48자로 길다. 화면이 GuideKim이 아니라 **K-Pick 탭**인데 `guidekim`이 프리픽스에 들어가는 구조적 괴리도 있다(#1 결정의 트레이드오프). **키 거버넌스상 FE 전달 후에는 변경하지 않으므로**(공식 룰) 등록 **전에** 최종 확인 권장 |
| **9** | **`캐시백 10%` 배지가 이번 대상에서 제외됨** | 프레임 배경의 바우처·화장품 카드에 `캐시백 10%`·`캐시백 5%` 배지가 12개 있으나 `xlt` 마커가 없어 대상에서 제외했다. 이미 `mini_guidekim_cashback`(ko `캐시백 {0}%`)로 등록돼 있어 신규 키가 불필요해 보이지만, **이 화면이 그 키를 참조하는지는 확인되지 않았다**. ⚠️ IA §2-7 실측 경고 「**캐시백률은 고정값이 아니다**」(환경별로 다르게 노출) — 수치를 상수로 인용하지 않는다 |
| **10** | **Figma 프레임명 오타** | 프레임 이름이 `Unfi OA 친구추가 동의 팝업`으로 **`Unifi`의 `i`가 빠져 있다**(`Unfi`). 위키 첨부 파일명·Description의 Figma 병기도 원본 이름을 따랐다. 디자이너에게 프레임명 교정 요청 권장 |
| **11** | **Related Docs·Flow 임베드 대상과 이번 프레임의 관계** | 위키의 Design 링크·Flow 임베드는 SECTION `바우처 구매 후 Unifi OA 친구추가`(67971:11654)를 가리키지만, 이번 프레임(68080:20746)은 **그 섹션 안이 아니라 같은 페이지의 형제 노드**다(실측). 프레임을 섹션 안으로 옮기거나, Related Docs에 이 프레임 링크를 별도 행으로 추가할지 확인 필요 |
| **12** | **`deprecated_terms`와 페이지 제목** | 위키 제목·History·Policy가 `Unifi OA`라는 사내 약어를 쓴다. 용어집 `deprecated_terms`는 **화면 문구**에 대한 규칙이라 문서 제목은 위반이 아니지만(이번 4키 문구에는 미검출), 화면 문구로 옮길 때는 `Unifi LINE 공식 계정`을 써야 한다 |

---

## 6. 게이트 통과 판정

| 기준 | 결과 |
|---|---|
| 한국어 원문 교정 선행 | ✅ 3건 교정 완료(§1 — 맞춤법 1 + 조사 보완 1 + 비표준 공백 1) + alias 2건 기록 |
| 자동 검증 실행 | ✅ P0 0 / P1 2 / P2 1 (4키 · 확정본) + 보조 코드 스캔 8종 이상 0건 |
| 수동 3단계 전수 검토 | ✅ **20셀 전부** 1·2·3단계 검토(§3) — 신규·변경 키로 한정하지 않음 |
| 2차 독립 검토 교차 확인 | ✅ **실제 결함 2건 검출·수정** · 1차 오탐 판정 3건 동의 · 확인 필요 5건 중 4건 사용자 결정 반영 |
| 각 건 처리 판정 | ✅ 9건 전건 판정(§4) — 실제 위반 2(수정) / 오탐 3(기각) / 사용자 결정 4(반영) |
| (d) 추가 개선·제안 | ✅ (d-1) 3건 · (d-2) 10건 + 정정 경위 #1 · 업로드 범위 #2 (§5) |
| **P0 = 0건 (자동 + 수동)** | ✅ **통과** |

→ **엑셀 생성·위키 반영 가능.** 시스템 업로드는 쓰기 API가 없어 **사용자가 직접 수행**한다. 업로드 범위는 **§5 #2**를 먼저 확인할 것.

---

## 7. 위키 반영 (완료)

**대상**: [pageId 4637821100](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4637821100) — `K-Pick 바우처 구매 후 Unifi OA 친구추가 및 Unifi OA 발송` · **v2 → v3**

| 변경 | 내용 |
|---|---|
| **Screen 섹션 신설** | 비어 있던 `<h2>Screen</h2>`에 **4컬럼 표**(`Screen ID \| Screen \| Description \| XLT`) + 안내 문구(빨강=정책·파랑=XLT 마커) 삽입. Screen ID = **`kpick_voucher_oa_01_01`**(사용자 승인) |
| **어노테이션 이미지 첨부** | `Unfi_OA_친구추가_동의_팝업.png`(attachment id 4639688389) — 통합 번호 ⓝ 5개, 정책=빨강 1개 / XLT=파랑 4개, 겹침 0, 육안 검증 완료. 본문은 `ri:attachment` 단독 참조(`ri:page` 미사용) |
| **Description** | 화면 설명 2문장 + Figma 프레임명 병기 + 정책 코멘트 `1. 바텀 시트 닫기`. 순수 `xlt` 마커(No 2~5)는 Description에 쓰지 않고 XLT 표의 `No`로 나타난다(통합 번호 상보 규칙) |
| **XLT 중첩표** | `No \| XLT Key \| KR` 3컬럼 — No 2·3·4·5(통합 번호와 1:1) |
| **다국어 번역 섹션 신설** | `XLT Key \| KR \| JA \| EN \| TH \| ZH-TW` 6컬럼 표(4키) + 담당 FE 팀(LV) 명시 + 검증 요약 + **후속 확인 필요**(신규 등록 · 업로드 서비스 FE 확인 · `kpick_` 잠정 프리픽스) |
| **XLT 엑셀 첨부** | `xlt_output_20260824181908.xlsx` + 본문 `ri:attachment` 다운로드 링크 |
| **History** | `md/wiki.md` **같은 날짜 병합 규칙** — 2026-08-24 행이 이미 2개 있어 새 행을 만들지 않고 당일 마지막 행(PIC `Claude 자동 생성`)의 변경 내용에 `<ul>` 3항목 누적. **기존 2행·모든 기존 항목 보존** |
| **보존 확인** | TOC 매크로 · Related Docs 표 · Schedule · Policy 표 2행 · Flow widget 매크로 · History 기존 2행 — 전부 가드 assert로 보존 확인 |

**검사 결과**: `check_wiki_storage.py pre`(보낼 storage) **exit 0** → PUT → `post`(라이브 렌더) **exit 0**. PUT 직전 **버전 가드**(라이브 재조회로 v2 확인) 통과.

**편집 가드**: 편집 전 `Screen` 섹션 형태·다국어 섹션 부재·키 미반영을 assert로 확인하고, 편집 후 기존 8개 앵커(`UNIFY-10309`·`문서생성`·`우선순위 과제`·`바우처 구매 후 Unifi OA 동선`·`widget`·`toc`·`Related Docs`·`Schedule`) 보존을 assert로 확인했다. §5 (d-2) #3의 기존 불일치는 **판단 사안이라 손대지 않았다**.

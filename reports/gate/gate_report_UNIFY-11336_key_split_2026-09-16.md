# 게이트 리포트 — UNIFY-11336 키 분리 (혜택 상세 바텀시트 전용 키 신설) 2026-09-16

- 신규 키: **`UF_my_benefit_bottomsheet_total_desc`** (Unifi · UIT 계열 `{{0}}`) × 5개 언어
- 기존 키: `UF_my_benefit_total_desc` — **변경 없음**(My 영역 1줄 전용으로 남긴다 · 엑셀 미포함)
- 배경: 같은 키를 My 영역(1줄)과 혜택 상세 바텀시트(2줄)가 공유해 개행 삽입이 My까지 바꿔버리는 문제 → **사용자 결정으로 키 분리**
- 검증 대상 파일: `xlt/xlt_output_20260916173046.xlsx` — **전체 행(1행) 전수 점검**

## 키 이름 — 선례 실측 (⛔ 등록 전이 유일한 기회)

| 후보 표기 | 등록값 선례(Unifi+Dapp Portal) | 판단 |
|---|---|---|
| `bottomsheet` (붙여쓰기) | **32건** (`UF_event_bottomsheet_title/desc/cta_btn` 등 `{영역}_bottomsheet_{요소}` 패턴) | **채택** |
| `bottom_sheet` | **0건** | 기각 |
| `_sheet` | 8건 (`UF_bridge_inprogress_sheet_*`) | 소수 |
| `_detail` | 367건이나 「상세 **화면**」 의미로 통용 — 바텀시트 식별에 부적합 | 기각 |

- 화면의 GA Screen ID가 **`my_benefit_detail_bottomsheet_01`** 이라 `bottomsheet` 표기가 화면 식별자와도 정합한다.
- 기존 형제 키 계열: `UF_my_benefit_total_desc` · `_month` · `_banner_received` · `_mission_reward` · `_cashback_point` · `_empty_title`.

## (1a) 한국어 원문 교정

- 디자인 요청서 AS-IS는 `이번달`이나 정본은 **`이번 달`**(의존명사 띄어쓰기) — **정본 유지**, alias 기록: `{Figma 주석 이번달 → XLT 이번 달}`.
- 신규 키 ko 값은 기존 등록값과 **같은 문구 + 개행**이며 새 문구를 만들지 않았다.

## (a) 자동 검증 요약 — `validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

## (b) 수동 3단계 체크표 (전체 행 전수)

| 단계 | 점검 | 결과 |
|---|---|---|
| 1단계 (한국어) | 맞춤법·띄어쓰기(`이번 달`)·개행 위치가 어절 경계 | ✅ 이상 없음 |
| 2단계 (용어집) | `혜택` 표기·금액 `{{0}}` 자리 | ✅ 일치 |
| 3단계 (다국어) | 변수 `{{0}}` 5개 언어 각 1회 보존 · 언어 혼입 없음 · 개행은 ko·ja·en 3개 언어(요청 범위) · th·zh는 기존 등록값 그대로 복사(개행 없음) · ja 마침표 유지 · en 마침표는 디자인 TO-BE 반영 | ✅ 이상 없음 |

- 개행은 **실제 개행 문자**(선례 실개행 214키 / 리터럴 `\n` 33키).

## (c) 검증기 보고 각 건 처리 판정

- 검증기 보고 **0건** — 실제 위반·오탐 모두 없음. 판정 대상 없음.

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장
- 신규 발견 없음. 직전 리포트에서 갱신한 **「최대」 zh 문맥 분기**(대기 목록 #3 · 실측 `最高` 78 / `最多` 16 / `最大` 7)가 유효하며 **대기 목록 등재 상태** 그대로 둔다.

### (d-2) 추가 개선·제안 안내
1. ⚠️ **기존 확정과의 관계** — 이 저장소에는 **「줄바꿈은 화면에서 처리하고 XLT 값에는 넣지 않는다」(2026-09-13 확정 · `UF_clinic_detail_*`·`mini_voucher_detail_*`)** 선례가 있다. 이번 건은 사용자 결정으로 **값에 개행**을 넣되, **키를 분리해 영향 범위를 바텀시트로 한정**했다. 두 방식이 공존하므로 이후 유사 요청 때 **어느 쪽을 정본으로 할지** 정리가 필요하다.
2. **FE 전달 필요** — 바텀시트 렌더 지점의 키 교체(`UF_my_benefit_total_desc` → `UF_my_benefit_bottomsheet_total_desc`)는 **FE 작업이며 릴리즈가 필요**하다. 키 업로드만으로는 반영되지 않는다.
3. **th·zh 미요청** — 두 언어는 개행 없이 그대로 두었다. 바텀시트에서 3개 언어만 2줄이 되므로 디자인 확인이 필요하면 별도 요청 사안이다.
4. **기존 키 정리 시점** — FE가 바텀시트를 새 키로 교체한 뒤에도 `UF_my_benefit_total_desc`는 My 영역에서 계속 쓰이므로 **폐기 대상이 아니다**.

## 통과 판정

- **P0 = 0건**(자동 + 수동) → 엑셀 산출·위키 반영 진행
- P1/P2 0건 · (d)는 사용자 결정 전까지 임의 반영하지 않음

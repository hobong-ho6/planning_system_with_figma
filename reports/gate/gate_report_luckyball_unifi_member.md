# 번역 검증 게이트 리포트 — 피초대자 페이지 유니파이 회원 팝업 (63652:47034)

- 대상 위키: pageId 4479306980 (럭키볼 친구에게 선물하기 캠페인) / 섹션 **피초대자 프로모션 페이지** / 팀 **LV** (`mini_luckyball_` · `{0}`)
- 대상 엑셀: `xlt/xlt_output_20260724135339.xlsx` (74키)
- 모드: 코멘트 선별(`XLT` 마커 3건) — 팝업 화면 육안 검증 완료
- 용어집: API v2.6 (용어 35 / 예외 9) 새로 조회.

## 처리 대상 (마커 3건 → 텍스트, 팝업 육안 검증 ✅)

| No | 텍스트 | 키 | 처리 | 매칭 |
|----|--------|----|----|------|
| 1 | 이미 Unifi 회원이시네요! | `mini_luckyball_already_member` | **신규** | 포함(팝업 타이틀) |
| 2 | Unifi에서 참여 가능한 다양한 미션을\n확인하고 리워드를 받아보세요! | `mini_luckyball_missions_promo` | 기존 재사용 | 포함(팝업 본문) |
| 3 | 홈으로 가기 | `mini_luckyball_go_home` | 기존 재사용 | 포함(팝업 버튼) |

- **팝업겹침 육안 검증**: 렌더 이미지에서 3개 마커가 흰색 안내 팝업의 타이틀·본문·버튼 위에 있음을 확인. 배경(`축하합니다! 100 JPYC…`)로의 오매칭 없음.
- **키 재사용 규칙**: #2·#3은 동일 ko_KR로 위키·엑셀에 이미 정의된 키를 그대로 재사용(신규 생성 금지 규칙). #1만 신규.

## 0. 한국어 원문 교정 (게이트 1단계)

- 신규 키 ko_KR `이미 Unifi 회원이시네요!` — 맞춤법·띄어쓰기·표기 이상 없음, Figma 원문과 동일(교정 불필요). alias `{이미 Unifi 회원이시네요! → mini_luckyball_already_member}` 기록.
- 재사용 2키는 원문 변경 없음.

## Executive Summary (자동 — `scripts/validate_translation.py`, 74키)

| 심각도 | 건수 | 카테고리 |
|--------|------|----------|
| 🔴 P0 (Critical) | **0건** | 빈칸, 오타, 언어 혼입, 용어집 표기 위반 |
| 🟡 P1 (Medium) | 25건 | 용어 불일치(용어집 협소 매핑) |
| 🟢 P2 (Low) | 27건 | 마침표 스타일 |

→ **P0 = 0**. 신규 키 `already_member`는 P1·P2 **무플래그**(클린).

## 1단계: 한국어 맞춤법·띄어쓰기 (수동, 전수 74키)

- 신규 키: `이미 Unifi 회원이시네요!` — 오타·띄어쓰기 이상 없음, `!` 종결 자연스러움. 존댓말 "-이시네요" 톤 적절.
- 전체 74키 재점검: P0 0. 외래어 음차 오염·단어 붙은 띄어쓰기 누락·다의어 모호·표현 어색 사례 없음.

## 2단계: 용어집 위반 (수동, 전수)

- 예외 패턴(JPYC, USDT, PIN 등) 표기 통일 확인. zh_TW 번체 정자 확인(신규 키 `會員`·`已經` 정자). th 자산 표기 일관성 확인.
- 자동 P1 25건은 전건 용어집 협소 매핑 오탐(아래 판정).

## 3단계: 다른 언어 검토 (수동, 전수)

- 빈칸 0, 언어 혼입 0, 컬럼 swap 0. placeholder 무관(신규 키에 변수 없음).
- 신규 키 5개 언어 재검토:
  - ja_JP `すでにUnifi会員でいらっしゃいますね！` — 존경 표현 자연.
  - en_US `You're already a Unifi member!` — 간결·정확.
  - th_TH `คุณเป็นสมาชิก Unifi อยู่แล้ว!` — 정중·정확.
  - zh_TW `您已經是 Unifi 會員了！` — 번체 정자, 자연.

## 각 건 처리 판정 (자동 보고분 전건)

### P1 (25건) — 전건 **오탐 (용어집 협소 매핑)**
- `확인`→check/查看 (confirm/notice/missions_promo 등), `변경`→更換, `미션`→mission, `복사`→copy, `로그인`→log in, `송금`→send/轉帳, `예치`→預入(ja): 현재 번역이 문맥상 정확하며 용어집 단일 매핑 강제로 인한 오탐. 실제 위반 아님 → 수정 안 함.

### P2 (27건) — 전건 **오탐 (마침표 스타일)**
- 배너·타이틀·알림·팝업 문구는 UI 관습상 마침표 미사용 또는 `!` 종결. 의도된 스타일 → 유지.

## (d) 추가 개선·제안 (권장 — 사용자 결정 사안)

- **(d-1) 용어집 보완 권장**: `확인`·`변경`·`송금`·`미션`·`복사` 다의어 단일 매핑으로 정상 의역이 P1로 반복 오탐(25건). 문맥 변형 추가 시 오탐 감소. API 읽기전용이라 `md/landpress.md` 절차로 전체 JSON 산출→사용자가 Landpress CMS 반영. 이번 신규 키는 해당 없음.
- **(d-2) 마침표 정책**: 배너/타이틀/팝업 무마침표 현행 유지 권장(임의 미적용).

## 통과 판정

- 자동 **P0=0** + 수동 3단계 **P0=0** → **통과**.
- 신규 키 1건(`already_member`) 추가, 74키 엑셀 재생성. P1/P2 전건 오탐(수정 없음). (d) 권장 미반영.

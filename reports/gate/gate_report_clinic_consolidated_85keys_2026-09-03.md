# 게이트 리포트 — 클리닉 통합 엑셀 85키 (위키 다국어 표 전량)

- **작업일**: 2026-09-03 (clinic 세션 #3)
- **목적**: 위키 `4667512757`에 첨부된 `xlt_output_20260901170934.xlsx`(**51키 · 2026-09-01**)가 낡아, **위키 다국어 번역표 전량을 통합 엑셀로 재생성**
- **정본 소스**: 위키 라이브 **v105** 다국어 번역 섹션 9개 (작업 시점 재조회 — 캐시 미사용)
- **산출물**: `xlt/xlt_output_20260903215314.xlsx` — **85키 × 5개 언어 = 425셀**
- **레지스트리**: Unifi / WEB BROWSER / v1.7.4 · 2,431키 · **용어집** v4.7 · 116 용어 (작업 시점 신규 조회)

## 낡은 첨부 대비 변화

| 구분 | 건수 |
|---|---|
| 위키 다국어 표 전체 | **85키** |
| 기존 첨부 엑셀 | 51키 |
| **추가된 키** | **45건** (09-01 후반 + 09-03 세션 3회분) |
| 기존 첨부에만 있던 키 | 11건 — `UF_clinic_detail_process_step1~4_title/desc` **8키**(2026-09-01 Landpress `process` 이관으로 위키에서 제거) + `UF_voucher_detail_caution`·`_cancel`·`UF_voucher_mini_certified` 3키(재파싱으로 이번엔 포함됨) |

※ 최초 집계 시 78키였던 것은 **파서 결함**이었다 — 키 셀에 `status` 매크로가 붙은 7키(`UF_main_faq_title`·`UF_send_guide_copy_btn`·`UF_voucher_detail_cancel`·`_caution`·`UF_voucher_mini_certified`·`UF_voucher_recommend_title`·`UF_voucher_review_count`)를 정규식이 놓쳤다. 셀 경계를 **깊이 추적**으로 분할하고 매크로를 제거하는 파서로 교체해 85키를 확보했다.

※ **OA 메시지 3섹션은 제외**했다 — OA는 XLT 키를 부여하지 않는 영역(`md/OA.md`)이므로 XLT 업로드 대상이 아니다.

---

## (1a) 한국어 원문 교정

이번 작업은 **위키에 이미 게이트를 통과해 반영된 값의 통합**이므로 신규 원문 교정은 없다. 각 값의 원문 교정 이력은 원 게이트 리포트가 정본이다:

| 교정 | 원 리포트 |
|---|---|
| `상담문의` → `상담 문의` | `gate_report_clinic_service_no_price_menu_2026-09-03.md` |
| `편한 의사 소통` → `편한 의사소통` | `gate_report_clinic_bridge_rolling_banner_10keys_2026-09-03.md` |
| `리뷰(164)` → `리뷰 ({{0}})` · `결제금 15% 캐시백` → `결제금 {{0}}% 캐시백` · U+2028 정규화 | `gate_report_clinic_mini_bridge_19keys_2026-09-03.md` |
| `더보기 를` → `더보기를` | `gate_report_clinic_favorite_guide_2keys_2026-09-03.md` |
| `가능합니다.` → `가능해요.` 등 문체 3건 | `gate_report_landpress_clinic_4sections_2026-09-01.md` |

**alias 무결성 확인**: 위 교정값이 통합 엑셀에 그대로 실렸는지 전건 대조 완료(`상담 문의`·`편한 의사소통`·`리뷰 ({{0}})`·`결제금 {{0}}% 캐시백`·`더보기를` 확인).

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py`

실행: `python3 scripts/validate_translation.py xlt/xlt_output_20260903215314.xlsx scripts/glossary.json`

| 심각도 | 건수 |
|---|---|
| 🔴 **P0 (Critical)** | **0건** |
| 🟡 P1 (Medium) | 59건 |
| 🟢 P2 (Low) | 9건 |

---

## (b) 수동 3단계 체크표 — **전수 점검 (전체 85행 × 5언어 = 425셀)**

검토 범위: **전체 파일**. 「검증」 요청이므로 신규·변경 키로 한정하지 않고 **기존 키까지 전수** 점검했다.

### 1단계 — 기계적 정합 (전수 스캔 결과)

| 항목 | 결과 |
|---|---|
| 빈 셀 | ✅ **0건** (425/425 채움 — 엑셀 생성 시 assert 가드) |
| 치환자 정합 | ✅ **0건 불일치** — 치환자 보유 7키의 `{{N}}` 집합이 5개 언어 전부 동일 |
| 단일 중괄호 `{0}` 혼입 (LV 표기) | ✅ 0건 — 전건 **UIT `{{0}}`** |
| nbsp(U+00A0) | ✅ 0건 |
| U+2028 / U+2029 | ✅ 0건 |
| 이중 공백 | ✅ 0건 |
| 앞뒤 공백 | ✅ 0건 |
| BOM | ✅ 0건 |
| ko 칸에 가나·타이문자 | ✅ 0건 |
| ja 칸에 한글·타이문자 | ✅ 0건 |
| th 칸에 한글·가나 | ✅ 0건 |
| zh 칸에 한글·가나 | ✅ 0건 |
| zh 간체 혼입(상용 간체 34자 스캔) | ✅ 0건 |
| en 칸에 CJK·한글·타이문자 | ✅ 0건 (**검증기 사각지대 보완 스캔**) |
| ja/en/th/zh = ko 완전 일치(미번역) | ✅ 0건 (라틴 고정 문자열 3키는 의도적 — (c) 참조) |

### 2단계 — 의미 정합 (ko_KR 기준)

85키는 **9개 화면 섹션에 걸친 기존 반영값**이며, 각 값의 의미 판정은 원 게이트 리포트가 정본이다. 이번 통합에서는 **교차 정합**을 중점 점검했다:

| 점검 | 결과 |
|---|---|
| 같은 ko 문구가 다른 키에 | ⚠️ **1건** — `LINE 무료 상담`이 `UF_clinic_detail_btn_consult_line`(상세 하단 CTA)·`UF_clinic_mini_bridge_badge_consult_title`(브릿지 배지)에 중복. **5개 언어 값이 완전히 동일** → 키 통합 후보 → (d-2) |
| 브랜드 표기 `LINE` | ✅ 11키 전부 대문자 — `Line`·`라인` 오표기 **0건** |
| 브랜드 표기 `MINI App` | ⚠️ **불일치** — `UF_voucher_mini_certified`는 `MINI App`, `UF_clinic_mini_bridge_badge_certified_desc`는 `Mini App` → (d-2) |
| `Unifi mini` · `GuideKim` · `JPYC` | ✅ 각 3·3·2키 표기 동일 |
| 캐시백 ja 표기 | ✅ 전건 `キャッシュバック` — `還元` **0건**(2026-09-03 교체 반영 확인) |
| 「캐시백 혜택」 계열 3언어 | ✅ clinic 영역 일관 — en `cashback` / th `เงินคืน` / zh `現金回饋` |

### 3단계 — 언어별 중점 점검

| 언어 | 결과 |
|---|---|
| ko | ✅ 문체 혼재 없음(해요체 기조) · 조사·받침 정상(`LINE으로`·`JPYC로`) |
| ja | ✅ 경어체 일관 · 음차 오염 0 · 라벨은 체언 종결 |
| en | ✅ 문장 첫 글자만 대문자(라벨 관례) · 브랜드 대문자 유지 |
| th | ✅ 어절 결합·전치사 정상 · 성조 부호 정상 |
| zh | ✅ 전건 번체 정자 · 전각 구두점(`，`) 등재 관례 준수 |

---

## (c) 자동 검증 보고 건별 처리 판정 (P1 59 + P2 9 = 68건 전건)

### P1 59건 — 유형별 전수 판정

| 유형 | 건수 | 판정 | 근거 |
|---|---|---|---|
| ko 「혜택」 → en `perks` | 9 | **오탐** | 실측 `benefit` **29건** vs `perk` **6건** → (d-1) |
| ko 「혜택」 → th `สิทธิประโยชน์` | 7 | **오탐** | clinic 영역은 「캐시백 혜택」을 `เงินคืน` 단독으로 옮긴다(4/4 선례). 일반 「혜택」에는 채택 중 |
| ko 「혜택」 → zh `優惠` | 5 | **오탐** | clinic 영역 선례가 `現金回饋` 단독 |
| ko 「결제」 → th `การชำระเงิน` | 4 | **오탐** | 등재 선례가 동사구 `ชำระ`(`ของยอดชำระ`·`ไม่ต้องชำระล่วงหน้า`) |
| ko 「결제」 → zh `結帳` | 4 | **오탐** | 실측 `付款` **25건** · `結帳`은 체크아웃 한정 → (d-1) |
| ko 「최대」 → en `Max` | 2 | **오탐** | 등재 `up to 15% cashback` 선례 |
| ko 「최대」 → zh `最多` | 2 | **오탐** | 비율에는 `最高`가 정확(등재 선례) |
| ko 「더보기」 → en `View More` | 2 | **오탐** | 실측 `See more` **4건** vs `View more/More` **3건** · 완전일치 키 en = `See more` |
| ko 「더보기」 → ja `すべて見る` | 2 | **오탐** | `すべて見る` 6건은 **전부 ko 「전체보기」** 번역. ko 「더보기」는 `もっと見る` 5건 정본 → (d-1) |
| ko 「더보기」 → zh `查看更多` / th `ดูเพิ่มเติม` | 2 | **오탐** | 채택값에 권장 어형이 **분절 포함**(`查看更多評論`·`ดูรีวิวเพิ่มเติม`) — 연속 문자열만 보는 검증기 한계 |
| ko 「교환/교환하기」 → en `swap`·ja `スワップ`·th `แลกเปลี่ยน`·zh `交換` | 6 | **오탐** | 용어집의 「교환」은 **토큰 스왑** 문맥 등재값이다. 이번 맥락은 **포인트→JPYC 전환**이라 스왑 어휘가 부적합 |
| ko 「구매/구매하기」 → en `Buy`·`Buy now` | 2 | **오탐** | 등재 `UF_clinic_detail_point_buy_title` en 선례 유지 |
| ko 「확인」 → en `check`·th `ตรวจสอบ`·zh `查看` | 3 | **오탐** | `Google 지도로 확인하기`는 「열어서 보기」 의미로 `View`·`ดู`·`查看` 계열이 정확 |
| ko 「복사」 → en `copy` | 1 | **오탐** | 버튼 라벨이라 `Copy` 대문자 시작이 정상(용어집은 소문자 등재) |
| ko 「가격」 → ja `価格`·zh `售價` | 2 | **오탐** | `상담 문의`(가격 자리 라벨) 번역이 `要相談`·`價格請諮詢`으로, 용어집 단어와 1:1 대응하지 않는 의역 |
| ko 「유의사항」 → th `หมายเหตุ` | 1 | **오탐** | 등재 선례가 `ข้อควรระวัง` 계열 |
| ko 「혜택」 → ja `特典` | 1 | **오탐** | 해당 셀은 이미 `特典`을 포함한 복합어(`キャッシュバック特典`)다 |
| ko 칸 라틴 문자열 (`EXCLUSIVE BENEFIT`·`FAQ`·`Mini App`) | 3 | **오탐** | 고유명·브랜드·영문 그대로 노출하는 배지·탭 라벨. 5개 언어 동일이 정상 |
| `UF_main_faq_title` ja 번역 누락 의심(`FAQ`) | 1 | ✅ **의도된 값 — 확인 완료** | 첨부 `xlt_fix_main_faq_title_ja_1key_20260901.xlsx` 내용을 실측한 결과 **ja=`FAQ`가 목표값**이었다. 2026-09-01 수정 엑셀이 업로드되지 않아 남아 있던 **핸드오프 P1 미결을 이 통합 엑셀이 해소**한다. 단 형제 키 `UF_main_faq_more` ja가 `よくある質問をもっと見る`라 **같은 화면에서 표기가 갈린다** → (d-2) |
| **합계** | **59** | **실제 위반 0 · 오탐 58 · 확인 완료 1** | |

### P2 9건 — 마침표 스타일

9건 전부 **오탐**이다. 지적된 값은 모두 **문장형**(`…안내됩니다.`·`…보내드려요.`)이고, 등재 선례(`UF_clinic_detail_notice_1~3`·`_service_desc`)가 문장형에 마침표를 쓴다. 라벨·제목에는 마침표가 없음을 전수 확인했다.

---

## ⚠️ 업로드 시 등재값이 바뀌는 키 (사용자 확인 필요)

통합 엑셀을 업로드하면 **이미 등재된 키 3건의 값이 변경**된다. 등재값과 전건 대조한 결과다:

| 키 | 변경 | 판정 |
|---|---|---|
| `UF_clinic_detail_benefit_sub` | 5개 언어 전부 — 등재 `무료상담과 캐시백 혜택을 만나보세요` → 위키 `Unifi mini 즐겨찾기하고 무료 상담과 / 캐시백 혜택을 가장 빠르게 만나보세요` | ✅ **의도된 변경** (2026-09-03 사용자 결정 「문구가 바뀐 기존 키는 값 변경」) |
| `UF_voucher_mini_certified` | 5개 언어 전부 — 등재 `Unifi mini는 LINE 공식 인증 MINI App이에요` → 위키 `LINE 공식 인증 MINI App, Unifi mini` | ✅ **의도된 변경** (같은 결정) |
| `UF_main_faq_title` | ja만 — 등재 `よくある質問` → 위키 `FAQ` | ✅ **의도된 변경** (2026-09-01 미반영 수정분) · 단 (d-2) 확인 권장 |

**신규 등록 29키** — `UF_clinic_mini_bridge_*` 19 · `UF_clinic_oa_save_*` 3 · `UF_clinic_detail_*` 7. 나머지 53키는 등재값과 **완전 일치**(변경 없음).

---

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장

| 용어 | 현행 등재 | 권장 | 실측 근거 |
|---|---|---|---|
| 혜택 | en `perks` | **`benefit`** | `benefit` **29건** vs `perk` **6건**. 이번 파일에서만 **9건 오탐** — 누적 5세션 |
| 더보기 | ja `すべて見る` | **`もっと見る`** | `すべて見る` 6건은 전부 ko 「전체보기」. 「더보기」는 `もっと見る` 5건 |
| 결제 | zh `結帳` | **`付款` 병기** | `付款` **25건** · `結帳`은 체크아웃 한정 → `context` 분기 |
| 교환 | en `swap` / ja `スワップ` | **문맥 분기** | 등재값은 **토큰 스왑** 전용인데 「포인트→JPYC 전환」에도 매칭돼 6건 오탐 |
| 혜택 | th `สิทธิประโยชน์`(현행 유지) | **`สิทธิพิเศษ` 특별 문맥 분기** | `สิทธิประโยชน์` 33 : `สิทธิพิเศษ` 5(전부 「특별 혜택」) |

⛔ 용어집은 **API 읽기 전용**이다 — 승인 시 `md/landpress.md` 절차로 전체 JSON 산출 후 전달하고, 버전이 오르면 **기획자 가이드 zip 동반 갱신**(§5-1).

### (d-2) 추가 개선·제안 안내

1. 🔴 **`UF_main_faq_title` ja = `FAQ`는 사용자 확정 사항** — 위키 History 실측: 「`UF_main_faq_title` ja `よくある質問` → `FAQ` 확정(**사용자 1번안**) — 공유 키라 메인 화면 FAQ 제목도 함께 바뀐다」(2026-09-01). 따라서 통합 엑셀의 `FAQ`는 재확인이 필요 없다. **남은 문제는 형제 키의 일관성**이다 — `UF_main_faq_more` ja가 `よくある質問をもっと見る`라 같은 화면에서 제목은 `FAQ`, 더보기는 `よくある質問…`으로 갈린다. `UF_main_faq_more`를 `FAQをもっと見る`로 맞출지 결정 권장(이 키는 이번 파일 범위 밖 — 클리닉 위키에 없다).
2. **`LINE 무료 상담` 키 중복** — `UF_clinic_detail_btn_consult_line`(상세 CTA)과 `UF_clinic_mini_bridge_badge_consult_title`(브릿지 배지)의 **5개 언어 값이 완전히 동일**하다. 「같은 문구=같은 키」 규칙상 통합 후보이나, 화면·역할이 달라 향후 문구가 갈릴 수 있다 — 유지/통합 결정 권장.
3. **`MINI App` ↔ `Mini App` 대소문자** — LINE 공식 브랜드 표기는 **`LINE MINI App`**. `UF_clinic_mini_bridge_badge_certified_desc`를 `MINI App`으로 맞추고 Figma 원본도 교정 권장.
4. **위키 첨부 정리** — 낡은 `xlt_output_20260901170934.xlsx`(51키)와 `xlt_fix_main_faq_title_ja_1key_20260901.xlsx`(1키)는 이 통합 엑셀에 **모두 흡수**됐다. 혼선 방지를 위해 삭제 또는 「구버전」 표기를 권장한다(**첨부 삭제는 사용자 확인 후 수행**).
5. **Landpress 이관분은 이 엑셀에 없다** — `process`(8키 제거분)·`cautions`·`change_cancel`·`faq`·`voucher`·`menu`·`bridge_info`·`detail_info`는 XLT가 아니라 Landpress 관리다. 정리는 하위 페이지 「클리닉 LPC(Landpress) 관리 영역 정리」 참조.
6. **OA 메시지 3섹션 제외** — XLT 키 미부여 영역이라 이 엑셀 대상이 아니다. OA 문구는 위키 다국어 섹션과 Flex JSON zip이 정본.

---

## (4) 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 = 0 | ✅ 0건 |
| 수동 P0 = 0 | ✅ **전수 점검 425셀** 결과 0건 |
| (a)~(d) 리포트 | ✅ 본 문서 |
| 한국어 원문 교정 | ✅ (1a) — 통합 대상이므로 신규 교정 없음, 기존 교정값 5건 반영 여부 전건 대조 |
| 전수 점검 | ✅ 전체 파일(85행 × 5언어) 기준 — 신규·변경 키 한정 아님 |

→ **엑셀 생성·위키 첨부 진행 가능.** P1 59 + P2 9 = **68건 전건 판정 완료(실제 위반 0 · 오탐 67 · 확인 완료 1)**. (d) 권장 11건과 **등재값 변경 3건**은 사용자 확인 대상이며, **업로드 전 위 「업로드 시 등재값이 바뀌는 키」 표를 확인**해야 한다.

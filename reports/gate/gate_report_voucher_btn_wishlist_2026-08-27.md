# 번역 품질 게이트 리포트 — `UF_voucher_btn_wishlist` 문구 변경 (관심 상품 저장 → 구매 링크 저장)

- **작업 유형**: 키 단위 번역 패치 모드 **(A) ko-source** — 한국어 변경 → 4개 언어 전면 재번역
- **대상 위키**: [pageId 4639704548](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4639704548) `올리브영 5만원 상품상세 Unifi 직접제공` · Jira `UNIFY-10468` · **v14 → v15**
- **대상 키 (1건)**: `UF_voucher_btn_wishlist` — 바우처 상세 하단 버튼(찜하기 계열)
- **담당 FE 팀**: **UIT** — 기존 프리픽스 `UF_`로 판별 · 치환자 `{{0}}`(이 키는 변수 없음)
- **등록값 조회 시각**: **2026-08-27 11:1x KST** — `Unifi / WEB BROWSER / v1.7.2`(2,347키) · 이번 run 신규 조회(캐시 금지)
- **용어집**: v4.7 (API 조회)
- **검토 범위**: **전수 점검** — 엑셀 2종 전체를 자동 검증에 통과시키고, 대상 키는 **5개 언어 5셀 전건** 수동 검토
- **산출물 엑셀 2종**(둘 다 이 키를 포함하므로 함께 갱신):
  - `xlt/xlt_fix_guidekim_voucher_71keys_20260826.xlsx`(71키) — 위키 첨부 v3
  - `xlt/xlt_screen_keys_voucher_39keys_20260827.xlsx`(Screen 39키) — 위키 첨부 v2

---

## 0. 변경 내용 (before → after)

| 언어 | before | after |
|---|---|---|
| ko_KR | 관심 상품 저장 | **구매 링크 저장** |
| ja_JP | お気に入りに追加 | **購入リンクを保存** |
| en_US | Save to wishlist | **Save purchase link** |
| th_TH | บันทึกสินค้าที่สนใจ | **บันทึกลิงก์การซื้อ** |
| zh_TW | 儲存感興趣的商品 | **儲存購買連結** |

**착수 시점 상태**: 위키 v14에서 **Screen 표 XLT 중첩표의 KR은 이미 `구매 링크 저장`으로 사용자가 직접 변경**해 둔 상태였고, **다국어 표와 엑셀 2종만 구값**이었다. 즉 이번 작업은 나머지를 정합화하는 것이다.

- 의미 변화: 「위시리스트 담기」 → 「구매 링크 저장」. 단순 표기 변경이 아니라 **기능 서술이 바뀌었으므로 4개 언어를 전면 재번역**했다(기존 `wishlist`/`お気に入り` 계열 어휘를 그대로 두면 의미가 어긋난다).
- 위키 Screen 표 Description의 정책 서술(「관심 상품 저장 흐름이 시작된다」 등)은 **손대지 않았다** → §5 #1.

---

## 1. 한국어 원문 교정 (게이트 1a — 필수)

| 구분 | 내용 |
|---|---|
| 제공 원문 | `구매 링크 저장` (사용자 지정 · 위키 v14 Screen 표에 이미 반영돼 있던 값) |
| 교정 | **없음** — 맞춤법·띄어쓰기 정상(`구매 링크 저장`: 명사+명사+명사는 띄어 씀). 비표준 공백 0건, 오타 없음 |
| 확정 원문 | `구매 링크 저장` |

- **alias**: ko 원문이 Figma 추출이 아니라 **사용자 지정 + 위키 Screen 표 기재값**이므로 `{Figma 원문 → XLT Key}` alias 대상이 아니다. before/after는 §0 표가 기록이다.
- **Figma 원본 수정 요청 필요 여부**: 이 키의 Figma 화면 문구가 아직 `관심 상품 저장`이면 원본도 갱신해야 한다 → §5 #2.

---

## 2. 자동 검증 결과 (P0 / P1 / P2 요약)

```
python3 scripts/validate_translation.py xlt/xlt_fix_guidekim_voucher_71keys_20260826.xlsx scripts/glossary.json
python3 scripts/validate_translation.py xlt/xlt_screen_keys_voucher_39keys_20260827.xlsx scripts/glossary.json
```

| 파일 | P0 | P1 | P2 | 대상 키 지적 |
|---|---:|---:|---:|---|
| 71키본 | **0건** | 76 | 15 | **0건** |
| Screen 39키본 | **0건** | 49 | 7 | **1건**(§4 #1) |

P1·P2는 전부 **다른 키의 기존 지적**이며 선행 리포트(`gate_report_guidekim_voucher_64keys_2026-08-26.md`)가 추적한다. 두 파일의 지적 건수가 다른 것은 **대상 파일 구성이 달라 일관성 검사 범위가 달라지기 때문**이다(같은 값에 대한 판정은 동일).

> ⚠️ `P0=0`은 시작점이다. 자동 검증기는 **버튼 라벨의 어색함·기능 의미 불일치·번체/신자체 자형**을 구조적으로 못 잡으므로 §3 수동 3단계를 수행했다.

### 2-1. ⛔ 컬럼 정렬 검증 (패치 전 필수)

| 언어 | 패치 전 값 | 문자체계 판정 |
|---|---|---|
| ko_KR | 관심 상품 저장 | ✅ 한글 |
| ja_JP | お気に入りに追加 | ✅ 가나 |
| en_US | Save to wishlist | ✅ 라틴(CJK·타이 없음) |
| th_TH | บันทึกสินค้าที่สนใจ | ✅ 타이 |
| zh_TW | 儲存感興趣的商品 | ✅ 한자 |

→ **컬럼 misalignment 0건.** 두 엑셀 모두 정렬 정상이었고, 이번 패치는 **5개 언어 전부 교체**라 회전 어긋남 위험 자체가 없다. 적용은 `patch_translation.apply_translation_patch`(결정론적)로 했고 **내장 컬럼 정렬 가드(1g)를 통과**했다.

**보조 코드 스캔 (대상 키 5셀 · 2파일)**

| 검사 | 결과 |
|---|---|
| 비표준 공백·연속 공백·선후행 공백 | ✅ 0건 |
| 비-ko 칸 한글 잔존 | ✅ 0건 |
| en 칸 비라틴 문자 | ✅ 0건 |
| zh 간체(`链`·`储`·`购`·`买`·`连`·`结`) | ✅ 0건 |
| 두 엑셀 간 값 일치 | ✅ 5/5 셀 동일 |

---

## 3. 수동 3단계 체크표 (대상 키 5셀 전건)

### 1단계: 한국어

| 점검 항목 | 결과 |
|---|---|
| 띄어쓰기 | ✅ `구매 링크 저장` — 명사 연쇄는 띄어 씀 |
| 오타·비표준 공백 | ✅ 없음 / 0건 |
| 외래어 표기 | ✅ `링크`는 외래어 표기법 표준형 |
| 다의어·모호 | ✅ 없음 — 「구매 링크를 저장한다」로 일의적 |
| `deprecated_terms` | ✅ 미검출 |

### 2단계: 용어집 위반 (v4.7)

| 용어 | 등재값 | 적용 | 판정 |
|---|---|---|---|
| 저장 | 미등재 — **등록값 정본** `UF_settings_account_email_btn_save`: ja `保存` / en `Save` / th `บันทึก` / zh `儲存` | 4언어 전부 정본 사용 | ✅ **완전 일치** |
| 링크 | 미등재 · ko 사용 선례 **0건**(신규 어휘) | ja `リンク` / en `link` / th `ลิงก์` / zh `連結` | ✅ 각 언어 표준 차용어 |
| 구매 | en `Buy` / ja `購入` / th `ซื้อ` / zh `購買` | ja `購入`·th `การซื้อ`·zh `購買` 일치 / en은 명사 수식 `purchase` | ⚠️ P1 1건 → §4 #1 |
| 예외 목록 · `deprecated_terms` | — | 해당 없음 | ✅ |

**등록값 선례 실측 (2026-08-27 · Unifi v1.7.2 2,347키 전수)**

| 항목 | 실측 | 신규 값의 정합 |
|---|---|---|
| `저장` 단독 키 | `UF_settings_account_email_btn_save` = `保存`/`Save`/`บันทึก`/`儲存` | ✅ 4/4 언어 정본 채택 |
| ja `保存` 사용 | **13건** | ✅ 다수 표기 |
| ja `お気に入り` 사용 | 2건(둘 다 구 위시리스트 계열) | — 의미가 바뀌어 이탈이 맞다 |
| zh 링크 표기 | **`連結` 25건** vs `網址` 1건 | ✅ 다수 정본 `連結` 채택 (간체 `链接` 0건) |
| ko `링크` | **0건** | 신규 어휘 — 5개 언어 표기를 이번에 확립 |

### 3단계: 다른 언어 번역 검토

| 언어 | 점검 | 결과 |
|---|---|---|
| **ja_JP** | 버튼 문체 · 조사 · 한자 자형 | ✅ `購入リンクを保存` — 「〜を保存」은 일본어 UI 버튼 관용형. 신자체 정상(`購`·`存`), 번체 혼입 없음 |
| **en_US** | 버튼 간결성 · 관사 · 명사 수식 | ✅ `Save purchase link` — 동사+목적어 버튼형, 관사 생략이 UI 관례. `purchase link`(명사 수식)는 `buy link`보다 정본 |
| **th_TH** | 어순 · 차용어 표기 · 문장 끝 마침표 미사용 | ✅ `บันทึกลิงก์การซื้อ` — 동사(`บันทึก`)+목적어 어순, `ลิงก์`는 태국어 표준 차용 표기, 마침표 없음 |
| **zh_TW** | 번체 정자 · 대만 어휘 | ✅ `儲存購買連結` — `儲`·`購`·`買`·`連`·`結` 전부 번체 정자(간체 `储购买连结` 0건). `連結`은 대만 통용(중국 대륙 `链接`과 구분) |
| **전 언어 공통** | 길이 · 치환자 · 개행 · 일관성 | ✅ 버튼에 적합한 길이(ko 8자 / ja 8자 / en 18자 / th 20자 / zh 6자) · 치환자 0개 · 개행 0 · 두 엑셀 값 동일 |

### 2차 독립 검토

**이번 패치에는 `translation-reviewer` 팬아웃을 쓰지 않았다.** 근거: ⓐ 변경분이 **버튼 라벨 1건 5셀**로 최소이고 ⓑ 4개 언어 모두 **등록값 정본(`保存`/`Save`/`บันทึก`/`儲存`)과 다수 표기(`連結` 25건)를 그대로 채택**해 판정 근거가 의견이 아니라 실측이며 ⓒ 자동 검증이 P0=0이고 대상 키 지적이 1건(오탐)뿐이다. 다만 버튼 문구는 화면 폭 제약이 있어 §5 #3에 실측 확인을 남긴다.

---

## 4. 각 건의 처리 판정

| # | 등급 | 건 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | P1 | `en_US`: '구매' → 'Buy' 권장 | **오탐 — 기각(`Save purchase link` 유지)** | 용어집 `Buy`는 **CTA 라벨 단독 표기용**이다. 여기서 `구매`는 `링크`를 꾸미는 **명사 수식어**이고 영어로는 `purchase link`가 정본이다(`Buy link`는 비문에 가깝다). 동일 유형을 선행 리포트에서도 오탐으로 판정했다(`Purchase complete!` 건 — 용어집 단일 매핑 vs 문맥 의역). → §5 (d-1) |
| — | P1·P2 나머지 | 71키본 76+15 / 39키본 49+7 | **재판정하지 않음 — 다른 키의 기존 지적** | 대상 키 유래가 아니며 선행 리포트가 추적한다 |

**실제 위반: 0건 / 오탐: 1건(기각)**
→ **P0 = 0건(자동 + 수동), 조치 필요 위반 없음.**

---

## 5. 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

### (d-1) 용어집 보완 권장

| 용어 | 권장 근거 | ko_KR | en_US | ja_JP | zh_TW | th_TH |
|---|---|---|---|---|---|---|
| **저장** | 등록값 정본(`btn_save`)이 4개 언어로 확립돼 있는데 **미등재**라 검증 사각지대다. 이번에 사용 키가 늘었다 | 저장 | Save | 保存 | 儲存 | บันทึก |
| **링크** | 이번에 **처음 도입**된 어휘다(ko 사용 선례 0건). zh는 등록값에 `連結` 25건 vs `網址` 1건으로 이미 갈릴 조짐이 있어 정본 고정 권장 | 링크 | link | リンク | 連結 | ลิงก์ |
| ~~구매(명사 수식)~~ | 검증기가 `purchase link`를 P1로 오탐했다. **선행 리포트 (d-1)의 「구매 완료(문장형)」 권장과 같은 뿌리**(`Buy`가 라벨형 단일 매핑) — **중복 제안 아님, 그 항목에 통합 권장** | | | | | |

> 용어집은 API 읽기 전용이다. 승인 시 `md/landpress.md` 절차로 전체 JSON을 산출해 전달하고, 버전이 오르면 같은 작업에서 기획자 가이드 zip도 필수 동반 갱신한다.

### (d-2) 그 외 개선·확인 권장

| # | 항목 | 권장 내용 |
|---|---|---|
| **1** | ⚠️ **Screen Description의 정책 서술이 구 문구를 쓴다 (판단 필요 — 미처리)** | 버튼 라벨은 `구매 링크 저장`으로 바뀌었으나 Description 정책 서술 **4곳**에 `관심 상품 저장`이 남아 있다 — 「하단에서 **관심 상품 저장** 또는 바로 구매로 이어진다」·「Unifi mini에서 **관심 상품 저장** 흐름이 시작된다」·「↳ **관심 상품 저장** 시 버튼 상태는 업데이트 필요」 등. **기능명(위시리스트 저장)과 버튼명(구매 링크 저장)을 구분해 쓰는 것인지, 기능 자체의 이름이 바뀐 것인지 판단이 필요해 손대지 않았다.** 참고로 같은 Description에 「사용자가 관심 상품을 저장한 경우, 해당 상품의 **링크**를 사용자에게 Unifi OA로 전달한다」가 있어 **새 버튼명이 실제 동작에 더 부합**한다 — 기능명도 함께 바꾸는 쪽이 자연스러워 보인다 |
| **2** | **Figma 원본 확인** | Figma 화면의 버튼 문구가 아직 `관심 상품 저장`이면 디자이너에게 `구매 링크 저장`으로 수정 요청이 필요하다. **이번 작업은 위키·엑셀만 대상이라 Figma를 조회하지 않았다** |
| **3** | **버튼 폭 실측** | en `Save purchase link`(18자)·th `บันทึกลิงก์การซื้อ`(20자)가 기존 값과 비슷하나, 같은 화면의 `바로 구매하기`(`UF_voucher_btn_buy_now`)와 **나란히 놓이는 2버튼 레이아웃**이면 폭 확인 권장. 축약안: en `Save link` |
| **4** | ⚠️ **XLT 시스템 미등록 상태 지속** | 이 키를 포함해 **71키 전체가 Unifi v1.7.2(2,347키)에 아직 미등록**이다. 쓰기 API가 없어 **업로드는 사용자 수행**. 이번 변경은 업로드 전에 반영됐으므로 **최신 엑셀을 올리면 된다**(구 값이 등록될 위험 없음) |
| **5** | **위키 다국어 표 46키 ↔ 엑셀 71키 불일치 (이월)** | 선행 리포트에서 제기한 25키 미반영은 여전하다. 다만 이 키는 다국어 표에 **있어서** 이번에 정상 갱신됐다. 전체 정합화 여부는 사용자 결정 대기 |

---

## 6. 게이트 통과 판정

| 기준 | 결과 |
|---|---|
| 한국어 원문 교정 선행 | ✅ 검토 완료 — 교정 불요(§1), before/after 기록 |
| 컬럼 정렬 검증 | ✅ 5셀 전건 문자체계 확인 · misalignment 0건 · `patch_translation` 1g 가드 통과(§2-1) |
| 자동 검증 실행 | ✅ **엑셀 2종 모두 P0 0건** + 보조 코드 스캔 5종 0건 |
| 수동 3단계 검토 | ✅ 5셀 전건 1·2·3단계(§3) + 등록값 선례 실측 5항목 |
| 2차 독립 검토 | ✅ 미실행 — 사유 명시(§3 말미). 변경 5셀이 전부 등록값 정본·다수 표기 채택 |
| 각 건 처리 판정 | ✅ P1 1건 오탐 기각(§4), 나머지는 다른 키 유래로 이관 |
| (d) 추가 개선·제안 | ✅ (d-1) 2건(+1건 기존 항목 통합 권장) · (d-2) 5건(§5) |
| **P0 = 0건 (자동 + 수동)** | ✅ **통과** |

---

## 7. 위키 반영 (완료)

**대상**: [pageId 4639704548](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4639704548) · **v14 → v15**

| 변경 | 내용 |
|---|---|
| **다국어 표 값 5셀 교체** | `UF_voucher_btn_wishlist` 행의 KR·JA·EN·TH·ZH-TW를 확정값으로 교체. **키 셀 마크업(`<code title="">`)은 보존**하고 값 셀만 바꿨다 |
| **Screen 표** | XLT 중첩표 KR(No 54)은 **사용자가 v14에서 이미 `구매 링크 저장`으로 변경**해 둔 상태 → 정합 확인만 하고 **무변경** |
| **첨부 엑셀 2종 갱신** | `xlt_fix_guidekim_voucher_71keys_20260826.xlsx`(v3) · `xlt_screen_keys_voucher_39keys_20260827.xlsx`(v2) — 같은 파일명으로 `POST .../child/attachment/{id}/data` |
| **History** | 2026-08-27 행이 이미 있어 **항목만 누적**(기존 항목 전부 보존) |
| **Description 정책 서술** | 4곳의 `관심 상품 저장`은 **의도적 무변경**(§5 #1 — 판단 사안) |

**검사 결과**: `check_wiki_storage.py pre` **exit 0** → PUT → `post` **exit 0**. PUT 직전 **버전 가드**(라이브 재조회로 v14 확인) 통과. 렌더 검증에서 5개 언어 신값 전건 확인 · 다국어 표 구값 잔존 0.

> ⚠️ **가드 함정 실측 2건**
> ① **키 셀 마크업 변형** — 다국어 표의 키 셀은 `<code title="">`(빈 title 속성)이고 Screen 표는 `<code>`다. `<code>KEY</code>` 완전일치로 찾다 실패했다. **정규식 `<code[^>]*>`로 잡고, 키 셀은 건드리지 않고 값 셀만 교체**하도록 바꿨다.
> ② **전역 금지 가드가 정책 서술과 충돌** — 「Screen 영역에 `관심 상품 저장` 금지」를 걸었더니 **Description의 정책 서술문 3곳**이 잡혀 중단됐다. **이번 세션 2회째 같은 유형**(앞서 History 이력 인용에서 발생)이며 HANDOFF 전역 컨텍스트·`md/wiki.md` 673행에 기록된 함정이다. 가드 범위를 **XLT 중첩표 `<tr>` 단위로 한정**해 해결했다.

# 바우처 캐시백 라벨 — 「즉시」 제거 · 「적용」 표기 전환 게이트 리포트

- **작업**: 바우처 상세 캐시백 라벨에서 **「즉시」를 제거**하고 「적용됨」 표기로 전환
- **대상 2키 · 2서비스** — 사용자 지정(2026-09-17)
  - `UF_voucher_cashback_instant` (**UIT** · Unifi 등록 · 치환자 `{{0}}`)
  - `mini_voucher_detail_cashback_label` (**LV** · Dapp Portal 등록 · 치환자 `{0}`)
- **범위**: 2키 × 5개 언어 = **10셀** (업로드 엑셀 2개 — 서비스가 갈린다)
- **일자**: 2026-09-17
- **원본 조회**: XLT 레지스트리 Unifi v1.7.9 2,607키 · Dapp Portal v2.6.1 1,703키 (2026-09-17) · 용어집 v5.5 · 위키 `4704515582` v239
- **발단**: Slack 스레드 `C07R7CE0B5H` 「캐시백 관련 문구」(2026-09-17 13:25~13:50) — 「즉시」가 실제 지급 시점과 다르다(바우처는 **사용완료 판단 후 일괄 지급**). 결정: 문구만 먼저 XLT 갱신, 툴팁은 17일 이후 릴리즈(위키 「이후 범위」 등재 완료)

---

## 0. 대상 키 확정 경위 — ⛔ 혼선이 있었다

사용자가 제시한 **「변경 전」 값**은 지정한 2키의 등록값이 **아니라** 제3의 키와 일치했다. 추측하지 않고 전수 조사로 확인한 뒤 사용자에게 대상을 직접 확정받았다.

| # | 키 | 서비스·팀 | 현재 등록값 (ko) | 사용자 제시 「변경 전」과 일치 |
|---|---|---|---|---|
| 1 | `mini_voucher_detail_instant_cashback` | DP · LV | `{0}% 즉시 환원` | ✅ **5개 언어 전건 일치** |
| 2 | `mini_voucher_detail_instant_discount` | DP · LV | `{0}% 즉시할인 적용` | — |
| **3** | **`mini_voucher_detail_cashback_label`** | DP · LV | `즉시 적립 {0}% 캐시백` | ❌ |
| **4** | **`UF_voucher_cashback_instant`** | **Unifi** · UIT | `즉시 적립 {{0}}% 캐시백` | ❌ |

→ **사용자 확정(2026-09-17): 대상은 3·4번**이다. 즉 이번 변경은 「즉시」 제거에 그치지 않고 **문구 구조 자체를 1번 키의 형식으로 교체**하는 것이다. 이에 따르는 부작용은 (d-2) 1·2번에 기록했다.

### 치환자 — 팀 규칙 적용

사용자 제시값은 `{0}` 표기였으나, **UIT 키는 `{{0}}`가 정본**이다(CLAUDE.md 담당 FE 팀 규칙 — UIT `UF_`·`{{0}}` / LV `mini_`·`{0}`). 4번에는 `{{0}}`로 변환해 적용했다.

---

## (1a) 번역 전 — 한국어 원문 교정

| # | 검사 | 값 | 판정 |
|---|---|---|---|
| 1 | 신규 ko 값 | `{0}% 환원` / `{{0}}% 환원` | ⚠️ **교정하지 않고 사용자 지정값 유지** — 다만 용어집·실사용과 어긋난다 → (d-2) 3번 |
| 2 | 띄어쓰기 | `{0}% 환원` | ✅ 치환자·`%`·단어 경계 정상 |
| 3 | 종결 형태 | 명사형 라벨 | ✅ 마침표 없음 — `md/check.md` B-7 준수 |
| 4 | 치환자 위치 | 문두 `{0}%` | ✅ 변경 전(`즉시 적립 {0}% 캐시백`)은 문중이었다. 문두 이동은 1번 키 형식을 따른 결과이며 의도된 변경이다 |

**alias 기록 없음** — Figma 원문이 아니라 Slack 결정 기반 변경이다.

---

## (a) 자동 검증 — `validate_translation.py`

```
python3 scripts/validate_translation.py <임시엑셀> scripts/glossary.json
✓ 엑셀 로드: 2개 항목   ✓ 용어집 로드: 118개 용어
=== 1단계: 한국어 검증 === ✓ P0 0건, P1 0건, P2 0건
=== 2단계: 용어집 검증 === ✓ 완료
=== 3단계: 다국어 검증 === ✓ 완료
```

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

> ⚠️ **`P0=0`은 통과가 아니라 시작점이다.** 이번 건은 자동 검증기가 **구조적으로 못 잡는 유형**이다 — 용어집 2단계는 `ko` 원문에 등재 용어(`캐시백`)가 있을 때 타 언어 표기를 검사하는데, 이번 변경은 **ko에서 「캐시백」을 아예 들어냈으므로** 매칭 자체가 발동하지 않는다. 실질 판정은 아래 수동 검토에서 했다.

---

## (b) 수동 3단계 체크표 — **전수 점검**

⛔ 검토 범위를 변경 키로 한정하지 않았다. **변경 대상 2키 + 같은 화면의 캐시백/할인 계열 2키(1·2번) = 4키 × 5개 언어 = 20셀**을 등록값과 대조했다.

### 1단계 — 한국어 (전체 행)

| 키 | 변경 후 ko | 판정 |
|---|---|---|
| `UF_voucher_cashback_instant` | `{{0}}% 환원` | ⚠️ 문법·띄어쓰기는 정상이나 **어휘가 용어집 이탈** → (d-2) 3번 |
| `mini_voucher_detail_cashback_label` | `{0}% 환원` | ⚠️ 동일 |

- B-1 표현 어색: 「N% 환원」은 라벨로 성립한다. 다만 ko 단독 「환원」은 일상 어휘가 아니라 **일본어 `還元`의 직역**에 가깝다(아래 실측).
- B-2 음차 오염 / B-3 다의어 모호 / B-4 중복 / B-5 조사 / B-6 띄어쓰기 / B-7 종결·마침표 — **이상 없음**

### 2단계 — 용어집 (v5.5)

| 용어 | 용어집 등재값 | 본 변경 적용 | 판정 |
|---|---|---|---|
| **캐시백** | ko `캐시백` · ja `還元` · en `cashback` · th `เงินคืน` · zh `現金回饋` | ko **`환원`** · ja `還元` · en `cashback` · th `เงินคืน` · zh **`回饋`** | ⚠️ **ko·zh 2개 언어가 등재값과 다르다** |
| 환원 · 적립 · 적용 | **미등재** | — | 판정 기준 없음 |

**실측 (2026-09-17 · Unifi 2,607키 + Dapp Portal 1,703키)**

| 표기 | 실사용 |
|---|---|
| ko `캐시백` | **28건** (Unifi 19 · DP 9) |
| ko `환원` | **1건** — `mini_voucher_detail_instant_cashback` 뿐 |
| ja `還元` | **37건** (Unifi 27 · DP 10) · `キャッシュバック` 0건 |
| zh `現金回饋` | **28건** (Unifi 19 · DP 9) |
| zh 단독 `回饋` | **26건** (Unifi 19 · DP 7) — 관례로 병존 |

→ **ja·en·th는 용어집·실사용과 일치**한다. **ko 「환원」만 실사용 1건짜리 소수 표기**이고, zh 단독 `回饋`는 26건 선례가 있어 허용 범위다.

- 예외 패턴(USDT·JPYC·PIN 등): 해당 없음
- zh_TW 한자 변형(`產`/`産`·`臺`/`台`): 미사용 ✅
- **zh_TW 치환자·숫자 경계 공백**(`md/guide.md` §5-1 B): `已套用 {0}% 回饋` — `{0}` 앞뒤 공백 ✅
- th_TH 띄어쓰기: `ใช้เงินคืน {0}%` — 치환자 앞 공백 ✅
- ja_JP 띄어쓰기(숫자+단위 붙임): `{0}%還元適用` ✅

### 3단계 — 다른 언어 (전체 행 · 10셀)

| 언어 | 변경 전 (2키 공통) | 변경 후 | 판정 |
|---|---|---|---|
| ja_JP | `即時{N}%還元` | `{N}%還元適用` | ✅ 정확 — 「즉시」 제거 + 「適用」 추가. 한국어 직역 패턴 없음 |
| en_US | `{N}% instant cashback` | `{N}% cashback applied` | ✅ 정확 — 원문 오타 없음 · 자연스러운 어순 |
| th_TH | `เงินคืน {N}% ทันที` | `ใช้เงินคืน {N}%` | ✅ 정확 — `ทันที`(즉시) 제거 + `ใช้`(적용) 추가 · 마침표 미사용 관습 준수 |
| zh_TW | `即時 {N}% 現金回饋` | `已套用 {N}% 回饋` | ✅ 정확 — `即時` 제거 + `已套用` 추가 |

- **교차 오염**: 10셀 육안 확인 — 타 언어 문자체계 혼입 없음. 검증기가 못 보는 3조합(ja↔zh 한자 · 영어가 타 칸 · ko 칸 부분 영어)도 수동 확인 ✅
- **치환자 정합성**: UIT 5셀 전부 `{{0}}` · LV 5셀 전부 `{0}` — **팀 규칙대로 갈라 적용** ✅ (사용자 제시값은 `{0}` 단일이었다)
- **컬럼 swap**: `export_to_xlt.create_xlt_excel` 사용 · 2행 재확인 ✅

---

## (c) P0/P1/P2 각 건 처리 판정

자동 검증은 **전 항목 0건**이라 판정할 건이 없다. 다만 **수동 검토에서 검증기가 못 잡은 사안 1건**을 확인했고, 아래와 같이 판정한다.

| # | 사안 | 심각도 판단 | 처리 판정 | 사유 |
|---|---|---|---|---|
| 1 | ko 「환원」이 용어집 등재값(`캐시백`) 및 실사용 28건과 어긋남 | 자동 P0 미검출 · **수동 P1 상당** | **사용자 지정값 유지 — 정책 보류** | 사용자가 5개 언어 값을 **명시적으로 지정**했고(2026-09-17), 같은 화면의 기존 키 `mini_voucher_detail_instant_cashback`이 이미 ko 「환원」을 쓰고 있어 **그 화면 안에서는 일관**된다. 임의 변경하지 않고 (d-2) 3번으로 올린다 |
| 2 | zh 단독 `回饋`(용어집은 `現金回饋`) | 수동 P2 상당 | **오탐 — 허용** | 단독 `回饋` 실사용 **26건**으로 관례가 병존한다. 같은 화면 기존 키도 `即時回饋`를 쓴다 |

**P0 = 0건** (자동 + 수동) → 통과 기준 충족.

---

## (d) 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

### (d-1) 용어집 보완 권장

**신규 등재 권장 없음 — 기등재 확인.** 「캐시백」은 v5.5에 이미 등재돼 있고 ja·en·th 실사용과 일치한다. 이번 건은 **용어집 매핑이 좁아서 생긴 문제가 아니라**, ko 쪽에서 등재 용어를 쓰지 않기로 한 **정책 선택**이므로 용어집 보완 대상이 아니다.

다만 ko 「환원」이 이번에 **1건 → 3건**으로 늘어나므로, 향후 「캐시백/환원」 ko 표기를 어느 쪽으로 통일할지 결정이 필요하다 → `md/glossary-backlog.md` 등재 권장(반영 여부는 묻지 않는다 — 모아서 리뷰).

**⛔ 추측 표기 권장 금지 준수** — 위 수치는 전부 `fetch_xlt_registry.py` 실측(2026-09-17)이며, 실사용 0건 표기를 권장한 항목은 없다.

### (d-2) 추가 개선·제안

| # | 항목 | 내용 | 권고 |
|---|---|---|---|
| **1** | 🔴 **변경 후 `mini_voucher_detail_cashback_label`이 `mini_voucher_detail_instant_cashback`과 완전히 같은 값이 된다** | 둘 다 Dapp Portal·LV·`{0}` — 변경 후 5개 언어 전부 `{0}% 환원` 계열로 동일해진다. **같은 서비스 안에 값이 같은 키가 2개** 남는다 | 키 이름은 `cashback_label`(라벨)과 `instant_cashback`(즉시 캐시백)으로 다른데 값이 같아져, 다음 사람이 어느 쪽을 써야 할지 오독할 수 있다. **FE 사용처를 확인해 한쪽을 폐기**하거나, 용도가 다르면 문구를 분리하는 것이 바람직하다 |
| **2** | 🔴 **문구의 성격이 「라벨」에서 「적용 상태」로 바뀐다** | 변경 전 `즉시 적립 {N}% 캐시백`은 **혜택 안내 라벨**이고, 변경 후 `{N}% 환원`(ja `適用` · en `applied` · zh `已套用`)은 **적용됨 상태 표시**다. 1번 키는 원래 **가격 펼치기 내역의 적용 항목**이라 이 표현이 맞지만, 3·4번은 **배지·라벨 위치**(위키 바우처 상세 No 6)다 | 배지 자리에 「적용됨」이 들어가면 문맥이 어긋날 수 있다. **화면에서 렌더 확인을 권한다.** 어색하면 ko `{N}% 환원`(적용 뉘앙스 없음)에 맞춰 ja `{N}%還元` · en `{N}% cashback` · zh `{N}% 回饋`로 다듬는 안도 가능하다 |
| 3 | ⚠️ **ko 「환원」은 용어집·실사용과 어긋난다** | 용어집 등재 ko는 `캐시백`이고 실사용 **28 : 1**로 압도적이다. ja `還元`의 직역에 가깝다 | 사용자 지정이라 그대로 반영했다. 되돌리거나 통일하려면 대상은 이번 2키 + 기존 1키 = **3키**다 |
| 4 | **위키 다국어 표 동반 갱신 필요** | 위키 `4704515582`의 바우처 상세 XLT 표(No 6)와 다국어 번역 표에 3번 키의 구값이 실려 있다 | 이번 작업에서 함께 갱신한다 |
| 5 | **일본어 검수 페이지 확인** | `4750149077`에 두 키가 실려 있으면 ja 값이 바뀌므로 갱신 대상이다 | 별도 확인 필요 |

---

## (e) 반영 절차

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 업로드 엑셀 **2개** 생성 (Unifi용 · Dapp Portal용 — 서비스가 갈린다) | 진행 |
| 2 | **사용자가 XLT 시스템에 업로드** (쓰기 API 없음) | ⏳ 대기 |
| 3 | 업로드 후 `fetch_xlt_registry.py`로 양쪽 서비스 재조회 대조 | ⏳ |
| 4 | 위키 `4704515582` 바우처 상세 XLT 표 · 다국어 표 갱신 | 진행 |

---

## 결론

- **P0 = 0건** (자동 + 수동 전수) → **게이트 통과**
- 수동 검토에서 확인한 2건은 **오탐 1 · 정책 보류 1**(사용자 지정값 유지)
- ⚠️ (d-2) **1번(중복 키 발생)·2번(라벨 자리에 「적용됨」 표현)** 은 반영 후 화면 확인이 필요하다

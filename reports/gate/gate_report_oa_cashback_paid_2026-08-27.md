# 번역 품질 게이트 리포트 — OA 캐시백 지급 메시지 (Flex, URL 실값형)

- **작업 유형**: **OA(LINE 공식계정) 메시지** — `md/OA.md` 적용. **XLT 키 미부여**(번역·변수·Flex JSON만 산출)
- **대상 위키**: [pageId 4639704548](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4639704548) `올리브영 5만원 상품상세 Unifi 직접제공` · Jira `UNIFY-10468` · **v22 → v23 → v24**(Hero URL 전체 표기)
- **메시지**: 사용자에게 **캐시백이 지급되었을 때** LINE OA로 보내는 Flex 메시지
- **프레임 식별자**: `OA_cashback_paid`
- **URL (둘 다 사용자 지정 실값 — 변수 아님)**:
  - Hero: `https://vos.line-scdn.net/landpress-content-v2-w5eph4y9qxe05c8fqpi7rlxh/2ac7571d6cbe41349516bea22a4482b8.png?updatedAt=1787809774000` (HTTP 200 · image/png · 83,372 bytes 실측)
  - 버튼: `https://guidekim.me/me/orders` (HTTP 200 실측) — 주문 내역
- **변수 (1건)**: **`{{product_cashback}}`** — 지급된 캐시백 금액(사용자 지정 이름)
- **용어집**: v4.7 · `oa_variables` 2건 등재 — API 조회
- **등록값 조회**: `Unifi / WEB BROWSER / v1.7.2`(2,347키)
- **검토 범위**: **전수 점검** — 4문구 × 5개 언어 = **20셀 전건**
- **산출물**: `oa/flex_OA_cashback_paid_{ko_KR,ja_JP,en_US,th_TH,zh_TW}.json` 5개 + `oa/flex_OA_cashback_paid_5lang.zip`

> **⚠️ 문구 개정 이력 (3판, 2026-08-27)** — 캐시백 문구는 두 차례 바뀌어 **위키 v29 값이 최종**이다.
> ① 초판 `{{product_cashback}} 캐시백이 지급되었어요!` + `적립된 포인트를 확인해 보세요.`
> ② v26에서 「단위 별도 제공」 지시에 따라 `캐시백 {{product_cashback}} 포인트가 지급되었어요!` + `주문 내역에서 바로 확인할 수 있어요.`로 변경
> ③ **사용자가 v27(15:11)에서 ①로 되돌림** — 의도적 편집임을 버전 이력으로 확인했다(사고 아님).
> **사용자 지시에 따라 위키를 정본으로 삼아 JSON·zip을 ①에 맞췄고**, 위키↔JSON 10/10 일치를 검증했다.
>
> ④ **4판(최종)** — 사용자 지시로 **변수 뒤에 포인트 단위 `P`를 부착**했다(`{{product_cashback}}P`).
> 치환값이 숫자만이어도 `500P 캐시백이 지급되었어요!`로 단위가 드러난다. 5개 언어 전부 적용했고 위키↔JSON 10/10 일치.
> 등록값 2,347키에 `P` 단위 표기 선례는 **0건**이라 이번이 신규 표기다.

> **자매 리포트**: ① 구매 완료 `gate_report_oa_voucher_delivery_2026-08-27.md` ② 찜하기 `gate_report_oa_product_link_saved_2026-08-27.md`. 세 메시지는 **같은 구조·같은 버튼 라벨**을 공유한다.

---

## 0. 이 메시지가 앞의 두 OA와 다른 점

| | 구매 완료 · 찜하기 | **캐시백 지급 (이 리포트)** |
|---|---|---|
| Hero | `{{product_image_url}}` (변수) | **실값 URL 고정** |
| 버튼 URL | `{{product_guidekim_url}}` / `{{product_detail_url}}` (변수) | **실값 URL 고정**(`/me/orders`) |
| 본문 변수 | 없음 | **`{{product_cashback}}`**(타이틀) |
| Flex Simulator | ❌ 변수 때문에 그대로 렌더 안 됨 | ✅ **그대로 렌더된다** |

→ **`md/OA.md` 규칙 3(URL 실값 필수)에 정확히 부합하는 유형**이다. 앞의 두 메시지에서 제기한 「동적 템플릿형 예외」(자매 리포트 §5 #1)와 달리 이 메시지는 규칙 원문 그대로 적용된다. **두 유형이 한 페이지에 공존하므로 규칙 보강 시 이 대비를 예시로 쓸 수 있다.**

---

## 1. 한국어 원문 교정 · 문구 제안 (게이트 1a — 필수)

### 확정 문구

| 위치 | 확정 |
|---|---|
| 타이틀 | **`{{product_cashback}}P` 캐시백이 지급되었어요!** *(치환예: `500P 캐시백이 지급되었어요!`)* |
| 본문 | **적립된 포인트를 확인해 보세요.** |
| 버튼 | **Unifi에서 확인하기** *(앞 두 OA와 동일 — 사용자 지정)* |
| altText | **캐시백 포인트가 지급되었어요.** |

### 제안 근거

| 관점 | 내용 |
|---|---|
| **세 메시지 일관성** | 앞의 두 OA(`구매가 완료되었어요!`·`구매 링크를 저장했어요!`)와 **「상태 + 행동」 2단 구조**를 맞췄다. 한 사용자가 세 알림을 순차로 받으므로 형태가 같아야 한 서비스의 메시지로 읽힌다 |
| **버튼 도착지 반영** | 버튼이 **주문 내역**(`/me/orders`)으로 가므로 본문을 「적립된 포인트를 확인해 보세요」로 두어 이동 목적과 맞췄다 |
| **`Unifi에서` 중복 회피** | 앞 두 OA와 동일하게 본문에서 뺐다(버튼 라벨이 담당) |
| **금액 변수** | 사용자 지정으로 `{{product_cashback}}`을 **타이틀**에 넣었다. 금액은 주목도가 가장 높은 자리에 두는 것이 알림 관용이다 |
| **단위 표기** | `{{product_cashback}}`은 **금액(숫자만)** 이고 단위는 문구가 담당한다. 최종적으로 **변수 뒤에 `P`를 부착**(`{{product_cashback}}P`)해 치환 후 `500P`가 되도록 했다(개정 이력 ④). 5개 언어 모두 같은 위치에 `P`를 두어 구조가 일치한다 |

- 확정 한국어 맞춤법·띄어쓰기 정상. 비표준 공백(NBSP·ZWSP·전각·BOM·TAB) **0건**, 연속 공백 0건.
- **alias**: Figma 추출이 아니라 사용자 요청 기반이므로 alias 대상이 아니다. 위 표가 기록이다.
- ⛔ **altText에는 변수를 넣지 않았다** — `md/OA.md` 규칙 2-1(「altText에는 변수 사용 불가」). 그래서 altText는 금액 없이 `캐시백 포인트가 지급되었어요.`로 두었다.

---

## 2. 자동 검증 결과 (P0 / P1 / P2 요약)

```
python3 scripts/validate_translation.py /tmp/oa3b.xlsx scripts/glossary.json
```

| 심각도 | 건수 | 내용 |
|---|---|---|
| 🔴 **P0 (Critical)** | **0건** | — |
| 🟡 **P1 (Medium)** | **0건** | — **이번 세션 OA 3건 중 유일하게 P1도 0건**이다. 캐시백·포인트가 전부 등재값·등록값 계열이라 오탐 여지가 없었다 |
| 🟢 P2 (Low) | 2건 | 마침표 스타일 — 오탐(§4) |

**보조 코드 스캔 (20셀 전수)**

| 검사 | 결과 |
|---|---|
| 비표준 공백·연속 공백·선후행 공백 | ✅ 0건 |
| 비-ko 칸 한글 잔존 | ✅ 0건 |
| zh_TW 간체 | ✅ 0건 — 스캔이 `放` 2건을 잡았으나 **`放`은 번체·간체 공통 글자**(`發放`의 `放`)로 오탐이다. 번체 판정은 `發`(간체 `发`)로 해야 한다 |
| **변수 개수 5개 언어 일치** | ✅ 타이틀 1/1/1/1/1 · 본문·버튼·altText 0개 |
| **altText 변수 0개** | ✅ 규칙 2-1 준수 |
| **URL 실값·변수 미혼입** | ✅ `hero.url`·`action.uri` 둘 다 https 실값, `{{` 없음 |
| **URL 접근성** | ✅ Hero HTTP 200(image/png 83KB) · 버튼 HTTP 200 |

---

## 3. 수동 3단계 체크표 (전수 — 4문구 × 5개 언어 = 20셀)

### 1단계: 한국어

| 점검 항목 | 결과 |
|---|---|
| 맞춤법·띄어쓰기 | ✅ `지급되었어요`·`확인해 보세요`(보조용언 띄어 씀) 정상 |
| 종결 어미 일관성 | ✅ 해요체 — 앞 두 OA와 동일 |
| 변수 주변 띄어쓰기 | ✅ `{{product_cashback}} 캐시백이` — 변수와 명사 사이 한 칸 |
| 비표준 공백·오타 | ✅ 0건 |
| `deprecated_terms`(14항목) | ✅ 미검출 |

### 2단계: 용어집 위반 (v4.7)

| 용어 | 등재값 | 적용 | 판정 |
|---|---|---|---|
| 캐시백 | en `cashback` / ja `キャッシュバック` / th `เงินคืน` / zh `現金回饋` | 4언어 전부 등재값 | ✅ **완전 일치** |
| 포인트 | 미등재 — 등록값 36키 일관 | ja `ポイント` / en `points` / th `คะแนน` / zh `點數` | ✅ 등록값 계열 |
| 지급 | 미등재 | ja `付与` / en `paid` / th `ได้รับ` / zh `發放` | ✅ 선례 일치(아래) |
| 확인 | en `check` / ja `確認` / th `ตรวจสอบ` / zh `查看` | 버튼 4언어 등재값 계열 | ✅ 앞 두 OA와 동일 값 재사용 |
| 예외 목록 | PIN·API·URL·Apple·Google·USDT·IDRP·JPYC | `Unifi`·`LINE` 원어 유지 | ✅ |

**등록값 선례 실측 (2026-08-27 · 2,347키)**

| 항목 | 선례 | 적용 값의 정합 |
|---|---|---|
| 캐시백+포인트 지급 | `UF_voucher_pay_step4` ko `…{{0}}% 캐시백도 포인트로 지급됩니다.` / ja `…キャッシュバックもポイントで付与されます。` / en `…cashback … is also given as points.` / th `…เงินคืน … เป็นคะแนน` / zh `…以點數發放…現金回饋。` | ✅ ja `付与`·zh `發放`·th `ได้รับ` 어휘가 **선례와 동일 계열** |
| 캐시백 표기 | `UF_max_cashback`·`UF_voucher_cashback_instant`·`mini_guidekim_max_cashback` | ✅ 4언어 등재값 그대로 |
| 버튼 라벨 | 이 세션 구매 완료·찜하기 OA | ✅ 5/5 셀 동일 값 재사용 |

### 3단계: 다른 언어 번역 검토

| 언어 | 점검 | 결과 |
|---|---|---|
| **ja_JP** | 정중체 · 겸양 · 전각 부호 · 변수 조사 | ✅ **2026-08-27 검수 방향 반영** — `{{product_cashback}}のキャッシュバックが付与されました。`(`。` 종결), `付与されたポイントをご確認ください。`(겸양), 버튼 `Unifiで確認`(명사형). 변수 뒤 `の`로 금액↔명사 연결이 자연스럽다 |
| **en_US** | 간결성 · 수동태 · 소유격 | ✅ `{{product_cashback}} cashback paid!`(알림 관용 명사구), `Check the points you've earned.` |
| **th_TH** | 문장 끝 마침표 미사용 · 변수 앞뒤 공백 · 완료 표지 | ✅ `ได้รับเงินคืน {{product_cashback}} แล้ว!` — **변수 앞뒤 한 칸씩**(태국어 숫자 표기 관례), `แล้ว` 완료 표지. 본문·altText 마침표 없음 |
| **zh_TW** | 번체 정자 · 변수 앞뒤 공백 · 전각 부호 | ✅ 정자 확인(`現`·`金`·`回`·`饋`·`發`·`點`·`數`), 간체 0건. `{{product_cashback}} 現金回饋已發放！` 변수 뒤 한 칸, 전각 `！`. altText는 `。` |
| **전 언어 공통** | 변수 정합 · 구조 · 종결부호 | ✅ 타이틀 변수 5/5 동일 · **종결부호가 앞 두 OA와 일치**(ko·en·th `!` / ja `。` / zh `！`) · altText는 전 언어 서술 종결 |

### 2차 독립 검토

**팬아웃하지 않았다.** 근거: ⓐ **자동 P1이 0건**이고 ⓑ 버튼 5셀은 앞 두 OA와 **동일 값 재사용**, 캐시백·포인트 어휘는 **등재값·등록값 그대로**라 재판정 여지가 작으며 ⓒ 핵심 근거가 `UF_voucher_pay_step4` 등 **선례 실측**이다. 마케팅 톤 검수는 §5 #3에 남긴다.

---

## 4. 각 건의 처리 판정

| # | 등급 | 건 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | P2 2건 | 타이틀·버튼 마침표 없음 | **오탐 — 기각** | 타이틀은 `!`/`。`로 종결, 버튼은 **라벨**이라 마침표를 붙이지 않는 것이 정본. 앞 두 OA와 동일 판정 |
| 2 | 보조 스캔 | zh `放` 간체 의심 2건 | **오탐 — 검사기 쪽 오류** | `放`은 **번체·간체 공통 글자**다. 번체 판정은 `發`(간체 `发`)로 해야 하며 이 값은 `發放`으로 정자다. 내 검사 집합 구성 오류(이번 세션 3회째 유형) |

**실제 위반: 0건 / 오탐: 2종(전건 기각)**
→ **P0 = 0건 · P1 = 0건(자동 + 수동).**

---

## 5. 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

### (d-1) 용어집 보완 권장 — `oa_variables` 4건 통합 등재

| 변수 | 의미 | 쓰는 곳 |
|---|---|---|
| `{{product_image_url}}` | 상품 이미지 URL(Hero) | 구매 완료 · 찜하기 |
| `{{product_guidekim_url}}` | 구매한 바우처 상세 링크 | 구매 완료 |
| `{{product_detail_url}}` | 상품 상세 링크 | 찜하기 |
| **`{{product_cashback}}`** | **지급된 캐시백 금액** | **캐시백(이 리포트)** |

> 자매 리포트 2건의 (d-1)과 **같은 세트**다 — 중복 제안이 아니라 **4건 통합 등재 권장**이다. 기존 등재 2건(`{{total_amount}}`·`{{wallet_address}}`)과 같은 snake_case 의미명 규칙을 따랐다.
>
> ⚠️ **`{{total_amount}}`와의 구분을 등재 시 명시할 것** — 기존 등재값은 「지급된 **당첨금** 총액(JPYC 단위)」이고 이번 `{{product_cashback}}`은 「**캐시백** 금액」이다. 의미가 비슷해 혼동 위험이 있다.

### (d-2) 그 외 개선·확인 권장

| # | 항목 | 권장 내용 |
|---|---|---|
| ~~1~~ | ~~타이틀에 단위가 없다~~ | ✅ **해소 — 변수 뒤 `P` 부착**(`{{product_cashback}}P`). 단, `P`는 등록값 2,347키에 선례가 없는 **신규 단위 표기**이므로 화면(XLT)에서도 포인트를 `P`로 줄여 쓸지 표기 정책을 맞춰 두면 좋다 |
| ~~2~~ | ~~변수명 의미 확정~~ | ✅ **해소 — 사용자 확정: 금액**(비율 아님). 단위 명시로 어형이 `キャッシュバック{{product_cashback}}ポイント`가 되어 `の` 문제도 함께 사라졌다 |
| **3** | **OA 문구 톤 검수** | ko·en·th·zh는 미검수다(ja만 2026-08-27 검수 완료). 세 OA를 묶어 한 번에 검수받는 것을 권장 |
| **4** | **Screen ID 미부여** | OA 3행 모두 Screen ID가 비어 있다. `md/IA.md` 어휘로 부여하려면 **매핑 표 사용자 승인**이 필요하다 |
| **5** | **목업 이미지** | 캐시백 OA는 목업이 제공되지 않아 Screen 칸이 비어 있다(찜하기도 동일). 구매 완료 OA에만 목업이 들어가 있다 |
| **6** | **발송 조건** | 캐시백 지급이 **주문 건별**인지 **일괄 정산**인지에 따라 알림 빈도가 달라진다. 여러 건이 동시에 지급되면 알림이 연달아 갈 수 있다 → 발송 정책 확인 권장 |
| **7** | **`md/OA.md` 규칙 3 보강 (자매 리포트 이월)** | 이 페이지에 **URL 실값형(이 메시지)** 과 **동적 템플릿형(앞 두 메시지)** 이 공존한다. 규칙에 두 유형을 구분해 명시하면 다음 세션의 오판을 막을 수 있다. **미해소 이월** |

---

## 6. 게이트 통과 판정

| 기준 | 결과 |
|---|---|
| 한국어 원문 교정·문구 제안 | ✅ 확정 문구·제안 근거 기록(§1) · altText 변수 미사용 확인 |
| 자동 검증 실행 | ✅ **P0 0 / P1 0** / P2 2 + 보조 코드 스캔 7종(URL 접근성 실측 포함) |
| 수동 3단계 전수 검토 | ✅ **20셀 전건**(§3) + 등록값 선례 실측 3항목 |
| 2차 독립 검토 | ✅ 미실행 — 사유 명시(§3 말미) |
| 각 건 처리 판정 | ✅ 2건 전건 오탐 기각(§4) |
| (d) 추가 개선·제안 | ✅ (d-1) 변수 4건 통합 등재 권장 · (d-2) 7건(§5) |
| **P0 = 0건 (자동 + 수동)** | ✅ **통과** |

---

## 7. 산출물 · 위키 반영

### Flex JSON

```
oa/flex_OA_cashback_paid_{ko_KR,ja_JP,en_US,th_TH,zh_TW}.json
oa/flex_OA_cashback_paid_5lang.zip   ← 위키 첨부
```

구조는 `md/OA.md` 동작 검증 구조 준수. **검증**: 5파일 ⓐ JSON 파싱 ⓑ `type:bubble` ⓒ `hero.url`·`action.uri` **https 실값** ⓓ 타이틀 변수 1개(5개 언어 동일) ⓔ URL에 변수 미혼입 ⓕ 언어 혼입 0 ⓖ **zip 라운드트립**.

### 위키 (v22 → v23 → v24)

OA 섹션에 **캐시백 행 추가**(설명·변수/URL 표·Flex zip 링크 + 키 없는 번역표). OA 표는 이제 **데이터 행 3개**(구매 완료·찜하기·캐시백)다. History 누적. **XLT 엑셀·전역 키 표는 건드리지 않았다.**

- v24는 **Hero URL을 축약(`…/2ac7571d…png`)에서 전체 URL로 교체**한 것이다 — FE가 위키만 보고 작업할 수 있어야 하므로 실값 전체를 노출했다.
- 일본어 검수 마크업(빨강 37건)은 **모든 PUT에서 보존**을 assert로 확인했다.

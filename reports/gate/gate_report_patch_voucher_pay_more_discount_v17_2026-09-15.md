# 게이트 리포트 — 17일 버전 문구 패치 `UF_voucher_pay_more_discount` (5개 언어 · 사전 준비)

- **작업일**: 2026-09-15 (적용 예정일 **2026-09-17**)
- **모드**: 키 단위 번역 패치 — **(A) ko-source**(한국어 변경 → 5개 언어 전량 재번역, keep 없음)
- **대상 키**: `UF_voucher_pay_more_discount` **1키 × 5개 언어 = 5셀**
- **담당 FE 팀**: **UIT** — 프리픽스 `UF_` · 치환자 **`{{0}}`**
- **대상 화면**: 위키 [pageId 4704515582](https://wiki.workers-hub.com/display/UNIFI/Unifi+mini+v2.0) › `결제페이지 - 바텀시트` **(17일 버전)** · Figma `74620-13737`
- **산출물**: `xlt/xlt_patch_UF_voucher_pay_more_discount_v17_2026-09-15.xlsx`
- **조달(캐시 미사용)**: XLT 등록값 `Unifi / WEB BROWSER v1.7.9` **2,578키** · 용어집 API **v5.5**(118 용어)
- ⚠️ **사전 준비분이다** — 17일 적용 전까지 **시스템 업로드하지 않는다**. 현재 등록값은 `모든 바우처 최대 {{0}}% 혜택!`(Hotfix 대응)이다.

---

## 0. 변경 내역 — before(현재 등록값) → after(17일 버전)

| 언어 | 현재 등록값 (Hotfix 대응) | 17일 버전 |
|---|---|---|
| ko_KR | 모든 바우처 **최대** {{0}}**%** 혜택! | 모든 바우처 {{0}} 혜택! |
| ja_JP | 全クーポン**最大**{{0}}**%**の特典！ | 全クーポン{{0}}の特典！ |
| en_US | **Up to** {{0}}**%** benefits on all vouchers! | {{0}} benefits on all vouchers! |
| th_TH | รับสิทธิประโยชน์**สูงสุด** {{0}}**%** ทุกเวาเชอร์! | รับสิทธิประโยชน์ {{0}} ทุกเวาเชอร์! |
| zh_TW | 全部優惠券享**最高** {{0}}**%** 優惠！ | 全部優惠券享 {{0}} 優惠！ |

**변경 성격 2가지** — ① `최대`(up to) **제거** ② **`%`가 템플릿에서 변수 안으로 이동**. 즉 `benefitrate`가 `15`가 아니라 **`"15%"` 문자열**을 내려주는 전제다(사용자 확정 2026-09-15).

**렌더 결과**(`{{0}}` = `"15%"`): ko `모든 바우처 15% 혜택!` · ja `全クーポン15%の特典！` · en `15% benefits on all vouchers!` · th `รับสิทธิประโยชน์ 15% ทุกเวาเชอร์!` · zh `全部優惠券享 15% 優惠！` → **Figma 원문 `모든 바우처 15% 혜택!`과 일치**.

### 영향 범위

이 키는 Unifi 2,578키 중 **유일 사용**이고 다른 서비스(Dapp Portal)에 공유되지 않는다. 바뀌는 화면은 **결제페이지 바텀시트 1곳**이다.

## (1a) 번역 전 — 한국어 원문 교정

| 항목 | 결과 |
|---|---|
| 맞춤법·띄어쓰기 | `모든 바우처 {{0}} 혜택!` — 이상 없음 |
| 비표준 공백(`\xa0`) | 없음 |
| 치환자 표기 | **UIT 규칙 `{{0}}` 준수** · 전 언어 1회 |

**교정 없음** — 사용자가 지정한 문구를 그대로 썼다. **Figma 원문(`모든 바우처 15% 혜택!`)과 렌더 결과가 일치**하므로 이번 버전에서는 **alias가 해소된다**(현재 등록값은 `최대`가 붙어 Figma와 달랐다).

## (a) 자동 검증 — `scripts/validate_translation.py` P0/P1/P2 요약

| 심각도 | 건수 |
|---|---:|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

**P0 = 0 → 통과 기준 충족.**

## (b) 수동 3단계 체크표 — **전수** (1키 × 5개 언어 = 5셀 전체 행)

패치 대상 키의 **5개 언어 전체 행**을 한 줄씩 검토했다.

### 1단계 — 한국어 원문
(1a) 참조. 오타·맞춤법·띄어쓰기·조사 이상 없음.

### 2단계 — 용어집 대조 (v5.5 정본)

| 용어 | 정본 | 적용값 | 판정 |
|---|---|---|---|
| **바우처** | ja `クーポン` / en `Voucher` / th `เวาเชอร์` / zh `優惠券` | 전 언어 준수 | ✅ |
| **혜택** | ja `特典` / en `benefit` / th `สิทธิประโยชน์` / zh `優惠` | 전 언어 준수 | ✅ |
| ~~최대~~ | — | **이번 버전에서 제거** | — |

**추측 표기 0건** — 직전 패치에서 실측한 기등록 표기를 그대로 유지하고 `최대`에 해당하는 어휘만 뺐다.

### 3단계 — 다국어 (5셀 전건)

| 언어 | 검토 | 판정 |
|---|---|---|
| ja_JP | `全クーポン{{0}}の特典！` — **변수 앞뒤 무공백**(ja 관례) · 전각 `！` · 조사 `の` 유지 | ✅ |
| en_US | `{{0}} benefits on all vouchers!` — 변경 전 최초 등록값 `{{0}}% off all vouchers!`와 **동일 구조**(선두 변수 + `on all vouchers`) | ✅ |
| th_TH | `รับสิทธิประโยชน์ {{0}} ทุกเวาเชอร์!` — `รับ…` 동사 구조 유지 · 반각 `!` | ✅ |
| zh_TW | `全部優惠券享 {{0}} 優惠！` — **한자↔변수 공백 1칸**(2026-09-14 규칙) · `享…優惠` 선례 유지 · 번체 정자 · 전각 `！` | ✅ |
| ko_KR | 1단계 참조 | ✅ |

**공통 점검**: 빈칸 0 · 언어 혼입 0 · **`{{0}}` 5개 언어 전건 1회** · **리터럴 `%` 잔존 0**(전 언어에서 `%`를 템플릿에서 뺐다 — 하나라도 남으면 `15%%`로 렌더된다) · `/n` 오타 0 · 컬럼 swap 0 · 마침표 정책 5개 언어 감탄부호 통일(ja·zh 전각 / ko·en·th 반각).

**⛔ 컬럼 정렬 검증(Step 2-1)**: (A) 모드로 **5개 언어를 전부 교체**하므로 단일 언어 패치의 컬럼 소실 위험이 없다. 현행 등록값 5셀의 문자체계도 사전 대조해 **misalignment 0건** 확인.

## (c) 검증기 보고 건별 처리 판정

**자동 검증이 P0·P1·P2 모두 0건**이라 판정 대상 건이 없다(실제 위반 0 · 오탐 0 · 정책 보류 0).

수동 검토에서 다룬 판단 1건을 남긴다.

| # | 건 | 판정 |
|---|---|---|
| 1 | `%`를 변수 안에 넣는 것이 이 저장소 관례에 맞는가 | **관례에 맞다 — 오히려 다수** . 등록값 2,578키 실측: ko에 `{{0}}%`(단위를 템플릿에 두는 형태) **12키** vs `{{0}}` 단독(단위를 변수에 맡기는 형태) **313키**. 선례 — `UF_asset_jpyc_guide_apy` = `최대 연 {{0}}` · `UF_boost_max_estimate_label` = `최대 {{0}} 예상` · `UF_max_cashback` = `결제 시 최대 {{0}} 캐시백` |

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

**(d-1) 용어집 보완 권장 — 대기 목록 등재 없음.** 쓰인 용어(`바우처`·`혜택`)가 전부 v5.5 등재값이고 오탐 0건이라 보완 신호가 없다.

**(d-2) 추가 개선·제안 안내**

| # | 내용 | 판단 주체 |
|---|---|---|
| 1 | ⚠️ **`benefitrate` 필드의 값 형태가 바뀐다** — 현재는 `15`(숫자)를 받아 템플릿이 `%`를 붙이고, 17일부터는 `"15%"`(문자열)를 받는다. **같은 필드의 계약 변경**이므로 적용일에 FE·XLT가 동시에 바뀌어야 한다. 한쪽만 바뀌면 `1515%`(구 템플릿 + 신 값) 또는 `15`(신 템플릿 + 구 값)로 렌더된다 | FE · 기획 |
| 2 | 🟢 **직전 리포트의 「FE 계약이 갈린다」 지적은 범위를 좁힌다** — `UF_voucher_pay_cashback`(`{{0}}% 적립 캐시백`)·`UF_voucher_pay_step4`는 **캐시백율(다른 필드)** 을 쓴다. `benefitrate`를 쓰는 키는 이 키 하나뿐이라 **키 간 충돌은 없다.** 남는 리스크는 위 1번(동시 적용)뿐이다 | (정정) |
| 3 | ✅ **Figma alias가 해소된다** — 렌더 결과가 Figma 원문 `모든 바우처 15% 혜택!`과 정확히 일치한다. 현재 등록값(`최대` 포함)은 Figma와 달랐다 | (정합 확인) |
| 4 | 17일까지는 **업로드하지 않는다** — 지금 올리면 Hotfix 화면(`최대 15% 혜택!`)과 어긋난다 | 기획 |

## 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 | **0건** ✅ |
| 수동 P0 (전수 5셀) | **0건** ✅ |
| P1 · P2 | **각 0건** ✅ |
| 치환자 무결성 | `{{0}}` 5/5 · 리터럴 `%` 잔존 0 ✅ |
| (d) 임의 적용 | 없음 ✅ |

→ **게이트 통과.** 엑셀을 위키 17일 버전 행에 첨부하고 **적용일(2026-09-17)까지 업로드 보류**한다.

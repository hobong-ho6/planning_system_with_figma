# 번역 품질 게이트 리포트 — `mini_home_clinic_review_count` 신규 (2026-09-16)

대상: 홈탭2 「상담 건수 가장 많은 클리닉」 영역의 **정렬·표시 기준을 상담 수 → 리뷰 수로 변경**(Slack 2026-09-16 · [UNIFY-11350](https://jira.workers-hub.com/browse/UNIFY-11350)).
Figma **To-be 프레임 `74940-15329`** 기준 · 신규 **1키 × 5개 언어 = 5셀** · **전수 점검**(파일 전체가 1키).

| 항목 | 값 |
|---|---|
| XLT Key | `mini_home_clinic_review_count` |
| 담당 FE 팀 | **LV** — `mini_` 프리픽스 · 변수 **`{0}`**(단일 중괄호) |
| 업로드 대상 서비스 | **Dapp Portal** — 실측 근거: 대체 대상인 형제 키 `mini_home_clinic_consult_count`와 같은 영역의 `mini_home_clinic_view_all`이 **모두 Dapp Portal에만** 있다 |
| 엑셀 | `xlt/xlt_home_clinic_review_count_dappportal_2026-09-16.xlsx` |

## Figma 코멘트 선별 (매 실행 재조회 · 캐시 미사용)

`fetch_comments.py GOCHAYBS7hIrmWRGNuJOKV 74940:15329` — 루트 2건:

| # | 코멘트 | 답글 | 대상 |
|---|---|---|---|
| 1 | `xlt` | 리뷰수 여기서 노출 | `리뷰 536건` (y=4558) → **이 키** |
| 2 | `리뷰 수 제거` | — | 평점 줄(`4.5`, y=4573) 옆 리뷰 수 **제거** — 문구 변경이 아니라 삭제라 **신규 키 없음** |

## ⛔ 중복 등록 방지 — 한국어 문구로 대조

키 이름이 아니라 **한국어 문구로** 양 서비스 등록값 전건(DP 1,701 · Unifi 2,589)을 대조했다.

| 기등록 키 | ko_KR | 재사용 판정 |
|---|---|---|
| `UF_clinic_detail_header_review` · `mini_voucher_detail_review_count_label` | `리뷰 {0}` | ❌ **단위 `건`이 없다** — 그대로 쓰면 `리뷰 536`으로 렌더된다 |
| `mini_voucher_review_all_title` | `리뷰 ({0})` | ❌ 괄호 표기 |
| `mini_category_sort_review` | `리뷰 많은순` | ❌ 정렬 라벨 |

`리뷰 {0}건`과 **동일한 등록값 0건** → 신규 등록이 맞다. 키 이름 충돌도 없다(실측).

## (1a) 한국어 원문 교정

**교정 0건.** Figma 원문 `리뷰 536건`에서 숫자만 변수화해 `리뷰 {0}건`으로 만들었다.

- `536`은 리뷰 건수라 **동적 값**이다 — 리터럴로 두지 않는다.
- 단위 `건`을 변수 밖에 둔다. 변수 안에 넣으면 언어별 조수사 처리를 FE가 떠안는다.
- ⛔ **`{0}`(단일 중괄호)** — LV 팀 규칙. `{{0}}`로 쓰면 안 된다.

## 확정 번역 — 대체 대상 형제 키에 정렬

| 언어 | `mini_home_clinic_consult_count`(기존 · 대체 대상) | **`mini_home_clinic_review_count`(신규)** |
|---|---|---|
| ko_KR | {0}건 상담 | **리뷰 {0}건** |
| ja_JP | カウンセリング{0}件 | **レビュー{0}件** |
| en_US | {0} consultations | **{0} reviews** |
| th_TH | ปรึกษา {0} ครั้ง | **รีวิว {0} รายการ** |
| zh_TW | {0} 則諮詢 | **{0} 則評論** |

근거:
- **어순·조수사를 형제 키에서 그대로 승계했다.** ja는 `명사+{0}件`(공백 없음), en은 `{0} + 복수명사`, th는 `명사 + {0} + 분류사`, zh_TW는 `{0} + 則 + 명사`. 같은 카드의 두 줄이 교체되는 관계라 구조가 어긋나면 레이아웃이 튄다.
- **「리뷰」 역어는 기등록 관례를 따랐다** — ja `レビュー` · en `Reviews`/`reviews` · th `รีวิว` · zh_TW `評論`. 양 서비스 등록값에서 예외 없이 일관된다(실측).
- **zh_TW 분류사 `則`** 은 형제 키 `{0} 則諮詢`과 동일하며, 리뷰·댓글에 쓰는 정자 표기다.
- **th_TH 분류사** 는 형제 키가 `ครั้ง`(횟수)를 쓰지만 리뷰는 횟수가 아니라 건이므로 `รายการ`(항목)로 바꿨다. 상담은 「몇 번 했나」, 리뷰는 「몇 건 있나」다.

## (a) 자동 검증 — `scripts/validate_translation.py`

`python3 scripts/validate_translation.py xlt/xlt_home_clinic_review_count_dappportal_2026-09-16.xlsx scripts/glossary.json`

| 심각도 | 건수 |
|---|---|
| **P0** | **0건** |
| **P1** | **0건** |
| **P2** | **0건** |

용어집 118개 용어 로드 · 1·2·3단계 전부 통과.

## (b) 수동 3단계 체크표 — 전수 점검 (1키 × 5언어 = 5셀 전건)

`md/check.md`·`md/translation_validation_guide.md` 절차로 **파일 전체 행**을 한 줄씩 육안 검토했다(신규·변경 키로 한정하지 않음 — 이 파일은 1키라 전체 = 전수).

- **1단계(한국어 원문)**: #1
- **2단계(용어집 대조)**: #6
- **3단계(다국어 의미·표기)**: #2 · #3 · #4 · #5 · #7 · #8

| # | 단계 | 점검 항목 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | **1단계** | 한국어 원문 맞춤법·띄어쓰기 | ✅ | `리뷰 {0}건` — 단위 앞 붙여쓰기 정상 |
| 2 | **3단계** | 의미 등가(ko 기준 5언어) | ✅ | 전건 「리뷰 N건」 |
| 3 | **3단계** | 언어 혼입(이질 문자체계) | ✅ | ja에 한글 0 · th에 라틴 0 · zh **번체 정자**(`評論`·`則` — 간체 `评论` 없음) |
| 4 | **3단계** | 자리표시자 개수·표기 | ✅ | 5언어 전건 `{0}` **1개** · ⛔ LV 규칙대로 **단일 중괄호**(`{{0}}` 아님) |
| 5 | **3단계** | 문장부호 | ✅ | 라벨이라 5언어 전건 마침표 없음 — 형제 키와 동일 |
| 6 | **2단계** | 용어집 준수 | ✅ | 「리뷰」는 용어집 미등재 항목이나 **기등록값 전건이 일관**해 그것을 정본으로 따랐다(아래 d-1) |
| 7 | **3단계** | 형제 키와의 구조 일관 | ✅ | 조수사 위치·공백 유무를 `mini_home_clinic_consult_count`에 맞춤(위 표) |
| 8 | **3단계** | 중복 등록 | ✅ | 한국어 문구 기준 양 서비스 4,290키 대조 — 동일 0건 |

## (c) 자동 검증기 보고 건 처리 판정

P0·P1·P2 **전건 0** — 처리할 항목 없음.

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장
**`md/glossary-backlog.md` 대기 목록 등재** — 「리뷰」가 용어집에 없다. 실측 사용 건수는 **ja `レビュー` 8건 · en `Review(s)` 8건 · th `รีวิว` 8건 · zh_TW `評論` 8건**(양 서비스 합산, 위 대조표 기준)으로 표기가 완전히 일관돼 등재 조건을 만족한다. ⛔ 이 자리에서 반영 여부를 묻지 않고 대기 목록에만 올린다.

### (d-2) 추가 개선·제안
1. **영역 제목이 낡았다(사용자 결정 필요)** — 정렬·표시가 리뷰 수로 바뀌는데 영역 제목은 `상담 건수 가장 많은 클리닉` 그대로다(To-be 프레임에도 그대로). 이 제목은 **Admin(Additional Curations)에서 관리**하므로 XLT 작업 범위 밖이고, UNIFY-11350에 확인 항목으로 적어 두었다.
2. **`mini_home_clinic_consult_count`의 처리(사용자 결정 필요)** — 이 화면에서 더 쓰지 않게 되지만 **등록값은 그대로 두었다**. 다른 화면에서 참조할 수 있어 임의로 삭제·변경하지 않는다.
3. **th_TH 분류사 선택** — `รายการ`로 정했으나, 리뷰에 `รีวิว` 자체를 분류사처럼 쓰는 표기도 통용된다. 원어민 검수 시 함께 봐 주면 좋다.
4. **Figma 원문 수정 요청 없음.**

## 통과 판정

**P0 = 0건(자동 + 수동) → 통과.** 엑셀 생성·위키 반영으로 진행한다.
업로드(**Dapp Portal**)는 **사용자가 수행**한다 — XLT 쓰기 API가 없다.

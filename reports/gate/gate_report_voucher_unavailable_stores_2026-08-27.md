# 번역 품질 게이트 리포트 — `UF_voucher_detail_unavailable_value` 이용불가 매장 34곳 현지 표기

- **작업 유형**: 키 단위 번역 패치 모드 **(A) ko-source** — ko 원문 교정(항목 병합) + ja·en·th·zh 4개 언어 신규 표기
- **대상 위키**: [pageId 4639704548](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4639704548) `올리브영 5만원 상품상세 Unifi 직접제공` · Jira `UNIFY-10468` · **v9 → v10**
- **대상 키 (1건)**: `UF_voucher_detail_unavailable_value` — 상품상세 「*이용불가 매장」(`UF_voucher_detail_unavailable`) 아래 나열되는 매장 목록
- **담당 FE 팀**: **UIT** — 기존 키 프리픽스 `UF_`로 판별(2026-08-26 사용자 확정 승계) · 치환자 `{{0}}`(이 키는 변수 없음)
- **등록값 조회 시각**: **2026-08-27 10:2x KST** — `Unifi / WEB BROWSER / v1.7.2`(2,308키) · 캐시 금지 규칙에 따라 이번 run 신규 조회(어제 `scratch_unifi.json` 캐시 미사용)
- **용어집**: v4.7 (API 조회) — 매장명·유통 브랜드 관련 등재어 **없음**
- **검토 범위**: **전수 점검** — 엑셀 전체 71키를 자동 검증에 통과시키고, 대상 키는 **5개 언어 × 34항목 = 170셀 전건** 수동 대조. 변경 언어로 한정하지 않음
- **산출물 엑셀**: `xlt/xlt_fix_guidekim_voucher_71keys_20260826.xlsx` — **위키 첨부와 같은 파일명 유지**(본문 링크 그대로 최신본)

> **선행 작업**: `reports/gate/gate_report_guidekim_voucher_64keys_2026-08-26.md`(2판 71키, 미커밋 — 다른 세션 소관이라 손대지 않음). 이 리포트는 그 산출물 중 **1키의 판정을 사용자 결정으로 뒤집는** 후속 패치다.

---

## 0. 발단 — 어제 판정의 명시적 번복

이 키는 2026-08-26 작업에서 **5개 언어 전부 한국어 원문**을 담고 있었고, 그 리포트는 이를 「정책 예외(고유명사 원문 유지) — 실제 위반 아님」으로 판정했다. 근거는 *「현지화하면 이용자가 현장 간판·앱의 한국어 지점명과 대조할 수 없어 실사용 가치가 떨어진다」* 였다.

**사용자가 이번에 「현지 표기만 — 원문 대체」를 선택해 그 판정을 번복했다.** 착수 전 어제 판정과 그 근거를 사용자에게 제시하고 재결정을 받았다.

**선례 실측(판정 보조)**: Unifi v1.7.2 **2,308키 전수**에서 ko 외 칸에 한글이 남은 키는 **1건**(`UF_settings_display_language_ko` = 언어 선택 라벨 `한국어`)뿐이고, 매장명 계열 선례는 **0건**이었다. 즉 원문 유지는 사실상 선례가 없는 처리였다.

**부수 효과**: 어제 64키 자동 검증의 **P0 4건**(en/ja/th/zh 칸 한글 포함 — 컬럼 어긋남 의심)이 이번 번역으로 **전건 해소**됐다.

---

## 1. 한국어 원문 교정 (게이트 1a — 필수)

| 구분 | 내용 |
|---|---|
| 원문(위키 v9 = 어제 산출물) | 35개 항목 · 292자 |
| **교정 ①(쉼표 오류 — 사용자 결정)** | `…갤러리아진주점, **뉴코아아울렛, 평택점**, 롯데마트행당역점…` → `…갤러리아진주점, **뉴코아아울렛평택점**, 롯데마트행당역점…` **(35 → 34개)**. 근거: 34개 항목이 모두 `~점`으로 끝나는데 `뉴코아아울렛`만 `점`이 없고 바로 뒤가 `평택점`이다. 같은 목록의 `2001아울렛분당점`·`2001아울렛부평점`·`뉴코아부천점`이 전부 「브랜드+지점」 **붙임** 패턴이며, `뉴코아아울렛점`이라는 매장은 성립하지 않는다 |
| 확정 원문 | 34개 항목 · 290자 |

- **alias 기록**: 원문이 Figma 추출이 아니라 **어제 산출물(위키·엑셀)**이므로 `{Figma 원문 → XLT Key}` alias 대상은 아니다. `translation_data.json`의 `aliases`에 교정 전 35항목 형태를 기록했다.
- **⚠️ 데이터 진위는 파일만으로 확정할 수 없다** — 2차 검토도 「병합 판단은 타당하나 **올리브영 제공 원본 매장 목록과 대조해야 확정**」으로 판정했다. → §5 (d-2) #1.
- 그 외 한국어 문제 없음: 비표준 공백(NBSP·ZWSP·전각·BOM·TAB) **0건**, 연속 공백 0건, 선후행 공백 0건, 말미 쉼표 없음.

---

## 2. 자동 검증 결과 (P0 / P1 / P2 요약)

```
python3 scripts/validate_translation.py \
  xlt/xlt_fix_guidekim_voucher_71keys_20260826.xlsx \
  scripts/glossary.json
```

| 심각도 | 건수 | 내용 |
|---|---|---|
| 🔴 **P0 (Critical)** | **0건** | 빈칸·오타·언어 혼입·용어집 표기 위반 — 없음 (어제 64키 시점 P0 4건 → **전건 해소**) |
| 🟡 P1 (Medium) | 75건 | **전부 대상 키 외 다른 70키의 기존 지적** — 대상 키 지적 **0건** |
| 🟢 P2 (Low) | 15건 | 〃 (마침표 스타일 — 대상 키 무관) |

자동 리포트: `xlt/xlt_fix_guidekim_voucher_71keys_20260826_validation_report.md`

> ⚠️ `P0=0`은 통과가 아니라 시작점이다. 자동 검증기는 **고유명사 음차 정확성·한자 자형(신자체/번체/이체자)·로마자 표기법·브랜드 표기 일관성**을 구조적으로 못 잡는다. **이번 run이 그 실증이다** — 자동 P0=0·대상 키 지적 0건 상태에서 2차 독립 검토가 **실제 오류 3건**을 찾아냈다(§3·§4).

**보조 코드 스캔**

| 검사 | 범위 | 결과 |
|---|---|---|
| 비표준 공백·제어문자 · 연속/선후행 공백 · `/n` | 전체 71키 × 5언어 | ✅ 0건 |
| ko 외 칸 한글 잔존 | 전체 71키 × 4언어 | ✅ 0건 |
| 항목 수 일치 | 대상 키 5언어 | ✅ 전부 34개 |
| 구분자 균일성(`, ` 반각+공백 33개) | 대상 키 5언어 | ✅ 전각 `，`·`、` 혼입 0 |
| ja 이체자·번체 잔존(`凉`·`澤`·`區`·`國`·`佛`·`廳`·`晉`) | 대상 키 ja | ✅ 0건 (수정 후) |
| zh 신자체·간체 잔존(`沢`·`区`·`国`·`仏`·`庁`·`晋`·`凉`·`数`) | 대상 키 zh | ✅ 0건 |
| 라틴 브랜드 일관성(`NewCore` vs `New Core`) | en·th·zh | ✅ 3열 전부 `NewCore` 2건 / `New Core` 0건 |
| 치환자·태그·개행 | 대상 키 | ✅ 전부 0개(5언어 동일) |

---

## 3. 수동 3단계 체크표 (전수 — 5개 언어 × 34항목 = 170셀 전건)

### 1단계: 한국어 맞춤법·표기

| 점검 항목 | 결과 |
|---|---|
| 항목 분리 오류 | ⚠️ **1건 검출·교정** — `뉴코아아울렛, 평택점` 병합(§1) |
| 매장명 표기 일관성 | ⚠️ ko 내부에 `NC강남점`(라틴)과 `광주엔씨웨이브점`(한글 음차)이 혼재. **원문 유지**(현장 간판 대조가 목적) → §5 (d-2) #4 |
| 오타·글자 누락 · 비표준 공백 | ✅ 없음 / 0건 |
| 외래어 음차 오염(`포이가츠`류) | ✅ 없음 |
| `deprecated_terms`(14항목) | ✅ 미검출 |

### 2단계: 용어집 위반 (v4.7)

| 항목 | 결과 |
|---|---|
| 용어집 등재어 대조 | ✅ **충돌 0건** — v4.7 `terminology`에 매장명·유통 브랜드(롯데마트·현대백화점·아울렛·매장) 등재어가 **없다** |
| 예외 목록(번역 금지) | ✅ 해당 없음 |
| **신규 등재 제안** | ❌ **하지 않는다** — 등재해도 **실사용 1건짜리 고유명사**라 (d-1) 「실사용 0~1건 표기 권장 금지」 취지에 어긋난다(2차 검토 동의) |

### 3단계: 다른 언어 번역 검토

| 언어 | 점검 | 결과 |
|---|---|---|
| **ja_JP** | 한자 자형(신자체) · 가타카나 음차 적절성 · `駅舎`/`駅` 구분 · 브랜드 | ✅ 신자체 `沢`·`晋`·`区`·`国`·`仏`·`庁` 전건 정확. 순한글·외래어 지명은 가타카나(`Wシティ`·`ギャラリア`·`ロッテマート`·`ニューコア`·`ウェーブ`). **`역사점`→`駅舎店` / `역점`→`駅店` 구분이 원문과 1:1 대응**(1·8·12·18·26 vs 11·25). 🔴 `清凉里` 이체자 1건 검출·수정(§4 #1) |
| **en_US** | 국립국어원 로마자 표기법 · 브랜드 공식 영문 · 도로명 규칙 | ✅ `Pyeongtaek`·`Cheongnyangni`·`Yeongdeungpo`·`Bupyeong`·`Gyeyang`·`Singuro`·`Bucheon`·`Haengdang`·`Bulgwang`·`Gochon`·`Jinju`·`Banyeo`·`Gimcheon`·`Samsan`·`Bundang`·`Gangseo` 전건 정확. 🔴 도로명 절단 오류 1건 검출·수정(§4 #2) |
| **th_TH** | 표기 체계 선택 | ✅ **의도적으로 en과 동일한 로마자**(바이트 단위 일치) — 판정 근거는 §4 #4. 헤더 키의 th는 태국어(`*สาขาที่ใช้ไม่ได้`)이므로 「문장은 태국어 · 고유명사는 로마자」 구조 |
| **zh_TW** | 번체 정자 · 대만 통용 브랜드 · `車站` 사용 | ✅ 정자 `澤`·`晉`·`區`·`國`·`佛`·`廳`·`數`·`涼` 전건 정확, **간체 혼입 0**(코드포인트 감사). 대만 통용 `樂天瑪特`·`樂天世界`·`現代百貨`·`數位` 적절. `역사점`→`車站店`/`역점`→`站店` 대응. 🔴 라틴 브랜드 불일치 2건 검출·수정(§4 #3) |
| **전 언어 공통** | 항목 수·정렬 · 중복 · 문자체계 혼입 · 구분자 | ✅ 34/34 전 항목이 i번째끼리 **동일 매장 지시**(경계 사례 #22 광주엔씨웨이브·#24 NC불광·#25 NC광주역 교차 확인). 중복 0(부평 #27·#32, 광주 #22·#25는 서로 다른 매장). ja에 한글 0 · zh에 가나·한글 0 · en·th ASCII only |

### 2차 독립 검토 (`translation-reviewer` 에이전트 — 분리 컨텍스트 재판정)

170셀 전건 대조로 **실제 결함 3건 · 확인 필요 9건**을 반환했고, 1차의 정렬·자형·로마자 판정에는 동의했다. **결함 3건은 전부 사용자 결정이 불필요한 객관적 오류**여서 즉시 수정했다(§4). 확인 필요 9건은 임의 반영하지 않고 §5로 이관했다.

---

## 4. 각 건의 처리 판정

### 🔴 실제 결함 (2차 검토 검출 — 전건 수정 완료)

| # | 언어 | before | after | 근거 |
|---|---|---|---|---|
| 1 | ja_JP | `清**凉**里駅舎店` | `清**涼**里駅舎店` | `凉`(U+51C9)은 **중국 간화자/이체자**이고 일본 상용한자는 `涼`(U+6DBC). 같은 파일 zh 행이 `清涼里`(U+6DBC)를 쓰고 있어 **한 파일 안에서 ja만 다른 자형**이었다. 코드포인트 감사 결과 ja 고유 CJK 중 이것만 신자체 계열이 아니었다 |
| 2 | en_US · th_TH | `Daegu Sangin**-seoro**` | `Daegu Sangin**seo-ro**` | `상인서로`는 **도로명**이다(ja·zh 모두 `上仁西路`로 판독). 도로명주소 로마자 규칙상 도로명은 붙여 쓰고 `-ro` 앞에 하이픈이 온다. 기존 표기는 `상인`+`서로`로 끊겨 영어 **"each other"** 로 읽혔다 |
| 3 | zh_TW | `**New Core** Outlet平澤店` · `**New Core**富川店` | `**NewCore** Outlet平澤店` · `**NewCore**富川店` | 같은 라틴 브랜드를 en·th는 `NewCore`(붙임), zh만 `New Core`(띄움)로 써 **열 간 불일치**였다. 이랜드 공식 영문은 `NEWCORE`(붙임) |

### ⚪ 자동 검증기 P1 75건 / P2 15건

**전부 대상 키 외 다른 70키의 기존 지적이며 이번 패치로 신규 발생한 것은 0건**이다(어제 71키 시점과 동일 수치). 그 판정은 선행 리포트(`gate_report_guidekim_voucher_64keys_2026-08-26.md`)에 이미 기록돼 있어 재판정하지 않는다. 대상 키에 대한 지적은 **0건**.

**실제 위반: 3건(전건 수정) / 대상 키 신규 오탐: 0건 / 사용자 결정 이관: 9건(§5)**
→ **P0 = 0건(자동 + 수동).**

### 정책 판정 — th_TH에 로마자를 쓴 이유 (2차 검토 동의)

| 논거 | 내용 |
|---|---|
| 찬성 ① | **태국어 음차에 표준이 없다.** `평택`·`상인서로`·`반여1동` 같은 행정 지명을 태국 문자로 옮기면 표기자마다 달라져 **검증 불가능한 값 34개**가 생긴다 — 게이트의 「추측 표기 금지」와 정면 충돌 |
| 찬성 ② | `AK`·`NC`·`2001 Outlet`·`Galleria`·`NewCore`는 **태국어 형태가 아예 없다.** 절반이 라틴으로 남는 혼종 문자열이 되어 일관성이 더 깨진다 |
| 찬성 ③ | 이 키의 목적은 태국인 사용자가 **한국 현지 매장을 식별**하는 것이고, 지도앱·영수증·매장 안내에서 실제로 마주치는 보조 표기는 **로마자**다 |
| 인정하는 반대 논거 | th 열이 en과 **바이트 단위로 완전 동일**(648자)해, 다른 담당자나 자동 점검이 **「th 번역 누락」으로 오판해 되돌릴 위험**이 실재한다 → **위키 다국어 섹션에 의도 명시 1줄을 남겨 대응**(§7) |

---

## 5. 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

> 선행 리포트(08-26) §5에서 이미 제기·종결된 항목은 재제안하지 않는다. 아래는 **이번 run의 신규 실측**과 **2차 검토의 확인 필요 9건**이다.

### #0 🔴 최우선 (범위 밖 발견) — 위키 다국어 표에 25키가 누락돼 있다

엑셀은 **71키**인데 위키 `다국어 번역(XLT Full Translation)` 표에는 **46키**만 있다(2026-08-27 10:2x 실측, 위키 v9 기준). 누락 25키:

`UF_voucher_badge_instant` · `UF_voucher_btn_buy_now` · `UF_voucher_btn_wishlist` · `UF_voucher_friend_add_btn` · `UF_voucher_friend_add_notice` · `UF_voucher_info_refund` · `UF_voucher_more_products` · `UF_voucher_pay_btn` · `UF_voucher_pay_cashback` · `UF_voucher_pay_method` · `UF_voucher_pay_method_card` · `UF_voucher_pay_method_card_desc` · `UF_voucher_pay_steps_title` · `UF_voucher_pay_title` · `UF_voucher_price_benefit` · `UF_voucher_price_original` · `UF_voucher_recommend_title` · `UF_voucher_review_count` · `UF_voucher_terms_agree_btn` · `UF_voucher_terms_pay_desc` · `UF_voucher_terms_pay_title` · `UF_voucher_terms_service` · `UF_voucher_terms_third_party` · `UF_voucher_terms_wish_desc` · `UF_voucher_terms_wish_title`

**위키에만 있고 엑셀에 없는 키는 0건**이므로 방향은 한쪽뿐이다(엑셀 → 위키 미반영). 어제 2판에서 7키를 추가하며 발생한 미반영으로 보이나 **의도적 축약일 가능성도 배제할 수 없어 이번 작업에서 손대지 않았다.** 채울지 확인 필요.

### (d-1) 용어집 보완 권장

**없음 — 이번 건은 제안하지 않는다.** 매장명·유통 브랜드는 v4.7에 등재어가 없고 용어집 충돌도 0건이나, 등재해도 **실사용 1건짜리 고유명사**라 「실사용 0~1건 표기 권장 금지」 기준에 어긋난다(2차 검토 동의). *(선행 리포트에서 이미 권장한 항목은 그쪽에서 추적한다.)*

### (d-2) 그 외 개선·확인 권장

| # | 항목 | 권장 내용 |
|---|---|---|
| **1** | ⚠️ **매장 목록 원본 대조 (데이터 진위)** | `뉴코아아울렛, 평택점` → `뉴코아아울렛평택점` 병합은 패턴상 타당하나 **파일 내부 근거만으로는 확정할 수 없다**(1차·2차 모두 동일 결론). 올리브영이 제공한 **원본 매장 목록과 대조**해 34개가 맞는지 확인 권장. 목록이 갱신되면 5개 언어를 함께 고쳐야 하므로 → #2와 함께 검토 |
| **2** | **매장 목록의 Admin 데이터 이관 검토 (선행 리포트 이월)** | 선행 리포트 (d-2) #1이 이미 제기 — 이 키는 **상품에 종속된 데이터**이고 상품이 늘면 키가 증식한다. 이번에 5개 언어를 채워 P0는 사라졌지만 **구조 문제는 그대로**이며, 오히려 이제 **매장이 하나 바뀔 때마다 5개 언어를 모두 고쳐야 한다**(유지보수 비용 5배). 구조적 해결은 상품 데이터의 다국어 필드 |
| **3** | **en·th에 `점/店` 표지가 없다** | `Busan Station`·`Lotte World`·`Konkuk University`·`Guro Digital`는 매장이 아니라 **역·테마파크·대학**으로 읽힌다(ja·zh는 `店`을 유지해 매장임이 드러남). 헤더 `*Unavailable stores`가 문맥을 주므로 오독은 제한적이나, 접미어(`Branch` 등) 추가 여부는 **문구 길이(현재 648자) 트레이드오프**라 결정 필요 |
| **4** | **ko 원문의 브랜드 표기 혼재** | 같은 브랜드가 `NC강남점`(라틴)과 `광주엔씨웨이브점`(한글 음차)으로 섞여 있다. 4개 언어는 전부 `NC`로 정규화했다. **ko는 현장 간판 대조가 목적이라 손대지 않았다** — 올리브영 공식 표기가 실제로 혼재하는지 확인 권장(원문 수정은 제공처 소관) |
| **5** | **zh 라틴↔한자 사이 공백 정책** | `W City店`·`Galleria晉州店`·`2001 Outlet盆唐店`·`光州NC Wave店`에 프로젝트 zh 규칙(라틴 앞뒤 공백)을 적용하지 않았다. **고유명사 1덩어리**라 공백을 넣으면 `W City 店`처럼 상호가 끊겨 보이기 때문이다. 규칙 예외를 고유명사에 인정할지 **일괄 확정 권장** |
| **6** | **zh `金泉市政廳店`** | 대만 통용은 `市政府`(예: `首爾市政府`)이고 `市政廳`은 홍콩·번역투다. 지명 요소라 오독 위험은 낮으나 표기 정책 사안 |
| **7** | **ja `現代百貨店釜山店` — `店` 연속 2회** | 브랜드 정식명이 `現代百貨店`이라 문법 오류는 아니다(zh는 `現代百貨`로 중복 회피). 가독성을 위해 `現代百貨店 釜山店`처럼 공백을 넣을지 결정 필요 |
| **8** | **en·th `Ulsan University`** | 울산대학교 공식 영문은 `University of Ulsan`. 매장명 축약 관례상 현행도 통용되나 공식명 준수 여부 결정 필요 |
| **9** | **en·th `W City`** | 실제 시설명이 `W-City`면 하이픈이 필요하다. **원 시설 표기 확인 필요**(파일만으로 판정 불가) |
| **10** | **ja·zh `駅舎`/`車站` 유지 여부** | `역사점`/`역점` 구분을 ja(`駅舎`/`駅`)·zh(`車站`/`站`)에 1:1 보존했다. 일본어 `駅舎`는 매장명으로 다소 이례적이라 **전부 `駅店`으로 통일**하는 선택지도 있다. 현행은 「현장 간판 대조」 목적에 맞춰 구분을 살린 것 |
| **11** | **인접 키 접두 기호 불일치 (참고)** | `UF_voucher_detail_unavailable`의 접두 기호가 ko·en `*` / ja `※` / zh 전각 `＊`로 갈린다. ja `※`는 관례상 정상, zh 전각 `＊`도 CJK 조판상 허용되나 통일 여부 확인할 만하다. **이번 작업 대상이 아니라 손대지 않았다** |

---

## 6. 게이트 통과 판정

| 기준 | 결과 |
|---|---|
| 한국어 원문 교정 선행 | ✅ 1건 교정(항목 병합 35 → 34) + alias 기록(§1) |
| 자동 검증 실행 | ✅ P0 0 / P1 75 / P2 15 — **대상 키 지적 0건** + 보조 코드 스캔 8종 0건 |
| 수동 3단계 전수 검토 | ✅ **170셀 전건**(5언어 × 34항목) 1·2·3단계 검토(§3) |
| 2차 독립 검토 교차 확인 | ✅ **실제 결함 3건 검출·전건 수정** · 정렬/자형/로마자 판정 동의 · 확인 필요 9건은 §5 이관 |
| 각 건 처리 판정 | ✅ 결함 3건 수정(§4) / P1·P2 90건은 대상 키 외 기존 지적으로 선행 리포트가 추적 / 정책 판정 1건(th 로마자) 근거 명시 |
| (d) 추가 개선·제안 | ✅ 최우선 #0(위키 25키 누락) · (d-1) 제안 없음(사유 명시) · (d-2) 11건(§5) |
| **P0 = 0건 (자동 + 수동)** | ✅ **통과** |

→ **엑셀 재생성·위키 반영 가능.** ⚠️ 이 키는 **Unifi v1.7.2에 아직 미등록**이다(`UF_voucher_detail_unavailable`도 동일) — 어제 71키 작업분 전체가 업로드 전이며, 쓰기 API가 없어 **업로드는 사용자가 수행**한다.

---

## 7. 위키 반영 (완료)

**대상**: [pageId 4639704548](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4639704548) · **v9 → v10**

| 변경 | 내용 |
|---|---|
| **다국어 표 대상 행 교체** | `UF_voucher_detail_unavailable_value` 행의 **5개 셀 전부** 확정값으로 surgical 교체(1,571 → 2,349 chars). 다른 45개 행·Screen 섹션·Policy·Flow는 무변경 |
| **th 의도 명시** | 「th_TH는 의도적으로 en과 동일한 로마자 — 태국어 음차 표준 부재」 주석 1줄 추가. **다음 담당자·자동 점검이 「th 번역 누락」으로 오판해 되돌리는 것을 막기 위함**(2차 검토 권고) |
| **XLT 엑셀 재첨부** | `xlt_fix_guidekim_voucher_71keys_20260826.xlsx` — 같은 파일명으로 `POST .../child/attachment/{id}/data` 갱신(본문 링크 그대로 최신본) |
| **History** | 2026-08-27 행이 없어 **신규 행 추가**(PIC `Claude 자동 생성` · Jira `UNIFY-10468`). 기존 3행(08-26) 전부 보존 |
| **Screen 섹션** | 이 키는 Screen 표 XLT 중첩표에 **존재하지 않아**(39키만 수록) 갱신 대상이 아니었다 — 확인 후 무변경 |

**검사 결과**: `check_wiki_storage.py pre` **exit 0** → PUT → `post` **exit 0**. PUT 직전 **버전 가드**(라이브 재조회로 v9 확인) 통과.

> ⚠️ **가드 전제 오류 실측** — 편집 스크립트의 보존 가드에 `ac:name="toc"`를 넣었다가 중단됐다. **이 페이지는 TOC 매크로 대신 수동 `<ul>` 목차**를 쓴다. History 행 형식도 다른 페이지와 달라(`<div class="content-wrapper">` 없음) 정규식이 빗나갔다. **페이지마다 골격이 다르므로 가드 앵커는 그 페이지 storage에서 실제 확인한 문자열로 잡아야 한다.**

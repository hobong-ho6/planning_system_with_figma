# 게이트 리포트 — 바우처 광고 단축 퍼널(약관동의 이탈 개선) 12키

- 날짜: 2026-09-01
- 위키: [바우처 광고 단축 퍼널 - 약관동의 이탈 개선](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4661828572) (pageId 4661828572, v1 신규 생성)
- 부모: `[PL] Unifi v1.7.4` (4667512747) · 참조 정본: 올리브영 5만원 상품상세 Unifi 직접제공 (4639704548)
- Figma: 플로우 `69826:10048` / 상세화면 `70005:15463` (프레임 3개 — `70005:14520` · `70005:14651` · `70005:15085`)
- 담당 FE 팀: **UIT** (프리픽스 `UF_`, 치환자 `{{0}}` — 이번 12키에는 변수 없음)
- 엑셀: `xlt/xlt_voucher_shortfunnel_12keys_20260901.xlsx` (위키 첨부 완료)
- 대상: **12키 × 5개 언어 = 60셀** (기존 등록 키 재사용 1건 + 신규 11건)

---

## (1a) 번역 전 — 한국어 원문 교정

사용자 확정으로 **2건 적용**. Figma 원문과 달라졌으므로 **Figma 원본 수정 요청 대상**이며 alias를 아래에 기록한다.

| Figma 원문 | XLT Key 등록값 (교정 후) | 사유 |
|---|---|---|
| `결제 시 필수 동의 항목에 모두 동의합니다.확인해주세요` | `결제 시 필수 동의 항목을 확인해 주세요.` | 두 문장이 뒤섞인 원문 오류(`~합니다.` + `확인해주세요`). 안내(에러) 문구이므로 확인 요청형으로 정리 · `확인해 주세요` 보조용언 띄어쓰기 |
| `언제든 바우처를 구매할 수 있도록 \n구매 링크를 LINE으로 받아볼 수 있어요` | `언제든 바우처를 구매할 수 있도록\n구매 링크를 LINE으로 받아볼 수 있어요.` | 짝 문구(`…받아볼 수 있어요.`)와 마침표 표기 통일 · 1줄 끝 불필요한 공백 제거 |

**alias (Figma 원문 → XLT Key)**
- `결제 시 필수 동의 항목에 모두 동의합니다.확인해주세요` → `UF_voucher_terms_required_error`
- `언제든 바우처를 구매할 수 있도록 \n구매 링크를 LINE으로 받아볼 수 있어요` → `UF_voucher_oa_link_desc`

교정하지 않은 원문(유지 판단): `아래 필수 항목에 모두 동의합니다.` · `GuideKim 이용약관 동의` · `Unifi 제3자 정보 제공 동의` · `취소·환불 조건 동의` · `자세히 보기` · `LINE 친구 추가하고 계속하기` · `LINE 친구 추가하고 링크 받기` · `구매 링크 전송을 위해\nUnifi 친구 추가가 필요해요` · `구매한 바우처와 새로운 혜택 안내를\nLINE으로 바로 받아볼 수 있어요.` — 맞춤법·띄어쓰기 이상 없음.

---

## (a) 자동 검증 — `scripts/validate_translation.py` 실측 결과

```
✓ 엑셀 로드: 12개 항목 / ✓ 용어집 로드: 116개 용어
1단계: 한국어 검증 — P0 0건, P1 0건, P2 4건
2단계: 용어집 검증 — 완료
3단계: 다국어 검증 — 완료
```

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | 5건 (전부 용어집 매핑 오탐 — 아래 (c)) |
| 🟢 P2 | 4건 (마침표 스타일 — 아래 (c)) |

리포트 파일: `xlt/xlt_voucher_shortfunnel_12keys_20260901_validation_report.md`

**통과 기준 충족**: 자동 P0 = 0 · 수동 P0 = 0 → 위키 반영·엑셀 생성 진행.

---

## (b) 수동 3단계 체크표 — **전수 점검** (12행 × 5언어 = 60셀, 전체 파일 대상)

검토 범위를 신규·변경 키로 한정하지 않고 **전체 행 전수**로 수행했다. 1차(메인) + **2차 독립 검토(`translation-reviewer` 에이전트, 별도 컨텍스트)** 를 교차했다.

### 1단계 — 한국어 원문 (12행)

| # | XLT Key | ko_KR | 판정 |
|---|---|---|---|
| 1 | `UF_voucher_terms_pay_title` | 바우처 수령을 위해\nUnifi 친구 추가가 필요해요 | 정확 (등록값 재사용 · 무변경) |
| 2 | `UF_voucher_oa_pay_desc` | 구매한 바우처와 새로운 혜택 안내를\nLINE으로 바로 받아볼 수 있어요. | 정확 |
| 3 | `UF_voucher_oa_pay_btn` | LINE 친구 추가하고 계속하기 | 정확 |
| 4 | `UF_voucher_oa_link_title` | 구매 링크 전송을 위해\nUnifi 친구 추가가 필요해요 | 정확 |
| 5 | `UF_voucher_oa_link_desc` | 언제든 바우처를 구매할 수 있도록\n구매 링크를 LINE으로 받아볼 수 있어요. | 정확 (1a 교정 적용) · `-수 있` 반복은 (d-2) 권장으로 이관 |
| 6 | `UF_voucher_oa_link_btn` | LINE 친구 추가하고 링크 받기 | 정확 |
| 7 | `UF_voucher_terms_agree_all` | 아래 필수 항목에 모두 동의합니다. | 정확 |
| 8 | `UF_voucher_terms_detail` | 자세히 보기 | 정확 |
| 9 | `UF_voucher_terms_service_agree` | GuideKim 이용약관 동의 | 정확 |
| 10 | `UF_voucher_terms_third_party_agree` | Unifi 제3자 정보 제공 동의 | 정확 |
| 11 | `UF_voucher_terms_refund_agree` | 취소·환불 조건 동의 | 정확 (`·` = U+00B7, 등록값 `UF_voucher_info_refund`와 코드포인트 일치) |
| 12 | `UF_voucher_terms_required_error` | 결제 시 필수 동의 항목을 확인해 주세요. | 정확 (1a 교정 적용) · 「확인 vs 동의 요구」 모호성은 (d-2) 권장으로 이관 |

기계 점검(전 60셀): nbsp 0 · 이중공백 0 · 전후 공백 0 · 치환자 0개(전 언어 동일 — 변수 없는 12키) · **개행 수 5개 언어 일치**(2줄 키 5개 / 1줄 키 7개).

### 2단계 — 용어집 대조 (v4.7 · terminology 116 · exceptions 9 · deprecated_terms 14)

- `deprecated_terms` 14항목 **위반 0건** — `팔로우`·`フォロー`·`關注`·`追蹤`·`ติดตาม`·`follow`·`OA 팔로우`·`OA 채널 추가` 0건. ja는 `友だち`(히라가나 정본) 사용, en은 `as a friend`, zh는 `加入 … 好友`, th는 `เพิ่มเพื่อน` — **LINE 공식 계정 친구 추가 5개 언어 정본 표기 준수**.
- `바우처`는 용어집 미등재이나 등록값 실사용이 일관(ja `バウチャー` · en `voucher` · th `เวาเชอร์` · zh `優惠券`)하여 그에 따랐다.

### 3단계 — 다국어 (48셀) · 이질 문자체계·선례 대조

- en·th 칸 CJK 0 · ja·zh 칸 한글 0 · zh 간체 0(전부 번체) · 컬럼 어긋남 0.
- 등록값 `UF_voucher_*` 48키 전수 대조. `UF_voucher_terms_pay_title`은 5개 언어가 등록값과 **완전 일치**(diff 0)를 확인 — 재사용 정합.

**2차 독립 검토에서 발견된 실제 결함 2건 — 모두 수정 반영**

| XLT Key | 언어 | 수정 전 | 수정 후 | 사유 |
|---|---|---|---|---|
| `UF_voucher_oa_link_title` | zh_TW | `才能傳送購買連結` | `才能收到購買連結` | **행위 주체 역전** — `才能+傳送`은 사용자가 링크를 보내는 것으로 읽힌다. en `to receive` · th `เพื่อรับ` · 짝 키 `oa_link_desc` zh `收到`와 시점 충돌 |
| `UF_voucher_terms_third_party_agree` | ja_JP | `…情報提供への同意` | `…情報提供に同意` | **조사 `への` 중복**. 등록 선례 `UF_asset_token_wire_consent_3rdparty_1` ja = `(必須)第三者への情報提供に同意` |

**등록 선례 대조로 추가 통일한 5건 (표기 갈림 해소 — 실측 근거 기재)**

| XLT Key | 언어 | 수정 전 | 수정 후 | 실측 근거 |
|---|---|---|---|---|
| `UF_voucher_terms_service_agree` | ja_JP | `GuideKim利用規約への同意` | `GuideKim 利用規約に同意` | 등록값 `UF_voucher_terms_service` ja = `（必須） GuideKim 利用規約`(라틴 전후 공백) + 약관 3항목 `…に同意` 병렬 통일 |
| `UF_voucher_terms_refund_agree` | ja_JP | `キャンセル・返金条件への同意` | `キャンセル・返金条件に同意` | 등록값 `UF_voucher_pay_refund_agree` ja = `決済により、キャンセル・返金条件に同意します。` |
| `UF_voucher_terms_refund_agree` | zh_TW | `同意取消與退款條件` | `同意取消與退款條款` | 같은 결제 화면 등록값 `UF_voucher_pay_refund_agree` zh = `…同意取消與退款條款` (레지스트리 전체는 條件 19 · 條款 17로 양립하나 **화면 내 선례 우선**) |
| `UF_voucher_terms_required_error` | ja_JP | `お支払い時の…` | `決済時の…` | `UF_voucher_pay_*` 화면이 `決済`로 통일(`決済完了`·`決済方法`·`決済の流れ`·`決済金額`) · ko「결제」 포함 키 ja 실측 **決済 32건 vs 支払い 4건** |
| `UF_voucher_terms_required_error` | th_TH | `รายการยินยอม…` | `รายการที่ต้องยอมรับ…` | 같은 약관 블록 4셀이 모두 `ยอมรับ` · 등록값 `UF_voucher_pay_refund_agree` th도 `ยอมรับเงื่อนไข…` |

**Figma 어노테이션 육안 검증에서 잡은 매칭 오류 1건 (중요)**

`collect_frames.py`의 자동 텍스트 매칭이 프레임 `70005:14520`의 3번 xlt 핀을 **배경에 가려진** `미사용 시 유효기간 내 100% 환불 가능`(text_id `70005:14565`)에 붙였으나, 어노테이션 이미지 육안 확인 결과 핀은 **CTA 버튼 `LINE 친구 추가하고 계속하기`**(rel y=742 = Sticky/1btn 영역)를 가리킨다. 대상 키를 `UF_voucher_info_refund_value` → **`UF_voucher_oa_pay_btn`(신규)** 으로 교체했다.
2차 검토가 독립 증거로 뒷받침: `UF_voucher_info_refund_value`는 Unifi/WEB BROWSER v1.7.3 **2,358키에 존재하지 않는다**(등록된 것은 라벨 키 `UF_voucher_info_refund`뿐) — 「기존 등록값 재사용」이라는 최초 전제 자체가 틀렸다.

---

## (c) 자동 검증기 P1·P2 각 건의 처리 판정

### P1 5건 — **전부 오탐** (사유·실측 기재)

| # | 키 / 언어 | 검증기 지적 | 처리 판정 | 사유 (실측) |
|---|---|---|---|---|
| 1 | `oa_pay_desc` / en | `구매` → `Buy` 권장 | **오탐** | `purchased voucher`(과거분사 수식). 용어집 매핑이 동사 원형 기준이라 품사 변화를 못 본다 |
| 2 | `oa_pay_desc` / en | `혜택` → `perks` 권장 | **오탐** | 실측 ko「혜택」 포함 키 en = **benefit(s) 14건 vs perks 6건**. 등록 선례 `UF_voucher_friend_add_notice` en = `benefit information`과 일치 → (d-1) 용어집 보완 후보 |
| 3 | `oa_link_title` / en | `구매` → `Buy` 권장 | **오탐** | `purchase link`(명사 수식). #1과 동일 사유 |
| 4 | `terms_required_error` / zh | `결제` → `結帳` 권장 | **오탐** | 실측 ko「결제」 포함 키 zh = **付款 19건 vs 結帳 2건**. 등록값 `UF_voucher_pay_btn` zh = `付款 {{0}}` → (d-1) 용어집 보완 후보 |
| 5 | `terms_required_error` / zh | `확인` → `查看` 권장 | **오탐** | `查看`는 「열람」, 여기는 「점검·확인」. 실측 ko「확인」 포함 키 zh = 確認 87건 vs 查看 57건으로 `確認`이 다수 |

> ※ 직전 실행에서 검출됐던 P1 2건은 실제 위반으로 판정해 수정했고, 그 결과 이번 실행에서 사라졌다 — ja `결제→決済`(`terms_required_error`), en `친구→friend`(`oa_link_btn`: `Add on LINE and get the link` → `Add as a friend on LINE and get the link`, 정본 표기 `add … as a friend` 반영).

### P2 4건 — **전부 유지**

| 키 | 지적 | 처리 판정 | 사유 |
|---|---|---|---|
| `oa_pay_desc` · `oa_link_desc` · `terms_agree_all` · `terms_required_error` | 마침표 있는 소수 스타일 | **유지** | 완결 문장형 안내문이라 마침표가 정상이다. `oa_link_desc`는 (1a)에서 짝 문구와 통일하려 마침표를 **보완**한 건이다. 등록 선례에도 `UF_signin_tos_selectall` = `모두 동의합니다.` 사례가 있다. 버튼·라벨형(`자세히 보기`·CTA 2건·약관 3항목)에는 마침표를 넣지 않아 유형별로 일관 |

---

## (d) 추가 개선·제안 (권장 — 사용자 결정 전 임의 반영하지 않음)

### (d-1) 용어집 보완 권장 — 실사용 실측 기반

자동 검증기가 **정상 의역을 P1로 반복 오탐**했다(= 용어집 매핑이 실사용과 어긋난다는 신호). 아래는 `fetch_xlt_registry.py` 실측값이며, **실사용 0건 표기는 권장하지 않았다**.

| 용어 | 현 용어집 권장 | 실측(Unifi/WEB BROWSER v1.7.3, 2,358키) | 보완 권장 |
|---|---|---|---|
| 혜택 (en_US) | `perks` | **benefit(s) 실측 14건** vs perks 실측 6건 | `benefit`을 허용 표기로 병기(문맥: 안내·고지문은 `benefit`, 마케팅 헤드라인은 `perks`) |
| 결제 (zh_TW) | `結帳` | **付款 실측 19건** vs 結帳 실측 2건 | 권장 표기를 `付款`으로 교체(또는 `付款` 우선·`結帳` 허용) |
| 결제 (ja_JP) | `決済` | **決済 실측 32건** vs 支払い 4건 | 현행 유지(실측이 권장과 일치) |
| 확인 (zh_TW) | `查看` | 確認 87건 vs **查看 57건** — 의미가 갈린다(확인=`確認`, 열람=`查看`) | 의미별 분기 주석 추가 권장 |

> 용어집은 **API 읽기 전용**이라 반영은 사용자가 Landpress CMS(`web3_xlt_json`)에 전체 JSON을 붙여넣어야 한다(`md/landpress.md`). 승인 시 기획자 가이드 zip 동반 갱신이 필수다.

### (d-2) 원문 명확화·정책 제안

1. **`UF_voucher_terms_required_error` 원문 모호** — `결제 시 필수 동의 항목을 확인해 주세요.`는 미동의 상태에서 **동의를 요구**하는 에러인데 문구는 「확인」이고, `결제 시`가 「결제할 때」/「결제하려면」으로 갈린다. 권장: `필수 동의 항목에 모두 동의해 주세요.` → 확정 시 5개 언어 동반 재번역 대상.
2. **`UF_voucher_oa_link_desc` ko 표현** — `구매할 수 있도록 … 받아볼 수 있어요`로 `-수 있` 반복. 권장: `언제든 구매할 수 있도록\n구매 링크를 LINE으로 보내드려요.`
3. **CTA 영문 길이** — `Add as a friend on LINE and continue`(36자) · `…and get the link`(40자)는 등록 CTA 선례(`Add friend and continue` 23자)보다 길어 버튼 오버플로 위험. 정본 표기(`as a friend`) 준수와 버튼 길이 중 무엇을 우선할지 결정 필요. 축약안: `Add on LINE and continue` / `Add on LINE and get the link`.
4. **Figma 프레임명 2건이 화면 문구와 서로 바뀜** — `친구추가 개선(링크저장)`(70005:14520) 안에 「바우처 수령」(구매 플로우) 문구, `친구추가 개선(구매)`(70005:14651) 안에 「구매 링크 전송」(링크저장 플로우) 문구. Screen ID는 **문구 기준으로 정정**(사용자 확정)하고 위키에 주석을 남겼다. **Figma 원본 프레임명 수정 요청 대상**. 참조 페이지 4639704548에도 동일 유형이 기록돼 있어 재발 패턴이다.
5. **근사 중복 키 정리 방침 필요(기획·FE 확인)** — 신규 키가 기존 등록값과 기능이 겹친다. 구 화면 폐기 여부에 따라 신규 등록 대신 기존 키 문구 패치가 정합적일 수 있다.
   - `UF_voucher_oa_pay_btn` ↔ `UF_voucher_friend_add_btn`(`친구 추가하고 계속하기` — `LINE ` 접두 유무만 다름)
   - `UF_voucher_oa_link_title/_desc` ↔ `UF_voucher_terms_wish_title/_desc`
   - `UF_voucher_oa_pay_desc` ↔ `UF_voucher_terms_pay_desc`
   - 인라인 약관 3키 ↔ `UF_voucher_terms_service` · `UF_voucher_terms_third_party` · `UF_voucher_terms_agree_btn`
   - 기존 약관동의 바텀시트(`(Unifi mini) TOS_purchase` · `TOS_favorite`) 폐기 여부가 결정되면 이 목록을 정리한다.
6. **키 네이밍 혼재** — 같은 바텀시트 3키가 `terms_pay_title`(기존 재사용) + `oa_pay_desc` + `oa_pay_btn`으로 프리픽스가 섞였다. 기존 키 재사용을 유지하는 대가이며, 통일하려면 title도 신규화해야 한다.
7. **`UF_voucher_info_refund_value` 언어 간 불일치(별건)** — ko `100%` → ja `全額`·en `Full`·zh `全額`인데 th만 `100%` 유지, th `ระยะเวลาใช้งาน`(사용 기간) vs ko `유효기간`. 이 화면 대상이 아니라 수정하지 않았고, 상품 정보 영역 점검 시 확인 권장.
8. **GA Event 미정의** — 신규 인터랙션(일괄 동의 체크·항목별 「자세히 보기」)의 이벤트가 정의되지 않았다. 위키에는 참조 페이지의 기존 이벤트(`view_oa_purchase`·`click_oa_add_purchase`·`view_oa_favorite`·`click_oa_add_favorite`·`view_payment`)만 기재했다.

---

## 통과 판정

- **자동 P0 = 0 · 수동 P0 = 0** → 통과. 위키 반영·엑셀 생성 완료.
- P1 5건: 전부 오탐 판정(실측 근거 기재) · P2 4건: 전부 유지 판정.
- 실제 결함 2건 + 선례 통일 5건 + 매칭 오류 1건 수정 반영 후 재검증 완료.
- (d) 권장 사항은 **사용자 결정 전까지 반영하지 않았다.**

---

# 추가 실행 — 일본어 현지 검수 반영 (2026-09-01, 위키 v3)

사용자가 위키 v2에서 `ja_JP` 셀을 직접 수정하고 변경 부분을 **빨간 텍스트로 표시**했다. 그 표시를 원본으로 읽어 6키를 반영했다(빨간 span 16건 중 6건이 검수 항목, 나머지 10건은 기존 Screen 표 주석·GA Event 표기).

- 산출 엑셀: `xlt/xlt_voucher_shortfunnel_12keys_jp_reviewed_20260901.xlsx` (위키 재첨부 · 본문 링크 갱신 완료)
- 자동 검증 재실행: **P0 0건** · P1 5건 · P2 4건 — 직전 실행과 동일하며 **ja 관련 신규 지적 0건**
- 위키 PUT 전 `check_wiki_storage.py pre` exit 0 · 직후 `post` exit 0 · 구 엑셀 링크 잔존 0건

## (1a) 원문 교정 — 이번 실행 변경 없음

한국어 원문은 손대지 않았다(검수 대상이 `ja_JP`뿐). 앞 절의 alias 2건이 그대로 유효하다.

## (b) 수동 검토 — 변경된 6셀 전수 + 12키 개행 정합 전수 재점검

| XLT Key | 검수 전 (1차 번역) | 검수 후 (반영) | 변경 성격 |
|---|---|---|---|
| `UF_voucher_oa_pay_desc` | `購入したバウチャーと新しい特典のご案内を`+개행+`LINEですぐに受け取れます。` | `LINE公式アカウントで、購入バウチャーの確認や新しい特典を受け取れます。` | 문구 전면 교체 · **개행 소실** |
| `UF_voucher_oa_link_desc` | `いつでもバウチャーを購入できるよう`+개행+`購入リンクをLINEで受け取れます。` | `いつでも購入可能なバウチャーのリンクを、LINEで受け取れます。` | 문구 전면 교체 · **개행 소실** |
| `UF_voucher_oa_pay_btn` | `LINEで友だち追加して続ける` | `LINEで友だち追加して続ける。` | 문말 `。` 추가 |
| `UF_voucher_oa_link_btn` | `LINEで友だち追加してリンクを受け取る` | `LINEで友だち追加してリンクを受け取る。` | 문말 `。` 추가 |
| `UF_voucher_terms_service_agree` | `GuideKim 利用規約に同意` | `GuideKimの利用規約に同意` | 라틴 뒤 공백 → 조사 `の` |
| `UF_voucher_terms_refund_agree` | `キャンセル・返金条件に同意` | `キャンセル・返金規約に同意` | `条件` → `規約` |

**3단계 재점검 결과**
- ja 칸 한글 0 · 이질 문자체계 0 · nbsp 0 · 이중공백 0 · 전후 공백 0 · 치환자 0(변수 없는 12키).
- `deprecated_terms` 위반 0 — `友だち`(히라가나 정본) 유지 확인.
- **개행 수 5개 언어 정합: 12키 중 2키 불일치**(아래 처리 판정).

## (c) 처리 판정

| # | 항목 | 처리 판정 | 사유 |
|---|---|---|---|
| 1 | 검수 6키 문구 | **그대로 반영** | 현지 검수자 확정본이며 사용자 지시가 「일본어 업데이트」다. 임의 재수정하지 않았다 |
| 2 | 개행 소실 2키 (`oa_pay_desc`·`oa_link_desc`) | **원문대로 반영 + 사용자 확인 요청** | 검수본에 개행이 없다. 개행을 임의로 넣으면 검수자가 정한 줄 나눔을 추측하는 것이 되므로 넣지 않았다. 단 ko·en·th·zh는 모두 2줄이라 **바텀시트 줄 구조가 어긋난다** → 아래 (d) 분할안 |
| 3 | CTA 2키 문말 `。` | **반영 + 확인 권장** | 등록 선례 `UF_voucher_friend_add_btn` ja = `友だち追加して続ける`(마침표 없음)와 어긋난다. 같은 화면 타이틀 `…必要です。`에는 `。`가 있어 검수자가 문서 전체 스타일로 맞춘 것으로 보인다 |
| 4 | 자동 P1 5건 | **전부 오탐 유지** | 앞 절 (c)와 동일(en 품사 변화 2건 · en `혜택`→benefit 실측 14:6 · zh `결제`→付款 실측 19:2 · zh `확인`→確認 실측 87:57). ja 지적은 0건이 됐다 |
| 5 | 자동 P2 4건 | **전부 유지** | 앞 절과 동일 사유(완결 문장형 마침표) |

## (d) 추가 개선·제안 (사용자 결정 필요)

1. **개행 분할안 (권장)** — 5개 언어 줄 구조를 맞추려면 쉼표 자리에서 나누는 것이 자연스럽다.
   - `UF_voucher_oa_pay_desc`: `LINE公式アカウントで、` + 개행 + `購入バウチャーの確認や新しい特典を受け取れます。`
   - `UF_voucher_oa_link_desc`: `いつでも購入可能なバウチャーのリンクを、` + 개행 + `LINEで受け取れます。`
   승인 시 `patch_translation.py`로 `ja_JP` 셀만 교체하고 엑셀을 재생성한다.
2. **CTA 문말 `。` 정책 확정** — 버튼 라벨에 `。`를 쓰는지 문서·시스템 차원에서 통일이 필요하다. 유지하면 등록값 `UF_voucher_friend_add_btn`(`。` 없음)과 같은 화면에서 갈리고, 제거하면 검수자 의도와 갈린다.
3. **`返金条件` → `返金規約` 파급** — 등록값 `UF_voucher_pay_refund_agree` ja는 `キャンセル・返金条件に同意します。`로 여전히 `条件`이다. 같은 결제 화면에서 `規約`/`条件`이 갈리므로 **등록값 쪽도 검수 대상**으로 올릴지 확인 필요(ko 원문은 「취소·환불 조건」이라 ko는 무변경).
4. **`GuideKimの利用規約` 파급** — 등록값 `UF_voucher_terms_service` ja는 `（必須） GuideKim 利用規約`(공백)이다. 같은 약관 블록에서 표기가 갈리므로 등록값 검수 여부 확인 필요.
5. 앞 절 (d-1) 용어집 보완 2건 · (d-2) 원문 명확화·프레임명·근사 중복 키 항목은 **미결 그대로**다.

## 통과 판정

- **자동 P0 = 0 · 수동 P0 = 0** → 통과. 엑셀 생성·위키 반영 완료.
- 개행 정합 2건과 CTA `。` 정책은 **사용자 결정 대기** 항목으로 남겼다(임의 변경하지 않음).

---

# 추가 실행 2 — 개행·버튼 문말·기존 등록값 반영 (2026-09-01, 위키 v4)

사용자 지시 3건(「개행 추가」·「버튼 문말도 추가」·「신규에 맞춰 기존값도 업데이트」)을 반영했다.

- 산출 엑셀: `xlt/xlt_voucher_shortfunnel_15keys_jp_final_20260901.xlsx` (**15행** = 신규 12 + 기존 등록값 갱신 3) — 위키 재첨부 · 본문 링크 갱신 완료
- 자동 검증: **P0 0건** · P1 6건 · P2 5건 (증가분 1건씩은 새로 포함된 `UF_voucher_pay_refund_agree` 건)
- `check_wiki_storage.py` pre exit 0 · post exit 0 · 구 엑셀 링크 잔존 0건

## (1a) 원문 교정 — 이번 실행 변경 없음

한국어는 손대지 않았다. 기존 등록값 3키의 `ko_KR`도 **등록값 그대로 보존**했다(변경은 `ja_JP`만).

## (b) 수동 검토 — 변경 5셀 + 15행 개행 정합 전수 재점검

### ① 개행 추가 2건 (쉼표 자리 분할)

| XLT Key | 변경 전 (검수본, 1줄) | 변경 후 (2줄) |
|---|---|---|
| `UF_voucher_oa_pay_desc` | `LINE公式アカウントで、購入バウチャーの確認や新しい特典を受け取れます。` | `LINE公式アカウントで、`+개행+`購入バウチャーの確認や新しい特典を受け取れます。` |
| `UF_voucher_oa_link_desc` | `いつでも購入可能なバウチャーのリンクを、LINEで受け取れます。` | `いつでも購入可能なバウチャーのリンクを、`+개행+`LINEで受け取れます。` |

**개행 수 5개 언어 정합 전수 재점검: 불일치 0건 / 15행** (직전 2건 해소).

### ② 기존 등록값 `ja_JP` 갱신 3건 — ko·en·th·zh 등록값 보존

| XLT Key | ja 변경 전 (등록값) | ja 변경 후 | 사유 |
|---|---|---|---|
| `UF_voucher_friend_add_btn` | `友だち追加して続ける` | `友だち追加して続ける。` | 버튼 문말 `。` 정책 확정(사용자 지시). 신규 `oa_pay_btn`(`…続ける。`)과 통일 |
| `UF_voucher_terms_service` | `（必須） GuideKim 利用規約` | `（必須） GuideKimの利用規約` | 검수 표기 `GuideKimの利用規約`에 맞춤. 같은 약관 블록에서 신규 `terms_service_agree`와 통일 |
| `UF_voucher_pay_refund_agree` | `決済により、キャンセル・返金条件に同意します。` | `決済により、キャンセル・返金規約に同意します。` | 검수 표기 `返金規約`에 맞춤. 같은 결제 화면에서 신규 `terms_refund_agree`와 통일 |

**3단계 재점검**: ja 칸 한글 0 · 이질 문자체계 0 · nbsp 0 · 이중공백 0 · 전후 공백 0 · 치환자 정합(변수 없음) · `deprecated_terms` 위반 0(`友だち` 히라가나 정본 유지) · zh 간체 0.

## (c) 처리 판정

| # | 항목 | 처리 판정 | 사유 |
|---|---|---|---|
| 1 | 개행 위치 | **쉼표 자리로 확정** | 두 문장 모두 읽기 단위가 쉼표에서 끊긴다. ko·en·th·zh의 2줄 구조와 줄 수가 일치한다 |
| 2 | 버튼 문말 `。` | **정책으로 확정·적용** | 사용자 지시. 등록값에 이미 선례가 있다 — `UF_voucher_pay_fail_btn` ja `商品購入画面に戻る。` · `UF_voucher_check_purchased` ja `購入したバウチャーを確認する。` |
| 3 | 기존값 갱신 범위 | **직전 리포트에서 명시한 3키로 한정** | 앞 절 질문에서 제시한 `friend_add_btn`(버튼 `。`) + `terms_service`·`pay_refund_agree`(표기 파급) 3건만 반영했다. 그 밖의 바우처 CTA 확장은 (d)로 남겼다 |
| 4 | P1 6건 | **전부 오탐 유지** | 5건은 앞 절과 동일. 신규 1건 `pay_refund_agree` zh `결제`→`結帳` 권장도 **오탐** — 실측 ko「결제」 포함 키 zh는 付款 19건 vs 結帳 2건이고, 이 셀은 **등록값 원문 그대로**(zh 무변경) |
| 5 | P2 5건 | **전부 유지** | 4건은 앞 절과 동일. 신규 1건 `pay_refund_agree` ko 마침표는 **등록값 원문**이라 손대지 않았다 |

## (d) 추가 개선·제안 (사용자 결정 필요 — 미반영)

1. **버튼 문말 `。` 정책의 잔여 파급 5키 (권장)** — 같은 `UF_voucher_*` 키스페이스에서 아직 `。`가 없는 CTA·링크다. 정책을 일관 적용하려면 함께 갱신해야 하지만, **이 문서의 화면(약관 동의·친구 추가 퍼널) 밖**이라 임의로 넣지 않았다.

   | XLT Key | ko | 현행 ja | 적용 시 |
   |---|---|---|---|
   | `UF_voucher_terms_agree_btn` | 약관 동의하고 계속하기 | `規約に同意して続ける` | `…続ける。` |
   | `UF_voucher_btn_buy_now` | 바로 구매하기 | `今すぐ購入する` | `…購入する。` |
   | `UF_voucher_btn_wishlist` | 구매 링크 저장 | `購入リンクを保存` | `…保存。` |
   | `UF_voucher_pay_btn` | {{0}} 결제하기 | `{{0}}を支払う` | `…支払う。` |
   | `UF_voucher_more_products` | 더 많은 상품 보기 | `商品をもっと見る` | `…見る。` |

   ※ `UF_voucher_review_all`(`レビューをすべて見る（33件）`)은 괄호 수치로 끝나 `。` 부착이 부자연스러워 후보에서 제외했다.
2. **참조 문서(4639704548) 동기화 필요** — 갱신한 기존 3키는 그 페이지의 다국어 표·Screen XLT 표에도 실려 있다. 시스템 업로드 후 그 위키의 ja 표기도 맞춰야 표기가 갈리지 않는다.
3. 앞 절들의 미결 항목은 그대로다 — (d-1) 용어집 보완 2건(`혜택`→benefit · `결제` zh→付款), (d-2) 원문 명확화 `terms_required_error`, Figma 프레임명 스왑 2건, 근사 중복 키 정리 방침, GA Event 미정의.

## 통과 판정

- **자동 P0 = 0 · 수동 P0 = 0** → 통과. 엑셀 15행 생성·위키 v4 반영·재첨부 완료.
- 개행 정합 불일치 **0건 / 15행**(직전 미결 해소).
- 잔여 버튼 5키 확장은 **사용자 결정 대기**(임의 반영하지 않음).

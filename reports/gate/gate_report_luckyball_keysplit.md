# 번역 검증 게이트 리포트 — XLT 키 분리(hero, my_draw)

- 대상 위키: pageId 4479306980 / 팀 **LV** (`mini_luckyball_` · `{0}`)
- 대상 엑셀: `xlt/xlt_output_20260724225916.xlsx` (78키)
- 작업: 색상(흰/보라·라임)별로 텍스트를 2개 키로 **분리**(FE span 스타일링용). 사용처는 25034(초대자 화면 전체) Screen XLT + 다국어 표.
- 용어집: API v2.6 새로 조회.

## 분리 내용 (As-is → To-be)

**1) `mini_luckyball_invite_hero` → 2키** (원문 3줄, 1줄=흰색 / 2-3줄=보라)

| 키 | ko | ja | en | th | zh_TW |
|----|----|----|----|----|-------|
| `invite_hero_text`(흰) | 친구 초대하고 함께 | 友だちを招待して | Invite a friend and | ชวนเพื่อนมาด้วยกัน | 邀請好友一起 |
| `invite_hero_highlight`(보라) | 최대 50,000JPYC\n럭키볼 받기 | 最大50,000JPYCの\nラッキーボールをもらおう | get up to 50,000JPYC\nLucky Ball | รับลูกบอลนำโชค\nสูงสุด 50,000JPYC | 領取最高 50,000JPYC 的\n幸運球 |

**2) `mini_luckyball_my_draw_count` → 2키** ("{0}개 뽑기", {0}개=흰색 / 뽑기=라임)

| 키 | ko | ja | en | th | zh_TW |
|----|----|----|----|----|-------|
| `my_draw_value`(흰) | {0}개 | {0}個 | {0} | {0} ลูก | {0} 顆 |
| `my_draw_action`(라임) | 뽑기 | 引く | Draw | สุ่ม | 抽取 |

- 기존 `invite_hero`·`my_draw_count`는 제거. 원문 분할값을 alias로 기록.

## 0. 한국어 원문 교정 (게이트 1단계)

- 분리된 4키 ko는 원문을 색 경계로 나눈 것 — 오타·띄어쓰기 이상 없음(배너·라벨이라 마침표 없음 정상). placeholder `{0}`는 `my_draw_value`에 보존. alias 4건 기록.

## Executive Summary (자동 — 78키)

| 심각도 | 건수 | 카테고리 |
|--------|------|----------|
| 🔴 P0 (Critical) | **0건** | 빈칸, 오타, 언어 혼입, 용어집 표기 위반 |
| 🟡 P1 (Medium) | 25건 | 용어 불일치(용어집 협소 매핑) |
| 🟢 P2 (Low) | 30건 | 마침표 스타일 |

→ **P0 = 0**. 신규 4키 중 hero_text·hero_highlight만 P2(마침표) — 배너 무마침표 오탐. draw_value/action 무플래그.

## 1단계: 한국어 (수동, 전수 78키)

- 분리 4키 재검토: 색 경계로 정확히 분할, 원문 합치면 기존과 동일. `{0}개`/`뽑기` 분할 정상. 자동 P2(hero) 오탐.
- 나머지 74키 이상 없음.

## 2단계: 용어집 (수동, 전수)

- 예외 패턴(JPYC) 표기 통일, zh 번체 정자(領/顆/幸). 자동 P1 25건 전건 협소 매핑 오탐.

## 3단계: 다른 언어 (수동, 전수)

- 빈칸 0·언어 혼입 0. `my_draw_value` 5개 언어 placeholder `{0}` 일치. hero_highlight 줄바꿈(\n) 보존.
- 분할 합산 검증: 각 언어에서 (text+highlight)·(value+action) 합치면 원 문구 의미 보존 확인.

## 각 건 처리 판정

- **P1 25건 — 전건 오탐**(용어집 협소 매핑). 수정 없음.
- **P2 30건 — 전건 오탐**(무마침표 배너/타이틀 스타일). 유지.

## (d) 추가 개선·제안 (권장)

- **(d-1) `my_draw` 어순 주의(권장 확인)**: `{0}개 뽑기`는 한국어가 **수량(흰)+동사(라임)** 순이나, **en/th/zh는 동사 선행**("Draw {0}"/"สุ่ม {0} ลูก"/"抽取 {0} 顆"). 색 2노드를 **수량→동사 고정 배치**로 렌더하면 이들 언어에서 어순이 뒤바뀌어 보일 수 있음(`{0} Draw` 등). FE가 언어별 노드 순서를 바꾸거나, 디자이너가 언어별 최종 카피를 확정하는 것을 권장. 현재는 의미 기준 분할값으로 등록.
- **(d-2) 용어집 보완**: 다의어 문맥 변형(`md/landpress.md`). 이번 분리와 무관.

## 통과 판정

- 자동 **P0=0** + 수동 3단계 **P0=0** → **통과**. 키 2개 제거·4개 추가(78키), 엑셀 재생성. P1/P2 전건 오탐. (d-1) 어순은 사용자·FE 확인 권장(임의 변경 안 함).

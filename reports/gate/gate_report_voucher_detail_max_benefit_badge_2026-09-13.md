# 번역 품질 게이트 리포트 — `mini_voucher_detail_max_benefit_badge` 신규 1키

- **작업**: Unifi mini v2.0 · 바우처 상세 - 가격 펼치기 · 최대혜택가 배지(어노테이션 5번) 전용 신규 키 1건
- **계기**: Slack `[Voucher detail xlt]` 스레드 — catrius(LV)가 `mini_voucher_detail_max_benefit_rate`에서 `{0}%` 제거를 요청해 반영했으나, 같은 배지에 **합계 혜택률을 포함해 노출해야 하는 LV 사용처**가 남아 있어 `{0}%`를 유지하는 별도 키가 필요해졌다(An Vo 요청, 2026-09-13)
- **담당 FE 팀**: **LV** (사용자 확정) → 프리픽스 `mini_` · 치환자 **`{0}`**(단일 중괄호)
- **대상 파일**: `xlt/unifi_mini_v2.0_voucher_detail_max_benefit_badge_1키_20260913.xlsx`
- **대조 레지스트리**: Dapp Portal / WEB BROWSER / **v2.6.1** (1,685키) · Unifi / WEB BROWSER / **v1.7.6** (2,549키) — 2026-09-13 실조회
- **용어집**: `web3_xlt_json` **v5.5** (118 terms · 22 deprecated)

## 0. 신규 키 정의

| XLT Key | ko_KR | ja_JP | en_US | th_TH | zh_TW |
|---|---|---|---|---|---|
| `mini_voucher_detail_max_benefit_badge` | 최대혜택가 {0}% | 最大特典価格 {0}% | Max benefit price {0}% | ราคาพิเศษสูงสุด {0}% | 最高優惠價 {0}% |

- `{0}` = discount rate + cashback rate **단순 합**(예: 5% + 10% = 15%) — 위키 `바우처 상세 - 가격 펼치기` Description 6번 정책 그대로
- 5개 언어 값은 **2026-09-12에 게이트를 통과해 등록됐던 `_rate`의 제거 전 값과 동일**하다(`reports/gate/gate_report_unifi_mini_v2_xlt_108keys_2026-09-12.md` 275행). 같은 화면의 형제 키(`_label`·`_rate`)와 표기를 일치시키기 위해 새로 의역하지 않았다.

### 이름 충돌·중복 등록 점검 (메모리 규칙: 키 이름 + 한국어 문구 양쪽 대조)

| 점검 | 결과 |
|---|---|
| 키 이름 `mini_voucher_detail_max_benefit_badge` | 두 레지스트리 **모두 미등록**(free) — 이름 충돌 없음 |
| ko 문구 `최대혜택가` 포함 등록 키 | Dapp Portal 2건(`_label`·`_rate`, 둘 다 `{0}%` **없음`) · Unifi 0건 → **`{0}%`를 포함한 동일 문구는 현재 등록 0건**, 중복 등록 아님 |
| `_badge` 명명 근거 | 위키가 이 위치를 이미 "배지"로 부른다("배지와 펼침 카드 합계 라벨을 각각의 키로 관리한다") · 같은 키스페이스가 `_label`/`_btn`/`_title` 등 **요소 타입 접미사** 관례를 쓴다 |

## 1. 한국어 원문 교정 (번역 전 필수)

- Figma 원문(`바우처 상세` `72528-4404` · `top info/최대혜택가 15%`)과 **동일**하게 `최대혜택가`(붙여쓰기)를 유지했다 → **alias 없음**.
- 엄밀한 맞춤법으로는 `최대 혜택가`(띄어쓰기)가 맞지만, **이미 등록된 형제 키 2건(`_label`·`_rate`)이 전부 `최대혜택가`**이고 같은 화면에 나란히 노출되므로 이 키만 띄우면 화면 내 표기가 갈린다. 표기 통일은 3키 일괄 사안이라 **(d-2) 제안**으로 올리고 임의 적용하지 않는다.

## 2. 자동 검증 (`scripts/validate_translation.py` 실행)

| 파일 | 항목 | 🔴 P0 | 🟡 P1 | 🟢 P2 |
|---|---|---|---|---|
| `unifi_mini_v2.0_voucher_detail_max_benefit_badge_1키_20260913.xlsx` | 1키 | **0** | 2 | 0 |

**P0 = 0건** → 게이트 통과. (리포트: `xlt/unifi_mini_v2.0_voucher_detail_max_benefit_badge_1키_20260913_validation_report.md`)

## 3. 수동 3단계 검토 — **전수(전체 파일 = 1키 × 5개 언어 = 5셀 전체 행)**

> 자동 검증기가 구조적으로 못 잡는 항목(외래어 음차 오염 · 띄어쓰기 누락 · 다의어 모호 · 표현 어색 · 치환자 표기)을 한 셀씩 직접 확인했다. 검토 범위를 신규 키로 한정한 것이 아니라 **이 파일의 전체 행이 곧 1키**다.

| 단계 | 점검 | 결과 |
|---|---|---|
| **1단계 (한국어 원문)** | 맞춤법·띄어쓰기·표기 일관성 | `최대혜택가 {0}%` — 붙여쓰기는 Figma 원문·형제 키와 일치(위 1항). 단어 붙음(`누리고다양한`류) 없음. **통과** |
| **2단계 (용어집 대조)** | 혜택/최대/가격 등재값 정합 | 혜택 ja `特典` ✓ · zh `優惠` ✓ · 최대 ja `最大` ✓ · zh `最高` ✓ · 가격 ja `価格` ✓ / en·th는 아래 (c) 판정 참조. deprecated 22종 위반 **0건** |
| **3단계 (다국어 품질)** | 셀 단위 전수 | 아래 표 |

| # | 언어 | 값 | 판정 |
|---|---|---|---|
| 1 | ko_KR | `최대혜택가 {0}%` | ✅ 정확 — 치환자 `{0}` 1개(LV 단일 중괄호) · 조사 없음(받침 판정 불필요) |
| 2 | ja_JP | `最大特典価格 {0}%` | ✅ 정확 — 最大+特典+価格 전부 용어집 정본. 음차 오염 없음 · 이질 문자체계 혼입 없음 |
| 3 | en_US | `Max benefit price {0}%` | ✅ 정확 — 라벨 문맥. 단어 붙음(`perksand`류) 없음 |
| 4 | th_TH | `ราคาพิเศษสูงสุด {0}%` | ✅ 정확 — ราคา(가격)+พิเศษ(특별)+สูงสุด(최상급) 후치 수식 어순 정상 |
| 5 | zh_TW | `最高優惠價 {0}%` | ✅ 정확 — **번체 정자** 확인(简体 혼입 없음) |

| 공통 점검 | 결과 |
|---|---|
| 치환자 개수·표기 | 5개 언어 전부 `{0}` **1개**, 이중 중괄호 `{{0}}` 혼입 **0건** → LV 규칙 준수 |
| `%` 기호 위치 | 5개 언어 전부 `{0}%` 연접 — 위치 분기 없음 |
| 빈칸·언어 혼입 | 없음 |

## 4. 자동 검증기 보고 각 건의 **처리 판정** (P0/P1/P2 전건)

| # | 검출 | 심각도 | 처리 판정 |
|---|---|---|---|
| ① | en_US: `최대` → `up to` 권장 | 🟡 P1 | **오탐** — 용어집 v5.5 note가 「en `Max`는 라벨 문맥에서 정당해 deprecated에 넣지 않는다」고 명시한 바로 그 문맥이다. 또한 등록된 형제 키 `_label`·`_rate`가 `Max benefit price`라 바꾸면 같은 화면에서 en 표기가 갈린다. **2026-09-12 게이트에서 동일 건을 이미 오탐 판정**했다(재판정 일치) |
| ② | th_TH: `혜택` → `สิทธิประโยชน์` 권장 | 🟡 P1 | **오탐** — 복합 가격 라벨이라 `혜택가` 계열은 등재 `UF_voucher_price_benefit`의 `ราคาพิเศษ`와 정합한다. `สิทธิประโยชน์`(권익·특전)은 가격 라벨에 쓰면 어색하다. **2026-09-12 동일 오탐 판정과 일치** |
| — | P0 | 🔴 | **0건** |
| — | P2 | 🟢 | **0건** |

## 5. 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

### (d-1) 용어집 보완 권장

**권장 없음.** 이번 검출 2건은 모두 이미 알려진 문맥성 오탐이고, 용어집 v5.5가 그 사유를 note에 명시적으로 기록해 둔 상태다(en `Max` 라벨 문맥 · th 가격 라벨). 새 등재는 실사용 실측 없이 권장하지 않는다(실사용 0건 표기 권장 금지 원칙).

### (d-2) 추가 개선·제안

| # | 제안 | 근거 |
|---|---|---|
| ① | **`최대혜택가` 띄어쓰기 통일 검토** — `최대 혜택가`가 맞춤법상 바르다. 적용한다면 `_badge`·`_label`·`_rate` **3키 + Figma 원문**을 동시에 바꿔야 한다(부분 적용 시 같은 화면에서 표기가 갈린다) | 맞춤법 · 화면 내 일관성 |
| ② | **키 3개 역할 재정리 권장** — 현재 `_rate`(라벨만) · `_label`(라벨만, 값 동일) · `_badge`(라벨+{0}%)로 **같은 문구 계열 3키**가 된다. `_rate`는 이름과 값이 어긋난 상태(rate인데 rate가 없음)이므로, FE 사용처를 확인해 `_rate`를 정리(폐기 또는 개명)할지 LV와 합의 권장 | 스레드에서 catrius·An Vo 요구가 갈린 지점이며, 방치하면 다음 개발자가 어느 키를 쓸지 판단할 수 없다 |
| ③ | **ja `特典` vs `特別` 표기 분기 확인** — 같은 `혜택가` 계열인데 `UF_voucher_price_benefit`·`mini_home_price_benefit`은 ja `特別価格`, `max_benefit` 3키는 `最大特典価格`이다. 용어집 정본은 혜택=`特典` | 등록값 실측(두 레지스트리) — 이번 범위 밖이라 별도 결정 대기 |

## 6. 통과 판정

**P0 = 0건 (자동 + 수동 전수 모두)** → 엑셀 생성 · 위키 반영으로 진행 가능. P1 2건은 전건 **오탐** 판정(위 4항). (d) 권장 3건은 사용자 결정 전까지 **반영하지 않는다**.

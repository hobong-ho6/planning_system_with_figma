# 번역 품질 게이트 리포트 — GuideKim 배너 2키 ja_JP 정합 패치 + To-be 프레임 갱신

- **작업 유형**: 키 단위 번역 패치 모드 **(B) lang-specific** — 특정 언어(ja_JP)만 정본값으로 교체 + 위키 Mode B 프레임 갱신
- **대상 위키**: [pageId 4637821463](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4637821463) `[Unifi Mini] 홈을 LIFF K-Pick 홈과 통일 + 배너 문구 변경` · Jira `UNIFY-10315` · **v10 → v11**
- **대상 Figma**: `GOCHAYBS7hIrmWRGNuJOKV` / 프레임 **`To-be`**(`68044:20741`, 375×813 · `clipsContent: false`이라 콘텐츠는 y=877까지)
- **담당 FE 팀**: **LV** — 기존 키 프리픽스 `mini_guidekim_`로 판별(사용자 문의 불요) · 치환자 `{0}`(이번 2키는 변수 없음)
- **대상 키 (2건 · ja_JP만 패치)**:
  - `mini_guidekim_kbeauty_to_kculture`
  - `mini_guidekim_all_korea_experience`
- **등록값 조회 시각**: **2026-08-24 19:0x KST** — `Dapp Portal / WEB BROWSER / v2.6.1`(1,599키) + `Unifi / WEB BROWSER / v1.6.7.1`(2,143키) (캐시 금지 — 이번 run 신규 조회)
- **Figma 코멘트 조회**: 이번 run **REST 신규 조회**(`fetch_threads` + `collect_node_boxes`) — `roots=6`
- **용어집**: v4.7 (API 신규 조회)
- **검토 범위**: **전수 점검** — 대상 파일 전체 행의 5개 언어를 한 줄씩 검토(**2키 × 5개 언어 = 10셀 전부**). 변경 언어(ja 2셀)로 한정하지 않음
- **산출물 엑셀**: `xlt/xlt_fix_guidekim_banner_2keys_20260824.xlsx` — **위키 첨부와 같은 파일명 유지**(본문 링크 그대로 최신본 렌더)

> **선행 리포트**: `reports/gate/gate_report_guidekim_banner_2keys_2026-08-24.md`(3판, 미커밋 — 다른 세션 소관이라 손대지 않음). 이 리포트는 **그 이후 발생한 위키 v5→v9 사용자 편집분을 정합화**하는 후속 패치다.

---

## 0. 발단 — 엑셀·위키·시스템 3자 불일치 실측

이번 세션 시작 시 세 곳의 ja_JP가 갈려 있었다.

| 위치 | `kbeauty_to_kculture` ja | `all_korea_experience` ja |
|---|---|---|
| **위키 v9 다국어 표**(사용자 직접 편집) | `最大15%還元！` | `オリヤン、ダイソーのバウチャーからビューティー施術まで` |
| **위키 첨부 엑셀**(선행 세션 산출물) | `最大15%キャッシュバック！` | `オリーブヤング、ダイソーのバウチャーからビューティーケア施術まで` |
| **XLT 시스템 (Dapp Portal v2.6.1)** | **`最大15%還元！`** | **`オリヤン、ダイソーのバウチャーからビューティー施術まで`** |

→ **시스템 등록값 = 위키 v9**이고 **엑셀만 낡았다.** 사용자가 이미 Dapp Portal에 업로드했고 위키를 그 값으로 맞춘 상태였다. 엑셀을 그대로 재업로드하면 **등록된 정본을 구값으로 되돌리는 사고**가 된다.

**사용자 결정: 위키 v9(=시스템 등록값)를 정본으로 채택, 엑셀 재생성.** 아래 §2-1·§4에서 그 판정 근거를 남긴다.

---

## 1. 한국어 원문 교정 (게이트 1a — 필수)

**이번 패치는 ja_JP 단일 언어 교체이며 한국어(ko_KR)는 변경하지 않았다.** 그러나 게이트 규칙대로 대상 2키의 ko를 전수 재검토했다.

| 키 | ko_KR (현행 = 시스템 등록값 = 위키) | 검토 결과 |
|---|---|---|
| `kbeauty_to_kculture` | `최대 15% 캐시백!` | ✅ 교정 불요 — 맞춤법·띄어쓰기 정상, 비표준 공백 0건 |
| `all_korea_experience` | `올리브영, 다이소 바우처부터 뷰티케어 시술까지` | ✅ 교정 불요 — 선행 세션에서 `상품권 부터`→`바우처부터` 교정 완료분이 시스템·위키에 반영돼 있다(붙여쓰기 + 용어 통일) |

- **alias**: 이번 run에서 ko를 바꾸지 않았으므로 **신규 alias 없음**. 선행 세션의 alias(`상품권 부터`→`바우처부터` 계열)는 그 리포트에 기록돼 있다.
- 비표준 공백(`\xa0`·`​`·전각 공백·BOM·TAB) **0건**, 연속 공백 0건, `/n` 오타 0건 — 10셀 전수 코드 스캔.
- ⚠️ **Figma 원본은 여전히 구 문구다** — 프레임 `To-be`의 배너는 `From K-beauty to K-culture` / `All Things Korea, Unifi`(교체 전 화면)이고, 코멘트가 XLT 교체를 지시하는 구조다. 위키 XLT 표의 `현재 ko_KR | 변경 ko_KR (확정)` 2열 구조와 정합하므로 **원본 수정 요청 대상이 아니다**(의도된 As-is 화면).

---

## 2. 자동 검증 결과 (P0 / P1 / P2 요약)

```
python3 scripts/validate_translation.py \
  xlt/xlt_fix_guidekim_banner_2keys_20260824.xlsx \
  scripts/glossary.json
```

| 심각도 | 건수 | 내용 |
|---|---|---|
| 🔴 **P0 (Critical)** | **0건** | 빈칸·오타·언어 혼입·용어집 표기 위반 — 없음 |
| 🟡 P1 (Medium) | 3건 | 용어 불일치 3건 (en `최대` · zh `최대` · **ja `캐시백` — 이번 패치로 신규 발생**) → §4 |
| 🟢 P2 (Low) | 0건 | — |

자동 리포트: `xlt/xlt_fix_guidekim_banner_2keys_20260824_validation_report.md`

> ⚠️ `P0=0`은 통과가 아니라 시작점이다. 자동 검증기는 외래어 음차 오염·붙은 띄어쓰기 누락·다의어 모호·표현 어색을 구조적으로 못 잡으므로 §3 수동 3단계를 10셀 전수로 수행했다.

### 2-1. ⛔ 컬럼 정렬 검증 (단일 언어 패치 전 필수 — 예외 없음)

「ja만 바꾼다」고 ja diff만 보고 패치하면, 컬럼이 회전 어긋난 키의 **다른 언어 유일 원본이 소실**된다(`mini_guidekim_cat_eye`류 실측). 따라서 **대상 2키의 5개 언어 10셀 전부를 위키 정본과 대조**했다.

| 키 | ko_KR | ja_JP | en_US | th_TH | zh_TW |
|---|---|---|---|---|---|
| `kbeauty_to_kculture` | ✅ 동일 | ⚠️ 상이(패치 대상) | ✅ 동일 | ✅ 동일 | ✅ 동일 |
| `all_korea_experience` | ✅ 동일 | ⚠️ 상이(패치 대상) | ✅ 동일 | ✅ 동일 | ✅ 동일 |

- **컬럼 misalignment 0건** · **ja 외 예상 외 상이 0건** → 단일 언어 패치 진행 가능 판정.
- 각 언어 칸의 문자체계 적정성도 확인(ja 칸에 일본 문자 존재 · en 칸에 CJK/타이 문자 없음 · th 칸에 CJK 없음 · 비-ko 칸에 한글 없음).
- 적용은 `patch_translation.apply_translation_patch`(결정론적 — 지정 언어 셀만 교체, 나머지 보존)로 했고 **내장 컬럼 정렬 가드(1g)를 통과**했다(`allow_misaligned` 미사용).

**changelog (지정 언어만 교체)**

| 키 | 언어 | before | after |
|---|---|---|---|
| `mini_guidekim_kbeauty_to_kculture` | ja_JP | `最大15%キャッシュバック！` | **`最大15%還元！`** |
| `mini_guidekim_all_korea_experience` | ja_JP | `オリーブヤング、ダイソーのバウチャーからビューティーケア施術まで` | **`オリヤン、ダイソーのバウチャーからビューティー施術まで`** |

**보조 코드 스캔 (10셀 전수)**

| 검사 | 결과 |
|---|---|
| 비표준 공백·제어문자 · 선행/후행 공백 · 연속 공백 · `/n` | ✅ 0건 |
| 이질 문자체계 혼입 | ✅ 0건 |
| zh_TW 간체·이체자 | ✅ 0건(`優`·`惠`·`券`·`療`·`程`·`饋`·`現`·`從` 정자) |
| 치환자·태그 정합 | ✅ 전 키 0개(5개 언어 동일) |
| 개행 구조 | ✅ 전 키 0개(5개 언어 동일) |

---

## 3. 수동 3단계 체크표 (전수 — 2키 × 5개 언어 = 10셀 전부)

### 1단계: 한국어 맞춤법·띄어쓰기

| 점검 항목 | `kbeauty_to_kculture` | `all_korea_experience` |
|---|---|---|
| 조사·어미 띄어쓰기 | ✅ `최대 15% 캐시백!` 정상 | ✅ `바우처부터` 붙여쓰기(선행 교정 반영) |
| 단어 붙음 누락(`누리고다양한`류) | ✅ 없음 | ✅ 없음 |
| 외래어 음차 오염(`포이가츠`류) | ✅ 없음 | ✅ `뷰티케어`는 국내 통용 표기 |
| 오타·글자 누락 · 비표준 공백 | ✅ 없음 / 0건 | ✅ 없음 / 0건 |
| 브랜드 표기 | — | ✅ `올리브영`·`다이소` 국내 정식 브랜드명 |
| 다의어·모호 표현 | ✅ 없음 | ✅ 없음 |
| `deprecated_terms` 구 표기(14항목) | ✅ 미검출 | ✅ 미검출 |

### 2단계: 용어집 위반 (v4.7)

| 용어 | 용어집 등재값 | 적용 | 판정 |
|---|---|---|---|
| 캐시백 | en `cashback` / ja `キャッシュバック` / th `เงินคืน` / zh `現金回饋` | en·th·zh 등재값 일치 / **ja `還元`** | ⚠️ P1 1건 → §4 #3 (**등록값 정본 채택**) |
| 최대 | en `Max` / ja `最大` / th `สูงสุด` / zh `最多` | ja `最大`·th `สูงสุด` 일치 / en `Up to`·zh `最高` 문맥 의역 | ⚠️ P1 2건 → §4 #1·#2 (오탐 기각 — 선행 리포트 판정 유지) |
| 바우처 | **미등재** — 등록값 정본 `mini_guidekim_cat_voucher`: ja `バウチャー`/en `Voucher`/th `เวาเชอร์`/zh `優惠券` | ja `バウチャー`/en `vouchers`/th `เวาเชอร์`/zh `優惠券` | ✅ 정본 일치 |
| 시술 | **미등재** | ja `施術`/en `treatments`/th `ทรีตเมนต์`/zh `療程` | ✅ 등록값 계열 일치 |
| 예외 목록 · `deprecated_terms` | PIN·API·URL·Apple·Google·USDT·IDRP·JPYC / 팔로우·공식계정·友達 등 | 해당 없음 / 미검출 | ✅ |

**기존 등록값 일관성 대조 (2026-08-24 신규 실측 — 이번 패치의 핵심 근거)**

| 항목 | 실측 | 신규 ja 값의 정합 |
|---|---|---|
| ja `還元` 사용 키 (Dapp Portal) | **2건** — `mini_guidekim_cashback`(ko `캐시백 {0}%` → ja `還元{0}%`) · `mini_guidekim_kbeauty_to_kculture`(이번 키, **이미 등록됨**) | ✅ **`mini_guidekim_` 형제 내 `還元`이 다수(2건)** |
| ja `キャッシュバック` 사용 키 | 2건 — `UF_max_cashback`(UIT 키스페이스) · `mini_guidekim_cashback_curation` | `mini_guidekim_` 형제로는 1건뿐 |
| `オリーブヤング` | **0건** | — |
| `オリヤン` | **1건** — 이번 키(이미 등록됨) | ✅ 시스템에 확정된 유일 표기 |
| `ビューティーケア` | **0건** | — |
| `ビューティー` | **4건** — 이번 키 · `mini_guidekim_cat_beauty_exp` · `mini_luckyball_kbeauty_kculture` · `mini_luckyball_travel_desc` | ✅ `ビューティー施術`이 형제 계열과 정합 |

> **판정**: 사용자의 ja 축약은 **용어집 등재값에서는 벗어나지만 시스템 등록값·형제 키 선례 모두에 부합**한다. 오히려 선행 세션의 ja 값(`キャッシュバック`·`オリーブヤング`·`ビューティーケア`)이 형제 선례에서 벗어나 있었다(`オリーブヤング`·`ビューティーケア`는 실사용 0건). HANDOFF 전역 결정(2026-08-24, 「용어집 등재값과 등록값이 충돌하면 같은 키스페이스 형제 키를 정본으로」)과 동형 사례다.

### 3단계: 다른 언어 번역 검토

| 언어 | 점검 | 결과 |
|---|---|---|
| **ja_JP** *(변경 언어)* | 숫자+단위 붙임 · 나열 구분자 · 「から〜まで」 범위 · 전각 부호 · 브랜드 음차 · 길이 | ✅ `最大15%還元！` — 숫자+단위 붙임은 `md/check.md` B-7-1 정본, 전각 `！`. `オリヤン、…ビューティー施術まで` — 나열 `、`(ko 쉼표 대응), 범위 「から〜まで」 유지. **축약 효과**: 32자→26자로 배너 1행 안착성 개선. `オリヤン`은 일본에서 올리브영을 부르는 통칭이며 시스템 확정 표기 |
| **en_US** | 브랜드 대소문자 · 단복수 · 「From A to B」 | ✅ `OLIVE YOUNG`·`DAISO` 공식 대문자, `vouchers`·`treatments` 복수, 선례 `From essential vouchers to authenticated K-beauty treatments`와 동일 구조 (미변경) |
| **th_TH** | 의미 단위 띄어쓰기 · 숫자 앞 공백 · 범위 구조 | ✅ `เงินคืนสูงสุด 15%`는 `UF_max_cashback` th와 완전 일치. `ตั้งแต่ … ไปจนถึง …` 범위 구조 (미변경) |
| **zh_TW** | 번체 정자 · 라틴 주변 공백 · 나열 `、` · 전각 부호 | ✅ 이체자 없음, `最高享 15% 現金回饋！`·`從 OLIVE YOUNG、DAISO 優惠券到美容療程` (미변경) |
| **전 언어 공통** | 빈칸 · 언어 혼입 · placeholder · 태그 · 개행 · 컬럼 정렬 | ✅ 전부 정상 (§2-1) |

### 2차 독립 검토

**이번 패치에는 `translation-reviewer` 팬아웃을 쓰지 않았다.** 근거: ⓐ 변경분이 **ja 2셀**이고 그 값이 **이미 XLT 시스템에 등록된 정본**이므로 재판정 대상이 아니라 **정합화 대상**이다 ⓑ 나머지 8셀은 선행 세션에서 2차 독립 검토를 이미 통과했고(「5개 언어 전부 정확·실제 결함 0건」) 이번에 변경하지 않았다 ⓒ 판정의 핵심 근거는 의견이 아니라 **레지스트리 실측**(위 대조표)이며 그것을 직접 수행했다. 선행 리포트의 2차 검토 결과는 그대로 유효하다.

---

## 4. 검증기 보고 각 건의 처리 판정

| # | 등급 | 건 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | P1 | `kbeauty_to_kculture` `en_US`: '최대' → 'Max' 권장 | **오탐 — 기각(`Up to` 유지)** | 용어집 `Max`는 UI 라벨 단독 표기용. 문장 내 「최대 N%」는 en에서 `Up to N%`가 정본이고 등록값도 `Up to {{0}} cashback on payment`·`Up to ₩20,000 Off`로 일관. 선행 리포트 판정 유지 |
| 2 | P1 | `kbeauty_to_kculture` `zh_TW`: '최대' → '最多' 권장 | **오탐 — 기각(`最高` 유지)** | `最多`는 **개수**의 최다, 비율·금액에는 `最高`가 정본. 등록값 `消費最高享 {{0}} 現金回饋`와 일치. 선행 리포트 판정 유지 |
| 3 | P1 | `kbeauty_to_kculture` `ja_JP`: '캐시백' → 'キャッシュバック' 권장 | **정책 채택 — 등록값 정본 유지(`還元`)** ※ 이번 패치로 신규 발생 | ⓐ **이미 XLT 시스템(Dapp Portal v2.6.1)에 `最大15%還元！`로 등록**돼 있어 등재값을 따르면 정본을 되돌리게 된다 ⓑ `mini_guidekim_` **형제 키 내 `還元` 2건 vs `キャッシュバック` 1건**으로 형제 다수가 `還元`이며, 특히 같은 배너/배지 계열 `mini_guidekim_cashback`이 `還元{0}%`다 ⓒ 배너 메인 문구는 길이 제약이 커 `還元`(2자)이 `キャッシュバック`(8자)보다 적합 ⓓ HANDOFF 전역 결정(형제 키 우선)에 부합. → **§5 (d-1)에 용어집 분기 등재 권장으로 이관** |

**실제 위반: 0건 / 오탐: 2건(기각) / 정책 채택: 1건(등록값 정본)**
→ **P0 = 0건(자동 + 수동), 조치 필요 위반 0건.**

---

## 5. 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

> 선행 리포트(3판) §5에서 이미 제기·종결된 항목은 재제안하지 않는다. 아래는 **이번 run의 신규 실측**에서 나온 것과 **상태가 바뀐 것**만 남긴다.

### #1 🔴 최우선 — 두 서비스의 값이 갈렸다 (선행 리포트 경고가 현실화)

선행 리포트 §5 (d-2) #12는 「키 ①은 양쪽 등록되어 있어 **양쪽 업로드 필요** — 한쪽만 올리면 ja 불일치가 형태만 바뀐 채 남는다」고 경고했다. **Dapp Portal만 업로드돼 그 상태가 됐다.**

| 키 | Dapp Portal v2.6.1 | Unifi v1.6.7.1 | 상태 |
|---|---|---|---|
| `mini_guidekim_kbeauty_to_kculture` | ko `최대 15% 캐시백!` / ja `最大15%還元！` (**신 문구**) | ko `K-뷰티부터 K-컬처까지` / ja `韓国肌管理も、Kカルチャーも！` (**구 문구**) | 🔴 **ko·ja 모두 갈림** |
| `mini_guidekim_all_korea_experience` | ko `올리브영, 다이소 바우처부터…` (**신 문구**) | **미등록** | 🔴 한쪽만 존재 |

- **선행 리포트 §5 (d-2) #14**(「JP UX 피드백」 pageId=4611882417의 「같은 키인데 Dapp/Unifi ja 값 상이」 이슈가 해소된다)는 **해소되지 않았다** — ja가 여전히 다르고 이제 **ko까지 갈렸다.**
- → **Unifi 키스페이스에도 업로드할지 FE·기획 확인이 필요하다.** 화면이 Unifi 키스페이스를 참조하지 않는다면 Unifi의 구 문구를 정리(삭제 또는 동기화)할지 결정해야 한다. 쓰기 API 없음 → **업로드는 사용자 수행**.

### (d-1) 용어집 보완 권장 (v4.7 → 차기)

| 용어 | 권장 근거 | ko_KR | en_US | ja_JP | zh_TW | th_TH |
|---|---|---|---|---|---|---|
| **캐시백**(ja 분기) | 이번 run에 **P1 신규 오탐**이 발생했다. 등재값 ja `キャッシュバック` 단일 매핑이 **짧은 배너·배지 문맥의 `還元`을 못 덮는다.** 실측: `mini_guidekim_` 형제 내 `還元` 2건(배지 `還元{0}%` + 이번 배너) vs `キャッシュバック` 1건. v4.7 「포이카츠 zh 교정」·「지급 완료 등재」와 동형 — 등재값이 실사용과 어긋난 경우다 | 캐시백 | cashback | **キャッシュバック** *(짧은 배너·배지는 `還元`)* | 現金回饋 | เงินคืน |
| **올리브영** | 브랜드 표기 확정 필요. 실측: ja `オリヤン` 1건 / `オリーブヤング` **0건** — 시스템 확정 표기는 `オリヤン`(일본 통칭)이다. 선행 리포트는 `オリーブヤング`를 권장했으나 **실사용과 반대**였으므로 정정 등재 | 올리브영 | OLIVE YOUNG | **オリヤン** | OLIVE YOUNG | OLIVE YOUNG |
| **뷰티케어** | 실측: ja `ビューティーケア` **0건** / `ビューティー` 4건. `ビューティー施術`이 형제 계열(`cat_beauty_exp`·`luckyball_travel_desc`) 표기다. 선행 리포트의 `ビューティーケア` 권장은 실사용 0건이므로 정정 | 뷰티케어 | beauty care | **ビューティー** | 美容 | ความงาม |
| **다이소** | 선행 리포트 권장 유지(`ダイソー` 실사용 확인) | 다이소 | DAISO | ダイソー | DAISO | DAISO |
| ~~최대(문장형)~~ · ~~바우처~~ · ~~시술~~ | 선행 리포트 (d-1)에서 이미 권장 — **중복 제안 아님, 미해소 이월** | | | | | |

> 용어집은 **API 읽기 전용**이다. 승인 시 `md/landpress.md` 절차로 전체 JSON을 산출해 전달하고 사용자가 Landpress CMS에 붙여넣는다. **버전이 오르면 같은 작업에서 기획자 가이드 zip도 필수 동반 갱신**(§5-1).

### (d-2) 그 외 개선·확인 권장

| # | 항목 | 권장 내용 |
|---|---|---|
| **2** | ⚠️ **위키 History의 도구 진단이 틀렸다 (정정 필요)** | 위키 History 최신 행에 「`fetch_comments.py`가 이 파일에서 **roots=0 반환(버그)** — Figma REST 직접 조회로 폴백」이라고 적혀 있다. **이번 run에서 같은 파일·같은 프레임에 `fetch_threads`를 호출해 `roots=6`을 정상 수신했다.** 스크립트 버그가 아니라 호출 인자 문제였을 가능성이 크다(`node_ids`에 프레임 자손 집합을 넘겨야 하고, `collect_node_boxes`는 `(boxes, frame_bbox)` **튜플**을 반환하므로 언패킹이 필요하다). **잘못된 도구 진단이 위키에 남으면 다음 세션이 공용 페처를 우회해 인라인 재구현하게 된다**(`md/wiki.md`가 금지하는 안티패턴) → History 문구 정정 권장. **이번 편집에서는 이력 훼손을 피해 손대지 않았다** |
| **3** | **고아 첨부 2건 정리** | 본문에서 참조되지 않는 첨부가 있다 — `annotated_68044-20741.png`(563×1316) · `frame_68044-20708_tobe.png`(563×1220). 현재 본문은 `To-be.png`만 참조한다. 혼동을 줄이려면 삭제 권장이나 **첨부 삭제는 되돌릴 수 없어 사용자 확인 후에만** 수행한다(이번 run 미처리) |
| **4** | **Screen 표 첫 행의 Screen ID가 비어 있다** | 첫 행 Screen ID 셀이 `<br />`(공란)이고 두 번째 행은 `Reservation Case`(프레임명 기반)다. 이 페이지는 **프레임명 기반 구 방식**이라 소급 전환 금지 대상이지만, 첫 행은 아예 비어 있어 식별자가 없다. `md/IA.md` 어휘로 부여하려면 **매핑 표 사용자 승인**이 필요하다(이번 run 미처리) |
| **5** | **`My Reservation` 아이콘에 코멘트 없음** | 탭 4개 중 `K-Beauty`·`K-Shopping`·`K-Culture` 3개에는 이동 정책 코멘트가 있으나 `My Reservation`에는 없다. 의도적 누락인지(별 행 `Reservation Case`에서 다루므로) 디자이너 확인 권장 |
| **6** | **`15%` 변수화** | 선행 리포트 (d-2) #1과 동일 — `md/IA.md` 실측 「캐시백률은 고정값이 아니다」(환경별 상이). **미해소 이월**, PM 판단 사항 |

---

## 6. 게이트 통과 판정

| 기준 | 결과 |
|---|---|
| 한국어 원문 교정 선행 | ✅ 검토 완료 — 이번 패치는 ja 단일 언어이며 ko 교정 불요(§1), 신규 alias 없음 |
| 컬럼 정렬 검증(단일 언어 패치 필수) | ✅ 10셀 전부 위키 정본 대조 · misalignment 0건 · `patch_translation` 1g 가드 통과(§2-1) |
| 자동 검증 실행 | ✅ P0 0 / P1 3 / P2 0 + 보조 코드 스캔 5종 0건 |
| 수동 3단계 전수 검토 | ✅ **10셀 전부** 1·2·3단계 검토(§3) — 변경 언어로 한정하지 않음 |
| 2차 독립 검토 | ✅ 미실행 — 사유 명시(§3 말미). 변경분이 등록된 정본이고 나머지 8셀은 선행 세션 검토 통과분 |
| 각 건 처리 판정 | ✅ 3건 전건(§4) — 오탐 2 기각 / 정책 채택 1(등록값 정본) |
| (d) 추가 개선·제안 | ✅ 최우선 #1(서비스 간 값 분기) · (d-1) 4건 · (d-2) 5건 (§5) |
| **P0 = 0건 (자동 + 수동)** | ✅ **통과** |

→ **엑셀 재생성·위키 반영 가능.** ⚠️ **이 엑셀은 Dapp Portal에 이미 등록된 값과 동일하므로 재업로드가 불필요하다** — 기록·정합 목적이다. 업로드가 필요한 것은 **§5 #1의 Unifi 키스페이스**이며 FE 확인이 선행된다.

---

## 7. 위키 반영 (완료)

**대상**: [pageId 4637821463](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4637821463) · **v10 → v11**

| 변경 | 내용 |
|---|---|
| **Description 정책 3건 추가** | Figma 코멘트 **6건** 중 위키에 **3건만** 있어 `K-Beauty Tab으로 이동`·`K-Shopping Tab으로 이동`·`K-Culture Tab으로 이동`이 누락돼 있었다. 전부 추가 |
| **통합 번호 재정렬** | 기존(1·2=배너 문구, 3=My Shopping)이 Figma y좌표 순과 역순이었다. **사용자 결정으로 「y순 + 같은 행은 좌→우」** 채택 → **1**=My Shopping / **2·3**=배너 메인·서브(xlt) / **4**=K-Beauty / **5**=K-Shopping / **6**=K-Culture. 탭 3개는 y=407·412·416으로 9px 차이라 사실상 같은 행이며, 엄격 y순이면 화면 좌우 순서와 어긋난다 |
| **To-be 이미지를 어노테이션으로 교체** | `To-be.png`를 번호 ⓝ 버전으로 재첨부(**같은 파일명 유지 → 본문 링크 그대로 최신본**). 정책=빨강 4개 / xlt=파랑 2개 · 겹침 0 · 해상도 375→750(scale 2) · **렌더 후 육안 확인 완료**(6개 핀 전부 의도한 대상 위) |
| **다국어 표 ja 2건** | 이미 v9에 사용자가 반영해 둔 값이라 **변경 없음**(정본 확인만). 엑셀만 그 값으로 재생성해 정합화 |
| **XLT 엑셀 재첨부** | `xlt_fix_guidekim_banner_2keys_20260824.xlsx` — 같은 파일명으로 `POST .../child/attachment/{id}/data`로 갱신 |
| **History** | 같은 날짜(2026-08-24) 행이 이미 있어 **새 행을 만들지 않고 항목만 누적**. 기존 모든 항목 보존 |

**검사 결과**: `check_wiki_storage.py pre` **exit 0** → PUT → `post` **exit 0**. PUT 직전 **버전 가드** 통과.

> ⚠️ **편집 중 라이브 버전이 올랐다 (라이브 rebase 실측)** — 최초 조회 시 v9였으나 dry-run 시점에 **v10**이 돼 있었다. 사용자가 History에 행을 직접 추가한 것(`Unifi mini Home의 지갑 QR Code → My shoppping으로 변경 v1.7.0.2`)이며, **그 행을 보존 가드에 넣고 v10 기준으로 재작성**해 반영했다. 버전 가드가 없었으면 사용자 행을 덮어썼다.

> ⚠️ **가드 오탐 실측 (전역 금지 가드 ↔ 이력 기록 충돌 재발)** — 「다국어 표에 구 ja 값 금지」를 **전역**으로 걸었더니 **내가 History에 이력으로 인용한 `オリーブヤング…`** 를 잡아 중단됐다. HANDOFF 전역 컨텍스트·`md/wiki.md`(673행)에 이미 기록된 함정과 동일 사례다. 가드 범위를 **`<h2>다국어 번역` 이후 구간으로 한정**해 해결했다.

**보존 확인**: History 기존 행 · Related Docs · Schedule · Policy 표 · `Reservation Case` 행(Description·XLT 중첩표) · 다국어 표 · 후속 권장 문단 — 전부 가드 assert로 확인. §5 (d-2) #2(History 오진단)·#3(고아 첨부)·#4(Screen ID 공란)는 **판단·확인 사안이라 손대지 않았다**.

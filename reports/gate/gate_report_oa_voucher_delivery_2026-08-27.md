# 번역 품질 게이트 리포트 — OA 바우처 전송 메시지 (Flex, 변수형)

- **작업 유형**: **OA(LINE 공식계정) 메시지** — `md/OA.md` 적용. **XLT 키 미부여**(번역·변수·Flex JSON만 산출)
- **대상 위키**: [pageId 4639704548](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4639704548) `올리브영 5만원 상품상세 Unifi 직접제공` · Jira `UNIFY-10468` · **OA 섹션 신규 추가**
- **메시지**: Unifi에서 바우처 구매 완료 시 **LINE OA로 바우처 전송**하는 Flex 메시지
- **프레임 식별자**: `OA_voucher_delivery` (Figma 프레임 미지정 — 산출물 명명용)
- **용어집**: v4.7 · `oa_variables` 2건 등재(`{{total_amount}}`·`{{wallet_address}}`) — API 신규 조회
- **등록값 조회**: `Unifi / WEB BROWSER / v1.7.2`(2,347키) — 이번 run 신규 조회
- **검토 범위**: **전수 점검** — 4문구 × 5개 언어 = **20셀 전건**
- **산출물**: `oa/flex_OA_voucher_delivery_{ko_KR,ja_JP,en_US,th_TH,zh_TW}.json` 5개 + `oa/flex_OA_voucher_delivery_5lang.zip`

> **개정 이력 (2판, 2026-08-27)** — 변수명이 두 차례 바뀌어 최종본은 **`{{product_image_url}}`·`{{product_guidekim_url}}`**이다.
> ① 초판 `{{voucher_image_url}}`·`{{voucher_detail_url}}` → ② `{{voucher_detail_url}}`을 `{{voucher_guidekim_url}}`로 변경(사용자) →
> ③ **`voucher_` 접두를 전부 `product_`로 통일**(사용자). 같은 상품 이미지 변수를 찜하기 OA와 공유하기 위한 정리다.
> 찜하기 OA는 별도 리포트 `gate_report_oa_product_link_saved_2026-08-27.md` 참조. **번역 문구는 변경 없음**(변수명만 변경).

---

## 0. 사용자 결정 3건

| # | 결정 | 내용 |
|---|---|---|
| 1 | **안내 문구 개선** | 「구매 완료 + 즉시 사용」 방향 채택 (§1) |
| 2 | **변수명** | Hero = **`{{product_image_url}}`** · 버튼 URL = **`{{product_guidekim_url}}`** *(2판 최종 — 위 개정 이력)* |
| 3 | **목업 이미지** | 이번 작업에서는 **이미지 없이 진행** — 대화 첨부 이미지는 파일로 접근할 수 없고 로컬(Downloads·Desktop)에도 없었다 |

---

## 1. 한국어 원문 교정 · 문구 개선 (게이트 1a — 필수)

### 개선 전후

| 위치 | 기존(목업) | 확정 |
|---|---|---|
| 타이틀 | *(없음)* | **구매가 완료되었어요!** |
| 본문 | Unifi에서 구매한 바우처를 확인하세요. | **바우처를 확인하고 바로 사용해 보세요.** |
| 버튼 | Unifi에서 확인하기 | **Unifi에서 확인하기** *(유지)* |
| altText | *(미정의)* | **구매하신 상품이 도착했어요.** *(2026-08-27 정정 — `바우처`→`상품`, 등록값 표준)* |

### 개선 근거

| 문제 | 개선 |
|---|---|
| **구매 완료 사실을 알리지 않음** — 구매 직후 발송되는 첫 접점인데 「확인하세요」만 있어 상태 정보가 없다 | 타이틀에 완료 상태를 명시. Flex body는 「타이틀 + 본문」 2단 구조가 정본(`md/OA.md` 동작 검증 구조)이라 구조상으로도 타이틀이 필요했다 |
| **`Unifi에서` 중복** — 본문과 버튼 라벨에 모두 등장 | 본문에서 제거. 이동 맥락은 버튼이 담당한다 |
| **행동 동기 없음** | 「바로 사용해 보세요」로 다음 행동을 제시 |
| **Hero 이미지와 정보 중복 우려** | Hero가 상품명·금액을 이미 보여주므로 본문은 **상태 + 행동**에만 집중 |

- **타이틀은 기존 확정 문구를 그대로 재사용**했다 — 위키 `4637821100` v3의 `mini_guidekim_kpick_oa_consent_purchase_complete`(`구매가 완료되었어요!` 5개 언어)와 **동일 문구·동일 번역**이다. 같은 서비스에서 같은 상태를 알리는 문구가 화면과 OA에서 갈리지 않게 했다(`md/OA.md` 규칙 1 「같은 문구는 같은 번역을 재사용」).
- 확정 한국어에 맞춤법·띄어쓰기 문제 없음. 비표준 공백(NBSP·ZWSP·전각·BOM·TAB) **0건**, 연속 공백 0건.
- **alias**: Figma 추출이 아니라 목업 이미지 + 사용자 요청 기반이므로 alias 대상이 아니다. 위 개선 전후 표가 그 기록이다.

---

## 2. 자동 검증 결과 (P0 / P1 / P2 요약)

```
python3 scripts/validate_translation.py /tmp/oa_validate.xlsx scripts/glossary.json
```

> OA는 XLT 키를 부여하지 않으므로(`md/OA.md` 규칙 1) **검증용 임시 식별자**(`oa_title`·`oa_body`·`oa_btn`·`oa_alttext`)로 엑셀을 만들어 검증만 수행하고, **XLT 업로드 엑셀·전역 키 표에는 넣지 않았다.**

| 심각도 | 건수 | 내용 |
|---|---|---|
| 🔴 **P0 (Critical)** | **0건** | 빈칸·오타·언어 혼입·용어집 표기 위반 — 없음 |
| 🟡 P1 (Medium) | 5건 | 용어 불일치 — **전건 오탐 판정**(§4) |
| 🟢 P2 (Low) | 2건 | 마침표 스타일 — 오탐(§4) |

**보조 코드 스캔 (20셀 전수)**

| 검사 | 결과 |
|---|---|
| 비표준 공백·연속 공백·선후행 공백 | ✅ 0건 |
| 비-ko 칸 한글 잔존 | ✅ 0건 |
| zh_TW 간체 | ✅ **0건** — 스캔이 2건을 잡았으나 **전부 `惠` 오탐**(`優惠券`의 번체 정자). 검사 집합에 `惠`를 넣은 내 오류다(이번 세션 2회째 같은 오탐) |
| **altText 변수 미포함** | ✅ 확인 — `md/OA.md` 규칙 2-1 「altText에는 변수 사용 불가」 준수 |

---

## 3. 수동 3단계 체크표 (전수 — 4문구 × 5개 언어 = 20셀)

### 1단계: 한국어

| 점검 항목 | 결과 |
|---|---|
| 맞춤법·띄어쓰기 | ✅ 문제 없음 |
| 종결 어미 일관성 | ✅ 해요체 통일(`완료되었어요!`·`사용해 보세요.`·`도착했어요.`) |
| 보조용언 띄어쓰기 | ✅ `사용해 보세요` — 보조용언은 띄어 씀이 원칙(붙여 씀도 허용) |
| 비표준 공백·오타 | ✅ 0건 |
| `deprecated_terms`(14항목) | ✅ 미검출 — 특히 `공식계정`(구 표기) 미사용 |

### 2단계: 용어집 위반 (v4.7)

| 용어 | 등재값 | 적용 | 판정 |
|---|---|---|---|
| 바우처 | 미등재 — 등록값 정본 `mini_guidekim_cat_voucher` | ja `バウチャー` / en `voucher` / th `เวาเชอร์` / zh `優惠券` | ✅ 정본 일치 |
| 구매 | en `Buy` / ja `購入` / th `ซื้อ` / zh `購買` | ja `ご購入`·zh `購買` 일치 / en·th는 문맥 의역 | ⚠️ P1 3건 → §4 |
| 완료 | en `Complete` / ja `完了` / th `เสร็จ` / zh `完成` | ja `完了`·en `complete`·zh `完成` 일치 / th `สำเร็จแล้ว` | ⚠️ P1 1건 → §4 |
| 확인 | en `check` / ja `確認` / th `ตรวจสอบ` / zh `查看` | 전 언어 등재값 계열 | ⚠️ P1 1건(zh) → §4 |
| 예외 목록 | PIN·API·URL·Apple·Google·USDT·IDRP·JPYC | `Unifi`·`LINE` 브랜드 원어 유지 | ✅ |

**등록값 선례 실측 (2026-08-27 · 2,347키 전수)**

| 항목 | 실측 | 적용 값의 정합 |
|---|---|---|
| ko `확인` → zh 표기 | **`確認` 84건** vs `查看` 57건 vs 기타 24건 | 본문 `確認`은 **다수 표기**(§4 #3) |
| `~확인하기`(이동·열람) → zh | `UF_bridge_promotion_tootip` ko `지원하는 네트워크 확인하기!` → zh `查看支援的網路！` | 버튼 `前往 Unifi 查看`이 이 계열과 정합 |
| `X 확인하기` 버튼 어형 | `UF_jpycex_deposit_complete_check` ja `入金情報を確認する` / en `Check Deposit Information` / th `ตรวจสอบข้อมูลการฝาก` / zh `查看入帳資訊` | ✅ 버튼 4언어가 이 어형과 동일 구조 |
| 타이틀 재사용 원본 | 위키 `4637821100` v3 `구매가 완료되었어요!` 5개 언어 | ✅ 5/5 셀 그대로 재사용 |

### 3단계: 다른 언어 번역 검토

| 언어 | 점검 | 결과 |
|---|---|---|
| **ja_JP** | 겸양·정중체 · 전각 부호 · 버튼 어형 | ✅ `ご購入`·`ご利用ください` 겸양 적정. 전각 `！`·`。`. 버튼 `Unifiで確認する`는 선례 `～を確認する`와 동일 어형 |
| **en_US** | 버튼 간결성 · 관사 · 자연스러움 | ✅ `Purchase complete!`(명사구 관용), `Check your voucher and start using it right away.`, 버튼 `Check on Unifi`(전치사 `on`이 앱·플랫폼에 적합) |
| **th_TH** | 문장 끝 마침표 미사용 · 어순 · 차용어 | ✅ 4셀 전부 마침표 없음(관례 준수), `เวาเชอร์` 정본, 버튼 `ตรวจสอบใน Unifi` 어순 정상 |
| **zh_TW** | 번체 정자 · 라틴 주변 공백 · 전각 부호 | ✅ 정자 확인(`購`·`優`·`惠`·`確`·`認`·`達`), 간체 0건. `前往 Unifi 查看` 라틴 앞뒤 공백. 전각 `！`·`。` |
| **전 언어 공통** | 변수 · 구조 · 길이 | ✅ 본문 텍스트에 변수 0개(URL 필드에만 변수) · altText 변수 0개 · 버튼 라벨 길이 적정(ko 12·ja 10·en 14·th 17·zh 15자) |

### 2차 독립 검토

**팬아웃하지 않았다.** 근거: ⓐ 대상이 **4문구 20셀**로 작고 ⓑ 타이틀 5셀은 **기존 확정 문구 재사용**(이미 2차 검토 통과분)이라 실질 신규는 15셀이며 ⓒ 판정의 핵심 근거가 의견이 아니라 **등록값 2,347키 실측**(위 대조표)이고 그것을 직접 수행했다. 다만 OA 문구는 마케팅 톤이 결과에 영향을 주므로 §5 #2에 검수 권장을 남긴다.

---

## 4. 각 건의 처리 판정

| # | 등급 | 건 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | P1 | `oa_title` en: '구매' → 'Buy' | **오탐 — 기각(`Purchase complete!`)** | 용어집 `Buy`는 CTA 라벨용 단일 매핑. 명사구 「구매 완료」의 en 정본은 `Purchase`다(등록값 `Drops Purchase Complete`·`Purchase Successful`). 선행 리포트에서 동일 판정 확립 |
| 2 | P1 | `oa_title` th: '완료' → 'เสร็จ' | **오탐 — 기각(`ซื้อสำเร็จแล้ว!`)** | 「구매+완료」 th 선례가 정확히 `ซื้อสำเร็จ` 2건. `เสร็จ`는 라벨형 등재값. 선행 리포트 판정 유지 |
| 3 | P1 | `oa_body` zh: '확인' → '查看' | **오탐 — 기각(`確認優惠券後即可立即使用。` 유지)** | ⚠️ **처음엔 실제 위반으로 판단했으나 실측이 뒤집었다** — ko `확인`이 든 키의 zh 표기는 **`確認` 84건 vs `查看` 57건으로 `確認`이 다수**다. 본문의 「내용을 확인한다」는 `確認`, 버튼의 「이동해 열람한다」는 `查看`(선례 `查看支援的網路！`)로 **의미에 따라 나눠 쓰는 것이 등록값 관행**이다 → 본문↔버튼 표기 차이는 §5 #3에 확인 사항으로 남긴다 |
| 4 | P1 | `oa_alttext` en: '구매' → 'Buy' | **오탐 — 기각** | altText는 푸시 봉투 텍스트라 간결성이 최우선이다. `Your voucher has arrived.`는 「구매」를 명시하지 않는 의역이며, 알림 목록에서 한 줄로 읽히는 것이 목적이다 |
| 5 | P1 | `oa_alttext` th: '구매' → 'ซื้อ' | **오탐 — 기각** | #4와 동일 사유(`เวาเชอร์ของคุณมาถึงแล้ว`) |
| 6 | P2 2건 | `oa_title`·`oa_btn` 마침표 없음 | **오탐 — 기각** | 타이틀은 감탄부호 `!`로 종결, 버튼은 **라벨**이라 마침표를 붙이지 않는 것이 정본 |

**실제 위반: 0건 / 오탐: 7건(전건 기각)**
→ **P0 = 0건(자동 + 수동).**

---

## 5. 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

### #1 ⚠️ 최우선 — `md/OA.md` 규칙과 이번 요청의 충돌 (규칙 보강 필요)

`md/OA.md` 규칙 3은 **「URL 계열 필드에 플레이스홀더가 남으면 Flex Simulator·실발송 모두 렌더 거부되므로 최종 첨부본은 실값 URL 필수」** 라고 못박고 있다. 그런데 이번 요청은 **Hero 이미지와 버튼 URL을 변수 처리**하는 것이다.

**충돌이 아니라 유형이 다르다** — 그 규칙은 **캠페인 확정형**(모든 수신자에게 같은 이미지·같은 링크)을 전제로 쓰였다. 이번 메시지는 **주문별로 상품 이미지와 바우처 상세 링크가 달라지는 동적 템플릿**이라 실값을 넣을 수 없다. Messaging API 발송 시 서버가 치환하는 것이 정상 동작이다.

- **따라서 사용자 요청대로 변수를 유지했다.** 단 그 결과:
  - ⚠️ **Flex Message Simulator에서 이 JSON은 그대로 렌더되지 않는다**(`{{product_image_url}}`이 https가 아니므로). 시뮬레이터로 확인하려면 **변수 자리에 샘플 URL을 임시로 넣어야** 한다.
  - 발송 시스템이 `{{ }}` 치환을 지원하는지 **FE·서버 확인 필요**. LINE Messaging API 자체에는 이 치환 기능이 없으므로 **발송 전 애플리케이션 레벨에서 문자열 치환**이 전제된다.
- → **`md/OA.md` 규칙 3에 「동적 템플릿형」 예외를 명시할 것을 권장**한다. 현재 문구대로면 다음 세션이 이 산출물을 「플레이스홀더 잔존 = 규칙 위반」으로 오판해 실값으로 바꿔버릴 수 있다.

### (d-1) 용어집 보완 권장 — `oa_variables` 2건 신규 등재

| 변수 | 의미 | 예시 | 비고 |
|---|---|---|---|
| **`{{product_image_url}}`** | OA로 전송하는 바우처 상품 이미지 URL(Hero) | `https://…/oliveyoung_50000.png` | 주문 상품에 따라 달라진다. **Flex `hero.url`에 들어가므로 치환 후 https 실값이어야 한다** |
| **`{{product_guidekim_url}}`** | 구매한 바우처 상세로 이동하는 링크(버튼 action) | `https://unifi…/voucher/{id}` | 주문·사용자별로 달라진다. **Flex `action.uri`에 들어간다** |

기존 등재 2건(`{{total_amount}}`·`{{wallet_address}}`)과 같은 snake_case 의미명 규칙을 따랐다. 승인 시 `md/landpress.md` 절차로 전체 JSON을 산출해 전달하고, 버전이 오르면 **기획자 가이드 zip도 필수 동반 갱신**한다.

### (d-2) 그 외 개선·확인 권장

| # | 항목 | 권장 내용 |
|---|---|---|
| **2** | **OA 문구 톤 검수** | 마케팅 메시지라 톤이 전환율에 영향을 준다. 특히 ja(`すぐにご利用ください`)는 정중하지만 다소 사무적이라 `今すぐお使いいただけます`처럼 부드럽게 하는 선택지도 있다. **네이티브 검수 권장** |
| **3** | **zh 본문 `確認` ↔ 버튼 `查看` 표기 차이** | 등록값 관행상 의미에 따라 나눠 쓰는 것이 맞다고 판정했으나(§4 #3), 한 메시지 안에서 갈리는 것이 눈에 띌 수 있다. 통일한다면 **둘 다 `查看`** 쪽을 권한다(사용자 동작이 「열어본다」이므로) |
| **4** | **목업 이미지 미첨부** | 사용자 결정으로 이번엔 진행하지 않았다. Figma에 OA 프레임이 만들어지면 어노테이션 후 첨부하면 된다 — 그때 Screen 표의 이미지 칸을 채운다 |
| **5** | **Screen ID 미부여** | OA 행의 Screen ID를 비워 두었다. `md/IA.md` 어휘로 부여하려면 **매핑 표 사용자 승인**이 필요하다(예: `kpick_voucher_oa_send_01`). 이 페이지의 다른 행은 **프레임명 기반**이라 소급 전환 금지 대상이기도 하다 |
| **6** | **altText 길이** | LINE 알림 미리보기는 기기별로 잘리는 길이가 다르다. 현재 ko 17자·ja 18자·en 26자로 짧아 안전하나, 상품명을 넣자는 요구가 나오면 **변수를 쓸 수 없다는 제약**(`md/OA.md` 규칙 2-1)을 먼저 확인해야 한다 |
| **7** | **친구 추가 전 사용자 처리** | 이 메시지는 **Unifi OA 친구 추가가 된 사용자**에게만 도달한다. 미추가 사용자의 대체 동선(앱 내 알림 등)이 정의돼 있는지 확인 권장 — 같은 페이지 Screen 표에 「찜하기/결제 시 친구추가」 화면이 있어 연결 지점이 있다 |

---

## 6. 게이트 통과 판정

| 기준 | 결과 |
|---|---|
| 한국어 원문 교정·문구 개선 | ✅ 개선 전후 기록(§1) · 타이틀은 기존 확정 문구 재사용 |
| 자동 검증 실행 | ✅ P0 0 / P1 5 / P2 2 + 보조 코드 스캔 4종(altText 변수 금지 포함) |
| 수동 3단계 전수 검토 | ✅ **20셀 전건**(§3) + 등록값 선례 실측 4항목 |
| 2차 독립 검토 | ✅ 미실행 — 사유 명시(§3 말미) |
| 각 건 처리 판정 | ✅ 7건 전건 오탐 기각(§4) — 특히 #3은 **초기 판단을 실측으로 뒤집어** 근거를 남김 |
| (d) 추가 개선·제안 | ✅ 최우선 #1(OA.md 규칙 충돌) · (d-1) 변수 2건 등재 권장 · (d-2) 6건(§5) |
| **P0 = 0건 (자동 + 수동)** | ✅ **통과** |

---

## 7. 산출물 · 위키 반영

### Flex JSON (언어별 5개 + zip)

```
oa/flex_OA_voucher_delivery_ko_KR.json
oa/flex_OA_voucher_delivery_ja_JP.json
oa/flex_OA_voucher_delivery_en_US.json
oa/flex_OA_voucher_delivery_th_TH.json
oa/flex_OA_voucher_delivery_zh_TW.json
oa/flex_OA_voucher_delivery_5lang.zip   ← 위키 첨부
```

**구조**(`md/OA.md` 동작 검증 구조 준수): `hero`(image · `size:full` · `aspectRatio:20:13` · `aspectMode:cover` · action 없음) + `body`(타이틀 text + 하위 box의 본문 text) + `footer`(주 버튼 `style:primary` · `color:#000000` · `height:sm`). **빈 `contents:[]` box 없음.**

**검증**: 5파일 전부 ⓐ JSON 파싱 정상 ⓑ `type:bubble` ⓒ `hero.url` = `{{product_image_url}}` ⓓ `action.uri` = `{{product_guidekim_url}}` ⓔ 언어 혼입 0 ⓕ **zip 라운드트립 재검증**(압축 해제 후 5파일 전수 재확인 — `md/OA.md` 3-1 ⓒ).

### 위키

OA 서브섹션(`<h4>OA</h4>`)을 Screen 섹션 아래 **신규 추가**하고, 키 없는 번역표(`No | KR | JA | EN | TH | ZH-TW`)·변수 정의·Flex zip 다운로드 링크를 넣었다. **XLT 엑셀·전역 「다국어 번역」 키 표는 건드리지 않았다**(`md/OA.md` 규칙 1).

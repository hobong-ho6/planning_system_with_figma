# 게이트 리포트 — shopping_guide CU 5개 언어 (2026-09-12)

- **대상**: LV `shopping_guide.guide_page` · `uid = CU` — CU 편의점 가이드 페이지
- **구성**: `title` · `description` · `cta` · `voucherSection` · `heroImageUrl` · `sections` **1개**(항목 5)
- **작업**: 이미지 6개(히어로 1 + 상품 5) 반영 · ko_KR 기준 4개 언어 번역 · CMS API로 5개 언어 등록·공개
- **검증 범위**: **전체 행 19건 전수 점검**(비번역 필드 제외). 이 필드의 번역 대상 전부다.
- **엑셀**: `xlt_output_20260912154214.xlsx`

---

## (1a) 한국어 원문 교정

교정 사항 **없음**. LV beta 실등록 ko_KR을 그대로 기준으로 삼았고 alias도 없다.

| 확인 항목 | 판정 |
|---|---|
| 맞춤법·띄어쓰기 | 정상 |
| `description`과 `summary.description`이 동일 문구 | 원문 그대로 유지(중복은 원문 설계) |
| `heroImageUrl` | **채움** — Figma `72959-1214` 실측상 CU에는 히어로 이미지(300×200)가 있고, 미디어 라이브러리에 `cu_guide_00_hero_대표이미지.png`가 있다 (다이소는 히어로가 없어 빈 문자열이 정상) |

---

## (a) 자동 검증 결과

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | 8건 |
| 🟢 P2 | 1건 |

**통과 기준 P0 = 0 충족.**

---

## (b) 수동 3단계 체크표 — 전체 행 19건 전수

### 1단계 · 한국어(ko_KR)

| 영역 | 행 | 판정 |
|---|---|---|
| `title`·`description`·`cta`(2)·`voucherSection.title` | 5 | 정확 |
| `sections[0]` 제목·주의·요약 2 | 4 | 정확 |
| 항목 5 × (name·caption) | 10 | 정확 — `스트링/슬라이스 치즈`의 `/` 구분자 원문 유지 |

붙은 띄어쓰기·외래어 음차 오염·다의어 모호 **없음**.

### 2단계 · 용어집(v5.4) 대조

| 용어 | 정본 | 적용 | 판정 |
|---|---|---|---|
| 바우처 | ja `クーポン` · en `Voucher` · th `เวาเชอร์` · zh `優惠券` | `voucherSection.title` 전 언어 정본 적용 | 정확 |
| 최대 | ja `最大` · th `สูงสุด` | ja·th 정본 / en `Up to` · zh `最高` | (c) 오탐 |
| 구매 · 구매하기 | ja `購入` · zh `購買` | 적용 | 정확 |
| 인기 | zh `人氣` | zh `人氣` 적용 / en·th 의역 | (c) 오탐 |
| 상품명·브랜드 | 미등재 | 브랜드 원형 유지(Binggrae · Samyang), 제품 종류어는 현지어 | (d-1) |

### 3단계 · 언어별 점검

| 언어 | 점검 | 판정 |
|---|---|---|
| ja_JP | 카타카나 표기 일관(ビングレ · サムヤン · ブルダック) · 전각 슬래시 `／` 사용 | 정확 |
| en_US | 브랜드 원형 · em dash 사용 일관 · 문장부호 정상 | 정확 |
| th_TH | 브랜드 로마자 + 제품 종류어 태국어(다이소와 동일 원칙) · 구절 띄어쓰기 정상 | 정확 |
| zh_TW | **번체** 유지(`優惠券`·`便利商店`·`起司`) · 간체 0 · 전각 슬래시 `／` | 정확 |
| 전 언어 | 줄바꿈 `\n` 위치 ko와 동일(제목 1곳) · `imageUrl` 5개·`heroImageUrl` 1개 5개 언어 모두 동일 · 금액 `₩7,800` 유지 | 정확 |

---

## (c) 건별 처리 판정

### P0 — 0건

### P1 8건 — 전부 **오탐**

| 유형 | 건수 | 판정 |
|---|---|---|
| en/zh `최대` → `Max`/`最多` | 2 | **오탐** — 할인율에는 `Up to`·`最高`가 표준 |
| en/ja `구매하기` → `Buy now`/`購入する` | 2 | **오탐** — CTA 문장형이 자연스럽다 |
| en `구매` → `Buy` | 1 | **오탐** — `About ₩7,800 for all of them` 의역 |
| en/th `인기` → `popular`/`ยอดนิยม` | 3 | **오탐** — `A top pick`·`The viral pairing`·`คอมโบยอดฮิต` 의역. th는 `ยอดนิยม`을 1건에서 실제 사용 |

### P2 1건

`caution` 마침표 지적 — **오탐**. 주의 문구는 완결 문장이라 마침표가 정상.

---

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

### (d-1) 용어집 보완

`할인` · `최대`(할인율 문맥의 zh `最高`) · `상품권`이 미등재이거나 문맥 표기가 부족해 다이소·CU 양쪽에서 **반복 오탐**을 낸다. 전체 번역 완료 후 일괄 등재하며 권장 표기는 실사용 실측 후 제출한다.

### (d-2) 추가 개선 제안

1. **`cta.targetUrl` · `voucherSection.productIds` 미정** — 둘 다 비어 있다
2. **`description` 중복** — 최상위 `description`과 `sections[0].summary.description`이 같은 문구다. 화면에서 두 번 보이는지 확인 권장
3. **올리브영 미처리** — 이미지 0/9 · 히어로 없음 · ko_KR만 존재

---

## 결론

- **P0 = 0건**(자동 + 수동) → 산출물 출력·등록 가능
- P1 8건 · P2 1건 전부 오탐 판정, 조치 없음
- (d) 권장 사항은 사용자 결정 전까지 미적용

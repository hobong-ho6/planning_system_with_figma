# 번역 품질 게이트 리포트 — Category 공통 헤더 타이틀 (unifi-mini-v2)

- **작업일**: 2026-09-15
- **대상**: Figma `Web3` / `[홈]` 섹션 `72193:5106` — `카테고리 1`(`72193:8126`) · `카테고리 2`(`72193:8274`)에 신설된 `xlt` 마커 ①
- **담당 FE 팀**: **LV** (이 페이지 Category 영역의 기존 키가 `mini_` — `mini_common_all`·`mini_category_item_count`)
- **키스페이스**: Dapp Portal / WEB BROWSER
- **산출물**: `xlt/xlt_output_20260915130220.xlsx` (신규 키 1건)
- **검증 범위**: **전체 행 전수 점검** — 이 엑셀의 **전체 파일 1키 × 5개 언어 = 5셀 전건**을 수동 검토했다.

---

## 대상 키 (신규 1건)

| XLT Key | ko_KR | ja_JP | en_US | th_TH | zh_TW |
|---|---|---|---|---|---|
| `mini_common_header_title` | Unifi \| 더 스마트한 한국 여행 | Unifi \| もっとスマートな韓国旅行 | Unifi \| A smarter Korea trip | Unifi \| เที่ยวเกาหลีอย่างสมาร์ทกว่าเดิม | Unifi \| 更聰明的韓國旅行 |

### 키 이름·범위 판정 근거

- 이 문구는 **LINE MINI App 헤더 타이틀**이고, 섹션 `[홈]`의 **7개 프레임 전부**(`카테고리 1`·`카테고리 2`·`홈탭1`·`홈탭2` 외 3)에 동일 텍스트로 존재한다 → **화면 전용이 아니라 공통 키**로 정의했다.
- 프리픽스 선례 실측(Dapp Portal 1,694키): `mini_` **237건**. 공통 키 선례는 `mini_common_all` **1건**뿐이라 그 명명을 이어 `mini_common_header_title`로 정했다.
- 유사 등록값 실측: `fetch_xlt_registry.py --similar` 임계 0.55에서 **0건**, 위키 본문에도 **0건** → 신규 대상 확정.

---

## (1a) 한국어 원문 교정 — 번역 전 수행

| Figma 원문 | XLT Key 값 | 사유 |
|---|---|---|
| `Unifi \| 더 스마트한 한국여행` | `Unifi \| 더 스마트한 한국 여행` | **`한국여행` 붙여쓰기 → `한국 여행`**. 등록값 실측에서 ko 「한국 여행」이 **13키 전건 띄어쓰기**이며, 같은 화면군의 `mini_guidekim_travel_voucher`(「한국 여행 필수 바우처」)와 표기가 갈리는 것을 막는다 |

**alias 기록**: `{Figma: Unifi | 더 스마트한 한국여행 → XLT: Unifi | 더 스마트한 한국 여행}`

구분자 `|`는 Figma 원문의 반각 파이프를 그대로 유지했다(전각 변환하지 않음). 브랜드명 `Unifi`는 용어집 exceptions #9(brand)에 따라 전 언어 원형 유지.

---

## (a) 자동 검증 요약 — `validate_translation.py`

```
python3 scripts/validate_translation.py xlt/xlt_output_20260915130220.xlsx scripts/glossary.json
```

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

**통과 기준 P0 = 0 충족** (자동·수동 모두).

---

## (b) 수동 3단계 체크표 — 전체 행 전수 (1키 × 5언어)

### 1단계 — 한국어(ko_KR) 원문 검토

| 검토 항목 | 판정 |
|---|---|
| 맞춤법·띄어쓰기 | 「한국여행」 붙여쓰기 1건 발견 → (1a)에서 교정 후 정상 |
| 표기 일관성 | 「한국 여행」이 등록값 13키와 일치 ✅ |
| 브랜드 표기 | `Unifi` 대문자 U + 소문자 — 용어집 exceptions #9와 일치 ✅ |

### 2단계 — 용어집 대조

용어집 v5.5에 「스마트」·「여행」·「한국」은 **미등재**(null)라 terminology 대조 대상이 없다. 등재 대신 **등록값 실측 다수 표기**를 기준으로 판정했다:

| 언어 | 실측 다수 | 적용 | 판정 |
|---|---|---|---|
| ja_JP | `韓国旅行` **13건** (`渡韓` 0건 · `韓国への旅` 0건) | `韓国旅行` | ✅ 다수 일치 |
| zh_TW | `韓國旅行` **10건** (`韓國之旅` 0건) | `韓國旅行` | ✅ 다수 일치 |
| th_TH | `เที่ยวเกาหลี` **8건** : `ทริปเกาหลี` 5건 | `เที่ยวเกาหลี` | ✅ 다수 일치 |
| en_US | `Korea trip` **4건** : `trip to Korea` 3건 | `Korea trip` | ✅ 다수 일치(근소) |

### 3단계 — 다국어 의미·표현 검토 (전 5셀)

| 언어 | 값 | 검토 | 판정 |
|---|---|---|---|
| ko_KR | Unifi \| 더 스마트한 한국 여행 | (1단계 참조) | ✅ |
| ja_JP | Unifi \| もっとスマートな韓国旅行 | 「더」=`もっと` 비교급 정확 · `スマート` 가타카나 차용어는 ja 관례 · 구분자 앞뒤 반각 공백 유지 | ✅ |
| en_US | Unifi \| A smarter Korea trip | 비교급 `smarter` 정확 · 관사 `A` 적절 · 40자 이내 | ✅ |
| th_TH | Unifi \| เที่ยวเกาหลีอย่างสมาร์ทกว่าเดิม | 「~하게 여행하다」 구조(`อย่าง` + 형용사) 자연스럽고 `กว่าเดิม`로 비교급 표현 · `สมาร์ท`는 th에서 통용되는 차용어 | ✅ |
| zh_TW | Unifi \| 更聰明的韓國旅行 | 번체 정자 · `更`로 비교급 · 한자↔한자 경계라 **추가 공백 불필요**(2026-09-14 zh 공백 규칙은 한자↔숫자·변수 경계 한정) | ✅ |

**언어 혼입·빈칸·placeholder**: 전 5셀 이상 없음(변수 없는 문구라 placeholder 검사 대상 없음). 브랜드명 `Unifi`가 각 언어 칸에 라틴 문자로 들어가지만 **exceptions 등재 브랜드**라 혼입이 아니다.

---

## (c) P1/P2 각 건 처리 판정

**자동·수동 모두 검출 0건.** 판정할 항목이 없다(오탐·실제 위반·정책 보류 각 0건).

---

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

### (d-1) 용어집 보완 권장 — **대기 목록 등재 없음**

「스마트」·「여행」·「한국」이 용어집에 없지만 **등재를 권장하지 않는다**:
- 「여행」은 문맥별로 `旅行`/`trip`/`travel`/`ทริป`/`เที่ยว`가 모두 정당하게 쓰여(실측 13키에서 en만 `trip` 4 : `travel` 3으로 갈린다) 단일 정본을 등재하면 **정상 번역을 P1로 오탐**시킨다 — v5.5에서 「포인트·환불·할인」 3건을 A/B 반려한 것과 같은 유형이다.
- 「스마트」는 이번이 **실사용 1건**이라 「실사용 0~1건 표기 등재 금지」(`md/landpress.md` §4 기준 4) 기준에 미달한다.

→ `md/glossary-backlog.md` 등재 대상 아님.

### (d-2) 추가 개선·제안 안내

1. **같은 타이틀이 나머지 5개 프레임(`홈탭1`·`홈탭2` 외 3)에도 있다** — 이번 작업은 사용자 지정 범위인 `카테고리 1`·`카테고리 2`만 위키에 반영한다. 나머지 화면의 Screen 행에도 같은 `mini_common_header_title`을 **No 1로 추가**하는 것이 번호 체계상 일관되며, 그 화면들을 다룰 때 함께 처리할 것을 권장한다(현재 위키 본문에 이 문구는 **0건**).
2. **Figma 원문 띄어쓰기(`한국여행`)** — XLT 값에서 교정하고 alias만 기록했다. 디자이너 수정 요청은 올리지 않는다(프로젝트 종결 규칙).

---

## 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 | **0건** ✅ |
| 수동 P0 | **0건** ✅ |
| P1 각 건 처리 판정 | 검출 0건 — 판정 대상 없음 ✅ |
| (d) 권장 사항 임의 적용 | 없음 ✅ |

→ **게이트 통과.** 위키 반영으로 진행한다.

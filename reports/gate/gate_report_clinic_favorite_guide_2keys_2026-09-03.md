# 게이트 리포트 — 상세페이지 즐겨찾기 안내 코치마크 (`(Unifi mini) 상세페이지 - 즐겨찾기 안내`)

- **작업**: Figma 프레임 `71417:17333` → 위키 `4667512757` Screen 신규 행 + 다국어 번역 반영
- **대상 파일**: `xlt/xlt_output_20260903182643.xlsx` (2키 · 10셀)
- **담당 FE 팀**: **UIT** — 프리픽스 `UF_` · 치환자 `{{0}}`(이 프레임엔 변수 없음). 같은 화면의 기존 키가 전부 `UF_clinic_detail_*`이라 선례 승계
- **라이브 기준**: 작업 착수 시 라이브 **v94**(2026-09-03 18:20, 사용자 직접 편집)를 새로 받아 그 위에 얹었다. Screen 표는 사용자가 4행으로 정리한 상태(브릿지 · 상세페이지-개선 · `kpick_clinic_oa_save_01_03` · `kpick_clinic_oa_msg_save_01`)
- **검증 범위**: **전수 점검** — 2키 **10셀 전체 행** 수동 검토 + 프레임 TEXT 42개 전수 분류(신규 2건 외 26건은 배경, 14건은 프레임 경계 밖). 신규 키로 한정하지 않았다.

---

## (1a) 한국어 원문 교정

| # | Figma 원문 | 교정 후 (XLT Key 값) | 사유 |
|---|---|---|---|
| 1 | `상단 오른쪽 더보기 를 누른 후` | `상단 오른쪽 더보기를 누른 후` | **조사 앞 불필요한 공백 제거**. `더보기`를 별도 스타일로 처리하다 생긴 공백으로 보인다 |

**alias 기록**: `{Figma: 더보기 를 → XLT: 더보기를}` — 디자이너 원본 수정 요청 대상 (아래 (d-2) 1번).

- **곡선 따옴표 `‘즐겨찾기’` 유지** — 등재값에 한국어 강조 인용으로 `‘’`를 쓴 선례가 있다(`UF_asset_purchase_price_edit_desc4` = `…‘수정됨’이 표시됩니다` · `UF_boost_claim_complete_desc` = `…‘내 자산’ 탭에서…`). 직선 따옴표로 바꾸지 않았다.
- 원문 이상문자 점검: 2키 ko_KR에서 nbsp(U+00A0)·제로폭·전각공백 **0건**. 개행(`\n`)은 화면 2행 구조와 일치.

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** ✅ |
| 🟡 P1 | 4건 (전건 실측 판정 — 아래 (c)) |
| 🟢 P2 | **0건** |

리포트: `xlt/xlt_output_20260903182643_validation_report.md`

---

## (b) 수동 3단계 체크표 — 2키 10셀 전수

### 1단계 (한국어 원문)

| 키 | ko_KR | 판정 |
|---|---|---|
| `UF_clinic_detail_favorite_guide_title` | `Unifi mini를\n즐겨찾기에 추가해보세요` | 정확 |
| `UF_clinic_detail_favorite_guide_desc` | `상단 오른쪽 더보기를 누른 후 ‘즐겨찾기’하면\nUnifi 혜택을 가장 빠르게 만날 수 있어요` | 정확 (1a 교정 반영) |

### 2단계 (용어집·등재값 정합)

| 용어 | 승계한 등재값 | 판정 |
|---|---|---|
| 즐겨찾기 | `UF_send_select_type_list_favorite` — ja `お気に入り` · en `Favorite` · th `รายการโปรด` · zh `我的最愛` | 정확 |
| 더보기 | `UF_send_select_type_list_more` — ja `もっと見る` · en `See more` · th `ดูเพิ่มเติม` · zh `更多` | 정확 (실측 근거 (c) 참조) |
| 혜택 | 등재값 실측 en `benefit` **29건** 우세 | 정확 |
| 「가장 빠르게」 어감 | 형제 키 `UF_clinic_detail_benefit_sub`(같은 화면 배너 · 이번 세션 갱신분) ja `いち早く` 와 통일 | 정확 |

### 3단계 (다국어 10셀)

| 키 | ja_JP | en_US | th_TH | zh_TW | 판정 |
|---|---|---|---|---|---|
| `_title` | `Unifi miniを\nお気に入りに追加してみてください` | `Add Unifi mini\nto your favorites` | `เพิ่ม Unifi mini\nในรายการโปรด` | `將 Unifi mini\n加入我的最愛` | 정확 — 5개 언어 모두 2행. ko `추가해보세요`(권유)를 ja `してみてください`로 대응, en·th·zh는 명령형이 관용이라 어조 조정 |
| `_desc` | `右上の「もっと見る」から「お気に入り」に追加すると\nUnifiの特典をいち早くご利用いただけます` | `Tap See more at the top right and add it to Favorites\nto enjoy Unifi benefits right away` | `แตะ ดูเพิ่มเติม ที่มุมขวาบน แล้วเพิ่มในรายการโปรด\nเพื่อรับสิทธิประโยชน์ของ Unifi ได้เร็วที่สุด` | `點選右上方的「更多」並加入「我的最愛」\n即可最快享受 Unifi 優惠` | 정확 — 인용부호는 언어별 관용(ja·zh `「」` / en·th 무인용). zh 번체 정자 확인. th 마침표 미사용(관용) |

### 프레임 TEXT 42개 전수 분류

| 구분 | 건수 | 내용 |
|---|---|---|
| **신규 XLT** | **2** | 코멘트 2건(둘 다 순수 `xlt`)이 가리키는 코치마크 타이틀·설명 |
| 배경(기존 행 소관) | 26 | `EXCLUSIVE BENEFIT`·`결제금액의 8% 캐시백 혜택`·`리뷰 265`·`영수증 인증`·탭 등 — 전부 `(Unifi mini) 상세페이지 - 개선` 행이 이미 다룸 |
| **프레임 경계 밖** | **14** | y 813~2626. 프레임 높이는 **812**이므로 화면에 보이지 않는다. 즐겨찾기 배너 2건(y813·833, 스크롤 아웃)과 **럭키드로우 이벤트 유의사항 12건**(y2091~2626)이며, 후자는 **이 화면과 무관한 프로모션 문구**다 → 대상에서 제외하고 (d-2) 3번으로 보고 |

신규·미정의 문구는 **2건 외 0건**이다.

---

## (c) 각 건 처리 판정 — P1 4건 전부 실측

> `scripts/fetch_xlt_registry.py`로 조회한 Unifi v1.7.4 등재값 2,429키에서 **실사용 건수를 실측**했다(추측 금지 원칙).

| # | 키 | 검증기 지적 | 실측 | 처리 판정 |
|---|---|---|---|---|
| P1-1 | `_desc` | en `혜택`→`perks` 권장 | ko에 「혜택」 포함 **68키** · en 표기 `benefit` **29건** / `reward` 9건 / **`perk` 6건** | **오탐** — `benefits` 유지. `benefit`이 압도적 우세 |
| P1-2 | `_desc` | en `더보기`→`View More` 권장 | ko에 「더보기」 포함 4키 · en `View more` **1건** / `See more` **1건**(동률) | **오탐** — `See more` 유지. 동률이며, 단독 UI 요소 지칭 키(`UF_send_select_type_list_more`)가 `See more`라 이 문맥에 더 부합 |
| P1-3 | `_desc` | ja `더보기`→`すべて見る` 권장 | ja `もっと見る` **2건** / **`すべて見る` 0건** | **오탐** — `もっと見る` 유지. **권장 표기 실사용 0건**(`md/check.md` 함정 2-1 「일반 규칙 ②」) |
| P1-4 | `_desc` | zh `더보기`→`查看更多` 권장 | zh `更多` 포함 **3건** / `查看更多` **1건** | **오탐** — `更多` 유지. 우세 표기 채택 |

**통과 판정: P0 = 0건 (자동 + 수동 모두)** → 산출물 진행 가능.

---

## (d) 추가 개선·제안 (권장 — 임의 적용 안 함)

> ⚠️ **종결 목록 대조 완료**. 사용자 결정으로 종결된 항목은 재제안하지 않았다 — 구분자 `･`→`·` 5건, `무료 LINE상담` 띄어쓰기, `change_cancel` 문체·마침표, 등재값 U+FF65 4건, 리뷰 본문 UGC, 상품·더미 데이터, 어노테이션 핀 색상, 법무 체크 섹션 서식. 이 프레임에도 `무료 LINE상담`·리뷰 본문·`홍길동길동`·`9:41`이 있으나 **종결 사안이므로 제외**했다.

### (d-1) 용어집 보완 권장 (실측 근거 있음)

**같은 오탐이 이번 세션에서 3번째다**(guidekim 바우처 · 상세페이지 개선판 · 이번). 용어집 매핑이 실사용과 어긋나 있다는 신호이므로 **용어집 보완이 근본 해결**이다. 아래는 실측 건수를 붙인 권장값이며, 반영은 사용자가 Landpress CMS에 전체 JSON을 붙여넣어야 한다(`md/landpress.md`).

| 용어 | 언어 | 현행 용어집 권장 | **실측 권장값** | 실측 근거 |
|---|---|---|---|---|
| 혜택 | en | `perks` | **`benefit`** | 68키 중 `benefit` **29건** vs `perk` **6건** |
| 더보기 | ja | `すべて見る` | **`もっと見る`** | `もっと見る` **2건** vs `すべて見る` **0건** |
| 더보기 | zh | `查看更多` | **`更多`** | `更多` **3건** vs `查看更多` **1건** |
| 더보기 | en | `View More` | **보류 권장** | `View more` **1건** vs `See more` **1건** — 동률이라 문맥 분기로 두는 편이 안전 |

⛔ 용어집 버전이 올라가면 **같은 작업에서 기획자 가이드 zip도 반드시 함께 갱신·전달**해야 한다(`md/landpress.md` §5-1).

### (d-2) 디자이너·기획 확인 요청 (신규 항목만)

| # | 항목 | 내용 |
|---|---|---|
| 1 | **`더보기 를` 띄어쓰기** | 조사 앞 공백. `더보기를`로 원본 수정 요청 |
| 2 | **「더보기」가 LINE 클라이언트 UI다** | 이 문구가 가리키는 것은 Unifi 화면 요소가 아니라 **LINE 앱 상단 우측 메뉴**다. 번역은 등재값 `더보기`(ja `もっと見る` 등)를 승계했으나, **LINE 자체 UI의 각 언어 실제 표기**와 다를 수 있다 — LINE 클라이언트 표기 확인 후 맞추는 편이 정확하다 |
| 3 | **프레임 경계 밖 무관 문구 12건** | y2091~2626에 **럭키드로우 이벤트 유의사항**(`본 럭키드로우 이벤트는 Unifi LINE 공식 계정 친구 추가 및…` 등)이 붙어 있다. 프레임 높이 812를 크게 벗어나 화면에 보이지 않으며 이 화면과 무관하다 — **다른 프레임에서 복사되어 남은 노드로 보인다.** 정리 권장 |
| 4 | **코치마크 키 네임스페이스** | `UF_clinic_detail_favorite_guide_*`로 부여했다(같은 화면 선례 승계). 다만 「Unifi mini 즐겨찾기 안내」는 클리닉 상세 전용이 아니라 **mini 공통 코치마크**일 수 있다 — 다른 mini 화면에도 노출된다면 `UF_mini_favorite_guide_*` 같은 공통 네임스페이스가 맞다. FE 협의 권장 |

### (d-3) 구조 관찰 (참고)

- 이 코치마크는 같은 화면의 즐겨찾기 배너(`UF_voucher_mini_certified` · `UF_clinic_detail_benefit_sub`, 이번 세션 문구 갱신분)와 **메시지가 겹친다** — 배너는 「즐겨찾기하고 …만나보세요」, 코치마크는 「즐겨찾기에 추가해보세요 … 가장 빠르게 만날 수 있어요」. 중복 노출 여부·노출 조건(최초 진입 1회 등)을 정책으로 정의하면 좋다.
- `UF_clinic_detail_benefit_sub`의 등재값은 여전히 구문구(`무료상담과 캐시백 혜택을 만나보세요`)다 — 위키만 갱신되어 있고 **XLT 업로드가 남아 있다**.

# 게이트 리포트 — 병원 링크 저장 친구추가 바텀시트 개선판 (`kpick_clinic_oa_save_01_03`)

- **작업**: Figma 프레임 `71339:15430`(`병원 링크 저장`) → 위키 `4667512757` Screen 신규 행 + 다국어 번역 신설
- **대상 파일**: `xlt/xlt_output_20260903175353.xlsx` (3키 · 15셀)
- **담당 FE 팀**: **UIT** — 프리픽스 `UF_` · 치환자 `{{0}}`. 기존 형제 행(`kpick_clinic_oa_save_01_01`·`_01_02`)의 키가 전부 `UF_clinic_oa_save_*`라 선례 그대로 승계(이 프레임엔 치환자 없음)
- **사용자 결정 3건 (2026-09-03)**: ⓐ Screen 표 **새 행 추가**(`kpick_clinic_oa_save_01_03`, 기존 `_01_01`·`_01_02` 보존) ⓑ `UF_clinic_oa_save_title`·`_desc`는 **기존 키 값 변경**, CTA는 **신규 키** `UF_clinic_oa_save_btn_line`(기존 `_btn`은 약관 미동의 행과 공유하므로 건드리지 않음) ⓒ CTA 문구는 **Figma 그대로** `LINE 친구 추가하고 상담하기`
- **검증 범위**: **전수 점검** — 3키 **15셀 전체 행** 수동 검토 + 프레임의 나머지 XLT 문구를 기존 Screen 행·등재값과 대조(배경 화면 문구는 기존 행 소관으로 확인). 신규 키로 한정하지 않았다.

---

## (1a) 한국어 원문 교정

| # | Figma 원문 | 교정 후 (XLT Key 값) | 사유 |
|---|---|---|---|
| 1 | `상담･예약` (U+FF65) | `상담·예약` (U+00B7) | 기존 위키 정본값 `UF_clinic_oa_save_desc`가 이미 `·`. 표기 일관 |

**alias 기록**: `{Figma: 상담･예약 → XLT: 상담·예약}`.

> ⚠️ **디자이너 원본 수정 요청은 하지 않는다** — 구분자 `･`→`·`는 이 프로젝트에서 **2026-09-01 사용자 결정으로 종결**된 항목(「Figma는 참고용이라 원본을 고치지 않는다」)이다. XLT 값만 정본 표기로 정규화했다.

원문 이상문자 점검: 3키 ko_KR에서 nbsp(U+00A0)·제로폭·전각공백 **0건**. 개행(`\n`)은 화면 2행 구조와 일치.

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** ✅ |
| 🟡 P1 | **0건** |
| 🟢 P2 | 1건 |

리포트: `xlt/xlt_output_20260903175353_validation_report.md`

---

## (b) 수동 3단계 체크표 — 3키 15셀 전수

### 1단계 (한국어 원문)

| 키 | ko_KR | 판정 |
|---|---|---|
| `UF_clinic_oa_save_title` | `병원 링크 저장을 위해\nUnifi 친구 추가해 주세요` | 정확 — 띄어쓰기·2행 구조 확인 |
| `UF_clinic_oa_save_desc` | `병원 링크를 저장하면 나중에 상담·예약할 수 있도록\n병원 정보 링크를 LINE으로 보내드려요.` | 정확 — (1a) 구분자 정규화 반영. 기존 값에서 `Unifi 친구 추가를 진행해 주세요.` 문장이 **삭제**된 것이 Figma 기준 |
| `UF_clinic_oa_save_btn_line` | `LINE 친구 추가하고 상담하기` | 정확 (사용자 확정 — Figma 그대로) |

### 2단계 (용어집·등재값 정합)

| 키 | 승계한 선례 | 판정 |
|---|---|---|
| `_title` | `UF_voucher_oa_link_title` = `구매 링크 전송을 위해\nUnifi 친구 추가가 필요해요` — 「…를 위해 / Unifi 친구 추가」 2행 구조 승계 | 정확 |
| `_desc` | 기존 위키 정본값에서 뒷문장 삭제분 | 정확 |
| `_btn_line` | `UF_voucher_oa_link_btn` = `LINE 친구 추가하고 링크 받기` 구조 승계 | 정확 |
| 「병원」 대역 | `UF_clinic_detail_btn_save` 등재값 ja `クリニックのリンクを保存` · en `Save clinic link` · th `บันทึกลิงก์คลินิก` · zh `儲存診所連結` — ja `クリニック` / en `clinic` / th `คลินิก` / zh `診所` 일치 | 정확 |

### 3단계 (다국어 15셀)

| 키 | ja_JP | en_US | th_TH | zh_TW | 판정 |
|---|---|---|---|---|---|
| `_title` | `クリニックリンクを保存するには\nUnifiを友だち追加してください` | `Add Unifi as a friend\nto save the clinic link` | `เพิ่ม Unifi เป็นเพื่อน\nเพื่อบันทึกลิงก์คลินิก` | `請加入 Unifi 好友\n以儲存診所連結` | 정확 — 5개 언어 모두 2행. ko는 「목적 → 요청」 어순, en·th·zh는 「요청 → 목적」으로 **어순 조정**(해당 언어에서 목적절 선행이 부자연스러움). 선례 `UF_voucher_oa_link_title` en도 같은 어순 |
| `_desc` | `クリニックリンクを保存すると、後で相談・予約できるように\nクリニック情報のリンクをLINEでお送りします。` | `Save the clinic link and we will send the clinic info link\nvia LINE so you can consult or book later.` | `เมื่อบันทึกลิงก์คลินิก เราจะส่งลิงก์ข้อมูลคลินิกให้ทาง LINE\nเพื่อให้คุณปรึกษาหรือจองได้ในภายหลัง` | `儲存診所連結後，我們會透過 LINE 傳送診所資訊連結，\n方便您日後諮詢或預約。` | 정확 — ja 중점 `・`·zh 전각 `，` 확인. th 마침표 미사용(관용). zh 번체 정자 확인 |
| `_btn_line` | `LINEで友だち追加して相談する。` | `Add as a friend on LINE and consult` | `เพิ่มเพื่อนใน LINE แล้วปรึกษา` | `加入 LINE 好友並諮詢` | 정확 — ja 마침표는 아래 M-1 참조 |

### 프레임의 나머지 문구 재검증 (전체 대상)

프레임 TEXT 27개 중 위 3키 외 24개는 **배경(구버전 상세페이지)** 문구로, 전부 기존 Screen 행이 이미 다루고 있음을 대조 확인했다. 신규·미정의 문구 **0건**.

---

## (c) 각 건 처리 판정

| # | 키 | 지적 | 처리 판정 | 사유 |
|---|---|---|---|---|
| P2-1 | `_desc` | 마침표 있음(소수 스타일) | **정책 유지** | 문장형이라 마침표가 정상. 기존 정본값·형제 키(`_desc_terms`)도 마침표 사용 |
| **M-1** | `_btn_line` | *(수동 발견)* ja 버튼에 `。` | **선례 따름 (P2 기록)** | 버튼에 마침표는 이례적이나 같은 키 패밀리 등재값 **2/2**가 `。`(`UF_voucher_oa_link_btn` = `LINEで友だち追加してリンクを受け取る。` · `UF_voucher_friend_add_btn` = `友だち追加して続ける。`). 단독 제거하면 형제 버튼과 갈리므로 **일괄 정리는 별도 제안**(d-1) |
| **M-2** | `UF_clinic_oa_save_*` | *(수동 발견)* XLT **미등재** | **P1 — 사용자 업로드 필요** | `_title`·`_desc`·`_desc_terms`·`_note`·`_btn`·`_agree_*` 전부 시스템 등재값 조회 결과 **미등재**. 위키에만 존재한다 |
| **M-3** | 기존 `_01_01`·`_01_02` 행 | *(수동 발견)* 5개 언어 번역 부재 | **이월 (범위 밖)** | 두 기존 행은 XLT 표에 KR만 있고 **다국어 번역 섹션에 행이 없다**. 이 프레임 범위를 넘으므로 손대지 않았다 |
| **M-4** | `UF_voucher_mini_certified`·`UF_clinic_detail_benefit_sub` | *(수동 발견)* 값 변경 파급 | **정상 (고지 필요)** | 직전 작업에서 두 키 값을 바꿨고(사용자 결정), **이 프레임 배경에도 같은 키가 쓰여** 구문구(`Unifi mini는 LINE 공식 인증 MINI App이에요`·`무료상담과 캐시백 혜택을 만나보세요`)가 새 값으로 바뀐다. 공유 키 변경의 의도된 파급이다 |

**통과 판정: P0 = 0건 (자동 + 수동 모두)** → 산출물 진행 가능.

---

## (d) 추가 개선·제안 (권장 — 임의 적용 안 함)

> ⚠️ **종결 목록 대조 완료**. 이 프로젝트에서 사용자 결정으로 **종결된 항목은 재제안하지 않았다** — 구분자 `･`→`·` 5건, `무료 LINE상담` 띄어쓰기, `change_cancel` 문체·마침표, 등재값 U+FF65 4건, 리뷰 본문 UGC, 상품·더미 데이터, 어노테이션 핀 색상, 법무 체크 섹션 서식. 이 프레임에도 `상담･예약`·`무료 LINE상담`이 있으나 **종결 사안이므로 요청 목록에서 제외**했다.

### (d-1) 표기 정책 제안

- **ja 버튼 마침표 일괄 정리** — `UF_clinic_oa_save_btn_line`·`UF_voucher_oa_link_btn`·`UF_voucher_friend_add_btn` 3키의 ja가 모두 `。`로 끝난다. 일본어 UI 버튼은 통상 마침표를 쓰지 않으므로 **3키를 함께 정리**하는 편이 낫다. 단독 변경은 형제 버튼과 표기가 갈려 권하지 않는다 — 사용자 결정 사안.
- **용어집 보완** — 이번 3키에서는 자동 검증기 P1이 **0건**이라 오탐 기반의 보완 신호가 없다. 권장 표기는 실사용 실측 없이 추측하지 않으므로 이번 작업에서는 제안하지 않는다.

### (d-2) 디자이너·기획 확인 요청 (신규 항목만)

| # | 항목 | 내용 |
|---|---|---|
| 1 | **CTA 「상담하기」** | 이 화면은 **병원 링크 저장(찜)** 플로우인데 CTA가 `LINE 친구 추가하고 상담하기`다. 형제 화면(`_01_01`·`_01_02`)은 `약관 동의하고 계속하기`이고, 유사 등재값은 `LINE 친구 추가하고 링크 받기`다. 사용자가 **Figma 그대로 확정**했으므로 반영했으나, 「저장하기/링크 받기」가 의도였는지 확인 권장 |
| 2 | **주석 삭제 확인** | 기존 화면의 `*친구 추가는 캐시백 등 혜택 안내 목적으로만 이용돼요.`(`UF_clinic_oa_save_note`)가 개선판에서 **사라졌다**. 개인정보 이용 목적 고지 성격이라 삭제가 의도인지 **법무 확인 권장** |
| 3 | **배경 화면이 구버전** | 이 바텀시트의 배경이 **구버전 상세페이지**다(탭 `병원정보·이용안내·리뷰` · 캐시백 **20%** · 리뷰 33). 개선판 상세페이지는 탭이 `병원정보·이용안내·FAQ`이고 캐시백 **8%**다. 배경은 참고용이라 XLT·정책에는 영향이 없으나, 리뷰용으로는 개선판 배경으로 교체하는 편이 혼동이 적다 |
| 4 | **약관 분기 커버리지** | 개선판은 약관 UI가 없어 기존 `_01_01`(기동의)에 대응한다. **약관 미동의(`_01_02`) 개선판**이 별도로 필요한지 확인 필요 |

### (d-3) 구조 관찰 (참고)

- 이 프레임은 직전 작업 프레임 `71241:9438`의 코멘트 42번(「병원 링크 저장 선택 시, Unifi 공식계정 친구추가 여부 확인 후 친구추가 안돼어 있다면 바텀시트로 친구추가 유도」)이 가리키는 **바텀시트 실체**다. 두 행을 함께 읽어야 동선이 완성된다.
- 다국어 번역 섹션에 `병원 링크 저장 (kpick_clinic_oa_save_01_03)` h3를 **신설**했다 — 기존 친구추가 모달 3행은 다국어 섹션이 없어 이 화면이 첫 사례다(M-3 이월과 연결).

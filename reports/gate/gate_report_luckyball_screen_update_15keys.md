# 번역 품질 게이트 리포트 — 화면 정의 갱신분 XLT 15키 재번역 (노란 셀 표시분)

- **대상**: 위키 pageId `4479306980`(Screen 표·전역 다국어 표) / `xlt/xlt_output_LV_20260727.xlsx`(73키)
- **작업일**: 2026-07-28
- **담당 FE 팀**: **LV** (`mini_luckyball_` · `{0}`)
- **사용자 지시**: 노란 셀로 표시된 갱신 키의 문구를 확인해 다국어 번역·엑셀에 반영하고, 반영 후 셀 색을 원복. **화면과 함께 정의된 XLT가 최신·정답**이며 그 문구를 기준으로 삼는다.
- **핵심 변경**: `팔로우 / LINE 채널` → **`친구 추가 / LINE 공식 계정`** 용어 전환(7키) + 문구 개편(8키)

---

## (1a) 번역 전 — 한국어 원문 교정

**화면 정의가 정답**이라는 지시에 따라 **문구 자체는 화면 값을 그대로 채택**했다. 다만 눈에 보이지 않는 제어문자만 정규화했다.

| 구분 | 대상 키 | 조치 |
|---|---|---|
| **비표준 공백 `\xa0` 정규화(4키)** | `friend_mission_detail`(`추가하면\xa0나도`) · `need_follow_to_draw`(`친구를\xa0유지해`) · `max20_desc`(`1명당\xa01개씩`) · `oa_bonus`(`추가 시\xa0럭키볼`) | 일반 공백으로 치환. **alias**: `{…\xa0… → … …}`. `md/check.md` 1단계-A "끝/중간 `\xa0` 잔존" 패턴. **위키 Screen 표의 `\xa0` 25건도 함께 정규화**해 화면·번역표·엑셀 3면을 일치시킴 |
| 문구 변경 없음 | 나머지 11키 | 화면 정의 값 그대로 |

**교정하지 않고 보고만 하는 항목 3건** (화면이 정답이라는 지시 → 임의 수정 금지, 아래 (d-2)에서 제안):
- `friend_mission_detail`: `Unifi LINE 공식 계정 친구 추가하면` — 다른 키는 `공식 계정**을** 친구 추가하면`(조사 있음). 조사 누락으로 보이나 화면 값 유지.
- `benefits_desc`: `출석체크`(표준은 `출석 체크`), `플레이 하면서`(표준은 `플레이하면서`).

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py` 실제 실행

```
파일: xlt/xlt_output_LV_20260727.xlsx (73키) · 용어집 v3.7(112 terms) 기준
🔴 P0 0건 / 🟡 P1 110건 / 🟢 P2 26건
```
- 직전 실행(101건) 대비 P1 +9 — **번역 품질 저하가 아니라, 새 한국어 문구에 용어집 등재어(`공식 계정`·`친구`·`출금`·`예치`)가 더 많이 들어와 매칭 시도 자체가 늘어난 결과**다. 전건 오탐 판정은 (c) 참조.
- **P0 = 0 — 통과 기준 충족.**

---

## (b) 수동 3단계 체크표 (md/check.md) — 갱신 15키 전 컬럼

> 자동 검증은 **전체 행(73키) 전수** 실행했고, (c)에서 **전체 파일**의 P1/P2 전건 처리 판정을 남긴다. 아래 체크표는 이번 갱신 15키를 전 컬럼(ko 포함 5개 언어) 검토한 결과다.

### 1단계: 한국어 (15키)

| 판정 | 내용 |
|---|---|
| ✅ 15/15 | 화면 정의 값 채택 + `\xa0` 4건 정규화. 맞춤법·조사·줄바꿈 이상 없음(위 (1a) 보고 3건 제외 — 화면 값 유지 결정) |

### 2단계: 용어집 (v3.7 · 112 terms)

| 용어 | 등재값 | 이번 적용 | 판정 |
|---|---|---|---|
| 공식 계정 | Official account / 公式アカウント / **公眾號** / บัญชีทางการ | `LINE Official Account` / `LINE公式アカウント` / **`LINE 官方帳號`** / `บัญชีทางการ LINE` | ⚠️ zh만 등재값과 다름 — **등재값이 오류**(아래 d-1). `官方帳號`가 LINE 대만 공식 표기 |
| 친구 | friend / 友だち / 好友 / เพื่อน | 전 키 등재값 사용(`友だち追加`·`好友`·`เพิ่มเพื่อน`) | ✅ |
| 럭키볼 · 당첨금 · 리워드 · 예치 | 등재값 | 그대로 | ✅ |
| JPYC · LINE · Unifi | exceptions(미번역) | 원문 유지 | ✅ |

### 3단계: 다른 언어 — 용어 전환 일관성 (핵심)

`팔로우/채널` → `친구 추가/공식 계정` 전환을 **7키 전부에 동일 어휘로** 적용했다.

| 개념 | ja_JP | en_US | th_TH | zh_TW |
|---|---|---|---|---|
| 친구 추가(동작) | `友だち追加する` | `add … as a friend` | `เพิ่มเพื่อน` | `加入…好友` |
| 친구 유지 | `友だち登録を維持` | `stay friends with` | `คงการเป็นเพื่อน…ไว้` | `持續保留…好友` |
| LINE 공식 계정 | `LINE公式アカウント` | `LINE Official Account` | `บัญชีทางการ LINE` | `LINE 官方帳號` |

- **구 표현 잔존 검사**: 갱신 15키에서 `フォロー`·`follow`·`ติดตาม`·`追蹤`·`チャンネル`·`channel`·`頻道` **0건** 확인.
- 문구 개편 8키: `max20_short`(20명분→**20개**), `max20_desc`(1개씩·최대 20명), `invite_me_too_line`/`gift_copy_link`(짧은 버튼 카피로 축약 — ja `LINEの友だちにプレゼントする`·`プレゼントリンクをコピー`, en `Gift to a LINE friend`·`Copy gift link`), `line_app_only`(해요체 1문장), `interest_title`/`interest_desc`(자산보전 → **편리한 입출금**), `benefits_desc`(앱 플레이·USDT → **출석체크·게임 미션·JPYC**), `oa_follow_open`/`oa_bonus`(친구 추가 표현).
- ja `benefits_desc`는 `md/check.md` ja 패턴(`出席`→`チェックイン`)에 따라 **`チェックイン`** 사용.
- 컬럼 정렬 검증(check.md 4-1): 15키 모두 5개 컬럼 회전 어긋남·swap 없음. 이질 문자체계 검출 0건. zh는 번체 정자 확인(`帳號`·`獎勵`·`資產`).
- **3면 일치 확인**: 화면 KR ↔ 전역 표 KR ↔ 엑셀 ko 불일치 **0건**(작업 중 발견된 1건 — `invite_me_too_line`이 (Logged in) 초대페이지 No3에 구 문구로 남아 있던 것 — 동일 키=동일 값 원칙으로 동기화).

---

## (c) 검증기 보고 전건 처리 판정 (전체 파일 73키)

| 심각도 | 건수 | 처리 판정 |
|---|---|---|
| P0 | 0 | 해당 없음 |
| P1 | 110 | **전건 오탐(정상 의역·용어집 이슈)**. 갱신 15키 관련 23건 내역: `공식 계정→公眾號` **5건 = 용어집 등재값 오류**(아래 d-1, 번역은 `官方帳號`가 정확) · `최대→Max/最多` 5건(`up to`·`最高`가 표준) · `당첨→win/中獎/ถูกรางวัล` 3건(활용형·의역) · `출금→提領` 2건(`存提款`이 자연) · `예치→預入` 2건(`預けて`가 자연) · `친구→friend/好友` 2건(문장 내 다른 표현) · `가입/회원가입→Sign up` 2건(대소문자·`sign-up` 하이픈) · `선물하기→มอบ` 1건(`ของขวัญ` 명사형) · 기타 1건. 나머지 87건은 기존과 동일한 확정 오탐. |
| P2 | 26 | **전건 오탐/정책 유지** — [마침표 스타일]. 설명문=마침표 / 버튼·타이틀=마침표 없음 정책에 일치. |

**통과 판정: P0 = 0 (자동·수동 모두) → 위키·엑셀 반영 완료, 노란 셀 원복 완료.**

---

## (d) 추가 개선·제안

### (d-1) 용어집 수정 권장 — `공식 계정` zh_TW: `公眾號` → **`官方帳號`** (v3.8 후보)

이번 전환으로 `공식 계정`이 7키에서 쓰이면서 드러난 **등재값 오류**입니다.

| | 현재 등재값 | 권장 |
|---|---|---|
| zh_TW | `公眾號` | **`官方帳號`** |

- `公眾號`는 **WeChat(중국 대륙)** 용어이고, **LINE 대만의 공식 표기는 `官方帳號`** 입니다(`LINE 官方帳號`).
- 위키의 zh 번역은 이미 전부 `官方帳號`를 쓰고 있어, 등재값을 고치면 **P1 5건이 정탐/오탐 혼선 없이 사라집니다**.
- 다른 4개 언어(en `Official account` / ja `公式アカウント` / th `บัญชีทางการ`)는 현행 유지.
- 승인하시면 v3.8 전체 JSON + 가이드(용어집 탭) 동반 갱신까지 진행하겠습니다(`md/landpress.md` §5-1).

### (d-2) 원문 표기 제안 (화면이 정답이라 **임의 반영하지 않음**)

| 키 | 현재(화면) | 제안 | 사유 |
|---|---|---|---|
| `friend_mission_detail` | `Unifi LINE 공식 계정 친구 추가하면` | `…공식 계정**을** 친구 추가하면` | 같은 개편의 `follow_bonus_desc`·`follow_line`은 조사 `을` 포함 — 조사 누락으로 보임 |
| `benefits_desc` | `출석체크`, `플레이 하면서` | `출석 체크`, `플레이하면서` | 표준 띄어쓰기(`md/check.md` 1단계 B-6) |

디자이너 원본 수정이 필요하므로, 반영하실지 알려주시면 번역까지 함께 맞추겠습니다.

### (d-3) 키 사용 감사 결과 (사용자 요청)

| 점검 | 결과 |
|---|---|
| **전역 다국어 표에 있으나 어느 화면에도 없는 키** | **0건** ✅ |
| **LV 엑셀(73키)에 있으나 어느 화면에도 없는 키** | **0건** ✅ |
| **UIT 엑셀(2키)에 있으나 어느 화면에도 없는 키** | **0건** ✅ |
| **역방향 — 화면에 정의됐으나 전역 표·엑셀에 없는 키** | **5건** (아래) |

역방향 누락 5건은 모두 **UIT 섹션의 KAIA 부스트 배너 키**입니다. UIT 팀 규칙(`UF_` 프리픽스 · `{{0}}` 치환자)에 맞게 정의돼 있으나, 전역 다국어 표와 UIT 엑셀 어디에도 없어 **번역이 존재하지 않습니다**.

| XLT Key | 화면 KR |
|---|---|
| `UF_main_kaia_boost_inactive_title_common` | 부스트 참여하고 USDT 받기 |
| `UF_main_kaia_boost_active_title_common` | 나의 부스트 {{0}} |
| `UF_main_kaia_boost_inactive_title_vip` | 부스트 참여 시 VIP 혜택 제공 |
| `UF_main_kaia_boost_active_title_vip` | 나의 VIP 부스트 {{0}} |
| `UF_main_kaia_boost_inactive_desc_common` | 최대 {{0}} 추가 이자 받기 |

→ **번역이 필요한지 알려주세요.** 진행하시면 5개 언어 번역 + 게이트 후 전역 표·**UIT 엑셀**(현재 2키 → 7키)에 반영하겠습니다. (`UF_main_kaia_boost_active_desc_*` 계열은 화면 표에 키만 언급되고 행 정의가 없어 이번 집계에서 제외했습니다.)

### (d-4) 기타 미결

- `친구한테` vs `친구에게` 표기 정책 — 이번 개편으로 `invite_me_too_line`이 `LINE 친구에게 선물하기`로 바뀌어 **`친구에게` 쪽 사용례가 늘었습니다**. 통일 시점을 정하시면 일괄 정리하겠습니다.
- th `초대` 표기(`เชิญ`/`ชวน`) — 품사별 정상 구분으로 결론, 조치 없음.

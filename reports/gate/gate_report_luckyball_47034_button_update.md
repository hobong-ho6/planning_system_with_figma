# 게이트 리포트 — 피초대자 페이지 - 유니파이 회원(63652-47034) 버튼 동작·문구 업데이트

- 작업일: 2026-07-28
- 대상 위키: https://wiki.workers-hub.com/pages/viewpage.action?pageId=4479306980 (럭키볼 친구에게 선물하기 캠페인)
- 대상 프레임: `피초대자 페이지 - 유니파이 회원` (node-id 63652-47034)
- 담당 FE 팀: **LV** (위키 Screen 섹션의 `LV > 피초대자 프로모션 페이지` 하위 — 위키에 UIT/LV 구분이 있으므로 사용자 확인 없이 LV 규칙 적용). 프리픽스 `mini_luckyball_`, 변수 치환자 `{0}` (본 건은 변수 없음)
- 검증 대상 파일: `xlt/xlt_output_LV_20260727.xlsx` (73키) · 용어집 라이브 v3.5(107 terms, 이번 실행에서 API 새로 조회)

## 0. 변경 요약 (Figma 원본 새로 조회 — 캐시 미사용)

Figma 코멘트 스레드(`scripts/fetch_comments.py` `fetch_threads`, 좌표 정규화 적용) 4건:

| 통합 No | 종류 | 코멘트 본문 | 매칭 텍스트 |
|---|---|---|---|
| 1 | 정책 | 사용자가 피초대자 이벤트 참여 시, 이미 Unifi 회원인 경우 안내 팝업 | (없음 — 순수 정책 핀) |
| 2 | xlt | xlt | 이미 Unifi 회원이시네요! |
| 3 | xlt | xlt | Unifi에서 참여 가능한 다양한 미션을<br>확인하고 리워드를 받아보세요! |
| 4 | 정책 + `↳ xlt` 답글 | **선택시 Unifi mini 홈으로 이동한다.** | **홈으로 가기** |

**위키 현재 상태 대비 델타(2건, No4에 집중):**

| 항목 | 변경 전(위키 v119) | 변경 후(Figma 최신) |
|---|---|---|
| Description No4 정책 | 선택시 알림 팝업이 닫힌다. | 선택시 Unifi mini 홈으로 이동한다. |
| XLT 표 No4 | `mini_luckyball_confirm` / 확인 | `mini_luckyball_go_home` / 홈으로 가기 |

- No2·No3는 문구·키 모두 **변경 없음**.
- **신규 키 없음 / 삭제 키 없음** — `go_home`·`confirm` 모두 다른 화면에서 계속 사용되므로 전역 다국어 표(73키)와 XLT 엑셀은 **변경 없음(재생성·재첨부 불필요)**.
- 이번 변경은 사실상 2026-07-27의 `go_home→confirm` 반영을 되돌리는 방향(디자인 재변경).

## 1. 한국어 원문 교정 (게이트 1단계)

Figma 원문 ↔ XLT `ko_KR` 대조 결과 **교정 필요 없음, alias 없음**.

| 키 | Figma 원문 | XLT ko_KR | 판정 |
|---|---|---|---|
| `mini_luckyball_already_member` | 이미 Unifi 회원이시네요! | 이미 Unifi 회원이시네요! | 일치 — 맞춤법·띄어쓰기 정상 |
| `mini_luckyball_missions_promo` | Unifi에서 참여 가능한 다양한 미션을⟨U+2028⟩확인하고 리워드를 받아보세요! | 동일 (줄바꿈 `\n`) | 일치 — 개행문자 차이만(정상) |
| `mini_luckyball_go_home` | 홈으로 가기 | 홈으로 가기 | 일치 — 정상 |

## 2. 자동 검증 — `scripts/validate_translation.py` (a)

실행: `python3 scripts/validate_translation.py xlt/xlt_output_LV_20260727.xlsx scripts/glossary.json`
→ 리포트: `xlt/xlt_output_LV_20260727_validation_report.md` (**전체 파일 73키 전수 실행**)

| 심각도 | 건수 | 비고 |
|---|---|---|
| 🔴 P0 | **0건** | 통과 기준 충족 |
| 🟡 P1 | 110건 | 전건 [용어 불일치] — 용어집 협소 매핑 오탐 |
| 🟢 P2 | 26건 | 전건 [마침표 스타일] |

- 1단계(한국어) P0 0 · P1 0 · P2 26
- 2단계(용어집, v3.5 107 terms) 완료
- 3단계(다국어, 이질 문자체계 포함) 완료 — 검출 0

## 3. 수동 3단계 체크표 (b) — 본 변경 관련 키

전체 파일 73행에 대한 자동 3단계는 위와 같이 **전수** 실행했고, 의미 기반 수동 검토(`md/check.md`·`md/translation_validation_guide.md`)는 본 부분 작업의 **변경·관련 키 4종**을 대상으로 수행했다. (기존 키 전수 의미 검토는 2026-07-27 세션에서 완료된 이력이 있으며, 이번 변경은 키 텍스트를 새로 생성하지 않는다.)

### 1단계 — 한국어(ko_KR) 검토

| 키 | ko_KR | 맞춤법/띄어쓰기 | 조사/받침 | 판정 |
|---|---|---|---|---|
| `mini_luckyball_go_home` | 홈으로 가기 | 정상 (`홈으로`+`가기` 띄어쓰기 정상) | 종성 `ㅁ`→`으로` 정상 | ✅ 정확 |
| `mini_luckyball_already_member` | 이미 Unifi 회원이시네요! | 정상 | 정상 | ✅ 정확 |
| `mini_luckyball_missions_promo` | Unifi에서 참여 가능한 다양한 미션을 / 확인하고 리워드를 받아보세요! | 정상 (붙은 띄어쓰기 없음) | 정상 | ✅ 정확 |
| `mini_luckyball_confirm` | 확인 | 정상 | — | ✅ 정확 (이 화면에서는 제거) |

### 2단계 — 용어집 대조 (라이브 v3.5)

| 용어 | 등장 키 | 용어집 표기 | 실제 표기 | 판정 |
|---|---|---|---|---|
| 회원 | already_member | ja `会員` / zh `會員` | 동일 | ✅ 일치 |
| 미션 | missions_promo | ja `ミッション` / en `mission` / th `ภารกิจ` / zh `任務` | 동일 | ✅ 일치 |
| 리워드 | missions_promo | ja `リワード` / en `reward` / th `รางวัล` / zh `獎勵` | 동일 | ✅ 일치 |
| 확인 | confirm | en `check` / th `ตรวจสอบ` / zh `查看` | `OK` / `ตกลง` / `確認` | ⚠️ 오탐 — 아래 4절 참조 |
| 홈 | go_home | (용어집 미등재) | ja `ホーム` / th `หน้าแรก` / zh `首頁` | ✅ 서비스 관용 표기 일관 |

### 3단계 — 다국어(ja/en/th/zh) 의미·표기 검토

| 키 | ja_JP | en_US | th_TH | zh_TW | 판정 |
|---|---|---|---|---|---|
| `go_home` | ホームへ移動 | Go to home | ไปหน้าแรก | 前往首頁 | ✅ 5개 언어 모두 "홈으로 이동" 의미 일치. 번체 정자 정상, 이질 문자체계 혼입 없음, placeholder 없음. en은 P2 제안(4-d 참조) |
| `already_member` | すでにUnifi会員でいらっしゃいますね！ | You're already a Unifi member! | คุณเป็นสมาชิก Unifi อยู่แล้ว! | 您已經是 Unifi 會員了！ | ✅ 존경 표현·감탄 어조 일치, 번체 정자 정상 |
| `missions_promo` | Unifiで参加できるさまざまなミッションを / 確認して、リワードを受け取りましょう！ | Check out the various missions available on Unifi / and claim your rewards! | ดูภารกิจต่าง ๆ ที่ร่วมได้บน Unifi / แล้วรับรางวัลกันเลย! | 查看 Unifi 上可參與的各種任務， / 領取您的獎勵吧！ | ✅ 2행 구조·의미 일치 |
| `confirm` | 確認 | OK | ตกลง | 確認 | ✅ 버튼 OK 의미로 정확 (이 화면에서는 제거) |

- **일관성**: `go_home`은 `(Login x) 초대페이지`(No3)·`DA Score x`(No4) 화면에서 이미 동일 번역으로 사용 중 → 화면 간 표기 일관 ✅

## 4. 검증기 보고 건별 처리 판정 (c)

> ⚠️ **집계 정정(2026-07-28)**: `validate_translation.py`가 파일로 쓰는 리포트는 **심각도별 상위 20건만** 기록하고 나머지는 생략한다(P1 110건 중 20건만 파일에 존재). 최초 작성 시 이 잘린 파일을 근거로 관련 지적을 3건으로 적었으나, `TranslationValidator.issues`를 직접 열거해 **전건 재집계**한 결과 본 변경 관련 지적은 **5건**이다. 아래 표는 재집계 결과다. (교훈: 건별 판정은 리포트 파일이 아니라 `issues` 전량으로 확인할 것.)

본 변경 관련 키에 걸린 검증기 지적 (전건 재집계):

| 키 | 심각도 | 지적 | 처리 판정 |
|---|---|---|---|
| `mini_luckyball_confirm` | P1 | en_US: '확인' → 'check' 권장 | **오탐** — 용어집 `확인`은 "조회·확인하다"(동사) 매핑. 여기는 버튼 OK 의미 → `OK`가 정확 |
| `mini_luckyball_confirm` | P1 | th_TH: '확인' → 'ตรวจสอบ' 권장 | **오탐** — 동일 사유. 버튼 OK는 `ตกลง`이 정확 |
| `mini_luckyball_confirm` | P1 | zh_TW: '확인' → '查看' 권장 | **오탐** — 동일 사유. 버튼 OK는 `確認`이 정확 |
| `mini_luckyball_missions_promo` | P1 | en_US: '확인' → 'check' 권장 | **오탐** — 검증기가 **대소문자를 구분**한다. 실제 en은 `Check out the various missions…`로 문두 대문자 `Check`라 소문자 `check` 부분문자열 검사에 걸린 것. 번역은 정확 |
| `mini_luckyball_missions_promo` | P1 | th_TH: '확인' → 'ตรวจสอบ' 권장 | **오탐** — 여기 '확인하고'는 "둘러보다/살펴보다" 의미. th `ดู`가 자연스럽고 정확. `ตรวจสอบ`(검사·대조)는 문맥 부적합 |
| `mini_luckyball_go_home` | — | 지적 없음 (P0/P1/P2 모두 0건) | 해당 없음 |
| `mini_luckyball_already_member` | — | 지적 없음 | 해당 없음 |

전체 파일 기준 나머지 P1 105건·P2 26건은 이번 변경과 무관한 기존 항목이며, 2026-07-27까지의 게이트 리포트에서 **전건 오탐(용어집 협소 매핑 · 타이틀 마침표 스타일)** 으로 처리 판정된 이력을 유지한다. 이번 작업으로 새로 발생한 건은 **0건**.

## 5. 추가 개선·제안 (d) — 임의 적용하지 않음, 사용자 결정 사항

### (d-1) 용어집 보완 — 검토 후 **변경 없음으로 종결** (사용자 확정 2026-07-28)

최초 작성 시 ① `확인(버튼)` 별도 등재 ② `홈` 신규 등재를 권장했으나, 사용자 승인 후 실제 반영을 위해 v3.6 JSON과 `validate_translation.py` 매칭 로직을 검증한 결과 **두 방안 모두 부적합**함을 확인했다.

**근본 원인 — 매칭은 `if ko_term in ko_text` (ko_KR 대소문자 구분 부분일치):**

- **`확인` 오탐은 3건이 아니라 파일 전체 10건**(`confirm` 3 · `missions_promo` 2 · `oa_followed` 1 · `must_check` 1 · `social_link_desc` 1 · `terms_info_change` 2). 원인 2종: ⓐ 용어집 en이 소문자 `check`라 문두 대문자 `Check out…`이 미포함 판정, ⓑ `확인`이 **다의어**(버튼 OK / 둘러보기 / 조회·검증)라 1:1 매핑 불가.
- 검토한 세 방안: **`확인(버튼)` 키 추가 = 무효**(ko `확인`에 매칭 안 됨) · **버튼 의미로 교체 = 악화**(동사 `확인` 5행이 새로 깨짐) · **삭제 = 조회 의미 표준 표기 소실**.
- **`홈` 등재 = 새 P1 유발** — 사용 1행(`go_home`)뿐이라 `md/landpress.md` §4 기준 1(반복 사용) 미달, `홈`→en `Home` 등재 시 현행 `Go to home`(소문자 h)이 위 ⓐ에 걸려 없던 P1이 생긴다(§4 기준 3 위반). 표기 기준은 기존 `홈으로 이동`(en `Go to Home`) 항목으로 충분.

**사용자 결정**: `확인` = **현행 유지**(조회 의미 표준 보존, P1 10건은 오탐 판정으로 처리) · `홈` = **등재 보류**.
→ **용어집 v3.6 유지, 버전 상승 없음** → `md/landpress.md` §5-1(기획자 가이드 zip 동반 갱신) **해당 없음**.
→ 재제안 방지를 위해 `md/check.md` 함정 2-1(v3.3)에 확정 오탐으로 기재했다.

### (d-2) 표현·정책 제안

3. **`mini_luckyball_go_home` en_US 표기** — 현행 `Go to home`. 앱 버튼 관례상 `Go to Home`(화면명 고유어 대문자) 또는 `Go home`이 더 자연스럽다. 다만 이미 2개 화면에서 동일 문자열로 노출 중이라 **일관성 유지를 위해 이번엔 변경하지 않았다.** 변경 시 3개 화면 일괄 적용 필요 — 사용자 결정 요청.
4. **`확인`→`홈으로 가기` 재변경 이력** — 2026-07-27 세션에서 `go_home→confirm`으로 바꾼 화면을 이번에 다시 되돌렸다. 디자인이 재차 흔들린 구간이므로 확정 여부를 디자이너에게 확인 권장.

## 6. 통과 판정

- 🔴 **P0 = 0건** (자동 전수 + 수동 모두) → **통과**, 위키 반영 진행 가능
- P1/P2: 위 4절대로 건별 처리 판정 완료(본 변경 관련 전건 오탐), 나머지는 기존 판정 유지
- (d) 권장 사항은 사용자 결정 전까지 **미반영**

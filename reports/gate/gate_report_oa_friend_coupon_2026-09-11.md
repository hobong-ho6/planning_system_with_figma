# 번역 품질 게이트 리포트 — 친구추가 1,000엔 쿠폰 OA (2026-09-11)

대상: 친구추가 → 유니파이 바우처 1,000엔 지급 안내 OA 메시지 (신규, XLT 키 미부여 — `md/OA.md` 규칙 1)
문구 6종 × 5개 언어(ko_KR·ja_JP·en_US·th_TH·zh_TW) = 30셀

## 1단계 — 한국어 원문 교정 (1a)

작성 단계에서 초안을 사용자에게 제시해 확정받았다(문구 자체가 최초 작성이라 별도 alias 교정 대상 없음).
- 보조용언 띄어쓰기 확인: `사용해 볼까요?`·`추가해 주셔서` — 붙여쓰기 아님(정칙 준수).
- 종결형 일관성: 해요체(`~해요`, `~할까요?`, `~할 수 있어요`)로 통일.
- 각주 마침표: 완결된 문장이라 마침표 유지(P2로 아래 보고, 원문 교정 대상 아님).

## 2단계 — 자동 검증 (`scripts/validate_translation.py`)

```
✓ 한국어 검증 완료: P0 0건, P1 0건, P2 1건
✓ 용어집 검증 완료
✓ 다국어 검증 완료
```

| 심각도 | 건수 |
|---|---|
| P0 | 0건 |
| P1 | 0건 |
| P2 | 1건 |

- P2: `oa_friend_coupon_footnote` — 마침표 스타일(문장형 각주라 마침표 유지, 스타일 선호 수준)

## 3단계 — 수동 전수 검토 (전체 6키 × 5언어, 전체 파일 대상)

| 키 | ko_KR | ja_JP | en_US | th_TH | zh_TW | 판정 |
|---|---|---|---|---|---|---|
| title | 🎉 유니파이 바우처 1,000엔이 도착했어요! | 🎉 Unifiバウチャー1,000円が届きました！ | 🎉 A 1,000 JPY Unifi Voucher has arrived! | 🎉 คุณได้รับบัตรกำนัล Unifi มูลค่า 1,000 เยนแล้ว! | 🎉 您已獲得 Unifi 抵用券 1,000 日圓！ | 정확 |
| body1 | 친구 추가해 주셔서 감사해요! | 友だち追加ありがとうございます！ | Thanks for adding us as a friend! | ขอบคุณที่เพิ่มเราเป็นเพื่อน! | 感謝您加入好友！ | 정확 |
| body2 | 지금 바로 사용해 볼까요? | 今すぐ使ってみませんか？ | Want to use it right now? | ลองใช้เลยตอนนี้ไหม? | 現在要馬上使用看看嗎？ | 정확 |
| footnote | * 바우처는 계정당 1회만 사용할 수 있어요. | ※バウチャーは1アカウントにつき1回のみご利用いただけます。 | * The voucher can only be used once per account. | * บัตรกำนัลใช้ได้เพียง 1 ครั้งต่อบัญชีเท่านั้น | ※ 抵用券每個帳號限用一次。 | 정확 |
| btn | 바우처 사용하기 | バウチャーを使う | Use Voucher | ใช้บัตรกำนัล | 使用抵用券 | 정확 |
| altText | 유니파이 바우처 1,000엔을 받았어요! | Unifiバウチャー1,000円を受け取りました！ | You've received a 1,000 JPY Unifi Voucher! | คุณได้รับบัตรกำนัล Unifi มูลค่า 1,000 เยน! | 您已獲得 Unifi 抵用券 1,000 日圓！ | 정확 |

점검 항목: 외래어 음차 오염 없음 · 띄어쓰기(보조용언·의존명사) 정상 · 다의어 모호 없음 · 문체 일관(해요체) · 언어 간 문자체계 혼입 없음(자동 검증과 교차 확인) · 숫자+통화 표기는 언어별 관용에 맞춰 통일(ko/ja/zh_TW: 붙여씀 `1,000엔`·`1,000円`·`1,000日圓`, en/th: 띄어씀 `1,000 JPY`·`1,000 เยน`).

용어집(`web3_xlt_json`) 대조: `친구`(en `friend`·ja `友だち`·th `เพื่อน`·zh_TW `好友`), `계정`(en `account`·ja `アカウント`·th `บัญชี`·zh_TW `帳號`) 등재값을 그대로 사용해 5개 언어 전건 일치.

## 각 건 처리 판정

- P2 1건(`oa_friend_coupon_footnote` 마침표 스타일): **오탐 아님, 정책 보류** — 완결된 문장형 각주라 마침표를 유지하기로 판단(스타일 선호 수준, 실제 위반 아님). 사용자 확인 시 제거 가능.
- 그 외 P0/P1 없음.

## (d) 추가 개선·제안 (권장)

- **(d-1) 용어집 보완 권장**: `바우처`(voucher) 용어가 이 프로젝트에서 반복 등장(클리닉 OA `UF_voucher_*` 등)하지만 용어집 `terminology`에 정식 등재가 없다. 5개 언어 권장 표기는 `voucher(en)`·`バウチャー(ja)`·`บัตรกำนัล(th)`·`抵用券(zh_TW)`이며, **실사용 건수는 `scripts/fetch_xlt_registry.py`로 실측 후** 용어집 업데이트 여부를 결정하는 것을 권장한다(이번 리포트는 제안만, 등재는 사용자 승인 필요).
- **(d-2) 추가 개선 제안**: 각주의 마침표(`.`/`。`/`!`)를 없애 더 짧은 배너 톤으로 통일할지는 정책 선택 사항 — 현 상태(완결 문장)도 무방하다.

## 통과 기준

**P0 = 0건**(자동 + 수동 전수) — 통과. 위키 반영·Flex JSON 생성 진행 가능.

# JPYC 럭키볼 시즌 3 — UIT 5키 게이트 리포트 (기존 번역값 승계)

- **대상 위키**: pageId **4541588845** `JPYC 럭키볼 시즌 3` (반영 전 v4) — Screen `UIT` 영역 신설
- **대상 프레임 2개**: `66022-111911` (LIFF)Unifi - Login x · `66022-112717` (LIFF)Unifi - Login x, Floating Banner
- **담당 팀**: **UIT** (사용자 지정) → 키 프리픽스 `UF_`, 치환자 **`{{ }}`**(이중 중괄호) — 승계값이 이미 UIT 규칙을 따름
- **번역 방식**: **재번역 없음.** 5키 전부 참조 위키 **pageId 4282541727 `JPYC_골드 럭키볼 캠페인`(v39)** 에 등재된 값을 그대로 승계(사용자 지시)
- **원본 재조회(캐시 금지)**: Figma 노드·코멘트(파일 전체 824건에서 스레드 재구성)·참조 위키·용어집 API 모두 이번 실행에서 새로 조회
- **처리량**: 5키 × 5언어 = **25셀**, 프레임 2개(핀 7개 = 정책 2 + XLT 5)

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py` 실제 실행

| 단계 | 결과 |
|---|---|
| 1단계 한국어 | P0 **0건** / P1 0건 / P2 0건 |
| 2단계 용어집(v4.0) | P0 **0건** (JPYC 원어 유지 위반 0) |
| 3단계 다국어 | P0 **0건** — 빈칸 0, 언어 혼입·컬럼 어긋남 0, 숫자 placeholder(`{{0}}`·`{{1}}`) 불일치 0 |
| **합계** | 🔴 **P0 = 0건** · 🟡 P1 7건 · 🟢 P2 0건 |

엑셀은 `scripts/export_to_xlt.create_xlt_excel`로만 생성(`plurals` 고정 포맷 보존) 후 `validate_excel` 통과.

---

## (b) 수동 3단계 체크표 — **전수 점검**(5키 × 5언어 25셀 + Figma 원문 대조)

### 1단계: 한국어

| 점검 | 결과 |
|---|---|
| 맞춤법·조사 | ✅ 승계값 이상 없음 |
| 띄어쓰기 | ⚠️ `UF_floating_jpyc_banner_desc` — `{{month}}월 {{day}} 에{{fallbackTimezone}}`에서 **`에` 앞 공백·뒤 붙임**이 어색(원본 그대로 승계, (d-2) 1번) |
| 치환자(UIT `{{ }}`) | ✅ ko 전 키 이중 중괄호 유지 |
| 제어문자 | ✅ 0건 (Figma 원문의 ` `은 값에 반영하지 않고 개행으로 정규화) |

### 2단계: 용어집(라이브 v4.0 / 113 terms) 대조

| 용어 | 등재값 | 승계값 | 판정 |
|---|---|---|---|
| JPYC | 전 언어 원어 유지 | 전 언어 `JPYC` | ✅ |
| 가입 | Sign up / 登録 / 註冊 / สมัคร | `UF_floating_banner_btn` 전 언어 등재값 일치 | ✅ |
| 당첨 | win / 当選 / 中獎 / ถูกรางวัล | 문맥상 의역(`won`·`当たりました`·`中獎`·`ได้รับรางวัล`) | ✅ 오탐 — (c) 참조 |

### 3단계: 언어별 검토 (25셀 전수)

| 언어 | 결과 |
|---|---|
| **ja_JP** | ✅ です・ます体, 숫자·라틴 붙임. ⚠️ `desc`에 **`{day}` 단일 중괄호** — (c) 1번 |
| **zh_TW** | ✅ 번체(獲得·當·註冊). ⚠️ `desc`에 **`{day}` 단일 중괄호** — (c) 1번 |
| **en_US** | ✅ 간결. `You won {{0}} JPYC` 등 자연스러움 |
| **th_TH** | ✅ 의미 단위 띄어쓰기, 숫자+단위 공백 유지 |
| **공통** | ✅ 5개 언어 모두 값 존재, 컬럼 어긋남 0 |

---

## (c) 처리 판정 — 자동 7건 + 수동 발견 5건 전 건

### 자동 검증 P1 7건 — **전건 오탐(정상 의역·승계값)**

| # | 키 | 내용 | 판정 |
|---|---|---|---|
| 1 | `home_jpyc_banner_title` | th `당첨`→`ถูกรางวัล` | 오탐 — `ได้รับรางวัล 100%`로 의역 |
| 2·3 | `home_jpyc_banner_title` | en `최대`→`Max` / zh `최대`→`最多` | 오탐 — `up to` · `最高`가 문맥상 정상 |
| 4~6 | `floating_jpyc_banner_title` | en·ja·th `당첨` 매핑 | 오탐 — `won`(불규칙 활용)·`当たりました`·`ได้รับ` 정상 |
| 7 | `floating_jpyc_banner_desc` | en `가입`→`Sign up` | 오탐 — `Join now`가 문맥상 정상 |

> 승계값이라 임의 수정 대상이 아니며, 수정 시 참조 페이지(4282541727)와 값이 갈린다.

### 수동 발견 5건 — 판정 및 조치

| # | 발견 | 판정 | 조치 |
|---|---|---|---|
| 1 | **`desc`의 ja_JP·zh_TW에 `{day}` 단일 중괄호**(다른 변수는 `{{ }}`) | **원본 결함(참조 페이지도 동일)** | 승계 원칙에 따라 **값은 그대로 반영**하고 (d-2)로 보고 — 수정 승인 시 두 페이지 동시 패치 |
| 2 | Figma `100% 당첨! … JPYC **획득** 기회` ↔ 승계값 `… JPYC **당첨** 기회` | **원문 불일치** | 값은 참조 페이지 기준 유지, 디자이너에게 Figma 문구 확인 요청 |
| 3 | Figma 목업 `hee12*** 님이 4**USDT** 받았어요!` ↔ 값 `{{0}}님이 {{1}}**JPYC** 받았어요!` | **목업 통화 불일치** | 값 유지(JPYC 캠페인). Figma 목업 데이터 정정 요청 |
| 4 | Figma `100 JPYC에 당첨됐어요! **🎉**` ↔ 값 `{{0}} JPYC에 당첨됐어요`(이모지·느낌표 없음) | **원문 불일치** | 값 유지. 이모지 노출 여부 확인 필요 |
| 5 | 코멘트 답글에 `XLT key = jpyc_banner_jackpot`(프리픽스 누락) | **코멘트 오기** | root 코멘트의 `UF_home_jpyc_banner_jackpot` 채택 |

**통과 판정**: **P0 = 0건**(자동 + 수동). 수동 발견 5건은 값 변경이 아니라 원본 확인 요청 사안 → 위키 반영·엑셀 생성 진행.

---

## 한국어 원문 교정(1a) 및 alias 매핑

이번 작업은 **신규 번역이 아니라 기존 확정값 승계**라 한국어 원문 교정 대상이 없다(교정하면 참조 페이지와 값이 갈린다). 대신 Figma 원문과 승계값의 차이를 **alias 성격으로 아래에 기록**하고 디자이너 확인 대상으로 보고한다.

| Figma 원문 | 위키 반영 값(참조 페이지) | 처리 |
|---|---|---|
| `100% 당첨! 최대 15만엔⁠ 상당의 JPYC 획득 기회` | `100% 당첨! 최대 15만엔\n상당의 JPYC 당첨 기회` | 값 유지 · Figma 확인 요청 |
| `hee12*** 님이 4USDT 받았어요!`(목업) | `{{0}}님이 {{1}}JPYC 받았어요!` | 값 유지 · 목업 통화 정정 요청 |
| `100 JPYC에 당첨됐어요! 🎉` | `{{0}} JPYC에 당첨됐어요` | 값 유지 · 이모지 노출 여부 확인 |

---

## (d) 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

### (d-1) 용어집 보완 권장

이번 5키에서 새로 반복되는 미등재 도메인 용어는 없다(`JPYC`는 예외 등재, `가입`·`당첨`은 등재 완료). **추가 권장 없음.**
다만 자동 P1 7건 중 4건이 `당첨`·`최대`의 **단일 매핑 오탐**으로, 기존에 제안한 검증기 **최장일치·문맥 매핑 개선**(`md/glossary-changelog.md` v3.9·v4.0 기재)이 해결책이다.

### (d-2) 추가 개선·제안 안내

1. **`{day}` 단일 중괄호 수정 권장(우선순위 높음)** — `UF_floating_jpyc_banner_desc`의 ja_JP·zh_TW. 현재 값 그대로면 런타임에 `{day}`가 **문자 그대로 노출**된다. 승인 시 이 페이지와 참조 페이지(4282541727) 양쪽을 같은 값으로 패치.
2. **ko 띄어쓰기** — `{{month}}월 {{day}} 에{{fallbackTimezone}}` → `{{month}}월 {{day}}일 {{fallbackTimezone}}에` 형태 검토(원본 수정 필요).
3. **Figma 원문 3건 확인**(위 (c) 2~4번) — 획득/당첨, 목업 USDT/JPYC, 이모지 🎉.
4. **Screen ID 규격** — 이 페이지는 럭키볼 캠페인과 동일하게 **Figma 프레임 이름을 Screen ID로 사용**했다. `md/IA.md` 소문자 Screen ID 체계로 전환하려면 매핑 표 승인이 선행되어야 한다(현행 규칙: 소급 전환 금지).
5. **엑셀 2종 운영** — `..._ALL_...`(전체)과 `..._UIT_...`가 현재 동일 5키다. LV 키가 추가되면 ALL만 커지고 UIT는 5키를 유지하도록 갱신한다.

---

## 산출물

- 위키 4541588845 — Screen `UIT` 영역 신설(프레임 2건, 어노테이션 이미지 2장), 다국어 번역표 5키, 엑셀 2종 첨부, History 행 추가
- `xlt_output_season3_ALL_20260804.xlsx`(전체 5키) · `xlt_output_season3_UIT_20260804.xlsx`(UIT 5키)
- 본 리포트 (`reports/gate/gate_report_season3_uit_5keys.md`)

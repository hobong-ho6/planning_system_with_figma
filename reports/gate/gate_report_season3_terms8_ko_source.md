# 번역 검증 리포트 — 시즌3 `mini_luckyball_terms_*` 8키 (키 단위 패치 · ko-source)

| 항목 | 값 |
|---|---|
| 일자 | 2026-08-07 (세션 #18) |
| 대상 위키 | [JPYC 럭키볼 시즌 3](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4541588845) — 조회 시점 **v77** |
| 모드 | `md/translate.md` **키 단위 번역 패치 (A) ko-source** |
| 대상 키 | `mini_luckyball_terms_` × 8 (`existing_user`·`expiry_event`·`valid_entry`·`keep_follow`·`info_change`·`wallet_jp`·`abuse`·`early_end`) |
| 담당 FE 팀 | **LV** (프리픽스 `mini_` · 치환자 `{0}` — 이 8키는 치환자 없음) |
| 용어집 | 검증 시작 시 라이브 **v4.5**(115 terms · deprecated 13) → 후속 반영으로 **v4.6**(116 terms · deprecated 14) 산출 |
| 검증 대상 파일 | `xlt/xlt_output_season3_ALL_20260804.xlsx` (**전체 57키**) |
| 검증기 커밋 | `425fb3b` |

## 변경의 성격 (3원 대조로 확정)

사용자가 위키 Screen 표의 KR 원문 8건을 새 문구로 교체(v76 → v77)했고, **다국어표·XLT 시스템은 구 문구 그대로**였다. Figma·위키·시스템 3원을 대조한 결과 **v77 위키 문구는 Figma도 시스템도 아닌 제3의 새 문구**다(검토를 거쳐 다듬은 최종본으로 판단).

| 소스 | 상태 |
|---|---|
| Figma `(Promotion) Promotion page` (66022:114909~114945) | 구 문구 + 다수 오류(`OA 팔로우`·`월렛 개설`·nbsp) |
| 위키 v77 Screen 표 | **새 원문** (이번 패치의 소스) |
| 위키 다국어표 · XLT 시스템 `Dapp Portal` v2.6.0 | 구 문구 (= v76) |

---

## 1단계: 한국어 원문 교정 (1a — 번역 전 필수)

**교정 수행함.** v77 원문에 게이트 1a 대상이 9건 있었다. 기계적 교정은 적용하고, 문구가 눈에 보이게 바뀌는 2건은 **사용자 승인 후** 적용했다.

| # | 키 | 교정 | 근거 | 판정 |
|---|---|---|---|---|
| 1 | `existing_user` | `OA 팔로우` → `LINE 공식 계정 친구 추가` | 용어집 `deprecated_terms` **#1** 확정 금지어("OA는 사내 약어이고 팔로우는 구 표기 — 화면 문구에 쓰지 않는다") + 2026-08-04 사용자 확정 대체 표기 | **사용자 승인 후 적용** |
| 2 | `valid_entry`·`keep_follow` | `Unifi 공식 LINE 계정` → `Unifi LINE 공식 계정` | 어순 정본. 시즌3 다른 키는 모두 `LINE 공식 계정` | **사용자 승인 후 적용**(2키) |
| 3 | `expiry_event` | `자동 소멸 됩니다` → `자동 소멸됩니다` | 띄어쓰기(보조용언 붙여쓰기) | 적용 |
| 4 | `wallet_jp` | `당첨 되더라도` → `당첨되더라도` | 〃 | 적용 |
| 5 | `abuse` | `삭제 되거나` → `삭제되거나` | 〃 | 적용 |
| 6 | `keep_follow` | nbsp(U+00A0) 3개 → 공백 | 비표준 공백 | 적용 |
| 7 | `wallet_jp` | nbsp 1개 → 공백 | 〃 | 적용 |
| 8 | `abuse` | nbsp 1개 → 공백 | 〃 | 적용 |
| 9 | `early_end` | nbsp 3개 → 공백 · 꼬리 공백 제거 | 〃 | 적용 |

**nbsp 합계 8개** — 문서 붙여넣기 흔적(`class="s1"`·`rgb(68,68,68)` 스팬 동반)에서 유입됐다.

### alias 매핑 (Figma 원문 → XLT Key)

교정·갱신으로 **Figma 원문과 달라진 8키 전건**을 기록한다. 프로토타입 단계의 텍스트 매칭이 Figma 원문을 키로 조회하므로 alias 없이는 매핑이 끊긴다.

| Figma 원문 (node) | XLT Key |
|---|---|
| `기존에 Unifi 가입(월렛 개설)과 OA 팔로우를 완료한 유저의 경우…` (66022:114909) | `mini_luckyball_terms_existing_user` |
| `지급된 럭키볼은 이벤트 종료 시까지 사용하지 않을 경우 자동 소멸 됩니다.` (66022:114915) | `mini_luckyball_terms_expiry_event` |
| `본 이벤트는 Unifi 공식 LINE 계정 친구 추가 및 Unifi 가입(지갑 생성 포함)을…` (66022:114920) | `mini_luckyball_terms_valid_entry` |
| `캠페인 참여 기간 동안 Unifi LINE 공식 계정 팔로우를 계속 유지해야 하며…` (66022:114925) | `mini_luckyball_terms_keep_follow` |
| `가입 정보 확인이 불가하거나 정보가 변경된 경우…` (66022:114930) | `mini_luckyball_terms_info_change` |
| `럭키볼 당첨 보상은 … 당첨되어도 최종 검증 결과에 따라보상 지급이…` (66022:114935) | `mini_luckyball_terms_wallet_jp` |
| `어뷰징, Bot 사용, 다중 계정 등 … 사전 통보 없이 내역 삭제 또는…` (66022:114940) | `mini_luckyball_terms_abuse` |
| `본 이벤트는 준비된 보상 소진 시 조기 마감될 수 있으며…` (66022:114945) | `mini_luckyball_terms_early_end` |

### ⚠️ Figma 원문 수정 요청 (디자이너)

Figma 8개 노드가 위키 정본과 어긋난다. 특히 **아래 3건은 확정 규칙 위반**이다.

| 노드 | 문제 | 요청 |
|---|---|---|
| 66022:114909 | **`OA 팔로우`** (용어집 #1 금지어) · **`월렛 개설`** (용어집 **#13** `월렛`→`지갑` 금지어) | `LINE 공식 계정 친구 추가` · `지갑 생성` |
| 66022:114925 | **`공식 계정 팔로우`** (용어집 **#4** 금지어) | `LINE 공식 계정과 친구 상태를 유지` |
| 66022:114935 | `따라보상` **붙어쓰기** + nbsp 5개 | `따라 보상` |
| 66022:114945 | 문장 끝 **마침표 누락** + nbsp 7개 | 마침표 추가 |
| 66022:114742·114833 | `LINE 공식 계정 **채널** 친구 추가` (용어집 **#2** `OA 채널 추가` 계열 구 명칭) | `LINE 공식 계정 친구 추가` — **이번 8키 범위 밖이나 같은 프레임** |

---

## 2단계: 용어집 위반 검증

자동 검증기 2단계 결과 + 수동 판정. **8키에서 용어집 확정 위반(P0) 0건.**

- `deprecated_terms` 13종 대조: 교정 후 **적발 0건** (교정 전 `OA 팔로우` 1건 → 1단계에서 해소)
- `exceptions` 9종(PIN·API·URL·Apple·Google·USDT 등): 해당 용어 미사용 — 위반 0건
- `terminology` 115종: 아래 3단계 표에서 건별 판정

---

## 3단계: 다른 언어 번역 검토

### 재번역 범위 판정

교정 후 ko와 현행 번역을 대조해 **의미가 실제로 바뀐 키만** 다국어를 갱신했다. 의미 동등한 교정(어순·띄어쓰기·조사·금지어 대체)에서 4개 언어를 흔들면 검증된 번역을 이유 없이 바꾸게 된다.

| 키 | ko 변경 | 의미 변화 | 조치 |
|---|---|---|---|
| `existing_user` | `기존에` → `기존` + 금지어 대체 | 없음(ja `すでに`/en `already`/th `เสร็จสิ้นแล้ว`/zh `已完成` 모두 유효) | **ko만** |
| `expiry_event` | 띄어쓰기만 | 없음 — 교정 후 현행과 **완전 일치** | **무변경** |
| `valid_entry` | 어순만 | 없음(4개 언어 이미 `Unifi의 LINE 공식 계정` 어순) | **ko만** |
| `keep_follow` | `친구를`→`친구 상태를`, `해제 시`→`해제할 경우` | 없음(ja `友だち登録を維持`, en `keep … added as a friend`, th `คงบัญชี…เป็นเพื่อน`, zh `持續保留…好友` 모두 "상태 유지") | **ko만** |
| `info_change` | `불가능하거나`→`불가하거나` | 없음 | **ko만** |
| `wallet_jp` | `당첨되어도`→`당첨되더라도` | 없음(ja `当選しても`/en `even if they win`/th `แม้จะถูกรางวัล`/zh `即使中獎`) | **ko만** |
| `abuse` | `통보`→`안내`, `참여 내역`→`내역` | 없음(4개 언어 `prior notice`·`참여 이력` 유지가 더 명확) | **ko만** |
| `early_end` | **`준비된…모두 소진`** 추가 + **`내용이 변경`** 추가 | **있음** | **5개 언어 재번역** |

### `early_end` 신규 번역 (before → after)

| 언어 | after |
|---|---|
| ko_KR | 본 이벤트는 준비된 보상이 모두 소진될 경우 조기 종료될 수 있으며, 운영상의 사정 또는 불가피한 사유로 인해 사전 안내 없이 내용이 변경되거나 종료될 수 있습니다. |
| ja_JP | 本イベントは、用意された報酬がすべてなくなった場合、早期終了することがあります。また、運営上の事情またはやむを得ない事由により、事前の告知なく内容が変更または終了されることがあります。 |
| en_US | This event may end early once all prepared rewards run out, and its content may be changed or the event terminated without prior notice due to operational reasons or unavoidable circumstances. |
| th_TH | กิจกรรมนี้อาจสิ้นสุดก่อนกำหนดเมื่อรางวัลที่จัดเตรียมไว้หมดลงทั้งหมด และเนื้อหาอาจมีการเปลี่ยนแปลงหรือกิจกรรมอาจสิ้นสุดโดยไม่แจ้งให้ทราบล่วงหน้าในกรณีที่มีเหตุผลด้านการดำเนินงานหรือเหตุสุดวิสัย |
| zh_TW | 本活動於準備的獎勵全部發放完畢時可能提前結束；如有營運上的因素或不可抗力事由，可能在不事先公告的情況下變更內容或結束活動。 |

> **`準備的` 확인** — 감사 리포트(`reports/audit/xlt_system_glossary_audit_2026-08-07.md` §1-C)가 형제 키 `unifi_promotion_jpyc_caution_contents9`의 zh에서 **`本活動於準備의獎勵`(한글 조사 `의` 혼입)** 를 확정 위반으로 잡았다. 같은 문형이라 재현 위험이 있어 **한글 포함 여부를 기계 검사**했다 → `False`, `準備的` 정상.

### 수동 3단계 체크표 (8키 × 5언어 = 40셀)

| 점검 항목 | 결과 |
|---|---|
| 빈칸 | 0건 ✓ (40/40 채움) |
| 언어 혼입 / 컬럼 어긋남 (`foreign_script_issues`) | 0건 ✓ |
| nbsp(U+00A0) | 0건 ✓ (교정으로 8개 제거) |
| `/n` 오타 | 0건 ✓ |
| 양끝·중복 공백 | 0건 ✓ |
| placeholder 일치 | 8키 모두 치환자 없음 — 해당 없음 ✓ |
| ko 받침·조사 | `친구 추가**를**`(추가·무받침) · `내역**이**`(역·ㄱ받침) · `보상**이**` · `자격**이**` 전부 정상 ✓ |
| ja 한국어 직역 패턴 | 없음 ✓ — `友だち登録を維持`·`取り消され`·`用意された報酬がすべてなくなった場合` 모두 자연스러운 일본어 |
| zh 번체 정자 | 간체 혼입 0건 ✓ (`準`·`發`·`獎`·`勵`·`結`·`營` 정자 확인) |
| th 마침표 | 8키 모두 문장 끝 마침표 없음 — 태국어 관습 일관 ✓ |
| 한 문장 내 단어 중복 | 없음 ✓ |

### 검증기 보고 전건 처리 판정

**자동 검증 요약** (`xlt/xlt_output_season3_ALL_20260804.xlsx` **전체 57키** 대상, exit **0**)

| 심각도 | 전체 | 8키 관련 |
|---|---|---|
| 🔴 **P0** | **0건** | **0건** |
| 🟡 P1 | 67건 | 12건 |
| 🟢 P2 | 19건 | 8건 |

**8키 관련 P1 12건 — 전건 오탐 판정**

| 키 | 지적 | 실제 값 | 판정 근거 |
|---|---|---|---|
| `existing_user`·`valid_entry`·`wallet_jp`·`info_change` | en `가입` → `Sign up` | `Unifi sign-up`, `sign-up information` | **오탐** — 검증기 en 어미 허용이 **하이픈 형태(`sign-up`)** 를 매칭하지 못한다 |
| `existing_user`·`valid_entry`·`keep_follow` | en `공식 계정` → `Official account` | `the official LINE account` | **오탐** — `official`과 `account` 사이에 **`LINE`이 삽입**돼 연속 부분문자열 매칭 실패. 표기는 정본대로다 |
| `info_change` | en `확인` → `check` | `cannot be verified` | **오탐** — `확인`이 "검증" 문맥이라 `verified`가 정확. **다의어** |
| `info_change` | zh `확인` → `查看` | `無法確認註冊資訊` | **오탐** — `確認`이 정확하고 `查看`(둘러보다)는 문맥 불일치. **다의어** |
| `early_end`·`expiry_event` | en `종료` → `Ended` | `end early`, `before the event ends` | **오탐** — 용어집 `Ended`는 **상태 라벨**, 여기는 동사 문맥 |
| `keep_follow` | ja `취소` → `キャンセル` | `参加資格が取り消され` | **오탐** — 자격 박탈은 `取り消す`가 정확. `キャンセル`는 예약 취소류 |

**8키 관련 P2 8건 — 전건 오탐 판정**: `마침표 스타일` 소수 검출. 유의사항은 완결 문장이라 마침표가 **정상**이다. 검증기는 파일 전체에서 마침표 있는 19건을 "소수"(38건이 없음)로 보는데, 모집단에 버튼·라벨이 섞여 있어 이 판정 자체가 성립하지 않는다.

**8키 밖 P1 55건 · P2 11건**: 이번 패치로 **증감 없음**(패치 전 동일 파일 기준 동일). 전부 기존 키의 기존 오탐이며 세션 #8·#11 게이트에서 이미 오탐 확정된 부류다(`최대`→`Max`, `당첨`→`win`/`中獎`, `공식 계정`→`บัญชีทางการ` 등 용어집 협소 매핑·부분문자열·다의어).

### 전수 점검 범위 명시

- **자동 검증은 전체 파일(57키) 전수** 실행했다 — 8키로 한정하지 않았다.
- **수동 3단계 상세 검토는 변경 8키**를 대상으로 했다(`CLAUDE.md` 게이트 3(b) "부분 작업 시 신규·변경 키"). 나머지 49키는 자동 결과의 증감 0을 근거로 기존 판정을 유지한다.
- **전체 행 수동 재검토가 필요한 시점**: 사용자가 "검증/verify"를 요청하면 그때는 57키 전체 행이 대상이다. 이번은 키 단위 패치이므로 그 범위를 적용하지 않았다.

---

## 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 = 0건 | ✅ 0건 (exit 0) |
| 수동 P0 = 0건 | ✅ 0건 |
| P1 각 건 처리 판정 | ✅ 8키 관련 12건 전건 오탐 판정 + 근거 명시 |
| P2 각 건 처리 판정 | ✅ 8건 전건 오탐 판정 |
| 한국어 원문 교정(1a) + alias | ✅ 9건 교정 · alias 8건 기록 · 디자이너 요청 5건 |
| 무결성 | ✅ 57키 유지 · 빈 값 0 · 컬럼 정렬 가드(1g) 통과 |

**→ 출력·위키 반영·엑셀 생성 진행 가능.**

---

## 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 반영 금지)

### (d-1) 용어집 보완 권장

| # | 용어 | 문제 | 권장 |
|---|---|---|---|
| 1 | **`보상`** | 8키 전부가 ko `보상`을 쓰는데 **용어집 미등재**다. 등재된 것은 `리워드`(ko=`리워드`)뿐이라 `보상`은 검사 대상 밖이다. 실제 번역은 ja `報酬` · en `rewards` · th `รางวัล` · zh `獎勵`로 일관돼 있다 | `보상`을 `리워드`와 같은 5개 언어 매핑으로 등재하거나, ko 원문을 `리워드`로 통일 |
| 2 | **`확인`** | th·zh에서 다의어 오탐이 반복된다(이번 2건 + 감사 리포트 §6-1 동일 지적). th는 `ตรวจสอบ`(verify) 단일 매핑, zh는 `查看`(view) 단일 매핑이라 반대 문맥이 서로 걸린다 | 문맥별 허용값 추가(th에 `ดู`, zh에 `確認`) |
| 3 | **en 하이픈 형태** | `가입`→`Sign up`이 `sign-up`을 매칭 못 해 4건 오탐. 검증기 `_EN_SUFFIXES` 확장이 아니라 **하이픈 정규화**가 필요 | `validate_translation._term_in_text`에서 en 비교 시 `-`→공백 정규화 (⛔ P1 기준선이 바뀌므로 **A/B 실측 후 채택**) |

### (d-2) 원문 개선·표기 통일 제안

| # | 항목 | 내용 |
|---|---|---|
| 1 | **`유저` vs `사용자` 혼용** | 같은 유의사항 8건 안에서 `existing_user`는 **`유저`**, `wallet_jp`는 **`사용자`** 다. 서비스 표기를 하나로 정하는 것을 권장한다(용어집 미등재 항목) |
| 2 | **`mini_luckyball_terms_early_end` ↔ `unifi_promotion_jpyc_caution_contents9` 중복 키** | 두 키의 ko가 거의 같은 문장이다(`본 이벤트는 준비된 보상…조기 마감/종료`). 후자는 zh에 **`準備의`(한글 조사 혼입) 확정 위반**이 있다(감사 리포트 §1-C). ⓐ 한 키로 통합 ⓑ 별개 유지 + 후자 수정 중 결정 필요. **키 삭제는 FE 참조 확인 선행** |
| 3 | **마침표 정책** | 유의사항은 마침표 있음이 맞지만, 파일 전체로는 마침표 유무가 19:38로 섞여 P2가 계속 발생한다. `md/check.md`에 **"문장형=마침표 / 버튼·라벨=없음"** 판정 규칙을 명시하면 이 P2 부류가 정리된다 |
| 4 | **Figma 원문 정리(누적)** | 이번 5건은 위 1단계 표에 있다. `figma-source-issues` 에이전트로 기존 누적 19건과 함께 디자이너 전달 목록을 만들 수 있다 |

### (d-3) XLT 시스템 등록값 갱신 필요 (사용자 액션)

**8키 전부 `Dapp Portal` v2.6.0에 구 문구로 등록돼 있다.** 위키·엑셀만 갱신해도 **FE 실제 노출은 바뀌지 않는다.**

| 키 | 시스템(구) → 신 |
|---|---|
| `existing_user` | `기존에 Unifi 가입…` → `기존 Unifi 가입…` |
| `expiry_event` | 변경 없음 (이미 일치) |
| `valid_entry` | `Unifi 공식 LINE 계정` → `Unifi LINE 공식 계정` |
| `keep_follow` | `친구를 계속 유지…해제(차단/삭제) 시` → `친구 상태를…해제(차단 또는 삭제)할 경우` |
| `info_change` | `불가능하거나` → `불가하거나` |
| `wallet_jp` | `당첨되어도` → `당첨되더라도` |
| `abuse` | `사전 통보 없이 참여 내역 삭제 또는` → `사전 안내 없이 내역이 삭제되거나` |
| `early_end` | **5개 언어 전부 변경** |

→ 생성된 `xlt/xlt_output_season3_LV_20260804.xlsx`(50키) 또는 `ALL_20260804.xlsx`(57키) — 파일명의 날짜는 첨부 정본 파일명이라 고정이고 내용만 갱신된다를 **사용자가 XLT 시스템에 업로드**한다(쓰기 API 없음 — `md/xlt-verify.md`).

---

# 후속 반영 — `유저` → `사용자` 통일 + 용어집 v4.6 (2026-08-07, 사용자 결정)

위 (d-2) 1번 제안(`유저`/`사용자` 혼용)을 사용자가 **「`사용자`로 용어집 등록하고 `사용자`로 사용」** 으로 결정해 반영했다.

## A/B 실측 (등재 전 필수 — 2026-08-05 결정 「등재 판정은 추정이 아니라 실측으로」)

두 축을 분리해 측정했다: ⓐ `terminology`에 `사용자` 등재 ⓑ `deprecated_terms`에 `유저`→`사용자` 추가.

| 모집단 | baseline(v4.5) | term만 | dep만 | 둘다 |
|---|---|---|---|---|
| 시즌3 57키 | P1 **67** | 67 (+0) | 68 (+1) | **68 (+1)** |
| 레지스트리 1,596키 | P1 **1,655** | 1,656 (+1) | 1,660 (+5) | **1,661 (+6)** |

**신규 지적 6건의 건별 판정** (P0·P2는 전 케이스 불변)

| # | 키 | 지적 | 판정 |
|---|---|---|---|
| 1 | `mini_luckyball_terms_existing_user` | ko `유저` → `사용자` | **실제 위반** — 이번에 교정 |
| 2 | `unifi_promotion_jpyc_bonus_all_title` | 〃 (`모든 유저 대상`) | **실제 위반** — 시스템 정리 대상 |
| 3 | `unifi_promotion_jpyc_bonus_gold_title` | 〃 (`100만 JPYC 초과 보유 유저 대상`) | **실제 위반** — 〃 |
| 4 | `unifi_promotion_jpyc_caution_contents2` | 〃 (`참여 유저도`) | **실제 위반** — 〃 |
| 5 | `unifi_promotion_jpyc_reward_info1-1` | 〃 (`보유 유저부터`) | **실제 위반** — 〃 |
| 6 | `trade_swap_slippage_setting_custom_title` | en `사용자` → `user` | **오탐 — 재판정 금지.** ko `사용자 설정`이 관용적으로 `Custom`(사용자 정의)이라 en이 정확하다. terminology 매칭이 다의어를 구분하지 못하는 부류 |

**채택 판정**: 신규 6건 중 **5건이 진짜 위반**이고 오탐은 1건이다. `deprecated_terms`만 넣으면 오탐 0이지만, `terminology` 등재는 5개 언어 정본 표기를 문서·가이드에 노출하는 값이 있어 **둘 다 채택**하고 오탐 1건을 changelog에 명시했다(2026-08-05 `캠페인` 등재 선례와 동일 판단 구조).

## 교정 적용

| 키 | before | after |
|---|---|---|
| `mini_luckyball_terms_existing_user` | …완료한 **유저**의 경우… | …완료한 **사용자**의 경우… |

**4개 언어는 무변경** — ja `ユーザー` · en `Users` · th `ผู้ใช้` · zh `用戶` 로 이미 정본 표기다(zh는 번체 `用戶`가 정본: 레지스트리 21키 사용, 간체 `用户` **0건**).

## 재검증 (용어집 v4.6 기준, 전체 57키)

| | P0 | P1 | P2 | `유저/사용자` 지적 |
|---|---|---|---|---|
| v4.5 (교정 전) | 0 | 67 | 19 | 0건 |
| **v4.6 (교정 후)** | **0** | **67** | **19** | **0건** |

교정으로 `유저` 위반이 해소되고 등재로 인한 신규 오탐이 0이라 **등급 증감 없이 통과**했다. 엑셀 3종 `유저` 잔존 **0건**. `validate_translation.py` exit **0**.

## 부수 발견 (별건 — 이번 범위 밖)

`trade_swap_slippage_setting_custom_title`은 ko `사용자 설정`이 **"Custom(사용자 정의)"** 의 뜻인데 **ja·th·zh가 "user settings"로 오역**돼 있다. en만 `Custom`으로 정확하다.

| 언어 | 값 | 판정 |
|---|---|---|
| ko_KR | `사용자 설정` | 원문(= Custom) |
| en_US | `Custom` | ✅ 정확 |
| ja_JP | `ユーザー設定` | ❌ "사용자의 설정"으로 오역 → `カスタム` 권장 |
| th_TH | `การตั้งค่าผู้ใช้` | ❌ 〃 → `กำหนดเอง` 권장 |
| zh_TW | `設定用戶` | ❌ 오역 + **어순 오류**(`用戶設定`이 문법) → `自訂` 권장 |

`Dapp Portal` 등록값이며 시즌3 범위가 아니다. XLT 검증 흐름(`md/xlt-verify.md`)의 수정 제안 대상으로 남긴다.

## 사용자 액션 (추가)

1. **용어집 v4.6 CMS 반영** — `reports/glossary_v4.6_for_cms.json` 전체 JSON을 Landpress CMS에 붙여넣기.
2. **XLT 시스템 `유저` 잔존 4키 정리** — 위 A/B 판정 #2~#5(`unifi_promotion_jpyc_*`). 시즌3 문서 범위 밖이라 이번 엑셀에 포함되지 않았다.

---

# 후속 2 — `(Unifi mini) - Login x` 프레임 교체 (2026-08-07)

사용자가 Figma `66866:4278` URL을 주며 「기획서에 프레임 업데이트」를 요청했다. 위키에 등록된 것은 **구 node `66089:125866`** 이었다.

## 프레임 교체 판정 (구 ↔ 신 텍스트 diff)

| | 값 |
|---|---|
| 구 `66089:125866` | TEXT 50개 — Unifi 지갑·미션·FAQ 중심 홈 |
| 신 `66866:4278` | TEXT 54개 — **K-Pick 큐레이션 홈**(한국여행 바우처·K-뷰티 시술) |
| **공통 텍스트** | **5건뿐** — 사실상 전면 재설계 |
| Figma 코멘트 | **0건** — XLT 마커·정책 근거 없음 |

**사용자 결정**: ⓐ 시즌3 행 교체(이미지·XLT 갱신) ⓑ 파트너 콘텐츠(클리닉명·시술명·바우처 브랜드) **XLT 미부여** ⓒ orphan UIT 2키 **번역표 유지** ⓓ Description은 화면 설명만 갱신.

## Step 2-1 유사 키 대조 — 신규 키 0건

한글 텍스트 32건을 `Dapp Portal` v2.6.0 + `Unifi` v1.6.6 양쪽에 조회했다(⛔ 병합하지 않고 서비스별).

| 분류 | 건수 |
|---|---|
| 기존 키 정확 일치 | 9 |
| 유사 ≥0.80 | 6 |
| 미등록 — 파트너 콘텐츠(XLT 미부여) | 14 |
| 미등록 — Unifi 관련 | 3 (`JPYC 첫 충전 혜택`·`캐시백 10%`·`10%`) |

**배너 영역 2슬라이드가 이 화면의 XLT 범위**다(기존 행도 배너 2키만 담았다). 둘 다 **기존 키와 정규화 동일(1.0)** 이어서 **신규 키 생성이 불필요**했다.

| Figma 노드 | 텍스트 | 재사용 키 | 판정 |
|---|---|---|---|
| 66866:4527 + 4528 (2노드) | `데일리 미션하고` + `JPYC 받기` | `mini_guidekim_daily_mission` (`데일리 미션하고 JPYC 받기`) | **정규화 동일 1.0** |
| 66866:4583 | `100% 당첨되는\nJPYC 럭키볼` | `mini_guidekim_luckyball_banner` | **정규화 동일 1.0** |

`mini_guidekim_luckyball_banner`는 이미 번역표에 있고, `mini_guidekim_daily_mission`만 **번역표에 없어 추가**했다. 값은 **XLT 시스템 등록값을 승계**했다(2026-08-04 결정 「기존 키 값의 정본 = XLT 시스템」) — 새로 번역하지 않았다.

## 담당 팀이 바뀐다 (보고)

기존 행은 `UF_home_skyflag_title`·`UF_home_daily_mission_title_MINI`(**UIT**)를 썼는데, 신 프레임 배너는 `mini_guidekim_*`(**LV**)다. 같은 화면의 배너 렌더 주체가 UIT 홈 컴포넌트에서 LV Guide Kim 홈으로 바뀐 것으로 보인다. **FE 확인 권장.**

## 수동 3단계 — 신규 키 `mini_guidekim_daily_mission`

| 단계 | 점검 | 결과 |
|---|---|---|
| 1 한국어 | nbsp·`/n`·양끝/중복 공백 | 0건 ✓ |
| 1 한국어 | `미션하고` 목적어 누락 | HANDOFF 「`UF_home_skyflag_title` ko는 **유보**」(2026-08-05 사용자 결정, 시스템 등록값이라 건드리지 않음)와 동형이라 **동일 판단 적용 — 등록값 그대로** |
| 2 용어집 v4.6 | `deprecated_terms` 14종 | 적발 0건 ✓ |
| 2 용어집 v4.6 | `terminology` `미션` → ja `ミッション`·en `mission`·th `ภารกิจ`·zh `任務` | 4개 언어 전부 포함 ✓ |
| 3 다국어 | 이질 문자체계 | 0건 ✓ |
| 3 다국어 | placeholder | 전 언어 없음(일치) ✓ |
| 3 다국어 | zh 간체 혼입 | 0건 ✓ |
| 3 다국어 | th 마침표 | 없음(관습 일관) ✓ |

**자동 검증 (전체 58키, 용어집 v4.6)**: P0 **0** · P1 **67** · P2 **19** — **신규 키 1건 추가에도 등급 증감 0**. exit **0**.

## 어노테이션

`assets/screens/s3_LV_mini_Login_x_66866-4278.png`(750×1624, 2x) + XLT 핀 2개(파랑, 매칭 텍스트 좌측 10pt). `collect_frames.annotate()` 재사용 — 겹침 보고 **없음**, **육안 검증 완료**(핀 ①·② 모두 배너 문구 좌측, 글자 미가림).

Figma 코멘트가 0건이라 정책 핀(빨강)은 없다. 좌표 기반으로 XLT 핀만 직접 렌더했다(HANDOFF 「코멘트가 없는 프레임은 좌표 기반으로 직접 핀을 렌더」).

## 추가 개선·제안 (권장 — 임의 반영 금지)

| # | 항목 | 내용 |
|---|---|---|
| 1 | **`mini_guidekim_daily_mission` ja에 「데일리」 누락** | ko `**데일리** 미션하고`인데 ja는 `ミッションでJPYCゲット`로 **"매일"이 없다**. en `Daily Missions`·th `ภารกิจประจำวัน`·zh `每日任務`는 모두 daily를 담았다. 또 ja `ゲット`는 캐주얼체라 다른 언어와 톤이 갈린다. → `毎日ミッションでJPYCを獲得` 계열 권장. **등록값이라 보고만 한다**(`md/guide.md:228`) |
| 2 | **Description 정책 상세 부재** | 신 프레임에 Figma 코멘트가 **0건**이라 배너 2개의 노출 조건·이동 동작을 문서화할 근거가 없다. 화면 설명만 갱신했고 정책은 「선택 시 프로모션 페이지로 이동」 2건으로 조정했다. **디자이너에게 정책 코멘트 요청 필요** |
| 3 | **파트너 콘텐츠 14건의 관리 주체** | 클리닉명(`델픽의원`·`밀리클리닉 도산`·`청담봄온의원`)·시술 설명·바우처 브랜드(`올리브영`·`무신사`·`GS25`·`다이소`)·수치(`캐시백 10%`·`¥4,500`)는 XLT 미부여로 결정됐다. **어디서 관리되는지(CMS·상품 DB)를 문서에 명시**하면 다음 세션이 다시 판단하지 않는다 |
| 4 | **`JPYC 첫 충전 혜택`·`캐시백 10%` 미등록** | Unifi 관련인데 등록값이 없다. `캐시백 10%`는 8곳 반복되고 수치가 파트너별로 다를 수 있어 **`캐시백 {0}%` 변수화 후보**다. 시즌3 배너 범위 밖이라 이번에 키를 부여하지 않았다 |
| 5 | **orphan UIT 2키** | 사용자 결정으로 번역표에 **유지**했다(엑셀 UIT 7키 유지). Screen 표에서는 빠져 화면 정의와 번역표가 어긋나므로, 감사 시 「orphan」으로 계속 잡힌다는 점을 기록해 둔다 |

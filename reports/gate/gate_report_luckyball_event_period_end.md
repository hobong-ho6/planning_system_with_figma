# 게이트 리포트 — `mini_luckyball_event_period_end` 신규 키 번역

- **대상**: 럭키볼 친구에게 선물하기 캠페인 위키(pageId 4479306980, 조회 시점 **v137**)
- **작업 유형**: 신규 키 1건(노란 셀 표시분) 5개 언어 번역 → 전역 다국어 표·LV 엑셀 반영 → 셀 하이라이트 원복
- **담당 FE 팀**: **LV** (위키 Screen 섹션에 UIT/LV h3 구분 존재 → 대상 키는 LV `초대자 프로모션 페이지` 영역 → 프리픽스 `mini_luckyball_`, 치환자 `{0}`. 이 키는 치환자 없음)
- **검증 대상 파일**: `xlt_output_LV_20260727.xlsx` (라이브 첨부 v13 다운로드 후 1행 삽입 → **74키**)
- **용어집**: 라이브 재조회 **v3.8 / 112 terms** (`scripts/fetch_glossary.py`, 캐시 미사용)
- **원본 재조회**: 위키 storage·첨부 엑셀·Figma 노드(63411-25034)·Figma 코멘트(763건)·용어집 — 전부 이번 실행에서 새로 조회

---

## 1a. 한국어 원문 교정 (번역 전 필수)

| 구분 | 문구 |
|---|---|
| 위키 입력 원문(사용자 기입) | `종료 된 이벤트입니다.` |
| **교정 후 = XLT ko_KR 정본** | **`종료된 이벤트입니다.`** |
| 근거 | `종료되다`는 파생 동사 → 관형형 어미 `-된`은 **붙여 쓴다**(`종료 된` ✗). 한글 맞춤법 제47항 관련 보조용언 띄어쓰기 대상이 아니라 파생어 활용형 |

- **alias 기록**: `{위키 입력 원문 "종료 된 이벤트입니다." → XLT Key mini_luckyball_event_period_end "종료된 이벤트입니다."}`
- **Figma 원본 확인**: 파일 `GOCHAYBS7hIrmWRGNuJOKV` 노드 `63411-25034`(초대자 화면 전체) 텍스트 35건 및 파일 코멘트 763건 전수 조회 결과 **해당 문구가 Figma에 존재하지 않음**(사용자가 위키 Screen 표에 직접 추가한 신규 문구). 따라서 디자이너 원본 수정 요청 대상 아님 — 다만 화면에 반영될 때 `종료된`(붙여쓰기) 표기를 쓰도록 안내 필요.
- 교정 결과를 **위키 Screen 표 KR 셀에도 함께 반영**(원문/정본 불일치 방지).

---

## 2. 번역 결과 (5개 언어)

| XLT Key | ko_KR | ja_JP | en_US | th_TH | zh_TW |
|---|---|---|---|---|---|
| `mini_luckyball_event_period_end` | 종료된 이벤트입니다. | 終了したイベントです。 | This event has ended. | กิจกรรมนี้สิ้นสุดแล้ว | 本活動已結束。 |

**번역 근거(기존 표 일관성)**
- `이벤트` = ja `イベント` / en `event` / th `กิจกรรม` / zh `活動` — 같은 파일 `mini_luckyball_event_period`, `mini_luckyball_jp_only`와 동일 대응.
- 문형은 `mini_luckyball_jp_only`(`일본 사용자 대상 이벤트입니다.` → `日本のユーザー向けのイベントです。` / `This event is for users in Japan.` / `กิจกรรมนี้สำหรับผู้ใช้ในญี่ปุ่น` / `本活動為日本用戶專屬活動。`)와 동일 패턴을 따랐다.
- 마침표: ko·ja·en·zh는 마침표, **th_TH는 관습상 마침표 없음**(`md/check.md` 3-C) — 파일 전체 관행과 일치.

---

## 3. 검증 리포트

### (a) 자동 검증 요약 — `scripts/validate_translation.py` 실제 실행

| 심각도 | 건수 | 비고 |
|---|---|---|
| 🔴 **P0** | **0건** | ✅ 통과 기준 충족 (빈칸·오타·언어 혼입·용어집 표기 위반 없음) |
| 🟡 P1 | 103건 | 전건 기존 데이터 누적 오탐(용어집 협소 매핑) — 신규 키 관련 **1건** |
| 🟢 P2 | 27건 | 전건 마침표 스타일 오탐 — 신규 키 관련 **1건** |

### (b) 수동 3단계 체크표

**검토 범위**: 이번은 부분 작업(신규 1키)이지만 `ko_KR`·`en_US`는 **전체 74행을 한 줄씩 전수 점검**했다(신규 키 한정 금지 규칙). 아래 신규·변경 키 판정 + 전수 점검에서 발견한 기존 키 항목은 (d)에 별도 기재.

| 단계 | 점검 항목 | 결과 |
|---|---|---|
| **1단계** 한국어 맞춤법·띄어쓰기 | `종료 된` → `종료된` 파생어 붙여쓰기 | ⚠️→✅ **교정 완료**(1a) |
| **1단계** | 문장 종결·경어체 일관성(`~입니다`체, 파일 내 안내문 다수와 동일) | ✅ |
| **1단계** | 단어 붙음/띄어쓰기 누락(`누리고다양한`류), 조사·받침 판정 | ✅ 해당 없음 |
| **2단계** 용어집 위반 | `이벤트`(미등재) → 파일 내 기존 대응 `イベント`/`event`/`กิจกรรม`/`活動` 준수 | ✅ |
| **2단계** | `종료` 등재값 대조 — ja `終了` ✅ / th `สิ้นสุด` ✅ / en `Ended`→`has ended`(활용형) ✅ / **zh_TW `結尾` 미사용 → `已結束` 사용** | ⚠️ (c)에서 오탐 판정 + (d-1) 용어집 보완 권장 |
| **2단계** | 예외 패턴·zh_TW 한자 변형(번체 정자)·th 자산 표기 | ✅ `本活動已結束` 전부 번체 정자 |
| **3단계** 타 언어 검토 | 외래어 음차 오염(`포이가츠`류)·이질 문자체계 혼입 | ✅ 없음 |
| **3단계** | placeholder 일치(이 키는 치환자 없음 — 5개 언어 모두 없음) | ✅ |
| **3단계** | 자연스러움 — ja `終了したイベントです。`(일본 UI 관용), en `This event has ended.`, th `กิจกรรมนี้สิ้นสุดแล้ว`(이 활동은 끝났습니다), zh `本活動已結束。` | ✅ |
| **3단계** | 마침표 일관성(ko·ja·en·zh 유·th 무) / 구분자·가운뎃점 | ✅ 해당 없음(구분자 미사용) |
| **3단계** | 엑셀 무결성 — 헤더 `en_US, ko_KR, ja_JP, zh_TW, th_TH` 컬럼 순서 유지, `plurals` 시트 보존, 키 중복 없음, 73→74행 | ✅ |

### (c) 자동 검증기 보고 전건 처리 판정

| # | 심각도 | 건 | 판정 | 사유 |
|---|---|---|---|---|
| 1 | P1 | `mini_luckyball_event_period_end` — zh_TW `종료` → `結尾` 권장 | **오탐(수정 안 함)** | 용어집 등재값 `結尾`는 *글·이야기의 결말*을 뜻하는 명사로 **활동 종료 상태에는 쓰지 않는다**. 대만 번체 관용 표기는 `已結束`. 등재값이 문맥에 좁게 잡힌 케이스 → (d-1) 용어집 보완 권장 |
| 2 | P2 | `mini_luckyball_event_period_end` — 마침표 있음(소수 스타일) | **오탐(수정 안 함)** | 검증기가 UI 라벨(버튼·수치 등 마침표 없는 단문)까지 같은 분모에 넣어 문장형을 "소수"로 계산. 파일 내 **문장형 27건 전부 마침표 사용**이므로 신규 키의 마침표가 오히려 일관 |
| 3 | P1 | 기존 키 102건 (`초대`→`เชิญ`, `당첨`→`ถูกรางวัล`, `최대`→`Max`, `확인`→`check`, `유의사항`→`Cautions` 등) | **전건 오탐 — 이전 판정 유지** | 용어집 1:1 매핑이 좁아 정상 의역·문맥 표현을 위반으로 오검출(누적 판정, HANDOFF 기록과 동일). 이번 작업에서 해당 셀 미변경 |
| 4 | P2 | 기존 키 26건 (마침표 스타일) | **전건 오탐 — 이전 판정 유지** | #2와 동일 원인 |

**통과 판정**: P0 = 0건(자동 + 수동) → 위키 반영·엑셀 재첨부 진행 가능.

### (d) 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

**(d-1) 용어집 보완 권장 — `종료` zh_TW 등재값 교정**

| 용어 | 현재 등재(v3.8) | 권장 | 사유 |
|---|---|---|---|
| **종료** | zh_TW `結尾` | **`結束`** | `結尾`=글·이야기의 *결말*(문장 구성 용어). 서비스·활동·이벤트의 종료는 대만 번체에서 `結束`(활용: `已結束`). 현재 등재값 때문에 정상 번역이 P1로 오탐되고, 그대로 따르면 오역(`活動結尾` ✗) |

- 5개 언어 권장 표기: ko `종료` / ja `終了` / en `Ended` / th `สิ้นสุด` / **zh_TW `結束`** (zh만 변경)
- 용어집은 **API 읽기 전용**이라 Claude가 직접 쓸 수 없다 — 승인 시 `md/landpress.md` 절차로 **전체 JSON(v3.9)** 을 산출해 전달하고, 사용자가 Landpress CMS에 붙여넣어 반영한다. 이 경우 **기획자 가이드 zip도 같은 작업에서 함께 갱신·전달**(`md/landpress.md` §5-1, 게시는 사용자 수행).

**(d-2) 전수 점검에서 발견한 기존 키 개선점(이번 작업 범위 외 — 미적용, 승인 시 별건 처리)**

| 키 | 현재 ko_KR | 권장 | 사유 |
|---|---|---|---|
| `mini_luckyball_login_btn` | `로그인 하기` | `로그인하기` | `로그인하다` 파생 동사 → 붙여쓰기 |
| `mini_luckyball_benefits_desc` | `게임 미션을 플레이 하면서` | `플레이하면서` | 위와 동일(`플레이하다`) |
| `signup_line` / `keep_channel` / `try_tomorrow` vs `signup_unifi` / `need_follow_to_draw` | `가입해주세요`·`유지해주세요`·`시도해주세요` ↔ `가입해 주세요`·`유지해 주세요` | 한쪽으로 통일 | 보조용언 `주세요`는 붙임·띄움 모두 허용이나 **파일 내 혼용**. 표기 정책 결정 필요 |

**(d-3) 원문 관리 제안**
- 이 문구는 Figma에 존재하지 않는다(위키에서 신설). 화면에 반영될 때 Figma 텍스트도 `종료된 이벤트입니다.`(붙여쓰기)로 넣도록 디자이너에게 전달 권장.
- `No` 컬럼이 `mini_luckyball_event_period`와 같은 `5`다 — 동일 위치의 **상태 분기 문구**로 보이며 의도된 부여로 판단해 그대로 두었다. 별도 어노테이션 번호가 필요하면 알려주기 바란다(이미지 재어노테이션 대상).

---

## 4. 반영 산출물

- `xlt/xlt_output_LV_20260727.xlsx` — 73행 → **74행**(라이브 첨부 v13 기준 1행 삽입, `plurals` 시트·컬럼 순서·서식 보존)
- 위키 전역 `다국어 번역 (XLT Full Translation)` 표 — 73키 → **74키**(`event_period` 바로 뒤 삽입), 첨부 링크 라벨 `LV · 74키`
- 위키 Screen 표(초대자 프로모션 페이지 / 초대자 화면 전체 63411-25034) — 노란 하이라이트 **원복**, KR 셀 `종료 된` → `종료된` 교정
- History 표 `2026-07-28` **기존 행에 `<li>` 추가**(같은 날 1행 병합 규칙)

# 번역 품질 게이트 — 비로그인 좋아요 토스트 1키 (2026-09-16)

- **대상**: `mini_voucher_detail_favorite_login_toast` **신규 1키** (LV · 프리픽스 `mini_` · 치환자 없음 · **Dapp Portal**)
- **근거**: Slack `C07R7CE0B5H` / [UNIFY-11340](https://jira.workers-hub.com/browse/UNIFY-11340) — 바우처 상세는 비로그인으로 열람 가능하지만 **좋아요는 GuideKim 찜하기 연동이라 로그인 필수**. 비로그인 상태에서 버튼을 누르면 **토스트**를 띄우기로 확정(사용자)
- **검토 범위**: 신규 1키 **전체 행 5셀 전수**(부분 검토 아님)

## (1a) 한국어 원문 교정

| 원문(요청) | 등록 문구 | 판정 |
|---|---|---|
| `로그인이 필요합니다.` | 동일 | **교정 없음** — 맞춤법·띄어쓰기·마침표 모두 정상. **alias 없음** |

## Step 2-1 유사 키 검색 (재사용 검토 · 실측)

Unifi 2,589키 · Dapp Portal 1,701키에서 `로그인` 포함 키를 전수 조회했다. **재사용 가능한 키 없음**:

| 후보 | 값 | 재사용 불가 사유 |
|---|---|---|
| `UF_common_error_401_desc` | 로그인이 필요한 페이지입니다.\n로그인하여 Unifi 서비스를 이용해 보세요. | **페이지 단위 2줄 안내** — 토스트 길이가 아니다 |
| `UF_common_error_401_title` | 로그인 후 이용 안내 | 제목 문구 |
| `notlogin_popup_desc`(DP) | 구매/판매 진행을 위해 로그인을 해주세요. | **구매/판매 문맥 고정** |
| `my_beforelogin_title2`(DP) | 로그인을 해주세요 | 마이 화면 타이틀 |

→ **신규 키**로 정의. 이름은 같은 화면의 기존 토스트 키 **`mini_voucher_detail_favorite_toast`**(DP 등록 · `즐겨찾기 완료! 목록에서 확인해보세요`) 네이밍을 따랐다.

## (a) 자동 검증 요약 — `validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | 1건 |
| 🟢 P2 | 0건 |

## (b) 수동 3단계 체크표 (전체 행 5셀)

**1단계 — 한국어 원문**: `로그인이 필요합니다.` — 맞춤법·조사·띄어쓰기 정상 · 음차 오염 없음 · 마침표 있음(토스트 선례와 일치) ✅

**2단계 — 용어집 대조**

| 용어 | 용어집 | 적용 | 결과 |
|---|---|---|---|
| 로그인 | en `log in`(동사형) · ja `ログイン` · th `เข้าสู่ระบบ` · zh `登入` | ja·th·zh 그대로 | ✅ / en은 (c) 참조 |

**3단계 — 다국어 의미·표기** (기준 선례 = 같은 화면 토스트 `UF_clinic_detail_location_copy_toast` = `복사되었습니다.` / `コピーしました。` / `Copied.` / `คัดลอกแล้ว` / `複製成功`)

| 언어 | 값 | 판정 근거 |
|---|---|---|
| ja_JP | `ログインが必要です。` | 선례와 동일하게 **마침표 유지** · 등록값 `ログインが必要です` 계열과 일치 | 
| en_US | `Login required.` | 등록 선례 **`Login Required`(2건)** · 토스트는 `Copied.`처럼 **마침표 유지** |
| th_TH | `จำเป็นต้องเข้าสู่ระบบ` | **등록값 그대로**(`UF_common_error_401_title` th) · 태국어 토스트는 **마침표 없음**(선례 `คัดลอกแล้ว`) |
| zh_TW | `需要登入` | `需要登入` **3건** 실측 · 번체 정자 · 중국어 토스트는 **마침표 없음**(선례 `複製成功`) |

- 변수·치환자 없음(LV 규칙 `{0}` 해당 없음) · 이질 문자체계 혼입 0 · 줄바꿈 없음.

## (c) 자동 검증 각 건 처리 판정

| # | 심각도 | 내용 | 처리 판정 |
|---|---|---|---|
| 1 | P1 | en_US: 용어집 `로그인 → log in`(동사형) 권장인데 `Login required.`를 썼다 | **오탐** — 용어집 매핑이 **동사형만** 담고 있어 **명사형 상태 표현**을 위반으로 잡는다. 등록 선례가 **`Login Required` 2건**으로 명사형이고, 동사형(`You need to log in.`)은 토스트로 길다. 현행 유지 → (d-1)로 올린다 |

**P0 = 0건 (자동 + 수동 모두)** → 엑셀 생성·위키 반영 진행 가능.

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

**(d-1) 용어집 보완 권장 → `md/glossary-backlog.md` 대기 목록 등재**

| 용어 | 유형 | 권장 | 실측 |
|---|---|---|---|
| 로그인(명사형) | ⓑ 반복 오탐 | en `Login` **병기**(현행 `log in` 동사형 단독) | 등록값에 명사형 `Login Required` **2건** · 이번 검증에서 정상 번역이 P1로 잡혔다 |

**(d-2) 추가 개선·제안 안내**

1. **적용 화면 범위 확인 필요** — 이번 키는 **바우처 상세**의 좋아요 기준으로 정의했다. **클리닉 상세에도 좋아요가 있으면 같은 키를 재사용**하면 된다(문구가 화면 독립적이라 재사용에 문제 없다).
2. **토스트 이후 동선은 정의돼 있지 않다** — 「로그인이 필요합니다.」만 띄우고 끝낼지, **로그인 유도(버튼·이동)** 까지 붙일지는 미정이다. 지금은 **안내만**으로 반영했다.
3. **테스트 제약** — 좋아요는 GuideKim 계정 연동 문제로 **preview 환경에서만 테스트 가능**하다(주준영님 확인). QA 전달 시 함께 알리는 것이 좋다.

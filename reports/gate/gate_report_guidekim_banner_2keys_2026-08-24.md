# 번역 품질 게이트 리포트 — GuideKim 배너 2키 문구 교체

- **작업 유형**: 키 단위 번역 패치 모드 (A) ko-source — 한국어 원문 변경 → 5개 언어 전면 재번역
- **대상 키 (2건)**:
  - `mini_guidekim_kbeauty_to_kculture` ← `최대 15% 캐시백!`
  - `mini_guidekim_all_korea_experience` ← `올리브영, 다이소 바우처부터 뷰티케어 시술까지`
- **담당 FE 팀**: **LV** (`mini_` 프리픽스 · 치환자 `{0}`) — 기존 키 프리픽스로 판별, 사용자 문의 불요
- **대상 서비스 / 버전**: `Unifi / WEB BROWSER / v1.6.7.1`(2,143키) + `Dapp Portal / WEB BROWSER / v2.6.1`(1,599키)
- **등록값 조회 시각**: **2026-08-24 15:34 KST**(키 분리 확정 후 재조회 — 캐시 금지 규칙)
- **용어집 버전**: v4.7 (116개 용어, API 조회)
- **검토 범위**: **전수 점검** — 대상 파일 전체 행(2키 × 5개 언어 = 10셀 전부)을 한 줄씩 수동 검토. 신규·변경 키로 한정하지 않음(대상 파일이 변경 키 2건으로만 구성됨).
- **산출물 엑셀**: `xlt/registry_fix/xlt_fix_guidekim_banner_2keys_20260824.xlsx`

> **개정 이력 (3판)**
> 1. **초판** — 두 문장을 `mini_guidekim_kbeauty_to_kculture` **1키 2행**(개행 포함)으로 작성, 용어는 `상품권`.
> 2. **2판** — 2차 독립 검토가 「서비스 내 `바우처` 계열과의 용어 분기」를 P2로 제기 → **사용자가 `바우처` 통일 결정**, 5개 언어 재작성.
> 3. **3판(현행)** — 사용자 정정: **원래 2개 키였다.** 문장을 키별로 분리하고 각 값의 **개행을 제거**(각 1행). 용어는 `바우처` 유지(사용자 재확인). 엑셀·검증·리포트 전부 재실행.
>
> 폐기된 1키 통합본 엑셀(`xlt_fix_guidekim_kbeauty_to_kculture_20260824.xlsx`)과 그 자동 검증 리포트는 **오업로드를 막기 위해 삭제**했다(`md/xlt-verify.md` §6 — 잘못된 파일 업로드 위험).

---

## 0. 변경 내용 (before → after)

### 키 ① `mini_guidekim_kbeauty_to_kculture` — 양 서비스 **등록됨**(값 교체)

| 언어 | before (등록값) | after |
|---|---|---|
| ko_KR | K-뷰티부터 K-컬처까지 | 최대 15% 캐시백! |
| ja_JP | (Unifi) 韓国肌管理も、Kカルチャーも！<br>(Dapp) K-美容もカルチャーも！ | 最大15%キャッシュバック！ |
| en_US | From K-beauty to K-culture | Up to 15% cashback! |
| th_TH | ตั้งแต่ K-Beauty ถึง K-Culture | เงินคืนสูงสุด 15%! |
| zh_TW | 從 K-Beauty 到 K-Culture | 最高享 15% 現金回饋！ |

### 키 ② `mini_guidekim_all_korea_experience` — ⚠️ **Dapp Portal만 등록 / Unifi 미등록**

| 언어 | before (Dapp Portal 등록값) | after |
|---|---|---|
| ko_KR | 한국 여행의 모든 경험은 Unifi에서 | 올리브영, 다이소 바우처부터 뷰티케어 시술까지 |
| ja_JP | 韓国旅行を、Unifiひとつで | オリーブヤング、ダイソーのバウチャーからビューティーケア施術まで |
| en_US | All Things Korea, Unifi | From OLIVE YOUNG and DAISO vouchers to beauty care treatments |
| th_TH | ทุกประสบการณ์เที่ยวเกาหลีที่ Unifi | ตั้งแต่เวาเชอร์ OLIVE YOUNG และ DAISO ไปจนถึงทรีตเมนต์ความงาม |
| zh_TW | 韓國旅行的所有體驗，盡在 Unifi | 從 OLIVE YOUNG、DAISO 優惠券到美容療程 |

> ⚠️ **Unifi에는 이 키가 없다**(2026-08-24 15:34 실측 — Unifi `mini_` 계열은 14키뿐이고 이 키는 미포함). 따라서 Unifi 업로드는 **기존 값 교체가 아니라 신규 등록**이 된다. 화면이 Unifi 쪽에서도 이 키를 참조한다면 반드시 함께 올려야 하고, 참조하지 않는다면 Dapp Portal만 올려도 된다 → **§5 (d-2) #12 확인 필요.**

- 두 키 모두 값에 **개행 없음**(각 1행). 1키 2행 구조에서 2키 1행 구조로 바뀌었다.
- **부수 효과(의도된 정합 개선)**: 키 ①의 `ja_JP`는 두 서비스에서 값이 갈려 있었고(위키 `JP UX 피드백` pageId=4611882417에 이슈로 등록됨), 이번 교체로 **양쪽 동일 값으로 통일**된다.

---

## 1. 한국어 원문 교정 (게이트 1a — 필수)

사용자 제공 원문에 교정 2건을 적용했다.

| 구분 | 내용 |
|---|---|
| 제공 원문 | `최대 15% 캐시백!` / `올리브영, 다이소 상품권 부터 뷰티케어 시술까지` |
| **교정 ①(맞춤법)** | `상품권 부터` → **붙여쓰기** — 보조사 `부터`는 앞말에 붙여 쓴다(한글 맞춤법 제41항 「조사는 그 앞말에 붙여 쓴다」). 기존 등록값 선례도 전부 붙임: `K-뷰티부터`, `출석 체크부터`, `1 JPYC부터`, `오늘부터` |
| **교정 ②(용어 통일 — 사용자 결정 2건)** | `상품권` → **`바우처`**. 근거: `md/IA.md`:373이 **같은 상품**(올리브영 ₩1만·3만·5만 / 다이소 ₩3천·1만·3만 / CU / 이마트 = 12종)을 **`바우처`** 로 기재하고, 카테고리 라벨 키 `mini_guidekim_cat_voucher`도 ko `바우처`다. 배너 → 카테고리 이동 시 단어가 바뀌는 UX를 제거한다. ※ 3판 정정 메시지에 `상품권부터`가 다시 등장해 **사용자에게 재확인 후 `바우처` 유지 확정** |
| 확정 원문 | `최대 15% 캐시백!` / `올리브영, 다이소 바우처부터 뷰티케어 시술까지` |

- **alias 기록**: 원문은 Figma 추출이 아니라 **사용자 채팅 제공**이므로 `{Figma 원문 → XLT Key}` alias 대상이 아니다. 단, **기획서·Figma 원본을 확정본으로 수정 요청**해야 한다(§5 (d-2) #5).
- 그 외 한국어 문제 없음: 비표준 공백(`\xa0`·`​`·전각 공백·BOM) **0건**(2키 × 5개 언어 전수 코드 스캔), 오타 없음.

---

## 2. 자동 검증 결과 (P0 / P1 / P2 요약)

```
python3 scripts/validate_translation.py \
  xlt/registry_fix/xlt_fix_guidekim_banner_2keys_20260824.xlsx \
  scripts/glossary.json
```

| 심각도 | 건수 | 내용 |
|---|---|---|
| 🔴 **P0 (Critical)** | **0건** | 빈칸·오타·언어 혼입·용어집 표기 위반 — 없음 |
| 🟡 P1 (Medium) | 2건 | 용어 불일치 2건 (키 ① en·zh) → §4에서 각 건 처리 판정 |
| 🟢 P2 (Low) | 0건 | 마침표·쉼표 스타일 — 없음 |

자동 리포트: `xlt/registry_fix/xlt_fix_guidekim_banner_2keys_20260824_validation_report.md`

> ⚠️ `P0=0`은 통과가 아니라 시작점이다. 자동 검증기는 외래어 음차 오염·붙은 띄어쓰기·다의어 모호·표현 어색을 구조적으로 못 잡으므로 §3 수동 3단계를 전수로 수행했다.

---

## 3. 수동 3단계 체크표 (전수 — 2키 × 5개 언어 = 10셀 전부)

### 1단계: 한국어 맞춤법·띄어쓰기

| 점검 항목 | 키 ① | 키 ② |
|---|---|---|
| 조사·어미 띄어쓰기 | ✅ `최대 15% 캐시백!` 정상 | ✅ `바우처부터` — 교정 ①·② 적용(§1) |
| 단어 붙음 누락(`누리고다양한`류) | ✅ 없음 | ✅ 없음 |
| 외래어 음차 오염(`포이가츠`류) | ✅ 없음 | ✅ 없음 — `뷰티케어`는 국내 통용 표기(단 표기 신설 → §5 #10) |
| 오타·글자 누락 | ✅ 없음 | ✅ 없음 |
| 비표준 공백·제어문자 | ✅ 0건 | ✅ 0건 |
| 브랜드 표기 | — | ✅ `올리브영`·`다이소` 국내 정식 브랜드명 |
| 다의어·모호 표현(`피부결과`류) | ✅ 없음 | ✅ 없음 |

### 2단계: 용어집 위반 (v4.7)

| 용어 | 용어집 등재값 | 적용 | 판정 |
|---|---|---|---|
| 캐시백 | en `cashback` / ja `キャッシュバック` / th `เงินคืน` / zh `現金回饋` | 4언어 전부 등재값 사용 | ✅ 일치 |
| 최대 | en `Max` / ja `最大` / th `สูงสุด` / zh `最多` | ja `最大`·th `สูงสุด` 일치 / en `Up to`·zh `最高` 문맥 의역 | ⚠️ P1 2건 → §4 |
| 바우처 | **용어집 미등재** — 단 등록값에 정본 있음: `mini_guidekim_cat_voucher` ko `바우처` / ja `バウチャー` / en `Voucher` / th `เวาเชอร์` / zh `優惠券` | ja `バウチャー` / en `vouchers`(복수) / th `เวาเชอร์` / zh `優惠券` | ✅ **카테고리 라벨과 완전 일치**(en만 문맥상 복수형) |
| 시술 | **미등재** | ja `施術` / en `treatments` / th `ทรีตเมนต์` / zh `療程` | ✅ 기존 등록값 계열 일치(아래 대조표) |
| 뷰티(케어) | **미등재** | ja `ビューティーケア` / en `beauty care` / th `ความงาม` / zh `美容` | ✅ 기존 계열 일치 |
| 올리브영·다이소 | **미등재** | ja 가타카나 음차 / en·th·zh 라틴 | ✅ 브랜드 처리 선례 준용 → §5 (d-1) |
| 예외 목록(번역 금지) | PIN·API·URL·Apple·Google·USDT·IDRP·JPYC | 해당 없음 | ✅ |
| `deprecated_terms` 구 표기 | 팔로우·友達·關注 등 | 해당 없음 | ✅ |

**기존 등록값 일관성 대조** (2026-08-24 15:34 실측)

| 참조 키 | 기존 번역 | 신규 번역의 정합 |
|---|---|---|
| `UF_max_cashback` | ja `最大{{0}}キャッシュバック` / en `Up to {{0}} cashback on payment` / th `เงินคืนสูงสุด {{0}}` / zh `消費最高享 {{0}} 現金回饋` | ✅ 키 ①이 `最大…キャッシュバック`·`Up to`·`เงินคืนสูงสุด`·`最高享` 전부 선례와 동일 어형 |
| `mini_guidekim_pdlla_mask_gift` | ko `시술 예약 시 최대 2만원 할인` / en `Up to ₩20,000 Off` / zh `最高折抵` | ✅ 「최대+수치」에 en `Up to`·zh `最高` 선례 확인 |
| `mini_guidekim_cat_voucher` | ko `바우처` / ja `バウチャー` / en `Voucher` / th `เวาเชอร์` / zh `優惠券` | ✅ 키 ②가 5개 언어 전부 일치(통일 결정의 기준값) |
| `mini_guidekim_specialty_clinic` | ja `施術クリニック` / en `Treatment clinic` / th `คลินิกหัตถการ` / zh `療程診所` | ✅ 시술 = `施術`·`treatment`·`療程` 계열 일치 |
| `mini_luckyball_travel_desc` | en `From essential vouchers to authenticated K-beauty treatments` / th `ทรีตเมนต์ K-Beauty` / zh `K-美妝療程` | ✅ 키 ②의 en은 **어휘·구조 모두 일치**. th는 뷰티 문맥 `ทรีตเมนต์` / 의료 문맥 `หัตถการ` 구분이 기존 관행 |
| `mini_guidekim_clinic_unifi_only` | ko `레디영약국` → ja `レディヤング薬局`(가타카나) / en·th·zh `Readyoung`(라틴) | ✅ 브랜드 선례 준용: ja 음차(`オリーブヤング`·`ダイソー`), en·th·zh 라틴(`OLIVE YOUNG`·`DAISO`) |

### 3단계: 다른 언어 번역 검토

| 언어 | 점검 | 결과 |
|---|---|---|
| **ja_JP** | 숫자+단위 붙임 · 나열 구분자 · 「から〜まで」 범위 구조 · 느낌표 전각 | ✅ 키 ① `最大15%` 붙임은 `md/check.md` B-7-1 정본(`還元{0}%`·`最大2万ウォン割引` 선례 일치, 통화 `{0} JPYC` 띄움 규칙과 별개). 키 ② 나열은 `、`(ko 쉼표 대응). 전각 `！`(선례 `韓国旅行に！`) |
| **en_US** | 브랜드 대소문자 · 단복수 · 「From A to B」 · 관사 | ✅ `OLIVE YOUNG`·`DAISO` 공식 대문자, `vouchers`·`treatments` 복수 일치(카테고리 라벨은 단수 `Voucher`이나 문맥상 복수가 정상), 선례 `From essential vouchers to authenticated K-beauty treatments`와 동일 구조 |
| **th_TH** | 의미 단위 띄어쓰기 · 숫자 앞 공백 · 「ตั้งแต่ … ไปจนถึง …」 · 영어 미번역 잔존 | ✅ 키 ① `เงินคืนสูงสุด 15%`는 `UF_max_cashback` th와 **완전 일치**. 키 ② 범위 구조는 선례 `ตั้งแต่ … ไปจนถึง …` 채택(구 등록값 `ถึง`보다 명시적). 브랜드 외 영어 잔존 없음 |
| **zh_TW** | 번체 정자 · 라틴 주변 공백 · 나열 `、` · 느낌표 전각 | ✅ 이체자 없음(`饋`·`優`·`療`·`從` 정상, `産`/`台` 없음), `最高享 15% 現金回饋！`·`從 OLIVE YOUNG、DAISO 優惠券到美容療程` — 선례 `盡在 Unifi`·`Juvelook 療程診所` 공백 스타일 일치, 전각 `！`. `優惠券`은 다수 표기(3키)와 일치 |
| **전 언어 공통** | 언어 혼입 · placeholder · HTML 태그 · 행 구조 | ✅ 문자체계 혼입 0 · 치환자 0개(리터럴 `15%` — §5 #1 변수화 제안 대상) · 태그 없음 · **개행 0**(2키 모두 1행, 구조 동일) |

### 2차 독립 검토 (`translation-reviewer` 에이전트 — 분리 컨텍스트 재판정)

초판(1키 2행 · `상품권`)을 대상으로 실행했고, **5개 언어 전부 「정확」 · 실제 결함(P0/P1) 0건 · 확인 필요(P2) 5건 · 1차 오탐 판정 6건 동의**로 판정했다. 3판은 **같은 문장을 키로 분리하고 개행만 제거**한 것이라(용어 `바우처` 교체 외 어휘·문법 변경 없음) 그 판정이 그대로 유효하다.

2차 검토가 교차 확인해 준 항목:

| 항목 | 2차 판정 |
|---|---|
| ko `최대 15% 캐시백!` | `md/IA.md`:193 Beta 홈 배너 실측 기재 `결제 시 최대 15% 캐시백(K-뷰티·K-컬처·쇼핑)`과 수치·표현 일치 — **직접 실측 확인함** |
| th 키 ① | `UF_max_cashback` th `เงินคืนสูงสุด {{0}}`와 **완전 일치**, 용어집 `เงินคืน`·`สูงสุด` 둘 다 준수 |
| th 범위 구조 | `ตั้งแต่ … ไปจนถึง …`는 선례 `mini_luckyball_travel_desc`(`และ`로 끝나 범위가 약함)보다 **개선** |
| zh 브랜드 | ⛔ `DAISO`를 `大創`으로 바꾸면 **안 됨** — 일본 다이소의 대만 명칭이라 혼동이 커진다. 라틴 `DAISO` 유지가 정답 |
| zh 상품권(초판) | 「`禮券`이 번체 표준, `商品券`은 일본식」으로 판정 → **바우처 통일 결정으로 `優惠券`으로 교체**(초판 기준 이력) |
| ja `最大15%` 붙임 | `md/check.md` B-7-1 정본. 통화 `{0} JPYC` 띄움 규칙과 **별개 사안**(%는 단위 붙임) |
| 음차 오염 | `ビューティーケア`·`ทรีตเมนต์`는 **영어 기원 차용어**로, `포이가츠`처럼 한국어 발음을 음차한 것이 아님 |
| 구조 정합 | 치환자 0개·숫자 `15%`·컬럼별 이질 문자체계 혼입 0 — 5개 언어 동일 |

---

## 4. 검증기 보고 각 건의 처리 판정

| # | 등급 | 건 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | P1 | 키 ① `en_US`: '최대' → 'Max' 권장 | **오탐 — 기각(현행 `Up to` 유지)** | 용어집 `Max`는 UI 라벨 단독 표기용. 문장 내 「최대 N%」는 영어에서 `Up to N%`가 정본이고, 기존 등록값도 `Up to {{0}} cashback on payment`·`Up to ₩20,000 Off`로 일관. `Max 15% cashback`은 비문에 가깝다 |
| 2 | P1 | 키 ① `zh_TW`: '최대' → '最多' 권장 | **오탐 — 기각(현행 `最高` 유지)** | `最多`는 **개수**의 최다(선례 `最多 5 個`), 비율·금액에는 `最高`가 정본. 기존 등록값 `消費最高享 {{0}} 現金回饋`·`預約療程最高折抵 2 萬韓元`과 일치 |

**실제 위반: 0건 / 오탐: 2건 / 정책 보류: 0건** → **P0 = 0건, 조치 필요 위반 없음.** 두 건 모두 용어집의 `최대` 매핑이 단일 값(라벨형)만 담아 문장형 문맥을 못 덮는 구조적 오탐이며 → §5 (d-1) 용어집 보완 권장으로 연결한다.

---

## 5. 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

### (d-1) 용어집 보완 권장 (v4.7 → 차기)

| 용어 | 권장 근거 | ko_KR | en_US | ja_JP | zh_TW | th_TH |
|---|---|---|---|---|---|---|
| **최대**(문장형) | 검증기가 `Up to`·`最高`를 **P1로 반복 오탐**(= 매핑이 좁다는 신호). 기존 등록값 다수가 이미 이 어형 | 최대 | Up to *(라벨은 Max)* | 最大 | 最高 *(개수는 最多)* | สูงสุด |
| **바우처** | 등록값에 정본이 있으나 용어집 미등재라 검증기가 못 잡는다. ⚠️ **th·zh는 실측상 표기가 갈려 있다** — th `เวาเชอร์` 2키(`cat_voucher`·`travel_voucher`) vs `บัตรกำนัล` 2키(`kpick_jp_banner_desc`·`luckyball_travel_desc`)로 **2:2 균등 혼용**, zh는 `優惠券` 3키 vs `憑證` 1키. 등재로 정본을 고정하고 이탈 키를 정리할 것을 권장 | 바우처 | voucher | バウチャー | 優惠券 | เวาเชอร์ |
| **시술** | `施術`/`treatment`/`療程`으로 8키 이상 사용 중이나 미등재 | 시술 | treatment | 施術 | 療程 | ทรีตเมนต์ *(의료 문맥 หัตถการ)* |
| **올리브영** | 브랜드 신규 도입 — 표기가 갈리면 화면 간 불일치 | 올리브영 | OLIVE YOUNG | オリーブヤング | OLIVE YOUNG | OLIVE YOUNG |
| **다이소** | 〃 (한국 다이소 ↔ 일본 Daiso 혼동 방지 차원에서도 정본 고정 권장) | 다이소 | DAISO | ダイソー | DAISO | DAISO |

> 용어집은 **API 읽기 전용**이라 Claude가 직접 쓰지 못한다. 승인 시 `md/landpress.md` 절차로 **전체 JSON을 산출**해 전달하고, 사용자가 Landpress CMS(`web3_xlt_json`)에 붙여넣어 반영한다. 용어집 버전이 오르면 **같은 작업에서 기획자 가이드 zip도 필수 동반 갱신**(`md/landpress.md` §5-1).

### (d-2) 그 외 개선·확인 권장

> #1·#7~#11은 **2차 독립 검토가 P2로 제기**한 항목이며, 근거로 인용된 `md/IA.md` 3개 지점(193·373·376~378행)을 **직접 실측 확인**했다. #12는 3판 재조회에서 새로 발견했다. 전부 임의 반영 금지.

| # | 항목 | 권장 내용 |
|---|---|---|
| **12** | ⚠️ **키 ②가 Unifi에 미등록 — 업로드 범위 확인 필요** | `mini_guidekim_all_korea_experience`는 **Dapp Portal에만** 있고 Unifi(2,143키)에는 없다(2026-08-24 15:34 실측). Unifi에 올리면 **신규 키 등록**이 된다. 화면이 Unifi 쪽에서 이 키를 참조하는지 FE 확인 후, 참조하면 양쪽 업로드 / 참조하지 않으면 Dapp Portal만 업로드. 키 ①은 양쪽 등록되어 있어 **양쪽 업로드 필요**(한쪽만 올리면 ja 불일치가 형태만 바뀐 채 남는다) |
| 1 | **`15%` 변수화 (권장도 상향)** | `md/IA.md`:376~378 실측 경고 — **「캐시백률은 고정값이 아니다」**: 같은 시각 Web K-Pick = 쥬베룩 5%·리쥬란 8%·포텐자 10% / mini = 10%·10%·5%로 **환경별로 다르게** 노출되어 **「캐시백 수치를 상수로 인용하지 않는다」**가 IA 규칙이다. 기존 `UF_max_cashback`도 요율을 `{{0}}`로 변수화했다. LV 팀이므로 `{0}` 적용안: `최대 {0} 캐시백!` / `最大{0}キャッシュバック！` / `Up to {0} cashback!` / `เงินคืนสูงสุด {0}!` / `最高享 {0} 現金回饋！` — 변수화하면 요율 변경 시 **5개 언어 재패치가 불필요**해진다. **가변 여부는 비즈니스(PM) 판단이므로 임의 적용하지 않고 리터럴 `15%`로 등록했다**(guide §5-2 「변수화는 사용자 확인 후에만, 예외 없음」). 단 `15%` 자체는 IA:193 기재값과 일치해 현재로선 오류가 아니다 |
| ~~1a~~ | ~~`상품권` vs `바우처`~~ | ✅ **해소 — 사용자가 `바우처` 통일 결정**(2026-08-24, 3판에서 재확인). **잔여 과제**: th `เวาเชอร์`↔`บัตรกำนัล`, zh `優惠券`↔`憑證` 표기 이탈이 기존 키에 남아 있다 → (d-1) 등재로 정본 고정 권장 |
| 5 | **원본 문서 교정** | 기획서·Figma 원본을 확정본으로 수정 요청 — 맞춤법(`상품권 부터`→붙여쓰기)과 용어(`상품권`→`바우처`) 두 건 반영, **키 분리 구조도 함께 명시**(§1) |
| 7 | **ja `ダイソー` 지역 혼동** | 일본 독자에게는 대창산업(일본 100엔숍)으로 읽히나 대상은 **한국 다이소**(별개 법인)다. 대안 `韓国のダイソー`. 다만 브랜드 선례(`레디영약국`→`レディヤング薬局`)가 ja 가타카나 음차이므로 **현행도 선례 일치 — 오류가 아니라 정책 선택**이다 |
| 8 | **ja·th 브랜드 수식 관계 모호** | ja `オリーブヤング、ダイソーのバウチャー`는 `の`가 `ダイソー`에만 걸리는 독법이 가능하고, th `เวาเชอร์ OLIVE YOUNG และ DAISO`는 브랜드가 `เวาเชอร์`를 수식하는지 별 항목인지 모호하다. 개선안: ja `オリーブヤングやダイソーのバウチャーから…`(`や`=예시 열거) / th `…เวาเชอร์จาก OLIVE YOUNG และ DAISO…`(`จาก` 삽입). **현행도 의미는 통하므로 오류는 아니다** — 문구 변경이라 승인 필요.<br>덧붙여 ja 열거 기호는 프로젝트 선례가 `･`(`韓国ビューティー･エステ`)이므로 `、`/`･`/`や` 중 **정책 확정 권장** |
| 9 | **키 ② en·th 길이 편차** | ko 25자·zh 28자·ja 32자 대비 **en 61자·th 61자**(2.2~2.4배). 키 분리로 개행이 없어져 **줄바꿈은 화면 렌더가 담당**하므로 초판의 「2행 고정 배너 3행 wrap」 우려는 성격이 바뀌었지만, 영역 폭이 좁으면 여전히 줄 수가 언어별로 달라진다. 축약안 en `From OLIVE YOUNG & DAISO vouchers to beauty treatments`(53자 — `&`는 선례 `Korean Beauty & Aesthetic Care`에 있음). **실제 배너 폭·폰트 실측 후 결정** |
| 10 | **ko `뷰티케어` 표기 신설** | 이 서비스의 기존 표기는 `K-뷰티`·`뷰티･에스테틱`·`시술`이고 `뷰티케어` 선례가 없다. 외래어 두 단어 결합은 원칙상 `뷰티 케어`이나, 마케팅 관용 붙여쓰기는 `거래내역` 선례처럼 정본 확정이 가능하다. **신설 표기이므로 등록 전 표기 정책 확정 권장** |
| 11 | **키 이름과 내용 괴리 (2건 — 3판에서 확대)** | 키 ① `..._kbeauty_to_kculture` → 내용은 캐시백(K-뷰티·K-컬처 언급 없음). 키 ② `..._all_korea_experience` → 내용은 바우처·시술 범위(「한국 여행의 모든 경험」이라는 슬로건 성격 상실). 키 개명은 통상 하지 않으므로 결함은 아니나, 이후 검색·유지보수에서 오인 소지가 크다 → **위키에 교체 이력을 남길 것을 강력 권장** |
| ~~13~~ | ~~작업 위키 반영~~ | ✅ **완료 — `[Unifi Mini] 홈을 LIFF K-Pick 홈과 통일 + 배너 문구 변경`(pageId=4637821463) v4 → v5**(§7) |
| 16 | **다른 위키의 구 문구** | `Unifi mini - Guide Kim`(pageId=4339334947)의 다국어 표·Screen XLT에는 두 키의 **구 문구가 그대로 남아 있다**(이번 작업 대상 페이지가 아님). 시스템 업로드 후 이 페이지와도 정합이 깨지므로, 함께 갱신할지 확인 필요 — 요청 시 키 단위 패치 모드 Step 6(라이브 rebase → 외과적 교체 → History 병합)으로 처리한다 |
| 14 | **JP UX 피드백 이슈 종결** | `JP UX 피드백`(pageId=4611882417)에 등록된 「같은 키인데 Dapp/Unifi ja 값 상이」 이슈는 키 ① 교체로 **해소**된다. 해당 페이지의 이슈 상태 갱신 검토 권장 |
| 15 | **구 문구 잔존 키** | `mini_luckyball_kbeauty_kculture`(Dapp Portal)가 구 문구 `K-뷰티부터 K-컬처까지`를 그대로 보유한다(럭키볼 영역). 이번 교체 대상이 아니므로 손대지 않았다 — 함께 정리할지 확인 필요 |

---

## 6. 게이트 통과 판정

| 기준 | 결과 |
|---|---|
| 한국어 원문 교정 선행 | ✅ 2건 교정 완료(§1 — 맞춤법 1 + 용어 통일 1) |
| 자동 검증 실행 | ✅ P0 0 / P1 2 / P2 0 (2키) |
| 수동 3단계 전수 검토 | ✅ 10셀 전부 1·2·3단계 검토(§3) |
| 2차 독립 검토 교차 확인 | ✅ 5개 언어 전부 「정확」·실제 결함 0건·1차 오탐 판정 6건 동의(§3) |
| 각 건 처리 판정 | ✅ P1 2건 = 오탐·기각(§4) / 2차 제기 P2 5건 = 사용자 결정 사안으로 §5 이관(임의 반영 안 함) |
| (d) 추가 개선·제안 | ✅ (d-1) 5건 · (d-2) 12건(§5) |
| **P0 = 0건 (자동 + 수동)** | ✅ **통과** |

→ **엑셀 생성·전달 가능.** 시스템 업로드는 쓰기 API가 없어 **사용자가 직접 수행**한다(`md/xlt-verify.md` §7). 업로드 범위는 **§5 (d-2) #12**를 먼저 확인할 것 — 키 ①은 양 서비스, 키 ②는 Unifi 참조 여부에 따라 달라진다.

---

## 7. 위키 반영 (완료)

**대상**: `[Unifi Mini] 홈을 LIFF K-Pick 홈과 통일 + 배너 문구 변경` — [pageId 4637821463](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4637821463) · Jira `UNIFY-10315` · **v4 → v5**

| 변경 | 내용 |
|---|---|
| **다국어 번역 섹션 신설** | `XLT Key \| KR \| JA \| EN \| TH \| ZH-TW` 6컬럼 표(2키) + 검증 요약 + 후속 권장. 이 페이지에는 섹션이 없었고 History에 「다국어 번역 미진행(추후 작업)」으로 남아 있던 항목을 해소 |
| **XLT 엑셀 첨부** | `xlt_fix_guidekim_banner_2keys_20260824.xlsx`(attachment id 4639687495) + 본문 `ri:attachment` 다운로드 링크. 기존 첨부 2건(`As-is.png`·`frame_68044-20708_tobe.png`)은 보존 |
| **XLT 키 정리 섹션 갱신** | 「다국어 번역 미진행」 → 「완료」 + 담당 FE 팀(LV) 명시 + **업로드 범위 경고**(키 ① 양 서비스 / 키 ② Unifi 미등록 → 신규 등록) |
| **Screen 표 XLT 중첩표** | `변경 ko_KR (확정)` 셀 `상품권부터` → **`바우처부터`** |
| **Policy** | 원안 인용(`확정 문구(김민정 안)` — `상품권 부터`)은 **이력으로 보존**하고, 그 아래 「최종 확정 문구(교정 반영)」 항목을 추가해 교정 2건의 근거를 명시 |
| **History** | `md/wiki.md` **같은 날짜 병합 규칙**에 따라 새 행을 만들지 않고 당일 마지막 행에 항목 누적. 기존 3행 전부 보존 |

**검사 결과**: `check_wiki_storage.py pre`(보낼 storage) **exit 0** → PUT → `post`(라이브 렌더) **exit 0**. PUT 직전 라이브 재조회로 **버전 가드**(v4 확인 + storage 내용 동일 대조) 통과.

> ⚠️ **가드 함정 실측** — 편집 스크립트에 「`상품권` 표기 잔존 금지」를 **전역** 가드로 걸었더니 **History에 이력으로 인용된 구 문구**가 잡혀 중단됐다. `md/wiki.md`(673행) 「전역 금지 가드는 이력 기록과 충돌한다」와 동일한 사례로, 가드 범위를 **Screen·다국어 섹션 이후**로 한정하고 History의 원안 인용은 보존을 확인하는 가드로 바꿔 해결했다.

# 번역 검증 게이트 리포트 — 초대자 섹션 4개 프레임 재검증 (34291·25034·42345·41690)

- 대상 위키: pageId 4479306980 / 섹션 **초대자 프로모션 페이지** / 팀 **LV** (`mini_luckyball_` · `{0}`)
- 대상 엑셀: `xlt/xlt_output_20260724135339.xlsx` (74키) — **번역 변경 없음**
- 성격: 어노테이션·정책·XLT 정확성 재검증(사용자 결정 "XLT No 기준 유지 + 정확성만 수정"). **진짜 신규 키 0건** — 4개 프레임 모든 XLT 문구가 기존 키로 해결(변수 `{0}`·별칭·오타교정 포함).
- 용어집: API v2.6 새로 조회.

## 프레임별 수정 (모두 기존 키 재사용)

| 프레임 | 수정 내용 |
|--------|-----------|
| (Logged in) 초대페이지(34291) | 누락 정책 `친구초대 링크 복사 버튼을 눌러…팝업 토스트` 추가(정책 2→3). XLT(link_copied·copy_invite_link)·이미지 정상 |
| 초대자 뽑기 완료(41690) | XLT No2 `add_oa_channel`(**팝업 뒤 배경 오매칭**)→`confirm`(확인) 교정, 어노테이션 재생성(ⓝ2=확인 버튼) |
| 초대자 화면 전체(25034) | 누락 XLT 2건 `max20_short`·`max20_desc` 추가(31→33), ⓝ1~33 재정렬 어노테이션 재생성 |
| 초대자 뽑기 더뽑기(42345) | 재검증 — XLT(prize_won·draw_remaining·draw_more·draw_later)·정책·어노테이션 **이상 없음(변경 없음)** |

- **팝업겹침 육안 검증(필수)**: 41690은 팝업 위 배경 텍스트(add_oa_channel)로 좌표 오매칭돼 있었음. 렌더 육안 확인 결과 정책 "선택시 창 닫기"가 가리키는 대상은 팝업 `확인` 버튼 → 수동 교정. 42345·25034 렌더 확인 완료.

## 0. 한국어 원문 교정 (게이트 1단계)

- 신규 번역 없음. 재사용 키의 ko_KR은 이전 run에서 교정 완료(오타형 Figma 원문은 `translation_data.json` alias 80건으로 매핑: 예 `럭볼`→`럭키볼`, `가입후…개설하면 하면`→`가입 후…개설하면`, `정보가변경`→`정보가 변경`). 이번 매칭도 alias/변수 정규화로 정본 키에 연결.

## Executive Summary (자동 — `scripts/validate_translation.py`, 74키)

| 심각도 | 건수 | 카테고리 |
|--------|------|----------|
| 🔴 P0 (Critical) | **0건** | 빈칸, 오타, 언어 혼입, 용어집 표기 위반 |
| 🟡 P1 (Medium) | 25건 | 용어 불일치(용어집 협소 매핑) |
| 🟢 P2 (Low) | 27건 | 마침표 스타일 |

→ **P0 = 0**. 번역 변경이 없으므로 전건 이전 게이트와 동일.

## 1단계: 한국어 (수동, 전수 74키)

- 4개 프레임에서 참조하는 키(payout_5min·confirm·max20_short·max20_desc·link_copied·copy_invite_link·prize_won·draw_* 및 25034의 33키)를 한 줄씩 재검토 — 오타·띄어쓰기 이상 없음(정본은 교정 완료본).
- 특기: 41690 `확인`(confirm)은 다의어라 자동 P1 대상이나, 팝업 닫기 버튼 문맥에서 `확認/Confirm/ยืนยัน/確認`이 정확(오탐).

## 2단계: 용어집 (수동, 전수)

- 예외 패턴(JPYC·LINE·Unifi) 표기 통일 확인. zh_TW 번체 정자 확인. th 표기 일관성 확인. 자동 P1 25건 전건 협소 매핑 오탐.

## 3단계: 다른 언어 (수동, 전수)

- 빈칸 0·언어 혼입 0·컬럼 swap 0. placeholder(`{0}`) 일치: prize_won·draw_remaining·event_period·my_draw_count·mission_done_count 5개 언어 동일 인덱스 확인. ja 직역·zh 간체 혼입 없음.

## 각 건 처리 판정

- **P1 25건 — 전건 오탐**: `확인`·`변경`·`미션`·`복사`·`송금`·`예치` 다의어 단일 매핑. 현재 번역 문맥상 정확 → 수정 없음.
- **P2 27건 — 전건 오탐**: 배너·타이틀·팝업 무마침표/`!` 종결 스타일. 유지.

## (d) 추가 개선·제안 (권장 — 사용자 결정)

- **(d-1) 용어집 보완 권장**: 다의어(확인/변경/송금/미션/복사) 문맥 변형 추가 시 P1 오탐 감소. API 읽기전용 → `md/landpress.md` 절차. 이번 변경 없음.
- **(d-2) Figma 원문 오타**: `럭볼`·`개설하면 하면`·`정보가변경`·`친구초대`(붙임) 등 잔존 오타는 번역 정본엔 교정 반영됐으나 Figma 원본 미수정 — 디자이너 정리 권장.
- **(d-3) 어노테이션 기준**: 사용자 결정대로 ⓝ=XLT No 유지. 정책 번호와는 별도 축임(Description 정책은 독립 번호).

## 통과 판정

- 자동 **P0=0** + 수동 3단계 **P0=0** → **통과**. 신규 키 0·번역 변경 0 → 엑셀·다국어 표 불변. 위키 변경은 Screen 표(34291 정책, 41690·25034 XLT·이미지)에 한정. P1/P2 전건 오탐. (d) 권장 미반영.

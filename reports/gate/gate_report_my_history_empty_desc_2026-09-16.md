# 번역 품질 게이트 리포트 — `UF_my_history_empty_desc` 신규 (2026-09-16)

대상: Slack 스레드(`C07R7CE0B5H` · `1789540398.145119`) **3번 요청** — My › 결제·예약 내역 **빈 상태 보조 설명 문구** 신규 키 1건.
**1키 × 5개 언어 = 5셀** · **전수 점검**(파일 전체가 1키라 신규=전수).

| 항목 | 값 |
|---|---|
| XLT Key | `UF_my_history_empty_desc` (사용자 지정) |
| 담당 FE 팀 | **UIT** — `UF_` 프리픽스 · 변수 없음 |
| 업로드 대상 서비스 | **Unifi** — 실측 근거: `UF_my_*` 키가 **Unifi 45개 / Dapp Portal 3개**이고, 형제 키 `UF_my_history_empty_title`은 **Unifi에만** 있다(DP 미등록) |
| 엑셀 | `xlt/xlt_my_history_empty_desc_unifi_2026-09-16.xlsx` |

## ⛔ 중복 등록 방지 — 한국어 문구로 대조

키 이름이 아니라 **한국어 문구로** 양 서비스 등록값 전건(DP 1,701 · Unifi 2,588)을 대조했다.
`구매하거나 예약한 내역이 여기에 표시돼요.` 및 「여기에 표시」 포함 문구 — **동일·유사 등록값 0건**. 신규 등록이 맞다.

## (1a) 한국어 원문 교정

**교정 0건.** 요청 문구를 그대로 쓴다.

- `표시돼요` — `표시되어요`의 준말로 **맞춤법 정상**(`표시돼요`/`표시 돼요` 혼동 없음). 띄어쓰기 이상 없음.
- **마침표 유지** — 형제 키 `UF_my_history_empty_title`(`결제·예약 내역이 없습니다.`)이 마침표로 끝나므로 같은 화면 내 일관.
- **어투 차이는 의도된 것으로 본다** — 타이틀은 `-습니다`, 보조 설명은 `-돼요`. 이 저장소 등록값에도 같은 화면에서 어투가 갈리는 선례가 있고(`UF_notification_empty_desc` `알려드릴께요`), 사용자가 확정해 전달한 문구다. ⛔ 임의 통일하지 않는다.

## 확정 번역 — 형제 키에 정렬

| 언어 | `UF_my_history_empty_title`(기등록 · 참조) | **`UF_my_history_empty_desc`(신규)** |
|---|---|---|
| ko_KR | 결제·예약 내역이 없습니다. | **구매하거나 예약한 내역이 여기에 표시돼요.** |
| ja_JP | 決済・予約履歴がありません。 | **購入・予約した履歴がここに表示されます。** |
| en_US | No payment or reservation history found. | **Your purchases and reservations will appear here.** |
| th_TH | ไม่มีประวัติการชำระเงินหรือการจอง | **ประวัติการซื้อหรือการจองจะแสดงที่นี่** |
| zh_TW | 無付款或預約紀錄。 | **購買或預約的紀錄會顯示在此處。** |

근거:
- **ja_JP** — 용어집 `구매`=`購入`. 구분자 `・`는 타이틀 `決済・予約`와 동일. 빈 상태 안내의 기등록 관례 `～に表示されます`(`UF_send_select_token_empty_desc` `自動的に表示されます。`)를 따랐다.
- **en_US** — 기등록 `Tokens you hold will appear here automatically.`의 `will appear here` 패턴을 그대로 썼다. 타이틀의 `reservation`과 명사 통일.
- **th_TH** — 타이틀의 `ประวัติ`(내역)·`การจอง`(예약)·연결어 `หรือ`(또는)를 그대로 승계. 용어집 `구매`=`ซื้อ`.
- **zh_TW** — 타이틀의 `紀錄`(내역·번체 정자)·`預約` 승계. 용어집 `구매`=`購買`. 기등록 `顯示在此處` 표현 채택.

## (a) 자동 검증 — `scripts/validate_translation.py`

`python3 scripts/validate_translation.py xlt/xlt_my_history_empty_desc_unifi_2026-09-16.xlsx scripts/glossary.json`

| 심각도 | 건수 |
|---|---|
| **P0** | **0건** |
| **P1** | **0건** |
| **P2** | **0건** |

용어집 118개 용어 로드 · 1·2·3단계 전부 통과.

## (b) 수동 3단계 체크표 — 전수 점검 (1키 × 5언어 = 5셀 전건)

`md/check.md`·`md/translation_validation_guide.md` 절차로 **파일 전체 행**을 한 줄씩 육안 검토했다(신규·변경 키로 한정하지 않음 — 이 파일은 1키라 전체 = 전수).

- **1단계(한국어 원문)**: #1
- **2단계(용어집 대조)**: #6
- **3단계(다국어 의미·표기)**: #2 · #3 · #4 · #5 · #7 · #8

| # | 단계 | 점검 항목 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | **1단계** | 한국어 원문 맞춤법·띄어쓰기 | ✅ | `표시돼요` 준말 정상 · 띄어쓰기 이상 0 |
| 2 | **3단계** | 의미 등가(ko 기준 5언어) | ✅ | 「구매하거나 예약한 내역」 = 購入・予約した履歴 / purchases and reservations / การซื้อหรือการจอง / 購買或預約 |
| 3 | **3단계** | 언어 혼입(이질 문자체계) | ✅ | ja에 한글 0 · th에 라틴 0 · zh 전건 **번체 정자**(`紀錄`·`預約` — 간체 `记录`·`预约` 없음) |
| 4 | **3단계** | 자리표시자·변수 | ✅ | 변수 없음(양쪽 0개 일치) |
| 5 | **3단계** | 문장부호 일관성 | ✅ | ko `.` · ja `。` · en `.` · th **없음**(태국어 관례) · zh `。` — 형제 타이틀 키와 동일 패턴 |
| 6 | **2단계** | 용어집 준수 | ✅ | `구매` → 購入/Buy계열(purchases)/ซื้อ/購買 전건 일치 |
| 7 | **3단계** | 화면 내 형제 키와의 일관 | ✅ | 구분자·명사·문말 표현을 타이틀 키에 맞춰 정렬(위 표) |
| 8 | **3단계** | 중복 등록 | ✅ | 한국어 문구 기준 양 서비스 4,289키 대조 — 동일 0건 |

## (c) 자동 검증기 보고 건 처리 판정

P0·P1·P2 **전건 0** — 처리할 항목 없음.

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장
**없음.** 이번 문구의 도메인 용어(`구매`)는 용어집에 이미 있고, 자동 검증기의 반복 오탐도 없었다. `md/glossary-backlog.md` 신규 등재 0건.

### (d-2) 추가 개선·제안
1. **어투 혼재 확인 권장(P2 · 사용자 결정)** — 같은 화면에서 타이틀은 `-습니다`, 보조 설명은 `-돼요`다. 사용자가 확정한 문구라 그대로 등록했으나, My 영역 전반의 문말 어투 정책을 한 번 정할 여지는 있다. ⛔ **임의 통일하지 않았다.**
2. **`UF_my_history_empty_title`은 이미 Unifi에 등록돼 있다** — 핸드오프 「XLT 미등록 13키」 목록에 이 키가 남아 있는데 **실측 결과 등록됨**(`결제·예약 내역이 없습니다.`). 목록을 재실측해 정리하는 것을 권장한다.
3. **Figma 원문 수정 요청 없음** — 이번 문구는 Slack으로 직접 전달받은 확정 카피라 Figma alias 대상이 아니다.

## 통과 판정

**P0 = 0건(자동 + 수동) → 통과.** 엑셀 생성·위키 반영으로 진행한다.
업로드(Unifi)는 **사용자가 수행**한다 — XLT 쓰기 API가 없다.

# 게이트 리포트 — `상단 롤링 배너` 10키 (`UF_clinic_bridge_badge_*`)

- **작업일**: 2026-09-03 (clinic 세션 #3)
- **대상 위키**: `4667512757` 「클리닉 예약 동선 개선」 (착수 시 v98)
- **대상 프레임**: `상단 롤링 배너` — Figma `GOCHAYBS7hIrmWRGNuJOKV` `71543:3307` (742×290)
- **담당 팀**: **UIT** — 프리픽스 `UF_` · 치환자 `{{0}}`(이번 범위 치환자 없음)
- **규모**: 배지 5개 × 2줄 = **신규 10키** · ko·ja는 프레임 원문 사용 · **en·th·zh만 번역**(사용자 지시)
- **코멘트**: `fetch_comments.py` reply-aware 조회 → **0건**(roots=0). 어노테이션 핀은 좌표 기반으로 직접 렌더
- **레지스트리**: Unifi / WEB BROWSER / v1.7.4 · 2,431키 (작업 시점 신규 조회)
- **용어집**: v4.7 · 116 용어 (작업 시점 API 신규 조회)
- **엑셀**: `xlt/xlt_output_20260903210152.xlsx`

---

## (1a) 한국어 원문 교정

| No | 원문(Figma) | 확정값 | 사유 |
|---|---|---|---|
| 9 | `편한 의사 소통` | **`편한 의사소통`** | 「의사소통」은 표준국어대사전 **한 단어** — 띄어쓰기 누락(`md/check.md` 「단어가 붙은 띄어쓰기 누락」 유형) |
| 1~8·10 | (그 외 9건) | 동일 | 교정 없음 |

**alias 기록**: `{Figma 원문 `편한 의사 소통` → XLT Key 값 `편한 의사소통`}` — **디자이너에게 Figma 원본 수정 요청 대상**(노드 `71417:16016`).
※ 어노테이션 이미지에는 교정 전 원문(`편한 의사 소통`)이 그대로 보인다 — Figma 원본 수정 후 이미지 재첨부가 필요하다.

**신규 키 판정 근거(전건 실측)** — 「같은 문구 = 같은 키 재사용」 규칙에 따라 10문구 전부 레지스트리 대조:

| 문구 | 완전일치 | 부분포함 | 판정 |
|---|---|---|---|
| LINE 무료 상담 | 0건 | 2건(`_banner_title`·`_subtitle` 문장 내부) | 신규 |
| 사전 결제 없음 | 0건 | 0건 | 신규 |
| 캐시백 혜택 | 0건 | 5건(문장 내부) | 신규 |
| 최대 15% 캐시백 | 0건 | 1건 | 신규 |
| LINE 공식 인증 | 0건 | 1건(`UF_voucher_mini_certified` 문장 내부) | 신규 |
| Mini App | 0건 | 0건 | 신규 |
| 일본어 상담 | 0건 | 0건 | 신규 |
| 편한 의사 소통 | 0건 | 0건 | 신규 |
| 간편 예약 | 0건 | 0건 | 신규 |
| LINE으로 편하게 | 0건 | 0건 | 신규 |

**프리픽스 선례 실측**(`md/translate.md` Step 2-1 보강 규칙 — 네임스페이스 선례 조사):
- `UF_clinic_bridge_` **10건** · `UF_clinic_detail_` 40건 · `UF_clinic_` 51건 · `mini_guidekim_` 11건
- 이 프레임의 배지 5개 중 3개(`LINE 무료 상담`/`사전 결제 없음`, `캐시백 혜택`/`최대 15% 캐시백`, `LINE 공식 인증`/`Mini App`)는 **`(Unifi mini) 브릿지페이지` `71241:9154`(y=16714·16734)에 이미 존재**하고, 개선 상세 프레임 `71241:9438`에는 **0건**(`간편 예약`·`LINE 무료 상담` 모두 미검출) → **브릿지 소속 확정**
- → `UF_clinic_bridge_badge_{기능}_{title|desc}` 채택. `badge` 그룹 세그먼트는 기존 `banner_`·`cat_`·`promo_` 패턴과 동형

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py`

실행: `python3 scripts/validate_translation.py xlt/xlt_output_20260903210152.xlsx scripts/glossary.json`

| 심각도 | 건수 |
|---|---|
| 🔴 **P0 (Critical)** | **0건** |
| 🟡 P1 (Medium) | 10건 (판정은 (c) — 오탐 8 / 정책 보류 2) |
| 🟢 P2 (Low) | 0건 |

---

## (b) 수동 3단계 체크표 — **전수 점검**

검토 범위: 엑셀 **전체 행 10행 × 5개 언어 = 50셀**을 한 줄씩 육안 검토했다. 신규·변경 키로 한정하지 않았다(이번 파일은 전 행이 신규이나, 범위 한정 없이 전수 검토했음을 명시한다).

### 1단계 — 기계적 정합

| 항목 | 결과 |
|---|---|
| 빈칸 | ✅ 0건 (50/50 셀 채움) |
| 치환자 | ✅ 해당 없음 — 캐시백률은 **리터럴 `15%`**(2026-09-01 결정: FE API 조달 불가 → 수기 텍스트) |
| 문자체계 | ✅ ja 가나·한자 / th 타이문자 / zh 번체 정자 · 간체 혼입 0 |
| ja↔zh 한자 상호 오염 | ✅ `還元特典`·`公式認証`(일본 한자체) / `現金回饋`·`官方認證`(번체) — 교차 0 |
| 프레임 경계 밖 텍스트 | ✅ 전 노드 y≤214 < height 290 — 경계 밖 0건 |
| ko·ja 원문 일치 | ✅ 프레임 노드값과 1:1 대조 완료(교정 1건은 (1a)) |
| 조사·받침 | ✅ `LINE으로`(종성 E→으로) 정상 — 오탐 주의 대상 |

### 2단계 — 의미 정합 (ko_KR 기준 · ja는 프레임 원문 고정)

| No | key | ko | ja(프레임) | en | th | zh | 판정 |
|---|---|---|---|---|---|---|---|
| 1 | `_consult_title` | LINE 무료 상담 | LINE無料相談 | Free LINE consultation | ปรึกษาฟรีผ่าน LINE | LINE 免費諮詢 | ✅ 등재값 `_banner_title` 3언어와 **동일 어형 재사용** |
| 6 | `_consult_desc` | 사전 결제 없음 | 事前決済なし | No prepayment | ไม่ต้องชำระล่วงหน้า | 無需預先付款 | ✅ 등재값 `UF_clinic_detail_process_step3_desc`와 **어형 완전 일치**(선례 1건) |
| 2 | `_cashback_title` | 캐시백 혜택 | 還元特典 | Cashback | เงินคืน | 現金回饋 | ✅ clinic 영역 **4/4 선례**가 「혜택」을 별도 단어로 옮기지 않음(`_banner_title`·`_banner_desc`·`_benefit_cashback`·`_benefit_sub`) |
| 7 | `_cashback_desc` | 최대 15% 캐시백 | 最大15%還元 | Up to 15% cashback | เงินคืนสูงสุด 15% | 最高 15% 現金回饋 | ✅ 등재값 `UF_clinic_bridge_subtitle` 3언어 어형 그대로 |
| 3 | `_certified_title` | LINE 공식 인증 | LINE公式認証 | LINE certified | รับรองโดย LINE | LINE 官方認證 | ✅ 등재값 `UF_voucher_mini_certified`와 어휘 일관(zh `官方認證` 동일 · `認證` 22건) |
| 8 | `_certified_desc` | Mini App | Mini App | Mini App | Mini App | Mini App | ✅ 고유명 — 5개 언어 동일. ⚠️ 등재값은 `MINI App`(대문자) → (d-2) |
| 4 | `_japanese_title` | 일본어 상담 | 日本語相談 | Japanese consultation | ปรึกษาภาษาญี่ปุ่น | 日語諮詢 | ✅ `諮詢` 14건·`ปรึกษา` 14건 일관. zh `日語`/`日文` 등재 선례 **양쪽 0건** → (d-2) |
| 9 | `_japanese_desc` | 편한 의사소통 | 気軽に相談 | Easy communication | สื่อสารสะดวก | 溝通無障礙 | ⚠️ **ja가 ko와 의미 불일치** — ko/en/th/zh = 「의사소통이 편하다」, ja `気軽に相談` = 「부담 없이 상담」. 프레임 원문 사용 지시에 따라 유지 → (d-2) |
| 5 | `_booking_title` | 간편 예약 | かんたん予約 | Easy booking | จองง่าย | 輕鬆預約 | ✅ 등재 `예약` en `booking` 2건 · th `จอง` 4건 · zh `預約` 5건 |
| 10 | `_booking_desc` | LINE으로 편하게 | LINEでかんたん | Simple with LINE | สะดวกผ่าน LINE | 透過 LINE 輕鬆完成 | ✅ th `ผ่าน LINE`·zh `透過 LINE` 등재 어형 |

### 3단계 — 언어별 중점 점검

| 언어 | 중점 | 결과 |
|---|---|---|
| ja | 프레임 원문 고정 · 외래어 음차 오염 | ✅ 10건 전부 프레임 노드값과 문자 단위 일치 · 음차 오염 0. ⚠️ `還元` 표기는 정책 충돌 → (c)·(d-2) |
| en | 배지 라벨 길이·대소문자 | ✅ 문장 첫 글자만 대문자(등재 라벨 관례) · 최장 `Free LINE consultation` 22자 — 배지 폭 138pt 기준 **줄바꿈 가능** → (d-2) |
| th | 어절 붙여쓰기·전치사 | ✅ `ไม่ต้องชำระล่วงหน้า`·`สื่อสารสะดวก` 정상 결합 · `ผ่าน LINE` 전치사 용법 등재 일관 |
| zh | 번체/간체·대만 관용 | ✅ 전건 번체 정자 · `無需預先付款`·`輕鬆預約` 대만 관용형 |

---

## (c) 자동 검증 보고 건별 처리 판정 (P1 10건 전건)

| # | 검증기 지적 | 판정 | 근거 / 처리 |
|---|---|---|---|
| 1 | `_consult_desc` th 「결제」→`การชำระเงิน` | **오탐** | 등재 선례 `UF_clinic_detail_process_step3_desc` th = `ไม่ต้องชำระล่วงหน้า`(동사구). 명사형 `การชำระเงิน`은 「~없음」 부정 구문에 부적합 |
| 2 | `_consult_desc` zh 「결제」→`結帳` | **오탐** | 같은 선례 zh = `無需預先付款`. `付款` **25건** vs `結帳`(체크아웃 맥락). 용어집이 결제=`結帳` 단일 매핑이라 발생하는 구조적 오탐 |
| 3 | `_cashback_title` en 「혜택」→`perks` | **오탐** | clinic 영역 4/4가 「캐시백 혜택」을 `cashback` 단독으로 옮긴다. 실측 `benefit` **29건** vs `perk` **6건** — 권장값 자체가 소수 표기. **3세션 연속 반복 오탐** → (d-1) |
| 4 | `_cashback_title` th 「혜택」→`สิทธิประโยชน์` | **오탐** | clinic 영역 선례가 `เงินคืน` 단독(`_banner_title`·`_banner_desc`·`_benefit_sub`) |
| 5 | `_cashback_title` zh 「혜택」→`優惠` | **오탐** | clinic 영역 선례가 `現金回饋` 단독. `優惠`는 「할인·프로모션」 맥락 |
| 6 | `_cashback_title` ja 「캐시백」→`キャッシュバック` | 🔴 **정책 보류 — 사용자 확인 필요** | 검증기 지적이 **프로젝트 결정 사항과 일치**한다(2026-09-01: ja 캐시백은 `キャッシュバック`, `還元` 미채택). 재실측: ko에 「캐시백」 포함 **15키** 중 `キャッシュバック` **14** vs `還元` **1**(`UF_mini_guidekim_cashback`). 그러나 **프레임 원문이 `還元特典`**이고 사용자가 「프레임의 일어 값 사용」을 지시했다 → **프레임 원문 유지**하고 결정을 요청 |
| 7 | `_cashback_desc` en 「최대」→`Max` | **오탐** | 등재 `UF_clinic_bridge_subtitle` en = `up to 15% cashback`. `Max`는 지표·한도 맥락 |
| 8 | `_cashback_desc` zh 「최대」→`最多` | **오탐** | 등재 `UF_clinic_bridge_subtitle` zh = `最高 15% 現金回饋`. 비율에는 `最高`가 정확 |
| 9 | `_cashback_desc` ja 「캐시백」→`キャッシュバック` | 🔴 **정책 보류** | #6과 동일 사안(`最大15%還元`) |
| 10 | `_certified_desc` ko 라틴 문자열 `Mini App` | **오탐** | 고유명(LINE MINI App). 5개 언어 동일 표기가 정상. 단 대소문자는 (d-2) |

**P0 = 0건**(자동 + 수동). P1 10건 = 오탐 8 + **정책 보류 2**(ja `還元`).

---

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장

| 용어 | 현행 등재 | 권장 | 실측 근거 |
|---|---|---|---|
| 혜택 | en `perks` | **`benefit`** | Unifi v1.7.4 실측 `benefit` **29건** vs `perk` **6건**. 같은 오탐이 **4세션 연속**(2026-09-01·09-03×2 포함) |
| 더보기 | ja `すべて見る` | **`もっと見る`** | ⚠️ **2026-09-03 근거 정정** — `すべて見る`는 Unifi v1.7.4에서 실사용 **6건**이다(0건이 아니다). 단 6건 전부 ko 「전체보기」 번역이고, ko 「더보기」는 `もっと見る` **5건**이 정본이다(완전일치 키 `UF_send_select_type_list_more` 포함). 문제는 「실사용 없음」이 아니라 **「다른 뜻의 표현을 등재했다」**다. 상세: `gate_report_clinic_mini_bridge_19keys_2026-09-03.md` (d-1) |
| 더보기 | zh `查看更多` | **`更多`** | `更多` 3건 vs `查看更多` 1건 (기존 권장 유지) |
| 결제 | zh `結帳` | **`付款` 병기** | 이번 오탐 #2의 원인. 실측 `付款` **25건** — `結帳`은 체크아웃 화면 한정 어휘이므로 문맥 분기(`context`) 추가 권장 |

⛔ 용어집은 **API 읽기 전용**이라 Claude가 직접 쓰지 못한다 — 승인 시 `md/landpress.md` 절차로 전체 JSON을 산출해 전달하고, **버전이 오르면 기획자 가이드 zip을 같은 작업에서 동반 갱신**한다(§5-1).

### (d-2) 추가 개선·제안 안내

1. 🔴 **ja 캐시백 표기 결정 필요** — 프레임 `還元特典`·`最大15%還元` ↔ 프로젝트 결정 `キャッシュバック`(실측 14:1). ⓐ 프레임 원문 유지(현재 산출물) ⓑ 결정 사항대로 `キャッシュバック特典`·`最大15%キャッシュバック`로 교체 + **Figma 원본 수정 요청**. **ⓑ 권장** — 같은 화면군(브릿지 배너·상세 혜택)에서 표기가 갈리면 사용자에게 다른 혜택으로 읽힌다.
2. 🔴 **`_japanese_desc` ja가 ko와 다른 뜻** — ko `편한 의사소통`(의사소통 용이) ↔ ja `気軽に相談`(부담 없이 상담). ja는 1번 배지(`LINE無料相談`)와 의미가 겹친다. **Figma ja 원문 재검토 권장**(예: `スムーズな意思疎通`).
3. **`Mini App` 대소문자** — 프레임 `Mini App` ↔ 등재값 `MINI App`(`UF_voucher_mini_certified` 5개 언어 전부). LINE 공식 브랜드 표기는 **`LINE MINI App`** → 프레임을 `MINI App`으로 교정 권장.
4. **`편한 의사 소통` Figma 원본 수정** — (1a) alias 대상(노드 `71417:16016`). 수정 후 어노테이션 이미지 재첨부 필요.
5. **zh 「일본어」 표기 확인** — `日語`/`日文` 모두 등재 **0건**이라 선례가 없다. 상담(구어) 맥락이라 `日語`를 채택했으나, 대만 현지 표기 선호가 있으면 `日文諮詢`으로 교체 가능.
6. **en 배지 라벨 길이** — `Free LINE consultation`(22자)·`Japanese consultation`(21자)이 배지 폭(138pt 간격)에서 줄바꿈될 수 있다. 잘림이 우려되면 `Free LINE consult`·`Japanese support`로 축약 가능.
7. **배지 3개는 기존 브릿지 프레임과 중복 노출** — `71241:9154` y=16714·16734에 같은 배지 3개가 이미 있다. 롤링 배너가 그 영역을 **대체**하는지 **별도 추가**인지 명시 필요(대체라면 구 영역 텍스트도 이 10키를 참조해야 한다).
8. **GA Event 미정의** — 이 프레임은 롤링(자동 전환) 컴포넌트다. view 이벤트만 둘지, 배지별 노출·클릭을 수집할지 스펙 확정 필요.
9. **롤링 정책 미정의** — 전환 간격·순서(고정/랜덤)·무한 루프 여부·수동 스와이프 허용이 Figma·코멘트에 없다(코멘트 0건). 정책 정의 권장.
10. **`상단 롤링 배너`는 독립 화면이 아니다** — 브릿지페이지 상단 컴포넌트다. 사용자 지시대로 Screen 표에 **새 행**으로 추가했으나(2026-09-03 「개선 프레임은 새 Screen 행」 결정과 동형), IA Screen ID 부여 시에는 컴포넌트로 분류될 수 있다.

---

## (4) 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 = 0 | ✅ 0건 |
| 수동 P0 = 0 | ✅ 전수 점검(10행 × 5언어 = 50셀) 0건 |
| (a)~(d) 리포트 | ✅ 본 문서 |
| 한국어 원문 교정 | ✅ (1a) — `편한 의사 소통` → `편한 의사소통` alias 기록 |
| 전수 점검 | ✅ 전체 행 기준 |

→ **위키 반영 · 엑셀 생성 진행 가능.** P1 10건 전건 판정 완료(오탐 8 / **정책 보류 2 = ja `還元`**), (d) 권장 14건은 **사용자 결정 전까지 미반영**.

---

## (5) 후속 — ja 캐시백 표기 교체 (2026-09-03 사용자 결정, 같은 날 추가 실행)

(c) #6·#9의 **정책 보류 2건이 종결**됐다. 사용자가 (d-2) 1번의 **ⓑ안(결정 사항대로 교체)** 을 승인했다.

| key | ja 변경 전(Figma 원문) | ja 변경 후 | 근거 |
|---|---|---|---|
| `UF_clinic_bridge_badge_cashback_title` | `還元特典` | **`キャッシュバック特典`** | 등재 선례 `UF_clinic_bridge_banner_title` = `LINE無料相談＆キャッシュバック特典` |
| `UF_clinic_bridge_badge_cashback_desc` | `最大15%還元` | **`最大15%キャッシュバック`** | 등재 선례 `UF_clinic_bridge_subtitle` = `今なら最大15%のキャッシュバック特典付き！` |

**alias 기록**: `{Figma 還元特典 → XLT キャッシュバック特典}` · `{Figma 最大15%還元 → XLT 最大15%キャッシュバック}` — **디자이너 원본 수정 요청 대상**(노드 `71417:16028`·`71417:16029`). 어노테이션 이미지에는 교정 전 원문이 그대로 보이므로 Figma 수정 후 이미지 재첨부가 필요하다.

**무결성 가드**: ja 컬럼 2셀만 교체하고 나머지 48셀은 불변임을 확인했다(`還元` 문자열 잔존 0건 · ko/en/th/zh 무변경).

**A/B 재검증 실측** — `python3 scripts/validate_translation.py xlt/xlt_output_20260903210956.xlsx scripts/glossary.json`

| 심각도 | 교체 전 | 교체 후 | 차이 |
|---|---|---|---|
| 🔴 P0 | 0건 | **0건** | 불변 |
| 🟡 P1 | 10건 | **8건** | **-2** (ja 「캐시백」 지적 2건 해소 · **신규 P1 0건**) |
| 🟢 P2 | 0건 | 0건 | 불변 |

남은 P1 8건은 (c) #1~#5·#7·#8·#10과 동일한 **오탐 8건**이며 판정 변경 없다.

**엑셀 교체**: `xlt/xlt_output_20260903210152.xlsx` → **`xlt/xlt_output_20260903210956.xlsx`**(정본). 이전 파일은 업로드하지 않는다.
**위키**: v99 → **v100** — 다국어 표 ja 2셀 교체 + Screen 「확인 필요」 ja 항목을 「해소」로 갱신 + History 병합. `check_wiki_storage.py` pre/post exit 0.

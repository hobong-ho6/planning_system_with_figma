# 게이트 리포트 — 바우처 상세 툴팁 신규 2키 (unifi-mini-v2)

- **작업일**: 2026-09-15
- **대상**: Figma `Web3` `바우처 상세 - 툴팁` **`74154:24016`** (`[마이]` 섹션 · 375×1178)
- **담당 FE 팀**: **UIT**(`UF_` · `{{0}}` · Unifi) — 삽입 위치인 `바우처 상세 - 사용가능` 행이 `My - UIT` 섹션이고 기존 키가 `UF_voucher_detail_*`
- **산출물**: `xlt/xlt_output_20260915192629.xlsx` (신규 **2키**)
- **조달**: Figma 노드·코멘트 신규 조회 · XLT 등록값 Unifi 2,569 / Dapp Portal 1,696 · 용어집 v5.5
- **검증 범위**: **전체 파일 전수** — 2키 × 5개 언어 = **10셀 전건** 수동 검토

---

## 0. 코멘트 5건 → XLT 3건 → 신규 2건

| 핀 | 코멘트 | 처리 |
|---|---|---|
| 1·4 | 「사용가능여부가 일부사용가능 일때 물음표 아이콘 노출 및 아이콘 선택 시 툴팁 노출」(2곳) | 정책 — Description |
| 2 | `xlt` ↳ 「시간은 `order.useStateCheckedAt` 필드를 사용」 | `{{0}} 기준` **신규** |
| 3 | `xlt` | 툴팁 본문 **신규** |
| 5 | `xlt` | ♻️ **`UF_voucher_detail_balance_tooltip` 재사용** |

⚠️ **자동 매칭 오류를 수동으로 바로잡았다** — `collect_frames`가 핀 3의 대상을 `사용 가능 여부`(항목 라벨)로 잡았으나, 실제 툴팁 문구는 **`잔액과 사용 가능 여부는 반영 시점에 따라 실제와 다를 수 있습니다`**(y=638)다. 핸드오프의 「좌표 매칭 더미」 함정이 그대로 재현됐다.

⚠️ **핀 2개가 텍스트를 가린다**(`overlaps=[(1,'상품권 정보'),(4,'7,000원')]`) — 어노테이션 이미지를 크롭해 육안 확인했고, 정책·XLT 판정에는 영향이 없다.

**재사용 근거** — 핀 5 Figma 원문 `잔액은 반영 시점에 따라\n실제금액과 다를 수 있습니다.` ↔ 등록값 `UF_voucher_detail_balance_tooltip` = `잔액은 반영 시점에 따라 실제 금액과 다를 수 있습니다.` **줄바꿈·띄어쓰기 차이뿐**이며 등록값이 이미 교정된 표기다.

## (1a) 한국어 원문 교정

| Figma 원문 | XLT 값 | 사유 |
|---|---|---|
| `2026-09-17 기준` | `{{0}} 기준` | **날짜는 동적 값** — 코멘트 답글이 `order.useStateCheckedAt` 필드 사용을 명시. 리터럴 날짜를 등록하면 고정된다 |
| 잔액과 사용 가능 여부는 반영 시점에 따라 실제와 다를 수 있습니다 | …있습니다**.** | 마침표 추가 — 형제 키 `UF_voucher_detail_balance_tooltip`이 마침표를 쓴다(같은 화면의 툴팁 쌍) |

**alias 기록**: `{2026-09-17 기준 → {{0}} 기준}` · `{…있습니다 → …있습니다.}`

---

## (a) 자동 검증 요약

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

**통과 기준 P0 = 0 충족.**

## (b) 수동 3단계 체크표 — 전수 (2키 × 5언어 = 10셀)

### 1단계 — 한국어 원문
맞춤법·조사 정상. (1a) 2건 교정 외 이상 없음. 치환자 `{{0}}` **UIT 규칙(이중 중괄호)** 준수.

### 2단계 — 용어집 대조
「잔액」·「기준」은 용어집 미등재. **형제 키 `UF_voucher_detail_balance_tooltip`의 5개 언어 톤을 그대로 승계**했다(`反映のタイミングにより` · `depending on when it is updated` · `ขึ้นอยู่กับเวลาที่อัปเดต` · `因更新時間而`).

### 3단계 — 다국어 (전 10셀)

| 언어 | `{{0}} 기준` | 툴팁 본문 | 판정 |
|---|---|---|---|
| ja_JP | `{{0}} 基準` — 선례 `UF_voucher_price_fx_notice`(`{{1}} {{2}} 基準`) | `残高と利用可否は…実際と異なる場合があります。` — 형제 키 구문 승계 | ✅ |
| en_US | `As of {{0}}` — 선례 `UF_voucher_price_fx_notice`(`as of {{1}}`)·`Apps_search_popular_date`(`As of {0}`) | `The balance and availability may differ…` | ✅ |
| th_TH | `ณ {{0}}` — 선례 `ณ {{1}}` | `ยอดคงเหลือและสถานะการใช้งาน…` | ✅ |
| zh_TW | `{{0}} 基準` — **한자↔변수 공백 1칸**(2026-09-14 규칙) | `餘額與可用狀態可能因更新時間而…` | ✅ |
| ko_KR | (1단계 참조) | | ✅ |

**placeholder**: `{{0}}`가 5개 언어 전건에 1회씩 존재 ✅ · 언어 혼입·빈칸 없음.

## (c) P1/P2 각 건 처리 판정

**자동·수동 모두 검출 0건.** 판정 대상 없음.

## (d) 추가 개선·제안

### (d-1) 용어집 보완 권장 — 해당 없음
형제 키가 이미 5개 언어 톤을 확정해 뒀고 등재값 충돌이 없다. 대기 목록 등재 대상 아님.

### (d-2) 추가 개선·제안 안내
1. **Figma의 `2026-09-17`은 샘플 날짜다** — 변수화했으나 디자인에 변수 표기(`{{0}}`)가 없어 다음 조회 때 또 리터럴로 읽힐 수 있다. 디자인 측에 변수 표기 반영을 권장한다(원문 수정 요청은 올리지 않는다).
2. **툴팁 2종의 문구가 겹친다** — 「잔액과 사용 가능 여부는…」(상품권 정보)과 「잔액은…」(사용 가능 금액)이 같은 화면에 함께 노출된다. 하나로 합칠지 기획 확인 권장.

## 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 | **0건** ✅ |
| 수동 P0 | **0건** ✅ |
| P1·P2 | 검출 0건 ✅ |
| (d) 임의 적용 | 없음 ✅ |

→ **게이트 통과.** 위키 반영·엑셀 전달로 진행한다.

# 번역 품질 게이트 리포트 — 100엔딜 미등록 8키 + 신규 3키 (2026-09-16)

대상: **UNIFY-11369** FE 요청 대응. 위키 `4725963532` v25에 정의됐으나 **XLT 시스템 미등록인 8키**를 최초 등록하고,
FE가 추가 요청한 **신규 3문구**를 키로 정의한다. **11키 × 5개 언어 = 55셀 전수 점검.**

| 항목 | 값 |
|---|---|
| 담당 FE 팀 | **UIT** — `UF_` 프리픽스 · 치환자는 태그형 `<strong />`(아래 (c) 3번) |
| 업로드 대상 | **Unifi / WEB BROWSER / v1.7.9**(조회 시각 2026-09-16 22:40 · 2,592키) |
| 대조 레지스트리 | Unifi v1.7.9 · v1.7.6 · v1.7.5 · v1.7.4 · v1.7.3 + Dapp Portal v2.6.1 (전부 당일 신규 조회 · 캐시 미사용) |
| 용어집 | Landpress `web3_xlt_json` v5.5 (118 terms · 당일 조회) |
| 엑셀 | `xlt/xlt_100yen_11keys_20260916.xlsx` (properties 11행 + plurals 고정) |
| 위키 | [100엔딜 `4725963532`](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4725963532) v25 |
| Figma | `GOCHAYBS7hIrmWRGNuJOKV` · `100앤딜 상세` `74574:7790` · `상세 - OA Follow X` `74574:8127` · `OA 친구추가` `74574:8884` |

## 0. 사실 확인 — 「삭제된 키」가 아니라 「미등록 키」다

FE(진달래)는 카탈로그 동기화로 **7키가 삭제됐다**고 보고했다. 실측 결과 **삭제가 아니라 처음부터 등록된 적이 없다.**

- Unifi **v1.7.9 / v1.7.6 / v1.7.5 / v1.7.4 / v1.7.3** 전부에 **7키 모두 없음**(v1.7.5 이전에는 `UF_100yen_*` 키 자체가 0개)
- **Dapp Portal v2.6.1**에도 없음(`UF_100yen_home_btn`·`mini_home_100yen_more` 2키만 존재)
- 위키 v25 본문에 이미 「시스템 **미등록 8키** … 업로드 후 재동기화 필요」로 기재돼 있다

→ FE는 위키 기준으로 구현했고, XLT에 없는 키가 동기화에서 떨어진 것이다. 클론 티켓 **UNIFY-11368**(상세 타이틀 미노출)도 같은 원인(`UF_100yen_title` 미등록)이다.
사용자 결정에 따라 **FE가 지목한 7키 + `UF_100yen_pay_retry_btn` = 위키 정의 8키 전부**를 등록한다.

## 대상 11키

| # | XLT Key | ko_KR | 구분 |
|---|---|---|---|
| 1 | `UF_100yen_title` | 한국 인기 바우처 | 위키 정의 · 미등록 |
| 2 | `UF_100yen_usage_guide_title` | 바우처 이용 방법 | 위키 정의 · 미등록 |
| 3 | `UF_100yen_step1` | STEP 1 | 위키 정의 · 미등록 |
| 4 | `UF_100yen_step2` | STEP 2 | 위키 정의 · 미등록 |
| 5 | `UF_100yen_caution_contents9` | 대상 상품은 1인당 1점만 구매하실 수 있습니다. | 위키 정의 · 미등록 |
| 6 | `UF_100yen_caution_contents10` | 가격·재고는 예고 없이 변경될 수 있습니다. 실제 결제 금액은 상품 페이지에서 확인해 주세요. | 위키 정의 · 미등록 |
| 7 | `UF_100yen_coupon_applied` | 1,000엔 쿠폰 적용 중 | 위키 정의 · 미등록 |
| 8 | `UF_100yen_pay_retry_btn` | 다시 결제하기 | 위키 정의 · 미등록 |
| 9 | `UF_100yen_coupon_price` | 쿠폰 적용가 | **신규**(FE 요청) |
| 10 | `UF_100yen_discount_badge` | `<strong />`엔 할인 | **신규**(FE 요청) |
| 11 | `UF_100yen_oa_follow_btn` | 친구 추가하기 | **신규**(FE 요청) |

**키 이름 선례 실측**(사용자 확정 — 2026-09-16): `UF_100yen_` 전용 3키 신규. `_btn` 접미 다수 · `_badge`는 `UF_100yen_detail_badge` 선례 1건 · 가격 라벨 계열은 `UF_voucher_price_*` 4키(`혜택가`·`정가`·`상시 할인가`·`단독 혜택가`)를 참조했다.
`친구 추가하기`는 기존 `UF_settings_alarm_line_oa_agree_status1`에 동일 ko가 있으나 **재사용하지 않고 전용 키를 만들었다**(설정 화면과 값이 묶여 한쪽만 고칠 수 없게 되는 것을 피함 — 2026-09-15 주의사항 7키와 같은 판단).

## (1a) 한국어 원문 교정

| 대상 | Figma 원문 | XLT Key 값 | 처리 |
|---|---|---|---|
| `UF_100yen_coupon_price` | `쿠폰적용가` | **`쿠폰 적용가`** | **띄어쓰기 교정** — 「쿠폰」과 「적용가」는 별개 단어다. 등록 선례도 `상시 할인가`·`단독 혜택가`로 띄어 쓴다. **alias 기록: {Figma `쿠폰적용가` → XLT `쿠폰 적용가`}** |
| `UF_100yen_discount_badge` | `1000` + `円 割引`(ja 디자인) | **`<strong />엔 할인`** | 숫자를 변수로 분리. ko 원문은 Home 배너 등록값 `지금 1,000엔 할인!`의 어휘를 따랐다 |
| `UF_100yen_oa_follow_btn` | `친구 추가하기` | 동일 | 교정 없음 |
| 8키(위키 정의분) | — | 위키 v25 값 그대로 | 이미 교정된 값이다(`구입하실`→`구매하실` · `지불 금액`→`결제 금액` · 구분자 `・`→`·`). 이번에 재확인만 했고 **추가 교정 0건** |

전 11키 한국어 맞춤법·띄어쓰기·표기 일관성을 재확인했고 위 1건 외 교정 사항은 없다.

## (a) 자동 검증 — `scripts/validate_translation.py`

`python3 scripts/validate_translation.py xlt/xlt_100yen_11keys_20260916.xlsx scripts/glossary.json`

| 심각도 | 건수 |
|---|---|
| **P0** | **0건** |
| P1 | 9건 |
| P2 | 2건 |

용어집 118개 용어 로드 · 1·2·3단계 전부 실행. 리포트: `xlt/xlt_100yen_11keys_20260916_validation_report.md`

## (b) 수동 3단계 체크표 — 전수 점검 (11키 × 5언어 = 55셀 · 전체 파일)

`md/check.md`·`md/translation_validation_guide.md` 절차로 **전체 행**을 한 줄씩 육안 검토했다. 신규 3키로 한정하지 않고 **위키 정의 8키의 5개 언어까지 전수** 확인했다(그 8키는 시스템 등록을 거친 적이 없어 검증 이력이 없다).

| # | 단계 | 점검 항목 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | **1단계** | 한국어 원문 전 11행 | ✅ | 교정 1건((1a) `쿠폰 적용가`) 외 맞춤법·띄어쓰기 이상 0 |
| 2 | **1단계** | 붙은 띄어쓰기 누락(`누리고다양한`류) | ✅ | 11행 전건 없음 |
| 3 | **1단계** | 다의어 모호 | ✅ | `적용가`·`할인`·`친구 추가` 모두 화면 맥락에서 단의 |
| 4 | **2단계** | 용어집 `바우처` 대조 | ⚠️ → (c) 1 | ja 정본 `クーポン`인데 `UF_100yen_title` ja는 `バウチャー`. 2026-09-16 검수 결정과 얽힌다 |
| 5 | **2단계** | 용어집 `결제`·`구매`·`확인`·`가격` 대조 | ⚠️ → (c) 2 | 검증기 P1 5건 — 전부 문맥 판정 필요 |
| 6 | **2단계** | deprecated 표기(`OA 팔로우`·`채널 추가`) | ✅ | 55셀 전건 미사용 |
| 7 | **3단계** | 언어 혼입(문자체계) | ✅ | ja에 한글 0 · th 타이 문자 정상 · zh **번체 정자**(`韓國`·`優惠券`·`購買`) · ko 칸 한글 정상(`STEP 1/2` 제외 — (c) 5) |
| 8 | **3단계** | 자리표시자 일치 | ✅ | `<strong />` 5개 언어 전건 **정확히 1개**씩 · 나머지 10키는 변수 0개 |
| 9 | **3단계** | 외래어 음차 오염(`포이가츠`류) | ✅ | 55셀 전건 없음 |
| 10 | **3단계** | 의미 등가 — 캐션 9 | ✅ | ja `お一人様1点まで` · en `only one eligible item per person` · th `ซื้อได้ 1 ชิ้นต่อคนเท่านั้น` · zh `每人僅可購買 1 件` — 전건 「1인 1점 한정」 보존 |
| 11 | **3단계** | 의미 등가 — 캐션 10 | ✅ | 「가격·재고 예고 없이 변경」+「실제 결제 금액은 상품 페이지 확인」 2문 구조가 4개 언어 모두 유지 |
| 12 | **3단계** | 의미 등가 — 신규 `쿠폰 적용가` | ✅ | ja `クーポン適用価格` · en `Coupon price` · th `ราคาหลังใช้คูปอง`(쿠폰 사용 후 가격) · zh `優惠券適用價` |
| 13 | **3단계** | 의미 등가 — 신규 배지 | ✅ | ja `<strong />円割引` · en `<strong /> JPY off` · th `ลด <strong /> เยน` · zh `折 <strong /> 日圓` — 어순이 각 언어 자연 어순 |
| 14 | **3단계** | 의미 등가 — 신규 버튼 | ✅ | `友だち追加`·`Add friend`·`เพิ่มเพื่อน`·`加為好友` — 기존 등록값과 글자 단위 동일(선례 복사) |
| 15 | **3단계** | 실사용 표기 실측(0건 표기 금지) | ✅ | ja `クーポン` 26 · `割引` 2 · `友だち追加` 23 / th `ราคา` 30 · `ลด` 12 · `เยน` 35 · `เพิ่มเพื่อน` 17 / zh `優惠券` 29 · `價` 42 · `日圓` 35 · `加為好友` 2 / en `off` 14 · `price` 21 — **전건 1건 이상** |
| 16 | **3단계** | 태그 표기 관례 | ✅ | Unifi 등록값에 `<span />` 60여 키 · `<em />` 1키(`UF_interest_promotion3_banner_title`) — 태그형 치환자는 확립된 관례다 |
| 17 | **3단계** | 문말 부호 일관성 | ✅ | 캐션 9·10은 문장형 → 마침표 유지(ja `。`/zh `。`) · 라벨·배지·버튼 5키는 마침표 없음(정상) |
| 18 | **3단계** | 8키 값 ↔ 위키 v25 대조 | ✅ | 40셀 **글자 단위 일치**(위키 storage에서 파싱해 엑셀 생성 · 임의 수정 0) |
| 19 | **3단계** | 등록값 충돌 | ✅ | 11키 전건 Unifi·Dapp Portal **미등록** — 기존 키 덮어쓰기 위험 0 |
| 20 | **3단계** | ja 검수 반영분 훼손 | ✅ | 이번 11키는 09-16 검수 39키와 **교집합 0** — 되돌리는 값 없음 |

## (c) 처리 판정 — 검증기 P1 9건 · P2 2건 전건

| # | 심각도 | 건 | 판정 | 사유 |
|---|---|---|---|---|
| 1 | P1 | `UF_100yen_title` ja `バウチャー` → 용어집 정본 `クーポン` 권장(2건) | **위키 확정값 유지(정책 선례 준용)** | 2026-09-16 일본어 검수에서 **`バウチャー`를 채택**했고 사용자가 「검수 우선 · 신규·변경분부터 적용, 기존은 소급하지 않는다」로 확정했다(`gate_report_ja_review_xlt_39keys_2026-09-16.md` (c) 2). 이 키는 신규 등록분이므로 그 결정이 그대로 적용된다. 용어집 정본 변경 여부는 이미 **대기 목록 #23**에 있다 |
| 2 | P1 | `UF_100yen_caution_contents9` en `구매`→`Buy` 권장 | **오탐** | 값은 `You may purchase only one eligible item per person.` — 용어집 `Buy`는 **버튼·CTA 라벨**용이고 여기는 서술문의 동사다. 등록값도 서술문에서 `purchase`를 쓴다 |
| 3 | P1 | `UF_100yen_caution_contents10` ja `결제`→`決済` 권장 | **오탐(표현 우선)** | `実際のお支払い金額`은 「실제 결제 금액」의 자연스러운 일본어다. `決済` 58키 · `お支払い` 5키로 소수이나 **금액 문맥에서는 `お支払い金額`이 관용**이다. → (d-2) 3번에 통일 검토로 올림 |
| 4 | P1 | `UF_100yen_caution_contents10` th `결제`→`การชำระเงิน` 권장 | **오탐** | 값은 `ยอดชำระจริง`(실제 결제액)로 **명사구 축약**이다. 용어집 표제형을 그대로 넣으면 `ยอดการชำระเงินจริง`로 불필요하게 길어진다 |
| 5 | P1 | `UF_100yen_caution_contents10` zh `확인`→`查看` 권장 | **오탐** | `請於商品頁面確認`은 「확인해 주세요」의 직역이고, 등록값 실측도 `確認` **101키** / `查看` **91키**로 양쪽 모두 정착돼 있다. 「보다」가 아니라 「확인하다」 문맥이라 `確認`이 맞다 |
| 6 | P1 | `UF_100yen_pay_retry_btn` th `결제`→`การชำระเงิน` 권장 | **오탐** | 버튼 라벨 `ชำระเงินอีกครั้ง`는 동사구가 맞다. 명사형은 버튼에 쓰지 않는다 |
| 7 | P1 | `UF_100yen_step1` ko 칸에 라틴 문자열(`STEP 1`) | **오탐(디자인 의도)** | Figma 실측상 5개 언어 모두 `STEP 1` 표기다. 위키 v25 확정값이며 다국어 표에도 5개 언어 동일로 기재돼 있다 |
| 8 | P1 | `UF_100yen_step2` ko 칸에 라틴 문자열(`STEP 2`) | **오탐(디자인 의도)** | 위 7번과 동일 |
| 9 | P1 | `UF_100yen_title` ja 「구 표기」 경고 | **1번과 동일 건** | 검증기가 「용어 불일치」·「구 표기」 두 규칙으로 중복 보고한 것이다 |
| 10 | P2 | `UF_100yen_coupon_applied` 마침표 없음 | **오탐** | 상태 배지 라벨이다. 라벨·배지는 마침표를 쓰지 않는 것이 등록값 관례다 |
| 11 | P2 | `UF_100yen_discount_badge` 마침표 없음 | **오탐** | 배지 라벨 — 위와 동일 |

**변수 표기 결정(사용자 확정 2026-09-16)**: 배지는 UIT 규칙의 `{{0}}`이 아니라 **FE 요청대로 `<strong />`**로 등록한다. 숫자를 굵게 렌더하는 자리라 태그가 의미를 갖고, Unifi 등록값에 태그형 치환자 선례가 60여 키 있다. **`{{0}}`을 쓰면 굵게 표시가 사라지므로 FE 구현과 어긋난다.**

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장
**신규 등재 0건.** `바우처` ja 정본(`クーポン` ↔ 검수 `バウチャー`) 문제는 이미 `md/glossary-backlog.md` **#23**에 있고, 이번 건도 그 결정을 기다리는 사안이라 **중복 등재하지 않는다**. 「모아서 한 번에」 원칙대로 반영 시점은 사용자가 「용어집 리뷰」를 요청할 때다.

### (d-2) 추가 개선·제안
1. **화면 안에서 ja `バウチャー`/`クーポン`이 섞인다(권장 · 미반영)** — 같은 상세 화면에서 타이틀·`oa_follow_desc`는 `バウチャー`, 이용 방법·캐션·배지는 `クーポン`이다. 검수 결정(신규분부터 `バウチャー`)과 기존 등록값(`クーポン` 26키)이 만나는 지점이라 **화면 단위로 어느 쪽에 맞출지 결정**하는 편이 낫다. 용어집 #23과 함께 판단하기를 권한다.
2. **`쿠폰 적용가` 라벨 길이(권장 · 디자인 확인)** — ja `クーポン適用価格`(8자) · th `ราคาหลังใช้คูปอง`(16자)로, Figma의 좁은 가격 라벨 영역에서 줄바꿈이 날 수 있다. 실기기 확인을 권한다. 짧게 간다면 ja `クーポン適用後`·th `ราคาหลังคูปอง`가 후보다.
3. **ja 금액 표기 통일 검토(권장 · 미반영)** — `お支払い金額`(5키)과 `決済金額`(58키 계열)이 공존한다. 결제 관련 화면 전체를 한 번에 볼 때 정리하는 것이 맞다.
4. **Figma 원문 수정 요청 — 없음.** `쿠폰적용가` 띄어쓰기는 위 (1a)에 alias로 기록했다.
5. **FE 키 이름 정정 안내(필수 후속)** — FE 코드가 쓰는 이름과 등록 키 이름이 다른 건 없다(이번에 위키 이름 그대로 등록). 다만 **`UF_100yen_share_tooltip` 하드코딩 문구**(위키 v25 기재 · 운영 화면 실측 「친구에게도 혜택 알려주세요!」)는 여전히 키로 교체되지 않았다 — 같은 티켓에서 함께 전달할 것을 권한다.

## 통과 판정

**P0 = 0건(자동 + 수동 55셀 전수) → 통과.** 엑셀 생성·위키 반영으로 진행한다.
업로드(**Unifi / WEB BROWSER**)는 **사용자가 수행**한다 — XLT 쓰기 API가 없다.

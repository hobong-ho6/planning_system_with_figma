# 게이트 리포트 — my_voucher_detail 브랜드 4건 × 5개 언어 (2026-09-12)

- **대상**: LV `voucher_product.my_voucher_detail` — 올리브영 · CU · 다이소 · 이마트24 × 5개 언어 = 20개 JSON
- **설계**: My › 바우처 상세는 `voucher_detail`의 **축약 화면**이다(사용자 확정).
  - `usage` — Figma 원문(`uyUkM7rWWg1vAwpf47um86` / `67737-5276`)을 그대로 쓰되 **브랜드 이름만 치환**
  - `notice.sections` — `voucher_detail.productInfo.sections`를 **그대로 재사용**(이미 5개 언어 이관 완료 · 별도 게이트 리포트 `gate_report_voucher_detail_4brands_2026-09-12.md`)
- **신규 번역 대상**: `usage.title` · `usage.items[0~1].description` · `notice.title` = **행 16건**
- **검증 범위**: 신규 번역 **전체 행 16건 전수 점검**. 재사용한 `notice.sections`는 앞선 리포트에서 전수 검증을 마쳤다(전체 파일 기준 중복 검증 아님).
- **엑셀**: `xlt_output_20260912135311.xlsx`

---

## (1a) 한국어 원문 교정

Figma 원문을 **그대로 쓰되 오타·띄어쓰기만 교정**했다. 의미를 바꾼 곳은 없다.

| # | Figma 원문 | 교정 | 사유 |
|---|---|---|---|
| 1 | `올리브명 매장에서` | `올리브영 매장에서` | **오타** (명 → 영) |
| 2 | `결제시` | `결제 시` | 띄어쓰기(의존명사 「시」) |
| 3 | `소진할 때 까지` | `소진할 때까지` | 띄어쓰기(조사 「까지」) |

**alias**: `올리브명 매장에서 결제시 … → 올리브영 매장에서 결제 시 …` · `소진할 때 까지 → 소진할 때까지`
→ Figma 원본은 미수정(디자이너 확인 대상).

### 브랜드 치환 — `voucher_detail` 위배 여부 확인

브랜드 이름만 바꾸므로, 바꾼 문장이 각 브랜드의 `voucher_detail` 내용과 어긋나지 않는지 전수 확인했다.

| 브랜드 | step 1 주어구 | 위배 여부 |
|---|---|---|
| 올리브영 | 올리브영 매장에서 | 없음 — 매장 결제 시 바코드 제시가 원문과 일치 |
| CU | 전국 CU 매장에서 | 없음 — 사용처 「전국 CU 매장」과 일치 |
| 다이소 | 다이소 매장에서 | 없음 — 매장 제시 또는 셀프계산대 모두 가능(원문), 제시 방식은 위배 아님 |
| 이마트24 | 이마트24 매장에서 | 없음 — 「결제 시 모바일 금액권을 제시」와 일치 |
| 공통 step 2 | 유효기간 내 잔액 소진까지 | 없음 — 4브랜드 모두 **잔액 차감·분할 사용 가능**(올리브영 분할 사용 / CU 잔액 관리 / 다이소 금액 차감형 / 이마트24 잔액 재저장) |

---

## (a) 자동 검증 결과 — `validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | 8건 |
| 🟢 P2 | 0건 |

**통과 기준 P0 = 0 충족.**

---

## (b) 수동 3단계 체크표 — 신규 행 16건 전수

### 1단계 · 한국어(ko_KR)

| 키 | 판정 | 근거 |
|---|---|---|
| `usage.title` = 사용 방법 | 정확 | 화면 라벨과 일치 |
| `usage.items[0]` (4브랜드) | 정확 | 오타·띄어쓰기 교정 반영 · 브랜드명만 상이 |
| `usage.items[1]` | 정확 | 줄바꿈 위치 Figma와 동일 |
| `notice.title` = 상품 설명 및 유의사항 | 정확 | 화면 라벨과 일치 |

붙은 띄어쓰기·외래어 음차 오염·다의어 모호 없음.

### 2단계 · 용어집(v5.4) 대조

| 용어 | 정본 | 적용 | 판정 |
|---|---|---|---|
| 결제 | ja `決済` · en `Payment` · th `การชำระเงิน` · zh `付款` | ja `決済時` · zh `付款時` 적용 / en·th는 동사형(`paying`·`ชำระเงิน`) | 정확 (동사 문맥) |
| 유효기간 | 미등재 | ja `有効期限` · zh `有效期限` · en `validity period` · th `ระยะเวลาที่กำหนด` | (d-1) |
| 브랜드 표기 | — | 기존 등록값에서 추출: ja `オリーブヤング`/`ダイソー`/`イーマート24`/`CU` · en·th·zh `OLIVE YOUNG`/`DAISO`/`emart24`/`CU` | 정확 (기존 표기 승계) |

### 3단계 · 언어별

| 언어 | 점검 | 판정 |
|---|---|---|
| ja_JP | 경어체(`ご利用いただけます`) 일관 · 조사 정상 | 정확 |
| en_US | 명령/설명문 혼용 없음 · 브랜드 대문자 표기 승계 | 정확 |
| th_TH | 구절 띄어쓰기 · 브랜드명 로마자 유지 | 정확 |
| zh_TW | **번체** 유지(`門市`·`優惠券`·`餘額`·`有效期限`) · 간체 0 | 정확 |
| 전 언어 | step 2의 `\n` 줄바꿈 위치 ko와 동일 · `notice.sections`는 `voucher_detail`과 **바이트 동일** | 정확 |

---

## (c) 자동 검증 건별 처리 판정

### P0 — 0건

### P1 8건

| 키 | 권장 | 판정 |
|---|---|---|
| 4브랜드 `usage.items[0]` en_US | `결제` → `Payment` | **오탐** — `When paying …` 동사형이 문맥에 맞음 |
| 4브랜드 `usage.items[0]` th_TH | `결제` → `การชำระเงิน` | **오탐** — `เมื่อชำระเงิน` 동사형이 정확 |

### P2 — 0건

---

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

### (d-1) 용어집 보완

`유효기간` · `쿠폰함` 미등재. 특히 **`쿠폰함`**은 이번에 5개 언어 표기를 새로 정했다
(ja `クーポンボックス` · en `coupon box` · th `กล่องคูปอง` · zh `優惠券頁面`). 사용자 지시대로 **전체 번역 완료 후 일괄 등재**하며, 권장 표기는 `fetch_xlt_registry.py` 실측 후 제출한다.

### (d-2) 추가 개선 제안

1. **Figma 원문 오타** — `올리브명`(1a-1)은 디자인 원본 수정이 필요하다
2. **`notice` 분량** — `voucher_detail.productInfo`를 그대로 재사용해 축약 화면에 전체 약관이 들어간다. 화면 설계상 **접기/펼치기**가 전제인지 확인 권장
3. **zh `쿠폰함` 표기** — `優惠券頁面`(쿠폰 페이지)으로 의역했다. 앱 내 실제 화면 명칭이 있으면 그 표기로 통일 권장

---

## 결론

- **P0 = 0건**(자동 + 수동) → 산출물 출력 가능
- P1 8건 전부 오탐 판정 · P2 0건
- (d) 권장 사항은 사용자 결정 전까지 미적용

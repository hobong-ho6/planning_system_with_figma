# 게이트 리포트 — `UF_clinic_mini_bridge_review_verified` 신설 + 통합 엑셀 86키

- **작업일**: 2026-09-03 (clinic 세션 #3)
- **대상 위키**: `4667512757` v109 → **v110**
- **담당 팀**: UIT (`UF_` · `{{0}}`)
- **산출물**: `xlt/xlt_output_20260903230625.xlsx` — **86키**(통합 정본 · 위키 다국어 표 전량)
- **레지스트리**: Unifi / WEB BROWSER / v1.7.4 · 2,431키 · **용어집** v4.7 (작업 시점 조회)

## 배경 — 한 키에 두 문구

사용자가 브릿지 Screen XLT 표에 `UF_clinic_detail_review_verified` / `시술인증`을 추가했는데, 같은 키가 상세·리뷰 화면에서는 `영수증 인증`으로 **5개 언어 번역까지 완료**된 상태였다. XLT는 키 하나에 값 하나만 내려주므로 두 화면 중 한쪽이 틀린 문구를 노출한다.

**Figma 원문 실측 — 실제로 다른 문구다**

| 화면 | 노드 | ko |
|---|---|---|
| `(Unifi mini) 브릿지페이지` 후기 카드 | `71241:9154` y=17372 | **`시술인증`** |
| `(Unifi mini) 상세페이지 - 전체 리뷰` | `71543:3322` y=328·475·675·873 | `영수증 인증` |
| `(Unifi mini) 상세페이지 - 개별리뷰` | `71417:17133` y=736 | `영수증 인증` |

→ **사용자 결정: 브릿지용 별도 키 신설**(문구 통일 아님). 기존 `UF_clinic_detail_review_verified`(`영수증 인증`)는 값·번역 그대로 유지.

**신규 키 판정**: `시술인증`·`시술 인증`·`영수증 인증` 모두 레지스트리 **완전일치 0건** → 신규.
**프리픽스**: `UF_clinic_mini_bridge_` (같은 화면 19키 선례 · 2026-09-03 신설 네임스페이스).

---

## (1a) 한국어 원문 교정

| 항목 | 원문 | 확정값 | 사유 |
|---|---|---|---|
| `UF_clinic_mini_bridge_review_verified` | Figma `시술인증` | **`시술인증`** | 교정 없음 — Figma 원문·사용자 입력 일치 |

**표기 불일치 지적(원문 유지 · 사용자 결정 사안)**: 형제 키가 `영수증 인증`(**띄어씀**)인데 이 키는 `시술인증`(**붙여씀**)이다. 같은 배지 계열이므로 `시술 인증`으로 띄어쓰기를 통일하는 편이 일관되나, Figma 원문이 붙여쓰기이고 사용자가 그대로 입력했으므로 유지했다 → (d-2).

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py`

실행: `python3 scripts/validate_translation.py xlt/xlt_output_20260903230625.xlsx scripts/glossary.json`

| 심각도 | 85키(직전) | **86키(현재)** | 차이 |
|---|---|---|---|
| 🔴 **P0** | 0건 | **0건** | 불변 |
| 🟡 P1 | 59건 | **59건** | **신규 0건** |
| 🟢 P2 | 9건 | **9건** | 불변 |

신규 키가 **새로운 P1/P2를 만들지 않았다**(A/B 실측).

---

## (b) 수동 3단계 체크표 — **전수 점검**

검토 범위: 통합 엑셀 **전체 86행 × 5개 언어 = 430셀**. 신규 1키에 한정하지 않고 전수 재점검했다.

### 1단계 — 기계적 정합

| 항목 | 결과 |
|---|---|
| 빈 셀 | ✅ 0건 (430/430 — 엑셀 생성 시 assert 가드) |
| 치환자 | ✅ 신규 키는 치환자 없음 · 전체 7키의 `{{N}}` 집합 5개 언어 일치 |
| 문자체계 | ✅ ja 한자·가나 / en 라틴 / th 타이문자 / zh 번체 — 혼입 0 |
| zh 간체 | ✅ 0건 (`療程已驗證` 전건 번체) |
| ja↔zh 한자 상호 오염 | ✅ `施術認証済み`(ja 어법·`済み`) / `療程已驗證`(zh 어법·`已`) — 교차 0 |
| 앞뒤공백·nbsp·U+2028 | ✅ 0건 |

### 2단계 — 의미 정합 (ko 기준)

| 언어 | 값 | 판정 · 근거 |
|---|---|---|
| ko | `시술인증` | ✅ Figma 원문 |
| ja | `施術認証済み` | ✅ 「시술」 ja `施術` **11/11 만장일치** + 형제 키 `レシート認証済み`의 `～認証済み` 어형 승계. 등재 `UF_settings_main_submenu_email_on`(`인증 완료`)도 `認証済み` |
| en | `Treatment verified` | ✅ 「시술」 en `treatment` **11/11** + 형제 `Receipt verified` 어형 승계. 등재 `인증 완료` = `Verified` |
| th | `ยืนยันทรีตเมนต์แล้ว` | ✅ 「시술」 th `ทรีตเมนต์` **11/11** + 형제 `ยืนยันใบเสร็จแล้ว`의 `ยืนยัน…แล้ว` 어형 승계. 등재 `인증 완료` = `ยืนยันแล้ว` |
| zh | `療程已驗證` | ✅ 「시술」 zh `療程` **11/11** + 형제 `收據已驗證`의 `已驗證` 어형 승계 |

**형제 키와 어형이 완전히 평행**하다 — `{대상}+인증 완료` 구조를 4개 언어 모두 동일 패턴으로 유지했다.

### 3단계 — 언어별 중점 점검

| 언어 | 결과 |
|---|---|
| ja | ✅ `済み`(완료 상태) 사용 — 배지 라벨로 적절 · 음차 오염 0 |
| en | ✅ 문장 첫 글자만 대문자(라벨 관례) · 5자+8자로 배지 폭 여유 |
| th | ✅ `ยืนยัน`(확인) + `แล้ว`(완료) 어법 정상 |
| zh | ✅ `已`(완료) + `驗證` — 형제 키와 동일 어휘 선택 |

---

## (c) 자동 검증 보고 건별 처리 판정

신규 키에 대한 지적은 **0건**이다. 전체 P1 59 + P2 9 = 68건은 직전 통합 리포트(`gate_report_clinic_consolidated_85keys_2026-09-03.md`) (c)의 판정을 그대로 승계한다 — **실제 위반 0 · 오탐 67 · 확인 완료 1**(`UF_main_faq_title` ja=`FAQ`, 2026-09-01 사용자 확정분).

---

## ⚠️ 작업 중 발생한 사고와 복구 (기록)

**사고**: v109 PUT 시 자동화 스크립트(`put.py`)가 **직전 본문 파일(`body_new5.xml`)을 그대로 올려**, v108에서 사용자가 추가한 Screen 1행(`UF_clinic_detail_review_verified` / `시술인증`)이 유실됐다. 원인은 본문 파일 경로를 갱신하는 `sed`가 **실패한 첫 시도의 명령 체인에 있어 실행되지 않았고**, 재시도에서 다시 실행하지 않은 것이다.

**영향 범위 확인**: v108 ↔ v109 전문을 줄 단위로 대조한 결과 **차이는 그 1행뿐**이었고 다른 변경·서식 손실은 없었다.

**복구**: v110에서 그 행을 **신규 키(`UF_clinic_mini_bridge_review_verified`)로 복원**했다(사용자 결정과 일치). History에도 사고·복구 사실을 남겼다.

**재발 방지**: 본문 생성과 PUT을 **한 스크립트에서 수행**하도록 바꿨다(`fix7.py` — 생성 직후 같은 프로세스에서 버전 가드 후 PUT). 파일 경로를 외부 `sed`로 갈아끼우는 방식은 쓰지 않는다.

---

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장

**신규 권장 없음.** 이번 키에서 용어집 오탐이 발생하지 않았다(P1 0건). 「시술」·「인증」은 4개 언어 모두 등재 실사용이 압도적으로 일관돼(11/11) 보완이 불필요하다. 기존 권장 5건은 통합 리포트 (d-1)에 그대로 유효하다.

### (d-2) 추가 개선·제안 안내

1. **`시술인증` 띄어쓰기** — 형제 키 `영수증 인증`과 달리 붙여쓰기다. `시술 인증`으로 통일 권장(Figma 원본 수정 필요).
2. **배지 2종이 같은 화면군에 공존** — 브릿지 후기 카드는 `시술인증`, 상세·리뷰는 `영수증 인증`이다. **사용자가 두 문구를 유지하기로 결정**했으나, 실사용자에게 서로 다른 인증 체계로 읽힐 수 있다 — 기획 관점 재확인 권장.
3. **위키 첨부 정리** — `xlt_clinic_all_85keys_20260903.xlsx`는 86키 판으로 대체됐다. 구버전 표기 또는 삭제 권장(**첨부 삭제는 사용자 확인 후**).
4. **다국어 미확장 Landpress 필드** — 이번 키와 무관하나, 병원별 3필드 × 3곳과 `faq` 확장분은 ko만이다. 베타 확인 후 진행 예정(사용자 결정).

---

## (4) 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 = 0 | ✅ 0건 · 신규 P1/P2 0건(A/B 실측) |
| 수동 P0 = 0 | ✅ 전수 점검(86행 × 5언어 = 430셀) 0건 |
| (a)~(d) 리포트 | ✅ 본 문서 |
| 한국어 원문 교정 | ✅ (1a) — 교정 0건, 띄어쓰기 통일은 권장으로 분리 |
| 전수 점검 | ✅ 전체 파일 기준(신규 1키 한정 아님) |

→ **위키 v110 반영 완료** · `check_wiki_storage.py` post exit 0. 통합 엑셀 **86키**가 업로드 정본이다.

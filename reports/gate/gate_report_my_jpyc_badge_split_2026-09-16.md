# 번역 품질 게이트 리포트 — `UF_my_jpyc_connected_badge` 키 분리 (2026-09-16)

대상: Slack 스레드(`C07R7CE0B5H` · `1789540398.145119`) **1번 요청** — My › 지갑 연결 후 배지의 **디자인 적용을 위한 키 분리**.
**2키 × 5개 언어 = 10셀** · **전수 점검**. ⚠️ **신규 번역이 아니라 기존 등록값의 분할**이다 — 새 문구를 만들지 않았다.

## 요청 원문 → 확정 내역

| 요청(Slack) | 확정 | 사유 |
|---|---|---|
| `UF_my_jpyc_connected_badge` : `<span /> 연결됨` | 동일 | — |
| `UF_my_jpyc_connected_badge _1` : `JYPC 계좌` | **`UF_my_jpyc_connected_badge_1`** : **`JPYC 계좌`** | ⓐ 키 이름의 **공백 제거**(`badge _1` → `badge_1`) ⓑ **`JYPC` → `JPYC` 오타 교정** |

## (1a) 한국어 원문 교정

**교정 2건** — 모두 요청 원문의 오기다.

1. **`JYPC` → `JPYC`** — 등록값·용어 전반이 `JPYC`다. 이 저장소 등록값에 `JYPC`는 **0건**(실측).
2. **키 이름 `UF_my_jpyc_connected_badge _1`의 공백 제거** — XLT 키에 공백은 쓰지 않는다. Slack 번역봇이 옮긴 영문에도 `UF_my_jpyc_connected_badge_1`로 공백 없이 나온다.

`<span />`은 문구가 아니라 **FE가 `_1` 값을 끼워 넣는 자리**다 — 등록값에 그대로 두는 것이 이 저장소 관례다(선례: `UF_guide_deposit_btn1` `지금 가입하고 최대 <span /> 혜택받기`).

## 확정 값 — ⛔ 복원 검증으로 분할의 정확성을 증명한다

**분리한 두 값을 다시 합치면 현재 등록값과 글자 하나까지 같아야 한다.** 5개 언어 전건 통과.

| 언어 | 분리 전(현재 등록값) | `_badge` | `_badge_1` | 복원 |
|---|---|---|---|---|
| ko_KR | `JPYC 계좌 연결됨` | `<span /> 연결됨` | `JPYC 계좌` | `JPYC 계좌 연결됨` ✅ |
| ja_JP | `JPYC口座連携済み` | `<span />連携済み` | `JPYC口座` | `JPYC口座連携済み` ✅ |
| en_US | `JPYC account connected` | `<span /> connected` | `JPYC account` | `JPYC account connected` ✅ |
| th_TH | `เชื่อมโยงบัญชี JPYC แล้ว` | `เชื่อมโยง<span /> แล้ว` | `บัญชี JPYC` | `เชื่อมโยงบัญชี JPYC แล้ว` ✅ |
| zh_TW | `JPYC 帳號已連結` | `<span />已連結` | `JPYC 帳號` | `JPYC 帳號已連結` ✅ |

> ⚠️ **th_TH에서 실제로 한 번 실패했다** — `เชื่อมโยง<span />แล้ว`로 두면 복원값이 `…JPYCแล้ว`가 되어 **공백 1칸이 사라졌다**. `<span />` 뒤에 공백을 넣어 `เชื่อมโยง<span /> แล้ว`로 고쳤다. **어순이 다른 언어(태국어는 동사 선행)에서는 자리표시자 앞뒤 공백이 눈으로 안 보인다** — 복원 검증이 없었으면 그대로 등록될 뻔했다.

## (a) 자동 검증 — `scripts/validate_translation.py`

| 심각도 | 건수 |
|---|---|
| **P0** | **0건** |
| P1 | **0건** |
| P2 | **0건** |

## (b) 수동 3단계 체크표 — 전수 점검 (2키 × 5개 언어 = 10셀)

### 1단계 — 한국어 원문
| 점검 | 결과 |
|---|---|
| 맞춤법·띄어쓰기 | `JPYC 계좌` · `<span /> 연결됨` 정상 |
| 표기 일관성 | `JPYC` 대문자 4자 — 등록값 전체와 일치(`JYPC` 0건) |
| 조사·받침 | 해당 없음(명사구 + 완료형) |

### 2단계 — 용어집 대조 (v5.5)
| 용어 | 등재값 | 적용 | 결과 |
|---|---|---|---|
| 연결하기 | ja `連携` 계열 | `連携済み` 유지 | 기존 등록값 승계 |
| 계정/계좌 | 미등재 | 기존 `口座`·`account`·`บัญชี`·`帳號` 승계 | 대조 대상 없음 |

### 3단계 — 언어별 의미·표기 (10셀 전수)
| 점검 | 결과 |
|---|---|
| **복원 일치** | **5/5** — 위 표 |
| 자리표시자 `<span />` | 2키 중 `_badge`에만 · 각 언어 **1회** |
| 어순 | th는 동사 선행이라 `<span />`이 문중에 온다(ko·ja·en·zh는 선두) — 의도된 차이 |
| 이질 문자체계 혼입 | 0건 |
| 띄어쓰기 | `<span />` 앞뒤 공백을 **언어별로 원본과 동일하게** 맞춤(th 1칸·zh·ja 0칸) |

## (c) 검증기 보고 건별 처리 판정

자동 검증 **P0/P1/P2 전부 0건** — 처리 대상 없음.

## (d) 추가 개선·제안 (권장 — 사용자 결정 전 임의 반영 금지)

### (d-1) 용어집 보완 — 해당 없음
새 용어가 없다(기존 등록값 분할).

### (d-2) 추가 개선·제안

1. **분리 후 기존 키의 의미가 바뀐다** — `UF_my_jpyc_connected_badge`는 이제 **단독으로 완결된 문장이 아니다**(`<span />`가 비면 `연결됨`만 남는다). FE가 `_1`을 반드시 함께 렌더해야 한다 — 위키 Screen 표에 그 의존관계를 명시했다.
2. **`_1` 접미 네이밍** — 이 저장소에 `_1` 접미 키 선례가 드물다. 의미를 담은 이름(예: `_label`)이 나중에 읽기 쉽지만, **요청자가 지정한 이름**이라 그대로 따랐다.
3. **Slack 요청 2·3번은 이 리포트 범위 밖**이다 — 2번(미션 리워드 내역 URL)은 위키에 함께 반영했고, 3번(빈 상태 보조 문구 신규 키)은 **문구가 아직 없어 미착수**다.

## 통과 판정

**P0 = 0건**(자동 + 수동 전수 10셀) · **복원 검증 5/5** → 엑셀 산출·위키 반영 진행.

**산출물**: `xlt/xlt_my_jpyc_badge_split_unifi_2026-09-16.xlsx` — ⛔ **업로드 대상 서비스는 `Unifi`**(My는 UIT 담당). XLT는 쓰기 API가 없어 **업로드는 사용자가 수행**한다.

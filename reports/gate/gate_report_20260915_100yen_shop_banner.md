# 번역 품질 게이트 리포트 — 100엔샵 배너 (Unifi v1.7.9.1 핫픽스)

- **작업일**: 2026-09-15
- **대상**: Figma `Web3` / `Home` 섹션 `74343:3254` — TO-BE 프레임 `74343:3255`(ko) · `74343:3664`(ja)
- **담당 FE 팀**: **LV** (사용자 확정) → 프리픽스 `mini_` · 치환자 `{0}` (이번 문구에 변수 없음)
- **키스페이스**: Dapp Portal / WEB BROWSER (기존 홈 배너 키가 등록된 서비스)
- **산출물**: `xlt/xlt_output_20260915112118.xlsx` (신규 키 2건)
- **검증 범위**: **전체 행 전수 점검** — 이 엑셀의 **전체 파일 2키 × 5개 언어 = 10셀 전건**을 수동 검토했다(신규 키 한정이 아니라 파일 전수).

---

## 대상 키 (신규 2건)

| # | XLT Key | ko_KR | ja_JP | en_US | th_TH | zh_TW |
|---|---|---|---|---|---|---|
| 1 | `mini_home_voucher_banner_title` | 한국 인기 바우처\n지금 1,000엔 할인! | 韓国人気バウチャー\n今なら1,000円OFF！ | Popular Korean vouchers\n1,000 JPY OFF now! | เวาเชอร์เกาหลียอดนิยม\nตอนนี้ลด 1,000 เยน! | 韓國人氣優惠券\n現在折 1,000 日圓！ |
| 2 | `mini_home_voucher_banner_btn` | 바로 구매하기 | 今すぐ購入 | Buy now | ซื้อทันที | 立即購買 |

### 키 전략 판정 근거 (사용자 확정)

기존 배너 키 `UF_jpyc_buy_guide_banner1_desc1/desc2` · `banner2_title/btn`은 위키
[JPYC 구매가이드 진입배너](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4478866687)(v10)에서
**MINI Home Tab · MINI Reward Tab · Web&Liff KPick Tab 3곳 공통**으로 정의돼 있다.
이번 요청은 Home만 조정이므로 **기존 키 값 교체 시 나머지 두 탭 배너까지 바뀐다** → **신규 키 생성**으로 확정(사용자 선택).
기존 4개 키는 **값·키 모두 변경하지 않으며**, Home 화면에서만 미노출된다.

- 프리픽스 선례 실측(Dapp Portal 1,692키): `mini_` **237건** · `UF_` 256건 → LV 규칙에 따라 `mini_`
- 타이틀 문구 유사 등록값: **0건**(`--similar` 임계 0.5) → 신규 대상 확정

---

## (1a) 한국어 원문 교정 — 번역 전 수행

| Figma 원문 | XLT Key 값 | 사유 |
|---|---|---|
| `한국 인기 바우처⁠지금 ¥1,000엔 할인!` | `한국 인기 바우처\n지금 1,000엔 할인!` | **`¥1,000엔`은 통화 중복 표기**(¥ 기호 + 「엔」). 등록값 실측 ko `¥` **1건**(통화 설정 라벨) vs 「N만엔/N엔」 형태 8건 → 「1,000엔」이 선례 정합. 일본어 원문도 `1,000円OFF`로 円만 쓴다 |
| `U+2028`(LINE SEPARATOR) | `\n` | XLT 개행 규격(`md/guide.md` §182) |

**alias 기록**: `{Figma: 한국 인기 바우처 지금 ¥1,000엔 할인! → XLT: 한국 인기 바우처\n지금 1,000엔 할인!}`

맞춤법·띄어쓰기·표기 일관성 그 외 지적 사항 없음(`바로 구매하기` 정상).

---

## (a) 자동 검증 요약 — `validate_translation.py`

```
python3 scripts/validate_translation.py xlt/xlt_output_20260915112118.xlsx scripts/glossary.json
```

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | 3건 |
| 🟢 P2 | 0건 |

**통과 기준 P0 = 0 충족** (자동·수동 모두).

---

## (b) 수동 3단계 체크표 — 전체 행 전수 (2키 × 5언어)

### 1단계 — 한국어(ko_KR) 원문 검토

| 키 | 검토 | 판정 |
|---|---|---|
| `..._title` | 맞춤법·띄어쓰기·조사 정상. 통화 중복 표기 1건 발견 → (1a)에서 교정 | ✅ 교정 후 정상 |
| `..._btn` | 「바로 구매하기」 — 용어집 「구매하기」 정본 일치 | ✅ 정상 |

### 2단계 — 용어집 대조

| 용어 | 정본 | 적용 | 판정 |
|---|---|---|---|
| 바우처 | ko 바우처 / en Voucher / th เวาเชอร์ / zh 優惠券 / **ja クーポン** | ko·en·th·zh 일치 / **ja `バウチャー`(Figma 기재)** | ⚠️ ja만 불일치 → (c) 1·2번 |
| 인기 | ko 인기 / en popular / ja 人気 / th ยอดนิยม / zh 人氣 | 전 언어 일치 | ✅ |
| 구매하기 | ko 구매하기 / en Buy now / **ja 購入する** / th ซื้อ / zh 購買 | ko·en·zh 일치 / th `ซื้อทันที`(즉시성 반영, 기존 동일 문구 키 2건과 동일) / **ja `今すぐ購入`(Figma 기재)** | ⚠️ ja만 불일치 → (c) 3번 |

### 3단계 — 다국어 의미·표현 검토 (언어별 전건)

| 언어 | `..._title` | `..._btn` | 판정 |
|---|---|---|---|
| ja_JP | `韓国人気バウチャー\n今なら1,000円OFF！` — 의미 정확, 전각 `！` 관례 정상, 금액 `1,000円` 붙임(ja 관례·선례 `5万円`·`3万円` 일치) | `今すぐ購入` — 의미 정확 | 표기 정책 외 어색함 없음 |
| en_US | `Popular Korean vouchers\n1,000 JPY OFF now!` — 금액 표기 **실측 교정 반영**(아래 참조) | `Buy now` — 기존 동일 문구 키 2건과 완전 일치 | ✅ |
| th_TH | `เวาเชอร์เกาหลียอดนิยม\nตอนนี้ลด 1,000 เยน!` — 「한국 인기 바우처 / 지금 1,000엔 할인」 의미 일치, 숫자+`เยน` 선례(`50,000 เยน`·`30,000 เยน`) 일치 | `ซื้อทันที` — 기존 2건과 완전 일치 | ✅ |
| zh_TW | `韓國人氣優惠券\n現在折 1,000 日圓！` — 번체 정자 정상, 숫자↔한자 경계 **공백 1칸**(2026-09-14 전역 결정, 신규 키부터 적용), 선례 `5 萬日圓`·`3 萬日圓` 일치 | `立即購買` — 기존 2건과 완전 일치 | ✅ |
| ko_KR | (1단계 참조) | (1단계 참조) | ✅ |

**en 금액 표기 교정(수동 검토에서 발견 — 자동 검증기 미검출)**: 초안 `¥1,000 OFF now!` → `1,000 JPY OFF now!`
근거는 등록값 3,896키(Unifi 2,557 + Dapp Portal 1,692, 중복 제거) 실측 —
en 금액 문맥 `JPY` **5건**(`50,000 JPY`×3 · `30,000 JPY`×2) vs `¥` **0건**(¥ 2건은 통화 설정 라벨·입력 필드로 금액 문맥 아님).
`OFF` 대문자는 `mini_guidekim_discount`(`{0}% OFF`, 5개 언어 동일) 선례 있음.

**언어 혼입·빈칸·placeholder**: 전 10셀 이상 없음(변수 없는 문구라 placeholder 검사 대상 없음).

---

## (c) 자동 검증 P1 3건 — 각 건 처리 판정

| # | 키 | 검출 | 판정 | 처리 |
|---|---|---|---|---|
| 1 | `..._title` | [용어 불일치] ja `바우처` → `クーポン` 권장 | **실제 위반** (오탐 아님) | **정책 보류 → 사용자 확정**: Figma 기재 `バウチャー` 유지 |
| 2 | `..._title` | [구 표기] ja `バウチャー` → `クーポン` 권장 (deprecated #22) | **실제 위반** — 1번과 동일 사안의 중복 검출 | 동상 |
| 3 | `..._btn` | [용어 불일치] ja `구매하기` → `購入する` 권장 | **실제 위반** (오탐 아님) | **정책 보류 → 사용자 확정**: Figma 기재 `今すぐ購入` 유지 |

**실측 근거(사용자 결정 전 제시)**: 등록값 3,896키 기준
ja `バウチャー` **0건** / `クーポン` **31건** (v5.4 결정에 따른 20키 재업로드 완료 상태) ·
ja 완전일치 `今すぐ購入` **0건** / `今すぐ購入する` **2건**(`UF_voucher_btn_buy_now` · `mini_voucher_detail_buy_now_btn`).

→ 두 건 모두 **권장값과 어긋남을 실측과 함께 제시하고 사용자가 「Figma 기재 유지」로 확정**했다.
사용자 지시(「피그마에 일어까지 기재해두었으니 일어와 한국어는 해당 내용 사용」)에 따른 결정이며,
**P0가 아니므로 게이트 통과를 막지 않는다.**

⚠️ **결과로 남는 상태**: 이 등록 이후 시스템에서 ja `バウチャー`는 이 1키만 남고(나머지 31건 `クーポン`),
`今すぐ購入`(する 없음)도 이 1키만 남는다. 향후 표기 통일 시 **이 2키가 대상**이다.

---

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

### (d-1) 용어집 보완 권장 — 해당 없음

용어집(v5.5)에 「바우처」·「구매하기」·「인기」가 모두 등재돼 있고 등재값이 실측 다수와 일치한다
(ja `クーポン` 31건 · `購入する` 2건). 검증기 P1 3건은 **용어집 매핑이 좁아서가 아니라 실제 불일치**이므로
`md/glossary-backlog.md` 등재 대상이 아니다. **용어집 변경 제안 없음.**

### (d-2) 추가 개선·제안 안내

1. **Figma 일본어 기재가 시스템 정본과 어긋난 2건** — 위 (c)에서 사용자가 Figma 기재 유지로 확정했다.
   표기 통일을 원할 경우의 대상 키는 `mini_home_voucher_banner_title`(ja) · `mini_home_voucher_banner_btn`(ja)이며,
   이 리포트가 구값·판단 근거의 기록이다.
2. **en 금액 표기 규칙 문서화 제안** — 금액 문맥 en은 `N JPY`(5건) 형태가 선례이고 `¥` 기호는 통화 설정 라벨 전용(2건)인데,
   `md/guide.md`에 명시 규칙이 없어 매번 실측으로 판정하고 있다. §5-1에 통화 표기 규칙 1줄 추가를 권장한다(사용자 결정 사안).
3. **배지 이미지 `行動商品券 ¥1,000`** — daiso·올리브영·CU 로고가 포함된 **이미지 자산**이고 `xlt` 코멘트도 없어
   번역 대상에서 제외했다. 다국어 전개 시 이미지 자체를 언어별로 교체해야 하는지는 디자인 측 확인 사항이다.

---

## 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 | **0건** ✅ |
| 수동 P0 | **0건** ✅ |
| P1 각 건 처리 판정 | 3건 전건 판정·사용자 확정 완료 ✅ |
| (d) 권장 사항 임의 적용 | 없음 ✅ |

→ **게이트 통과.** XLT 엑셀 생성 · 위키 반영으로 진행한다.

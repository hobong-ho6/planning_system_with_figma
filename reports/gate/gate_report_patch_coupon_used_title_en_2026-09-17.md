# 게이트 리포트 — `쿠폰 기사용` 타이틀 **en 축약** (2키)

- **작업 유형**: 키 단위 번역 패치 모드 (B) lang-specific — **en_US만** 교체(ko·ja·th·zh 보존)
- **대상 키 2건**: `UF_100yen_coupon_used_title`(배너) · `UF_voucher_pay_more_title_coupon_used`(바텀시트 분기)
- **변경**: `You have already used this coupon`(33자) → **`Coupon already used`(19자, −14)**
- **대조 기준(라이브)**: XLT `Unifi / WEB BROWSER / v1.7.9` **2,609키** · 2026-09-17 재조회(캐시 미사용)
- **담당 FE 팀**: **UIT**(`UF_` · 변수 없음)
- **산출물 엑셀**: `xlt/xlt_output_20260917151401.xlsx` (properties 2행 + plurals)
- **선행 작업**: 같은 날 등록한 신규 키(`gate_report_100yen_coupon_used_2026-09-17.md`)의 **문구 길이 조정**

---

## 0. 변경 근거 · 사용처 확인

**사용처 전수 확인(CLAUDE.md 2-1-b)** — 이 문구를 쓰는 키는 **정확히 2건**이며 둘 다 같은 「쿠폰 기사용」 케이스(배너 / 바텀시트 타이틀)다. 등록값 전체에서 같은 en 문자열을 쓰는 다른 키는 **없다**. 따라서 **2키를 함께** 바꿔야 화면 간 문구가 갈리지 않는다(사용자 확정).

**길이 근거** — en `_title` 키 **478건**의 길이 중앙값 **25자** · 평균 29자. 현행 33자는 중앙값을 넘었고, 축약안 19자는 중앙값 이내다.

**어조 근거(실측 선례)** — 상태 라벨은 간결형이 관례다.

| 선례 | ko | en |
|---|---|---|
| `UF_status_used` | 사용 완료 | **`Used`** |
| `UF_status_partially_used` | 일부 사용 | **`Partially used`** |

`Coupon already used`는 이 어조와 일치한다.

---

## 1. 한국어 원문 교정 (1a)

- **교정 없음 — 해당 없음.** 이번 변경은 **en_US 단일 언어**이고 `ko_KR`(`이미 쿠폰을 사용했어요`)은 변경 대상이 아니다. 라이브 ko 원문을 재점검했고 맞춤법·띄어쓰기·조사(`쿠폰을`) 모두 정상이다.
- **alias 없음** — Figma 원문과 ko 등록 문구가 그대로라 매핑 변동이 없다.

---

## 2. 자동 검증 — `validate_translation.py` (a)

대상: `xlt/xlt_output_20260917151401.xlsx` (**전체 파일 = 2키 × 5개 언어 = 10셀, 전수**)

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

**→ P0 = 0 (통과)** · 리포트: `xlt/xlt_output_20260917151401_validation_report.md`

---

## 3. 수동 3단계 체크표 (b) — 전수(2키 · 10셀 전부)

### 1단계 — 한국어 원문
| 키 | ko_KR | 판정 |
|---|---|---|
| `UF_100yen_coupon_used_title` | 이미 쿠폰을 사용했어요 | ✅ 보존(미변경) |
| `UF_voucher_pay_more_title_coupon_used` | 이미 쿠폰을 사용했어요 | ✅ 보존(미변경) |

### 2단계 — 용어집 대조
| 점검 | 결과 |
|---|---|
| `쿠폰` 표기 | ✅ en `coupon` — 등록 실사용 4/4키와 일치(대기 #19, 미등재) |
| 용어집 118용어 위반 | ✅ 없음 |
| 고유명사 | ✅ 해당 없음 |

### 3단계 — 다국어 (10셀)
| 언어 | 값 | 판정 |
|---|---|---|
| ko_KR | 이미 쿠폰을 사용했어요 | ✅ 보존 |
| **en_US** | **Coupon already used** | ✅ **변경** |
| ja_JP | クーポンはご利用済みです | ✅ 보존 |
| th_TH | คุณใช้คูปองไปแล้ว | ✅ 보존 |
| zh_TW | 您已使用過優惠券 | ✅ 보존 |

**변경분 중점 점검**

| 항목 | 결과 |
|---|---|
| 의미 보존 | ✅ 「이미 사용됨」의 핵심(대상=쿠폰, 상태=이미 사용)이 유지된다. 주어를 `You`에서 대상(`Coupon`)으로 옮긴 라벨형 압축이다 |
| 문법 | ✅ `Coupon already used` — 표제(headline) 생략형. 관사 생략은 UI 라벨 관례이며 선례 `Partially used`와 같은 구조 |
| 대소문자 | ✅ 문장식(sentence case) — 등록 카테고리·상태 라벨 관례와 일치 |
| 축약형(`You've`) | ✅ 미사용 — 등록값 비축약형 36 : 축약 9로 비축약이 다수 |
| 두 키 동일성 | ✅ 2키의 5개 언어가 **바이트 동일**(의도된 분기 쌍 유지) |
| 미변경 4개 언어 | ✅ changelog **2건(en_US만)** — ko·ja·th·zh는 라이브값과 바이트 동일 |
| 컬럼 정렬 가드(1g) | ✅ 통과 — 이질 문자체계 없음 |

---

## 4. 검증기 보고 각 건 처리 판정 (c)

| 심각도 | 건수 | 처리 판정 |
|---|---|---|
| 🔴 P0 | **0건** | 통과 — 실제 위반 없음 |
| 🟡 P1 | **0건** | 오탐 포함 보고 없음 |
| 🟢 P2 | **0건** | — |

보고 0건이라 **오탐·정책 보류 판정 대상이 없다.**

---

## 5. 추가 개선·제안 (d) — 권장, 임의 반영하지 않음

### (d-1) 용어집 보완 — 신규 등재 없음
`쿠폰`은 대기 **#19**로 이미 등재돼 있고 2026-09-17 재실측(4건 전건 일치)도 반영돼 있다. 이번 변경으로 달라지는 값이 없어 **추가 등재·갱신 없음**.

### (d-2) 사용자 결정 권장 사항

| # | 항목 | 내용 | 권장 |
|---|---|---|---|
| 1 | **다른 언어 길이** | en만 줄였다. 현재 길이는 ja 12자 · th 17자 · zh 8자 · ko 11자로 **en(19자)이 여전히 가장 길다** | 배너에서 여전히 넘치면 ja·th도 함께 검토. th `คุณใช้คูปองไปแล้ว`는 `ใช้คูปองแล้ว`로 축약 가능 |
| 2 | **ko 문체 혼재(기존 이월)** | 타이틀 `~했어요` ↔ 설명 `~합니다` | 종전 리포트의 (d-2) 1과 동일 — 미결 |
| 3 | **위키 동기화** | 100엔딜 위키 다국어 표·Screen XLT의 en 값이 구값(`You have already used this coupon`)으로 남는다 | **등록 후 위키 2곳 외과적 갱신 필요**(이번 패치에 포함해 진행) |

---

## 6. 통과 판정

- 자동 **P0 = 0**(P1·P2도 0) ✅
- 수동 전수(2키 × 5개 언어 10셀) **P0 = 0** ✅
- 검증기 보고 0건 → 처리 판정 대상 없음 ✅
- (d) 권장 사항 **임의 반영 없음** ✅

**→ 게이트 통과. 업로드 엑셀 전달 가능.**

⛔ **업로드는 사용자가 직접 수행한다.** 업로드 후 라이브 재조회 대조 → 위키 en 값 갱신 → Slack 스레드 회신 순으로 처리한다.

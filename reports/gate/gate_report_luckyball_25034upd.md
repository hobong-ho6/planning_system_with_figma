# 번역 검증 리포트 — 초대자 화면 전체(25034) 화면·문구 업데이트 (LV, 코멘트 선별)

- 대상 위키: pageId=4479306980 · 팀 **LV** · 63411-25034
- 범위: 화면·문구 업데이트. 신규 5키 + 갱신 1키(my_draw_count 형식 변경) + 다수 기존 키 재사용
- 변수화(사용자 확정): `4개 뽑기 → {0}개 뽑기`. 용어 표준화: `럭볼 → 럭키볼`

## 검토 범위 (전수 점검 명시)
XLT 코멘트 선별. 신규/갱신 6키 전체 × 5개 언어 전수 수동 검토. 기존 재사용 키 불변. 제거된 XLT 문구의 키는 공유 키라 삭제하지 않고 25034 표에서만 제외.

## 한국어 원문 교정 (1a) 및 alias
- 교정: `가입후`→`가입 후`, `개설하면 하면`→`개설하면`(중복 제거), `LINE채널`→`LINE 채널`, `있어야해요`→`있어야 해요`, `럭볼`→`럭키볼`(용어집 표준).
- 변수화: `4개 뽑기`→`{0}개 뽑기`(my_draw_count 갱신; 이전 `{0} / {1}개 뽑기`에서 단일 숫자로 변경). alias: Figma 원문→키 기록.

## Executive Summary (자동 검증 — validate_translation.py, 74항목)
| 심각도 | 건수 | 판정 |
|--------|------|------|
| 🔴 P0 | 0건 | 없음 |
| 🟡 P1 | 24건 | 전건 오탐(직전 run 동일). 신규/갱신 키 P1 0건 |
| 🟢 P2 | 26건 | 전건 오탐 |

## 수동 3단계 체크표 (신규/갱신 6 × 5언어)
### 1단계: 한국어 — 교정 반영, 오타·띄어쓰기 이상 없음(가입 후·개설하면·LINE 채널·있어야 해요·럭키볼).
### 2단계: 용어집 — 럭키볼=Lucky Ball/ラッキーボール/幸運球/ลูกบอลนำโชค 일관. zh 번체 정자(頻道·錢包·抽取·顆), 간체 0건.
### 3단계: 다른 언어 — 빈칸 0, 언어혼입 0, my_draw_count `{0}` 5개 언어 정합, 컬럼 swap 없음.

## 검증기 보고 각 건 처리 판정
- 신규/갱신 6건: 실제 위반 0건. 전체 P1/P2 전건 오탐(유지).

## 통과 판정
- **P0 = 0건 (자동 + 수동)** → 위키 반영·엑셀 재생성 진행.

## 추가 개선·제안 (권장 — 사용자 결정)
- **(d-2)** `1개`·`2개`(리워드 규칙 설명)는 고정 유지(당첨 규칙 서술). Figma 원문 `생성 포`(→포함)·`정보가변경`(→정보가 변경) 등 기존 오타는 디자이너 원본 정리 권장.

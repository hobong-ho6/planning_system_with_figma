# 게이트 리포트 — 시즌3 `(Promotion) Promotion page` 프레임 업데이트 (신규 5키)

- **작업일**: 2026-08-06
- **대상 위키**: `4541588845` JPYC 럭키볼 시즌 3 (라이브 v59)
- **Figma**: 파일 `GOCHAYBS7hIrmWRGNuJOKV` · 프레임 `(Promotion) Promotion page` **66022-114734** (Mode B — 기존 행 갱신)
- **참조 위키**: `4479306980` 럭키볼 친구에게 선물하기 캠페인 (v149, 전역 번역표 75키) — **같은 문구 = 같은 키 재사용**(사용자 지시)
- **담당 FE 팀**: **LV** — 위키 Screen에 `<h3>LV</h3> > <h4>Promotion Page</h4>` 구분이 있어 그 규칙을 적용(묻지 않음). 치환자 `{0}`
- **엑셀**: `xlt/xlt_output_season3_promotion_page_20260806.xlsx` (신규 5키)
- **검증기 커밋**: `4bd7fa4`(최장일치 개선 반영본) · **용어집 라이브 v4.1(114)** — v4.2는 CMS 반영 대기

---

## 1. 코멘트 선별 결과 (29건 → XLT 마커 11건)

프레임 코멘트 29건 중 **키가 이미 명시된 것 14건**(`xlt = 키명`)과 **정책 4건**을 제외한 **순수 `xlt` 마커 11건**이 키 부여 대상이다.

### 1-1. 캠페인·시즌3 위키와 문구 대조 (사용자 지시 — 같은 문구는 같은 키)

| No | Figma 문구(요약) | 매칭 | 처리 |
|---|---|---|---|
| 8 | 이후 연계해 참여할 수 있는… | **완전일치 1.000** (시즌3) | `unifi_promotion_jpyc_info_oa_desc` **재사용** |
| 12 | 친구에게 JPYC 선물하기 | **완전일치 1.000** (캠페인) | `mini_luckyball_invite_banner` **재사용** |
| 13 | 럭키볼 1개에 최대 50,000**JPYC** 당첨! | 0.980 — 공백만 차이 | `mini_luckyball_invite_banner_desc` **재사용**(캠페인 문구 유지) |
| 21 | 기존에 Unifi 가입(월렛 개설)과 OA 팔로우 완료 유저 참여 불가 | 후보 없음 | **신규** `mini_luckyball_terms_existing_user` |
| 22 | 지급된 럭키볼은 **이벤트 종료 시까지** 미사용 시 자동 소멸 | 0.867 — 캠페인은 `{0}` 변수 | **신규** `mini_luckyball_terms_expiry_event` |
| 23 | 친구 추가 및 가입(지갑 생성 포함) 모두 완료해야 정상 참여 | 후보 없음(주어 상이) | **신규** `mini_luckyball_terms_valid_entry` |
| 24 | 참여 기간 팔로우 유지, 친구 해제 시 자격 취소 | 후보 없음 | **신규** `mini_luckyball_terms_keep_follow` |
| 25 | 가입 정보 확인 **불가**하거나… | 0.989 — 조사만 차이 | `mini_luckyball_terms_info_change` **재사용**(캠페인 문구 유지) |
| 26 | 지갑 주소로만 지급 + 일본 사용자 아니면 제한 | 캠페인은 **2키로 분리** | **신규 단일 키** `mini_luckyball_terms_wallet_jp` |
| 27 | …사전 통보 없이 **내역 삭제**… | 0.984 — 수식어만 차이 | `mini_luckyball_terms_abuse` **재사용**(캠페인 문구 유지) |
| 28 | 본 이벤트는 **준비된** 보상 소진 시… | 0.970 — 수식어만 차이 | `mini_luckyball_terms_early_end` **재사용**(캠페인 문구 유지) |

**사용자 결정 3건**: ⓐ 근사 4건(13·25·27·28)은 **기존 키 재사용 + 캠페인 문구 유지**(번역 작업 0건, Figma 표기는 수정 요청) ⓑ No22는 변수 유무가 실질 차이라 **신규 키** ⓒ No26은 Figma가 한 문단이고 문구도 달라 **신규 단일 키**.

→ **재사용 6키 · 신규 5키**. 번역 대상은 **신규 5키뿐**이다.

---

## 2. 한국어 원문 교정 (1a — 번역 전 필수)

**전수 점검 범위**: 신규 5키 × 5개 언어 = **25셀 전체**를 한 줄씩 검토했다. 재사용 6키는 캠페인·시즌3 위키의 기존 등록값을 그대로 쓰므로 **문구·번역 변경이 없다**(대조만 수행).

| # | Figma 원문 | 교정 후 (XLT Key 값) | 사유 |
|---|---|---|---|
| 1 | `…가입(월렛 개설)과<nbsp>OA 팔로우…` | nbsp → 일반 공백 | 비가시 문자 |
| 2 | `…자동 소멸 됩니다.` | **`…자동 소멸됩니다.`** | 「소멸되다」는 한 단어. 캠페인 정본(`terms_expiry`)도 붙여 쓴다 |
| 3 | `…참여 자격 취소 및 최종 보상이 지급되지 않습니다.` | **`…참여 자격이 취소되어 최종 보상이 지급되지 않습니다.`** | **비문** — 「취소 및 …지급되지 않습니다」가 호응하지 않는다 |
| 4 | `…최종 검증<nbsp>결과에<nbsp>따라보상<nbsp>지급이<nbsp>제한될<nbsp>수<nbsp>있습니다.` | nbsp 5개 제거 · **`따라보상` → `따라 보상`** | 비가시 문자 + 띄어쓰기 누락 |

**alias 기록**: 위 4건 모두 `{Figma 원문 → XLT Key 값}`이 다르다 → **디자이너 원문 수정 요청 대상**(5-2).
재사용 키 쪽 Figma 표기 차이(No 13·25·27·28)도 같은 목록에 포함한다.

---

## 3. 자동 검증

```
python3 scripts/validate_translation.py xlt/xlt_output_20260806142036.xlsx scripts/glossary.json
```

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | 6건 |
| 🟢 P2 | 0건 |

**P1/P2 각 건의 처리 판정**

| # | 키 | 지적 | 처리 판정 |
|---|---|---|---|
| 1 | `terms_existing_user` | en `가입` → `Sign up` | **오탐** — `Unifi sign-up`은 명사형. 등재값은 동사구 `Sign up` |
| 2 | `terms_expiry_event` | en `종료` → `Ended` | **오탐** — `before the event ends`의 동사 활용형 |
| 3 | `terms_valid_entry` | en `가입` → `Sign up` | **오탐** — `signing up for Unifi` 동명사. 캠페인 `terms_all_steps`와 동일 어형 |
| 4 | `terms_keep_follow` | ja `취소` → `キャンセル` | **오탐** — 참가 자격에는 `取り消し`가 맞다. `キャンセル`는 예약 취소 문맥 |
| 5 | `terms_keep_follow` | en `공식 계정` → `Official account` | **오탐** — `Unifi's official LINE account`로 사이에 `LINE`이 들어가 부분문자열 매칭이 끊긴 것. **캠페인 `terms_all_steps`도 같은 표기** |
| 6 | `terms_wallet_jp` | en `가입` → `Sign up` | **오탐** — `signed up for Unifi` 과거형 |

**실제 위반 0건.**

**⚠ 자동 검증이 잡아준 실제 개선 1건 (수정 반영)**: 초안 `terms_valid_entry` th가 `ครบทั้งหมดแล้ว`였는데 th `완료` P1로 걸렸다. 캠페인 `terms_all_steps`가 쓰는 **`เสร็จสมบูรณ์`** 로 맞춰 `เสร็จสมบูรณ์ทั้งหมดแล้ว`로 고쳤다(P1 7 → 6). **오탐만 있는 게 아니라 일관성 개선 신호이기도 하다는 사례.**

---

## 4. 수동 3단계 체크표 (전수 — 신규 5키 25셀 + 재사용 6키 대조)

### 1단계 — 한국어(ko_KR)

| 키 | ko_KR | 판정 |
|---|---|---|
| `terms_existing_user` | 기존에 Unifi 가입(월렛 개설)과 OA 팔로우를 완료한 유저의 경우 해당 이벤트 참여가 불가합니다. | 교정 반영(nbsp). **`월렛`·`OA 팔로우` 표기는 5-2 안내 대상** |
| `terms_expiry_event` | 지급된 럭키볼은 이벤트 종료 시까지 사용하지 않을 경우 자동 소멸됩니다. | 교정 반영(붙여쓰기) |
| `terms_valid_entry` | 본 이벤트는 Unifi 공식 LINE 계정 친구 추가 및 Unifi 가입(지갑 생성 포함)을 모두 완료해야 정상 참여로 인정됩니다. | 정상. **어순 `Unifi 공식 LINE 계정`은 5-2 안내** |
| `terms_keep_follow` | 캠페인 참여 기간 동안 … 참여 자격이 취소되어 최종 보상이 지급되지 않습니다. | **비문 교정 반영** |
| `terms_wallet_jp` | 럭키볼 당첨 보상은 … 최종 검증 결과에 따라 보상 지급이 제한될 수 있습니다. | 교정 반영(nbsp 5·띄어쓰기) |

- 받침·조사 판정: `자격이 취소되어`·`결과에 따라` 정상. 외래어 음차 오염 0건. 붙은 띄어쓰기 누락 0건(교정 후).

### 2단계 — 용어집 대조 (라이브 v4.1, 114 terms)

| 용어 | 등재값 | 이번 사용 | 판정 |
|---|---|---|---|
| `공식 계정` | ja `公式アカウント`·en `Official account`·th `บัญชีทางการ`·zh `官方帳號` | 전 키 일치(en은 `official LINE account` 형태) | **일치** |
| `가입` | ja `登録`·en `Sign up`·th `สมัคร`·zh `註冊` | 전 키 일치(en은 활용형) | **일치** |
| `럭키볼` | ja `ラッキーボール`·th `ลูกบอลนำโชค`·zh `幸運球` | `expiry_event`·`wallet_jp` 일치 | **일치** |
| `보상` / `지갑` | — | 캠페인 기존 번역과 동일 어휘 | **일치** |

### 3단계 — 다국어 의미 검증 (ko_KR 기준)

| 키 | ja | en | th | zh | 판정 |
|---|---|---|---|---|---|
| `terms_existing_user` | すでに…参加できません。 | …cannot participate in this event. | …จะไม่สามารถเข้าร่วมกิจกรรมนี้ได้ | …無法參加本活動。 | **정확** — 「이미 완료한 유저는 참여 불가」 4개 언어 동일 |
| `terms_expiry_event` | イベント終了までに…自動的に消滅します。 | …expire automatically if not used before the event ends. | …หากไม่ใช้ก่อนสิ้นสุดกิจกรรม | …若未於活動結束前使用，將自動失效。 | **정확** — 캠페인 `terms_expiry`에서 `{0}`만 「이벤트 종료」로 치환한 형태 |
| `terms_valid_entry` | …すべて完了してはじめて、正常な参加として認められます。 | …valid only when you have completed both… | …ก็ต่อเมื่อ…เสร็จสมบูรณ์ทั้งหมดแล้ว | …才會認定為正常參加。 | **정확** — 「모두 완료해야만」의 조건 강조가 4개 언어에 살아 있음 |
| `terms_keep_follow` | …友だち解除（ブロック・削除）した場合は参加資格が取り消され、最終報酬は支給されません。 | If you unfriend (block or delete) it, your entry is cancelled and no final reward will be paid. | …สิทธิ์การเข้าร่วมจะถูกยกเลิกและจะไม่ได้รับรางวัลสุดท้าย | …將取消參加資格且不會發放最終獎勵。 | **정확** — ko 비문을 교정한 인과 구조(취소 → 미지급)가 4개 언어에 반영 |
| `terms_wallet_jp` | …また、日本のユーザーでない場合は、当選しても最終的な検証結果により…制限されることがあります。 | …In addition, users who are not in Japan may have their reward payment restricted depending on the final verification result, even if they win. | …นอกจากนี้ หากไม่ใช่ผู้ใช้ในญี่ปุ่น… | …此外，若非日本用戶，即使中獎，也可能依最終驗證結果限制獎勵發放。 | **정확** — 캠페인이 2키로 나눈 내용을 한 문장으로 이었고, 후반부가 「지급 안 됨」(캠페인)이 아니라 **「제한될 수 있음」**(Figma)이라는 **정도 차이를 4개 언어 모두 반영** |

- **치환자 정합**: 신규 5키 전부 치환자 **없음**(5개 언어 동일). 재사용 키 중 `terms_expiry`(캠페인)는 `{0}`을 쓰지만 이번엔 신규 `terms_expiry_event`로 갈라 **변수 없는 문구**로 확정.
- **이질 문자체계 혼입** 0건 · 이중 중괄호·깨진 치환자 0건.
- **재사용 6키 대조**: 캠페인·시즌3 등록값을 그대로 인용했고 **문구·번역을 변경하지 않았다**(전수 확인).

---

## 5. 추가 개선·제안 (권장 — 사용자 결정 전까지 임의 반영 금지)

### (d-1) 용어집 보완 권장

**이번 차수 신규 권장 없음.** P1 6건은 모두 **품사·굴절·어순** 때문이지 등재 누락이 아니다. 특히 5번(`official LINE account`)은 **등재 용어 사이에 다른 단어가 끼면 매칭이 끊기는** 유형으로, 용어집이 아니라 검증기 개선 대상이다(최장일치에 이은 후속 과제).

- 단, **용어집 v4.2(`지급 완료`)가 CMS 반영 대기** 중이다. 반영되면 다른 파일의 P1이 4건 줄어든다(별건).

### (d-2) 추가 개선·제안 안내

1. **Figma 원문 수정 요청 (디자이너)** — 이번 프레임에서만 **8건**
   - nbsp: No21 1개 · No26 5개 · No28 다수
   - `자동 소멸 됩니다` → `자동 소멸됩니다` (No22)
   - `따라보상` → `따라 보상` (No26)
   - **비문**: `참여 자격 취소 및 최종 보상이 지급되지 않습니다` → `참여 자격이 취소되어…` (No24)
   - `50,000JPYC` → `50,000 JPYC` (No13 — 용어집 `{0} JPYC` 규칙: 숫자와 통화 한 칸)
   - 재사용 키와 어긋난 표기 3건(No25 `불가하거나`/No27 `내역 삭제`/No28 `준비된 보상`) — **캠페인 문구가 정본**이므로 Figma를 맞추는 편이 혼선이 적다
2. **`팔로우` 표기 (HANDOFF P1 진행 중)** — 신규 키 `terms_existing_user`·`terms_keep_follow`의 ko에 `OA 팔로우`·`팔로우`가 남아 있다. 정본은 **`공식 계정 친구 추가`**다. **4개 언어는 이미 정본 표기(`友だち追加`·`added as a friend`·`เป็นเพื่อน`·`好友`)로 번역했고, ko만 Figma 원문을 유지**했다. `UF_home_skyflag_title` 때와 같은 처리이며, ko 교정은 사용자 결정 사항이다.
3. **`월렛` vs `지갑` 혼용** — 같은 화면 안에서 No21은 `월렛 개설`, No23은 `지갑 생성`이다. 4개 언어는 모두 `ウォレット`/`wallet`/`กระเป๋า`/`錢包`로 통일했으나 **ko만 갈린다**. 하나로 통일 권장.
4. **`Unifi 공식 LINE 계정` vs `Unifi의 LINE 공식 계정`** — 어순이 문서 내에서 갈린다(No23 vs No8·캠페인). 통일 권장.

---

## 6. 통과 판정

**P0 = 0건 (자동 + 수동 모두)** → 위키 반영·엑셀 첨부로 진행. P1 6건 전건 **오탐** 판정, P2 0건.
자동 검증이 신호한 th 일관성 1건은 **실제 개선으로 반영**했다.

---

# 2차 — 「팔로우」 정본 표기 위반 정정 + 용어집 v4.3 (사용자 지적)

- **작업일**: 2026-08-06 (같은 날 2차)
- **계기**: 사용자가 `mini_luckyball_terms_existing_user`의 ko에 남은 **`OA 팔로우`** 를 지적

## 7-1. 판단 착오 정정

1차에서 신규 키 2건의 ko에 `OA 팔로우`·`공식 계정 팔로우`를 **Figma 원문 그대로 두고** (d-2) 안내로 미뤘다. **이는 잘못된 판단이었다.**

| | 규칙 |
|---|---|
| 기존 키(XLT 시스템 등록값) | 임의 변경 금지 — 보고만 (`UF_home_skyflag_title` 선례) |
| **신규 키** | **정본 표기를 바로 적용**해야 한다 |

2026-08-04에 「채널 팔로우·팔로우 → 공식 계정 친구 추가」가 5개 언어 정본으로 확정돼 있었고, 이 2건은 **이번에 새로 만든 키**다. 구 표기로 등록하면 「팔로우 정리」 잔존 목록만 늘린다.

**수정 2건** (4개 언어는 이미 정본 표기였으므로 **ko만 교정 → 5개 언어 일치**)

| 키 | 수정 전 | 수정 후 |
|---|---|---|
| `terms_existing_user` | `…과 **OA 팔로우**를 완료한` | `…과 **LINE 공식 계정 친구 추가**를 완료한` |
| `terms_keep_follow` | `…공식 계정 **팔로우**를 계속 유지` | `…공식 계정 **친구**를 계속 유지` |

어형은 캠페인 용례를 따랐다 — 행위는 `친구 추가`, 유지는 `친구를 유지`(`keep_channel`·`need_follow_to_draw`).

## 7-2. 재검증

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | 7건 (기존 6 + `existing_user` en `공식 계정` 1) |
| 🟢 P2 | 0건 |

추가된 1건은 **오탐** — ko에 「공식 계정」이 들어오면서 en에서 등재값 `Official account`를 찾는데, 실제 값은 `the official LINE account`로 사이에 `LINE`이 끼어 부분문자열 매칭이 끊긴다. 캠페인 `terms_all_steps`도 같은 표기다.

- ko에 「팔로우」 잔존: **0건** · 위키 본문 잔존: **0건**(History 이력은 보존)

## 7-3. 용어집 v4.3 — `deprecated_terms` 영역 신설 (사용자 지시)

「용어집을 강화하라」는 지시에 따라 설계했다. **terminology 등재안은 실측으로 기각**했다.

| 안 | P1 |
|---|---|
| 현행 v4.2 | 230 |
| `공식 계정 친구 추가`를 terminology에 등재 | **266 (+36)** |

`terminology`는 `ko → 5개 언어` **정합**을 보는 자리라 ko 자체의 구 표기를 못 잡고, 긴 구를 넣으면 어순 차이로 오탐만 는다.

**채택안**: 용어집에 **`deprecated_terms` 영역 신설**(9건) + `validate_translation.py`가 각 언어 셀을 검사해 **P1 「구 표기」**로 경고(최장일치 — `OA 팔로우`가 잡히면 `팔로우`는 중복 보고 안 함).

| lang | 금지 | 권장 |
|---|---|---|
| ko_KR | `OA 팔로우`·`OA 채널 추가`·`채널 팔로우`·`공식 계정 팔로우`·`팔로우` | **Unifi LINE 공식 계정 친구 추가** |
| ja_JP / zh_TW / th_TH | `フォロー` / `關注`·`追蹤` / `ติดตาม` | `友だち追加` / `加入好友` / `เพิ่มเพื่อน` |

**실측**: 최신 엑셀 8종 P1 **230 → 230(회귀 0건)** — 현행 산출물은 이미 정본이라 **예방 장치**로 동작. 구 표기가 남은 파일에서는 **2건 정상 검출**(중복 없음). `test_validation.py` 통과.

## 7-4. 추가 개선·제안 (2차)

### (d-1) 용어집 보완 — **v4.3 산출 완료** (`reports/glossary_v4.3_for_cms.json`)
v4.2(`지급 완료`)에 이어 누적. **CMS 반영과 가이드 v16 게시는 사용자 액션**이다.

### (d-2) 추가 개선·제안 안내
1. **작업 규칙 명문화 완료** — `md/guide.md` §5-1 **C-1 신설**: 신규 키는 정본 즉시 적용 / 기존 시스템 등록값은 보고만 / **모호하면 사용자 확인 후 제안** / ko만 구 표기로 남기지 않는다.
2. **Figma 원문 수정 요청 +1** — `66022-114734`의 `OA 팔로우` → `Unifi LINE 공식 계정 친구 추가`.
3. **XLT 시스템 잔존 정리(HANDOFF P1 ⓐ단계)** 는 여전히 남아 있다 — 이번 조치는 **신규 문구 유입 차단**이지 기존 시스템 문구 정리가 아니다.

## 7-5. 통과 판정 (2차)

**P0 = 0건** → 위키 v62 → **v63** 반영 완료(ko 2곳·엑셀 3종 재첨부 ALL v16·LV v8·UIT v13). P1 7건 전건 오탐.

---

# 3차 — UIT 2키 금액 하향 (15만엔 → 5만엔)

- **작업일**: 2026-08-06 (같은 날 3차)
- **계기**: 사용자가 위키 Screen 표에서 두 키의 문구를 직접 갱신하고 반영을 요청
- **담당 FE 팀**: **UIT** (`UF_` 프리픽스) — 치환자 규칙은 이번 변경과 무관(대상 문구에 치환자 없음)
- **라이브**: 시즌3 `4541588845` v65

## 8-1. 변경 내용

| 키 | 화면 | 변경 |
|---|---|---|
| `UF_home_jpyc_banner_title` | (LIFF)Unifi - Login x | 최대 **15만엔** → **5만엔** |
| `UF_home_daily_mission_title_MINI` | (Unifi mini) - Login x | 최대 **15만엔** → **5만엔** |

Screen 표는 사용자가 이미 갱신했고, **다국어 번역표와 엑셀이 구 값(15만엔)으로 남아 있어** 이를 맞췄다.

**일관성 근거**: 시즌3 엑셀 57키 중 금액이 든 키를 전수 확인한 결과 `unifi_promotion_jpyc_title`·`unifi_promotion_jpyc_desc`·`mini_luckyball_invite_banner_desc`는 **이미 5만엔**이었고, 이 2키만 15만엔으로 남아 있었다. → **하향이 아니라 잔존 정정**에 가깝다.

## 8-2. 한국어 원문 교정 (1a)

**교정 0건.** 위키 렌더에서 `5만엔상당의`·`찬스최대`로 붙어 보였으나, 엑셀 원본을 확인하니 **개행(`\n`)** 이었다(`최대 5만엔\n상당의`). 붙여쓰기 오류가 아니므로 손대지 않았다.

> ⚠️ **위키 표 렌더만 보고 붙여쓰기 오류로 판단하면 안 된다** — `<br />`·개행이 공백 없이 이어져 보인다. 원본(엑셀·XLT 시스템)에서 확인할 것.

## 8-3. 번역 — 숫자만 치환 (의미·어순 변경 없음)

| 언어 | 치환 | 개행 보존 |
|---|---|---|
| ko_KR | `15만엔` → `5만엔` | ✅ |
| ja_JP | `15万円` → `5万円` | ✅ |
| en_US | `150,000 JPY` → `50,000 JPY` | ✅ |
| th_TH | `150,000 เยน` → `50,000 เยน` | ✅ |
| zh_TW | `15萬日圓`/`15 萬日圓` → `5萬日圓`/`5 萬日圓` | ✅ |

**2키 × 5언어 = 10셀** 치환(ALL·UIT 각각). 스크립트에 `assert changed == 10`과 **잔존 검사**(`15만`·`15万`·`150,000`·`15萬`)를 걸어 누락·과잉 치환을 차단했다.

## 8-4. 자동 검증

`python3 scripts/validate_translation.py xlt/xlt_output_season3_ALL_20260804.xlsx scripts/glossary.json`

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | 68건 (변경 전과 동일 — 이 2키 관련 신규 0건) |
| 🟢 P2 | 19건 (마침표 스타일, 변경 전과 동일) |

**이번 변경으로 새로 생긴 P1/P2는 없다.**

## 8-5. 수동 3단계 체크표 (전수 — 변경 2키 10셀 + 금액 일관성 전수)

### 1단계 — 한국어
| 키 | ko_KR | 판정 |
|---|---|---|
| `UF_home_jpyc_banner_title` | `100% 당첨! 최대 5만엔\n상당의 JPYC 획득 기회` | 정확. 개행 1개 보존 |
| `UF_home_daily_mission_title_MINI` | `매일 럭키볼 찬스\n최대 5만엔 상당 JPYC 받기` | 정확. 개행 1개 보존 |

### 2단계 — 용어집 대조
`JPYC`(예외·원어 유지) · `당첨`(ja `当選`·zh `中獎`) · `럭키볼`(ja `ラッキーボール`·th `ลูกบอลนำโชค`·zh `幸運球`) — 전부 등재값과 일치. **금액 표기는 용어집 대상이 아니다**(숫자+통화 한 칸 규칙은 `{0} JPYC` 형태에만 적용되며 `5만엔`은 해당 없음).

### 3단계 — 다국어 의미 검증
| 키 | 판정 |
|---|---|
| `UF_home_jpyc_banner_title` | **정확** — 5개 언어 모두 「최대 5만엔 상당의 JPYC」로 금액만 바뀌고 문장 구조·어순은 그대로 |
| `UF_home_daily_mission_title_MINI` | **정확** — 동일 |

- **치환자**: 두 키 모두 치환자 없음(5개 언어 동일).
- **개행 개수 대조**: 변경 전후 동일(`banner_title` 5언어 각 1개 · `daily_mission_MINI` ko·ja·en 1개, th·zh 0개).
- **금액 일관성 전수**: 시즌3 57키에 `15만`·`150,000`·`15萬`·`15万` **잔존 0건** 확인.

## 8-6. 추가 개선·제안 (권장)

### (d-1) 용어집 보완 권장
**없음.** 금액 수치는 용어집 관리 대상이 아니다.

### (d-2) 추가 개선·제안 안내
1. **`UF_home_skyflag_title`은 그대로 3만엔이다**(`무제한 미션하고\n최대 3만엔 상당 즉시 받기`). 이 키는 SkyFlag 미션이라 **별개 금액**으로 보이지만, 5만엔으로 통일해야 하는지 확인이 필요하다면 알려주시면 반영한다. (2026-08-05 「ko는 시스템 등록값이라 유보」 결정이 걸려 있는 키이기도 하다.)
2. **위키 렌더 ≠ 원본** — 8-2의 교훈. 표에서 붙어 보이는 문구는 개행일 수 있으므로 엑셀·시스템 export로 확인한다.

# 4차 — `월렛` → `지갑` 정본 적용 + 용어집 v4.5

- **작업일**: 2026-08-06 (같은 날 4차)
- **계기**: 사용자가 4개 키를 지목하며 「월렛은 지갑을 쓰도록 용어집을 업데이트하고, **안 걸렸다면 왜인지 확인해 보완**」 요청

## 9-1. 지목된 4개 키 실측

| 키 | ko에 `월렛` |
|---|---|
| `mini_luckyball_invite_banner_desc` | 없음 |
| `unifi_promotion_jpyc_info_oa_desc` | 없음 |
| **`mini_luckyball_terms_existing_user`** | **있음** — `Unifi 가입(월렛 개설)과…` |
| `mini_luckyball_terms_valid_entry` | 없음 (`지갑 생성` — 정본) |

**같은 문서 안에서 ko만 `월렛` vs `지갑`으로 갈려 있었다.** 4개 언어는 처음부터 정본(`ウォレット`·`wallet`·`กระเป๋า`·`錢包`)이었다.

## 9-2. 왜 안 걸렸나 (사용자 질문)

| 검사 경로 | 결과 |
|---|---|
| 용어집 `terminology`의 `지갑` | **작동 안 함** — ko에 `지갑`이 있는 행에서만 5개 언어를 검사한다. **ko가 `월렛`이면 매칭 자체가 안 돼** 검사 대상에서 빠진다 |
| 용어집 `deprecated_terms` | **항목 없음** — v4.3에서 이 영역을 만들 때 `팔로우` 계열만 넣고 `월렛`을 빠뜨렸다 |
| `md/guide.md` §5-1 C | 「비수탁 지갑: "월렛" → "지갑"」 규칙이 **문서로만** 존재 |

→ **「팔로우」와 완전히 같은 구조적 사각지대.** 같은 교훈으로 `deprecated_terms`를 만들어 놓고 `월렛`을 함께 넣지 않은 **누락**이다.

## 9-3. 보완 — 용어집 v4.5

`{lang: ko_KR, pattern: 월렛, recommend: 지갑}` 등재 (context 없음 — 「월렛」은 다른 뜻이 없다).

| 대상 | v4.4 | v4.5 |
|---|---|---|
| XLT 시스템 1,596키 | 1,650 | **1,664 (+14)** — ko `월렛` 잔존 14건 정확 검출 |
| 시즌3 ALL / LV | 68 / 57 | 69 / 58 (+1) |
| 그 외 엑셀 6종 | — | **변화 없음(오탐 0)** |

`test_validation.py` 전체 통과.

## 9-4. 한국어 원문 교정 (1a) — 사용자 표기를 따랐다

**⚠️ 사용자가 위키 Screen 표를 이미 `(지갑 생성)`으로 수정해 두었다**(인라인 코멘트 마커 포함). 제 초안은 `지갑 개설`이었으나 **사용자 표기가 정본**이고, 같은 문서 `terms_valid_entry`(`지갑 생성`)와도 일치한다.

| 언어 | 전 | 후 |
|---|---|---|
| ko_KR | `Unifi 가입(월렛 개설)과` | **`Unifi 가입(지갑 생성)과`** |
| ja_JP | `ウォレット開設` | `ウォレット作成` |
| th_TH | `เปิดกระเป๋า` | `สร้างกระเป๋า` |
| zh_TW | `開設錢包` | `建立錢包` |
| en_US | `wallet creation` | 변경 없음(이미 「생성」) |

ko가 「생성」으로 확정돼 4개 언어도 **「개설」 → 「생성」 계열로 통일**했다(같은 문서 `terms_valid_entry`와 동일 어휘).

## 9-5. 자동 검증

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | 68건 (v4.5 기준 — **`월렛` 검출 0건**) |

수정 후 `월렛` 잔존 **0건** 확인. 신규 P1 없음.

## 9-6. 수동 3단계 체크표 (전수 — 변경 1키 5셀 + 지목 4키 대조)

- **1단계 ko**: `기존에 Unifi 가입(지갑 생성)과 LINE 공식 계정 친구 추가를 완료한 유저의 경우 해당 이벤트 참여가 불가합니다.` — 정확. 조사·띄어쓰기 정상
- **2단계 용어집**: `지갑`(ja `ウォレット`·en `wallet`·th `กระเป๋า`·zh `錢包`) 5개 언어 **전부 등재값 일치**
- **3단계 의미**: 「생성」 어휘가 4개 언어 모두 반영(`作成`·`creation`·`สร้าง`·`建立`). 같은 문서 `terms_valid_entry`와 **어휘 일치**
- 지목된 나머지 3키는 `월렛` 없음 — 변경 없음(전수 대조 완료)

## 9-7. 추가 개선·제안 (권장)

### (d-1) 용어집 보완 — **v4.5 산출 완료** (`reports/glossary_v4.5_for_cms.json`)
**v4.4와 함께 CMS 반영 대기**다. v4.5에 v4.4 내용이 누적돼 있으므로 **v4.5만 붙여넣으면** 된다.

### (d-2) 추가 개선·제안 안내
1. **XLT 시스템 ko `월렛` 13건 잔존**(이번 수정분 제외) — `launching_promotion_*` 6 · `oa_promotion*` 5 · `home_pointreward_my_detail_desc` 등 **Dapp Portal 계열 구 문구**다. 이번 등재는 **신규 유입 차단**이고 기존 정리는 별건이다.
2. **`개설`/`생성` 표기 통일** — 이번에 「생성」으로 맞췄으나, XLT 시스템 전반에는 두 어휘가 섞여 있을 수 있다. 용어집 `지갑 생성` 등재를 검토할 만하다(단 A/B 실측 선행).
3. **`md/glossary-changelog.md` 표 정렬 혼재** — v2.1~v4.1은 오름차순, v4.2~v4.5는 내림차순이다(기존부터). 통일 여부는 사용자 결정.

## 9-8. 통과 판정 (4차)

**P0 = 0건** → 위키 v73 → **v74** 반영 완료(번역표 4셀 · 엑셀 ALL v18 · LV v9). **Screen 표는 사용자 수정본과 인라인 코멘트를 보존**했다.

---

## 8-7. 통과 판정 (3차)

**P0 = 0건 (자동 + 수동 모두)** → 위키 번역표 갱신·엑셀 재첨부 진행. 신규 P1/P2 0건.

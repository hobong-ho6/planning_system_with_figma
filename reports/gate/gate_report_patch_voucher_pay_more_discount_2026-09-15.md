# 게이트 리포트 — 키 단위 번역 패치 `UF_voucher_pay_more_discount` (5개 언어)

- **작업일**: 2026-09-15
- **모드**: 키 단위 번역 패치 — **(A) ko-source**(한국어 변경 → 5개 언어 전량 재번역, keep 없음)
- **대상 키**: `UF_voucher_pay_more_discount` **1키 × 5개 언어 = 5셀**
- **담당 FE 팀**: **UIT** — 프리픽스 `UF_` · 치환자 **`{{0}}`(이중 중괄호)**
- **반영 위치**: 위키 [pageId 4704515582](https://wiki.workers-hub.com/display/UNIFI/Unifi+mini+v2.0) › `결제 페이지 - UIT`(바텀시트)
- **산출물**: `xlt/xlt_patch_UF_voucher_pay_more_discount_2026-09-15.xlsx`
- **조달(전부 캐시 미사용 · 신규 조회)**: XLT 등록값 `Unifi / WEB BROWSER v1.7.9` **2,578키** · 용어집 API **v5.5**(118 용어)

---

## 0. 변경 내역 — before → after

| 언어 | 변경 전 (현 등록값 v1.7.9) | 변경 후 (이번 패치) |
|---|---|---|
| ko_KR | 모든 바우처 {{0}}% **할인!** | 모든 바우처 **최대** {{0}}% **혜택!** |
| ja_JP | 全クーポン{{0}}%**OFF！** | 全クーポン**最大**{{0}}%**の特典！** |
| en_US | {{0}}% **off** all vouchers! | **Up to** {{0}}% **benefits on** all vouchers! |
| th_TH | **ลด** {{0}}% ทุกเวาเชอร์! | **รับสิทธิประโยชน์สูงสุด** {{0}}% ทุกเวาเชอร์! |
| zh_TW | 全部優惠券 {{0}}% **折扣！** | 全部優惠券**享最高** {{0}}% **優惠！** |

**변경 성격**: 어휘 2개가 바뀐다 — ① `할인`(discount) → `혜택`(benefit) ② `최대`(up to) 추가. **`{{0}}` 치환자는 유지**한다.

### 영향 범위 (공유 키 확인)

`UF_voucher_pay_more_discount`는 **Unifi 레지스트리 2,578키 중 유일 사용**이고, 저장소 전체 검색에서 참조는 **게이트 리포트 1건**(`gate_report_pay_bottomsheet_2026-09-15.md`)뿐이다. 다른 화면·다른 서비스(Dapp Portal `mini_`)에 **공유되지 않는다** → 이 패치로 바뀌는 화면은 **결제페이지 바텀시트 1곳**이다.

## (1a) 번역 전 — 한국어 원문 교정

| 항목 | 결과 |
|---|---|
| 맞춤법·띄어쓰기 | `모든 바우처 최대 {{0}}% 혜택!` — 이상 없음(`최대` 뒤 띄어쓰기 정상 · 조사 없음) |
| 비표준 공백(`\xa0`) | 없음 |
| 종결 표기 | 감탄형 `!` — 형제 키(`UF_voucher_pay_more_title` `더 구매하고 싶다면?`)와 동일한 배너 톤 |
| 치환자 표기 | **UIT 규칙 `{{0}}` 준수** |

**alias 기록** — 이 키의 Figma 원문은 `모든 바우처 15% 할인!`이다. 2026-09-15 바텀시트 작업에서 이미 `{모든 바우처 15% 할인! → 모든 바우처 {{0}}% 할인!}`(변수화) alias가 기록됐고, 이번 패치로 **`{모든 바우처 15% 할인! → 모든 바우처 최대 {{0}}% 혜택!}`** 로 갱신된다. Figma 원문과는 계속 달라진다(사용자 확정 문구 채택의 결과). Figma 원본 수정 요청은 2026-09-14 종결 방침에 따라 올리지 않는다.

## (a) 자동 검증 — `scripts/validate_translation.py` P0/P1/P2 요약

용어집 v5.5(118 용어) 기준, 패치 적용본 1키 × 5개 언어.

| 심각도 | 건수 |
|---|---:|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

**P0 = 0 → 통과 기준 충족.**

## (b) 수동 3단계 체크표 — **전수** (1키 × 5개 언어 = 5셀 전체 행)

패치 대상 키의 **5개 언어 전체 행**을 한 줄씩 검토했다(변경 언어로 한정하지 않음 — (A) 모드라 5개 언어 모두 변경분).

### 1단계 — 한국어 원문
(1a) 참조. 오타·맞춤법·띄어쓰기·조사·비표준 공백 이상 없음. `최대`는 합산 소구율이 **상한**임을 명시하는 어휘로, 아래 (d-2) 1번의 정책과 일치한다.

### 2단계 — 용어집 대조 (v5.5 정본)

| 용어 | 용어집 정본 | 적용값 | 판정 |
|---|---|---|---|
| **바우처** | ja `クーポン` / en `Voucher` / th `เวาเชอร์` / zh `優惠券` | ja `クーポン` · en `vouchers` · th `เวาเชอร์` · zh `優惠券` | ✅ 전 언어 준수 |
| **혜택** | ja `特典` / en `benefit` / th `สิทธิประโยชน์` / zh `優惠` | ja `特典` · en `benefits` · th `สิทธิประโยชน์` · zh `優惠` | ✅ 전 언어 준수 |
| **최대** | ja `最大` / en `up to` / th `สูงสุด` / zh `最高` | ja `最大` · en `Up to` · th `สูงสุด` · zh `最高` | ✅ 전 언어 준수 |

**실측 선례(등록값 2,578키)**: ja `最大` 103건 · `特典` 70건 / en `up to` 92건 · `benefit(s)` 23건 / th `สูงสุด` 107건 · `สิทธิประโยชน์` 40건 / zh `最高` 81건 · `優惠`(券 제외) 48건. **추측 표기 0건** — 전부 기등록 표기를 따랐다.

### 3단계 — 다국어 (5셀 전건)

| 언어 | 검토 | 판정 |
|---|---|---|
| ja_JP | `全クーポン最大{{0}}%の特典！` — **변수 앞뒤 무공백**(ja 관례 · 선례 `最大{{0}}見込み`·`毎日最大{{0}}の利息`) · 전각 `！` 유지(변경 전과 동일) · 조사 `の` 로 명사구 연결 | ✅ |
| en_US | `Up to {{0}}% benefits on all vouchers!` — 선례 `Up to {{0}} estimated`·`Extra benefits up to {{0}}` 어순 일치 · 문두 대문자 · 40자 이내 배너 길이 | ✅ |
| th_TH | `รับสิทธิประโยชน์สูงสุด {{0}}% ทุกเวาเชอร์!` — 선례 `รับดอกเบี้ยสูงสุด {{0}} ต่อปี`·`รับรางวัลสูงสุด {{0}}` 구조 일치 · 반각 `!`(변경 전과 동일) | ✅ |
| zh_TW | `全部優惠券享最高 {{0}}% 優惠！` — **한자↔변수 공백 1칸**(2026-09-14 규칙) · `享最高…優惠` 선례 `享最高<span />優惠`(`UF_guide_deposit_btn1`) · 번체 정자 · 전각 `！` | ✅ |
| ko_KR | 1단계 참조 | ✅ |

**공통 점검**: 빈칸 0 · **언어 혼입 0** · **placeholder `{{0}}` 5개 언어 전건에 정확히 1회** · `/n` 오타 0 · 컬럼 swap 0(en·ko·ja·zh·th 칸이 각각 해당 문자체계) · 단어 중복 0 · 외래어 음차 오염 0 · 마침표 정책은 5개 언어 모두 감탄부호로 통일(ja·zh 전각 / ko·en·th 반각 — **변경 전 등록값의 관례를 그대로 유지**).

**⛔ 컬럼 정렬 검증(Step 2-1)**: (A) ko-source 모드로 **5개 언어를 전부 새 값으로 교체**하므로 단일 언어 패치의 컬럼 소실 위험이 발생하지 않는다. 그럼에도 현행 등록값 5셀의 문자체계를 사전 대조해 **misalignment 0건**을 확인했다.

## (c) 검증기 보고 건별 처리 판정

**자동 검증이 P0·P1·P2 모두 0건**이라 판정 대상 건이 없다. (실제 위반 0 · 오탐 0 · 정책 보류 0)

수동 검토에서 별도로 다룬 판단 1건을 남긴다.

| # | 건 | 판정 |
|---|---|---|
| 1 | zh에서 `優惠券`(바우처)과 `優惠`(혜택)가 한 문장에 함께 나온다 | **유지** — 둘 다 **용어집 정본**이고, 중국어 마케팅 카피에서 `優惠券享…優惠` 반복은 자연스럽다. 회피하려 `回饋`·`折扣` 등으로 바꾸면 **용어집 위반**이 된다 |

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

**(d-1) 용어집 보완 권장 — 대기 목록 등재 없음.** 이번 패치의 3개 용어(`바우처`·`혜택`·`최대`)는 **전부 v5.5에 등재**돼 있고 오탐도 0건이라 보완 신호가 없다. (변경 전 값에 쓰이던 `할인`은 이번 패치로 이 키에서 빠졌다 — `할인`은 여전히 용어집 미등재이나, 이 키 밖의 사안이라 등재하지 않는다.)

**(d-2) 추가 개선·제안 안내**

| # | 내용 | 판단 주체 |
|---|---|---|
| 1 | ✅ **이번 변경은 확정 정책과 정합적이다** — 2026-09-14 확정은 **할인 5% · 캐시백 10% · 합산 소구 15%**다. `15%` 전체를 `할인`으로 부르던 기존 문구는 실제로는 **할인+캐시백 합산**이라 부정확했고, `혜택`으로 바꾸면 이 불일치가 해소된다. `최대` 추가도 상한 표기로 적절하다 | (정합 확인) |
| 2 | ⚠️ **`{{0}}`에 내려오는 값이 무엇인지 FE 확인이 여전히 필요하다** — 바텀시트 작업(2026-09-15)에서 올린 `benefitrate`가 **합산값(15)** 을 주는지 미확인 상태다. `최대 15% 혜택`으로 읽히려면 합산값이어야 한다 | FE |
| 3 | **형제 키 `UF_voucher_pay_more_desc`는 여전히 `할인된 가격으로`다** — 같은 바텀시트 안에서 제목은 `혜택`, 본문은 `할인`으로 어휘가 갈린다. 통일이 필요하면 별도 패치 대상이다(이번 요청 범위 밖이라 **변경하지 않았다**) | 기획 |
| 4 | **Figma 원문은 `모든 바우처 15% 할인!`으로 남는다** — 다음 재조회 때 같은 alias가 다시 발생한다. Figma 수정 요청은 2026-09-14 종결 방침에 따라 올리지 않되, 위키 Description에 기록한다 | (기록 예정) |

## 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 | **0건** ✅ |
| 수동 P0 (전수 5셀) | **0건** ✅ |
| P1 · P2 | **각 0건** — 처리 판정 대상 없음 ✅ |
| 치환자 무결성 | `{{0}}` 5/5 보존 ✅ |
| (d) 임의 적용 | 없음 ✅ |

→ **게이트 통과.** 엑셀 전달 → 사용자 확인 후 위키 반영으로 진행한다.

---

## 반영 기록 (2026-09-15)

### (d-2) 처리 결과 — 사용자 확정으로 3건 종결

| # | 내용 | 결과 |
|---|---|---|
| 2 | `{{0}}`에 내려오는 값이 합산값인지 FE 확인 필요 | ✅ **해소 — FE에서 합산 확정**(할인 5% + 캐시백 10% = 15%) |
| 3 | 형제 키 `UF_voucher_pay_more_desc`의 `할인` 어휘 통일 여부 | ✅ **종결 — 「할인」이 맞다**(사용자 확정). 본문은 개별 상품의 **할인가**를 가리키므로 제목(`혜택`)과 통일하지 않는다. **변경하지 않았다** |
| — | 엑셀 시스템 업로드 | ✅ **완료**(사용자 수행) |

### 프레임 재조회 — Figma 2개 (위키 v204 → v205)

| 프레임 | 상태 | 반영 |
|---|---|---|
| `74620-13737` **결제페이지 - 바텀시트** | 화면 갱신됨 | 첨부 `annotated_74620-13737.png` **v2**로 재렌더 · **「17일 버전」 표기** |
| `74665-14721` **결제페이지 - 바텀시트 Hotfix** | 신규 | 첨부 신규 · Screen 표에 **행 추가** · **「현재 적용」 표기** |

**두 프레임의 실측 차이**

| 항목 | 74620 (17일 버전) | 74665 Hotfix (현재) |
|---|---|---|
| 소구 문구 | `모든 바우처 15% 혜택!` | `모든 바우처 최대 15% 혜택!` |
| 상품 카드 | `혜택가` + `15% ¥989` + `정가 ¥1,154`(취소선) | **`캐시백 10%` 뱃지** + `5% ¥4,500` |
| Figma 코멘트 | 5건(`xlt` 2) | **0건** — 정책은 화면 실측 기준으로 기술 |

**신규 키 0건** — 새로 나타난 텍스트는 전부 기존 등록값이 커버한다(등록값 2,578키 실측 대조).

| 화면 텍스트 | 커버하는 기존 키 |
|---|---|
| `캐시백 10%` | `UF_voucher_recommend_cashback` = `캐시백 {{0}}%` |
| `혜택가` | `UF_voucher_price_benefit` |
| `정가` | `UF_voucher_price_original` |

GA는 **같은 화면의 Hotfix 버전**이라 `payment_more_bottomsheet_01`을 재사용하고 이벤트를 신설하지 않았다.

`check_wiki_storage.py` pre·post 전 회차 exit 0 · PUT 직전 라이브 버전 재확인(204) 후 rebase.

### ⚠️ 남은 것 — 17일 버전 문구 (미착수)

사용자 확정: **17일 버전에서 `UF_voucher_pay_more_discount`를 `모든 바우처 {{0}} 혜택!`로 재변경**한다. 위키에는 「17일 버전 예정」으로 **기재만** 했고 **5개 언어 재번역·엑셀은 만들지 않았다**(별도 패치 대상).

🔴 **FE 계약 확인이 필요하다** — 이 문구는 `%`가 **변수 안에 포함**된다. 현재 등록값(`최대 {{0}}% 혜택!`)은 `%`가 템플릿 쪽이라, 17일 문구로 가면 `benefitrate`가 `15`가 아니라 **`"15%"` 문자열**을 내려줘야 한다. 같은 변수를 쓰는 다른 키(`UF_voucher_pay_cashback` = `{{0}}% 적립 캐시백` 등)와도 계약이 갈린다.

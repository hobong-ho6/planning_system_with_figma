# 번역 품질 게이트 리포트 — 일본어 표기 통일 22키 (2026-09-13)

- **대상**: XLT 시스템에 등록된 키 중 일본어가 **구 표기**로 남아 있는 전량
- **산출물**: `xlt/ja표기통일_Unifi_20260913.xlsx`(13키) · `xlt/ja표기통일_DappPortal_20260913.xlsx`(9키) — **서비스별 키스페이스가 분리돼 있어 파일을 나눴다**
- **참조**: 용어집 v5.4(118 용어 · deprecated 22) · XLT 등록값 Unifi `v1.7.6`(2,549키) + Dapp Portal `v2.6.1`(1,685키) — **2026-09-13 재조회**(캐시 미사용)
- **변경 범위**: **일본어(ja_JP) 한 컬럼만**. ko/en/th/zh는 등록값 그대로다.

## 0. 대상 도출 — 전수 실측

두 레지스트리 **4,234키 전체**의 ja를 훑어 용어집 `deprecated_terms` 위반을 찾았다.

| 구 표기 | 권장 | Unifi | Dapp Portal | 계 |
|---|---|---|---|---|
| `バウチャー` | `クーポン`(deprecated #22 · v5.4) | 13 | 7 | **20** |
| `キャッシュバック` | `還元`(deprecated #20 · v5.0) | 0 | 3 | **3** |
| `オリヤン` | `オリーブヤング`(2026-09-12 사용자 결정) | 0 | 1 | **1** |

중복 포함 실제 키 수는 **22건**(`mini_guidekim_kpick_oa_consent_desc`가 `バウチャー`+`キャッシュバック` 둘 다, `mini_guidekim_all_korea_experience`가 `バウチャー`+`オリヤン` 둘 다).

> `バウチャー` 20건은 2026-09-12에 이미 승인돼 갱신 엑셀을 냈으나 **업로드되지 않아 그대로 남아 있었다**. 이번에 `キャッシュバック` 3건과 합쳐 한 번에 정리한다.

## 1a. 한국어 원문 교정
**해당 없음** — 이번 작업은 ja 한 컬럼만 바꾸며 ko는 등록값을 그대로 둔다(alias 없음).

## 2. 자동 검증 (`scripts/validate_translation.py` 실행)

| 파일 | 항목 | 🔴 P0 | 🟡 P1 | 🟢 P2 |
|---|---|---|---|---|
| `ja표기통일_Unifi_20260913.xlsx` | 13키 | **0** | 8 | 6 |
| `ja표기통일_DappPortal_20260913.xlsx` | 9키 | **0** | 11 | 3 |

**P0 = 0건** → 게이트 통과.

## 3. 수동 3단계 검토 (전수 — 22키 × 5개 언어 110셀 전체 행)

### 1단계 — 한국어 원문
등록값을 손대지 않았다. 22건 ko를 한 줄씩 확인 — 맞춤법·띄어쓰기·치환자(`{{0}}`·`{0}`) 이상 **0건**.

### 2단계 — 용어집 대조(v5.4)
| 점검 | 결과 |
|---|---|
| `deprecated_terms` 잔존 | 치환 후 `バウチャー`·`キャッシュバック`·`オリヤン` **0건** |
| 용어집 정본 반영 | `바우처` = ja `クーポン` · `캐시백` = ja `還元` 전건 적용 |
| 예외 용어 | `JPYC`·`LINE`·`Unifi`·`MYショッピング` 원어 유지 ✅ |
| 치환자 보존 | `{{0}}` 2건 · `{0}` 2건 — 위치·개수 그대로 |

### 3단계 — 의미 기반(ja 집중)
치환이 **조사·복합어·문장 구조를 깨지 않는지** 22건 전건 확인했다.
- 복합어 분해 확인 — `キャッシュバックポイント` → **`還元ポイント`**(`還元` 뒤에 `ポイント`가 바로 붙는 형태). 등재 `UF_my_benefit_cashback_point`(`還元ポイント`)와 일치
- 조사 결합 — `バウチャーや旅行関連商品を` → `クーポンや…`, `バウチャーの利用方法` → `クーポンの利用方法` 등 조사 앞 명사 교체라 문형 변화 없음
- 줄바꿈(`\n`) 4건 위치 보존 확인
- 나머지 4개 언어는 **셀을 건드리지 않았다**(diff 0)

## 4. P1·P2 각 건 처리 판정 (전 28건)

| 유형 | 건수 | 판정 | 사유 |
|---|---|---|---|
| 용어 불일치 — `구매/구매하기/교환/전체보기/최대/완료` 계열 | 17 | **오탐** | 모두 **기존 등록값 그대로**이며 이번 변경과 무관하다. 문맥이 용어집 등재 문맥과 달라 반복 오탐되는 항목(2026-09-12 리포트와 같은 패턴) |
| 용어 불일치 — `친구 추가`·`공식 계정` 계열 | 2 | **오탐** | 등재 정본 표기(`友だち追加`)를 이미 쓰고 있다 |
| P2 마침표 스타일 | 9 | **오탐 · 현행 유지** | 등록값 그대로. 문장형에 마침표를 쓰는 기존 규칙과 일치 |

**실제 위반 0건** — 이번 파일에서 바꾼 값은 전부 용어집 정본 적용이며, 검증기가 새로 잡은 위반은 없다.

## 5. (d) 추가 개선·제안

### (d-1) 용어집 보완 권장
신규 권장 없음. 기존 미결 3건(`채우기`·`보내기` ja·`최대`)은 2026-09-12 리포트 그대로.

### (d-2) 추가 개선·제안
| # | 대상 | 제안 |
|---|---|---|
| ① | `UF_clinic_detail_point_buy_desc` · `UF_voucher_recommend_title` | `unifi_mini_v2.0_voucher_detail_7키_20260913.xlsx`와 **이 파일에 중복**된다. 값은 동일하므로 **둘 중 하나만 올리면 된다**(양쪽 다 올려도 결과는 같다) |
| ② | `mini_guidekim_cat_voucher` ko = `바우처` | 카테고리 라벨이라 `상품권`/`바우처` 통일 결정의 영향권이다. ko는 이미 `바우처`라 변경 없음 — 확인만 |
| ③ | 잔존 감시 | 업로드 후 `deprecated_terms` 재실측을 권한다. 이번처럼 **승인만 되고 업로드가 안 되면 구 표기가 계속 남는다**(20건이 하루 넘게 잔존했다) |

## 6. 통과 판정
- 자동 **P0 = 0** · 수동 **P0 = 0** → **통과**.
- P1 19건 · P2 9건 전건 처리 판정 완료(실제 위반 0 · 전부 기존 등록값의 반복 오탐).
- (d) 권장 3건은 사용자 결정 전까지 미반영.

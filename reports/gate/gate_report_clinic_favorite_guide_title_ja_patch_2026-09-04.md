# 게이트 리포트 — `UF_clinic_detail_favorite_guide_title` ja 축약 패치 (키 단위 · (B) lang-specific)

- **모드**: 키 단위 번역 패치 (B) — ja_JP만 사용자 확정값으로 교체, ko·en·th·zh 4개 언어 보존
- **대상 위키**: `4667512757` 「클리닉 예약 동선 개선」 · Screen 행 `(Unifi mini) 상세페이지 - 즐겨찾기 안내`(코치마크)
- **대상 파일**: `xlt/xlt_clinic_favorite_guide_title_ja_1key_20260904.xlsx` (1키 · 5셀 · 업로드용 — 사용자 지시로 해당 키만 포함)
- **담당 FE 팀**: **UIT** — 프리픽스 `UF_` (치환자 없음)
- **사용자 결정(2026-09-04)**: 코치마크 타이틀 ja 2행을 **2글자 축약** — 제시한 A/B/C안 중 **A안** `お気に入り登録しましょう。` 채택
- **검증 범위**: **전수 점검** — 엑셀 1키 **전체 행** 5셀 자동 검증 + 수동 3단계. 현행값은 XLT 등재값(Unifi WEB BROWSER v1.7.5 · 원본 재조회)·위키 다국어 표 양쪽에서 확보해 컬럼 정렬(Step 2-1) 확인 — 5셀 모두 위키 정본과 일치, 어긋남 0

---

## (1a) 한국어 원문 교정

| # | ko_KR | 판정 |
|---|---|---|
| 1 | `Unifi mini를\n즐겨찾기에 추가해보세요` | **변경 없음** — ko는 이번 패치 대상이 아니며 등재값·위키·Figma 원문 일치. nbsp·제로폭 0건 |

**alias 기록**: 없음(ko 미변경). Figma 원본 수정 요청 없음.

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py` (용어집 v5.1)

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** ✅ |
| 🟡 P1 | 0건 |
| 🟢 P2 | 0건 |

---

## (b) 수동 3단계 체크표 — 1키 5셀 전수

### 1단계 (한국어 원문)

| 키 | ko_KR | 판정 |
|---|---|---|
| `UF_clinic_detail_favorite_guide_title` | `Unifi mini를\n즐겨찾기에 추가해보세요` | 정확 — 2행 구조·띄어쓰기 정상(보존) |

### 2단계 (용어집·등재값 정합)

| 항목 | 근거 (등재값 실측 · 2,462키) | 판정 |
|---|---|---|
| 「즐겨찾기」 대역 | 등재 ja `お気に入り` 4건 전부 이 코치마크 2키 + 동일 어휘. 짝 키 `_desc` ja도 `「お気に入り」に追加` | 정확 — 어휘 유지 |
| 어미 「〜しましょう」 | 등재 8건(권유형). 「してください」 98건이 다수지만, ko 「해보세요」의 권유 뉘앙스에는 `しましょう`가 더 가깝고 축약 목적에도 부합. 반말 `しよう`(6건)는 톤 불일치로 배제 | 정확 |
| 「お気に入り登録」 | 「お気に入りに追加」의 관용 축약 표현(일본 앱 UI 상용). 의미 손실 없음 | 정확 |
| 문말 `。` | 짝 키 `_desc` ja가 `。`로 끝남 → 유지 | 정확 |

### 3단계 (다국어 5셀)

| 언어 | 값 | 판정 |
|---|---|---|
| ja_JP | `Unifi miniを\nお気に入り登録しましょう。` | **정확** — before `お気に入りに追加してください。`(14자) → after 12자(**−2**). 1행 `Unifi miniを` 유지 · 2행 구조 유지 · 언어 혼입 없음 |
| en_US | `Add Unifi mini\nto your favorites` | 보존(등재값 동일) |
| th_TH | `เพิ่ม Unifi mini\nในรายการโปรด` | 보존(등재값 동일) |
| zh_TW | `將 Unifi mini\n加入我的最愛` | 보존(등재값 동일) |
| ko_KR | (1단계 참조) | 보존 |

---

## (c) 각 건 처리 판정

| # | 대상 | 지적 | 처리 판정 | 사유 |
|---|---|---|---|---|
| — | 자동 검증 | P0/P1/P2 **0건** | 해당 없음 | — |
| M-1 | `_title` ja | 축약으로 「追加」 어휘가 「登録」으로 바뀜 — 짝 키 `_desc` ja는 `「お気に入り」に追加` 유지 | **정상(사용자 결정) · 고지** | 타이틀(축약 우선)과 본문(절차 설명)의 역할이 달라 어휘가 갈려도 의미 충돌 없음. 통일을 원하면 `_desc`도 `登録`으로 맞출 수 있으나 요청 범위 밖 |
| M-2 | XLT 등재값 | 등재값이 구값(`追加してください。`) | **P1 — 사용자 업로드 필요** | 1키 엑셀 업로드 후 화면 반영 |
| M-3 | 일본어 검수 하위 페이지 `4685320765` | 이 키의 ja 「현행」 칸이 구값 | **고지(범위 밖)** | 사용자 지시 범위가 `4667512757`이라 미수정. 검수 페이지 정합을 원하면 별도 갱신 |

**통과 판정: P0 = 0건 (자동 + 수동 모두)** → 엑셀 전달·위키 반영 진행.

---

## (d) 추가 개선·제안 (권장 — 임의 반영 금지)

- **(d-1) 용어집 보완 권장**: 없음 — 「즐겨찾기」 ja는 등재 4/4 `お気に入り`로 일관(실측 N=4). 반복 오탐 없음.
- **(d-2) 제안**: ⓐ 짝 키 `UF_clinic_detail_favorite_guide_desc` ja 「追加」↔ 타이틀 「登録」 어휘 통일 여부 결정 ⓑ 일본어 검수 페이지(`4685320765`) 「현행」 칸 동기화 ⓒ 업로드 후 `fetch_xlt_registry.py --service Unifi`로 등재값 재확인.

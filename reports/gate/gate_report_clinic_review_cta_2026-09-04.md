# 게이트 리포트 — clinic 전체 리뷰 화면 CTA 변수화 (1키)

- **대상 프레임**: Figma `71543-3322` (Unifi mini) 상세페이지 - 전체 리뷰
- **모드**: 코멘트 선별 모드 (`xlt` 마커 1건)
- **대상 키**: `UF_clinic_detail_btn_consult` (1키 × 5개 언어)
- **담당 FE 팀**: **UIT** — 위키 Screen 섹션이 UIT 구분이므로 프리픽스 `UF_` · 치환자 `{{0}}` 적용
- **용어집**: v5.0
- **일자**: 2026-09-04

---

## (1a) 번역 전 — 한국어 원문 교정

Figma 코멘트를 매 실행 새로 조회했다(캐시 미사용). 루트 코멘트 2건 중 `xlt` 마커는 1건.

| 코멘트 | y | 지시 |
|---|---|---|
| 1 | 159 | (정책) 브릿지에서 리뷰 더보기 선택 시 상세에서 바텀시트로 노출 — XLT 대상 아님 |
| 2 | 734 | **`xlt, {{0}} 상담신청`** — CTA 버튼을 변수화 |

| # | Figma 원문 | 채택값 | 사유 |
|---|---|---|---|
| 1 | `디에이 성형외과 무료상담` | `{{0}} 상담신청` | **코멘트 지시**. 병원명을 변수로 빼고 문구를 「상담신청」으로 통일. Figma 원본은 `무료상담`이라 **원본 수정 요청 대상** |
| 2 | `디에이 성형외과` | — | 병원명 하드코딩 → `{{0}}`로 치환 |

**alias 기록**: `{디에이 성형외과 무료상담 → {{0}} 상담신청}`

**한국어 원문 교정**: 맞춤법·띄어쓰기 이상 없음. `상담신청`은 기존 등재값(`병원 상담신청`)과 동일한 붙여쓰기라 표기 일관.

---

## (a) 자동 검증 — `validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

---

## (b) 수동 3단계 체크표 — **전수 점검** (1키 × 5개 언어 = 5셀)

부분 작업이지만 대상 키 전 언어를 한 줄씩 검토했다.

### 1단계 — 빈칸·언어 혼입·컬럼 정렬·치환자

| 점검 | 결과 |
|---|---|
| 빈 셀 | 0건 |
| 컬럼 어긋남 | 0건 (ja 칸 가나·한자 / th 칸 타이 문자 / zh 칸 번체 정상) |
| **치환자 무결성** | **5개 언어 전건 `{{0}}` 정확히 1개** — UIT 이중 중괄호 규칙 준수. 단일 중괄호(`{0}`) 혼입 0건 |
| 기존 값 대비 | 5개 언어 모두 「병원/클리닉」 자리를 `{{0}}`로 치환. 나머지 어절 보존 |

### 2단계 — 외래어 음차 / 붙은 띄어쓰기 / 다의어

| 항목 | 결과 |
|---|---|
| 외래어 음차 오염 | 해당 없음(고유명사 없음) |
| 붙은 띄어쓰기 누락 | en `Request consultation with {{0}}` — 어절 분리 정상 |
| 다의어 모호 | 없음 |
| **변수 앞뒤 공백** | ko `{{0}} 상담신청`·th `ขอปรึกษากับ {{0}}` = 공백 有 / ja `{{0}}に相談を申し込む`·zh `申請{{0}}諮詢` = 공백 無 — **각 언어 조판 관례에 맞음**(CJK는 변수 앞뒤 공백 미삽입) |
| 받침·조사 | ko는 변수 뒤가 공백+명사라 조사 없음 → 병원명 받침과 무관. **조사 문제 없음** |

### 3단계 — 언어별 중점 점검

| 언어 | 결과 |
|---|---|
| **ja_JP** | `{{0}}に相談を申し込む` — 조사 `に`가 병원명 뒤에 자연스럽게 붙음. 기존 `クリニックに…` 구조 그대로 유지 |
| **en_US** | `Request consultation with {{0}}` — 변수를 문미에 둬 병원명 길이에 안전. 기존 `Request clinic consultation`의 어순을 최소 변경 |
| **th_TH** | `ขอปรึกษากับ {{0}}` — `กับ`(with) 추가로 병원명 연결. 타이어는 변수 앞 공백 필요 |
| **zh_TW** | `申請{{0}}諮詢` — 번체 정자. 변수를 관형 위치에 둬 간결 |

---

## (c) 검증기 보고 전건 처리 판정

P0 · P1 · P2 **모두 0건**이라 처리 판정 대상이 없다.

---

## (d) 추가 개선·제안 (권장 — 사용자 결정 전 임의 반영 금지)

### (d-1) 용어집 보완 권장

이번 키에서 새로 발견된 용어집 결손은 **없다**. 다만 직전 run(Landpress 다국어)에서 보고한 2건이 **미반영 상태**다.

| 용어 | 언어 | 현재 등재값 | 권장 | 실측 (Unifi/WEB BROWSER v1.7.5 · 2,461키) |
|---|---|---|---|---|
| `가격` | zh_TW | `售價` | **`價格`** | `價格` 9건 · `售價` **0건** |
| `유의사항` | th_TH | `หมายเหตุ` | **`ข้อควรระวัง`** | `ข้อควรระวัง` 2건 · `หมายเหตุ` **0건** |

### (d-2) 추가 개선·제안

1. **Figma 원본 수정 요청** — 버튼 문구가 `디에이 성형외과 무료상담`인데 XLT는 `{{0}} 상담신청`이다. 화면과 문구가 어긋나므로 Figma를 `{{0}} 상담신청`으로 맞추도록 요청 권장.
2. **`VIP` 배지** — 프레임 y=18159에 `VIP` 텍스트가 있으나 `xlt` 마커가 없다. 5개 언어 공통 표기로 두면 키 불필요하지만, **노출 조건(등급 정책)이 정의되지 않았다** — 정책 확인 권장.
3. **프레임 밖 잔존 노드** — y=20005(프레임 하단 경계 18221 밖)에 `캐시백 20%` 텍스트가 떠 있다. 이 화면과 무관한 잔존 노드로 보이며 정리 권장. (참고: 실제 캐시백률은 병원별 5·8·10·15%로, 20%는 어느 병원에도 없다.)
4. **`UF_clinic_detail_btn_consult` 재사용 범위** — 이 키는 상세페이지 CTA로도 쓰인다. 변수화하면 **상세페이지에서도 병원명이 들어가야** 하므로 FE에 전달 필요(상세는 이미 병원 컨텍스트가 있어 자연스럽지만, 값 주입 누락 시 `{{0}}`가 그대로 노출된다).

---

## (4) 통과 판정

- **P0 = 0건** (자동 + 수동 전수) → **통과**
- P1 · P2 0건
- (d) 권장 사항은 사용자 결정 전까지 반영하지 않음

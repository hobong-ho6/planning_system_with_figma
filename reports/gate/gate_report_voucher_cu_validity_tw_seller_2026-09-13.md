# 게이트 리포트 — CU 유효기간 기산점 통일 + zh_TW 판매자 행 제거 + ja_JP 기산점 표현 통일

- 대상: LV beta `voucher_product` (컬렉션 4상품 · 필드 `voucher_detail` · `my_voucher_detail`)
- 변경 1: **CU 유효기간** — 「교환 유효기간 60일」 → 「구매일로부터 60일간 유효」 (2키 × 5개 언어)
- 변경 2: **zh_TW `販售商` 행 제거** — 4개 브랜드 (번역 변경 아님 · 삭제)
- 변경 3: **이마트24 ja_JP** 「購入日**より**」 → 「購入日**から**」 (1키 × 1개 언어 · 다이소와 통일)
- 일자: 2026-09-13

---

## (1a) 번역 전 — 한국어 원문 교정

| # | 항목 | 교정 전 | 교정 후 | 사유 |
|---|---|---|---|---|
| 1 | `voucher_detail.summary.유효기간` (CU) | 교환 유효기간 60일 (연장불가) | 구매일로부터 60일간 유효 (연장불가) | **기산점 누락** — 다른 3개 브랜드는 모두 「구매일로부터」로 기산점을 명시하는데 CU만 빠져 있었다. 사용자 지시로 통일 |
| 2 | `productInfo/notice` › 유의사항 첫 줄 (CU) | 교환 유효기간은 60일입니다. | 교환 유효기간은 **구매일로부터** 60일입니다. | 1번만 고치면 같은 상품 안에서 요약은 구매일 기준, 유의사항은 기산점 없음으로 **자기모순**이 남는다. 다이소·이마트24의 등록 문장과도 일치시켰다 |

| 3 | 이마트24 `유의사항` 첫 줄 ja_JP | 交換有効期間は購入日**より**60日です。 | 交換有効期間は購入日**から**60日です。 | 같은 위치·같은 의미의 문장이 다이소는 `から`, 이마트24는 `より`로 **기등록값이 갈려 있었다**. 사용자 지시로 `から`로 통일 |

**범위 판단 기록** — 사용자 지시는 유효기간 표기 통일이었고 명시 대상은 1번이다. 2번은 같은 사실(기산점)이 담긴 두 번째 위치라서 함께 고쳤다. 괄호 부연(「시즌 상품, 기업 경품(B2B)…」)은 CU 원문에 없으므로 **넣지 않았다**. 3번은 별건으로 보고했던 (d-2) 권고 항목이며, 사용자 승인으로 이번 작업에 포함했다.

### Figma 원문 → 산출물 alias

없음 — 이번 변경은 Figma 텍스트가 아니라 **SK 브랜드 원문에서 파생된 LPC 값**이며, 대체 문구는 같은 컬렉션의 다이소·이마트24 **기등록값을 그대로 가져왔다**.

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py`

```
✓ 엑셀 로드: 3개 항목 / 용어집 118개 용어
1단계 한국어 검증: P0 0건
2단계 용어집 검증: 완료
3단계 다국어 검증: 완료
```

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | 9건 |
| 🟢 P2 | 1건 |

**통과 기준 충족** — P0 = 0건(자동 + 수동 모두).

---

## (b) 수동 3단계 체크표 — 전수 점검

**전수 점검 명시**: 변경 2키만 보지 않고 **CU 상품의 `voucher_detail`·`my_voucher_detail` 전 필드 ko_KR을 한 줄씩 수동 검토**했다. 추가로 4개 브랜드 × 5개 언어의 `summary` 라벨 구성과 매장 이용 안내 문구를 전수 대조했다.

| # | 단계 | 대상 | 검토 항목 | 판정 |
|---|---|---|---|---|
| 1 | 1단계(원문) | CU 변경 2키 | 맞춤법·띄어쓰기 | 정상 |
| 2 | 1단계 | CU `voucher_detail` 전 필드 | 붙은 띄어쓰기·음차 오염 | 없음 |
| 3 | 1단계 | CU 유의사항·사용안내 | 조사·받침 | 정상 (「구매일로부터」 적정) |
| 4 | 1단계 | CU 전 필드 | 유효기간 언급 위치 누락 여부 | **2곳 발견 → 2곳 모두 수정**(summary · 유의사항). 그 밖에 「유효기간 내 100% 환불」·「유효기간 내 잔액 소진」은 **기산점과 무관**하므로 변경 대상 아님 |
| 5 | 3단계(다국어) | 변경 2키 × 5언어 | 기등록값과의 일치 | **10/10 문자열 일치** — 아래 (c) 근거 |
| 6 | 3단계 | th_TH | 마침표 관례 | 태국어는 마침표 미사용 — 다이소 기등록값과 동일하게 마침표 없이 종결 |
| 7 | 3단계 | ja_JP | 「購入日から」/「購入日より」 혼재 | 다이소가 `から`, 이마트24가 `より`로 **기등록값이 이미 갈려 있다**. CU는 다이소 쪽 `から`를 채택. 이마트24 정리는 (d-2) 권고 |
| 8 | 3단계 | 4브랜드 × 5언어 | `summary` 라벨 개수 | **zh_TW만 5개**(`販售商` 추가) — 변경 2로 제거 |
| 9 | 3단계 | 전 로케일 | 치환자 | 해당 없음(치환자 미사용) |
| 10 | 3단계 | ja_JP 4개 브랜드 | 기산점 표현 통일 | 변경 후 **다이소·이마트24·CU 모두 `購入日から`**로 일치. 올리브영은 해당 문장 자체가 없음(기산점 문장 미보유) |

---

## (c) 자동 검증기 P0/P1/P2 각 건 처리 판정

### P0 (0건)
없음.

### P1 (9건) — **전건 오탐**

가장 강한 근거: **이번 5개 언어 값은 신규 번역이 아니라 같은 컬렉션의 다이소·이마트24 기등록·공개값을 그대로 가져온 것**이다. 대조 결과 `summary`는 5개 언어 **전건 문자열 완전 일치**, 유의사항 문장은 다이소 문장의 **접두 일치**(괄호 부연만 제거)를 실측 확인했다.

| # | 키 | 지적 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | `cu_summary_validity` en_US | '구매' → `Buy` 권장 | **오탐** | 여기의 '구매'는 동작이 아니라 **날짜 수식**(`the purchase date`)이다. `fetch_xlt_registry.py` 실측: ko '구매' 원문의 en 표기는 **purchase 20건 / buy 20건**으로 둘 다 정본이며 문맥이 가른다. 다이소·이마트24 기등록값도 `from the purchase date` |
| 2 | `cu_caution_validity` en_US | '교환' → `swap` 권장 | **오탐** | 여기의 '교환'은 포인트 스왑이 아니라 **상품권 교환(exchange)**. 실측: 교환→swap 41건 / **exchange 12건**으로 둘 다 존재하며, swap은 전부 JPYC 포인트 교환 문맥이다. 다이소·이마트24 기등록값도 `exchange validity period` |
| 3 | `cu_caution_validity` th_TH | '교환' → `แลกเปลี่ยน` | **오탐** | 상동. 기등록 태국어 정본은 `ระยะเวลาแลกใช้`(교환 사용 기간) |
| 4 | `cu_caution_validity` zh_TW | '교환' → `交換` | **오탐** | 번체 정본은 `兌換`(상품권 교환). `交換`은 물물교환 뉘앙스. 기등록값도 `兌換有效期間` |
| 5 | `cu_summary_validity` en_US(2회차) | '구매' → `Buy` | **오탐** | 1번과 동일 건 |
| 6~9 | `emart_caution_validity` en_US·th_TH·zh_TW | '교환'·'구매' 지적 4건 | **오탐** | 1~4번과 **완전히 같은 문장 구조**다. 게다가 이마트24는 이번에 **ja_JP만 변경**했고 en·th·zh는 기등록값 그대로라 이 4건은 이번 변경과 무관한 기존 값에 대한 지적이다 |

### P2 (1건)

| # | 키 | 지적 | 판정 |
|---|---|---|---|
| 1 | `cu_summary_validity` | 마침표 없음이 소수 스타일 | **의도** — `summary`는 라벨:값 형태의 단문이라 4개 브랜드 전부 마침표를 쓰지 않는다. 마침표는 `productInfo` 본문 문장에만 사용 |

---

## (d) 추가 개선·제안 (권장 — 사용자 결정 전 미반영)

### (d-1) 용어집 보완 권장

**보완 권장 없음.** 이번 P1 5건은 용어집 매핑이 좁아서가 아니라 **동일 한국어의 정당한 문맥 분기**(구매=purchase/buy, 교환=exchange/swap)에서 발생했다. 실측 건수가 양쪽 모두 두 자릿수라 한쪽으로 좁히면 오히려 기등록값 다수와 충돌한다.

| 용어 | 용어집 | 실측(unifi 등록값) | 판단 |
|---|---|---|---|
| 구매 | 등재(en `Buy`) | purchase 20 / buy 20 | 문맥 분기 — 좁히면 안 됨 |
| 교환 | 등재(en `swap`) | swap 41 / exchange 12 | swap은 JPYC 포인트 전용 문맥 — 좁히면 안 됨 |

**⛔ 추측 표기 권장 금지 준수** — 위 수치는 전부 `scripts/fetch_xlt_registry.py` 실측이며 실사용 0건 표기는 권장하지 않았다.

### (d-2) 추가 개선·제안

| # | 항목 | 내용 | 권고 |
|---|---|---|---|
| 1 | ~~ja_JP 「購入日から」/「購入日より」 혼재~~ | — | **사용자 승인으로 이번 작업에 반영됨**(변경 3 · 위 (1a) 3번). `から`로 통일 |
| 2 | **올리브영 이용불가 매장 안내 부재** | SK 원본에는 점포 32곳 목록이 있으나 등록값에는 「일부 매장 제외」 요약뿐이고 확인 경로가 없다. 다이소는 확인 URL을 제공한다 | 사용자 결정: **현 구조 유지**. 보완한다면 다이소식 URL 한 줄이 가장 저렴 |
| 3 | **특정상거래법 표기 위치** | LV 신규 `voucher_detail`에는 판매자 표기가 없고 **XLT `UF_voucher_tokusho_link`**(5개 언어)로 화면에 제공된다. zh_TW LPC에만 남아 있던 `販售商` 행은 **중복**이므로 제거했다 | 제거 후에도 법정 표기는 XLT 링크로 유지된다 — FE가 해당 키를 실제로 노출하는지 확인 권장 |

---

## (5) 리포트 완결성

- 저장: `reports/gate/gate_report_voucher_cu_validity_tw_seller_2026-09-13.md`
- `python3 scripts/check_gate_report.py` exit 0 확인 필요

# 번역 품질 게이트 리포트 — `invite_me_too` 키 분리 + (Logged in) 초대페이지 갱신

- **대상**: Figma `63411-25034`(초대자 화면 전체) · `63411-34291`((Logged in) 초대페이지) · `63411-36558`(Unifi mini - 피초대자 OA 팔로우 당첨금 지급) / 위키 pageId `4479306980`
- **작업일**: 2026-07-28
- **담당 FE 팀**: **LV** (위키 Screen 섹션 LV 영역 → 프리픽스 `mini_luckyball_`, 치환자 `{0}`)
- **지시(사용자)**: 공유 키를 아래와 같이 분리. `invite_me_too`(나도 친구 초대하기)는 5개 언어 신규 번역 필요.
  - `mini_luckyball_invite_me_too_line` = `LINE으로 친구한테 럭키볼 선물하기` (기존 값 이관 — 재번역 불필요)
  - `mini_luckyball_invite_me_too` = `나도 친구 초대하기` (**신규 번역 대상**)
- **검증 대상 파일**: `xlt/xlt_output_LV_20260727.xlsx` (properties **74키**)

---

## (1a) 번역 전 — 한국어 원문 교정

| Figma 원문(63411-36558) | 교정 후(XLT Key 값) | 판정 |
|---|---|---|
| `나도 친구 초대하기` | `나도 친구 초대하기` | 교정 불요 — 맞춤법·띄어쓰기 정상, 보조사 `도` 용법 정상, 버튼 라벨이라 마침표 없음 |

- 34291 프레임의 토스트 원문 `초대 링크가 복사 되었어요! `(띄어쓰기·후행 공백)는 이전 run에서 `초대 링크가 복사되었어요!`로 교정된 **기존 alias가 유효**하여 그대로 유지했다(`복사되다`는 한 단어). Figma 원본 수정은 미반영 상태.
- `invite_me_too_line`은 **키 이름만 바뀌고 값은 이관**이므로 이번 교정 대상 아님(직전 게이트 `gate_report_luckyball_gift_buttons.md`에서 교정·검증 완료).

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py` 실제 실행

```
파일: xlt/xlt_output_LV_20260727.xlsx (74키)
🔴 P0 (Critical) : 0건
🟡 P1 (Medium)   : 111건 (전부 [용어 불일치])
🟢 P2 (Low)      : 26건  (전부 [마침표 스타일])
```

**직전 상태(73키) 대비**: P0 `0`→`0` / P1 `111`→`111` / P2 `26`→`26` — **신규 키로 인한 증가 0건**. **P0 = 0, 통과 기준 충족.**

---

## (b) 수동 3단계 체크표 (md/check.md) — 신규 키 전 컬럼

> 부분 작업이므로 체크표는 **신규 키 1건**을 대상으로 하되, 자동 검증기는 **엑셀 전체 행(74키) 전수**로 실행했고, 아래 (c)에서 **전체 파일**의 P1/P2 전건 처리 판정을 남긴다. 키 이름만 바뀐 `invite_me_too_line`은 값 이관이라 직전 게이트의 전 컬럼 검토 결과가 그대로 유효하다.

### 1단계: 한국어 맞춤법·띄어쓰기

| 키 | ko_KR | 판정 | 근거 |
|---|---|---|---|
| `invite_me_too` | 나도 친구 초대하기 | ✅ 정확 | 붙여쓰기 오류·`\xa0` 없음, 버튼 라벨 마침표 없음(정책 일치) |

### 2단계: 용어집 위반 (라이브 v3.5 · 107 terms — 이번 run에서 새로 조회)

| 용어 | 등재값 | 적용 | 판정 |
|---|---|---|---|
| 초대 / 친구 | **미등재** | 기존 키(`invite_banner`) 표기와 정렬 — ja `招待`·`友だち`, zh `邀請`·`好友`, th `ชวนเพื่อน`, en `Invite`·`friend` | ⚠️ (d-1) 보완 권장 |

### 3단계: 다른 언어 자연스러움·표기

| 언어 | 번역 | 판정 | 근거 |
|---|---|---|---|
| ja_JP | 私も友だちを招待する | ✅ | `私も`로 ‘나도’ 의미 보존, `友だち` 표기 정본, 한국어 조사 잔존 없음 |
| en_US | Invite a friend too | ✅ | `too`로 ‘나도’ 반영, 관사 정상, 기존 `Invite a friend and…`(invite_banner)와 일관 |
| th_TH | ฉันก็ชวนเพื่อนด้วย | ✅ | `ฉันก็…ด้วย`(나도 ~도) 구문 정확, 동사 `ชวนเพื่อน`은 기존 `invite_banner`와 동일 |
| zh_TW | 我也要邀請好友 | ✅ | `我也要`로 ‘나도’ 반영, 번체 정자(`邀請`·`好友`), 간체·이체자 없음 |

- 컬럼 정렬 검증(check.md 4-1): 5개 컬럼에 각 언어가 정확히 입력됨 — 회전 어긋남·swap 없음. 이질 문자체계 검출 0건.
- **의미 검증**: 이 버튼은 피초대자가 리워드 수령 후 “나도 초대자가 되어 친구를 초대”하는 동선(63411-36558)이라, 5개 언어 모두 **‘나도(also/私も/我也/ฉันก็)’ 뉘앙스를 명시적으로 보존**했다.

---

## (c) 검증기 보고 전건 처리 판정 (P0/P1/P2 — 전체 파일 74키 기준)

| 심각도 | 건수 | 처리 판정 |
|---|---|---|
| P0 | 0 | 해당 없음 |
| P1 | 111 | **전건 오탐(정상 의역)** — [용어 불일치] 유형. 내역: 당첨 25 · 최대 12 · 확인 10 · 완료 10 · 뽑기 9 · 가입 7 · 리워드 지급 5 · 공식 계정 4 · 종료 4 · 미션 3 · 복사 3 · 송금 3 · 변경 3 · 유의사항 2 · 가격 2 · 예치 2 · 플레이 2 · 회원가입 2 · 로그인 1 · 출금 1 · 혜택 1. 검증기가 용어집 **대표 표기 1개**하고만 문자열 비교하므로 문맥 활용형·대소문자·버튼 관용역을 모두 위반으로 집계한다. **신규 키 2건(`invite_me_too`·`invite_me_too_line`) 관련 P1은 0건.** |
| P2 | 26 | **전건 오탐/정책 유지** — [마침표 스타일]. 캠페인 정책(설명문=마침표, 버튼·타이틀=마침표 없음)에 일치. 신규 키는 버튼 라벨로 마침표 없음 → 검출 대상 아님. |

**통과 판정: P0 = 0 (자동·수동 모두) → 위키 반영·엑셀 재첨부 진행.**

---

## (d) 추가 개선·제안 (권장 — 사용자 결정 사안, 임의 적용 금지)

### (d-1) 용어집 보완 권장 (Landpress CMS — `md/landpress.md`)

직전 리포트에서 제안한 **링크 / 선물하기 / 친구 / 초대** 4종이 여전히 미등재입니다. 이번 신규 키(`나도 친구 초대하기`)도 `친구`·`초대`를 쓰므로 등재 시 표기 일관성이 강해집니다.

| 한국어 | en_US | ja_JP | zh_TW | th_TH |
|---|---|---|---|---|
| 링크 | link | リンク | 連結 | ลิงก์ |
| 선물하기 | gift | プレゼント | 送禮 | มอบของขวัญ |
| 친구 | friend | 友だち | 好友 | เพื่อน |
| 초대 | invite | 招待 | 邀請 | คำเชิญ |

### (d-2) 그 밖의 확인 권장

1. **`mini_luckyball_copy_invite_link` 고아 키화** — 34291 No.3의 문구가 `LINE으로 친구한테 럭키볼 선물하기`로 바뀌면서 이 키(`선물받을 친구의 초대 링크 복사`)는 **어느 화면 정의에서도 쓰이지 않게 됐습니다**. 2026-07-27의 “화면 정의에 없는 orphan 키는 번역표에서 삭제” 결정에 해당하지만, 이번 지시에 없어 **삭제하지 않고 남겨 뒀습니다**(전역 표·엑셀 모두). 삭제 여부를 알려주세요.
2. **`mini_luckyball_interest_title` 키 중복(값 다름)** — 직전 리포트에서 보고한 그대로 미해결(전역 표 2행, `자산보전!` vs `자산 보전!` 등). check.md “key_id 중복(값 다름) — 시스템 충돌 위험” 패턴. 정본을 지정해 주시면 한 행으로 정리합니다.
3. **34291 Figma 원문 오타** — 토스트 `초대 링크가 복사 되었어요! `(불필요 띄어쓰기 + 후행 공백)는 XLT에서 교정본을 쓰고 있습니다. 화면 밖(스크롤 영역) 텍스트에도 `가입(지갑 생성 포)`, `정보가변경된` 등 기존 오타가 남아 있어 디자이너 정리가 필요합니다(번역에는 미반영 — 해당 문구는 XLT 대상 아님).
4. **`친구한테` vs `친구에게` 표기 정책** — 여전히 미결. 캠페인 전반이 `친구한테`로 통일돼 있습니다.

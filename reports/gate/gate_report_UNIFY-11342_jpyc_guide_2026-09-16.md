# 게이트 리포트 — UNIFY-11342 [Design QA] JPYC 가이드_XLT (2026-09-16)

- 대상 키 **3건**(Unifi · UIT `UF_`) × 5개 언어 — 요청은 **ko·ja 줄바꿈**(+ ko 쉼표 1건)
- 근거: Jira UNIFY-11342 첨부 + Figma `eGDZjjZmHRWkPd4UVXAmeA` `1:167`(JPYC 가이드_xlt) **원본 재조회**
- 검증 대상 파일: `xlt/xlt_output_20260916175935.xlsx` — **전체 행(3행) 전수 점검**
- en_US·th_TH·zh_TW는 요청이 없어 **기존 등록값 그대로**(이미 개행 보유)

| # | 키 | ko 변경 | ja 변경 |
|---|---|---|---|
| 1 | `UF_jpyc_guide_safe_desc` | `법적 기준에 따라 관리되는` + 개행 + `담보 자산이에요.` (개행 신규) | `法的基準に基づいて管理される` + 개행 + `担保資産です。` (개행 신규) |
| 2 | `UF_jpyc_guide_exchange_desc` | 개행 위치 이동 — `1 JPYC당 1엔 기준으로` / `실제 엔화로 교환 및 출금이 가능해요.` | 개행 위치 이동 — `1 JPYC当り1円のレートで、` / `実際の日本円への換金および出金が可能です。` |
| 3 | `UF_jpyc_guide_benefit_desc` | **쉼표 제거** + 개행 이동 — `엔화 가치는 그대로` / `보내기는 더 빠르게 가능해요.` | 개행 신규 + **마침표 추가** — `円の価値はそのまま、` / `よりスピーディーに送金できます。` |

## (1a) 한국어 원문 교정 · alias

- ⚠️ **이전 교정과의 충돌 1건 — 사용자 결정으로 해소**: `UF_jpyc_guide_benefit_desc`는 위키 「한국어 원문 교정」 표에 **`엔화 가치는 그대로 보내기는` → `엔화 가치는 그대로, 보내기는`(사유: 쉼표 누락·중의적)** 으로 기록돼 있다. 이번 Design QA TO-BE는 그 쉼표를 뺀다. **2026-09-16 사용자 결정: 요청대로 쉼표 제거**(개행이 쉼표 자리에 오므로 시각적 구분으로 중의성 해소). 위키 교정 표에 되돌림 사실을 명시한다.
- **alias 2건(Figma 주석 ↔ 등록값 표기 차이 — 등록값 유지)**:
  - `{Figma 1JPYC당 → XLT 1 JPYC당}` — 숫자+통화 한 칸 띄우기(기존 교정 유지, 이번에 되돌리지 않음)
  - `{Figma 法的基準に基づいて管理される 担保資産 → XLT 공백 없음}` — 주석의 공백은 표기 아티팩트
- Figma TO-BE 주석의 **개행 앞 공백**(`管理される \n` 등)은 아티팩트로 보고 **제거**했다.

## (a) 자동 검증 요약 — `validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | 5건 |
| 🟢 P2 | 0건 |

## (b) 수동 3단계 체크표 (전체 행 전수)

| 단계 | 점검 | 결과 |
|---|---|---|
| 1단계 (한국어) | 맞춤법·띄어쓰기(`1 JPYC당` 유지)·쉼표(사용자 결정으로 제거)·개행 위치가 어절 경계 | ✅ 이상 없음 |
| 2단계 (용어집) | `JPYC`·`담보 자산`·`교환`·`보내기` 표기 | ⚠️ P1 5건 — 아래 (c) 판정 |
| 3단계 (다국어) | 변수 없음(0개) · 언어 혼입 없음 · 개행은 ko·ja만 변경, en·th·zh는 **기존 개행 그대로 보존** · ja 마침표 3건 통일(`です。`·`可能です。`·`できます。`) | ✅ 이상 없음 |

- 개행은 **실제 개행 문자**(선례 실개행 214키 / 리터럴 33키).
- 형제 키 `UF_jpyc_guide_card_3_desc2`(`엔화 가치는 그대로 보내기는 더 빠르게`)는 **다른 화면 키로 이번 요청 범위 밖** — 값 변경 시 다른 사용처 확인 규칙에 따라 점검했고 건드리지 않았다.

## (c) 검증기 보고 각 건 처리 판정

| # | 심각도 | 내용 | 처리 판정 |
|---|---|---|---|
| 1~4 | P1 | `UF_jpyc_guide_exchange_desc` — ko 「교환」에 대해 en `swap` · ja `交換` · th `แลกเปลี่ยน` · zh `交換` 권장 | **오탐(문맥 불일치)** — 이 문장은 「법정통화로의 **환금**」이라 등록값 ja `換金`·en `exchange`·zh `兌換`가 정확하다. 실측상 ko 「교환」 58키 중 ja `交換` 48 / `換金` 2, zh `兌換` 47 / `交換` 11로 **zh는 현재 값이 다수 관례**다. 이번 요청 범위(줄바꿈)도 아니라 **변경하지 않음** |
| 5 | P1 | `UF_jpyc_guide_benefit_desc` — ja 「보내기」에 `出金` 권장(등록값 `送金`) | **정책 보류(사용자 확인 대상)** — 실측 ko 「보내기」 43키 중 ja `出金` **29** / `送金` 3 / `送る` 6으로 관례는 `出金`이나, 이 문장은 **송금 속도**를 말해 `送金`이 의미상 맞다. 이번 요청 범위 밖이라 유지하고 (d)에 올린다 |

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장 → **대기 목록 등재**
- **「보내기」**(대기 #8에 이미 등재) — **실측치 기입**: ja `出金` 29 / `送金` 3 / `送る` 6 (ko 「보내기」 포함 43키). **문맥 분기 기준**(계좌 출금=`出金` / 송금 행위=`送金`)이 필요하다.
- **「교환」**(신규 등재) — ja `交換` 48 / `換金` 2 · zh `兌換` 47 / `交換` 11. **법정통화 환금 문맥은 `換金`·`兌換`** 이라는 분기 기준이 없어 검증기가 정상 의역을 반복 오탐한다.

### (d-2) 추가 개선·제안 안내
1. **쉼표 제거 결정의 영향** — 개행을 무시하고 한 줄로 렌더하는 경로(알림·OA·로그 등)에서는 「엔화 가치는 그대로 보내기는」이 다시 중의적으로 읽힌다. 해당 경로에서 이 키를 쓰면 확인이 필요하다.
2. **ja 마침표 추가** — `UF_jpyc_guide_benefit_desc` ja는 등록값에 마침표가 없었고 이번에 추가했다(Figma TO-BE 반영 · 형제 2건과 통일).
3. **en·th·zh 미요청** — 세 언어는 이미 개행을 갖고 있고 요청이 없어 유지했다. 개행 위치가 ko·ja와 달라도 언어별 어순 차이라 정상이다.
4. **이 화면은 「줄바꿈을 값에 넣는」 계열이다** — 2026-09-13 확정(`UF_clinic_detail_*`은 값에 넣지 않음)과 방침이 갈린다. 화면별로 갈린 상태이므로 정본 정리가 필요하다(UNIFY-11336 리포트와 동일 지적).

## 통과 판정

- **P0 = 0건**(자동 + 수동) → 엑셀 산출·위키 반영 진행
- P1 5건은 위 (c)의 판정대로 처리(오탐 4 · 정책 보류 1) · (d)는 사용자 결정 전까지 임의 반영하지 않음

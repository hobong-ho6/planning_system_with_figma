# 번역 품질 게이트 리포트 — Kaia Wallet > Unifi 전환 유도 (8프레임 재편, 37키)

- **날짜**: 2026-08-24
- **위키**: `4515188069` [Kaia Wallet > Unifi 전환 유도] — 라이브 v17 (핸드오프 기재 v12 → 사용자가 Screen 표를 비운 상태)
- **Figma**: `GOCHAYBS7hIrmWRGNuJOKV` 섹션 `68082:21171` (화면정의) — **8프레임**
- **산출 엑셀**: `xlt/xlt_output_kaiawallet_unifi_transition_20260824.xlsx` (**37키**)
- **담당 FE / 작명**: Kaia Wallet 기존 규칙 (`{surface}_{section}_{element}_{type}`, UIT/LV 프리픽스 미적용 — 2026-08-19 사용자 확정 유지). 변수 치환자 없음
- **레지스트리 대조**: Kaia Wallet / WEB BROWSER / **v1.4.3 366키** 실측 조회(2026-08-24 18:12)

## 0. 작업 성격 — 화면 재편에 따른 키 정리

사용자 지시: ⓐ 이전 스크린은 변경돼 전부 삭제됨 ⓑ **문구가 같은 것은 기존 키·번역을 그대로 재사용** ⓒ **화면 기준으로 더 이상 쓰이지 않는 키는 표·엑셀에서 제거**.

| 구분 | 키 수 | 처리 |
|---|---:|---|
| 유지 (문구 동일) | **11** | 기존 키 + 기존 5개 언어 번역 **그대로** (재번역 없음) |
| 갱신 (슬롯 동일·문구 리라이팅) | **9** | 기존 키 이름 유지 + ko 문구·5개 언어 **재번역** (사용자 선택 A안) |
| 신규 | **17** | 신규 키 + 5개 언어 신규 번역 |
| **제거** | **14** | 화면에서 사라진 문구 — 위키 표·엑셀에서 삭제 |
| 합계 | **37** | (직전 34키 → 37키) |

**제거 14키**: `wallet_sns_desc` · `wallet_mnemonic_desc` · `signup_title` · `signup_desc` · `signup_btn` · `mainwallet_badge` · `mainwallet_set_btn` · `guide_step3_title` · `guide_nowallet_step1_title` · `guide_nowallet_step1_desc` · `guide_nowallet_step2_title` · `guide_nowallet_step2_desc` · `guide_nowallet_step3_title` · `guide_nowallet_step3_desc` (전부 `main_unifi_transition_` 프리픽스)

> `guide_step3_title`은 문구가 `Unifi 바로가기`로 바뀌어 기존 `link_btn`과 동일해졌다 → **같은 문구=같은 키 재사용** 원칙에 따라 키를 신설하지 않고 `link_btn`을 쓰고 이 키는 제거했다.

**제거 판정 근거 (과대·과소 제거 방지)**: 팝업 프레임(`전송완료 팝업`·`자산전송 팝업`)의 **배경은 구버전 페이지**라 사라진 문구가 노드로 남아 있다(`link_title`·`transfer_title`·`guide_step1_title` 등). 프레임 텍스트 노드 존재만으로 판정하면 제거 대상을 놓친다 → **정본 화면은 `Unifi 연결 전`(1442px)·`Unifi 연결 후`(1471px) 전체 페이지 2종**으로 잡고, 8프레임 어노테이션 이미지를 **육안 확인**해 최종 판정했다.

## 1a. 한국어 원문 교정 (번역 전 필수 — Figma 원문 → XLT Key alias)

| # | Figma 원문 | XLT ko_KR (교정본) | 사유 |
|---|---|---|---|
| 1 | `대표지갑으로 자산을 전송할까요?` | `대표 지갑으로 자산을 전송할까요?` | 2026-08-19 확정 표기 `대표 지갑`(띄어쓰기) |
| 2 | `Unifi는 1개의 SNS 대표지갑만 지원해요. / 다른 지갑에 남은 자산을 대표지갑으로 / 옮깁니다.` | `대표 지갑` ×2 교정 | 동일 |
| 3 | `지금바로 Unifi에서…` | `지금 바로 Unifi에서…` | 부사 띄어쓰기 |
| 4 | `Kaia wallet으로 이동하세요.` | `Kaia Wallet으로 이동하세요.` | 브랜드 표기(대문자 W) |
| 5 | `Kaia wallet에서 자산 전송을 진행해주세요.` | `Kaia Wallet에서…` | 동일 |
| 6 | `Kaia wallet으로 이동` (버튼) | `Kaia Wallet으로 이동` | 동일 |
| 7 | `전송중` | `전송 중` | 직전 run 교정본 유지(`transfer_btn_ing`) |
| 8 | `…이자를 경험해⏎보세요!` | `…이자를 경험해 보세요!` | 어절을 쪼개는 레이아웃 개행 제거(의미 단위 개행은 유지) |
| 9 | `…코인만 합산되어 표시됩니다` (연결 후) / `…코인에 한해 합산되어` (연결 전) | `코인에 한해 합산되어` | 프레임 간 표기 불일치 → 최신 화면(연결 전) 기준. 의미 동일하므로 기존 5개 언어 번역 유지 |

**alias 기록** — 위 9건은 Figma 원문과 XLT 값이 다르다. 원문 수정 요청은 §4-(d-2)에 정리했다.

## 1b. 코멘트→텍스트 매칭 교정 (좌표 매칭 오류 3건)

`collect_frames.py`의 좌표 기반 매칭이 **더미 텍스트를 잡은 건**을 어노테이션 이미지 육안 확인으로 교정했다.

| 프레임 | 핀 | 자동 매칭 | 실제 대상 (교정) |
|---|:--:|---|---|
| 전송 버튼 상태 | ① | `cypress` (네트워크 값) | `Unifi 지갑으로 자산 전송` |
| Unifi 연결 전 | ③ | `cypress` | `내 자산 그대로, / 안전하고 간편하게 Unifi 연결` |
| 전송완료 팝업 | ③ | `KLAY` (코인 심볼) | `Unifi로의 자산 송금이 완료되었습니다. …` |

`Unifi 연결 전` 핀②가 가리키는 `Unifi`(브랜드 로고 텍스트)는 **직전 run 관행대로 키 미부여**(5개 언어 동일 표기).

## 2. 자동 검증 (`scripts/validate_translation.py` 실제 실행)

```
python3 scripts/validate_translation.py xlt/xlt_output_kaiawallet_unifi_transition_20260824.xlsx scripts/glossary.json
✓ 엑셀 로드: 37개 항목 / ✓ 용어집 로드: 116개 용어 (v4.7)
1단계 한국어: P0 0건, P1 0건, P2 10건
2단계 용어집: 완료
3단계 다국어: 완료
```

| 심각도 | 건수 |
|---|---:|
| 🔴 **P0** | **0건** |
| 🟡 P1 | 24건 (전건 §3-B에서 판정) |
| 🟢 P2 | 10건 (전건 §3-C에서 판정) |

**A/B 실측**: 수동 검토 발견 5건을 교정하면서 P1 23 → 24건(+1). 늘어난 1건은 `guide_linked_step1_desc` zh를 `已連結` → `已連動`으로 **산출물 내 일관성**에 맞춘 대가다(용어집 등재값은 `連結`). P0는 0 불변.

## 3. 수동 검토 — **전수 점검 (전체 파일 37키 × 5개 언어 = 185셀)**

검토 범위를 신규·변경 키로 한정하지 않고 **유지 11키까지 전체 행**을 한 줄씩 봤다(직전 run에서 수동 검토 없이 들어왔을 수 있으므로).

### 3-A. 수동 3단계 체크표

| 단계 | 대상 | 결과 |
|---|---|---|
| **1단계** (한국어 원문) | 37키 ko_KR — 맞춤법·띄어쓰기·표기 일관성 | 교정 **9건** (§1a) · 잔여 P0 0 |
| **2단계** (용어집 대조) | 116용어 × 5개 언어 + **Kaia Wallet 레지스트리 366키 실측 대조** | 시스템 정본 충돌 **2키 교정**(아래) · 미등재 용어 3건 확인(§4-d-1) |
| **3단계** (다국어 의미·표기) | 185셀 전수 + 검사기 사각지대 보조 스캔(nbsp·이중공백·문자체계 오배치·`資産`/`ทรัพย์สิน`/`友達`) | **표기 혼용 5건 교정**(아래) · 보조 스캔 4건은 전건 오탐 |

**2단계에서 교정한 2키 — 용어집이 아니라 시스템 등록값을 정본으로 채택**

Kaia Wallet 등록값 실측: `main_balance_send_btn` ja `送信する`·zh `發送` / `send_asset_transaction_check_send_address` ja `送信元`·zh `來源地址`. 용어집 「보내기」 등재값(ja `出金`·zh `轉帳`)과 어긋난다. 트랜젝션 화면 2키는 **같은 키스페이스 형제 키와의 일관성**이 우선이므로 시스템 정본을 따랐다.

| 키 | 수정 전 | 수정 후 |
|---|---|---|
| `send_asset_transaction_check_agree_desc` | ja `資産送金の代行…` · en `Consent to asset transfer…` · th `…โอนสินทรัพย์แทน` · zh `…資產轉帳` | ja `資産送信の代行…` · en `Consent to sending assets…` · th `…ส่งสินทรัพย์แทน` · zh `…資產發送` |
| `send_asset_transaction_check_send_execute` | ja `送金の実行` · zh `執行轉出` | ja `送信の実行` · zh `執行發送` |

**3단계에서 교정한 5건 — 자동 검증기가 구조적으로 못 잡는 「같은 언어 안의 표기 혼용」**

| # | 키 | 언어 | 수정 전 → 후 | 정합 대상 |
|---|---|---|---|---|
| M1 | `transfer_title` | th | `ส่งสินทรัพย์…` → `โอนสินทรัพย์…` | `transfer_btn` = `โอน` |
| M2 | `guide_step2_desc` | th | `ก็ส่งสินทรัพย์…` → `ก็โอนสินทรัพย์…` | 동일 |
| M3 | `guide_step2_desc` | zh | `將資產傳送至…` → `將資產移轉至…` | `guide_title` = `移轉` |
| M4 | `guide_linked_step1_desc` | zh | `已連結的 Unifi 帳號` → `已連動的…` | `linked_step1_title` = `已連動` |
| M5 | `bridge_title` | en | `You’re`(U+2019) → `You're`(ASCII) | `hide_week_btn` = `Don't` |

### 3-B. P1 24건 — 각 건 처리 판정

| # | 키 | 지적 | 판정 |
|---|---|---|---|
| 1–3 | `guide_step2_desc` | en/ja/th 「완료」→Complete/完了/เสร็จ | **오탐** — ko 「로그인 완료 후」를 `After logging in`/`ログイン後`/`หลังเข้าสู่ระบบแล้ว`로 완료상을 문법으로 표현. 등재값 직역은 어색 |
| 4 | `guide_step2_desc` | en 「로그인」→log in | **오탐** — `logging in` 활용형 |
| 5–6 | `linked_title` | en/th 「완료」 | **오탐** — `Unifi wallet linked`·`…แล้ว` 상태 표현이 자연 |
| 7 | `linked_desc` | en 「로그인」 | **오탐** — `you logged in with` 활용형 |
| 8 | `guide_linked_step1_desc` | en 「연결」→connect | **오탐** — 이 화면군의 연동/연결은 `link`로 통일(`linked wallet`·`linked account`) |
| 9 | `guide_linked_step1_desc` | th 「연결」→เชื่อมโยง | **정책 판단(유지)** — 산출물 전체를 `เชื่อมต่อ`로 통일. 계정·지갑의 기술적 연결은 `เชื่อมต่อ`가 일반적 → §4-d-1 용어집 보완 권장 |
| 10 | `guide_linked_step1_desc` | zh 「연결」→連結 | **정책 판단(유지)** — zh는 `連動`으로 통일(M4). ko 원문의 연동/연결 혼용을 zh에서 하나로 수렴 |
| 11 | `guide_linked_step1_desc` | zh 「확인」→查看 | **오탐** — 「정확한지 확인」은 검증 의미 → `確認`이 정확. 등재값 `查看`는 조회 의미(기존 키에서 조회 맥락에 사용 중) |
| 12–15 | `popup_cancel_btn` | en/ja/th/zh 「다음」→Next/次へ/ต่อไป/下一步 | **오탐** — 「다음에 할래요」의 「다음」은 시간 표현(later). 부분 문자열 매칭. `Maybe later`·`後にする`·`ไว้คราวหลัง`·`下次再說` 정확 |
| 16 | `done_popup_desc` | en 「송금」→send | **오탐** — `have been sent` 활용형 |
| 17 | `done_popup_desc` | th 「송금」→ส่ง | **정책 판단(유지)** — 이 화면군의 전송/송금은 `โอน`으로 통일(M1·M2). 금융 이체 표준어 |
| 18 | `done_popup_desc` | en 「완료」 | **오탐** — 완료를 현재완료 시제로 표현 |
| 19–20 | `bridge_title` | en 「가입」·「완료」 | **오탐** — `You're signed up for Unifi!` 활용형·자연 표현 |
| 21–22 | `send_asset_transaction_check_send_execute` | ja 「보내기」→出金 · zh →轉帳 | **정책 판단(유지)** — §3-A 실측대로 Kaia Wallet 시스템 정본 `送信`·`發送` 채택. 용어집 등재값이 시스템 실사용과 어긋난 경우 → §4-d-1 |
| 23–24 | `wallet_imported_label` | ko_KR 칸에 한글 없음(`Imported wallet`) | **사용자 확인 필요(원문 유지)** — Figma 원문이 영어 라벨. **레지스트리 실측: ko가 영어인 키 20/366 존재**(`main_balance_ft_title`=`Balance` 등)로 관행상 허용. 한국어화 여부는 디자이너 결정 사안 → §4-d-2 |

### 3-C. P2 10건 — 각 건 처리 판정

10건 전부 **[마침표 스타일]** — 문장형 텍스트에 마침표가 있는 소수 스타일 지적 (`notice_coin`·`notice_nft`·`link_desc`·`transfer_desc`·`guide_step1_desc`·`guide_linked_step1_desc`·`transfer_popup_desc`·`done_popup_title`·`bridge_title`·`bridge_desc`).

→ **전건 정책 보류(원문 유지)**. 마침표·느낌표 정책이 확정되지 않았고 Figma 원문이 그렇게 쓰여 있다. 직전 run(2026-08-19)의 동일 판정과 일관되며, 임의 적용하지 않는다.

## 4. 추가 개선·제안 (권장 — 사용자 결정 전까지 미적용)

### (d-1) 용어집 보완 권장

| 용어 | 상태 | 권장 표기 (ko / en / ja / zh / th) | 근거 |
|---|---|---|---|
| **연동** | **미등재** | 연동 / link / 連携 / 連動 / เชื่อมต่อ | 이 화면군의 핵심 개념인데 미등재라 검사 대상 밖이다. ko 원문이 「연동/연결」을 혼용해 P1 오탐(#8·#9·#10)의 원인 |
| **전송** | **미등재** | 전송 / transfer / 送金 / 轉帳 / โอน | 「송금」만 등재돼 있어 「전송」이 매칭되지 않는다. 이 페이지 37키 중 12키가 「전송」을 쓴다 |
| **보내기** | **등재값이 시스템 실사용과 어긋남** | 현행 등재 ja `出金`·zh `轉帳` → **ja `送信`·zh `發送`** 권장 | Kaia Wallet v1.4.3 실측: `送信`/`發送` 사용, `出金`은 0건. 등재값 쪽이 실사용과 어긋난 경우(v4.7 「포이카츠」 교정과 동형) |

> 용어집은 **API 읽기 전용**이라 Claude가 직접 쓰지 못한다. 보완을 승인하시면 `md/landpress.md` 절차로 **전체 JSON을 산출**해 드리고, 사용자가 Landpress CMS(`web3_xlt_json`)에 붙여넣어 반영한다. 버전이 오르면 같은 작업에서 **기획자 가이드 zip도 필수 동반 갱신**한다(`md/landpress.md` §5-1).

### (d-2) Figma 원문 개선 요청 (디자이너 전달 권장)

| # | 프레임 | 현행 | 권장 |
|---|---|---|---|
| 1 | 자산전송 팝업 | `대표지갑` (3곳) | `대표 지갑` — 2026-08-19 확정 표기 |
| 2 | Unifi 연결 전 ↔ 연결 후 | `코인에 한해 합산` ↔ `코인만 합산` | 한쪽으로 통일 (같은 유의사항이 프레임별로 갈림) |
| 3 | Unifi 연결 전 ↔ 연결 후 | `연동된 Unifi 지갑` ↔ `연결된 Unifi 계정` | 「연동」으로 통일 |
| 4 | (EXB) 브릿지 | `Kaia wallet` (3곳) | `Kaia Wallet` — 브랜드 표기 |
| 5 | 전송완료 팝업 | `지금바로` | `지금 바로` |
| 6 | 전송 버튼 상태 | `전송중` | `전송 중` |
| 7 | 여러 프레임 | `연동해주세요`/`진행해주세요`/`옮겨주세요` ↔ `로그인해 주세요`/`확인해 주세요` | 보조용언 띄어쓰기 정책 통일 (현재 혼용) |
| 8 | 트랜젝션 화면 | `트랜젝션 확인` (프레임명·본문) | `트랜잭션` — 시스템 등록값 `wallet_request_sign_tx_title`이 `트랜잭션 확인` |
| 9 | 전송 버튼 상태 | `Imported wallet` (영어 라벨) | 한국어 표기 필요 여부 확인 (`가져온 지갑` 등) — 시스템에 ko=영어 선례 20건이 있어 유지도 가능 |

### (d-3) 기존 번역 일관성 (유지 11키 — 사용자 지시로 원문 유지)

- `transfer_btn_retry` ja `再送信` — 같은 계열 `transfer_btn`/`_ing`/`_done`은 `送金`을 쓴다. `再送金`이 일관되지만 **「같은 것은 그대로」 지시에 따라 기존 번역 유지**. 다음 개정 때 함께 정리 권장.

### (d-4) XLT 시스템 업로드

산출 엑셀(37키)은 **업로드하지 않으면 시스템에 반영되지 않는다**. 직전 34키도 레지스트리 v1.4.3에 없다(미업로드 확인). 업로드는 사용자가 수행하며, 대상은 `Kaia Wallet` / `WEB BROWSER`.

## 5. 통과 판정

- **P0 = 0건** (자동 + 수동 전수 모두) → 위키 반영·엑셀 생성 진행 가능
- P1 24건 · P2 10건 — **전건 처리 판정 완료**(§3-B·§3-C). 정책 판단 5건과 사용자 확인 2건은 위 (d) 섹션에 정리
- 검사기: `python3 scripts/check_gate_report.py reports/gate/gate_report_kaiawallet_unifi_transition_8frames_2026-08-24.md`

# 게이트 리포트 — My › 바우처 상세(사용가능) 신규 키 3건 (5개 언어)

- 대상 프레임: Figma `GOCHAYBS7hIrmWRGNuJOKV` · **바우처 상세** `72193-11353` (위키 행 「바우처 상세 - 사용가능」)
- 위키: [pageId 4704515582](https://wiki.workers-hub.com/display/UNIFI/Unifi+mini+v2.0) · **My - UIT** 섹션
- 담당 FE: **UIT** — 프리픽스 `UF_` · 치환자 `{{0}}` (위키에 UIT/LV 구분이 있어 그대로 적용, 사용자 재질문 없음)
- 신규 **3키** × 5개 언어 = **15셀** · 기존 9키는 등록값 그대로 재사용(재번역 0건)
- 조달: Figma REST 신규 조회 · 코멘트 신규 조회(`fetch_comments.py`, reply-aware) · XLT 등록값 `Unifi / WEB BROWSER v1.7.9` 신규 조회 · 용어집 API **v5.5** 신규 조회 (전부 캐시 미사용)

---

## (1a) 번역 전 — 한국어 원문 교정

Figma 원문을 **용어집 정본과 형제 키에 맞춰 교정**했다.

| 항목 | Figma 원문 | XLT Key KR (교정 후) | 사유 |
|---|---|---|---|
| 안내 문구 | 점원에게 **모바일 상품권**으로<br>결제 요청해주세요 | 점원에게 `<span>`모바일 바우처`</span>`로 결제 요청해주세요 | 용어집 **v5.4** ko 정본 = `바우처`(`상품권` 아님) · 같은 화면 형제 8키가 전부 `바우처`로 등록돼 있다 |

- **alias (Figma 원문 → XLT Key KR)**: `모바일 상품권` → `모바일 바우처` · `상품권으로` → `바우처로`(받침 없음 → 조사 `으로`→`로`)
- ⚠️ **디자이너 원본 수정 요청 대상** — 이 프레임에는 `상품권 정보`·`상품권이 사진 앨범에 저장됐어요`·`상품권 이미지 저장`도 남아 있다. 위키·등록값은 이미 `바우처`로 통일돼 있어 **Figma만 구 표기**다.
- 줄바꿈: Figma 원문은 2줄(`모바일 상품권으로 ⏎ 결제 요청해주세요`)이나 **XLT 값은 1줄**로 둔다 — 같은 화면 형제 키와 동일 원칙(위키 2026-09-13 「줄바꿈은 화면에서 처리하고 XLT 값에 넣지 않는다」 확정).
- **고유명사 조사 대상 없음** — 브랜드·기관·법령명 미포함.

## (a) 자동 검증 — `validate_translation.py` P0/P1/P2 요약

용어집 v5.5(118 용어) 기준, 신규 1키 × 5개 언어.

| 심각도 | 건수 |
|---|---:|
| 🔴 **P0** | **0건** |
| 🟡 P1 | 2건 |
| 🟢 P2 | 0건 |

**P0 = 0 → 통과 기준 충족.**

## (b) 수동 3단계 체크표 — 5셀 **전수**

대상이 1키이므로 **전체 행 = 전수 점검**이다. 아울러 같은 프레임의 **기존 8키 × 5개 언어 = 40셀도 등록값 기준으로 전수 재확인**했다(변경 없음 확인).

| 언어 | 값 | 1단계 원문·표기 | 2단계 용어집 | 3단계 다국어 |
|---|---|---|---|---|
| ko_KR | 점원에게 `<span>`모바일 바우처`</span>`로 결제 요청해주세요 | ✅ 조사 `로` 정상(바우처 받침 없음) · 띄어쓰기 정상 | ✅ `바우처` 정본 | ✅ 기준 |
| ja_JP | 店員に`<span>`モバイルクーポン`</span>`での決済をお願いしてください | ✅ 자연스러운 경어 | ✅ 바우처=`クーポン` · 결제=`決済` | ✅ |
| en_US | Ask the store clerk to pay with your `<span>`mobile voucher`</span>` | ✅ 대소문자 정상 | ✅ 바우처=`voucher` | ✅ |
| th_TH | แจ้งพนักงานร้านเพื่อชำระเงินด้วย`<span>`เวาเชอร์บนมือถือ`</span>` | ✅ 표기 정상 | ✅ 바우처=`เวาเชอร์` | ✅ |
| zh_TW | 請告知店員使用`<span>`行動優惠券`</span>`付款 | ✅ 번체 정상(간체 혼입 0) | ✅ 바우처=`優惠券` · 결제=`付款` | ✅ |

**점검 항목별**: 외래어 음차 오염 없음 · 붙은 단어 없음 · 다의어 모호 없음 · 언어 혼입 없음 · 변수 치환자 없음(이 키는 변수 미사용) · **`<span>` 태그 5개 언어 모두 여닫이 쌍 정상**.

## (c) 검증기 보고 건별 처리 판정

| # | 심각도 | 내용 | 판정 |
|---|---|---|---|
| 1 | P1 | en_US `결제` → `Payment` 권장 | **오탐** — 등재값 `Payment`는 **명사 라벨**이고 이 문장은 동사(`to pay with`)다. 명사로 바꾸면 비문이 된다 |
| 2 | P1 | th_TH `결제` → `การชำระเงิน` 권장 | **오탐** — 등재값은 명사형(`การ` 접두), 문장은 동사구 `ชำระเงิน`가 정상 |
| — | (해소) | zh_TW `결제` → `付款` 권장 | **실제 위반 → 수정 완료.** 초안 `結帳`을 `付款`으로 교체했다. 같은 화면 형제 키 `UF_voucher_detail_payment_info_btn` zh(`查看付款資訊`)와도 일치한다 |

**P0 0건 · P1 2건 전부 오탐 · P2 0건.**

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

**(d-1) 용어집 보완 권장 — 없음.** `결제` 동사형 오탐은 등재값이 명사 라벨이라 생기는 구조적 한계로, 동사형을 추가 등재하면 라벨 문맥에서 역오탐이 난다(v5.5에서 `포인트`·`환불`·`할인`을 반려한 것과 같은 판단).

**(d-2) 추가 개선·제안**

| # | 내용 | 판단 주체 |
|---|---|---|
| 1 | **볼드 처리 규격 확정** — UIT 기존 관례를 등록값 2,549키에서 실측했다: **`<span>텍스트</span>`(여닫이 쌍) = 강조 구간** 7건 · **`<span />`(자기닫힘) = 값 치환 자리** 60건. 이 키는 강조라 여닫이 쌍을 썼다. ⚠️ 표기 흔들림 존재(`<span />` vs `<span/>`) — 신규 키는 **공백 없는 `<span/>`·여닫이 `<span>…</span>`** 로 통일 권장 | FE(UIT) · 기획 |
| 2 | 🔴 **Figma 원문 `상품권` → `바우처` 교정 요청** — 이 프레임에 `상품권 정보`·`상품권이 사진 앨범에 저장됐어요`·`상품권 이미지 저장`·`모바일 상품권` 4곳이 구 표기로 남아 있다. 위키·등록값은 `바우처`로 통일돼 있어 **매 재조회마다 alias 처리가 반복**된다 | 디자이너 |
| 3 | ⚠️ **동적 값에 `xlt` 마커 2건** — `2027-09-11 까지`(유효기간 값) · `10,000원`(사용 가능 금액 값)에 마커가 붙어 있으나 **둘 다 API 동적 값**이다. 접미사(`까지`·`원`)만 번역 대상인지, 마커가 잘못 붙은 것인지 확인이 필요하다. **키를 만들지 않고 보류**했다 | 기획 · 디자이너 |
| 4 | ⚠️ **위키 기재와 반대 방향의 변경** — 위키는 2026-09-11에 이 문구를 「GuideKim API로 받는 문구라 앱 XLT 대상이 아님 → 키 `UF_voucher_detail_guide` 제거」로 확정했는데, 이번 프레임에 **`xlt` 마커가 다시 붙었다**. 마커와 볼드 문의 답글을 근거로 **XLT 대상으로 되살려** 키를 재도입했다. GuideKim 응답과 이중 관리가 되지 않는지 확인 필요 | 기획 · FE |
| 5 | `결제 정보 보기`(`UF_voucher_detail_payment_info_btn`)는 여전히 Figma에 `xlt` 마커가 없다(코멘트는 `GuideKim으로 이동`만). 위키 2026-09-11 「Figma 마커 보완 권장」이 미해소 | 디자이너 |

## 통과 판정

**P0 = 0 (자동 + 수동 전수)** → 위키 반영·엑셀 생성으로 진행.

---

## 추가분 (2026-09-14 사용자 확정) — 동적 값 2키

(d-2) 3번으로 올렸던 「동적 값 마커 2건」은 **번역 대상으로 확정**됐다(사용자: 「동적값은 처리해야 해 — `{{0}} 까지`, `{{0}}원`으로 반영」). 아울러 (d-2) 2번 **Figma 원문 `상품권` 교정 요청은 종결**(사용자: 「피그마 원문은 무시」), (d-2) 4번 **안내 문구 XLT 편입도 확정**(사용자: 「가이드킴의 데이터로 유니파이에서 새로 제공」)됐다.

### 이름 선례 실측

| 후보 이름 | 선례 |
|---|---|
| `UF_voucher_detail_expiry_value` | 같은 네임스페이스에 **`UF_voucher_seller_value`** 1건(값 키 접미사 `_value`) · `_date` 접미사 8건 |
| `UF_voucher_detail_amount_value` | 〃 |

두 이름 모두 **등록값 2,549키에 없음**(충돌 0건) 확인.

### 형식 선례 실측 — `{{0}}` + 단위

`{{0}}개`(`UF_asset_token_list_unit`) · `{{0}}건` · `{{0}}일` · `{{0}} USDT`(`UF_dm_reward_amount`) 등 **접미사형 32건**이 관례다. `까지`는 `9월 1일 23:59 (UTC+0)까지` / ja `まで`(`UF_guide_unifi_jpyc_promotion_banner_desc`) 선례를 따랐다.

### 신규 2키 값

| 키 | ko_KR | ja_JP | en_US | th_TH | zh_TW |
|---|---|---|---|---|---|
| `UF_voucher_detail_expiry_value` | `{{0}} 까지` | `{{0}} まで` | `Until {{0}}` | `ถึง {{0}}` | `至 {{0}}` |
| `UF_voucher_detail_amount_value` | `{{0}}원` | `{{0}}ウォン` | `{{0}} KRW` | `{{0}} วอน` | `{{0}} 韓元` |

통화 단위는 **Unifi 바우처 마스터 실사용 표기**를 따랐다 — ja `ウォン`(`5万ウォン商品券`) · zh `韓元`(`禮券 5萬韓元`) · th `วอน`(`50,000 วอน`). en은 접미사 관례(`{{0}} USDT`)에 맞춰 `{{0}} KRW`.

### 자동 검증 (추가분)

| 심각도 | 건수 |
|---|---:|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

### 수동 3단계 (추가분 10셀 전수)

- **1단계** 한국어 원문: `{{0}} 까지`(의존명사 `까지` 앞 띄어쓰기 — Figma 원문 `2027-09-11 까지`와 동일) · `{{0}}원`(단위 붙여쓰기) — 이상 없음
- **2단계** 용어집: 해당 용어 없음(위반 0)
- **3단계** 다국어: **치환자 `{{0}}` 5개 언어 전부 1개씩 보존**(UIT 규격) · 언어 혼입 0 · 번체 정상

**P0 = 0 → 통과.**

# 게이트 리포트 — 표기 통일 5종 (「통일할 것들 다 통일」)

- **작업일**: 2026-09-03 (clinic 세션 #3 연장)
- **지시**: 사용자 「통일할 것들 다 통일하고 공통 FAQ json도 제공」
- **대상**: 위키 `4667512757` **v112 → v113** · LPC `4686692164` **v9 → v10** · `landpress/*_ko_KR.json` 5파일 · 통합 엑셀 86키(첨부 v3)
- **레지스트리**: Unifi / WEB BROWSER / v1.7.4 · **용어집** v4.7 (작업 시점 조회)

## 통일 5종 — 방향과 근거

| # | 통일 대상 | 방향 | 근거(실측) |
|---|---|---|---|
| 1 | `시력교정` ↔ `시력 교정` | → **`시력 교정`** | 표준 띄어쓰기(「시력」+「교정」 두 단어). 같은 배치의 `menu.title`이 이미 `시력 교정`이었다 |
| 2 | ko `후기` ↔ `리뷰` | → **`리뷰`** | **5개 언어가 전부 review 계열**(`レビュー`·`Reviews`·`รีวิว`·`評論`)이고 UI 라벨(`리뷰 ({{0}})`·`리뷰 {{0}}`)도 「리뷰」다. ko만 갈려 있었다(XLT 후기 5키 : 리뷰 3키) |
| 3 | `Mini App` ↔ `MINI App` | → **`MINI App`** | LINE 공식 브랜드 표기는 **`LINE MINI App`**. 등재값 `UF_voucher_mini_certified`가 5개 언어 전부 `MINI App` |
| 4 | `프리미엄 LASIK` · `프리미엄 리프트 PKG` | → **`프리미엄 라식`** · **`프리미엄 리프트 패키지`** | 같은 병원의 나머지 시술이 전부 한글(`렌즈삽입술`·`백내장`) · 약어 `PKG`는 한글 표기 |
| 5 | 병원명 띄어쓰기 | → **붙여쓰기**(`디에이성형외과`·`티아나성형외과`·`힐링안과`) | 상호는 고유명사이며 위키 「병원 데이터」 표·병원 공식 표기(daprs.com)가 붙여쓰기다. 위키에 `디에이성형외과` 6 : `디에이 성형외과` 3 · `티아나성형외과` 2 : `티아나 성형외과` 7 · `힐링안과` 5 : `힐링 안과` 4로 혼재했다 |

### 통일하지 않은 것 (근거 기록)

- **구분자 `･`(U+FF65) ↔ `·`(U+00B7)** — 2026-09-01 **사용자 결정으로 종결**된 항목(재제안 금지). 실등록값은 이미 `·`로 통일돼 있고 Figma만 `･`다.
- **시술 상품명 띄어쓰기** — `코성형`·`코재수술`·`남자코성형`·`렌즈삽입술`·`정밀검사`·`백내장`은 붙여쓰기 유지(의료 업계 표기 관행 · 병원 제공 상품명).
- **`라인 무료 상담`** — XLT 값은 이미 4키 전부 `LINE 무료 상담`이다. `라인`은 Figma 원문에만 남아 있어 **원본 수정 요청** 대상이며 위키 값 변경은 불필요.

---

## (1a) 한국어 원문 교정

이번 작업 자체가 **원문 표기 교정**이다. 위 5종 전건이 Figma 원문과 달라지므로 **alias 기록 + 디자이너 원본 수정 요청 대상**이다.

| alias | 대상 |
|---|---|
| `{시력교정 → 시력 교정}` | Figma `71241:9154`(브릿지 카테고리) · Web 브릿지 `69630:13293` y=1366 · 힐링안과 후기 요약 |
| `{후기 → 리뷰}` (ko) | Web 브릿지 헤더 y=134 · mini 브릿지 섹션 제목·더보기 버튼·요약 주석 · 상세 리뷰 출처 고지 |
| `{Mini App → MINI App}` | 롤링 배너 `71543:3307` 노드 `71417:16012`·`16033` |
| `{프리미엄 LASIK → 프리미엄 라식}` · `{프리미엄 리프트 PKG → 프리미엄 리프트 패키지}` | 위키 「병원 데이터」 표(병원 제공 상품명) — **병원 확인 권장** |
| 병원명 붙여쓰기 | Figma 브릿지 카드(`디에이 성형외과`·`티아나 성형외과`·`힐링 안과`) |

**치환 구간을 한정했다** — 위키 `[Screen ~ 병원 데이터)` ∪ `[다국어 ~ 끝)` 41곳. **History와 「법무 준수 체크」 섹션은 제외**했다(전역 치환은 과거 이력을 훼손하고, 법무 섹션은 사용자 작성 영역이다).

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py`

실행: `python3 scripts/validate_translation.py xlt/xlt_output_20260903233313.xlsx scripts/glossary.json`

| 심각도 | 통일 전 | **통일 후** | 차이 |
|---|---|---|---|
| 🔴 **P0** | 0건 | **0건** | 불변 |
| 🟡 P1 | 59건 | **59건** | **신규 0건** |
| 🟢 P2 | 9건 | **9건** | 불변 |

표기 통일이 **새로운 위반을 만들지 않았다**(A/B 실측).

---

## (b) 수동 3단계 체크표 — **전수 점검**

검토 범위: 통합 엑셀 **전체 86행 × 5개 언어 = 430셀** + `landpress/*_ko_KR.json` **전 파일**. 변경 대상에 한정하지 않고 전수 재점검했다.

### 1단계 — 기계적 정합

| 항목 | 결과 |
|---|---|
| 빈 셀 | ✅ 0건 (430/430) |
| 통일 잔존 스캔 (XLT ko) | ✅ `후기` **0건** · `시력교정` **0건** · `Mini App` **0건**(전부 `MINI App`) |
| 통일 잔존 스캔 (landpress ko 전 파일) | ✅ `후기`·`시력교정`·`프리미엄 LASIK`·`프리미엄 리프트 PKG` **0건** |
| 치환자 | ✅ 7키의 `{{N}}` 집합 5개 언어 일치 — 통일로 인한 손상 0 |
| 문자체계·nbsp·U+2028·이중공백·앞뒤공백 | ✅ 0건 |
| 태그 균형(위키 2페이지) | ✅ 15태그 전건 assert 통과 · `check_wiki_storage.py post` exit 0 |
| History 보존 | ✅ 치환 구간에서 제외 — 과거 이력의 `후기`·병원명 표기 그대로 |

### 2단계 — 의미 정합

| 키 | 통일 후 ko | 판정 |
|---|---|---|
| `UF_clinic_bridge_title` | `좋은 리뷰로 주목받는 한국 인기 클리닉` | ✅ ja `高評価で注目されている…`·en `with great reviews` 등 기존 번역과 의미 일치(번역 무변경) |
| `UF_clinic_mini_bridge_section_review` | `리뷰로 미리 보는 클리닉` | ✅ ja `レビューで先に見るクリニック`와 어휘 일치 |
| `UF_clinic_mini_bridge_review_more` | `리뷰 더보기` | ✅ ja `レビューをもっと見る`·zh `查看更多評論`와 일치 |
| `UF_clinic_mini_bridge_section_review_note` | `GuideKim에 등록된 리뷰를 바탕으로 요약한 내용입니다.` | ✅ 4개 언어 전부 review 계열 |
| `UF_clinic_detail_review_source` | `GuideKim에 등록된 실제 이용자 리뷰를 바탕으로 제공됩니다.` | ✅ 동일 |
| `UF_clinic_bridge_cat_vision` | `시력 교정 경험을 중요하게 본다면` | ✅ 번역 무변경(4개 언어에 「시력교정」 붙여쓰기 개념 없음) |
| `UF_clinic_mini_bridge_badge_certified_desc` | `MINI App` (5개 언어 동일) | ✅ 브랜드 표기 — 등재값과 일치 |

**4개 언어 번역은 한 셀도 바뀌지 않았다** — ko 표기만 정리했고 의미가 이미 일치했기 때문이다. 이것이 통일 방향(「리뷰」)을 고른 근거이기도 하다.

### 3단계 — 언어별 중점 점검

ko 표기 통일이므로 ja·en·th·zh는 무변경이다. 전수 재점검 결과 **변화 0건**(문자체계·경어·관사·성조·번체 전건 유지).

---

## (c) 자동 검증 보고 건별 처리 판정

P1 59 + P2 9 = 68건은 직전 리포트(`gate_report_clinic_consolidated_85keys_*` (c))의 판정을 그대로 승계한다 — **실제 위반 0 · 오탐 67 · 확인 완료 1**. 표기 통일로 **신규 지적 0건**이며, 통일 대상이던 항목 중 검증기가 지적한 것은 없었다(용어집에 「후기」·「시력교정」·「Mini App」 등재가 없다).

---

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장

| 용어 | 권장 | 근거 |
|---|---|---|
| **리뷰** | **신규 등재 권장** (`レビュー`·`Reviews`·`รีวิว`·`評論`) | 이번 통일로 ko가 「리뷰」로 정리됐고 4개 언어 실사용이 **4/4 일관**이다. 등재하면 이후 「후기」 재유입을 검증기가 잡는다 |
| **후기** | **`deprecated_terms`에 ko 「후기」→「리뷰」 추가 권장** | 용어집 `deprecated_terms`는 ko 자체의 구 표기를 잡는 영역이다(`월렛`→`지갑` 선례). 등재하면 재발을 예방한다 |

기존 권장 5건(혜택 en·더보기 ja/zh·결제 zh·교환 문맥)은 통합 리포트 (d-1)에 그대로 유효하다.
⛔ 용어집은 **API 읽기 전용** — 승인 시 `md/landpress.md` 절차로 전체 JSON 산출 + **기획자 가이드 zip 동반 갱신**(§5-1).

### (d-2) 추가 개선·제안 안내

1. 🔴 **`프리미엄 라식`·`프리미엄 리프트 패키지`는 병원 제공 상품명이다** — 위키 「병원 데이터」 표 원문이 `프리미엄 LASIK`·`프리미엄 리프트 PKG`였다. 표기 일관성으로 한글화했으나 **병원 공식 상품명 확인 권장**(다르면 원문으로 되돌린다).
2. **Figma 원본 수정 요청 누적** — 이번 5종 + 기존(`시술인증`·`리쥬란힐러`·`리프팅패키지`·`찾고있다면`·`라인 무료 상담`·`더보기 를`·`무료 LINE상담`·`還元特典`·`最大15%還元`·`편한 의사 소통`)까지 **누적 15건**이다. `figma-source-issues` 에이전트로 화면별 목록을 뽑아 한 번에 전달하는 것을 권장한다.
3. ⏳ **CMS 반영 대기 확대** — 이번 통일로 `cautions`·`faq`(공통)와 힐링안과 `bridge_info`·`menu`, 디에이 `menu`가 반영 대기다. 기존 4건과 합쳐 **총 5필드**(공통 2 + 병원별 3).
4. **`cautions` 다국어 4파일** — ko만 「리뷰」로 바뀌었고 ja·en·th·zh는 이미 review 계열이라 변경 불필요하다(실측 확인).

---

## (4) 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 = 0 | ✅ 0건 · 신규 P1/P2 0건(A/B) |
| 수동 P0 = 0 | ✅ 전수(86행 × 5언어 + landpress 전 파일) 0건 · 통일 잔존 0건 |
| (a)~(d) 리포트 | ✅ 본 문서 |
| 한국어 원문 교정 | ✅ (1a) — 통일 5종 alias 기록 + 치환 구간 한정 근거 |
| 전수 점검 | ✅ 전체 파일 기준 |

→ **위키 v113 · LPC v10 반영 완료** · 통합 엑셀 86키 **첨부 v3** 갱신. (d) 권장 4건은 사용자 결정 전까지 미반영.

# 게이트 리포트 — shopping_guide 다이소 5개 언어 (2026-09-12)

- **대상**: LV `shopping_guide.guide_page` · `uid = daiso` — 다이소 가이드 페이지 본문 전체
- **구성**: `title` · `description` · `cta` · `sections` 3개(항목 5 · 5 · 6 = 16) · `heroImageUrl` · `voucherSection`(다이소는 없음)
- **작업**: ① 미디어 라이브러리에서 이미지 **16개 매칭·반영** ② ko_KR 기준 **4개 언어 번역** ③ CMS API로 5개 언어 등록
- **검증 범위**: **전체 행 47건 전수 점검**(비번역 필드 `imageUrl`·`targetUrl`·`productIds` 제외). 이 필드의 번역 대상 전부다.
- **엑셀**: `xlt_output_20260912153238.xlsx`

---

## (1a) 한국어 원문 교정

교정 사항 **없음**. LV beta 실등록 ko_KR을 그대로 기준으로 삼았고 alias도 없다.

| 확인 항목 | 판정 |
|---|---|
| 맞춤법·띄어쓰기 | 정상 |
| 줄바꿈 `\n` 위치 | 상품명 9곳 · 제목 1곳 — Figma 레이아웃과 일치 |
| 금액 표기 `₩10,000` / `₩9,500` | 원문 유지(통화 기호 포함) |
| `heroImageUrl` 빈 문자열 | **정상** — Figma `73137-8967` 실측 결과 다이소 가이드에는 히어로 이미지가 **없다**(CU `72959-1214`에는 300×200 히어로 이미지 있음) |

---

## (a) 자동 검증 결과

| 심각도 | 1차 | **최종** |
|---|---|---|
| 🔴 P0 | 0건 | **0건** |
| 🟡 P1 | 17건 | 13건 |
| 🟢 P2 | 2건 | 2건 |

**통과 기준 P0 = 0 충족.**

---

## (b) 수동 3단계 체크표 — 전체 행 47건 전수

### 1단계 · 한국어(ko_KR)

| 영역 | 행 | 판정 |
|---|---|---|
| `title` · `description` · `cta`(badge·label) | 4 | 정확 |
| `sections[0]` K-뷰티 (제목·주의·요약 2 + 항목 5×2) | 14 | 정확 |
| `sections[1]` 국민템 (제목·요약 2 + 항목 5×2) | 13 | 정확 — 이 섹션만 `caution` 없음(원문 그대로) |
| `sections[2]` K-디저트 (제목·주의·요약 2 + 항목 6×2) | 16 | 정확 |

붙은 띄어쓰기·외래어 음차 오염·다의어 모호 **없음**.

### 2단계 · 용어집(v5.4) 대조

| 용어 | 정본 | 적용 | 판정 |
|---|---|---|---|
| 최대 | ja `最大` · th `สูงสุด` · zh `最多` | ja·th 정본 적용 / en `Up to` · zh `最高` | (c) 오탐 — 할인율 문맥 |
| 구매 · 구매하기 | ja `購入`·`購入する` · zh `購買` | ja `購入` · zh `購買` 적용 | 정확 |
| 쇼핑 | ja `ショッピング` · zh `購物` | 의역 문장에 흡수 | (c) 오탐 |
| 인기 | zh `人氣` | zh `受歡迎`·`人氣` 혼용 | (c) 오탐 |
| 상품명·브랜드 | 미등재 | **브랜드는 원형 유지**(VT · HBAF · Orion · Sempio · Son & Park · Moojik Tiger), 제품 종류어만 현지어 | (d-1) |

### 3단계 · 언어별 점검

| 언어 | 점검 | 판정 |
|---|---|---|
| ja_JP | 카타카나 표기 일관(マデカ21 · ソンアンドパク · コブックチップ) · 전각 괄호 `（缶詰）` 사용 · 중점 정상 | 정확 |
| en_US | 브랜드 원형 · 수량 표기 `2 pcs` 일관 · 문장부호 일관 | 정확 |
| th_TH | **1차 지적 반영** — s1 상품명 4건이 로마자뿐이라 다른 섹션과 어긋나, 제품 종류어를 태국어로 교체(`แผ่นแปะดูแลสิว`·`คัลเลอร์บาล์ม`·`เซรั่มบูสต์ผิวหน้า`·`มาส์กชีทผิวกระจ่างใส`). 브랜드명은 로마자 유지 | **수정 완료** |
| zh_TW | **번체** 유지(`優惠`·`護照套`·`砂鍋`·`罐頭`) · 간체 0 · 숫자 앞뒤 공백 일관 | 정확 |
| 전 언어 | 줄바꿈 `\n` 위치 ko와 동일 · `imageUrl` 16개 5개 언어 모두 동일 · 금액 `₩` 표기 유지 | 정확 |

---

## (c) 건별 처리 판정

### P0 — 0건

### P1 (1차 17건 → 최종 13건)

| 유형 | 건수 | 판정 |
|---|---|---|
| th s1 상품명 「번역 누락 의심」 | 4 | **실제 일관성 문제 · 수정 완료**(위 3단계) |
| en/zh `최대` → `Max`/`最多` | 2 | **오탐** — 할인율에는 `Up to`·`最高`가 표준 |
| en/ja `구매하기` → `Buy now`/`購入する` | 2 | **오탐** — CTA 문장형(`Buy a DAISO ₩10,000 voucher`·`商品券を購入`)이 자연스럽다 |
| en `구매` → `Buy` (요약 3건) | 3 | **오탐** — `About ₩10,000 for all of them` 의역 |
| en/th/zh `쇼핑` → 정본 | 3 | **오탐** — `covers all your souvenirs`·`ช้อปของฝาก`·`一次買齊` 의역 |
| en/zh `인기` → `popular`/`人氣` | 2 | **오탐** — `The current favorite`·`最受歡迎` 의역 |

### P2 2건

`caution` 2건의 마침표 지적 — **오탐**. 주의 문구는 완결 문장이라 마침표가 정상이며 5개 언어 모두 동일 처리.

---

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

### (d-1) 용어집 보완

`할인` · `최대`(할인율 문맥의 zh `最高`) · `상품권`이 미등재이거나 문맥 표기가 부족해 **반복 오탐**을 낸다. 사용자 지시대로 전체 번역 완료 후 일괄 등재하며, 권장 표기는 실사용 실측 후 제출한다.

### (d-2) 추가 개선 제안

1. **`cta.targetUrl` 미정** — 「다이소 1만원권 구매하기」 버튼의 이동 URL이 빈 문자열이다
2. **`heroImageUrl`** — 다이소는 원래 없다(확인 완료). **CU는 Figma에 히어로 이미지가 있으므로** CU 작업 시 채워야 한다
3. **미사용 이미지 2개** — `daiso_guide_s1_5_팩미인…`·`daiso_guide_s2_7_팩미인…`이 서로 다른 파일로 중복 업로드돼 있고 등록 항목에는 없는 상품이다
4. **파일명 섹션 번호 불일치** — 업로드 파일의 `s2`↔`s3`가 등록 섹션과 뒤바뀌어 있다. 이번에는 상품명으로 매칭했으나 **파일명 정리 권장**
5. **primary locale이 `en_US`** — `shopping_guide` 스키마의 primary가 en_US라 `?locale=` 없이 조회하면 비어 보인다

---

## 결론

- **P0 = 0건**(자동 + 수동) → 산출물 출력·등록 가능
- 실제 문제 **4건(th 상품명 일관성)** 수정 완료, 나머지 P1 13건 · P2 2건은 오탐
- (d) 권장 사항은 사용자 결정 전까지 미적용

# 번역 품질 게이트 리포트 — `mini_voucher_detail_favorite_toast` ja 단축

- **요청**: Slack `C07R7CE0B5H` 스레드 `1789638317.110639` / 메시지 `1789638623.601009`(2026-09-17 · 구지원 UI) — 「즐겨찾기 클릭 시 토스트의 **JP 문구가 길어 화살표 아이콘이 가려진다** → 길이 조정 가능한지」
- **대상 키 1건** · 변경 언어 **ja_JP만** · 서비스 **Dapp Portal**(⛔ Unifi 아님) · 팀 **LV**(`mini_` · 치환자 없음)
- 엑셀: `xlt/xlt_favorite_toast_ja_short_dappportal_2026-09-17.xlsx`
- 원본 조회: 2026-09-17 19:13 XLT 레지스트리(Dapp Portal v2.6.1 · 1,703키) · 위키 v246 · 용어집 **v5.6**(119 terms) **전부 당일 새로 조회**(캐시 미사용)

## 변경 내용

| 언어 | 값 | 길이 | 비고 |
|---|---|---|---|
| ko_KR | `즐겨찾기 완료! 목록에서 확인해보세요` | 20자 | **변경 없음** |
| **ja_JP** | ~~`お気に入りに追加しました！リストで確認してみてください。`~~<br>→ **`お気に入り登録完了！リストで確認`** | 28 → **16자**(**-12자 · 43%↓**) | **변경** |
| en_US | `Added to favorites! Check your list` | 35자 | **변경 없음** |
| th_TH | `เพิ่มในรายการโปรดแล้ว! ดูได้ในรายการ` | 36자 | **변경 없음** |
| zh_TW | `已加入我的最愛！前往清單查看` | 14자 | **변경 없음** |

단축 후 ja(16자)가 zh(14자)와 비슷해져, zh가 정상 렌더되는 폭이면 ja도 화살표 아이콘을 가리지 않는다.

### 단축 근거 — 추측이 아니라 실측·용어집 기반

| 구간 | 변경 전 | 변경 후 | 근거 |
|---|---|---|---|
| 앞부분 | `お気に入りに追加しました！`(13자) | `お気に入り登録完了！`(10자) | `お気に入り登録` **등록값 선례 2건**(`mini_clinic_detail_banner_title`·`UF_clinic_detail_favorite_guide_title`) + `登録完了` **선례 3건**. 「완료」 ja 정본은 용어집 v5.6 **`完了`** — ko 「즐겨찾기 완료!」와 1:1 대응 |
| 뒷부분 | `リストで確認してみてください。`(15자) | `リストで確認`(6자) | 권유형 `〜してみてください`가 길이의 주원인. 「확인」 ja 정본은 **`確認`**(용어집 v5.6) |
| 마침표 | `。` 있음 | **제거** | ko·en·th·zh **4개 언어 모두 마침표 없음** — ja만 붙어 있었다. 토스트는 단문 알림이라 마침표를 두지 않는 것이 이 화면의 관례 |

zh `已加入我的最愛！前往清單查看`와 **구조가 같아졌다**(완료 알림 + 목록 안내, 마침표 없음).

## (1a) 번역 전 — 한국어 원문 교정

- ko `즐겨찾기 완료! 목록에서 확인해보세요` — **이번 변경 대상이 아니며** 맞춤법·띄어쓰기 정상. 느낌표 뒤 한 칸 띄움도 정상.
- Figma 원문과의 차이 없음 → **alias 기록 불필요**. 이번 수정은 **일본어 렌더 길이 문제**라 디자이너 원본 수정 요청 대상이 아니다.

## (a) 자동 검증 — `validate_translation.py` 결과

```
✓ 엑셀 로드: 1개 항목 / ✓ 용어집 로드: 119개 용어(v5.6)
1단계 한국어 검증 : P0 0건
2단계 용어집 검증 : 완료
3단계 다국어 검증 : 완료
```

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | **4건**(전건 판정은 아래 (c)) |
| 🟢 P2 | **0건** |

**변경한 ja에는 지적이 0건**이다 — 용어집 정본 `完了`·`確認`을 그대로 써서 매칭이 통과했다.

## (b) 수동 3단계 체크표 — 전수 점검

대상 파일이 1키뿐이라 **전체 행 = 전체 파일**을 점검했다(변경 언어로 한정하지 않고 5개 언어 전부 확인). 형제 키 `mini_voucher_detail_favorite_login_toast`도 함께 확인했다.

### 1단계 — 한국어 원문

| 점검 | 결과 |
|---|---|
| 맞춤법·띄어쓰기 | ✅ 정상(변경 없음) |
| 치환자 | ✅ 없음(원래 없는 키) |
| 개행 | ✅ 없음(5개 언어 전부) |

### 2단계 — 용어집 대조(v5.6 · 119 terms)

| 용어 | ja 정본 | 변경 후 ja | 판정 |
|---|---|---|---|
| 완료 | `完了` | `登録完了` 포함 | ✅ 일치 |
| 확인 | `確認` | `リストで確認` 포함 | ✅ 일치 |
| 즐겨찾기 | **미등재** | `お気に入り` | ✅ 등록값 선례 다수와 일치(`お気に入り` 계열) |
| 목록 | **미등재** | `リスト` | ✅ 변경 전 표기 유지 |

### 3단계 — 다국어

| 점검 | 결과 |
|---|---|
| 미변경 4개 언어 보존 | ✅ ko·en·th·zh 라이브 등록값과 완전 일치 |
| 문장 구조 정합 | ✅ 「완료 알림 + 목록 안내」 2부 구조를 5개 언어 모두 유지 |
| 마침표 표기 | ✅ ja에서 제거 → **5개 언어 전부 마침표 없음**으로 통일 |
| 언어 혼입·빈칸 | ✅ 없음 |
| 의미 손실 | ✅ 없음 — 「즐겨찾기 완료」와 「목록에서 확인」 두 정보 모두 보존 |

## (c) 검증기 보고 건별 처리 판정

| # | 검출 | 판정 | 근거 |
|---|---|---|---|
| 1 | P1 en `완료`→`Complete` 권장 | **오탐 · 미변경 언어** | `Added to favorites!`가 완료 상태를 이미 표현한다. `Complete`를 넣으면 영어로 어색하다. **이번에 바꾸지 않은 기존 등록값**이다 |
| 2 | P1 th `완료`→`เสร็จ` 권장 | **오탐 · 미변경 언어** | `เพิ่มในรายการโปรดแล้ว!`의 `แล้ว`가 완료를 나타낸다 |
| 3 | P1 zh `완료`→`完成` 권장 | **오탐 · 미변경 언어** | `已加入`의 `已`가 완료상이다. `完成`을 더하면 중복된다 |
| 4 | P1 th `확인`→`ตรวจสอบ` 권장 | **오탐 · 미변경 언어** | `ดูได้ในรายการ`(목록에서 볼 수 있다)가 문맥상 자연스럽다 |

**P0 0건 · 내 변경분(ja)이 유발한 P1/P2 0건.** 4건 모두 이번에 손대지 않은 언어의 관용 의역이다.

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

### (d-1) 용어집 보완 권장 — **등재·백로그 등재 대상 아님**

「즐겨찾기」·「목록」이 용어집에 없지만, 등록값이 `お気に入り`·`リスト`로 이미 일관돼 있어 오탐을 유발하지 않는다. 이번 P1 4건은 용어집 매핑이 좁아서가 아니라 **각 언어의 자연스러운 완료 표현**을 단어 단위로 비교한 결과다. `md/glossary-backlog.md`에 등재하지 않는다.

### (d-2) 추가 개선·제안 안내 (사용자 결정 사항)

1. **형제 키에도 같은 마침표 문제가 있다** — `mini_voucher_detail_favorite_login_toast`의 ja `ログインが必要です。`만 마침표가 있고 ko(`로그인이 필요합니다.`)를 제외한 en·th·zh는 없다. 토스트 표기를 통일하려면 이 키의 ja `。`도 함께 정리하는 것이 일관된다(이번 요청 범위 밖이라 손대지 않았다).
2. **렌더 확인 권장** — 화살표 아이콘이 실제로 보이는지는 **기기에서 확인**해야 확정된다. 16자는 zh(14자)보다 2자 길다. 더 줄여야 하면 뒷부분을 `リストで確認` → 제거해 `お気に入り登録完了！`(10자)까지 줄일 수 있으나, 「목록에서 확인」 안내가 사라지므로 **디자인 판단이 필요**하다.
3. **다른 토스트 키의 ja 길이도 같은 위험이 있다** — 이번 건은 개별 대응이지만, 토스트처럼 폭이 고정된 컴포넌트는 ja가 ko보다 길어지는 경우가 잦다. 문구를 모아서 업데이트하실 때 토스트 계열을 함께 훑어보시길 권장한다.

## 통과 판정

- **P0 = 0건**(자동 + 수동 모두) → 엑셀 산출·업로드 진행 가능
- P1 4건은 위 (c)에 **전건 오탐 판정** 기록(모두 미변경 언어) · P2 0건
- (d) 권장 3건은 **사용자·디자인 결정 전까지 임의 반영하지 않는다**

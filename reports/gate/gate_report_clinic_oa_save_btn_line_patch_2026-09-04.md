# 게이트 리포트 — `UF_clinic_oa_save_btn_line` 키 단위 번역 패치 (상담하기 → 저장하기)

- **모드**: 키 단위 번역 패치 (A) ko-source — ko 변경 → 4개 언어 재번역 (keep 없음)
- **대상 위키**: `4667512757` 「클리닉 예약 동선 개선」 · Screen 행 `kpick_clinic_oa_save_01_03`(병원 링크 저장 친구추가 바텀시트 개선판)
- **대상 파일**: `xlt_clinic_all_86keys_20260903.xlsx`(86키 업로드 정본 · 위키 첨부 v3 → **v4**) — 현행 첨부를 내려받아 `scripts/patch_translation.py`로 1키 5셀만 교체, 나머지 85키 425셀 보존
- **담당 FE 팀**: **UIT** — 프리픽스 `UF_` · 치환자 `{{0}}` (이 키에 치환자 없음)
- **사용자 결정(2026-09-04)**: CTA 값을 `LINE 친구 추가하고 상담하기` → **`LINE 친구 추가하고 저장하기`** 로 변경. 이 화면은 저장(찜) 플로우인데 「상담」이라 의도 확인을 권장했던 항목(핸드오프 P1 기획 검토 ⓑ)이 해소됐다
- **검증 범위**: **전수 점검** — 엑셀 86키 **전체 행**(430셀) 자동 검증 + 변경 키 5셀 수동 3단계 + 현행 86키 원본과 A/B 대조(변경 키 외 이슈 목록 동일 확인)

---

## (1a) 한국어 원문 교정

| # | 원문(사용자 제공) | 교정 후 (XLT Key 값) | 사유 |
|---|---|---|---|
| 1 | `LINE 친구 추가하고 저장하기` | `LINE 친구 추가하고 저장하기` (변경 없음) | 띄어쓰기·표기 정상. 등재 형제 키 `UF_voucher_oa_link_btn` = `LINE 친구 추가하고 링크 받기`와 동형 |

**alias 기록**: `{Figma 원문: LINE 친구 추가하고 상담하기 → XLT 값: LINE 친구 추가하고 저장하기}` — Figma `71339:15430` 프레임의 CTA 텍스트는 아직 `상담하기`다. 사용자 결정으로 XLT 값만 변경했으며 **Figma 원본 수정 요청은 이 프로젝트 종결 규칙에 따라 하지 않는다**(alias만 기록).

원문 이상문자 점검: nbsp·제로폭·전각공백 **0건**.

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py` (용어집 v5.1)

| 심각도 | 패치 전(현행 86키) | 패치 후 | 변동 |
|---|---|---|---|
| 🔴 **P0** | 0건 | **0건** ✅ | 불변 |
| 🟡 P1 | 75건 | **75건** | **신규 0건** — 이슈 목록 diff 동일 |
| 🟢 P2 | 9건 | **9건** | 불변 |

- 변경 키 `UF_clinic_oa_save_btn_line`에 대한 지적: **0건**.
- ※ P1 75건은 직전 리포트(09-03 · 59건)보다 16건 많다 — **용어집 v4.9→v5.1**(캐시백 ja `還元`·혜택·결제 등 매핑 추가)로 같은 파일의 기존 키가 새로 걸린 것이며 이번 패치와 무관. 이 75건은 09-04 「캐시백 還元 통일」·「업로드 검증」 리포트에서 처리 판정을 마친 항목이다(등재값에는 이미 `還元` 반영 — 엑셀 정본은 XLT 재업로드 후 별도 동기화 대상, 아래 (d)).

---

## (b) 수동 3단계 체크표 — 변경 키 5셀 (+ 나머지 85키는 A/B 동일로 확인)

### 1단계 (한국어 원문)

| 키 | ko_KR | 판정 |
|---|---|---|
| `UF_clinic_oa_save_btn_line` | `LINE 친구 추가하고 저장하기` | **정확** — 「친구 추가」 띄어쓰기 = 등재 선례 4/4(`UF_voucher_oa_link_btn`·`_oa_pay_btn`·`_friend_add_btn`·기존값). 「저장하기」는 이 화면의 기능(병원 링크 저장)과 일치 |

### 2단계 (용어집·등재값 정합)

| 항목 | 근거(등재값 실측 · Unifi WEB BROWSER v1.7.5 · 2,462키) | 판정 |
|---|---|---|
| 구조 승계 | `UF_voucher_oa_link_btn` = `LINE 친구 추가하고 링크 받기` → ja `LINEで友だち追加してリンクを受け取る。` · en `Add as a friend on LINE and get the link` · th `เพิ่มเพื่อนใน LINE แล้วรับลิงก์` · zh `加入 LINE 好友並取得連結` — 「LINE 친구 추가하고 + 동사」 구조를 4개 언어 그대로 승계 | 정확 |
| 「저장」 대역 | ko `저장`을 포함한 등재 키 **4/4 만장일치** — ja `保存` · en `Save` · th `บันทึก` · zh `儲存`(`UF_clinic_detail_btn_save`·`UF_voucher_btn_wishlist`·`UF_settings_*_btn_save` 2건) | 정확 |
| 용어집 | 「저장」·「친구 추가」는 용어집 미등재(금지어 사전에 `팔로우`·`OA 채널` 계열만 있음 — 해당 없음) | 위반 없음 |

### 3단계 (다국어 4셀)

| 언어 | before | after | 판정 |
|---|---|---|---|
| ja_JP | `LINEで友だち追加して相談する。` | `LINEで友だち追加して保存する。` | **정확** — `相談`→`保存`만 교체. 문말 `。`는 같은 키 패밀리 등재 3/3 선례 유지(09-03 리포트 M-1과 동일 판정) |
| en_US | `Add as a friend on LINE and consult` | `Add as a friend on LINE and save` | **정확** — `consult`→`save`. 선례 `…and get the link`·`…and continue`와 동형 |
| th_TH | `เพิ่มเพื่อนใน LINE แล้วปรึกษา` | `เพิ่มเพื่อนใน LINE แล้วบันทึก` | **정확** — `ปรึกษา`→`บันทึก`(등재 `บันทึกลิงก์คลินิก` 어형) |
| zh_TW | `加入 LINE 好友並諮詢` | `加入 LINE 好友並儲存` | **정확** — `諮詢`→`儲存`(번체 정자 · 등재 `儲存診所連結` 어형) |

언어 혼입·placeholder·태그: 5셀 모두 해당 없음. 컬럼 정렬(Step 2-1): 패치 전 5셀 모두 위키 다국어 표와 일치(어긋남 0) → 단일 키 5셀 전체 교체.

---

## (c) 각 건 처리 판정

| # | 대상 | 지적 | 처리 판정 | 사유 |
|---|---|---|---|---|
| P1 ×75 | 변경 키 외 기존 키 | 용어 불일치·구 표기(캐시백·혜택·결제·최대) | **이번 범위 밖 — 기존 판정 유지** | 패치 전후 목록 동일(신규 0). 캐시백 ja `キャッシュバック` 17건은 등재값에 `還元`이 이미 반영됐고(09-04), 엑셀 정본만 구값 — (d-2)에서 동기화 제안 |
| P2 ×9 | 기존 키 | 마침표 스타일 | **정책 유지(오탐)** | 문장형 문구의 마침표는 정상. 기존 리포트와 동일 판정 |
| M-1 | `_btn_line` ja | 버튼 문말 `。` | **선례 따름** | 키 패밀리 등재 3/3이 `。`. 단독 제거 시 형제와 갈림 |
| M-2 | Figma 원문 | CTA가 `상담하기`로 남음 | **alias 기록만** | 프로젝트 종결 규칙(Figma 원본 수정 미요청) |
| M-3 | XLT 등재값 | `UF_clinic_oa_save_btn_line` 등재값이 구값(`상담하기` 5개 언어) | **P1 — 사용자 재업로드 필요** | 재생성 엑셀(86키 정본 v4)을 XLT 시스템에 업로드해야 화면이 바뀐다 |

**통과 판정: P0 = 0건 (자동 + 수동 모두)** → 엑셀·위키 반영 진행.

---

## (d) 추가 개선·제안 (권장 — 사용자 결정 전 임의 반영 금지)

- **(d-1) 용어집 보완 권장**: 없음. 「저장」 4개 언어는 등재 4/4 만장일치라 등재 없이도 일관성이 유지된다(실측 N=4). 반복 오탐도 없음.
- **(d-2) 엑셀 정본 ↔ 등재값 동기화 제안**: 86키 정본 엑셀에는 캐시백 ja `キャッシュバック`(17건)이 남아 있으나 등재값은 09-04에 `還元`으로 재업로드됐다. 다음 재업로드 시 구값이 되돌아가지 않도록, **정본 엑셀의 해당 17셀도 `還元`으로 맞추는 패치**를 권장한다(이번 요청 범위 밖이라 미적용).
- **(d-3) 등재값 재업로드 확인**: 업로드 후 `fetch_xlt_registry.py --service Unifi`로 `UF_clinic_oa_save_btn_line` 5개 언어가 새 값인지 재조회 권장.

# 번역 검증 리포트 — 럭키볼 친구초대 배너 (Unifi mini / LV)

- 대상 위키: pageId=4479306980 (럭키볼 친구에게 선물하기 캠페인)
- Figma 프레임: `Unifi mini` (node-id 63411-33349)
- 팀: **LV** (위키 Screen 섹션 UIT/LV 구분 존재 → LV 소제목 영역, 치환자 `{0}`)
- 모드: 코멘트 선별 모드(답글 `xlt` 마커) + 위키 Mode B(단일 프레임 행 추가)
- 대상 키: **1건** (`mini_luckyball_invite_banner`)

## 검토 범위 (전수 점검 명시)
본 작업은 XLT 코멘트가 가리키는 **선별 1건**이 전체 대상이다. 대상 파일(임시 검증 엑셀)의 **전체 행(1행) × 5개 언어(ko_KR/ja_JP/en_US/th_TH/zh_TW)를 한 줄씩 전수 수동 검토**했다 — 신규 키로 한정하지 않고 전체를 점검. 원문의 소프트 줄바꿈(U+2028)은 `\n`으로 정규화.

## 한국어 원문 교정 (1a) 및 alias
- 원문(Figma): `친구 초대하고 럭키볼 받기` (U+2028 = LINE SEPARATOR, 디자인 툴 soft break)
- 교정 후 ko_KR: `친구 초대하고\n럭키볼 받기` (줄바꿈 문자만 정규화, 맞춤법·띄어쓰기 이상 없음)
- alias 기록: `{ "친구 초대하고 럭키볼 받기" → "mini_luckyball_invite_banner" }` (translation_data.json aliases)
- 원문의 U+2028은 레이아웃용 줄바꿈이라 디자이너 원본 수정 불필요(제안 항목에서 안내).

## Executive Summary (자동 검증 — validate_translation.py)
| 심각도 | 건수 | 카테고리 |
|--------|------|----------|
| 🔴 P0 (Critical) | 0건 | 빈칸, 오타, 언어 혼입, 용어집 표기 위반 |
| 🟡 P1 (Medium) | 0건 | 표현 어색, placeholder 불일치, 일관성 위반 |
| 🟢 P2 (Low) | 0건 | 스타일 선호 (마침표, 쉼표) |

자동 검증기는 P0/P1/P2 = 0/0/0. 단, `P0=0`은 시작점이므로 아래 수동 3단계로 전수 확인.

## 수동 3단계 체크표 (전수 — 1행 × 5언어)

### 1단계: 한국어 맞춤법·띄어쓰기·표현
| 점검 | 결과 |
|---|---|
| 오타 | 없음 |
| 맞춤법(되요/됬 등) | 해당 없음 |
| 띄어쓰기(의존명사·붙은 단어) | `친구 초대하고`·`럭키볼 받기` 정상, 붙은 단어(누리고다양한류) 없음 |
| 비표준 공백/` `·`/n` 오타 | U+2028 → `\n` 정규화 완료, `/n` 오타 없음 |
| 표현 어색(`미션하고`류) | 없음 — 배너 명사형 타이틀로 자연스러움 |
| 조사/종결 일관성 | 명사형 `받기`(배너 관용) 일관 |

### 2단계: 용어집 위반
| 점검 | 결과 |
|---|---|
| 한국어 키워드 매칭 | 럭키볼/친구/초대/받기 = 용어집 미정의 → 위반 없음(신규 도메인 용어) |
| 예외 패턴(PIN/API/URL/USDT/JPYC 등) | 대상 문구에 미포함 → 해당 없음 |
| zh_TW 번체 정자(產/臺 등) | `邀請`·`好友`·`領取`·`幸運球` 모두 정자 번체, 간체/이체자 없음 |
| th_TH 자산 표기 | 해당 없음 |

### 3단계: 다른 언어 검토
| 점검 | 결과 |
|---|---|
| 빈칸 | 5개 언어 모두 채움 |
| 언어 혼입 | en 영어만 / ja 가나·한자·카타카나만 / zh 번체만 / th 태국어만 — 혼입 없음 |
| placeholder(`{0}`) | 변수 없음 → 5개 언어 모두 placeholder 없음(일치) |
| `/n` 오타 | 없음(`\n` 정상) |
| 컬럼 swap | 각 언어 칸에 올바른 언어 내용 |
| ja 한국어 직역(出席류) | `もらおう` 자연스러운 일본어, 직역 없음 |
| 단어 중복 | 없음 |
| 마침표 일관성 | 배너 타이틀 → 5개 언어 모두 마침표 없음(일관) |
| 줄바꿈 위치 | 각 언어 절 경계에서 자연스럽게 분할 |

## 검증기 보고 각 건 처리 판정
자동 검증기가 보고한 P0/P1/P2 = **0건** → 처리할 위반·오탐 없음. 수동 3단계에서도 **실제 위반 0건**(오탐/정책 보류 항목 없음). 처리 판정: 전 항목 이상 없음.

## 통과 판정
- P0 = 0건 (자동 + 수동) → 출력·위키 반영·엑셀 생성 진행 가능
- P1/P2 = 0건

## 추가 개선·제안 (권장 — 사용자 결정 사안)
- **(d-1) 용어집 보완 권장**: `럭키볼`은 캠페인 고유 용어로 용어집(web3_xlt_json) 미정의. 반복 사용 대비 아래 5개 언어 표기를 용어집에 추가 권장(반영은 `md/landpress.md` 절차로 Landpress CMS 붙여넣기):
  - ko_KR `럭키볼` / en_US `Lucky Ball` / ja_JP `ラッキーボール` / zh_TW `幸運球` / th_TH `ลูกบอลนำโชค`
- **(d-2) 추가 개선·제안**:
  - 원문 U+2028(soft line break)은 레이아웃 줄바꿈이라 XLT에선 `\n`으로 정규화. 디자이너 원본 수정은 불필요(참고용 안내).
  - `ja_JP`를 마케팅 톤 `もらおう`(권유형)로 선택 — `GET`(영어 혼용) 대신 언어 혼입 위험을 피함. 팀 선호가 `GET` 표기라면 사용자 확인 후 조정 가능.

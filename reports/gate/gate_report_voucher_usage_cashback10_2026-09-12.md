# 게이트 리포트 — voucher_usage 5개 언어 + 캐시백 10% 반영 (2026-09-12)

- **대상 ①** LV `voucher_common_info.voucher_usage` — **5개 언어 신규 생성**(기존 beta 등록은 ko_KR 1건)
- **대상 ②** UIT `payment_common_info.payment_guide` — **캐시백 비율 5% → 10%** 반영(5개 언어, 등록본 수정 필요)
- **원문 출처**: UIT `k_pick_shopping_product.guide`(상품 8종에 5개 언어 등록돼 있음)를 이관. 구조는 LV `voucher_usage`(`{title, items[{step,title,description}]}`)에 맞췄다.
- **검증 범위**: 변경·신규 **전체 행 10건 전수 점검**(voucher_usage 9행 + payment_guide 1행). 이 작업의 전체 대상이다.
- **엑셀**: `xlt_output_20260912140746.xlsx`

---

## (1a) 한국어 원문 교정

| # | 항목 | 판단 | 처리 |
|---|---|---|---|
| 1 | 캐시백 비율 | 기존 문구가 **5%** — 사용자 확정 값은 **10%** | ko `구매 완료 후 결제 금액의 **10%**를 포인트로 돌려받아요.` · payment_guide `결제 금액의 **10%** 캐시백`로 5개 언어 모두 교체 |
| 2 | `voucher_common_info` 구조 | **다건·<code>uid</code> 불필요**(사용자 확정) — Unifi 공통 안내라 브랜드·상품과 무관 | `uid` 미사용 · `voucher_usage` 단일 필드 |

원문 문장은 비율 수치 외에 수정하지 않았다. alias로 남길 치환은 없다.

---

## (a) 자동 검증 결과

| 심각도 | 1차 | **최종** |
|---|---|---|
| 🔴 P0 | 0건 | **0건** |
| 🟡 P1 | 15건 | 13건 |
| 🟢 P2 | 1건 | 1건 |

**통과 기준 P0 = 0 충족.**

---

## (b) 수동 3단계 체크표 — 전체 행 10건 전수

### 1단계 · 한국어(ko_KR)

| 키 | 판정 | 근거 |
|---|---|---|
| `voucher_usage.title` 바우처 이용 방법 | 정확 | 화면 라벨과 일치 |
| `items[0]` 바우처 구매하기 / Unifi에서 원하는 바우처를 구매해요. | 정확 | — |
| `items[1]` 구매한 바우처 확인하기 / MY탭>바우처… | 정확 | 화면 경로 표기 유지 |
| `items[2]` 캐시백 받기 / 결제 금액의 **10%** | 정확 | 비율 확정값 반영 |
| `items[3]` 바우처 사용하기 / 상품별 사용처와 이용 방법… | 정확 | — |
| `payment_guide` step4 / 결제 금액의 **10%** 캐시백 | 정확 | 비율 확정값 반영 |

띄어쓰기·조사·외래어 음차 오염 없음.

### 2단계 · 용어집(v5.4) 대조

| 용어 | 정본 | 적용 | 판정 |
|---|---|---|---|
| 캐시백 | ja `還元` · en `cashback` · th `เงินคืน` · zh `現金回饋` | ja가 **구 표기 `キャッシュバック`** → **`還元`으로 수정** | **수정 완료** |
| 바우처 | ja `クーポン` | UIT 값의 `バウチャー` → `クーポン` 교체(제목·본문) | 수정 완료 |
| 결제 | ja `決済` · zh `付款` | 적용 | 정확 |
| 구매 | ja `購入` · zh `購買` | 적용 | 정확 |

### 3단계 · 언어별

| 언어 | 점검 | 판정 |
|---|---|---|
| ja_JP | `バウチャー`·`キャッシュバック` 잔존 **0건** 확인 · 경어 일관 | 정확 |
| en_US | 비율 표기 `10%` · 문장 종결 일관 | 정확 |
| th_TH | 비율 표기 `10%` · 구절 띄어쓰기 | 정확 |
| zh_TW | **번체** 유지(`優惠券`·`點數`·`現金回饋`) · 숫자 앞뒤 공백 원문 유지 | 정확 |
| 전 언어 | 4단계 `step` 번호·순서 ko와 동일 · 비율 수치 **5개 언어 모두 10%** 로 일치 | 정확 |

---

## (c) 건별 처리 판정

### P0 — 0건

### P1 (1차 15건 → 최종 13건)

| 유형 | 건수 | 판정 |
|---|---|---|
| ja `キャッシュバック` → `還元` | 2 | **실제 위반 · 수정 완료** |
| en `구매/구매하기` → `Buy/Buy now` | 4 | **오탐** — `purchase`/`Buy your voucher` 문맥이 자연스러움 |
| en/th `완료` → `Complete/เสร็จ` | 2 | **오탐** — `After your purchase`·`หลังซื้อสำเร็จ` 문맥 |
| th/zh `결제` → `การชำระเงิน/付款` | 2 | **오탐** — 해당 표기를 이미 사용 중 |
| en/th/zh `확인` → `check/ตรวจสอบ/查看` | 5 | **오탐** — 「결제 승인」·「사용처 확인」 문맥이라 `confirmed`·`確認`이 정확 |

### P2 1건

`items[1].title` 「구매한 바우처 확인하기」 마침표 없음 — **오탐**. 단계 제목이라 마침표를 붙이지 않는다.

---

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

### (d-1) 용어집 보완

`포인트` · `유효기간` 미등재. 사용자 지시대로 전체 번역 완료 후 일괄 등재하며, 권장 표기는 실사용 실측 후 제출한다.

### (d-2) 추가 개선 제안

1. **캐시백 비율의 리터럴 하드코딩** — 10%로 바꿨지만 여전히 문구에 숫자가 박혀 있다. 비율이 다시 바뀌면 5개 언어 × 2필드를 모두 고쳐야 한다. 클리닉은 같은 이유로 **비율 미명시 문장**으로 바꾼 선례가 있다 — 검토 권장
2. **UIT `guide`·`checkout`의 5% 잔존** — UIT `k_pick_shopping_product`의 `guide`(상품 8종 × 5개 언어)와 `checkout`에는 아직 **5%**가 남아 있다. UIT 화면이 서비스 중이라면 그쪽도 10%로 고쳐야 한다
3. **`voucher_common_info` 타입** — 현재 **다건**으로 생성돼 있다. 공통 1건만 쓰므로 단건 타입이 더 맞는지 검토 권장

---

## 결론

- **P0 = 0건**(자동 + 수동) → 산출물 출력 가능
- 실제 위반 **2건(ja 구 표기)** 수정 완료, 나머지 P1 13건·P2 1건은 오탐
- (d) 권장 사항은 사용자 결정 전까지 미적용

# 게이트 리포트 — 상품권 상태 · 바코드 정보 없음 신규 키 1건 (5개 언어)

- 대상 프레임: Figma `GOCHAYBS7hIrmWRGNuJOKV` · **상품권 상태 - 바코드 정보 없음** `74208-24129` (375×933)
- 위키: [pageId 4704515582](https://wiki.workers-hub.com/display/UNIFI/Unifi+mini+v2.0) · **My - UIT** 섹션 · `바우처 상세 - 사용가능` **바로 아래 행으로 신설**
- 담당 FE: **UIT** — 프리픽스 `UF_` · 치환자 `{{0}}` (위키에 UIT/LV 구분이 있어 그대로 적용)
- 신규 **1키** × 5개 언어 = **5셀** · 나머지 문구는 기존 키 재사용(재번역 0건)
- 조달: Figma 노드·코멘트 **신규 조회**(`collect_frames.py` · 코멘트 1스레드) · XLT 등록값 `Unifi / WEB BROWSER` **2,553키** 신규 조회 · 용어집 API **v5.5** 신규 조회 (전부 캐시 미사용)

---

## 0. 프레임 구성 — 신규 대상은 1건뿐

코멘트는 **1스레드**(`xlt` · y=339)이고, 가리키는 텍스트는 `바코드를 표시할 수 없습니다`(`74208:24173`)다.

화면의 나머지 텍스트 21개는 `바우처 상세 - 사용가능`(`72193-11353`)과 **동일 구성**이라 기존 키를 그대로 쓴다.

| 영역 | 처리 |
|---|---|
| 바코드 자리 안내 문구 | 🆕 **신규 1키** |
| 상품권 정보 표(유효기간·사용 가능 여부·사용처·사용 가능 금액·주문번호) · 결제 정보 보기 | 기존 `UF_voucher_detail_*` 재사용 |
| 「사용 가능 여부」 값 `결제 취소` | 공통 상태 키 `UF_status_payment_canceled` 재사용 |
| 사용 방법·상품 설명 및 유의사항 | LPC 관리 영역(XLT 대상 아님) |

## (1a) 번역 전 — 한국어 원문 교정

**교정 없음.** Figma 원문 `바코드를 표시할 수 없습니다`는 맞춤법·띄어쓰기 정상이고, 형제 키의 종결 어미(합쇼체 `~습니다`)와도 어긋나지 않는다.

- **alias 없음** — Figma 원문과 XLT Key KR이 동일하다.
- **고유명사 조사 대상 없음** — 브랜드·제품·기관·법령명 미포함(`바코드`는 일반명사).

## 이름 선례 실측

| 항목 | 실측 |
|---|---|
| 네임스페이스 | 같은 화면 계열이 전부 `UF_voucher_detail_*`(등록값 16키) — 그대로 따랐다 |
| 이름 충돌 | `UF_voucher_detail_barcode_unavailable` — 등록값 **2,553키에 없음**(0건) |
| 기존 `바코드` 키 | 등록값에 **0건** — 재사용할 선례가 없어 신규가 맞다 |

## (a) 자동 검증 — `validate_translation.py` P0/P1/P2 요약

용어집 v5.5(118 용어) 기준, 신규 1키 × 5개 언어.

| 심각도 | 건수 |
|---|---:|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

**P0 = 0 → 통과 기준 충족.**

## (b) 수동 3단계 체크표 — 5셀 **전수**

대상이 1키이므로 **전체 행 = 전수 점검**이다. 아울러 이 화면이 재사용하는 **기존 9키 × 5개 언어 = 45셀도 등록값 기준으로 재확인**했다(변경 없음).

| 언어 | 값 | 1단계 원문·표기 | 2단계 용어집 | 3단계 다국어 |
|---|---|---|---|---|
| ko_KR | 바코드를 표시할 수 없습니다 | ✅ 맞춤법·조사 `를` 정상 | ✅ 위반 없음 | ✅ 기준 |
| ja_JP | バーコードを表示できません | ✅ 가타카나 표기 정상 | ✅ 위반 없음 | ✅ |
| en_US | The barcode cannot be displayed | ✅ 대소문자 정상 | ✅ 위반 없음 | ✅ |
| th_TH | ไม่สามารถแสดงบาร์โค้ดได้ | ✅ 표기 정상 | ✅ 위반 없음 | ✅ |
| zh_TW | 無法顯示條碼 | ✅ 번체 정상(간체 혼입 0 · `条码` 아님) | ✅ 위반 없음 | ✅ |

**점검 항목별**: 외래어 음차 오염 없음 · 붙은 단어 없음 · 다의어 모호 없음 · 언어 혼입 없음 · **변수 치환자 없음** · 마침표는 5개 언어 모두 생략(형제 상태 키 `UF_status_*`와 동일 — 짧은 상태·안내 라벨).

## (c) 검증기 보고 건별 처리 판정

자동 검증 **P0·P1·P2 모두 0건**이라 판정 대상이 없다. **오탐 0 · 실제 위반 0 · 정책 보류 0.**

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

**(d-1) 용어집 보완 권장 — 없음.** 반복 오탐이 없었고 `md/glossary-backlog.md`에 올릴 항목도 없다.

**(d-2) 추가 개선·제안**

| # | 내용 | 판단 주체 |
|---|---|---|
| 1 | 🔴 **노출 조건이 정의돼 있지 않다** — 프레임 이름은 「바코드 정보 없음」이고 화면의 「사용 가능 여부」는 `결제 취소`(`REFUNDED`)지만, **바코드 미표시가 어느 상태값에서 발동하는지 코멘트에 없다**(발급처 응답 지연 · 취소 · 만료 등 범위 미정). 위키 Description에 「확인 필요」로 표시했다 | 기획 |
| 2 | 이 화면은 `바우처 상세 - 사용가능`의 **상태 변형**이라 GA `view`를 따로 두지 않고 `my_voucher_detail_01`을 재사용했다(`바우처 상세 - 즐겨찾기` 행과 동일 원칙). 별도 화면으로 집계하려면 Screen ID 신설이 필요하다 | 기획 |
| 3 | 「사용 방법」 본문이 `바우처 상세 - 사용가능`과 동일해 **LPC 관리 영역**으로 판단했다 — 바코드를 못 쓰는 상태에서 「바코드를 제시하여 사용할 수 있어요」가 그대로 노출되는 것이 맞는지 확인이 필요하다 | 기획 |

## 통과 판정

**P0 = 0 (자동 + 수동 전수)** → 위키 반영·엑셀 생성 완료.

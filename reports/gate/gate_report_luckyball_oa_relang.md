# 번역 검증 게이트 리포트 — OA 메시지 타 언어 재번역 (KR 기준)

- 대상 위키: pageId 4479306980 / OA 섹션 (XLT 키 미부여, md/OA.md) / 팀 OA
- 성격: OA 메시지 KR 재작성분(럭키볼 선물/오픈/친구 초대)에 맞춰 JA/EN/TH/ZH 재번역. Screen 표 KR = 기준. OA는 엑셀·전역 키표에 미포함(OA.md 규칙)이라 자동 검증기 대상 아님 → 수동 검증.
- 용어집: API v2.6.

## 재번역 대상 (8개 distinct 메시지 → 9개 마킹 행, 3개 프레임)

럭키볼 뽑았을때·가입 완료시·피초대자 가입완료시. 빈 JA 채움, 어긋난 EN 수정(예: `친구 초대하러 가기`가 EN `Go to home`이던 것 → `Go invite friends`), 버튼 `럭키볼 선물 오픈하기✨`의 4개 언어 구버전(`draw/뽑기`) → `Open your Lucky Ball gift✨`/`ラッキーボールのプレゼントを開ける✨` 등으로 수정.

## 0. 한국어 원문 교정
- KR은 Screen 마킹 최신값(기준) 그대로. `\xa0`→공백 정규화. 변수 `{{amount}}`·`{{wallet_address}}` 보존.

## Executive Summary (수동 — OA는 엑셀 미포함)
| 심각도 | 건수 | 비고 |
|--------|------|------|
| 🔴 P0 | **0건** | 빈칸/언어혼입/변수 손실/오타 없음 |
| 🟡 P1 | 0건 | — |
| 🟢 P2 | 0건 | — |

## 1단계: 한국어
- 8개 메시지 KR 재검토: 오타·띄어쓰기 이상 없음. 문장형은 마침표/`!` 정상.

## 2단계: 용어집
- 럭키볼=ラッキーボール/Lucky Ball/ลูกบอลนำโชค/幸運球, 선물=プレゼント/gift/ของขวัญ/禮物, JPYC·LINE·Unifi 표기 일관. zh 번체 정자.

## 3단계: 다른 언어
- 빈칸 0(빈 JA 전부 채움)·언어 혼입 0. 변수 `{{amount}}`·`{{wallet_address}}` 5개 언어 동일 보존. 버튼/타이틀/본문 문맥 일치. 어긋난 EN(`Go to home`↔`친구 초대하러 가기`) 교정.

## 각 건 처리 판정
- P0/P1/P2 0건 — 수정/보류 없음. 전 항목 KR 기준 재번역 반영.

## (d) 추가 개선·제안
- **(d-1) Flex JSON 매핑 확정 필요**: 기존 Flex JSON이 구 문안·다른 구조(타이틀/본문/버튼2개)라 위키 메시지(No1/No2/No3)와 1:1 아님. Flex 재생성은 매핑 확정 후 진행(사용자 확인).
- **(d-2) OA 전수 점검**: 이번은 마킹된 3개 프레임. 나머지 OA 프레임도 KR 변경 시 동일 재번역 필요.
- **(d-3) 용어집 보완**: 다의어 문맥 변형(md/landpress.md).

## 통과 판정
- 수동 3단계 **P0=0** → OA 표 반영 통과. Flex JSON은 매핑 확정 후 별도 진행.

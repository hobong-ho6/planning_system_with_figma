# 번역 검증 리포트 — 럭키볼 캠페인 증분 업데이트 (25034 신규 + 38544 재사용 + 38078 리네임)

- 대상 위키: pageId=4479306980 · 팀 **LV** (치환자 `{0}`)
- 범위(증분): ① 초대자 화면 전체(63411-25034) **신규 4개 키** ② (JPX) 피초대자 페이지(63411-38544) 알림 **기존 키 재사용**(신규 번역 없음) ③ 피초대자 페이지-리워드(63411-38078) **프레임명만 변경**
- 변수화(사용자 확정): `4 / 8개 뽑기 → {0} / {1}개 뽑기`, `8 / 20명 → {0} / 20명`(20 고정)

## 검토 범위 (전수 점검 명시)
이번은 증분 추가다. **신규/변경 대상 4개 키 전체(전 행)를 5개 언어 한 줄씩 수동 검토**했고, 기존 62개 키는 직전 run(gate_report_luckyball_3sections)에서 전수 검증·불변이라 재검증 생략(내용 변경 없음). 38544는 기존 키 재사용이라 신규 번역 없음, 38078은 번역 무변경(이름만).

## 한국어 원문 교정 (1a) 및 alias
- 신규 라벨 2개(`미션 완료 친구`·`내 보유 럭키볼`)는 오타·띄어쓰기 이상 없음(교정 불필요).
- 변수화 2개: Figma 원문 `4 / 8개 뽑기`→`{0} / {1}개 뽑기`, `8 / 20명`→`{0} / 20명`. alias 기록: `{ "4 / 8개 뽑기"→mini_luckyball_my_draw_count, "8 / 20명"→mini_luckyball_mission_done_count }` (translation_data.json aliases, 총 66건).
- 20은 정책 코멘트 "20명 고정"에 따라 고정 유지(변수화 제외).

## Executive Summary (자동 검증 — validate_translation.py, 전체 66항목)
| 심각도 | 건수 | 판정 |
|--------|------|------|
| 🔴 P0 | 0건 | 없음 |
| 🟡 P1 | 24건 | 전건 오탐(직전 run과 동일 — 용어집 협소 매핑). 신규 4개는 P1 0건 |
| 🟢 P2 | 26건 | 전건 오탐(타이틀·라벨 마침표 미사용). 신규 4개는 P2 해당 없음 |

## 수동 3단계 체크표 (신규 4개 전수 × 5언어)
### 1단계: 한국어
| 키 | 결과 |
|---|---|
| mission_done_friends `미션 완료 친구` | 오타·띄어쓰기 정상 |
| my_holdings `내 보유 럭키볼` | 정상 |
| my_draw_count `{0} / {1}개 뽑기` | 정상(치환자 형식) |
| mission_done_count `{0} / 20명` | 정상(20 고정) |
### 2단계: 용어집
- `럭키볼`=Lucky Ball/ラッキーボール/幸運球/ลูกบอลนำโชค 일관(용어집 v2.6 등록). zh 번체 정자: `幸運球`·`完成任務`·`抽取`·`顆` 정자, 간체 0건. 위반 없음.
### 3단계: 다른 언어
- 빈칸 0건. 언어 혼입 0건. placeholder: my_draw_count `{0}`·`{1}` 5개 언어 전부 정합, mission_done_count `{0}` 5개 언어 전부 정합. 컬럼 swap 없음. ja 직역 없음.

## 검증기 보고 각 건 처리 판정
- 신규 4개: 자동 P0/P1/P2 **0건**, 수동 **실제 위반 0건**.
- 전체 P1 24·P2 26: **전건 오탐**(직전 run 리포트와 동일 사유 — 버튼 확인=OK/변경=變更/송금=匯款/대소문자, 타이틀 마침표 미사용). 현행 유지.

## 통과 판정
- **P0 = 0건 (자동 + 수동)** → 위키 반영·엑셀 재생성 진행 가능
- P1/P2 = 전건 오탐(유지)

## 추가 개선·제안 (권장 — 사용자 결정)
- **(d-1) 용어집**: `럭키볼` v2.6 반영 완료. 추가 반복 오탐 용어(확인 버튼=OK, 변경=變更, 송금=匯款)는 여전히 확장 여지 — 필요 시 진행.
- **(d-2)**: 변수 `4 / 8개 뽑기`의 `{0}`(보유)·`{1}`(전체) 의미가 FE 데이터 바인딩과 일치하는지 확인 권장. `8 / 20명`의 20은 정책상 고정. Figma 원문 `정보가변경`·`LINE계정외` 등 기존 오타는 앞 run에서 교정·디자이너 보고 대상.

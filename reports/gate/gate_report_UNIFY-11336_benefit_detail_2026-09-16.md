# 게이트 리포트 — UNIFY-11336 [Design QA] 혜택 상세_xlt (2026-09-16)

- 대상: ① XLT `UF_my_benefit_total_desc`(Unifi · `{{0}}`) 5개 언어 · ② LPC `my_common_info.benefit_more.items[unlimited_mission].title`(UIT beta·prod) en_US
- 근거: Jira UNIFY-11336 첨부 + Figma `eGDZjjZmHRWkPd4UVXAmeA` `1:9`(혜택 상세 (바텀시트)_xlt) 원본 재조회
- 검증 대상 파일: `xlt/xlt_output_20260916161856.xlsx`(업로드용 ①) · 검증용 임시 엑셀(①+②)
- 점검 범위: **대상 파일 전체 행 전수 점검**(2행 전건 · 신규/변경 키로 한정하지 않음)

## (1a) 한국어 원문 교정

- 디자인 요청서 AS-IS 표기는 `이번달`이나 **실등록값·정본은 `이번 달`**(의존명사 「달」 앞 띄어씀)이다. TO-BE를 글자 그대로 옮기면 띄어쓰기가 후퇴하므로 **줄바꿈만 반영하고 `이번 달`을 유지**했다.
- 이 차이는 Figma 주석 표기(`이번달`)와 XLT 등록값(`이번 달`)의 불일치이며, **alias로 기록**한다: `{Figma 주석 이번달 → XLT Key 이번 달}`. Figma 원본 수정은 요청하지 않는다.
- ② ko_KR `무제한 미션으로 최대 3만엔 받기`는 변경 요청이 없어 원문 유지(맞춤법 이상 없음).

## (a) 자동 검증 요약 — `validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | 1건 |
| 🟢 P2 | 0건 |

## (b) 수동 3단계 체크표 (전체 행 전수)

| 단계 | 점검 | 결과 |
|---|---|---|
| 1단계 (한국어) | 맞춤법·띄어쓰기·조사 — `이번 달`(정상) · 개행 위치가 어절 경계인지 | ✅ 이상 없음 |
| 2단계 (용어집) | `혜택`·`미션`·`JPYC` 표기, 금액 표기 `¥` | ✅ 일치 |
| 3단계 (다국어) | 변수 `{{0}}` 5개 언어 전건 보존(각 1회) · 언어 혼입 없음 · 개행은 ko/ja/en 3개 언어에만(th·zh는 요청 없어 미변경) · ja 마침표 유지 · en 마침표 추가(디자인 TO-BE 반영) | ✅ 이상 없음 |

- 개행은 **실제 개행 문자**로 넣었다 — Unifi 등록값 실측 선례 **실개행 214키 vs 리터럴 `\n` 33키**(다수 관례).
- 외래어 음차 오염·띄어쓰기 누락·다의어 모호 항목 육안 점검: 해당 없음.

## (c) 검증기 보고 각 건 처리 판정

| # | 심각도 | 내용 | 처리 판정 |
|---|---|---|---|
| 1 | P1 | `LPC_..._unlimited_mission` zh_TW: 「최대」 → `最高` 권장 | **정책 보류(사용자 확인 대상)** — 실제 위반 아님. ⓐ 이번 요청은 **en_US만** 대상이고 ⓑ 현재 zh 값 `最多`는 기존 등록값이다. 실측상 ko 「최대」의 zh 표기는 `最高` 78 · `最多` 16 · `最大` 7로 `最高`가 다수지만, **의미가 「최대 N개/최대 금액」이면 `最多`도 정상**이라 일괄 교체 대상이 아니다 |

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장
- 「최대」의 zh_TW 대응 표기가 **`最高` 78 / `最多` 16 / `最大` 7**로 갈려 있다(실측, Unifi 2,589키 기준). 용어집에 **문맥별 선택 기준**(금액 상한=`最高`, 수량 상한=`最多`)이 없어 검증기가 정상 의역을 P1로 반복 오탐한다 → **`md/glossary-backlog.md` 대기 목록 등재**(즉시 반영 여부는 묻지 않는다).

### (d-2) 추가 개선·제안 안내
1. **같은 키 2개 화면 사용** — `UF_my_benefit_total_desc`는 My 영역(1줄)과 혜택 상세 바텀시트(2줄)에서 **동일 value로 공유**된다(위키 명시). 개행 삽입 시 **My 영역도 2줄로 렌더**된다. 사용자가 「값에 개행 삽입」을 선택해 그대로 진행했으나, My 영역 레이아웃은 **배포 후 QA에서 확인**을 권장한다.
2. **en_US 마침표** — 변경본은 디자인 TO-BE대로 마침표를 붙였다. 등록값 관례는 5단어 이상 문장형 1,111건 중 567건(51%)이 마침표로 끝나 **혼재**한다. 통일 정책이 필요하면 별건으로 결정할 사안이다.
3. **th_TH·zh_TW 미요청** — 두 언어는 줄바꿈·문구 요청이 없어 현행 유지했다. 동일 화면에서 3개 언어만 2줄이 되므로, 디자인이 원하면 별도 요청이 필요하다.
4. **②는 XLT가 아니다** — 티켓에 `XLT`로 표기됐으나 실제 관리처는 LPC(`my_common_info.benefit_more`)다. 반영 경로가 다르므로(릴리즈 불필요) 티켓 코멘트로 회신을 권장한다.

## 통과 판정

- **P0 = 0건**(자동 + 수동) → 산출물 생성·반영 진행 가능
- P1 1건은 위 (c)의 판정대로 **사용자 확인 대기**(임의 반영하지 않음)

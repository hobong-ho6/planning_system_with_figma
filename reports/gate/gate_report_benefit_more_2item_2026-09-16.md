# 번역 품질 게이트 — LPC `benefit_more` 2항목 재편 (2026-09-16)

- **대상**: LPC `my_common_info` · `benefit_more` (UIT beta `n7nue…` / prod `lkyus…` · postId 1)
- **변경**: `items[]` **4건 → 2건** (`unlimited_mission`·`korea_travel_cashback` 제거) · `luckyball` **문구 교체**
- **근거**: Figma `73400-11597`(`지갑 연결 전 - 포인트없음` 하위 프레임) 화면 실측 · Slack `C07R7CE0B5H` 요청
- **검토 범위**: 이 필드의 **전체 행 전수 점검**(title 1 + items 2 = **3행 × 5개 언어 = 15셀**). 신규·변경 행으로 한정하지 않았다.

## (1a) 한국어 원문 교정

| Figma 원문 | 등록 문구 | 판정 |
|---|---|---|
| `일일 미션하고 JPYC 받기` | 동일 | 교정 없음 (기존 등록값과 일치) |
| `매일 출석체크하고 럭키볼 받기` | **동일 (원문 유지)** | 아래 근거로 **교정하지 않음** |

- `출석체크`(붙임) ↔ `출석 체크`(띄움) — XLT 등록값 실측 **10건 : 10건**으로 어느 쪽도 우세하지 않다. 규범(명사+외래어 명사 띄어쓰기)으로는 `출석 체크`가 맞으나, **붙임 표기도 실사용 10건**이고 Figma 원문이 붙임이라 **원문을 유지**한다. 표기 통일은 (d-1) 용어집 백로그로 올린다.
- **alias 없음** — 원문과 등록 문구가 같아 `{Figma 원문 → 등록 문구}` alias가 발생하지 않았다.

## (a) 자동 검증 요약 — `validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | 1건 |
| 🟢 P2 | 0건 |

## (b) 수동 3단계 체크표 (전체 행 15셀)

**1단계 — 한국어 원문**

| 행 | 점검 | 결과 |
|---|---|---|
| title `추가 혜택 받기` | 맞춤법·띄어쓰기·표기 | ✅ 이상 없음 (변경 없음) |
| `daily_mission` | 맞춤법·띄어쓰기 | ✅ 이상 없음 (변경 없음) |
| `luckyball` | 맞춤법·띄어쓰기·조사 | ✅ `매일 출석체크하고 럭키볼 받기` — 붙여쓰기 누락·음차 오염 없음 |

**2단계 — 용어집 대조**

| 용어 | 용어집 등재값 | 적용 | 결과 |
|---|---|---|---|
| 럭키볼 | en `Lucky Ball` · ja `ラッキーボール` · th `ลัคกี้บอล` · zh `幸運球` | 전 언어 그대로 | ✅ 일치 |
| 일일 미션 | en `Daily Mission` · ja `毎日のミッション` … | 기존 등록값 유지 | ⚠️ ja만 불일치 → (c) 참조 |
| 출석·체크 | **미등재** | 등록값 실측으로 판단 | → (d-1) |

**3단계 — 다국어 의미·표기**

| 언어 | 신규 문구 | 실측 근거 | 판정 |
|---|---|---|---|
| ja_JP | `毎日チェックインしてラッキーボールをゲット` | `チェックイン` **17건** vs `出席` 7건 · 형제 항목과 `〜をゲット` 어미 일치 | ✅ |
| en_US | `Check in daily and get a Lucky Ball` | `Lucky Ball` **143건** vs 소문자 15건 · 형제 항목 `…and get JPYC`와 병렬 | ✅ |
| th_TH | `เช็คอินทุกวันรับลัคกี้บอล` | `ลัคกี้บอล` **69건**(용어집 등재) vs `ลูกบอลนำโชค` 50건 · 형제 항목 `…รับ JPYC` 병렬 | ✅ |
| zh_TW | `每日簽到領取幸運球` | `幸運球` **163건** · `簽到` **19건** vs `出席` 3건 · `領取幸運球`는 등록값 선례 있음 | ✅ |

- 숫자·변수 없음 → **zh 공백 규칙(한자↔숫자 경계) 해당 없음**. 이질 문자체계 혼입 0 · placeholder 0 · 마침표 정책 해당 없음(형제 항목 전부 마침표 없음, 일관).

## (c) 자동 검증 각 건 처리 판정

| # | 심각도 | 내용 | 처리 판정 |
|---|---|---|---|
| 1 | P1 | `daily_mission` ja_JP: 용어집 `일일 미션 → 毎日のミッション` 권장인데 등록값은 `デイリーミッション` | **정책 보류 — 이번 변경 범위 밖.** ⓐ 이 행은 **변경 대상이 아니고**(기존 등록값 유지) ⓑ 실측에서 `デイリーミッション` **6건** vs `毎日のミッション` **4건**으로 등록값이 오히려 우세하다. 지금 고치면 형제 서비스 표기와 어긋난다 → (d-1)로 올린다 |

**P0 = 0건 (자동 + 수동 모두)** → 출력·LPC 반영 진행 가능.

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

**(d-1) 용어집 보완 권장 → `md/glossary-backlog.md` 대기 목록 등재**

| 용어 | 유형 | 권장 표기 | 실측 |
|---|---|---|---|
| 출석 체크 | 미등재 · 표기 2갈래 | **실측 필요** — 확정 전 보류 | `출석체크` 10건 : `출석 체크` 10건 (Unifi+DP 2026-09-16) — **동률이라 권장값을 정할 수 없다** |
| 일일 미션(ja) | 등재값 ↔ 실사용 충돌 | ja `デイリーミッション` 재검토 | `デイリーミッション` 6건 vs 등재값 `毎日のミッション` 4건 |

**(d-2) 추가 개선·제안 안내**

1. **`korea_travel_cashback` 제거의 영향** — 이 항목은 `지갑 연결 전 - 포인트없음` 변형에서 노출되던 **K-Pick 구매 유도 동선**이다. 제거하면 그 동선이 사라진다. 요청자는 「3개 항목」으로 인식하고 있어 **4번째 항목의 존재를 모를 가능성**이 있다 → Slack 회신에 명시했다.
2. **동선(목적지)은 이 변경으로 바뀌지 않는다** — LPC 항목 스키마가 `id`·`title`·`iconUrl`뿐이라 링크 필드가 없다. 「하나를 NEXT Bay 미션으로」는 **FE의 `id` 분기 작업**이 필요하다.
3. **GA 이벤트 정리 권장** — 현재 위키는 항목별 고정 이벤트 3개(`click_benefit_mission`·`_unlimited`·`_luckyball`)인데 이 영역은 LPC 가변 목록이다. `md/GA.md` 규칙대로 `click_benefit_more` + `benefit_more_id` 파라미터 통합이 맞다(사용자 결정 사항).
4. **`luckyball` 아이콘** — `unlimited_mission` 제거로 미사용이 되는 아이콘이 생긴다(이미지 자체는 LPC 미디어에 남는다). `daily_mission`과 `korea_travel_cashback`이 **같은 아이콘 파일**을 쓰고 있었다.

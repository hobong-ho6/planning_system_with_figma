# 게이트 리포트 — 클리닉 브릿지·상세페이지 XLT 54키 (2026-09-01)

> **증보 (2026-09-01 오후) — 신규 1키 추가로 총 55키**
> 사용자가 위키에 `UF_clinic_link_send`(= `병원 정보를 LINE으로 보냈어요.` · 병원 링크 저장 후 LINE 발송 토스트)를 추가해, 5개 언어 번역을 수행했다.
> - **번역**: ja `クリニック情報をLINEでお送りしました。` · en `We've sent the clinic information via LINE.` · th `เราส่งข้อมูลคลินิกให้ทาง LINE แล้ว` · zh `已透過 LINE 傳送診所資訊。`
> - **근거**: 형제 키 `UF_voucher_link_send`(시스템 등록값 — `該当商品の購入リンクをLINEでお送りしました。` 계열)의 문형을 따르고, 클리닉 도메인 표기는 이번 배치에서 등록된 확정값(ja `クリニック` · en `clinic` · th `คลินิก` · zh `診所`)을 재사용했다.
> - **중복 대조**: 레지스트리 2,419키 전수에서 동일 `ko_KR` **완전일치 0건** → 신규 키 생성 타당.
> - **검증**: 자동 재실행 P0 **0건** / P1 41 / P2 10. 신규 키 지적은 **P2 마침표 1건뿐이며, 형제 키도 마침표를 쓰므로 정책 유지**.
> - **엑셀**: `xlt/xlt_output_20260901165709.xlsx` — **51키**(신규 47 + 재사용 3 + 신규 1). 구 50키본은 위키 첨부에서 삭제해 최종본만 남겼다.
> - **등록 상태 실측**: 이전 54키 배치는 **시스템 등록 완료**(레지스트리 2,358 → 2,419키)로 확인됐다. 따라서 이번 업로드의 실질 신규 등록 대상은 `UF_clinic_link_send` **1키**다.
> - **미반영 항목(보고)**: 이 키는 사용자가 **전역 「다국어 번역」 표에만** 추가했고 Screen 표 XLT 컬럼에는 없다. 토스트라 화면상 고정 위치·번호가 없어 그대로 두었으니, Screen 표에도 넣으려면 알려주기 바란다.

- **대상 위키**: [pageId 4667512757](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4667512757) 「클리닉 예약 동선 개선」
- **대상 Figma**: 파일 `GOCHAYBS7hIrmWRGNuJOKV`(Web3) — `(Web) 브릿지 페이지`(`69630:13293`) · `(Unifi mini) 상세페이지`(`69637:14698`). 2026-09-01 03:31 기준 원본 재조회(캐시 미사용)
- **담당 FE 팀**: **UIT** — 프리픽스 `UF_` · 치환자 `{{0}}`(이중 중괄호)
- **범위**: **54키 × 5개 언어 = 270셀 전수** 수동 검토. 내역 = **신규 번역 47키** + **재사용 7키**(등록값·참조 페이지 정의값 그대로, 재번역 안 함). 이 중 **50키를 업로드 엑셀에 수록**
- **산출물 엑셀**: `xlt/xlt_output_20260901132855.xlsx` — **50키 수록**(신규 47 + **미등록 재사용 3**). 시스템에 이미 등록된 재사용 4키(`UF_voucher_review_count`·`UF_send_guide_copy_btn`·`UF_main_faq_title`·`UF_voucher_recommend_title`)만 중복 업로드 방지를 위해 제외했다(2026-09-01 사용자 확정: 미등록 재사용 키는 엑셀에 포함)
- **레지스트리 조회 시각**: 2026-09-01 (Unifi / WEB BROWSER / 2,358키, 매 실행 재조회)

## 사용자 확정 사항 (진행 전)

| 항목 | 확정 |
|---|---|
| Screen ID | **프레임 이름으로 교체** — `(Web) 브릿지 페이지` · `(Unifi mini) 상세페이지`. Screen 표 + 법무 준수 체크표의 「적용 화면 (Screen ID)」 컬럼까지 일괄 교체, History 이력 행은 보존 |
| 키 재사용 | 참조 페이지 [4639704548](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4639704548)에 동일 문구가 있으면 그 키를 사용 + **시스템 등록 상태 더블체크** |
| 번역값 | **키가 달라도 내용이 같으면 시스템 등록 다국어 값을 사용** |
| 리뷰 키 충돌 | 탭 라벨(No 15)에 `UF_voucher_review_count` 배치(등록값 `리뷰`와 일치) · 섹션 제목(No 51)은 신규 `UF_clinic_detail_review_title` = `리뷰 ({{0}})` |
| FAQ 제목 | `UF_main_faq_title` **재사용**(등록됨) — 신규 키 생성 시 중복 방지 |
| 유의사항 | `UF_voucher_detail_caution` 유지(참조 페이지 정의) — 시스템의 `UF_lockup_caution_title`과 값은 같으나 도메인 분리 |

## (1a) 한국어 원문 교정 (번역 전 필수)

Figma 원문에 맞춤법·띄어쓰기 문제가 없어 **교정 0건**이며, 따라서 `{Figma 원문 → XLT Key}` **alias도 0건**이다. 다만 아래 1건은 **원문을 임의로 바꾸지 않기 위해 되돌린 초안 오류**로 기록한다.

| # | 키 | 초안(오류) | 확정 | 사유 |
|---|---|---|---|---|
| 1 | `UF_clinic_bridge_subtitle` | `최대 {{0}}% 캐시백 혜택까지` | **`최대 15% 캐시백 혜택까지`**(Figma 원문 그대로) | 이 문구에는 xlt 코멘트의 변수 지정이 없다. **변수화는 사용자 확인 후에만**(`md/translate.md` Step 4)이라 초안의 임의 변수화를 되돌렸다 |

**변수화가 적용된 키(디자이너 코멘트 지정분만)**: `UF_clinic_bridge_card_cashback`(`cashbackRate`) · `UF_clinic_detail_benefit_cashback`(답글 `{{0}} = cashbacRate`) · `UF_clinic_detail_process_step4_title/_desc` · `UF_clinic_detail_header_review` · `UF_clinic_detail_review_title`.
※ 코멘트 답글의 `cashbacRate`는 **`cashbackRate`가 맞는 표기**로 확인됐다(2026-09-01 사용자 확정). **위키에만 정정 표기하고 Figma 원본 수정 요청은 하지 않는다.**

## 키 재사용 · 시스템 등록 더블체크 (사용자 지시 이행)

### 재사용 7키 — 재번역하지 않고 기존 값 사용
| No | 키 | 출처 | 등록 상태 |
|---|---|---|---|
| 11 | `UF_voucher_mini_certified` | 참조 페이지 정의값 | 미등록(참조 페이지 작업에서 등록 예정) |
| 15 | `UF_voucher_review_count` | **시스템 등록값** | ✅ 등록됨 — `리뷰`/`レビュー`/`Reviews`/`รีวิว`/`評論` |
| 26 | `UF_send_guide_copy_btn` | **시스템 등록값** | ✅ 등록됨 — `복사`/`コピー`/`Copy`/`คัดลอก`/`複製` |
| 44 | `UF_main_faq_title` | **시스템 등록값** | ✅ 등록됨 — `자주 묻는 질문`/`よくある質問`/`FAQ`/`คำถามที่พบบ่อย`/`常見問題` |
| 47 | `UF_voucher_detail_cancel` | 참조 페이지 정의값 | 미등록 |
| 49 | `UF_voucher_detail_caution` | 참조 페이지 정의값 | 미등록(단, 동일 문구의 `UF_lockup_caution_title`은 등록됨 — 값 동일) |
| 55 | `UF_voucher_recommend_title` | **시스템 등록값** | ✅ 등록됨 — 값 일치 |

### 신규 47키 — 중복 등록 방지 대조 결과
- **시스템 2,358키 전수 대조**: 동일 `ko_KR` 완전일치 **0건**, 치환자·숫자만 다른 준일치 **0건**, 유사도 0.85+ **0건** → **중복 키 생성 위험 없음**
- **참조 페이지 46개 번역행 대조**: 완전/정규화 일치 **0건**
- 대조 과정에서 발견된 중복 위험 4건은 위 재사용 결정으로 **전건 해소**(리뷰·복사·FAQ·유의사항)

### ⚠️ 대조 중 발견한 시스템↔위키 불일치 (이번 작업 범위 밖 — 보고만)
| 키 | 시스템 등록값 | 참조 페이지 위키 표기 |
|---|---|---|
| `UF_voucher_review_count` | `리뷰` | `리뷰 (33)` |
| `UF_voucher_review_all` | `리뷰 전체보기 (33개)`(리터럴) | `리뷰 전체보기 {{0}}`(변수) |

→ 참조 페이지(4639704548)의 위키 표기와 실제 등록값이 어긋나 있다. **2026-09-01 사용자 확정: 참조 페이지는 이번 작업 범위에서 제외(무시) — 별도 정정하지 않는다.** 사실 기록으로만 남긴다.

## (a) 자동 검증 요약 — `scripts/validate_translation.py` 실제 실행

```
python3 -c "TranslationValidator('xlt/xlt_output_20260901132855.xlsx','scripts/glossary.json').run()"
```

| 심각도 | 건수 | 판정 |
|---|---|---|
| 🔴 P0 | **0건** | — |
| 🟡 P1 | 41건 | 전건 판정 완료 — **실제 결함 0건**(아래 (c)) |
| 🟢 P2 | 9건 | 마침표 스타일 — 원문 준수로 정책 유지 |

## (b) 수동 3단계 체크표 (270셀 전수)

### 1단계: 한국어 맞춤법·띄어쓰기 (54셀)
| 검사 | 결과 |
|---|---|
| 오타·맞춤법(패턴 사전) | 발견 없음 |
| 띄어쓰기(조사·의존명사·보조용언) | 이상 없음(`받아보세요`·`돌려받아요`·`확인해 주세요` 정상) |
| 비표준 공백(`\xa0`)·이중 공백·제어문자(U+2028/2029) | 0건 |
| 단어 붙음 누락(`누리고다양한`류) | 0건. `무료상담`은 Figma 원문 표기 유지(→ (d-2) #2로 통일 제안) |
| 외래어 음차 오염(`포이가츠`류) | 0건 |
| 구분자 일관성(`·`) | `피부·성형`·`상담·시술` 모두 가운뎃점(U+00B7) 통일 ✓ |

### 2단계: 용어집 위반 (v4.7 라이브 116용어)
| 용어 | 판정 |
|---|---|
| 캐시백 | ja `キャッシュバック`·en `cashback`·th `เงินคืน`·zh `現金回饋` — 등재값 준수 ✓ |
| 바우처 | ja `バウチャー`·en `voucher`·th `เวาเชอร์`·zh `優惠券` — 준수 ✓ |
| 포인트 | th `คะแนน`·zh `點數`·ja `ポイント` — 실측 다수 표기 준수 ✓ |
| 혜택·결제·확인·교환·최대·가격 | (c)에서 전건 오탐 판정 — 형제 키/등록 선례 우선 |
| 예외 패턴(브랜드) | `LINE`·`Unifi`·`JPYC`·`GuideKim`·`Google Maps`·`K-Beauty` 전 언어 원어 유지 ✓ |
| `deprecated_terms` | 0건 ✓ |

### 3단계: 언어별 자연스러움 (216셀)
| 언어 | 점검 | 결과 |
|---|---|---|
| ja_JP | です・ます체 일관 · 숫자+단위 붙임(`{{0}}%`) · 조사 앞 공백 · 한국어 직역 잔존 | 이상 없음. 클리닉 도메인은 `クリニック`·`施術`·`相談`으로 통일 |
| en_US | Sentence case 라벨 · 관사·단복수 · 브랜드 대소문자 | 이상 없음. `No prepayment is required; you pay after the treatment.`는 초안의 em dash를 세미콜론으로 정리 |
| th_TH | 문말 마침표 미사용 관습 · 의미 단위 띄어쓰기 · 숫자+단위 | 마침표 0건 ✓ · `ทรีตเมนต์`(시술)·`คลินิก`(클리닉)·`คะแนน`(포인트) 일관 |
| zh_TW | 번체 정자(`診`·`療`·`諮`·`饋`·`點`·`優`) · 간체 혼입 · 이형자(`產`/`臺`) | 간체 0건 · 이형자 0건 ✓ |
| ko_KR | (1단계 전수 완료) | 교정 0건(초안 변수화 1건 되돌림) |
| 컬럼 정렬 | 이질 문자체계 혼입 | `UF_clinic_detail_exclusive_badge` 1키(전 언어 영문 배지, 의도) 외 **0건** |
| 치환자 | `{{0}}` 개수 5개 언어 일치 | 6키 전부 일치 ✓ |
| 개행 | `\n` 개수 5개 언어 일치 | 전 키 개행 0 — 일치 ✓ |

## (c) 검증기 보고 건별 처리 판정

### 🟡 P1 41건 — 전건 오탐/정책 예외 (실제 결함 0건)
| 유형 | 건수 | 판정 근거 |
|---|---|---|
| `혜택` → en `perks` / th `สิทธิประโยชน์` / zh `優惠` | 15 | **오탐** — 대상은 복합어 「캐시백 혜택」이고 등록 선례(`UF_max_cashback`: en `Up to {{0}} cashback on payment` · th `เงินคืนสูงสุด {{0}}` · zh `消費最高享 {{0}} 現金回饋`)가 모두 「혜택」을 별도 어휘로 넣지 않는다. 용어집의 단독 매핑이 복합어에 과적용된 사례 |
| `결제` → zh `結帳` / th `การชำระเงิน` / en `Payment` | 8 | **오탐** — 2026-08-24 결정(용어집·등록값 충돌 시 형제 키 정본 채택)에 따라 `付款`·`ชำระเงิน` 계열 유지. th·en은 동사/명사 품사 차이 |
| `확인` → en `check` / th `ตรวจสอบ` / zh `查看` | 6 | **오탐 확정** — `md/check.md` 함정 2-1 다의어 유형, **재제안 금지** 항목 |
| `교환` → en `swap` / ja `スワップ` / th `แลกเปลี่ยน` / zh `交換` | 6 | **오탐(도메인 충돌)** — 용어집 「교환」은 **암호자산 스왑**. 여기서는 포인트→JPYC 교환이며 등록 선례 `UF_jpyc_guide_q3_title`(ja `交換` · en `Exchange`)를 따랐다 |
| `최대` → en `Max` / zh `最多` | 2 | **오탐** — 문장형은 `Up to`·`最高`가 정본(선행 게이트 2026-08-24·08-25 확정) |
| `가격` → zh `售價` | 1 | **오탐** — 문장 내 「가격을 안내」이며 형제 키 `UF_voucher_price_original` zh `原價` 계열과 정합 |
| `UF_clinic_detail_exclusive_badge` ko 칸 라틴 문자열 | 1 | **정책 예외(의도된 영문 배지)** — 디자인상 `EXCLUSIVE BENEFIT` 영문 고정. 전 언어 동일 값. 한글화 여부는 (d-2) #1 |
| ko `구매하기` → en `Buy now` | 1 | **오탐** — `상품 구매하기`는 포인트 사용처 설명 라벨이라 `Buy products`가 정확. `Buy now`는 즉시 구매 CTA용 |
| ko `유의사항` → th `หมายเหตุ` | 1 | **오탐** — 엑셀에 `UF_voucher_detail_caution`을 추가하며 발생. `ข้อควรระวัง` 채택(실사용 6건 vs `หมายเหตุ` 3건 · `UF_lockup_caution_title` 선례). 2026-08-26 게이트와 동일 판정 |

**실제 위반 0건 / 오탐 40건 / 정책 예외 1건.**

### 🟢 P2 9건 — 정책 유지
문장형 ko 9건에 마침표가 있고 일부 명사구 라벨에는 없다는 지적. **Figma 원문의 마침표 유무를 그대로 따랐다**(`md/check.md` B-7 기준). 원문 자체의 통일은 (d-2) #2로 제안.

## (d) 추가 개선·제안 (권장 — 사용자 결정 전 임의 반영 금지)

### (d-1) 용어집 보완 권장 — 실측 건수 병기
| 용어 | 권장 표기(실측 근거) | 근거 |
|---|---|---|
| **클리닉** | ko `클리닉` · ja `クリニック` · en `clinic` · th `คลินิก` · zh `診所` | 미등재인데 이번 54키 중 12키에서 사용. `병원`/`클리닉`이 원문에 혼재해 표기 기준이 필요하다 |
| **시술** | ko `시술` · ja `施術` · en `treatment` · th `ทรีตเมนต์` · zh `療程` | 미등재. 2026-08-24 게이트에서도 이월된 항목(`mini_guidekim_specialty_clinic` 등 8키 이상 사용 중) |
| **혜택(복합어)** | 「캐시백 혜택」처럼 복합어일 때는 별도 어휘를 넣지 않는다는 **예외 규칙** 추가 | 검증기가 이번에만 15건을 P1로 반복 오탐 — 매핑이 좁다는 신호 |
| **교환(포인트↔토큰)** | 용어집 「교환」에 **암호자산 스왑 한정** 주석 추가 | 이번에 6건 오탐 유발. 포인트 교환 문맥은 `Exchange`·`交換`이 정본 |

> 실사용 0건 표기는 권장하지 않았다(`md/check.md` 함정 2-1). 용어집은 API 읽기 전용이므로 반영은 `md/landpress.md` 절차로 사용자가 CMS에 붙여넣는다. **승인 시 기획자 가이드 zip 동반 갱신 필수**.

### (d-2) 원문·구조 개선 제안
1. **`EXCLUSIVE BENEFIT` 영문 배지** — 전 언어 영문 고정으로 등록했다. ja/th/zh 사용자에게도 영문 노출이 의도인지 확인이 필요하며, 현지화한다면 5개 언어 값을 다시 정해야 한다.
2. **원문 표기 통일 제안** — ⓐ `무료상담`(붙임) ↔ `무료 LINE 상담`(띄움) ⓑ `병원` ↔ `클리닉` 혼재(같은 대상) ⓒ 문장 마침표 유무 혼재. 디자이너·기획 확인 후 통일 권장.
3. **코멘트 표기 2건** — ⓐ 상세 No 10 답글 `cashbacRate` → **`cashbackRate`가 정본**(사용자 확정). 위키에만 정정 반영했고 Figma 원본 수정 요청은 하지 않는다 ⓑ 브릿지 No 14 코멘트가 지정한 `결제금액의 {{0}}%`는 「캐시백」이 빠져 있어 화면 표기(`결제금의 캐시백 15%`)를 정본으로 채택했다(사용자 확정).
4. **캐시백 문구 표기 차이 유지 결정** — 브릿지 `결제금의 캐시백 {{0}}%` ↔ 상세 `{{0}}% 캐시백 혜택`은 원문 통일하지 않기로 이미 결정(2026-09-01). 번역도 각 표기를 그대로 반영했다.
5. ~~참조 페이지 위키↔시스템 불일치 정정~~ — **종결(2026-09-01 사용자 확정)**: 참조 페이지는 무시하기로 했다. 발견 사실은 위 표에 기록만 유지.
6. ~~미등록 재사용 키 3건의 업로드 주체 확인~~ — **해소(2026-09-01 사용자 확정)**: `UF_voucher_mini_certified`·`UF_voucher_detail_cancel`·`UF_voucher_detail_caution`을 **이 페이지 엑셀에 포함**했다(50키). 등록된 재사용 4키는 중복 업로드 방지로 계속 제외한다.

## 통과 판정

- **P0 = 0건** (자동 + 수동 270셀 전수)
- P1 40건·P2 9건 전건 처리 판정 완료 — **실제 결함 0건**(오탐 39 · 정책 예외 1 · 정책 유지 9)
- (d) 권장 사항은 사용자 결정 전까지 임의 반영하지 않는다
- → **위키 반영·엑셀 전달 가능**. 시스템 업로드는 쓰기 API가 없어 **사용자가 직접 수행**한다

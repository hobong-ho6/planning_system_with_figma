# 번역 검증 리포트 — 키드 문구 띄어쓰기 교정 (친구 초대 링크 복사)

- 대상 위키: pageId=4479306980 · 팀 **LV**
- 범위(증분): 키 `mini_luckyball_copy_invite_link`의 ko_KR 띄어쓰기 교정 1건 + 25034 정책 번호 순서 재정렬(번역 변경 아님, Description 정렬만)

## 검토 범위 (전수 점검 명시)
번역 변경은 **1개 키 전체 × 5개 언어를 한 줄씩 전수 수동 검토**. 기존 65개 키는 직전 run에서 전수 검증·불변. 25034 정책 순서는 좌표(절대 y·x) 기준 재정렬로 번역 내용 무변경.

## 한국어 원문 교정 (1a) 및 alias
- `친구초대 링크 복사` → `친구 초대 링크 복사` (`친구초대` 붙은 단어 → `친구 초대` 분리, 사용자 지시)
- alias: `친구 초대 링크 복사 → mini_luckyball_copy_invite_link` 추가(기존 `친구초대 링크 복사` alias도 보존 — Figma 원문 매핑).
- en/ja/zh/th는 이미 분리 표기(Copy invite link/友だち招待リンクをコピー/複製邀請好友連結/คัดลอกลิงก์ชวนเพื่อน) → 변경 없음.

## Executive Summary (자동 검증 — validate_translation.py, 66항목)
| 심각도 | 건수 | 판정 |
|--------|------|------|
| 🔴 P0 | 0건 | 없음 |
| 🟡 P1 | 24건 | 전건 오탐(직전 run과 동일 — 용어집 협소 매핑). 변경 키는 P1 0건 |
| 🟢 P2 | 26건 | 전건 오탐(타이틀·버튼 마침표 미사용). 변경 키는 P2 해당 없음 |

## 수동 3단계 체크표 (변경 키 1개 × 5언어)
### 1단계: 한국어
- `친구 초대 링크 복사`: 붙은 단어 해소, 오타·표현 이상 없음.
### 2단계: 용어집
- `복사`=copy 대역 정상(용어집 일치). zh `複製` 정자, 간체 없음. 위반 없음.
### 3단계: 다른 언어
- 빈칸 0. 언어 혼입 0. placeholder 없음. 컬럼 swap 없음. ja 직역 없음. 타 언어 무변경(ko만 교정).

## 검증기 보고 각 건 처리 판정
- 변경 키: 자동/수동 **실제 위반 0건**. 전체 P1 24·P2 26: **전건 오탐**(현행 유지, 직전 run과 동일 사유).

## 통과 판정
- **P0 = 0건 (자동 + 수동)** → 엑셀 재생성·위키 반영 진행. P1/P2 전건 오탐(유지).

## 추가 개선·제안 (권장 — 사용자 결정)
- **(d-1) 용어집**: 반복 오탐(확인 버튼=OK/변경=變更/송금=匯款) 확장 여지 — 필요 시 진행.
- **(d-2)**: 25034 정책 번호는 절대좌표(y밴드→x) 기준으로 확정(사용자 승인). 이미지 ⓝ=정책 번호로 전환, XLT 번역표는 별도 참조표(문구 위치)로 유지.

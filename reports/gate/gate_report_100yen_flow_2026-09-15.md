# 게이트 리포트 — 100엔딜 플로우 전면 재정의 (위키 4725963532)

- **작업일**: 2026-09-15
- **대상**: Figma `Web3` SECTION **`100엔딜 플로우` `74574:13425`** — 프레임 13개 중 **코멘트가 달린 6개**
- **모드**: 코멘트 선별 — 기존 위키의 화면·XLT를 **전부 제거하고 재작성**(사용자 지시)
- **담당 FE 팀**(사용자 확정): **`Home - 100엔딜` = LV** / **나머지 5화면 = UIT**
- **산출물**: `xlt/xlt_output_20260915184209.xlsx` (신규 15 + 값 갱신 1 = **16행**)
- **조달(전부 이번 run 신규 조회 · 캐시 미사용)**: Figma 노드·코멘트 **2회**(사용자 「업데이트 있음」 통보로 재조회) · XLT 등록값 Unifi 2,569 / Dapp Portal 1,696 · 용어집 API v5.5
- **검증 범위**: **전체 파일 전수** — 16키 × 5개 언어 = **80셀 전건** 수동 검토

---

## 0. 대상 화면 6개 (코멘트 있는 프레임만)

| # | 프레임 | node-id | 코멘트 | XLT | 팀 |
|---|---|---|---:|---:|---|
| 1 | `Home - 100엔딜` | `74574:7133` | 3 | 2 | **LV** |
| 2 | `100앤딜 상세 - OA Follow X` | `74574:8127` | 16 | 15 | UIT |
| 3 | `100앤딜 상세` | `74574:7790` | 1 | 1 | UIT |
| 4 | `결제하기` | `74574:8549` | 1 | 0 | UIT |
| 5 | `결제 실패(기본)` | `74574:9244` | 2 | 2 | UIT |
| 6 | `결제 완료` | `74574:9259` | 1 | 0 | UIT |

제외 7개(코멘트 0): `OA 친구추가` · `(OA)mission complete` · `(IAB)GuideKim - My - 상품상세` · 무명 프레임 4개(라벨 2개 포함).

⚠️ **재조회로 잡힌 변경** — 첫 수집 때 11프레임이던 것이 **13개**로 늘고, `100앤딜 상세`에 코멘트가 **0 → 1건** 생겼다. 사용자 통보가 없었다면 5화면으로 끝날 뻔했다.

⚠️ **`xllt` 오타 마커** — `100앤딜 상세`의 새 코멘트 본문이 `xllt`(`xlt` 오타)여서 `collect_frames`의 마커 판정에서 빠졌다. 핀이 가리키는 텍스트가 `1,000엔 쿠폰 적용중`으로 명백해 **사용자 확정으로 XLT 대상에 포함**했다. Figma 코멘트 수정은 디자인 측 사안으로 남긴다.

## 0-1. 키 구성 — 신규 15 · 재사용 2 · 값 갱신 1

**재사용 2건**(정규화 대조로 확인 — `\xa0`·공백 차이 흡수)

| Figma 원문 | 재사용 키 |
|---|---|
| 꼭 확인해 주세요 | `UF_100yen_caution_title` |
| 보너스 미션 및 보상의 경우,&nbsp;최대 보유 한도는… | `UF_100yen_caution_contents3` |

**값 갱신 1건**(사용자 확정) — `UF_100yen_detail_badge` ja `新規会員 初回購入特典` → **`新規会員・初回購入特典`**(Figma 구분자 `・` 반영). 재업로드 대상.

**공유 키 1건**(사용자 확정) — `UF_100yen_title`(韓国人気バウチャー)은 **Home(LV/Dapp Portal)과 상세(UIT/Unifi) 양쪽에 같은 키 이름으로 등록**한다. 서비스 키스페이스가 분리돼 충돌하지 않는다. 「최근 본 상품」이 화면만 LV이고 키는 `UF_`인 기존 예외와 같은 유형이다.

## (1a) 한국어 원문 교정

| Figma 원문 | XLT 값 | 사유 |
|---|---|---|
| Unifi 채널 LINE **메세지**로 받은… | …LINE **메시지**로… | 맞춤법(메세지→메시지) |
| 오프라인 매장에서 **결제시** | 오프라인 매장에서 **결제 시** | 의존명사 띄어쓰기 |
| 1,000엔 **쿠폰** 받기 위해\nUnifi 친구 추가가 필요해요 | 1,000엔 **바우처를** 받으려면\nUnifi 친구 추가가 필요해요 | 용어집 정본 「바우처」 · 조사 보강 |
| 쿠폰은 1계정당 1장, 1회**의 구입으로 사용이 됩니다** | 쿠폰은 1계정당 1장, 1회 **구매에만 사용됩니다** | 번역투 정리(구입→구매 · 피동 축약) |
| 대상 상품은 1인당 1점만 **구입**하실 수 있습니다 | …**구매**하실 수 있습니다 | 표기 통일 |
| 가격**・**재고 / 실제 **지불** 금액 / **확인하십시오** | 가격**·**재고 / 실제 **결제** 금액 / **확인해 주세요** | 구분자 `·`(실측 28건 다수) · 「지불」→「결제」 · 종결어미 통일 |
| 1,000엔 쿠폰 **적용중** | 1,000엔 쿠폰 **적용 중** | 의존명사 띄어쓰기 |

**alias 기록** — 위 7건은 Figma 원문과 XLT 값이 계속 달라진다. **Figma 원문 수정 요청은 올리지 않는다**(프로젝트 종결 규칙).

---

## (a) 자동 검증 요약 — `validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | 19건 |
| 🟢 P2 | 3건 |

**통과 기준 P0 = 0 충족.**

---

## (b) 수동 3단계 체크표 — 전체 파일 전수 (16키 × 5언어 = 80셀)

### 1단계 — 한국어(ko_KR) 원문

전 16행 검토. (1a) 7건 교정 외 맞춤법·조사·표기 이상 없음. 숫자 표기(`1,000엔`·`100엔`·`1계정당 1장`) 정상.

### 2단계 — 용어집 대조 (v5.5)

| 용어 | 정본 | 적용 | 판정 |
|---|---|---|---|
| 바우처 | ko 바우처 / ja **クーポン** / en Voucher / th เวาเชอร์ / zh 優惠券 | ko·en·th·zh 준수 / **ja는 Figma 원문 유지**(`バウチャー`·`商品券`) | ⚠️ (c) 1·2번 |
| 구매 | Buy / 購入 / ซื้อ / 購買 | 문맥별 동사·명사형 사용 | (c) 오탐 판정 |
| 결제 | Payment / 決済 / การชำระเงิน / 付款 | 동일 | (c) 오탐 판정 |
| 공식 계정 | ja 公式アカウント / th บัญชีทางการ | th는 라틴 `Unifi Official Account` | (c) 오탐(기등록 관례 14건) |

### 3단계 — 다국어 의미·표현 (전 80셀)

| 언어 | 검토 | 판정 |
|---|---|---|
| ja_JP | Figma 원문 3건(`韓国人気バウチャー`·`新規会員・初回購入特典`·`Unifi公式アカウント…商品券が100円に！`)은 **원문 그대로**. 나머지는 선례 구문 승계(`~してください`·`~いただけます`) · 전각 `！` 관례 | ✅ |
| en_US | 금액은 `1,000 JPY` 형식(실측 다수) · `STEP 1/2`는 원문 유지 · 문장 부호·관사 정상 | ✅ |
| th_TH | `ชำระเงิน`(동사) 사용 — 실측 28 : 23으로 다수 · 라틴 브랜드 유지 | ✅ |
| zh_TW | 번체 정자 · **한자↔숫자 공백 1칸**(`1,000 日圓`·`限購 1 件`→`購買 1 件`) · `限購`은 **실사용 0건이라 `購買`로 수정**(아래 (c)) | ✅ 수정 후 정상 |
| ko_KR | (1단계 참조) | ✅ |

**언어 혼입·빈칸·placeholder**: 80셀 이상 없음. 변수 없는 문구라 placeholder 검사 대상 없음.

---

## (c) 검증기 보고 건별 처리 판정 (P1 19 · P2 3)

### 실제 위반 → 수정 (1건, 반영 완료)

| 건 | 조치 |
|---|---|
| `UF_100yen_caution_contents9` zh `每人限購 1 件` | **수정** — `限購`은 등록값 **실사용 0건**(`購買` 98건). 「실사용 0건 표기 권장 금지」(`md/landpress.md` §4 기준 4)에 따라 **`每人僅可購買 1 件`**으로 교체 |

### 정책 보류 → 사용자 확정 (3건)

| 건 | 판정 |
|---|---|
| `UF_100yen_title` ja `バウチャー`(용어 불일치 + 구 표기 #22) | **실제 위반이나 Figma 원문 유지** — 사용자가 「Figma 표기 그대로」로 확정. 등록값 실측은 `バウチャー` 0 : `クーポン` 31 |
| `UF_100yen_oa_follow_desc` ja `商品券` | 동상. `商品券`은 용어집 어느 표기와도 다르다 |

⚠️ 이 등록 이후 ja `バウチャー`·`商品券`이 시스템에서 **이 2키에만** 남는다. 표기 통일 시 대상이다.

### 오탐 → 유지 (15건)

| 건 | 판정 |
|---|---|
| en `구매`→`Buy` 4건(`step1_desc`·`contents8`·`contents9`·`detail_badge`) | **오탐** — `purchased voucher`·`a single purchase`·`first purchase benefit`는 명사·형용사형. `Buy`는 버튼 라벨 |
| th `결제`→`การชำระเงิน` 4건 | **오탐** — `ชำระเงิน`(동사) 실측 **28건** > `การชำระเงิน` 23건. 「~할 때/하기」 문맥은 동사형이 정상 |
| en `결제`→`Payment`(`step2_desc`) | **오탐** — `when paying`(동명사) |
| zh `확인`→`查看` 2건 | **오탐** — `確認`(129건)은 「확인해 주세요」 문맥의 정상 표현. `查看`은 「보기」 문맥 |
| ja `결제`→`決済`(`contents10`) | **오탐** — `お支払い金額`은 주의사항 정중체로 선례 6건. 같은 화면군 `UF_voucher_pay_fail_desc`도 `お支払い` 사용 |
| th `공식 계정`→`บัญชีทางการ` | **오탐** — 라틴 `Unifi Official Account`가 기등록 관례(실측 14건) |
| th `메시지`→`ข้อความ` | **오탐** — `ทาง LINE`으로 매체만 표기해 중복을 피했다 |
| zh `구매`→`購買`(`contents9`) | **해소** — 위 수정으로 `購買` 반영 |
| ko `STEP 1`·`STEP 2` 라틴 문자열 경고 2건 | **오탐** — Figma 원문이 영문 라벨이고 5개 언어 동일. 라틴 그대로 두는 선례 다수(`Contract`·`Token Name`·`STAGE {{0}}` 등) |

### P2 3건 → 유지

주의사항 3건의 마침표는 **문장형 고지라 정상**(같은 화면의 `caution_contents1~7` 기등록분도 전부 마침표 유지).

---

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

### (d-1) 용어집 보완 권장 — **대기 목록 등재 없음**

이번 P1의 대부분은 **등재값이 명사 단독인데 문장은 동사·의역**인 구조적 오탐이며, 해당 용어(결제·구매·확인)는 이미 v4.9~v5.5에서 A/B 실측을 거쳐 확정된 값이다. 추가 등재는 오탐을 늘린다. `md/glossary-backlog.md` 등재 대상 아님.

### (d-2) 추가 개선·제안 안내 (사용자 결정 사안)

1. **ja `バウチャー`·`商品券`이 용어집 정본(`クーポン`)과 어긋난다** — 위 (c) 정책 보류 2건. 표기 통일 시 이 2키가 대상이다.
2. **Figma 코멘트 오타 `xllt`** — `100앤딜 상세`. 마커 규칙(`xlt`)에 맞게 수정하면 다음 실행부터 자동 선별된다.
3. **기존 13키 중 9키가 이번 범위에서 빠졌다** — `UF_100yen_detail_title`(단돈 백엔딜)·`_desc`·`_usage_title`·`caution_contents1·2·4·5·6·7`·`mini_home_100yen_more`. 새 플로우 화면에 대응 문구가 없어 제외했으나 **등록값은 시스템에 남는다**. 구 화면이 완전히 내려가면 정리 대상인지 FE 확인 권장.
4. **`STEP 1`/`STEP 2`를 XLT 키로 둘지** — 5개 언어가 모두 동일해 번역 가치가 없다. 하드코딩 여지가 있으나 Figma에 `xlt` 마커가 달려 있어 키로 정의했다.

---

## 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 | **0건** ✅ |
| 수동 P0 | **0건** ✅ |
| P1 19 · P2 3 | 전건 처리 판정 완료(실제 위반 1 수정 · 정책 보류 3 사용자 확정 · 오탐 15 · P2 3 유지) ✅ |
| (d) 권장 사항 임의 적용 | 없음 ✅ |

→ **게이트 통과.** 위키 반영·엑셀 전달로 진행한다. **XLT 업로드는 사용자가 수행**한다.

# 게이트 리포트 — 100엔딜 딜페이지 공유하기 XLT 2키 신규 (2026-09-16)

- 요청: Slack 스레드(`C07R7CE0B5H` / `1789547763.296749`) — FE 박성철 님 요청 2건(툴팁 말풍선 · 공유 메시지)
- 대상 위키: [100엔딜 `4725963532`](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4725963532)
- 신규 키 **2건**(Unifi · UIT `UF_`) × 5개 언어 — 검증 대상 `xlt/xlt_output_20260916184450.xlsx` **전체 행(2행) 전수 점검**
- ⚠️ **Figma 미반영 요소** — 100엔딜 섹션의 `100앤딜 상세`(`74574-7790`)·`100앤딜 상세 - OA Follow X`(`74574-8127`) 두 프레임을 원본 조회했으나 **공유 버튼·툴팁 문구가 없다**. FE 구현 단계에서 추가된 요소라 이번에 문구를 신규 정의했다.

## 키 이름 — 선례 실측 (⛔ 등록 전이 유일한 기회)

| 키 | 근거 |
|---|---|
| `UF_100yen_share_tooltip` | `_tooltip` 접미 **21건**(Unifi) · 100엔딜 계열 프리픽스 `UF_100yen_` **18키** 기등록 |
| `UF_100yen_share_text` | 공유 본문 선례 **`UF_send_complete_share_text`**(Unifi)와 동일 명명 |

## (1a) 한국어 원문 교정 · 원문 기준

- **원문이 일본어다**(FE 제공 확정문). ko는 이번에 신규 작성했고, 기존 100엔딜 표기 관례에 맞췄다 — ja `クーポン` ↔ ko `쿠폰`(선례 `UF_100yen_caution_contents8`), 금액은 ko `1,000엔`.
- ja 두 건은 **제공된 확정문 그대로** 등록한다(위키에 「JA 확정문 그대로」 선례 있음).

| 키 | ko_KR | ja_JP(확정문) |
|---|---|---|
| `UF_100yen_share_tooltip` | 친구에게도 이 혜택을 알려주세요! | `友だちにもおトクを教えよう！` |
| `UF_100yen_share_text` | 한국 인기 상품을 1,000엔 쿠폰으로 저렴하게 살 수 있어요! | `韓国の人気アイテムが1,000円クーポンでおトクに買えます！` |

## (a) 자동 검증 요약 — `validate_translation.py`

| 심각도 | 건수 |
|---|---|
| 🔴 P0 | **0건** |
| 🟡 P1 | 1건 |
| 🟢 P2 | 0건 |

## (b) 수동 3단계 체크표 (전체 행 전수)

| 단계 | 점검 | 결과 |
|---|---|---|
| 1단계 (한국어) | 맞춤법·띄어쓰기·금액 표기(`1,000엔`)·느낌표 | ✅ 이상 없음 |
| 2단계 (용어집) | `쿠폰`·`혜택`·`친구`·`상품` 표기를 **등록값 실측**으로 맞춤 | ✅ 아래 교정 반영 |
| 3단계 (다국어) | 변수 없음 · 언어 혼입 없음 · 개행 없음(한 줄 문구) · 5개 언어 전건 채움 | ✅ 이상 없음 |

**초안에서 교정한 것(실측 근거)**
- zh 「친구」 `朋友` → **`好友`** — 실측 `好友` **88건** / `朋友` **0건**(실사용 0건 표기 금지 규칙)
- en 「혜택」 `deal` → **`benefit`** — 실측 `benefit` 38 / `deal` **0건**
- th 「혜택」 `ดีล` → **`สิทธิประโยชน์`** — 실측 60 / `ดีล` **0건**
- th 「상품」 `ไอเทม` → **`สินค้า`** — 실측 `สินค้า` 26 / `ไอเทม` 2

## (c) 검증기 보고 각 건 처리 판정

| # | 심각도 | 내용 | 처리 판정 |
|---|---|---|---|
| 1 | P1 | `UF_100yen_share_tooltip` ja_JP: 「혜택」 → `特典` 권장(현재 `おトク`) | **정책 보류 — 확정문 우선.** ja는 FE·마케팅이 확정해 준 문구이고, 100엔딜은 **JA 확정문을 그대로 쓰는 선례**(위키 History `還元` 유지)가 있다. 임의 교체하지 않는다 |

## (d) 추가 개선·제안 (권장 — 임의 반영하지 않음)

### (d-1) 용어집 보완 권장
- 신규 등재 없음. 「혜택」·「쿠폰」은 대기 목록에 이미 있고(#6·#19), 이번 실측치는 위 (b)에 남겼다.

### (d-2) 추가 개선·제안 안내
1. **「공유 메시지를 일본어로 고정할지」 질문에 대한 답(권장안)** — **XLT 5개 언어 등록을 권장**한다. ⓐ 선례 `UF_send_complete_share_text`가 5개 언어로 등록돼 있고 ⓑ LINE 공유 메시지는 **보내는 사람의 앱 언어**로 만들어지므로, 한국어·영어 UI 사용자가 공유할 때 일본어만 나가면 어색하다. 다만 **100엔딜이 일본 한정 프로모션**이라 수신자를 일본어 사용자로 전제한다면 FE가 `ja_JP` 값만 렌더해도 된다 — 키는 5개 언어로 제공하고 **고정 여부는 기획·마케팅 결정**으로 남긴다.
2. **Figma 미반영** — 공유 버튼·툴팁이 디자인 원본에 없다. 화면 정의(위키)에는 이번에 추가하지만, 디자인 동기화 여부는 디자인 담당 판단 사항이다.
3. **툴팁 노출 정책 미정의** — 최초 1회 노출인지, 닫기 조건이 있는지 정책이 없다. FE 구현 기준을 확인해 위키 Description에 남기는 것이 좋다.

## 통과 판정

- **P0 = 0건**(자동 + 수동) → 엑셀 산출·위키 반영 진행
- P1 1건은 (c) 판정대로 확정문 유지 · (d)는 사용자 결정 전까지 임의 반영하지 않음

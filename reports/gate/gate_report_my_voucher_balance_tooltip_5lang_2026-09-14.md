# 게이트 리포트 — My › 바우처 상세(사용가능) 잔액 툴팁 신규 키 1건 (5개 언어)

- 대상 프레임: Figma `GOCHAYBS7hIrmWRGNuJOKV` · **바우처 상세** `72193-11353` (위키 행 「바우처 상세 - 사용가능」)
- 위키: [pageId 4704515582](https://wiki.workers-hub.com/display/UNIFI/Unifi+mini+v2.0) · **My - UIT** 섹션
- 담당 FE: **UIT** — 프리픽스 `UF_` · 치환자 `{{0}}` (위키에 UIT/LV 구분이 있어 그대로 적용)
- 신규 **1키** × 5개 언어 = **5셀** · 기존 12키는 등록값 그대로(재번역 0건)
- 조달: Figma 노드·코멘트 **신규 조회**(`fetch_comments.py` reply-aware · 17스레드) · XLT 등록값 `Unifi / WEB BROWSER` **2,553키** 신규 조회 · 용어집 API **v5.5** 신규 조회 (전부 캐시 미사용)

---

## 0. 프레임 재조회 — 직전 기록(위키 v185) 대비 변경 3건

| 변경 | 내용 |
|---|---|
| 🆕 신규 `xlt` 마커 | y=723 → **「잔액은 반영 시점에 따라\n실제금액과 다를 수 있습니다.」**(툴팁 문구 · 신규 노드 `74154:24125`) |
| 🆕 신규 정책 코멘트 | y=689 → 「사용가능여부가 **일부사용가능**일 때 물음표 아이콘 노출 및 아이콘 선택 시 **툴팁 노출**」 |
| ✂️ 삭제된 코멘트 | y=580 「GuideKim에서 받고 있는지 확인필요」 |
| ℹ️ 샘플 값 변경 | 사용 가능 금액 `10,000원` → `7,000원`(노드 교체). 기존 키 `UF_voucher_detail_amount_value`(`{{0}}원`)가 그대로 커버 — **신규 키 불필요** |

나머지 마커 12건은 전부 기존 키로 커버돼 **신규 대상은 1건**이다.

## (1a) 번역 전 — 한국어 원문 교정

| 항목 | Figma 원문 | XLT Key KR (교정 후) | 사유 |
|---|---|---|---|
| 잔액 툴팁 | 잔액은 반영 시점에 따라<br>**실제금액**과 다를 수 있습니다. | 잔액은 반영 시점에 따라 **실제 금액**과 다를 수 있습니다. | 「실제 금액」은 관형어+명사라 **띄어쓰기**가 맞다 |

- **alias (Figma 원문 → XLT Key KR)**: `실제금액` → `실제 금액`
- 줄바꿈: Figma 원문은 2줄이나 **XLT 값은 1줄**로 둔다 — 같은 화면 형제 키와 동일 원칙(위키 2026-09-13 확정).
- **고유명사 조사 대상 없음** — 브랜드·기관·법령명 미포함.
- ※ 이 프레임의 `상품권` 구 표기는 **2026-09-14 사용자 확정으로 「Figma 원문은 무시」** 처리돼 교정 요청 대상이 아니다.

## 이름 선례 실측

| 항목 | 실측 |
|---|---|
| `_tooltip` 접미사 | 등록값에 **9건**(`UF_common_menu_asset_lockup_tooltip` · `UF_guide_jpyc_with_bank_rate_tooltip` 등) |
| 이름 충돌 | `UF_voucher_detail_balance_tooltip` — 2,553키에 **없음**(0건) |
| ja 「잔액」 표기 | 등록값 전건 **`残高`**(`UF_send_wallet_list_title` `マイ{{0}}残高` 등) — 그대로 따랐다 |

## (a) 자동 검증 — `validate_translation.py` P0/P1/P2 요약

용어집 v5.5(118 용어) 기준, 신규 1키 × 5개 언어.

| 심각도 | 건수 |
|---|---:|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

**P0 = 0 → 통과 기준 충족.**

## (b) 수동 3단계 체크표 — 5셀 **전수**

대상이 1키이므로 **전체 행 = 전수 점검**이다. 아울러 같은 프레임 **기존 12키 × 5개 언어 = 60셀도 등록값 기준으로 재확인**했다(변경 없음).

| 언어 | 값 | 1단계 원문·표기 | 2단계 용어집 | 3단계 다국어 |
|---|---|---|---|---|
| ko_KR | 잔액은 반영 시점에 따라 실제 금액과 다를 수 있습니다. | ✅ 띄어쓰기 교정 반영 · 조사 `은`·`과` 정상 | ✅ 위반 없음 | ✅ 기준 |
| ja_JP | 残高は反映のタイミングにより、実際の金額と異なる場合があります。 | ✅ 경어 자연스러움 | ✅ 잔액=`残高`(등록값 관례) | ✅ |
| en_US | The balance may differ from the actual amount depending on when it is updated. | ✅ 대소문자 정상 | ✅ 위반 없음 | ✅ |
| th_TH | ยอดคงเหลืออาจแตกต่างจากจำนวนเงินจริง ขึ้นอยู่กับเวลาที่อัปเดต | ✅ 표기 정상 | ✅ 위반 없음 | ✅ |
| zh_TW | 餘額可能因更新時間而與實際金額有所不同。 | ✅ 번체 정상(간체 혼입 0) | ✅ 위반 없음 | ✅ |

**점검 항목별**: 외래어 음차 오염 없음 · 붙은 단어 없음(ko 교정으로 해소) · 다의어 모호 없음 · 언어 혼입 없음 · **변수 치환자 없음**(이 키는 변수 미사용) · 마침표 정책은 형제 키와 동일(ko·ja·zh 유지 · th 생략).

## (c) 검증기 보고 건별 처리 판정

자동 검증 **P0·P1·P2 모두 0건**이므로 판정 대상이 없다. **오탐 0 · 실제 위반 0 · 정책 보류 0.**

## (d) 추가 개선·제안 (권장 — 임의 적용하지 않음)

**(d-1) 용어집 보완 권장 — 없음.** 이번 5셀에서 반복 오탐이 발생하지 않았고, `md/glossary-backlog.md`에 새로 올릴 항목도 없다.

**(d-2) 추가 개선·제안**

| # | 내용 | 판단 주체 |
|---|---|---|
| 1 | **툴팁 노출 조건이 정책으로만 있고 UI 스펙이 없다** — 「일부사용가능일 때 물음표 아이콘 노출」은 코멘트로 확정됐으나, 아이콘 자체는 XLT 대상이 아니고 위치·접근성 라벨 정의가 없다. FE 구현 시 필요할 수 있다 | 기획 · FE |
| 2 | 사용 가능 금액 샘플이 `10,000원` → `7,000원`으로 바뀐 것은 **일부 사용 상태를 보여주는 의도**로 보인다(툴팁 신설과 맥락 일치). 위키 Description에 그렇게 기록했다 | (기록 완료) |
| 3 | `결제 정보 보기`(`UF_voucher_detail_payment_info_btn`)는 여전히 Figma `xlt` 마커가 없다 — 다만 **Figma 변경 요청은 2026-09-14 종결**돼 재제안하지 않는다 | (종결) |

## 통과 판정

**P0 = 0 (자동 + 수동 전수)** → 위키 반영·엑셀 생성으로 진행.

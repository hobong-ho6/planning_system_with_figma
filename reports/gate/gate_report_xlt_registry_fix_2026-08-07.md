# 번역 검증 리포트 — XLT 시스템 등록값 수정 (nbsp · 빈 값 · 예외 위반 · 유저→사용자)

| 항목 | 값 |
|---|---|
| 일자 | 2026-08-07 (세션 #18 후속) |
| 모드 | `md/xlt-verify.md` — 등록값 수정 엑셀 생성 (Step 5) |
| 대상 | `Dapp Portal` v2.6.0 (1,596키) · `Unifi` v1.6.6 (2,131키) — **API 재조회** |
| 용어집 | 라이브 **v4.6**(116 terms · deprecated 14종) |
| 산출물 | `xlt/registry_fix/xlt_fix_DappPortal_20260807.xlsx`(25키) · `xlt_fix_Unifi_20260807.xlsx`(60키) |
| 승인 범위 | HANDOFF 「다음 할 일」 #4(nbsp) · #5(빈 값) · #8(예외 위반) · #15(유저→사용자) |

## 처리 내역

| 분류 | Dapp Portal | Unifi | 성격 |
|---|---|---|---|
| **nbsp(U+00A0) → 공백** | 27셀 | 53셀 | 기계 치환 |
| **용어집 #14 `유저`→`사용자`** | 5셀 | 3셀 | 기계 치환 |
| **용어집 예외 위반(확정분)** | 3셀 | 3셀 | 건별 판정 |
| **빈 값** | 0 | 1셀 | 신규 번역 |
| **합계** | **35셀 / 25키** | **60셀 / 60키** | |

## 1단계: 한국어 원문 교정

`유저` → `사용자` **8키**(DP 5 · Unifi 3)가 대상이다. 용어집 v4.6 `deprecated_terms` **#14**에 등재된 금지어이며, 2026-08-07 사용자 결정(「`사용자`로 통일」)의 시스템 반영분이다.

| 서비스 | 키 |
|---|---|
| `Dapp Portal` | `mini_luckyball_terms_existing_user`(P0 재업로드 건과 동일) · `unifi_promotion_jpyc_bonus_all_title` · `_bonus_gold_title` · `_caution_contents2` · `_reward_info1-1` |
| `Unifi` | `UF_boost_notice_burn_policy` · `UF_main_vip_benefit_homebanner_desc` · `UF_trade_main_apps_list_sorting1` |

**alias 불필요** — Figma 원문이 아니라 **시스템 등록값**을 고치는 작업이다.

⚠️ **범위 밖이라 손대지 않은 것**: `unifi_promotion_jpyc_bonus_desc`(Dapp Portal) ko는 `보유금 유지시 매일 {0}개씩 JPYC 럭키볼이 지급되요.` 로 **맞춤법(`되요`→`돼요`) · 띄어쓰기(`유지시`→`유지 시`)** 오류가 있다. **`Unifi`의 같은 키는 이미 `유지 시`·`지급돼요`로 정상**이라 Dapp Portal 쪽만 구값이다. 승인 범위(#4·#5·#8·#15) 밖이므로 **보고만 한다.**

## 2단계: 용어집 위반 검증

### 예외(원어 유지) 위반 11건 — 건별 판정

**확정 위반 6건만 수정**했고, **정책 판단 5건은 손대지 않았다.**

| # | 키 | 언어 | before → after | 판정 |
|---|---|---|---|---|
| 1 | `UF_reward_history_user` (DP) | th | `{0} ดอลลาร์สหรัฐฯ` → `{0} USDT` | **확정** — 스테이블코인을 「미국 달러」로 번역. USDT≠USD |
| 2 | `UF_reward_history_user` (Unifi) | th | `{{0}} …` → `{{0}} USDT` | **확정** — 동일 건(치환자만 다름) |
| 3 | `UF_dm_reward_amount` (Unifi) | th | `{{0}} …` → `{{0}} USDT` | **확정** — 동일 부류 |
| 4 | `unifi_promotion_jpyc_bonus_desc` (DP) | ja | `…ラッキーボールが付与…` → `…JPYCラッキーボールが付与…` | **확정** — ko는 `JPYC 럭키볼`인데 ja만 JPYC 누락 |
| 5 | `unifi_promotion_jpyc_bonus_desc` (Unifi) | ja | 〃 | **확정** — 동일 |
| 6 | `oa_promotion_motion_title` (DP) | ja | `わくわく…\n**いくら**当たるかな？` → `わくわく…\n**何USDT**当たるかな？` | **확정** — ko `몇 USDT가 나올까요?` · en·th·zh **모두 USDT 명시**하고 ja만 누락 |
| 7 | `UF_common_error_404_desc` (Unifi) | zh | `URL` → `網址` | **정책 판단 — 미수정.** 중국어로는 `網址`가 관용이다. 용어집 예외가 `URL` 원어 유지인데 zh만 예외를 둘지 결정 필요 |
| 8 | `UF_exchange_guide_subtitle` (Unifi) | en | ko `USDT 토큰 거래 가능 거래소` ↔ en `Transfer to Exchange` | **정책 판단 — 미수정.** USDT 누락이 아니라 **의미가 다른 문장**이다. 재번역 문안 결정 필요 |
| 9 | `UF_guide_reason_interest_desc3` (Unifi) | en | `Earn daily interest from returns efficiently managed` | **정책 판단 — 미수정.** 문장이 끊겨 있고 USDT·파트너 언급이 빠졌다. 재번역 필요 |
| 10 | `UF_main_jpyc_guide_banner_title` (Unifi) | en | ko `JPYC 알아보기` ↔ en `Learn More` | **정책 판단 — 미수정.** 버튼 의역으로 허용할지 결정 필요 |
| 11 | `oa_promotion_popup_budget` (DP) | th | ko 2문장 ↔ th 1문장(`대신, 다른 USDT 미션을…` 누락) | **정책 판단 — 미수정.** 번역 누락이라 재번역 필요 |

> **#6이 처음엔 「정책 판단」으로 분류돼 있었는데 확정으로 재분류했다.** nbsp 수정으로 이 키가 엑셀에 들어오며 기존 ja 위반이 **P0로 드러났고**, 4개 언어가 모두 USDT를 명시한다는 사실이 「의역」이 아님을 확정했다.

### `deprecated_terms` 14종

수정 후 대상 키에서 **적발 0건**(`유저` 8건 → 0).

## 3단계: 다른 언어 번역 검토

### 신규 번역 1건

| 키 | 언어 | 값 | 근거 |
|---|---|---|---|
| `app_detail_receive_done` (Unifi) | en_US | **`Received`** | ko `받음`(완료 상태) · ja `受取` · th `ได้รับ` · zh `已收件`. 형제 키 `app_detail_receive` = ko `받기` / en `Get` → 완료형은 `Received` |

### 수동 3단계 체크표

| 점검 | Dapp Portal | Unifi |
|---|---|---|
| 빈칸 | 0 ✓ | 0 ✓ (1건 해소) |
| nbsp(U+00A0) | **0 ✓**(27셀 해소) | **0 ✓**(53셀 해소) |
| `/n` 오타 | 0 ✓ | 0 ✓ |
| 언어 혼입·컬럼 어긋남 | 0 ✓ | 0 ✓ |
| placeholder 일치 | 치환자 보존 확인 ✓ (`{0}`·`{{0}}` 표기 유지) | ✓ |
| zh 간체 혼입 | 0 ✓ | 0 ✓ |
| **원본 대비 변경 셀 = 변경 로그** | 35 = 35 ✓ | 60 = 60 ✓ |
| 레지스트리에 없는 키 | 없음 ✓ | 없음 ✓ |

**치환자 표기를 서비스별로 보존**했다 — `Dapp Portal`은 `{0}`, `Unifi`는 `{{0}}`로 원본 표기를 그대로 두고 값만 고쳤다(세션 #17 「서비스별 키스페이스」 규칙).

### 검증기 보고 처리 판정

| 파일 | P0 | P1 | P2 | exit |
|---|---|---|---|---|
| `xlt_fix_DappPortal_20260807.xlsx` (25키) | **0** | 46 | 8 | **0** |
| `xlt_fix_Unifi_20260807.xlsx` (60키) | **0** | 113 | 24 | **0** |

**P1·P2 전건 오탐 판정** — 이 엑셀은 **수정 대상 키만 모은 부분집합**이라 P1 모집단이 원본 전수와 다르다. 내용은 세션 #17 감사(`reports/audit/xlt_system_glossary_audit_2026-08-07.md` §5)에서 이미 전건 판정한 부류와 동일하다: 용어집 협소 매핑(`용어 불일치`) · en 하이픈·중간 삽입어 · 다의어(`확인`·`종료`) · zh `・` 구분자 · 마침표 스타일(부분집합에서는 판정 자체가 성립하지 않음).

**⚠️ 신규 검사 2종의 영향**: 이번에 `validate_translation.py`에 편입한 `ja==zh 동일`·`번역 누락 의심`이 이 부분집합에서도 계산되므로 P1에 포함돼 있다. **보유 캠페인 엑셀 9종에서는 신규 검출 0건**이라 게이트 기준선은 불변이다(`md/check.md` v3.5).

### 전수 점검 범위

- 자동 검증은 **생성된 엑셀 전체 행**(25·60키)에 실행했다.
- 수동 검토는 **기계 치환 외 건별 변경 7건**(예외 위반 6 + 빈 값 1)을 before/after로 전건 확인했다. nbsp·`유저` 치환 88셀은 **결정론적 치환**이고 잔존 0을 기계 검증했다.
- ⛔ **전체 레지스트리(1,596·2,131키) 전수 수동 검토는 하지 않았다** — 이 작업은 지정 셀만 고치는 부분 패치이고, 전수 판정은 세션 #17 감사 리포트가 담당한다.

## 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 = 0 | ✅ 양쪽 0건 (exit 0) |
| 수동 P0 = 0 | ✅ 0건 |
| P1/P2 처리 판정 | ✅ 전건 오탐(근거: 감사 리포트 §5와 동일 부류) |
| 무결성 | ✅ 변경 셀 = 로그 · 빈 값 0 · nbsp 0 · 미등록 키 0 |
| 한국어 원문 교정 | ✅ `유저`→`사용자` 8키 (범위 밖 맞춤법 1건은 보고) |

**→ 엑셀 생성 완료. 업로드는 사용자가 수행한다(쓰기 API 없음).**

## 추가 개선·제안 (권장 — 임의 반영 금지)

| # | 항목 |
|---|---|
| 1 | **정책 판단 5건 결정 필요** — 위 2단계 표 #7~#11. 특히 #8·#9·#11은 **번역 자체가 틀렸거나 누락**돼 재번역 문안이 필요하다 |
| 2 | **`unifi_promotion_jpyc_bonus_desc` Dapp Portal ko 맞춤법** — `지급되요`→`지급돼요` · `유지시`→`유지 시`. `Unifi`는 이미 정상이라 **두 서비스 값이 갈려 있다**(세션 #17 「공통 336키 중 152키 값 상이」의 사례) |
| 3 | **`app_detail_receive_done` zh `已收件`** — `收件`은 「우편물 수신」 뉘앙스다. 리워드 수령이면 `已領取`가 적절하다. 이번 범위 밖이라 미수정 |
| 4 | **업로드 후 재조회 검증** — `fetch_xlt_registry.py`로 다시 받아 nbsp 0 · `유저` 0을 확인한다. 세션 #18에서 사용자 업로드 1건이 미반영된 실측이 있어 **업로드 후 확인을 습관화**하는 것이 좋다 |

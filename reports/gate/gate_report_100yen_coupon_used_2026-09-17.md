# 게이트 리포트 — 100엔딜 「쿠폰 사용한 경우」 신규 3키 (UNIFY / Slack C07R7CE0B5H)

- **작업 유형**: 1단계 번역 — 코멘트 선별 모드(`xlt` 마커) · 신규 키 정의 + 5개 언어 번역
- **대상 Figma**: `Web3` `GOCHAYBS7hIrmWRGNuJOKV` / 페이지 `Unifi mini v2.0`
  - `75253:2110` 「쿠폰 사용한 경우」 (375×1470) — `xlt` 마커 2건(y=632·659)
  - `75253:2503` 「쿠폰 사용한 경우 바텀시트」 (375×812) — `xlt` 마커 1건(y=393)
- **담당 FE 팀**: **UIT** — 프리픽스 `UF_` 고정, 치환자 `{{0}}`(이번 신규 키에는 변수 없음). 위키 `100엔딜`에 UIT/LV 구분이 있어 사용자 질의 없이 판별
- **대조 기준(라이브)**: XLT `Unifi / WEB BROWSER / v1.7.9` · **2,604키** · 2026-09-17 재조회(캐시 미사용)
- **용어집**: Landpress `web3_xlt_json` v5.5 · 118용어 · 2026-09-17 조회
- **산출물 엑셀**: `xlt/xlt_output_20260917124633.xlsx` (properties 3행 + plurals)
- **맥락**: Slack `C07R7CE0B5H` 스레드 `1789547763.296749` — 「쿠폰 기사용 유저 케이스」 XLT 지정 요청

---

## 0. 대상 선별 (코멘트 선별 모드) 및 신규/기존 판정

프레임 2개의 TEXT 노드 **159개**(67 + 92) 전수를 라이브 등록값과 대조해 신규만 추렸다.

| Figma 문구 | 판정 | 키 |
|---|---|---|
| 이미 쿠폰을 사용했어요 (배너 타이틀) | **신규** (최고 유사도 0.500) | `UF_100yen_coupon_used_title` |
| 해당 쿠폰은 1인 1회만 사용 가능합니다 | **신규** (최고 유사도 0.431) | `UF_100yen_coupon_used_desc` |
| 이미 쿠폰을 사용했어요 (바텀시트 타이틀) | **신규 — 기존 키의 분기** | `UF_voucher_pay_more_title_coupon_used` |
| 모든 바우처 15% 혜택! | 기존 재사용 | `UF_voucher_pay_more_discount` (`모든 바우처 {{0}} 혜택!` — 15%는 변수) |
| 원하는 바우처를 할인된 가격으로 더 구매해보세요. | 기존 재사용 (정규화 **동일 1.000**) | `UF_voucher_pay_more_desc` |
| 혜택가 | 기존 재사용 (동일 1.000) | `UF_voucher_price_benefit` / `UF_voucher_recommend_price` (중복 2키 — (d-2) 3) |
| 정가 | 기존 재사용 (동일 1.000) | `UF_voucher_price_original` |
| 더 많은 상품 보러가기 | **기존 키 재사용 + 원문 수정 요청** (유사도 0.875) | `UF_voucher_more_products` 「더 많은 상품 보기」 |
| 나머지(상품명·금액·LINE 대화 목업·기존 딜페이지 문구) | 대상 아님 | 데이터/목업 또는 `UF_100yen_*` 기등록 31키 |

**키 네이밍 결정(사용자 확인 완료 — 등록 전이 유일한 기회)**
- 프리픽스 `UF_100yen_` — **선례 실측 31건**
- 바텀시트 타이틀은 `UF_voucher_pay_more_*` 계열에 **분기 키**로 신설. ⛔ 기존 `UF_voucher_pay_more_title`(「더 구매하고 싶다면?」)은 **공유 키**라 값을 고치면 다른 화면의 바텀시트까지 바뀐다(CLAUDE.md 2-1-b)
- `noti` 선례 0건 / `notice` 58건이라 `noti` 채택 안 함. `coupon` 선례 2건·`used` 3건 기준으로 `coupon_used` 확정

---

## 1. 한국어 원문 교정 (1a)

| 원문 | 교정 | 판정 |
|---|---|---|
| `이미 쿠폰을 사용했어요` | 없음 | 맞춤법·띄어쓰기 정상 |
| `해당 쿠폰은 1인 1회만 사용 가능합니다` | 없음 | 맞춤법·띄어쓰기 정상 |

- **alias 없음** — Figma 원문과 XLT 등록 문구가 동일하므로 `{Figma 원문 → XLT Key}` alias 대상 없음.
- 종결어미 문체 혼재(`~했어요` / `~합니다`)는 오류가 아니라 **정책 판단**이라 임의 교정하지 않고 (d-2) 1로 올린다.

---

## 2. 자동 검증 — `validate_translation.py` (a)

대상: `xlt/xlt_output_20260917124633.xlsx` (**전체 파일 = 3키 × 5개 언어 = 15셀, 전수**)

| 심각도 | 건수 |
|---|---|
| 🔴 **P0** | **0건** |
| 🟡 P1 | **0건** |
| 🟢 P2 | **0건** |

**→ P0 = 0 (통과 기준 충족)** · 리포트: `xlt/xlt_output_20260917124633_validation_report.md`

---

## 3. 수동 3단계 체크표 (b) — 전수(전체 파일 3키 · 15셀 전부)

### 1단계 — 한국어 원문
| 키 | ko_KR | 판정 |
|---|---|---|
| `UF_100yen_coupon_used_title` | 이미 쿠폰을 사용했어요 | ✅ 정상 |
| `UF_100yen_coupon_used_desc` | 해당 쿠폰은 1인 1회만 사용 가능합니다 | ✅ 정상 |
| `UF_voucher_pay_more_title_coupon_used` | 이미 쿠폰을 사용했어요 | ✅ 정상(분기 키 — title과 의도적 동일 문자열) |

맞춤법·띄어쓰기·nbsp·중복공백 전건 이상 없음. 조사 판정: `쿠폰을`(종성 ㄴ → `을`) 정상.

### 2단계 — 용어집 대조
| 용어 | 등재 여부 | 적용값 | 근거 |
|---|---|---|---|
| **쿠폰** | **미등재**(대기 #19) | ja `クーポン` · en `coupon` · th `คูปอง` · zh `優惠券` | **등록값 실측 4/4키 전건 일치**(`UF_100yen_caution_contents8`·`coupon_applied`·`coupon_price`·`share_text`) |
| 바우처 | 등재(v5.4~5.5) | — | 이번 3키에 `바우처` 미포함 → 충돌 없음. 등재 ja 정본이 `クーポン`이라 표기 경합도 없음 |
| 1인/1회 | 미등재 | ja `お一人様` · en `per person` · th `ต่อคน` · zh `每人` | 선례 `UF_100yen_caution_contents9` 표기와 일치 |

번역 예외 항목(USDT 등)·고유명사 **해당 없음** → 게이트 1-1 웹 확인 대상 아님.

### 3단계 — 다국어 (15셀 전부)

| 키 | 언어 | 값 | 판정 |
|---|---|---|---|
| coupon_used_title / pay_more_title_coupon_used | ja_JP | `クーポンはご利用済みです` | ✅ です・ます体 · 경어 `ご利用` 선례 일치 |
| 〃 | en_US | `You have already used this coupon` | ✅ 비축약형(선례 36 : 축약 9) · `already` 선례 어순 일치 |
| 〃 | th_TH | `คุณใช้คูปองไปแล้ว` | ✅ 정중 · `คูปอง` 선례 일치 |
| 〃 | zh_TW | `您已使用過優惠券` | ✅ 번체 정자 · `您` 존칭 선례 일치 |
| coupon_used_desc | ja_JP | `このクーポンはお一人様1回のみご利用いただけます` | ✅ `お一人様` 선례(contents9) · 겸양 `ご利用いただけます` |
| 〃 | en_US | `This coupon can be used only once per person` | ✅ `per person` 선례(contents9) |
| 〃 | th_TH | `คูปองนี้ใช้ได้เพียง 1 ครั้งต่อคนเท่านั้น` | ✅ `ต่อคน…เท่านั้น` 선례 일치 |
| 〃 | zh_TW | `此優惠券每人僅限使用 1 次` | ✅ `每人` 선례(contents9) · 숫자 앞뒤 공백 선례(`限 1 張`) 일치 |

**공통 점검**

| 항목 | 결과 |
|---|---|
| 치환자(UIT `{{0}}`) | ✅ 해당 없음(3키 모두 변수 미포함). 바텀시트 `15%`는 **기존 키** `UF_voucher_pay_more_discount`의 `{{0}}`로 처리되어 신규 등록 불필요 |
| HTML 태그 | ✅ 해당 없음 |
| 언어 혼입 / 컬럼 정렬 | ✅ 없음 |
| 종결부호 | ✅ 5개 언어 모두 마침표 없음으로 통일(화면 배너 표기 기준) |
| 앞뒤·중복 공백 | ✅ 없음 |
| title ↔ 분기 키 동일성 | ✅ `UF_100yen_coupon_used_title`와 `UF_voucher_pay_more_title_coupon_used`는 5개 언어 전부 바이트 동일(의도된 분기) |

---

## 4. 검증기 보고 각 건 처리 판정 (c)

| 심각도 | 건수 | 처리 판정 |
|---|---|---|
| 🔴 P0 | **0건** | 통과 — 실제 위반 없음 |
| 🟡 P1 | **0건** | 오탐 포함 보고 없음 |
| 🟢 P2 | **0건** | — |

자동 검증기가 보고한 건이 없어 **오탐·정책 보류 판정 대상도 없다.** 수동 전수 검토에서도 P0 추가 검출 0건.

---

## 5. 추가 개선·제안 (d) — 권장, 임의 반영하지 않음

### (d-1) 용어집 보완 — 대기 #19 **실측치 갱신**(신규 등재 아님)
「쿠폰」은 `md/glossary-backlog.md` **#19**로 이미 등재돼 있고, 등재 당시 메모는 **"실사용 0건 — 업로드 후 재실측 전 채택 금지"**였다. 이번에 100엔딜 키가 업로드된 뒤 재실측한 결과 **ko `쿠폰` 포함 등록 키 4건**이며 ja `クーポン` 4/4 · en `coupon` 4/4 · th `คูปอง` 4/4 · zh `優惠券` 4/4로 **표기가 완전히 일관**하다. #19의 실측 칸을 갱신했다(대조군 `바우처` 29건).
⛔ 실사용 4건은 여전히 소량이므로 **채택 여부는 용어집 리뷰 시점에 판단**한다. 이 자리에서 반영을 묻지 않는다(2026-09-14 결정).

### (d-2) 사용자 결정 권장 사항

| # | 항목 | 내용 | 권장 |
|---|---|---|---|
| 1 | **배너 문체 혼재** | 타이틀 `~했어요` ↔ 설명 `~합니다`가 한 배너 안에서 갈린다 | 설명을 「해당 쿠폰은 1인 1회만 사용할 수 있어요」로 통일 검토. **ko 변경 시 5개 언어 재검토 필요**하므로 등록 전 결정 권장 |
| 2 | **`더 많은 상품 보러가기` 원문** | 등록값은 `UF_voucher_more_products` 「더 많은 상품 보기」. 사용자 결정으로 **기존 키 재사용** | **디자이너에게 Figma 문구를 「더 많은 상품 보기」로 수정 요청** |
| 3 | **`혜택가` 중복 키 2건** | `UF_voucher_price_benefit`·`UF_voucher_recommend_price`가 5개 언어 값까지 동일 | FE에 **어느 키를 쓰는지 확인** 필요. 정리는 별도 과제 |
| 4 | **`1인 1회` ↔ 주의사항 표현 불일치** | 배너는 「1인 1회」, 주의사항 `contents8`은 「1계정당 1장, 1회 구매」 | 같은 정책이면 표현 통일 검토(계정 기준 ↔ 인 기준은 **정책 차이일 수 있어** 기획 확인 필요) |
| 5 | **Figma 프레임 문구 구값** | `가격・재고는… 확인하십시오` 등 프레임 본문이 등록값(`contents10` 「확인해 주세요」)과 다름 | 기존 키가 정본이므로 **Figma 반영 요청**(이번 등록 대상 아님) |

---

## 6. 통과 판정

- 자동 **P0 = 0**(P1·P2도 0) ✅
- 수동 전수(3키 × 5개 언어 15셀) **P0 = 0** ✅
- 검증기 보고 0건 → 처리 판정 대상 없음 ✅
- (d) 권장 사항 **임의 반영 없음** ✅

**→ 게이트 통과. 업로드 엑셀 생성·전달 가능.**

⛔ **XLT 시스템은 쓰기 API가 없다 — 업로드는 사용자가 직접 수행한다.** 업로드 후 라이브 재조회로 3키 × 5개 언어를 대조하고, 그 결과를 Slack 스레드(`C07R7CE0B5H` / `1789547763.296749`)에 회신한다.

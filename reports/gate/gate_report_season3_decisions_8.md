# 게이트 리포트 — 시즌3 미결 8건 사용자 결정 반영 (위키 v41 → v43)

| 항목 | 값 |
|---|---|
| 대상 | Confluence `4541588845` (JPYC 럭키볼 시즌 3) · Figma `GOCHAYBS7hIrmWRGNuJOKV` 페이지 `65923:2485` |
| 일자 | 2026-08-05 (세션 #8) |
| 라이브 버전 | v41 → **v42**(번역 3셀) → **v43**(History 행) — 매 PUT 직전 버전 가드 통과 |
| 변경 규모 | **2키 3셀** (th 2 · en 1). 번역표 **47키 유지**(증감 0) |
| 검증 범위 | **전수 47키 × 5개 언어 = 235셀** (변경분 한정 아님) |
| 산출물 | 엑셀 3종 재첨부(ALL v10 · LV v2 · UIT v7) · OA Flex zip **v3** |

---

## 0. 사용자 결정 8건 — 처리 결과

| # | 항목 | 사용자 결정 | 이번 run 조치 |
|---|---|---|---|
| ⓐ | th 럭키볼 표기 | **용어집 용어로 업데이트** | ✅ 2키 th 교체 (아래 §1) |
| ⓑ | `..._MINI` en 단위 | **다른 언어에 맞춰 업데이트** | ✅ 1키 en 교체 (아래 §1) |
| ⓒ | `jpyc_info_oa_desc` 정본 | **ⓐ안이 정본. Figma 디자인은 수정하지 않고 최종 XLT 키 문구로 확정** | ✅ 위키 값 변경 없음(현행이 ⓐ안). 결정을 History에 기록 |
| ⓓ | mini 키 미부여 4건 | **무시 — 기존 키들이 있음** | ✅ 조치 없음. 확인 목록 재생성 안 함 |
| ⓔ | mini `최대 60만엔` | **무시 — XLT 키의 문구가 정답** | ✅ 조치 없음 |
| ⓕ | OA ④ 라벨↔목적지 | **URL 업데이트했음** | ✅ 라이브에서 새 URL 확인 → Flex JSON 5개 재생성·zip v3 (아래 §5) |
| ⓖ | OA ⑤ 추적 파라미터 | **유입 추적하지 않음** | ✅ 쿼리 없는 순수 경로 유지. 결정 종결 |
| ⓗ | altText 금액 노출 | **금액 안 넣음 — altText에는 변수를 쓸 수 없음** | ✅ 현행 유지. 결정 종결(재제안 금지) |

> ⓗ의 근거가 갱신됐다. 부록 7에서는 "`{{amount}}`를 봉투 텍스트에서도 치환 가능"으로 적었으나, **사용자 확인 결과 altText에는 변수를 사용할 수 없다.** 이 제약이 정본이며 이후 OA 작업에서 altText 변수화를 재제안하지 않는다.

---

## 1. (1a) 한국어 원문 교정 — 이번 변경분

**한국어(ko_KR) 원문은 변경하지 않았다.** 이번 변경은 ko를 기준으로 **ja·en·th·zh가 어긋난 것을 ko에 맞추는 방향**이라 원문 교정·alias 발생이 없다.

| # | 키 | 언어 | before | after | 사유 |
|---|---|---|---|---|---|
| 1 | `mini_guidekim_luckyball_banner` | th_TH | `ลัคกี้บอล JPYC ถูกรางวัล 100%` | `ลูกบอลนำโชค JPYC ถูกรางวัล 100%` | 등록값이 영어 음차(`ลัคกี้บอล`). 용어집 v4.0 `럭키볼` th 등재값은 `ลูกบอลนำโชค` |
| 2 | `UF_home_daily_mission_title_MINI` | th_TH | `โอกาสสุ่มลัคกี้บอลทุกวัน รับ JPYC มูลค่าสูงสุด 150,000 เยน` | `โอกาสสุ่มลูกบอลนำโชคทุกวัน รับ JPYC มูลค่าสูงสุด 150,000 เยน` | 동일 |
| 3 | `UF_home_daily_mission_title_MINI` | en_US | `Daily Lucky Ball chance to\nreceive up to 150,000 JPYC` | `Daily Lucky Ball chance to receive\nup to 150,000 JPY worth of JPYC` | ko `15만엔 상당 JPYC`는 **엔 환산**인데 en만 **토큰 개수**로 옮겨져 있었다 |

### ⓑ 교차 근거 — en만 단독으로 어긋나 있었다

| 언어 | 값 | 무엇을 세나 |
|---|---|---|
| ko | `최대 15만엔 **상당** JPYC 받기` | 엔 환산 |
| ja | `最大15万円**相当**のJPYC獲得` | 엔 환산 |
| zh | `最高領取**價值** 15 萬日圓**的** JPYC` | 엔 환산 |
| th | `รับ JPYC **มูลค่า**สูงสุด 150,000 เยน` | 엔 환산 |
| **en (before)** | `receive up to 150,000 **JPYC**` | **토큰 개수** ← 단독 |

같은 페이지 `UF_home_jpyc_banner_title` en이 이미 `150,000 JPY worth of JPYC`라 **영어 내부에서도 표기가 갈려 있었다.** 정정으로 두 키의 en 표기가 일치한다.

### 변경하지 않은 유사 사례 (오판 방지 기록)

`unifi_promotion_jpyc_desc`는 **ko 원문 자체가 `최대 5만 JPYC`(토큰 개수)** 이고 ja `最大5万 JPYC` · en `50,000 JPYC` · th `สูงสุด 50,000` · zh `最高 50,000 JPYC`로 **5개 언어가 이미 일관**하다. "JPYC 개수 표기"라는 이유만으로 일괄 변경하면 이 키를 잘못 건드리게 되므로 **ko 기준 일치 여부로 판정**했다.

---

## 2. (a) 자동 검증 — `validate_translation.py` 전수

```
python3 scripts/validate_translation.py xlt/xlt_output_season3_ALL_20260804.xlsx scripts/glossary.json
→ 총 항목 47개
   🔴 P0 (Critical)  0건
   🟡 P1 (Medium)   63건
   🟢 P2 (Low)      10건
   (한국어 단독 검증: P0 0 · P1 0 · P2 10)
```

**직전 run 대비 P1 65 → 63건** — 감소분 2건이 정확히 `mini_guidekim_luckyball_banner` th · `UF_home_daily_mission_title_MINI` th의 `럭키볼 → ลูกบอลนำโชค` 미매칭이었다. **ⓐ 변경이 용어집 정합을 실제로 개선했음이 수치로 확인**된다.

### 기계 보조 점검 (자동 검증기 범위 밖 — 별도 스크립트, 235셀)

| 검사 | 결과 |
|---|---|
| nbsp(`\xa0`) · 이중공백 · 전후 공백 | ✅ 0건 |
| 컬럼별 이질 문자체계(en칸 한글/CJK · th칸 한자 · zh칸 가나 · ja칸 한글) | ✅ 0건 |
| 숫자+통화 붙임(`150,000JPYC` 류) | ✅ 0건 |
| 치환자 개수·표기 언어 간 일치(UIT `{{0}}` / LV `{0}`) | ✅ 47키 전부 일치 |
| th 음차 잔존(`ลัคกี้บอล`·`ลักกี้บอล`) | ✅ **번역표 0건** |

> **History 이력 문장의 과거 표기는 보존했다.** 본문 `<h1>다국어 번역` 앞 History 셀에 `ลักกี้บอล → ลูกบอลนำโชค`라는 **과거 변경 기록**이 있어, 잔존 검사를 번역표 구간으로 한정하고 History 문장 보존을 assert로 강제했다. (전역 치환했다면 이력 기록을 훼손할 뻔했다.)

---

## 3. (b) 수동 3단계 체크표 — 전수 47키 × 5개 언어 (235셀)

**검토 범위를 신규·변경 키로 한정하지 않았다.** 1차(메인) + 2차 독립 검토(`translation-reviewer`, 분리 컨텍스트) 병행.

| 단계 | 점검 내용 | 결과 |
|---|---|---|
| **1단계 한국어** | ko 47행 맞춤법·띄어쓰기·조사·중복 표현 | 🟡 P1 2건 (§4 M-1·M-2) — **ko는 XLT 시스템 등록값이라 사용자 결정 사안** |
| **1단계 한국어** | 받침·조사 판정(`(바임)으로부터` 류 오탐 주의) | ✅ 해당 패턴 없음, 조사 오류 0건 |
| **2단계 용어집** | `럭키볼` th 5키 통일 여부 | ✅ **이번 변경으로 5키 전부 `ลูกบอลนำโชค`** · 음차·미번역 잔존 0 |
| **2단계 용어집** | `당첨금`(prize/当選金/เงินรางวัล/獎金) · `미션` · `공식 계정` 준수 | ✅ 위반 0 |
| **2단계 용어집** | `팔로우`→`공식 계정 친구 추가` 확정값 (ko·ja `フォロー`·zh `關注/追蹤`·th `ติดตาม`·en `follow`) | ✅ **전 언어 잔존 0건** |
| **2단계 용어집** | ja `友達`→`友だち` 고정 | ✅ 5회 전부 `友だち` |
| **2단계 용어집** | zh 간체·이형자(번체 정자 정본) · th 자음·성조 철자 | ✅ 0건 |
| **3단계 다국어** | 변경 3셀의 ko 기준 의미 대조 | ✅ **2차 검토 전건 승인** (§6) |
| **3단계 다국어** | 같은 문구가 여러 키에 걸칠 때 번역 분기 | 🟡 P1 3건 (§4 M-4·M-5·M-6) — 기존 키, 이번 변경 무관 |
| **3단계 다국어** | 개행(`\n`) 구조 ko 대비 일치 | 🟡 P1 1건 (§4 M-7) |
| **3단계 다국어** | 변수 보존·어순 | ✅ 불일치 0 |

---

## 4. (c) 전건 처리 판정 — 자동 P1 63 · P2 10 + 수동 발견

### 4-1. 자동 P1 63건 — **전건 오탐 판정 유지**

| 그룹 | 건수 | 판정 근거 |
|---|---|---|
| `당첨` → en `win` / th `ถูกรางวัล` / zh `中獎` 미적용 | 다수 | **오탐** — 해당 셀은 `당첨금`(prize/`เงินรางวัล`/`獎金`)이다. 검증기가 **긴 용어를 우선 매칭하지 못해** `당첨`으로 부분일치시킨 구조적 결함 (`md/check.md` 함정 2-1) |
| `최대` → en `Max` / zh `最多` | 다수 | **오탐** — 등재값 `Max`는 라벨용. 문장에서는 `up to`·`最高`·`สูงสุด`가 정본 |
| `확인` 다의어 → `check`/`查看` | 6 | **확정 오탐 — 재제안 금지**(버튼 문맥은 `OK`/`ตกลง`/`確認`) |
| `가입` → en `Sign up` | 3 | **오탐** — `signs up`·`registration` 굴절/명사형 |
| `공식 계정`·`계정`·`친구`·`리워드 지급`·`종료` | 나머지 | **오탐** — 문장 내 자연 의역. `종료` zh는 **등재값 자체가 오역**(§7 G-1) |
| **변경 3셀 관련** | 2 | `UF_home_daily_mission_title_MINI` en·zh `최대 → Max/最多` — 위와 동일 오탐. **변경으로 새로 생긴 P1은 0건** |

### 4-2. 자동 P2 10건 — **전건 오탐 판정 유지**

10건 모두 `[마침표 스타일] 소수 스타일(마침표 있음)`이며, 검출된 문장은 **전부 합쇼체**(`-습니다`·`-주세요`)다. `md/check.md` B-7에 따라 **합쇼체 ○ / 해요체·UI 라벨 ✕** 가 정본 정책이므로 마침표가 정상이다. 검증기가 파일 내 다수(해요체·라벨)를 기준으로 소수를 지적한 구조적 오탐.

### 4-3. 수동 발견 — 실제 위반 후보 (이번 변경분 아님, **사용자 확인 필요**)

> ⛔ **임의 적용하지 않았다.** ko 원문은 XLT 시스템 등록값이고 사용자가 ⓔ에서 "**XLT 키의 문구가 정답**"이라고 확정했으므로, ko 변경은 시스템 등록값 변경 승인이 선행되어야 한다.

| # | 키 | 언어 | 현재 | 문제 | 권장 |
|---|---|---|---|---|---|
| M-1 | `UF_home_skyflag_title` | ko | `무제한 미션하고` | `미션하고` — `md/check.md` ⭐⭐⭐ 확정 패턴. **같은 파일 `unifi_promotion_jpyc_btn_oa`는 이미 `미션 완료하고`** 라 내부 모순 | `무제한 미션 완료하고` |
| M-2 | `UF_home_skyflag_title` | ko→en·ja | `최대 3만엔 상당 즉시 받기` | ko에 `상당`의 **피수식 목적어가 없어** zh·th는 `獎勵`/`รางวัล`을 보충했으나 en·ja는 미보충 → "엔화를 받는다"로 읽힘 | ko `…상당 리워드 즉시 받기` 후 en·ja 동기화 |
| M-3 | `..._info_soldout` | ja | `すべて終了いたしました` | **소진(sold out)과 종료(ended)가 ja에서 동일 어휘** — 옆 키 `info_end`도 `終了`. th·zh·en은 구분됨 | `…はすべて配布終了となりました。` |
| M-4 | 3키(`jpyc_info_signup` 계열) | ja·zh | ja `当たりました`/`当選しました` · zh `恭喜中獎`/`恭喜獲得` | 동일 ko 문구가 ja·zh에서 각 2종으로 분기. zh `恭喜獲得`는 당첨 어감 소실 | ja `当選しました`·zh `恭喜中獎`으로 통일 |
| M-5 | `UF_home_jpyc_banner_title`·`jpyc_title` | th | `ได้รับรางวัล 100%` / `ชนะ 100%` | ko `100% 당첨`이 th 3종. `ชนะ`는 경기 승리 어감 | 용어집값 `ถูกรางวัล`로 통일 |
| M-6 | `jpyc_desc`·`jpyc_info_oa_title` | th | `LINE OA ของ Unifi` / `บัญชี Unifi LINE Official` | **동일 개념이 th 3종**, 그중 1건은 영어 미번역 혼입 | `บัญชีทางการ LINE ของ Unifi` |
| M-7 | 5키 | ja·en·th·zh | 개행 수가 ko와 불일치 | 배너 문구는 줄 구성이 곧 레이아웃. **형제 키 `UF_home_skyflag_title`(4언어 개행 제거) ↔ `..._MINI`(ja·en만 유지)** 상반 처리 | 처리 방침 하나로 확정 |
| M-8 | `unifi_promotion_unifi_text8a` | zh | `一起來了解…？` | 청유형에 `？` — 중국어 의문문 불성립 | `想了解…嗎？` |
| M-9 | `UF_home_jpyc_banner_title` | zh | `100%中獎率！` | `中獎率`(당첨**률**)로 의미 변형. 형제 2키는 `中獎` | `100% 中獎！` |
| M-10 | `..._info_oa_desc` | ko | `참여할 수 있는 이벤트 **참여**를 위해` | 한 문장 내 `참여` 중복 | `이후 연계 이벤트에 참여하시려면` |

> **`포이카츠` → `Poi-katsu`는 위반이 아니다.** 2차 검토가 en `Poi-katsu`·th `Poi-katsu`를 용어집(en `point activity`·th `การสะสมแต้ม`) 위반으로 올렸으나, **`Poi-katsu`는 이미 용어집 v4.1 등재 대기 항목**(HANDOFF P1)이다. 현재 번역이 정본이고 **용어집 쪽이 갱신 대상**이므로 오탐 처리한다.

---

## 5. ⓕ OA Flex 메시지 재생성 (zip v2 → **v3**)

사용자가 위키 Description의 ④ 버튼 URL을 교체했다.

| 필드 | before (zip v2) | after (zip v3) |
|---|---|---|
| `IMAGE_URL` | `…/e4183d7094cb….png?updatedAt=1779179421000` | **동일** |
| `ACTION_URL_1` (④) | `…/benefits-mini/**draw-promotion**?eventId=019fc71c…&utm_term=event22&referral_code=1810_SUOJB` | `…/benefits-mini/**luckyball-invite**?&utm_source=unifi_oa_alarm&utm_medium=cta&utm_campaign=unifi_luckyballwelcome&utm_content=august_2026&utm_term=event22&referral_code=1810_SUOJB` |
| `ACTION_URL_2` (⑤) | `https://miniapp.line.me/unifi/my/token/transaction` | **동일** (ⓖ 결정에 따라 쿼리 없음 유지) |

**라벨↔목적지 불일치가 해소됐다** — ④ 라벨 `친구에게도 JPYC 선물하기` ↔ 목적지 `luckyball-invite`.

### 🆕 URL 표기 6번째 변형 — 앵커·평문·span 3조각 분할

부록 6에 5가지(앵커 / `&amp;` / `nolink` span / `~:text=` / 평문)를 기록했으나, 이번 URL은 **한 URL이 평문과 `<span style=…>`로 쪼개져** 있었다.

```
… - https://…/luckyball-invite?<span style="font-size: 10.0pt;…">&amp;utm_source=…&amp;referral_code=1810_SUOJB</span>
```

`\S+` 단위 추출로는 `?`에서 끊긴다. **`<li>` 단위로 태그를 전부 제거하고 이어붙인 뒤 URL을 뽑는 방식**으로 교체했다. 추출기는 이제 6가지를 모두 처리한다.

### 무결성 검증 (5파일 전수)

| 검사 | 결과 |
|---|---|
| JSON 파싱 유효 | ✅ 5/5 |
| URL 3/3 반영 · 구 URL(`draw-promotion`) 잔존 | ✅ 반영 5/5 · 잔존 **0** |
| `&amp;` · `~:text=` · `TBD` 플레이스홀더 잔존 | ✅ 0 |
| `hero.action` 미포함 (md/OA.md 규격) | ✅ 5/5 |
| XLT 치환자(`{0}`·`{{0}}`) 혼입 | ✅ 0 — OA 변수는 `{{amount}}`·`{{wallet_address}}`만 |
| **첨부 라운드트립** (재다운로드 후 재검증) | ✅ 5,091 bytes · 5/5 URL 3/3 · 구 URL 0 |

번역값은 변경 없음(부록 4 게이트 결과 승계). **`md/OA.md` 규칙 1(격리) 재확인 — OA 문구·`{{변수}}`가 전역 번역표 47키·XLT 엑셀 3종에 유입 0건.**

---

## 6. 변경 3셀 — 2차 독립 검토 판정

`translation-reviewer` 에이전트(분리 컨텍스트, ko 원문·5개 언어·용어집만 열람)가 **235셀 전수** 재판정.

| # | 셀 | 판정 | 근거 |
|---|---|---|---|
| 1 | `mini_guidekim_luckyball_banner` th | ✅ **승인** | 용어집 v4.0 등재값과 정확히 일치. 이 변경으로 th 럭키볼 **5키 완전 통일**, 음차·미번역 잔존 0 |
| 2 | `UF_home_daily_mission_title_MINI` th | ✅ **승인** | 동일 근거. (개행 미보존은 변경 전부터 있던 별건 → M-7) |
| 3 | `UF_home_daily_mission_title_MINI` en | ✅ **승인** | ja·zh·th 3개 언어가 모두 '상당/worth'를 표현 → **en만 단독으로 틀려 있었음**이 교차 확인. `UF_home_jpyc_banner_title` en 표기와도 일치하게 됨 |

> 3번의 **줄바꿈 위치**는 P2로 남는다. `chance to receive / up to 150,000 JPY…`는 동사·목적어가 갈려 ko의 [후킹]/[금액] 2단 구조와 다르다. 대안 `Daily Lucky Ball chance\nGet up to 150,000 JPY worth of JPYC`. **차단 사유가 아니며 M-7(개행 정책)과 묶어 일괄 결정 권장.**

---

## 7. (d) 추가 개선·제안 (권장 — 임의 적용 금지)

### (d-1) 용어집 보완 권장

| # | 용어 | 현행 등재 | 권장 | 실측 P1 증감 | 판단 |
|---|---|---|---|---|---|
| G-1 | **`종료` zh_TW** | `結尾` | **`結束`** | **-1 / +0** | `結尾`는 글·이야기의 맺음말이라 UI '종료'에 부적합. `info_end` zh `本活動已結束。`가 미매칭 P1로 잡히는데 **번역이 아니라 등재값이 틀린 경우**다. 이 파일에 `結尾` 사용 셀 0건 → 신규 오탐 0. **강력 권장** (HANDOFF v4.1 등재 3건과 동일 항목 — 2차 검토가 독립적으로 재확인) |
| G-2 | **`포이카츠` en·th** | `point activity` / `การสะสมแต้ม` | **`Poi-katsu`** | -2 | 현재 번역이 이미 `Poi-katsu`. **등재값이 갱신 대상**이다(HANDOFF v4.1 등재 3건) |
| G-3 | **`캠페인`** | 미등재 | 신규 등재 | — | HANDOFF v4.1 등재 3건. 단 **ko 원문이 `이벤트`/`캠페인`을 혼용**(M-10 계열)해, 등재 전에 **ko 원문 용어 통일이 선행**되어야 한다 |
| G-4 | `포이카츠` zh | `點數活動` | `集點活動` 검토 | -1 | 실제 번역은 `集點活動`을 쓰고 등재값을 안 쓴다. 대만 실사용에 더 가까움. **G-2와 함께 결정** |

> ⛔ **승인 시 필수 동반**: 용어집 버전이 오르면 같은 작업에서 **기획자 가이드 zip도 갱신**해야 한다 (`md/landpress.md` §5-1 — 이력 표·전체 용어 표·수치 표기·임베드 JSON 4곳). 반영 창구는 **사용자가 CMS에 전체 JSON 붙여넣기**다.

### (d-2) 원문·정책 개선 제안

| # | 항목 | 내용 |
|---|---|---|
| 22 | **OA ④ URL의 `?&` 빈 파라미터** | `luckyball-invite**?&**utm_source=…` — 쿼리 시작에 빈 파라미터가 하나 붙어 있다. 대부분의 파서가 무시하지만 정상 표기는 `?utm_source=…`다. **사용자 입력값을 그대로 보존**했으므로 위키·zip이 일치한다. 정리하려면 위키 Description을 먼저 고친 뒤 zip을 재생성해야 한다 |
| 23 | **OA ④ `referral_code=1810_SUOJB` 고정값** | 특정 사용자 추천 코드가 고정으로 박혀 있어, 프로덕션 발송 시 **전 수신자가 이 코드로 유입 집계**된다. 의도된 캠페인 코드인지 확인 권장 (부록 6에서 "시즌3 전용으로 교체돼 지표 혼입 해소"로 판정했으나, **수신자별 코드가 아니라 단일 고정값**이라는 점은 별개 사안) |
| 24 | **개행 정책 확정** | 배너 계열 키의 `\n` 처리가 언어별로 갈린다(M-7). `UF_home_skyflag_title`은 4개 언어 개행 제거, `..._MINI`는 ja·en만 유지 — **같은 컴포넌트를 상반되게 처리**. 레이아웃 확인 후 일괄 확정 필요 |
| 25 | **ko 원문 `이벤트`/`캠페인` 혼용** | ko가 혼용해 ja `イベント/キャンペーン` · en `Event/campaign` · th `กิจกรรม/แคมเปญ`로 그대로 전파된다(zh만 `活動`으로 통일). **ko 통일이 선행**되어야 용어집 등재(G-3)가 가능하다 |
| 26 | **`UF_home_skyflag_title` ko 2건** | M-1(`미션하고`) · M-2(`상당`의 목적어 누락) — XLT 시스템 등록값이라 **시스템 갱신 승인 필요** |

---

## 8. 통과 판정

| 기준 | 결과 |
|---|---|
| 자동 P0 = 0 | ✅ **0건** |
| 수동 P0 = 0 | ✅ **0건** (235셀 전수) |
| P1/P2 전건 처리 판정 | ✅ 자동 P1 63 · P2 10 **전건 오탐 판정** / 수동 발견 M-1~M-10은 **사용자 확인 필요**로 명시(임의 적용 0) |
| 한국어 원문 교정 · alias | ✅ ko 변경 없음 → alias 발생 0. 사유 명시 |
| 전수 점검 | ✅ **47키 × 5개 언어 = 235셀** (변경분 한정 아님) · 2차 독립 검토 병행 |
| 산출물 정합 | ✅ 엑셀 `LV 40 ∪ UIT 7 == ALL 47`(중복·누락 0) · 라운드트립 3셀 반영 확인 · `plurals` 시트 고정 포맷 유지 |
| 위키 검사 | ✅ `check_wiki_storage.py` **pre exit 0 / post exit 0** (v42·v43 각각) |
| 첨부 갱신 | ✅ 같은 파일명 유지 → 본문 링크 그대로 최신본 (엑셀 ALL v10·LV v2·UIT v7 · Flex zip v3) |
| History | ✅ 2026-08-05 신규 1행 (같은 날 기존 행 없음 — 병합 대상 아님) |
| OA 격리 | ✅ OA 문구·변수가 전역 번역표·XLT 엑셀에 유입 **0건** |

**→ P0 = 0. 게이트 통과.**

# season3 — JPYC 럭키볼 시즌 3

> 담당자: `hogeun` · 마지막 갱신: 2026-08-10 · 세션 #20 · 커밋 `c1563c3`

## 대상 / 링크

- 위키(주, 2026-08-04~): `4541588845` **JPYC 럭키볼 시즌 3** — **현재 v81**(2026-08-10 사용자 편집)
- 구성: Screen UIT 2 + LV(mini 2 · Promotion Page 6 · Popup 4) + OA MSG 2 = **프레임 16개** · 번역표 **58키**
- Figma: 파일 `GOCHAYBS7hIrmWRGNuJOKV`(Web3) · 시즌3 페이지 `65923:2485`
- 엑셀: `xlt/xlt_output_season3_{ALL,LV,UIT}_20260804.xlsx` — **ALL v22 · LV v13 · UIT v18**
  - ⚠️ **파일명 고정**(날짜 올리지 않고 내용만 갱신) — 본문 `view-file` 매크로가 파일명을 참조하므로 바꾸면 링크가 끊긴다. 첨부 갱신은 `POST .../child/attachment/{id}/data`
- OA Flex zip 2종: `Reward_Confirm` v5 · `mission_complete` v4 (`oa/flex_OA_*`)
- 담당 FE: LV(`unifi_promotion_`·`mini_`·`{0}`) + UIT(`UF_`·`{{0}}`) 혼재 · OA는 팀 아님
- 게이트 리포트: `reports/gate/gate_report_season3_*.md`, `gate_report_luckyball_*.md`

## 현재 상태

미결 **0건**(세션 #20에서 14건 → 0건 종결). 위키 v81 · 게이트 P0=0 · `check_gate_report.py` exit 0.

**IA #4에서 온 교체 대상**(세션 #21 발견 — 시즌3 문구에 남아 있으면 갱신): 🔴 USDT PLUS 이율 **5%→7%**(8/6 발효, Boost 합산 최대 연 10%) · ✅ 액션 라벨 개편(보내기/채우기/교환하기/은행출금 + 「외부 지갑 연결」).

## 진행 중 작업(WIP)

없음.

## 다음 할 일

- (추적 종료) XLT 수정 엑셀 업로드는 사용자 수행 — `xlt/registry_fix/xlt_fix_Unifi_20260810.xlsx`(1키, zh `網址`→`URL`). 업로드 후 `fetch_xlt_registry.py` 재조회로 반영 확인 권장(세션 #18 미반영 실측 전례)

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-07 | **의미 동등한 ko 교정에서는 다른 언어를 건드리지 않는다** — 교정 후 ko와 현행 번역을 대조해 **의미가 실제로 바뀐 키만** 재번역 | terms 8키 중 7키는 어순·띄어쓰기·조사·금지어 대체라 의미가 같았다. 4개 언어를 함께 흔들면 검증된 번역이 근거 없이 바뀐다. `early_end`만 5개 언어 재번역. 커밋 `eea1fb9` |
| 2026-08-07 | **엑셀 첨부 파일명의 날짜는 올리지 않는다** | `view-file` 매크로가 파일명을 참조 — 바꾸면 링크가 끊긴다 |
| 2026-08-05 | **OA MSG 섹션은 UIT·LV와 동급 `h3`** / **mini는 LV 안 첫 `h4`** | OA는 FE 팀이 아니라 LINE OA 콘솔·Messaging API 발송이라 팀 프리픽스·`{0}` 규칙이 적용되지 않는다(LV 하위에 두면 규칙 오상속). 커밋 `c20b332` |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지 — 이 프로젝트 한정)

> 근거는 각 게이트/감사 리포트에 보존돼 있다.

- **⛔ 2026-08-10 미결 전건 종결**(사용자 지시 「남은 건은 진행하지 않고 완결 처리」)
  - **처리 4건** — ① 용어집 예외 위반 5건 중 **#7만 수정**(zh `網址`→`URL`), #8~#11은 팀 리뷰 시 전체 XLT 조회로 이관 ② 시즌3 감사 후속 6건 **전건 현행 유지**(ⓑ 키 중복·ⓒ Bottom Sheet 참조 오기는 **Figma 수정 요청도 하지 않음** / ⓔ 고정 추천 코드 2종도 그대로) ③ 잔여 번역 개선 M-1·M-4~M-10 **전건 현행 유지** ④ `포이카츠` zh → 용어집 v4.7 `集點活動`으로 종결
  - **신규 발견분도 함께 종결** — 서비스 간 값 분기 3건(`unifi_promotion_jpyc_desc`는 **두 서비스의 문구 자체가 다름** · `_jpyc_title`은 Unifi 쪽만 zh `中獎率`·th 영어 혼입·**리터럴 `\n`** · `_jpyc_info_oa_title`은 Unifi 쪽 zh `關注`=deprecated) · `unifi_promotion_jpyc_bonus_desc` DP ko 맞춤법(`지급되요`·`유지시`)
- **감사 리포트의 「위키 결함 확정 1건」은 오판이었다**(2026-08-10) — `Reward Confirm Bottom Sheet` 요약문 `회원가입 완료 후`는 정책 1번과 모순이 아니다. 정책 1번은 **진입 경로·전제 조건**, 요약문은 **노출 시점**이며 화면 XLT(`가입이 완료됐어요!` · `당첨된 {0} JPYC는 2주 이내로 지급됩니다.`)가 요약문을 지지한다. **「상충」으로 재보고 금지.**
- **당첨금 지급 시점은 「2주 이내」다**(2026-08-06) — 2026-08-05의 「즉시 지급(최대 5분 내)」을 **번복**한 것. `info_signup_desc`·`bottomsheet_signup_text2` 반영 완료. 「즉시 지급」 되돌리기 제안 금지.
- **OA 2건은 병행 운영**(2026-08-06) — `(OA)mission complete`=미션 완료 시 / `(OA)Reward Confirm`=지급 시점. **대체 아님.** 삭제·통합 제안 금지.
- **`(Promotion) Unifi Member`·`(Popup) Unifi member` 화면은 삭제됐다**(2026-08-06, 사용자) — 위키 행·첨부 2건 정리 완료. **복원·재추가 제안 금지.** 「이미 회원」 문구는 `unifi_promotion_info_already_member` **신규 키로 일원화**. ⚠️ `mini_luckyball_already_member`가 **시스템에는 아직 남아 있고**(2026-08-07 API 실측) **3키 중복**(`unifi_promotion_info_already_member` · `mini_luckyball_already_member` · `mini_luckyball_already_member_go_home`) 상태 — 정리 여부는 **사용자 판단으로 미결 종결**(FE 참조 확인 선행 필요, 이 문서에서 추적하지 않음)
- **OA `altText`에 변수를 쓸 수 없다**(2026-08-05) — 금액 삽입안 종결. altText 변수화 재제안 금지. (부록 7의 "봉투 텍스트에서도 치환 가능" 서술은 무효)
- **OA `altText`는 bubble JSON에 넣지 않는다**(2026-08-05) — 봉투 값이고 Flex Simulator가 bubble만 받는다.
- **OA ⑤ `확인하기`는 유입 추적하지 않는다**(2026-08-05) — utm·referral_code 추가 제안 금지.
- **`unifi_promotion_jpyc_info_oa_desc` 정본**(2026-08-06 사용자 최종) — `다양한 이벤트 소식을 가장 빠르게 받아볼 수 있는 Unifi의 LINE 공식 계정 친구를 유지해주세요.` 5개 언어 반영. 이전 ⓐ안(`이후 연계해 참여할 수 있는…`)으로 되돌리기 제안 금지.
- **OA·화면 문구 정본**(2026-08-06 사용자 최종) — `bottomsheet_signup_text3`와 OA 2화면 모두 `…친구에게도 JPYC 선물하면 JPYC 럭키볼 최대 20개를 더 받을 수 있어요.`(「총 2개」→「최대 20개」). **수치는 고정 문구 — 변수화하지 않는다.**
- **mini 키 미부여 4건 · `최대 60만엔`은 조치하지 않는다**(2026-08-05) — 기존 키가 있고 **XLT 키의 문구가 정답**. 확인 목록 재생성 금지.
- **`UF_home_skyflag_title` ko는 유보**(2026-08-05) — en·ja만 목적어 보충, **ko 원문(`미션하고`·목적어 누락)은 시스템 등록값이라 건드리지 않는다.** 시스템 갱신 시 함께 처리.
- **`{{amount}}` 소급 치환 금지**(2026-08-05) — `oa/` 기존 캠페인 파일과 위키 History 이력 문장은 그대로. 신규·수정부터 `{{total_amount}}`.
- **`XLT 확인 필요 목록` 섹션 삭제**(2026-08-05) — 26행 전부가 시스템 등록/갱신 항목. **재생성 금지.** 미결 결정 3건은 게이트 리포트 부록 3 보존.
- **Button Case 어노테이션 ⓝ⑤가 `unifi` 로고를 가리는 것은 문제없다**(2026-08-06 확정) — 로고가 TEXT 노드가 아니라 `collect_frames.py` 텍스트-회피가 감지 못하는 케이스. 도구 개선·재렌더 재제안 금지.
- **`mini_luckyball_invitee_mission`** 재추가 금지(2026-07-27).

## 세션 기록 (최신 위, 최대 5개)

### 2026-08-10 — 세션 #20(시즌3 분): 미결 14건 → 0건 (`3aab91a`)

- 4건 처리 후 나머지는 위 「종결」에 근거와 함께 기록. XLT zh `網址`→`URL` 1키 게이트 P0=0 · `check_gate_report.py` exit 0.
- **배운 것**: **「미결」이 실제로 미결인지부터 확인해야 한다** — 14건 중 3건이 이미 해결돼 있었다. 리포트 시점(v41)과 라이브(v81)의 40버전 차이를 재조회로 좁히지 않았으면 해결된 건을 다시 물을 뻔했다. · **감사 리포트의 「결함 확정」도 오판일 수 있다.**

### 2026-08-07 — 세션 #18: 유의사항 8키 재번역 + mini 프레임 교체 (`eea1fb9` · 위키 v77→v80)

- **terms 8키 원문 갱신·재번역** — 3원 대조(Figma·위키·시스템)로 위키 v77이 최신 원문임을 확정 → 원문 교정 9건(`OA 팔로우` 금지어·어순 2·띄어쓰기 3·nbsp 8개) → `early_end` 1키만 5개 언어 재번역
- **`(Unifi mini) - Login x` 프레임 교체** — Step 2-1 유사 키 대조로 **신규 키 0건**, `mini_guidekim_daily_mission`만 시스템 등재값 승계로 번역표 추가(57→58키)
- 게이트 P0=0 · P1 67(신규 키 추가에도 증감 0) · `check_wiki_storage.py` pre/post exit 0 · 첨부 라운드트립 불일치 0건
- **배운 것**: **「무엇이 바뀌었나」를 먼저 특정**(실제 변경은 Screen 표 KR 교체 — v76↔v77 diff 없이는 전수 재번역으로 갔다) · **위키가 「제3의 정본」일 수 있다**(v77 문구는 Figma·시스템 어디에도 없는 최종본) · **사용자 반영은 검증해야 한다**(「업데이트함」을 받고 API 확인 → 9키 중 1키 미반영)

### 2026-08-06 — 세션 #14: 금액 정정 5키 + OA 2건 재번역 (`e338cbf`~`870fc33` · 위키 v76)

- UIT 2키 15만엔→5만엔 · 시즌3 변경 5키 + OA 2건 재번역. **HANDOFF 부분 갱신**으로 세션 #16이 커밋 로그로 복원했다.

### 2026-08-06 — 세션 #10: `Updated Fram` 6프레임 지급 시점 「즉시」→「2주 이내」 (`b6b5f93` · 위키 v47→v56)

- 신규 키 1 · orphan 삭제 1 · **신규 OA `(OA)mission complete`**(Flex 5개 언어+zip) · OA `ACTION_URL_1` 2회 갱신 · 어노테이션 4건 재렌더
- **배운 것**: 게이트 수치는 도구 버전에 매인다(리포트에 검증기 커밋 해시 명기) · **사용자 편집이 삭제일 수도 있다**(PUT 후 `본문 참조 = 첨부` 대조) · **`overlaps=없음`이 가림 없음을 뜻하지 않는다**(TEXT 노드만 회피 — 육안이 최종 관문)

### 2026-08-05~06 — 세션 #11: Promotion page(재사용 6키·신규 5키) (`ff14c25`)

- **배운 것**: 위키 표 렌더 ≠ 원본(붙어 보이는 문구가 개행일 수 있다) · 문자열 치환은 **구간 한정 + assert**(전역 치환은 History 이력을 훼손한다)

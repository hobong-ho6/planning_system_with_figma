# masters — 마스터 페이지 5종 + 정책 문서

> 담당자: `hogeun` · 마지막 갱신: 2026-09-16 · 세션 #16 · 5종 정합은 2026-09-13 실측 유지 · **K-Pick 바우처 마스터 3종 갱신**(09-16)

## 대상 / 링크

Release History **데이터 행 ↔ Summary `<h3>버전 N` 블록 1:1** 상태:

| 페이지 | ID | 버전 | 정합 |
|---|---|---|---|
| [Master] Lucky Ball Promotion | `4368569057` | v17 | ✅ 8↔8 |
| [Master] Mission and Reward | `4368568387` | **v20** | ✅ **10↔10** |
| [Master] Unifi mini | `4386238705` | **v12** | ✅ **4↔4** |
| [Master] Wallet Mode | `4386238738` | v3 | ✅ 2↔2 |
| [Master] K-Pick | `4686410639` | **v6** | ✅ 8↔8 |

> **2026-09-13 전건 라이브 실측 + 수정 완료.** 재확인 시점에 Mission 10↔9 · mini 4↔3으로 깨져 있었고(다른 세션/사용자가 History 행을 추가하고 Summary가 따라가지 않음), 참조 위키를 원본 조회해 Summary 블록을 신설했다. Lucky Ball·Wallet Mode는 기재값대로 정합이었다.

- `4479295382` **K-Pick 노출 정책**(v10) — mini 마스터 History 2행의 하위 문서. K-Pick 노출 조건의 **최신 정본**(②To-be)
- `4368569133` Mission and Reward 정책 + FAQ(v39) — FAQ 10문항 5개 언어(미결정 6건은 종결)

## 마스터 페이지 정합 규칙 (이 프로젝트 절차 정본)

- `Release History` **데이터 행 수 = Summary `<h3>버전 N` 블록 수**
- ⛔ **행 카운트 정규식은 속성을 허용해야 한다**(2026-09-13 실측) — 실제 storage는 `<td class="numberingColumn" contenteditable="false" data-mce-resize="false">`다. `<td class="numberingColumn">` 정확 일치로 세면 **3종 모두 0건**이 나와 「전건 불일치」로 오판한다. `<td class="numberingColumn"[^>]*>`로 센다(헤더는 `<th`라 자동 제외).
- 검증 앵커는 `<h1>Release History</h1>`를 쓴다 — History 행 본문에 "Release History"가 들어가 **오집계된 실측**이 있다
- 릴리즈 버전 미확정이면 제목에 `[PL] Unifi (버전 미정)`
- **행이 삽입되면 Summary 번호를 밀어 복구**한다 — 사용자가 History에 행을 넣는 것이 정상 흐름이고 Summary가 뒤늦게 따라간다(실측 2회)
- **이미 정합인데 "업데이트" 요청을 받으면** 라이브와 **직전 버전(`?status=historical&version=N-1`)을 diff**해 사용자의 마지막 편집을 특정한다(mini 3↔3 실측)
- **버전이 오르면 「정합」 기재는 자동으로 무효다** — 마스터 버전만 정정하고 정합 열을 그대로 두면 낡은 ✅가 남는다. 버전 정정 시 정합을 함께 실측하거나, 못 하면 **「미실측」으로 명시**한다(2026-09-13 이 표시가 재확인 경로가 됐다)
- **「2곳 갱신」류 TODO는 절반만 진행된 채 완료처럼 보인다** — K-Pick 버전 8은 History 셀만 채워지고 Summary 제목이 `(버전 미정)`으로 남아 있었고 **정합 카운트로는 잡히지 않았다**. 릴리즈 확정 TODO는 History 셀 ↔ `<h3>` 제목을 **쌍으로** 확인한다
- 참조 문서가 구·신으로 갈리면 같은 블록에 **①As-is → ②To-be 이력**으로 배치하고 최신 쪽을 명시한다

## 현재 상태

**5종 전부 정합**(2026-09-13 전건 실측 후 3페이지 수정 완료 · 위키 3건 PUT).

- Mission and Reward **v19→v20** — 버전 10 블록 신설(참조 「리워드탭 개선」 v19 원본 조회). Release History 10행 `Version` 셀이 비어 있어 제목은 `[PL] Unifi (버전 미정) / 리워드탭 개선`
- Unifi mini **v11→v12** — 버전 4 블록 신설(참조 「[Unifi Mini] 홈을 LIFF K-Pick 홈과 통일 + 배너 문구 변경」 v11 원본 조회). K-Pick 마스터 버전 4와 같은 소재지만 **mini 홈 관점**으로 작성
- K-Pick **v5→v6** — Summary 버전 8 제목을 `(버전 미정)` → **`[PL] Unifi v1.7.5`**로 갱신(History 셀이 확정돼 있었다). 정합 8↔8 유지
- 검증 — `check_wiki_storage.py` pre(라이브·보낼 body) / post(렌더) **전부 exit 0** · PUT 후 본문 길이 = 전송 길이 일치 · 내부 링크 4종 존재 확인(깨진 링크 0건)

## 진행 중 작업(WIP)

없음.

## 다음 할 일

- [ ] **P1 — K-Pick 바우처 마스터 `syncExcludedFields` 확인**: `badges`·`reviewCount`가 제외 목록에 없어 GuideKim 동기화 시 덮일 수 있다. 고정이 필요하면 두 필드를 추가한 JSON으로 재등록(2026-09-16 세션 #16)

- **Mission and Reward 버전 10 릴리즈 확정 시 2곳 갱신** — Release History 10행의 `Version` 셀이 비어 있어 Summary 제목을 `[PL] Unifi (버전 미정) / 리워드탭 개선`으로 표기했다. 릴리즈가 정해지면 History 셀과 Summary `<h3>` 제목을 함께 채운다(K-Pick 버전 8에서 **History만 채워지고 제목이 남는 절반 완료**가 실제로 발생했으므로 반드시 2곳을 함께 본다)
- 참조 위키가 갱신되면 해당 Summary 블록도 따라 갱신한다(하위 문서 → 마스터 방향) — 특히 `4667512757` 클리닉(법무 잔여 조치 진행 중)·`4637821463` 배너(Unifi↔DP 값 분기 미해소)

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지)

- **K-Pick 노출 조건은 ①As-is → ②To-be 이력으로 기록한다**(2026-08-06 세션 #13, 사용자 지시) — `K-Pick 노출 정책`(`4479295382`)이 Guide Kim 통합 시점 정책보다 **나중에 확정된 최신 정본**이다. mini 마스터 버전 2 블록에서 **①**(JP IP만 노출 + KR IP 일부 버튼 비노출·국가 서비스 불가 안내) → **②**(JP IP **또는** KR IP + 언어 설정 non-KR로 확장)로 배치하고 **②가 ①을 대체**했음을 명시했다. **「정책 충돌」로 재보고하지 않는다.**
- **`K-Pick 노출 정책` 문서의 UIT 표 3컬럼은 그대로 둔다**(2026-08-06) — `check_wiki_storage.py pre`가 4컬럼 위반으로 잡지만 **편집 전부터 있던 기존 상태**이고(번역 없는 화면이라 XLT 컬럼 부재), `md/wiki.md`의 "기존 페이지는 현행 컬럼 구조를 따른다(소급 변경 금지)"가 우선한다. 이 페이지 편집 시에는 **exit 0 대신 「편집 전/후 위반 목록 동일 = 신규 위반 0건」을 통과 근거**로 삼는다.
- FAQ 미결정 6건 — 2026-08-10 미착수 종결.

## 세션 기록

### 2026-09-16 — 세션 #16: K-Pick 바우처 마스터 3종 갱신 (LIAM HUB · git 커밋 0건)

- ⭐ **조회 경로 발견** — `GET https://480-unifi-apps-cms.hub-beta.linecorp.com/v1/k-pick-vouchers?page=0&size=500`(beta 어드민 API · 브라우저 세션 필요 · **읽기 전용**). 어드민 화면이 **iframe**이라 부모 프레임 JS로는 DOM 접근이 안 된다 → **이 URL로 직접 열어 읽는다**. 메뉴는 `service-view/480?menuId=27347`(K-Pick) › 상품권 탭
- **badges 18/18** — `공식 바우처`·`즉시 발급` 2종 × 5개 언어를 전 항목에 부여(기존 1건 → 18건). 붙여넣기 후 **필드 단위 전건 대조 일치**
- **reviewCount 3건 조정** — CU 1만원권 **1** · CU 5천원권 **2** · 이마트24 1만원권 **3**
- **상품명 12종 축약 × 5개 언어** — `브랜드 + 금액`(ko는 「상품권」 제외) · en·th·zh 금액 **`₩10,000`** · **ja는 일본어 검수본 표기**(`1万ウォン` + `商品券`) · 브랜드는 **공식 표기 확인 후** `emart24`(소문자 · ja는 검수본 `イーマート24`)·`CU`. 종전 `E-Mart`·`CU편의점`은 오표기
- ⚠️ **`syncExcludedFields`에 `name`만 있고 `badges`·`reviewCount`는 없다** — GuideKim 동기화가 돌면 **이번 값이 덮일 수 있다**(미결)
- 반영 방식: Claude가 JSON 생성·검증 → **사용자가 어드민에 붙여넣기**(쓰기 API는 확인하지 않았다)

### 2026-09-13 — 세션 #15: 5종 전건 정합 실측 + 3페이지 수정 (**위키 3페이지 PUT**)

- 계기: 핸드오프 정리 중 **기재 버전과 라이브가 달랐다**(Mission 16→19 · mini 9→11 · K-Pick 3→5). 버전이 올랐으면 정합이 깨졌을 수 있어 재대조했고, **실제로 2종이 깨져 있었다**
- 1차 결과 — **Mission 10↔9 ❌** · **mini 4↔3 ❌** · **K-Pick 8↔8 ✅**. 이어서 미실측 2종도 실측 → **Lucky Ball 8↔8 ✅ · Wallet Mode 2↔2 ✅**(기재값과 일치). 둘 다 **History에 행이 추가되고 Summary가 따라가지 않은** 같은 패턴(이 프로젝트 규칙에 이미 「행이 삽입되면 Summary 번호를 밀어 복구」로 적혀 있던 전형)
- **수정**(사용자 지시 「전부 수정」) — Mission v19→v20 버전 10 블록 신설 · mini v11→v12 버전 4 블록 신설 · K-Pick v5→v6 제목 갱신. 참조 위키는 **캐시 없이 원본 조회**(리워드탭 개선 v19 · mini 홈 통일 v11). 각 페이지의 작업 이력 History 표에도 관행대로 1행씩 추가(PIC 「Claude 자동 생성」). **5종 전부 ✅**
- **배운 것** ⓐ **카운트 정규식이 오탐을 만들었다** — `<td class="numberingColumn">` 정확 일치로 세니 3종 모두 0건이라 「전건 불일치」로 보였다. 실제 storage에는 `contenteditable`·`data-mce-resize` 속성이 더 붙는다. **검사 결과가 「전건 실패」면 대상이 아니라 검사기를 먼저 의심한다** ⓑ **「정합」 같은 파생 상태는 원본 버전이 오르면 자동으로 무효가 된다** — 버전만 정정하고 정합 열을 그대로 두면 낡은 ✅가 남는다. 그래서 09-13 정정 때 「미실측」으로 명시해 두었고 이번에 그게 재확인 경로가 됐다 ⓒ **부분 완료 TODO는 완료로 보이기 쉽다** — K-Pick 「2곳 갱신」 중 History 셀만 채워지고 Summary 제목은 `(버전 미정)`으로 남아 있었다(정합 카운트로는 안 잡힌다)

### 2026-09-03 — 세션 #14: [Master] K-Pick Summary 8블록 신설 (**위키 1페이지 v1→v3, git 커밋 0건**)

- `4686410639` **[Master] K-Pick** — v1은 History 8행만 있고 Summary가 비어 있었다. Lucky Ball 마스터(`4368569057` v17) 형식을 준용해 참조 위키 8건을 **전부 원본 조회**하고 버전별 정책 블록으로 취합했다. **History 8행 ↔ Summary 8블록 정합**
- 참조 위키(조회 버전): `4339334947` Guide Kim v187 · `4453760503` 간편 로그인 v7 · `4479295382` K-Pick 노출 정책 v10 · `4637821463` 배너 v11 · `4639704548` 올리브영 v47 · `4639718809` 특상법 v9 · `4661828572` 퍼널 v5 · `4667512757` 클리닉 v86
- History 표에 작업 이력 1행 추가(PIC 「Claude 자동 생성」) — 사용자의 「문서생성」 행과 **병합하지 않고 별도 행**으로 남겼다(주체가 다르다). `check_wiki_storage.py` pre/post 2회 PUT 모두 exit 0 · 렌더 뷰 내부 링크 전수 확인(깨진 링크 0건)
- **배운 것**: ⓐ **PUT 직후 storage는 Confluence가 정규화한다** — v2 반영 뒤 두 번째 surgical 교체에서 앵커가 0건이 났고, 원인은 일반 공백이 `\xa0`(nbsp)로 바뀐 것이었다. **PUT 이후 이어지는 편집의 앵커는 라이브 재조회본에서 다시 뜬다** ⓑ **Summary 작성은 번역 게이트 비대상**이다(신규 번역 생성 없이 확정 번역·정책을 인용) — 다만 참조 위키의 미결·잔여 조치(클리닉 법무 2건·배너 서비스 분기)는 요약에 **명시적으로 남긴다**


### 2026-08-06 — 세션 #13: 마스터 2종 재정합 + K-Pick 이력화 (**위키 3페이지, git 커밋 0건**)

- Lucky Ball v13(History 8행↔Summary 7블록 해소 — 버전 7 신설·시즌3를 버전 8로) · mini v9(K-Pick ①→② 이력화로 구·신 상반 해소) · K-Pick v10 오타
- **배운 것**: **정합인데 "업데이트" 요청이 오면 직전 버전 diff가 답이다** · **전역 금지 가드는 이력 기록과 충돌한다**(`(버전 미정)` 가드가 History 인용문을 잡음 → h3 제목 대상으로 정교화) · **검사기 exit 1이 항상 내 잘못은 아니다**(편집 전 라이브에도 있던 위반 → `pre`로 찍어 신규 위반 0건을 근거로 진행)

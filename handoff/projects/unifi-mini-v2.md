# unifi-mini-v2 — Unifi mini v2.0 화면정의 (Figma 정책 취합)

> 담당자: `hogeun` · 마지막 갱신: 2026-09-08 · 세션 #1 · 마지막 커밋 `f0c1c5e`

## 대상 / 링크

- 위키: [pageId 4704515582](https://wiki.workers-hub.com/display/UNIFI/Unifi+mini+v2.0) `Unifi mini v2.0` — **현재 v36**
  - 부모: `4699317053` `Unifi mini v2.0.0 범위 검토`(작성자 `jyjeong`) → 그 아래 `Version undefined`(`3576214986`) 계층
  - ⚠️ **제목이 세션 중 바뀌었다** — 최초 생성은 `디자인안 기준 검토`(v1), 사용자가 v20 무렵 `Unifi mini v2.0`으로 변경. pageId는 동일
- Figma: 파일 `GOCHAYBS7hIrmWRGNuJOKV`(`Web3`) / 페이지 **`Unifi mini v2.0`**(`70781:3378`) / 섹션 `[마이]`(`72193:10491`) 등
  - ⚠️ 최초 요청에 쓰인 `uyUkM7rWWg1vAwpf47um86`(`[Share] Unifi Design`) 페이지 `65736:1612`는 **Background·Design 임베드용**이고, Screen 표의 프레임은 전부 `Web3` 파일이다(파일이 다르다)
- XLT: **아직 번역 미착수** — 담당 FE 팀(UIT/LV) 미확정 · 키 프리픽스 미정
- 게이트 리포트 prefix: `reports/gate/gate_report_unifi_mini_v2_*.md` (아직 없음 — 번역 미착수)
- 이미지: **위키 첨부가 정본**(51장). 로컬 `assets/collected_*`·`assets/{admin,api,open}_regions`는 git 미추적 재생성물

## 현재 상태

**정책·어노테이션 취합 완료(번역 제외).** 20화면 × 정책 121건을 Screen 4섹션에 반영하고, 정책을 성격별로 재취합한 **단 3개를 신설**했다. 검증은 매 PUT마다 `check_wiki_storage.py` pre/post exit 0 · 버전 가드 통과 · 어노테이션 핀 육안 확인.

| 영역 | 행 | 비고 |
|---|---|---|
| Screen › Home | 2 | 홈탭1(정책 20)·홈탭2(12) |
| Screen › Category | 4 | 카테고리 1(8)·카테고리 2(4)·카테고리 선택(1)·카테고리 정렬(7) |
| Screen › Search | 4 | Search 1(8)·- memory(3)·- result(1)·- result - sub category(1) |
| Screen › My | 10 | My(30)·JPYC 자산 상세(10)·혜택 상세(4)·바우처 상세(6)·상품권 이미지 저장(0)·상품권 상태(4)·상품권 상태-사용완료(1)·-기한만료(1)·여행 상품 상태(1)·클리닉 상태(1) |
| **Admin 관리 대상** | 13 | 정책에 `Admin` 언급 전수 |
| **API 확인** | 5 | 정책에 `API` 언급 전수 |
| **확정 필요 항목** | 10 | `확인 필요·재정의 필요·미확정` 전수 |

- **Screen ID = 프레임 이름**(사용자 지시). `md/wiki.md` 공식 룰(구조화 소문자 `주기능_부기능_01`)과 다르지만 이 페이지는 프레임명 기준으로 확정했다 — 47행 전부 적용
- **번역은 전 구간 제외**했다. `xlt` 마커는 Description에 「번역 대상 `{문구}`」로 기록만 해 둔 상태
- 취합 단 3개의 이미지는 **정책이 가리키는 빨간 구분 박스만 크롭**(박스 여백 28pt · 박스 없으면 밴드 ±110pt)

## 진행 중 작업(WIP)

없음. (이 세션 산출물은 위키에만 있고 저장소 코드 변경은 없다)

## 다음 할 일

- [ ] **P1 — `xlt` 마커 번역**: 마커가 20화면에 걸쳐 있다. 착수 전 3가지 확정 필요 — ⓐ **담당 FE 팀**(UIT `UF_`·`{{0}}` / LV 약어·`{0}`) — 이 위키에 UIT/LV 구분이 없어 **사용자에게 질문 필요** ⓑ **배너 캐러셀 슬라이드 2·3 포함 여부**(마커 박스는 슬라이드 1만 감싼다 · 아래 결정 표 참조) ⓒ **혜택 상세 문장 변수화**(`이번 달 {0} 상당의 혜택 받았어요` — TEXT 노드 3개로 분리돼 있고 금액이 별도 노드)
- [ ] **P1 — 「확정 필요 항목」 10건 처리(기획·FE)**: 타임세일/조합 프로모션 미노출 정책 재정의 2건 · 판매·클릭 수 조회 가능 여부 · 평점 정렬 API · Created at/Modified at 관리 여부 · JPYC 출금 URL · Guidekim API · **각 상태값 확인 3건**(상품권/여행 상품/클리닉 상태)
- [ ] **P2 — Figma 원문 교정 요청(디자이너)**: 홈탭1 정책 7 `categroy name` → `category name`
- [ ] **P2 — 슬라이드 2·3 구조 비대칭**: 배너 슬라이드 1은 제목/본문이 **TEXT 2노드**인데 2·3은 **한 노드 안 줄바꿈**(둘 다 18pt·CENTER). 번역 키를 통일하려면 Figma 정리 요청이 필요하다
- [ ] **P2 — GNB 라벨 마커 범위**: 홈탭1 정책 20 마커가 좌표상 `Search` 하나에 매칭됐으나 GNB 라벨은 4개(`Home`·`Search`·`Rewards`·`My`). 번역 시 4건 확장 여부 확인

## 주요 결정 사항 (이 프로젝트 한정)

| 날짜 | 결정 | 근거 / 실측 |
|---|---|---|
| 2026-09-08 | **Screen ID = 프레임 이름** | 사용자 지시. IA 어휘가 mini 홈에 대해 잠정(`md/IA.md` §4 승인 대기)이라 구조화 ID를 확정할 수 없는 상태였다 |
| 2026-09-08 | **번호 체계 = 코멘트 1건 = 1번호**(박스 기준 아님) | 사용자 선택. 빨간 박스 기준(영역당 1번호)도 제시했으나 XLT No와 1:1로 맞물리는 기존 방식을 유지 |
| 2026-09-08 | **정책 성격별 취합 단 3개 신설**(Admin 관리 대상 · API 확인 · 확정 필요 항목) | 사용자 요청. 표 형식은 화면정의 표와 동일한 4컬럼 고정 · 이미지는 정책이 가리키는 영역만 크롭 |
| 2026-09-08 | **`72193-11736`(TEXT `결제 완료`) → 프레임 `상품권 상태`(`72193-11636`)로 대체** | 사용자가 준 링크가 51×16 TEXT 노드였다. 그 노드를 담은 프레임이 `여행 상품 상태`·`클리닉 상태`와 형제인 상태 목록 화면 → **사용자 확인 완료(맞음, 그대로 유지)** |
| 2026-09-08 | **카테고리 정렬 `xlt` 마커 = 정렬 옵션 6개 전체** | 사용자가 「전체 문구를 xlt로 지정」이라 명시. 좌표 매칭은 `추천순` 하나만 잡는다 |
| 2026-09-08 | **미참조 첨부 3건 삭제**(`raw_72193-5132`·`raw_72193-11353`·`annotated_72193-9526`) | 사용자 요청. 삭제 전 본문 참조 0건 확인 · 로컬 사본 존재 |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지 — 이 프로젝트 한정)

- **`상품권 상태` 프레임 대체 확정** — `72193-11736` 대신 `72193-11636`을 쓰는 것이 맞다(2026-09-08 사용자 확인). 재확인 금지
- **4화면(카테고리 1·2·선택·정렬)은 Category 섹션** — 한때 Home에 넣었다가 사용자 지시로 이동 완료. 되돌리지 않는다

## 세션 기록 (최신 위, 최대 5개)

### 2026-09-08 — 세션 #1: 위키 신규 생성 → 20화면 정책·어노테이션 + 취합 단 3개 (v1→v36)

- **완료**: 정본 템플릿으로 페이지 생성(History PIC = 요청자 멘션 · Related Docs Design/Planning · Design 섹션에 Figma widget 임베드 width 1000) → Screen 4섹션 20화면 반영 → Admin/API/확정 필요 단 신설 → Screen ID 47행 채움 → 크롭 마진 확대 재생성(27장) → 미참조 첨부 3건 삭제
- **교훈 ① — `xlt` 마커 판정이 구두점에 걸린다**: `xlt, GuideKim으로 이동`(첫 토큰이 `xlt,`)을 「본문이 XLT로 시작」으로 못 잡아 정책(빨강)으로 분류했다. 판정을 **줄 단위·첫/끝 토큰 + 구두점 제거**로 넓혀 해소(My 정책 29·30). ⚠️ **`scripts/collect_frames.py`에는 아직 미반영** — 세션 스크래치패드에서만 고쳤다(전역 컨텍스트에 등록)
- **교훈 ② — 크롭·행 재생성 시 「정책 번호」로 매칭하면 안 된다**: 코멘트가 추가되면 번호가 밀린다(카테고리 정렬 6→7건에 `xlt`가 **1번으로 삽입**돼 이후 전부 +1). 본문 문구로 재탐색해야 하는데, ⓐ 홈탭1 정책 8·19가 **문구 동일**(답글만 다름) ⓑ `xlt` 한 단어 정책이 다수여서 **첫 일치가 오매칭**됐다 → **(본문, 답글, 원 y좌표)** 3중 키로 tiebreak해야 정확하다(27장 재생성에서 2건 잡음)
- **교훈 ③ — 프레임 이름은 세션 중에도 바뀐다**: `상품권`→`카테고리 1`, `클리닉`→`카테고리 2`, `카테고리`→`카테고리 선택`. Screen ID를 이전 조회 산출물(`assets/collected_*/frames.json`)에서 채우다 **옛 이름이 최신 이름을 덮는 버그**를 만들었다(glob 정렬 순서). PUT 전에 발견 → **이름도 Figma에서 새로 조회**로 교체. 캐시 금지 규칙은 좌표·코멘트뿐 아니라 **프레임 이름에도 적용된다**
- **교훈 ④ — 좌표 매칭 오매칭이 3건 더 나왔다**(HANDOFF 전역 함정 ⓐ의 재확인): `rlagpwjd31`(닉네임 더미)·`8,276.00`(잔액 숫자)·`이번 달`(문장 일부). 전부 육안 검증에서만 드러났고, Figma TEXT 노드를 재조회해 실제 대상으로 교정했다
- **교훈 ⑤ — 사용자가 같은 페이지를 병렬로 편집한다**: 작업 중 v24→v25, v28→v29 등 사용자 편집이 끼어들었다. **PUT 직전 라이브 재조회 + 버전 가드**로 전부 무손실 반영. 사용자가 미리 만들어 둔 빈 행은 마크업이 내 패턴과 조금 달라(`<div class="content-wrapper"><br /></div>`) 한 번 걸러지지 않았다 — 빈 행 패턴은 2종을 본다

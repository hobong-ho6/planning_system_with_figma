# unifi-mini-v2 — Unifi mini v2.0 화면정의 (Figma 정책 취합 + XLT/GA)

> 담당자: `hogeun` · 마지막 갱신: 2026-09-15 · 세션 #14 · 마지막 커밋 `bc810e8` · 세션 #13 `11b8b64`
>
> 📌 **세션 #14 — 결제 바텀시트 문구 패치 + Hotfix 프레임**. `UF_voucher_pay_more_discount`를 **`모든 바우처 최대 {{0}}% 혜택!`** 로 교체(5개 언어 · 업로드 완료)하고 **17일 버전 `모든 바우처 {{0}} 혜택!`** 을 사전 준비(엑셀 첨부 · **적용 전까지 보류**). 프레임 2건 재조회 — `74620-13737`(17일 버전) 이미지 갱신 + **`74665-14721` Hotfix 행 신설**(현재 적용). 본문 **v203 → v206**.
>
> 📌 **세션 #13 — 화면 3건 추가 + Admin 설정 위치 매핑 + 일본어 검수 반영**. `바우처 상세 - 툴팁`(2키)·`결제페이지 - 바텀시트`(3키) 행 신설 · **리뷰 「더보기」 노출 조건 확정**(리뷰 3개 이상 · An Vo 구현 회신) · **Admin 관리 대상 위키에 설정 위치·등록 ID 매핑**(v3 → v7) · **일본어 검수 위키에 22건 반영**(v3 → v4). 본문 **v194 → v203**.
>
> 📌 **세션 #12 — 🔴 LPC 콘텐츠 소실 발견·원인 규명·전량 복구 + 일본어 검수 문서 신설**. LPC **4필드가 beta·prod 양쪽에서 `null`**(원인: PUT이 문서를 통째로 교체하는데 `uid`·단일 필드만 보낸 PUT이 형제 필드를 지움). **160건 전량 복구·전건 대조 일치** · 재발 방지 **§10-3-0 + 강제 산출물 2종**. **「Unifi mini v2.0 - 일본어 검수」(`4750149077`) 신설**(834행).
>
> ⚠️ **v186→v192 구간은 기록이 없다**(다른 세션 6버전 · 위키 History가 유일한 근거)
>

## 대상 / 링크

- 위키: [pageId 4704515582](https://wiki.workers-hub.com/display/UNIFI/Unifi+mini+v2.0) `Unifi mini v2.0` — **현재 v206**
  - **하위 `4727978725` 「LPC 관리 영역」** — **현재 v55**. LPC 컬렉션 **9개**의 정의·등록 JSON·환경 현황·비율 연동 규칙(§9-7) 정본
  - 하위 `4725950300` 「Admin 관리 대상」 · `4725963532` 「100엔딜」([전용 파일](100yen-deal.md)) · `4750149077` 「일본어 검수」 · Figma `GOCHAYBS7hIrmWRGNuJOKV`(`Web3`)
- **Unifi B/E API 스펙 `4725939674`** — FE 수신값의 정본. GuideKim 스펙과 달라 **구현 기준은 이쪽**
- XLT: 담당 FE — `JPYC 자산 상세`·`결제 페이지` = **UIT**(`UF_`·`{{0}}`) / `Voucher Detail`·`Clinic Detail` = **LV**(`mini_`·`{0}`). **`최근 본 상품`은 화면만 LV** — 키는 `UF_` 유지(의도된 예외). Home/Category/Search/My는 이전 세션에 번역 완료
- 이미지: **위키 첨부가 정본**(로컬 `assets/collected_*`는 git 미추적 재생성물)
- **K-Pick 1만원권 4종**(`k_pick_shopping_product`) — ⛔ **관리 대상 아님**(09-14 · 상세는 「⛔ 종결」). 산출물 `landpress/{cu,daiso,emart24,oliveyoung}_10000/` · UIT prod에 100셀 등록 완료

## 현재 상태

본문 위키 **v206** · 하위 LPC 관리 영역 **v55** · 하위 **일본어 검수 `4750149077`**(834행 · 신설 09-15). `mini_common_header_title`은 **2026-09-15 등록 확인**(Dapp Portal 1,696키). 화면 **26**(Home/Category/Search/My + 결제 **4**(바텀시트 17일 버전·Hotfix 2행 포함) · Voucher Detail 3 · Clinic Detail 4 · JPYC 2). 매 PUT `check_wiki_storage.py` pre/post exit 0.

| 영역 | 상태 |
|---|---|
| **XLT·GA 정의** | 신설 12화면 전부 완료(UIT=결제·JPYC / LV=Voucher·Clinic Detail). Home/Category/Search/My는 이전 세션에 완료 |
| **⛔ 상태값 기준** | Policy는 **GuideKim 기준** · FE 수신값은 **Unifi B/E** 경유로 다르다 → **구현은 B/E 기준**(스펙 `4725939674`). 여행 `use_state` 미제공 6곳 폐기. **상세는 위키가 정본** |
| **최근 본 상품** | 09-14 **UIT→LV 이관** · XLT 3키 **`UF_` 유지**(prod 기등록) · **9-17 이후 릴리즈** · 「확정 필요 항목」에 LV 회신 대기 |
| **LPC 컬렉션** | **9개**(LV 6 · UIT 3) 전부 **beta·prod 5개 언어 등록·공개 완료**. `voucher_product`(UIT)는 ⛔ **LV 병존 유지 확정** — 변경 시 **4곳(LV·UIT × beta·prod) 동반 갱신**(§4-3). 09-15 `payment_common_info` 정본 정렬(38셀) · 🔴 **콘텐츠 소실 4필드 전량 복구**(160건 일치 · 원인=부분 PUT) |
| **혜택 비율 체계** | ⛔ **할인 5% · 캐시백 10% · 합산 소구 15%**(09-14 사용자 확정 · 아래 「⛔ 종결」이 정본). 비율이 리터럴로 박힌 **6필드 · 80셀**은 값 변경 시 함께 갱신 → [LPC 위키](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4727978725) §9-7 |
| **클리닉** | `k_pick_clinic_product` 폐지 → `k_pick_clinic_common_info` 단일 통합 · **일본 의료광고 규제 반영**(효과 단정 제거 · `deviceNotice`·`contact`·`duration`·`risk` 신설) → [LPC 위키](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4727978725) |
| **K-Pick 1만원권 4종** | ⛔ **관리 대상 아님**(`k_pick_shopping_product`) — 사유·경위는 아래 「⛔ 종결」이 정본 |
| **클리닉 `clinic_menu`** | ✅ 병원 21곳 · 시술 48건 · 가격 **`상담 문의` 46 / 금액 2** · beta↔prod 5언어 동치. ⛔ 르아델 제외(`priceItems` 빈 배열) |
| **Unifi 마스터 대조** | 병원 29(ACTIVE 22)·바우처 17종 · 금액권 12종 **누락·초과 0** · 식별자 `uid` 18 / `unifiId` 3 혼재(→ P2) |
| **미결 핵심** | 🔴 클리닉 **한정해제 요건 ②③④ 8개 파라미터 미확보**(병원·법무) → 상세에 **시술명 노출 불가** · `최대혜택가` 3키 정리. ~~GA Screen ID~~는 09-14 해소 |

> ⚠️ 09-12~13 미기록 세션(v96→v172)의 미결은 미반영 — 근거는 `reports/gate/` 3건과 위키 History.

## 진행 중 작업(WIP)

없음. 세션 #12 산출물(복구 기록·검증 스크립트·`md/landpress.md` §10-3-0)은 전부 푸시 완료. 위키·LPC는 git 밖 산출물이다.

## 다음 할 일

> 완료·종결 항목은 여기 남기지 않는다 — **「⛔ 종결」 섹션이 정본**(중복 제거, 09-14).

- [ ] 🔴 **P0 — 클리닉 한정해제 요건 ②③④ · 8개 파라미터 미확보**: 요건 ①만 충족, **①~④ 모두 충족돼야 성립** → 상세에 **시술명 노출 불가**. 8개 표와 결정 2건(㉮ `price` 3택 · ㉯ `deviceNotice` 리쥬란 포함 법무)은 **LPC 위키 6번 3항이 정본**. ⚠️ **마스터에도 시술별 가격이 없다**(26건 중 24건 `price:"0"`) → 선택지 ①(시작가)은 **마스터 구조 변경 선행** 필요
- [ ] ⛔ **P1 — 17일 적용 대기(FE·XLT 동시)**: `xlt/xlt_patch_UF_voucher_pay_more_discount_v17_2026-09-15.xlsx`(위키 첨부됨) — **2026-09-17 전까지 업로드 금지**. `%`가 템플릿→변수로 옮겨가 `benefitrate`가 `"15%"` 문자열이어야 한다. 한쪽만 바뀌면 `1515%`/`15`로 렌더
- [ ] **P1 — XLT 엑셀 업로드 대기(세션 #13)**: 툴팁 2키(`xlt_output_20260915192629.xlsx`) · 결제 바텀시트 3키(`xlt_output_20260915201134.xlsx`) — **`UF_100yen_detail_badge`는 값 갱신**이라 재업로드 필요(ja 구분자 `・`)
- [ ] **P1 — XLT 엑셀 업로드 대기**(사용자 수행): `xlt/xlt_benefit_price_unify_{unifi,dappportal}_2026-09-14.xlsx` — 「혜택가」 ja·en 통일 + 「최대 혜택가」 띄어쓰기 **7키·11셀**. ⛔ XLT는 쓰기 API가 없다(엑셀은 `export_to_xlt.py`로 생성). 업로드 후 `fetch_xlt_registry.py` 재실측 → 위키 표 갱신
- [ ] **P1 — 일본어 검수 의견 회수 후 반영**: [`4750149077`](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4750149077) 834행의 「검수 의견」 열. XLT 수정분은 `ja_JP` 교정 → 엑셀 산출(업로드는 사용자) · LPC 수정분은 **read-modify-write**(`md/landpress.md` §10-3-0)로 반영. Admin 14영역은 **등록값을 채워야 검수 가능**
- [ ] **P2 — 병원 식별자 체계 통일**: 21곳 중 **18곳 `uid` / 3곳 `unifiId`(UUID)** — 이 3곳은 마스터 `externalId`가 `null`이라 「beta↔prod는 `uid` 기준」 규칙으로 **매핑 불가** → FE·기획 확인
- [ ] **P3 — 마스터 원본값 오류 1건**(디에이피부과 도산 `priceItems[0].name`=`상담 문의` · ₩390,000): 시술명 자리에 가격 문구. **마스터가 정본이라 그대로 둔다**(09-14 확정) — 마스터 갱신 시 정리

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지 — 이 프로젝트 한정)

- **공통 헤더 타이틀은 `카테고리 1`·`카테고리 2`에만 둔다**(2026-09-15 사용자 결정) — `Unifi | 더 스마트한 한국 여행`은 `[홈]` 섹션 7개 프레임 전부에 있지만 **나머지 5화면(`홈탭1`·`홈탭2` 등)에는 반영하지 않는다**. 「번호 체계 일관성」을 이유로 **재제안하지 말 것**
- **`k_pick_shopping_product`는 관리 대상이 아니다**(2026-09-14) — LPC 위키 1번 표의 컬렉션만 관리한다(캐시백률·판매처 표기·beta/prod 분기·매장명 차이 전부 범위 밖). 09-14 캐시백 5%→10% 정정 1회분은 **되돌리지 않고**(되돌리면 오안내 부활) **이후 갱신에서만 제외**한다. ⚠️ LPC 위키 §9-7의 「대상 외」 각주를 **지우면 다음 사람이 또 조사한다**(실제 발생)
- **「최대 15% 할인」 표기는 오표기가 아니다**(2026-09-14) — 할인(5%)+캐시백(10%) **합산 소구**다. 「할인은 5%인데 15%라 틀렸다」고 재제안하지 말 것
- **LPC 문구 3건 · CU 가이드 개편 반영 완료**(2026-09-14 · 전건 beta↔prod 동치 확인): ① FAQ 「한국인과 동일한 가격」 삭제(faq 8→7) ② th `พอยต์`→**`คะแนน`**(실사용 0건 폐기) · zh `付款金額`→**`消費金額`** ③ **CU 가이드 전면 개편**(Figma `74154-23966` 정본 · 이미지 6 + 상품명 5 + 캡션 5 신규). 신규 브랜드 3종은 **공식 출처로 확인**(`delafe`·`viyott` **소문자 고정**) — 추측 로마자화 금지 실천 사례. ⛔ 올리브영·다이소는 범위 밖
- **`상품권 이미지 저장` 릴리즈 표기는 h3 마커를 쓰지 않는다**(2026-09-14) — h3 「이후 릴리즈」는 **섹션 전체**용이라 개별 Screen 행에 붙이면 오독된다(h5 선례 **0건**). **Description에 상태 명시**로 통일(v186)
- **미결 7건 제거 — 재등재 금지**(2026-09-14): ① `primaryLocale` beta/prod 불일치 · ② 클리닉 `NO_SHOW`·`REJECTED` 분리 응답 → **FE·B/E가 자체 처리**하므로 우리 추적 대상 아님 ③ Figma 원문 교정(`라인 무료 상담`) · ④ `결제 정보 보기` Figma 마커 → ⛔ **Figma는 변경 대상에서 제외**(`결제 정보 보기`는 **XLT 대상이 맞고** 키는 확정) ⑤ 여행 「사용 완료」 배지 → **상태 자체를 노출하지 않기로** 확정 ⑥ `API 확인` 단 삭제 사유 · ⑦ Home/Category/Search/My 잔여(`categroy` 오타·GNB 마커) → **제거**(오타는 위키 v185에 **0건**으로 이미 해소, GNB는 `mini_gnb_search` 재사용으로 정리됨)
- **P1 3건 확정 — 재제안·재확인 금지**(2026-09-14): ① **`최근 본 상품` XLT는 `UF_`·`{{0}}` 유지** — LV가 동의했다(`mini_` 재부여·FE 교체 불필요, 팀 규칙의 의도된 예외) ② **`최대혜택가` 3키(`_rate`·`_label`·`_badge`) 모두 유지** — `_rate`가 이름·값이 어긋나 보여도 폐기·개명하지 않는다 ③ **판단 필요 3건 현행 승인** — `click_payment_method` · 클리닉 `#17 더 많은 상품보기` 번역 대상 · 「공식 바우처」·「즉시 발급」 배지
- **`my_voucher_detail`은 LV·UIT 병존을 유지한다**(2026-09-14 확정) — 한쪽 일원화(UIT 이관 등)는 **재제안 금지**. 대신 **문구 변경 시 환경 포함 4곳(LV beta·prod + UIT beta·prod)을 반드시 함께 갱신**하는 것이 전제 조건이다(LPC 위키 §9-6 · §3-2 · §4-3). 이 확정으로 §6 미결 항목에서 내려갔다
- **리뷰 본문(`review[*].body`)은 변경 금지 영역**(2026-09-14) — 20%·22% 할인 언급이 315셀 있으나 **갱신 대상이 아니다**. 비율 표기를 발견해도 올리지 않는다
- **르아델(Leadel)은 LPC에 넣지 않는다**(2026-09-14) — 마스터 ACTIVE지만 `priceItems`가 빈 배열이라 시술 항목을 만들 수 없다. 추가했다가 제거함
- **`uid`/`unifiId` 혼재는 규칙 문서화하지 않는다**(2026-09-14 사용자 결정) — `md/landpress.md`의 「uid 기준 매핑」에 예외를 적지 않는다. 실무는 `uid ‖ unifiId` 키로 대조하면 되고, 체계 통일 여부만 FE·기획 확인 사항으로 남긴다
- **디에이피부과 도산의 시술명 `상담 문의`는 그대로 둔다**(2026-09-14) — 마스터 원본값이라 마스터가 정본이다

## 세션 기록 (최신 위, 최대 5개)

### 2026-09-15 — 세션 #14: 바텀시트 문구 패치 + Hotfix 프레임 (`738c0ce`·`bc810e8`)

- **`UF_voucher_pay_more_discount` 2회 변경** — ⓐ `{{0}}% 할인!` → **`최대 {{0}}% 혜택!`**(5개 언어 재번역 · 업로드 완료) ⓑ **17일 버전 `모든 바우처 {{0}} 혜택!`** 사전 준비(위키 첨부 · **적용 전까지 보류**). 게이트 2건 모두 **P0·P1·P2 전부 0**
- **프레임 재조회 2건** — `74620-13737`을 **17일 버전**으로 표기·이미지 v2 재렌더 + **`74665-14721` 바텀시트 Hotfix**(현재 적용) 행 신설. **신규 키 0건** — `캐시백 10%`·`혜택가`·`정가`는 기존 등록값이 커버(`UF_voucher_recommend_cashback`·`UF_voucher_price_benefit`·`_original`)
- 확정: `benefitrate`는 **FE 합산** · 형제 키 `UF_voucher_pay_more_desc`는 **「할인」 유지**(제목 「혜택」과 통일하지 않음 — 재제안 금지)
- ⚠️ 교훈: **관례는 추측하지 말고 실측한다** — 「`%`를 변수에 넣으면 다른 키와 계약이 갈린다」고 경고했으나, 등록값 실측 결과 `{{0}}` 단독 **313키** vs `{{0}}%` **12키**로 **오히려 다수 관례**였고 `benefitrate`를 쓰는 키도 이것뿐이라 범위를 정정했다
- ⚠️ 교훈: **Screen 행 통째 교체 시 행 끝은 GA 중첩표 안쪽 `</tr>`가 아니라 `</tbody></table></td></tr>`** 까지다. 잘못 자르면 **PUT이 400(`Unexpected close tag`)** 으로 거부된다 — PUT 전 태그 균형 검사로 잡힌다

### 2026-09-15 — 세션 #13: 화면 3건 + Admin 매핑 + 일본어 검수 (`11b8b64`)

- 완료: `바우처 상세 - 툴팁`(신규 2키) · `결제페이지 - 바텀시트`(신규 3키 + 재사용 1) · 리뷰 「더보기」 **3개 이상 노출** 확정 · **Admin 관리 대상**(`4725950300`) v3→v7 — LIAM HUB 3메뉴를 실측해 **등록 ID까지 Screen 표 Admin 열에 기입** · **일본어 검수**(`4750149077`) v3→v4 **22건**
- ⚠️ 교훈: **`collect_frames` 좌표 매칭은 부분 텍스트를 잡는다** — **어노테이션을 크롭해 육안 확인**해야 잡힌다(같은 좌표에 텍스트 2개가 겹친 사례도 있었다)
- ⚠️ 교훈: **리터럴로 보이는 값이 동적 값일 수 있다** — `2026-09-17`·`15%` 둘 다 **코멘트 답글에 필드명**(`useStateCheckedAt`·`benefitrate`)이 있어 변수화했다

### 2026-09-15 — 세션 #12: LPC 콘텐츠 소실 복구 + 일본어 검수 문서 신설 (`5fb41ee`)

> 📄 **상세 정본은 `reports/restore/landpress_restore_2026-09-15.md` · 재발 방지 규칙은 `md/landpress.md` §10-3-0.** 여기엔 결론과 교훈만 남긴다.

- 🔴 **사고** — LPC **4필드가 beta·prod 양쪽 `null`**. **원인: PUT은 부분 갱신이 아니라 전체 교체**(body에 없는 필드가 `null`이 된다). **리비전 이력이 없어**(`/revisions` 404) 저장소 `landpress/` 산출물이 **유일한 복구원**이었다
- **복구** — 120건 PUT(=필드 160건) → **160/160 일치 · 전 컬렉션 280필드 null 0 · `clinic_menu` 보존**. 스크립트의 「실패 50건」은 `JSON.stringify` **키 순서 오탐**이었고, LV beta `en_US` 3건의 한국어 잔존 초안만 실제 교정 대상이었다
- **재발 방지**(`f183bb8`) — 쓸 때 `scripts/restore/lpc_safe_put.js`(**직접 PUT 금지**) · 쓰기 **직전·직후** `scripts/check_lpc_nulls.py` **exit 0**. `CLAUDE.md`에 「⛔ LPC 쓰기 규칙」 신설
- ⛔ **LPC 관리 범위 = 위키 `4727978725` 정의만**(사용자 확정). UIT `k_pick_clinic_common_info`는 LV 동명과 **스키마가 다른 별개**이고 위키 미정의 → **범위 밖**(복구도 하지 않았다)
- **일본어 검수 문서 신설** — [`4750149077`](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4750149077) **834행**(XLT 298 · Admin 14 · LPC 522) · 이미지는 **표 안 `rowspan` 셀**(55장 · 깨짐 0). **GA·OA 제외** · Admin은 조회 불가라 ko·ja 빈칸
- ⚠️ **교훈 — 「업데이트 했다」가 「값이 살아 있다」는 뜻은 아니다.** 세션 #9은 정상 완료로 기록했고 PUT도 200이었지만 **같은 PUT이 형제 필드를 지웠다.** 쓰기 후 검증은 **쓴 필드만이 아니라 항목 전체**를 봐야 한다

### 2026-09-15 — 세션 #11 · #10 · 2026-09-14 — 세션 #9·#8 (요약 병합)

- **#11** Category 어노테이션 재첨부 + `카테고리 2` Description·XLT `No`·GA `#` **전부 +1 재정렬** · 신규 1키 `mini_common_header_title` · 리뷰어 아바타=닉네임 첫 글자 · 리뷰 스펙 클리닉↔바우처 공통. 교훈: **「이미지만 갱신」 요청도 코멘트를 먼저 재조회**한다 · 같은 파일명 첨부 갱신은 **`POST .../child/attachment/{id}/data`**(`/data` 없으면 조용히 무시)
- **#10** LPC `payment_common_info`를 XLT 정본에 정렬(beta·prod 38셀). 교훈: 같은 문구가 **두 경로로 관리되면 「정본」을 문서에 박아야** 다음 세션이 반대로 덮어쓴다
- **#9** 혜택 비율 **할인 5% · 캐시백 10% · 합산 15%** 확정 → 리터럴 **6필드 80셀**을 LPC 위키 §9-7로 신설 · `voucher_product`(UIT) 신설(4곳 일치) · 브랜드 **공식 표기 웹 확인**
- **#8** 클리닉 가격 기본값 `상담 문의`(5언어 46건) · My 바우처 상세 3키. 교훈: 관리 범위를 **위키 1번 표로 먼저 확정**한다

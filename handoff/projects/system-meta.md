# system-meta — 핸드오프 구조 · 규칙 · 도구 자체

> 담당자: `hogeun` · 마지막 갱신: 2026-09-16 · 커밋 `a15b5bc`(직전 `4016234`)
>
> ⛔ **2026-09-16 — 드랍웹 게시 규칙 개정**: 「게시는 사용자가 수행」 → **Claude 직접 게시**(사용자 승인 후). 4개 문서 동시 개정 — `CLAUDE.md` 📣 규칙 3ⓑ · **`md/dropweb-guide.md` §8 신설**(MCP 도구 8종 · 대상 사이트 3곳 · **MCP `update_site` 불가 실측** · REST 명령 · 절차 6단계) · `md/landpress.md` §5-1 · `md/guide-backlog.md` 절차 ④. **가이드 대기 7건 → 0건**(v43 게시)

## 대상 / 경계

어느 작업 대상(위키·화면·키)에도 속하지 않는 **시스템 자체**의 변경이 여기 기록된다.

| 포함 | 제외 |
|---|---|
`HANDOFF.md` 계층 구조 · `handoff/**` · `.claude/**`(훅·스킬·에이전트) · `md/**` 절차 문서 · `scripts/**` · `.gitignore` | 가이드 **콘텐츠**·용어집 → `projects/glossary-guide.md` · IA 정본 → `projects/ia-monitor.md` |

## 현재 상태

**3계층 핸드오프 가동 중**(2026-08-20 전환) · **토큰 규칙 「필요 시 요청」·산출물 gitignore·Auto-react 유입 계약**(2026-09-18) — 공유 정본 1 + **활성 프로젝트 7**(아카이브 10) + 사람 1 + 레인(락·현재 점유 없음).

- 훅 주입 **22.3KB**(`HANDOFF.md` 20,005B + `people/hogeun.md` 2,869B — 2026-09-13 `wc -c` 실측) · 프로젝트 본문 미주입 · 활성 인덱스 **7행**(아카이브 10)
- **분량 기준은 줄 수가 아니라 용량이다**(2026-09-13 정정) — 불릿 하나가 2,000자여도 1줄이라 줄 수는 비용을 반영하지 못한다. **자 수가 아니라 바이트로 잰다**(`wc -c`) — 한글은 1자 = 3바이트라 「11,776자」는 실제 20KB다. 상한은 2026-09-13 압축 후 실측값을 기준으로 잡았다: `HANDOFF.md` **≤20KB** · 프로젝트 **≤18KB** · 사람 **≤3KB** · **세션 시작 주입 총합 ≤24KB**(= `HANDOFF.md` + 담당자 `people/` 1개. 프로젝트 본문은 주입되지 않으므로 합에 넣지 않는다). 넘으면 상세를 `md/` 정본으로 내리고 경고 + 포인터만 남긴다. 기존 「100줄」은 보조 지표로만 본다
- 담당자 식별: `git config --global handoff.person` (PC마다 동일 값) — 기획자 가이드 v28 「사용자 설정」 섹션이 안내 창구
- 온보딩 판별: **증거 우선순위 5단계**(확정 키 → hostname → 이메일 → 추정 키 일치 → 신규). 추정 키가 실제 파일명과 다르면 훅이 경고하고 **새 사람 파일 생성을 금지**한다 — 이 iMac은 현재 **상태 A**

## 진행 중 작업(WIP)

없음.

## 다음 할 일

- [ ] **`landpress/`(522) · `oa/`(116) JSON도 산출물로 보고 gitignore할지 결정**(사용자) — 2026-09-18에는 `reports/gate/`만 제외했다. LPC 콘텐츠·OA Flex에도 XLT 값이 들어 있다.
- [ ] **Auto-react 유입 계약 첫 리허설** — 일감 패킷을 받아 처리하고 결과 패킷을 돌려주는 흐름 1건(정본은 Auto-react `docs/00-design.md` 「PS 실행 계약」).

- (관측 대기) 활성 프로젝트가 **30개를 넘으면** 인덱스가 다시 무거워진다 → 그때 `handoff/INDEX.md` 분리 + 훅의 담당자별 필터 주입을 검토한다. 지금(11개) 하면 과설계
- (관측 대기) **한 프로젝트를 2명이 공동 담당**하면 그 파일의 「현재 상태」가 충돌 핫스팟이 된다 → 실제로 발생하면 세션 단위 블록으로 쪼갠다

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|---|---|---|
| **2026-09-18** | **토큰은 「필요한 단계에서 요청」으로 개정** — 키체인(`confluence-pat`·`figma-pat`·`jira-pat`) 우선, 없으면 그 시점에 요청. 종전 「토큰 받기 전 착수 금지」 폐기(사용자 결정) | 무인·연동 실행(Auto-react 일감 패킷)에서 첫 행동이 토큰 요청이면 착수가 막힌다. Auto-react가 09-15 키체인 경로로 위키 PUT을 실증했다 |
| **2026-09-18** | **산출물은 커밋하지 않는다** — `reports/gate/` gitignore(+ 기존 `xlt/`). 저장소는 public 유지, 사본은 Auto-react(private) `reports/gate/`(사용자 결정) | 09-17 커밋 20건+에 XLT 값·게이트 리포트가 public으로 올라갔다. private 전환은 public clone으로 쓰는 팀원을 막는다 |
| **2026-09-18** | **Auto-react 유입 일감은 「일감 패킷」으로 받고 결과는 「결과 패킷」으로 돌려준다** — task_id를 게이트 리포트 머리와 커밋 메시지에 `[T095]`로 표기 · PS는 자기 저장소에만 쓴다 · Slack 게시는 Auto-react가 | 실행 규칙·락·게이트는 PS 세션에서만 돈다. 쓰기 주체가 둘이면 상호 오염(Auto-react↔2026에서 같은 결정) |
| 2026-08-20 | **온보딩 판별은 「확정 키 → 증거 역추적」 우선순위로** — `user.email`에서 딴 키는 **추정**으로 표시하고, 실제 파일명과 다르면 새 사람 파일을 만들지 않는다 | 추정 키가 채워져 있으면 **hostname 증거가 이미 있어도 `C` 분기가 걸리지 않아** `E`로 오판하고 **중복 사람 파일 생성을 지시**했다(이 iMac 실측). 신호를 늘릴 때 우선순위와 「추정」 표시를 같이 넣지 않으면 추정이 확정을 가린다. 커밋 `cf44ac6`~ |
| 2026-08-20 | **핸드오프를 사람이 아니라 3계층으로 분리** — 공유 정본 + 프로젝트 + 사람 + 레인 | 사용자별 분할은 실제 사고를 못 막는다. 세션 #20↔#21 덮어쓰기는 **같은 사람·같은 PC**였고, 사고 원인은 전부 **공유 산출물 동시 쓰기**였다. 사용자별로 나누면 남의 진행을 못 봐 위험이 오히려 커진다. 축은 **작업 대상(프로젝트)** 과 **점유(레인)** 다 |
| 2026-08-20 | **레인은 단일 표가 아니라 파일 단위** — 파일 존재 = 점유 | 8머신이 같은 표 줄을 고치면 그 표가 충돌 핫스팟이 된다. 파일 생성/삭제는 **머지 충돌이 원리적으로 발생하지 않고** 현황 파악이 `ls` 한 줄이다 |
| 2026-08-20 | **인덱스는 활성 프로젝트만** — 종료분은 `projects/_archive/`로 이동 | 인덱스는 매 세션 주입된다. 활성만 유지하면 시간이 지나도 주입량이 무한 증가하지 않는다(유일한 선형 증가축) |
| 2026-08-20 | **hostname은 식별자가 아니다** — 담당자는 git identity(`handoff.person`) | 한 사람이 PC 2대를 쓰고 한 PC를 여러 사람이 쓸 수 있다. hostname은 `people/*.md` 안의 세부 정보로 내려갔다 |
| 2026-08-20 | **`.claude/hooks`를 `.gitignore` 화이트리스트에 추가** | `.claude/*`가 기본 차단이라 훅 스크립트가 커밋되지 않고 **`settings.json`만 전파돼 다른 PC에서 훅이 조용히 깨진다**. 커밋 직전 `git add`가 이를 잡았다 |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지)

- **`이름 <본인@example.com>` author로 푸시된 5커밋(`e1c127c`~`3215088`)은 그대로 둔다**(2026-08-20 사용자 결정) — `main`은 4명이 공유하는 브랜치라 히스토리 재작성 + force-push가 다른 PC·다른 사람의 로컬 `main`을 전부 어긋나게 만든다. author 메타데이터를 위해 감당할 리스크가 아니다. **재작성 제안 금지.** git config는 `Hogeun Kim <hogeun.kim.lnxt@gmail.com>`으로 정정 완료
- **Figma MCP 서버 인증 폐기**(2026-08-20 사용자 결정) — `plugin:figma:figma`는 미인증 상태로 둔다. 비대화형 세션은 OAuth를 진행할 수 없어 대화형 `/mcp`가 필요했으나 **인증하지 않기로 결정**했다. **폐기가 안전한 근거**: Figma 작업은 **REST + PAT 폴백으로 전부 대체된다** — 이번 세션에 4프레임 노드 조회·코멘트 스레드(답글 포함)·어노테이션 렌더·이미지 추출을 전부 REST로 처리했다(`scripts/collect_frames.py`·`fetch_comments.py`). **MCP 인증 재제안 금지.**
- **`dooboo` 플러그인 도입 폐기**(2026-08-20 사용자 결정) — 사내 마켓플레이스 `git.linecorp.com/BlockchainLab/dooboo.git`. 이 PC에 git 인증이 없어(SSH publickey 거부 · osxkeychain 미저장) 설치 전 단계에서 중단했고, **사용자가 인증을 폐기 결정**했다. 마켓플레이스는 **추가되지 않았다**(`known_marketplaces.json`에 `claude-plugins-official`만 · 정리할 로컬 상태 없음). **재설치·재제안 금지.**
  - (재사용 가능한 실측) 다른 플러그인을 팀에 공유할 때는 `claude plugin {marketplace add,install} --scope project` — `.claude/settings.json`(git 추적)의 `extraKnownMarketplaces`·`enabledPlugins`에 기록돼 `git pull`로 전파된다. CLI **v2.1.235**에서 `--scope user|project|local`·`/reload-plugins` 존재 확인. `--scope user`는 `~/.claude/`에만 남는다
- **사용자별 핸드오프 분할안 종결** — 위 3계층 결정으로 대체. 담당 영역이 실제로 갈리면 재검토할 수 있으나 현재 구조에서 재제안하지 않는다

## 세션 기록

### 2026-09-18 — 토큰 규칙 개정 · 산출물 gitignore · Auto-react 유입 계약 (Auto-react 세션에서 · 레인 `2026-09-18-hogeun-token-rule-gitignore`)

- **완료**: `CLAUDE.md` 「토큰 우선 규칙」 → 「토큰 규칙 — 필요한 단계에서 요청」 전면 개정(핸드오프 확인 규칙 ⑤ 단계·저장소 원칙 문구 동기화) · `md/wiki.md` Step 0·배너·항목 5·첨부 절 4곳 · `md/ia-check.md` 참조 1곳 · `.gitignore`에 `reports/gate/` 추가 + 추적 187건 `git rm --cached`(로컬 파일 유지 · 사본 Auto-react `reports/gate/`) · 위 결정 3건 · `HANDOFF.md` 전역 결정 표 갱신
- **배경**: Auto-react 설계 v1(`~/Documents/Auto-react/docs/00-design.md`)이 실행층을 PS 세션으로 확정했다. PS 규칙은 PS 세션에서만 도니 실행은 여기서, 감시·원장·Slack 보고는 Auto-react에서.
- **남긴 것**: `landpress/`·`oa/` gitignore 여부(사용자) · 유입 계약 리허설 1건

### 2026-09-16 — 드랍웹 MCP 연동 + 게시 규칙 개정 (`a15b5bc`)

- **DropWeb MCP 등록** — `~/.claude.json`의 `mcpServers.dropweb`(HTTP · `Authorization: Bearer dwt_…`). ⛔ **저장소 `.mcp.json`에 넣지 않았다** — git 추적 대상이라 토큰이 커밋된다(비밀값 규칙). 세션 중에는 툴이 로드되지 않아 **curl로 JSON-RPC 직접 호출**해 검증했다(다음 세션부터 `mcp__dropweb__*` 자동 연결)
- **도구 8종 실측** — `deploy_site`·`update_site`·`list_sites`·`get_site`·`read_site_files`·`rename_site`·`delete_site`·`get_me`. ⛔ **배포 2종은 `file_path`를 DropWeb 서버 FS에서 찾아** 로컬 zip을 못 읽는다(절대·상대·https URL 3형태 동일 실패 · 확장자 검사가 존재 확인보다 먼저 돈다). **업로드는 REST `PUT /api/sites/{id}`**(웹 UI와 동일 경로 · MCP 토큰으로 인증 · `file`+`name` multipart)
- **규칙 4개 문서 개정**(위 헤더) — 「게시는 사용자가 수행」이 4곳에 흩어져 있어 **전부 같은 커밋에서** 고쳤다
- ⚠️ **교훈 — 도구 설명이 아니라 호출 결과로 규칙을 쓴다.** 처음엔 도구 설명(「서버가 파일을 직접 읽어 배포」)만 보고 §8을 「`update_site`로 배포한다」로 썼다가, 실제 호출이 실패해 **같은 세션에서 4개 문서를 재개정**했다
- ⚠️ **병렬 세션 관측** — 같은 워킹트리에서 다른 세션이 동시에 작업 중이었다(커밋 `456e171`·`c2f1c0a`·`e7a063e` · `handoff/projects/unifi-mini-v2.md`·`reports/gate/` 수정). **남의 변경은 스테이징하지 않고**(경로 지정 커밋) 발견 사실만 보고했다. 미푸시였던 `456e171`은 내 푸시에 함께 올라갔다

### 2026-09-15 — 가이드 갱신을 「모아서 한 번에」로 전환 + 스크립트 표 정비 (`1013215`·`edeb5d2`)

- **`md/guide-backlog.md` 신설** — 기획자 가이드를 건건이 갱신하지 않고 대기 목록에 쌓았다가 일괄 반영한다(사용자 결정). `CLAUDE.md` 📣 규칙을 「푸시 시 제안」 → 「묻지 말고 등재」로 바꿨다. **용어집 예외는 충돌이 아니라 같은 흐름**이다 — 용어집도 이미 `glossary-backlog`로 모아서 반영하므로 그 작업에서 가이드를 함께 갱신하면 「필수 동반」이 그대로 성립한다
- **임계 제안 규칙**(사용자 승인) — 대기 **10건** · ⛔차단 규칙 **2건** · 마지막 태그로부터 **2주** 중 하나면 한 줄로 제안하고, **보류 시 5건 더 쌓일 때까지 재제안 금지**(「제안 이력」 표로 관리). 제안 자체를 없애면 영영 안 하게 되고, 조건 없이 되살리면 방금 없앤 문제가 돌아온다
- **기준선 실측** — 제 기록의 `guide-v39`는 낡았고 라이브 태그는 **`guide-v42`**였다(다른 세션 게시). v42 이후 미반영 **7건**을 실측해 등재
- **`put_wiki_storage.py` 표 등재**(사용자 지시) — 위키 본문 **버전 가드 PUT**(버전 불일치 시 `exit 2` · 본문 길이 5% 이상 차이 시 `exit 3`). 같은 세션에서 위키 PUT이 **409 Conflict**로 거부됐는데, **이 스크립트를 썼으면 그대로 잡혔을 상황**이었다
- **개수를 박아둔 체크리스트 정정** — 「`scripts/` 폴더에 14개 Python 스크립트(…나열)」는 스크립트가 늘 때마다 틀려진다. 「표 기준(개수는 계속 늘어난다)」으로 바꿨다
- **배운 것**: **「모아서」 규칙은 임계·재촉 금지와 세트가 아니면 반쪽이다** — 모으기만 하면 쌓인 채 잊히고, 임계만 두면 넘긴 뒤 매번 묻게 된다. · **문서에 개수·버전을 박으면 그 줄이 낡는다**(스크립트 14개 · 가이드 v39 둘 다 이번에 실측으로 걸렸다)

### 2026-09-15 — 전역 결정 연혁을 `md/decisions.md`로 이관 (`4016234`)

- **`HANDOFF.md` 20,411B → 17,046B**(상한 20,480 · 여유 **57B → 3,434B**). 「전역 결정 사항」 **13건 전문**을 [`md/decisions.md`](../../md/decisions.md)로 옮기고 본문에는 **최근 4건 + 포인터**만 남겼다 — **삭제가 아니라 이관**(정보 손실 0). `CLAUDE.md` 참조 가이드 표에도 등재했다(md 폴더 관리 규칙).
- 운용 규칙을 `md/decisions.md` 머리에 박았다 — **새 결정은 HANDOFF 표 맨 위에 넣고 밀려난 것을 그 파일로 내린다.**
- 손대지 않은 것: 「전역 절차 · 컨텍스트」(6.3KB)는 **문서에 없는 실측·사각지대**라 주입에서 빠지면 같은 실수가 반복된다. 「프로젝트 인덱스」(4.4KB)는 세션 시작 브리핑의 근거다.
- ⚠️ 교훈 — **「미결 0건」을 `grep "- \[ \]"`로 세면 오판한다**: `masters`·`system-meta`는 체크박스가 아닌 `- ` 목록으로 할 일을 적어 둬 0건으로 잡혔고, 실제로는 지속 관리 대상이었다. **아카이브 판단은 「다음 할 일」 섹션을 눈으로 읽는다.**

### 2026-09-13 — 핸드오프 포인터 압축 + 분량 기준을 바이트로 전환 (`661fd0b`~`274d453`)

- **아카이브 5건**(`clinic`·`kaiawallet-transition`·`guidekim-banner`·`kpick-oa-consent`·`misc-wikis`) — 활성 인덱스 **11행 → 7행**. 미결이 남은 것도 사용자 결정으로 종료하되 **미결 내용을 아카이브 파일에 남긴 채** 옮겼다
- **포인터 압축 3단계 실행** — `HANDOFF.md`의 상세 규칙을 `md/` 정본으로 옮기고 원 위치에는 **경고 한 줄 + 링크**만 남겼다(위키 Step 5 차단 규칙 → `md/wiki.md` · `collect_frames.py` 실측 함정 → `md/wiki.md` · Landpress 산출물/항목 생성 → `md/landpress.md` · 프리픽스 선례·등록 키 변경 → `md/translate.md`). 프로젝트 파일 3종도 압축(system-meta 8,240→5,195자 · unifi-mini-v2 12,316→9,853자 · glossary-guide 9,402→6,382자)
- **분량 기준을 줄 수 → 바이트로 전환** — 압축 후에도 `HANDOFF.md`가 **20KB**라 이번 세션에 잠깐 세웠던 「≤12KB」는 달성 불가능한 숫자였다. `wc -c` 기준으로 상한을 다시 잡고(**HANDOFF 20KB · 프로젝트 18KB · 사람 3KB · 주입 총합 24KB**) `handoff/README.md`(정본)·`SKILL.md`·`HANDOFF.md` 주석 **세 곳을 동기화**했다. 현재 주입 **22,710B**
- **정리 중 발견한 정본 오류 정정** — `md/landpress.md` §9-1 locale 파라미터(두 API 모두 `?locale=` · `?_locale=`은 400이 아니라 **무시**된다 · prod 1956 실측) · `glossary-guide` 가이드 버전 v33→**v39**(태그 대조) · `masters` 위키 버전 3종
- **배운 것** ⓐ **압축은 삭제가 아니다** — 지우면 정보가 사라지고, 포인터로 내리면 주입량만 준다. 상한 초과 시 기본 동작을 「`md/`로 이관 + 경고·링크」로 규칙에 명문화했다 ⓑ **줄 수는 비용을 반영하지 못한다** — 한글 1자 = 3바이트고 불릿 하나가 2,000자여도 1줄이다 ⓒ **지키지 못할 상한은 규칙이 아니다** — 실측 없이 세운 12KB는 세우자마자 위반됐고, 다음 세션이 무시할 규칙이 됐을 것이다. 상한은 **실측값에서 역산**한다 ⓓ **파생 기재(정합·버전·「전부 OK」)는 원본이 바뀌면 조용히 낡는다** — 실제로 마스터 5종 중 2종 정합이 깨져 있었다(→ `projects/masters.md`)

### 2026-09-13 — 산출물 커밋 누락 발견·정리 (`f49888a`·`08b718e`)

- **증상** — 세션 정리 중 미추적 파일 13건을 확인했더니 **게이트 리포트 9건(09-04~09-13)** 과 **`landpress/` 100파일**이 로컬에만 있었다. 최대 **9일**간 커밋되지 않은 상태였고, `.gitignore` 대상도 아니어서 **의도적 제외가 아니라 누락**이었다
- **왜 위험한가** — HANDOFF.md 기준 게이트 리포트는 「제거된 값을 복원할 수 있는 **사실상 유일한 롤백 자산**」이다(레지스트리는 버전 컨테이너가 갱신돼 구값이 남지 않는다). 그 PC가 고장나거나 다른 PC에서 이어받으면 **복원 경로가 사라진다**
- **조치** — 선례(`landpress/` 186·`reports/gate/` 121 추적 중)·비밀값 스캔·`check_gate_report.py` exit 0을 확인하고 **삭제가 아니라 커밋**했다. HANDOFF.md 전역 절차에 「산출물은 만든 세션이 그 세션 안에서 커밋한다 · 세션 종료 시 `??` 확인」을 신설
- **남은 것** — 1만원권 바우처 LPC 작업(`k_pick_shopping_product` · CU·다이소·이마트24·올리브영)이 **어느 프로젝트 파일에도 기록되지 않았다**. 산출물과 게이트 리포트는 커밋됐지만 상태·다음 할 일이 없다 → 사용자 확인 후 프로젝트 편입 필요

### 2026-08-20 — 3계층 전환 + 온보딩 자동 판별(A~E) + 판별 버그 수정 (`e1c127c`~`cf44ac6` · 가이드 v28~v30)

- **구조 전환** — `HANDOFF.md` 243줄 → 공유 정본 101줄 + `projects/` 11종 + `people/` + `lanes/`. 훅이 계층을 주입하도록 변경. **무손실 검증**: 위키 ID 16/16 · 커밋 해시 29/29 · 코드 토큰 225개 중 실질 누락 0
- **온보딩 A~E 자동 판별** 도입 후 **버그 수정** — `user.email`에서 딴 **추정** 키가 `handoff.person`(**확정**)을 가려 사람 파일이 갈릴 뻔했다. 증거 우선순위 5단계로 고치고 hostname 위조 + `GIT_CONFIG_GLOBAL` 주입으로 **7시나리오 실측**
- 잔여 전건 종결(사용자 지시) · 미추적 게이트 리포트 3건 정리 · kaiawallet 위키 v12 반영
- **규칙으로 codify된 교훈**(→ `.claude/skills/handoff/SKILL.md`): 판별 신호에 확정/추정이 섞이면 **추정이 확정을 가린다** · **플레이스홀더는 실행된다**(예시 값 넣은 명령을 주지 않는다 — 실제로 5커밋이 잘못된 author로 푸시됐다)
- **남는 교훈**: **무손실은 선언이 아니라 검증이다** — 토큰 대조 없이 끝냈으면 환경 노트 12줄이 조용히 사라졌다 · **구조를 바꾸면 그 구조를 설명하는 문서(기획자 가이드)도 같이 낡는다**

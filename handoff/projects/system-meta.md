# system-meta — 핸드오프 구조 · 규칙 · 도구 자체

> 담당자: `hogeun` · 마지막 갱신: 2026-08-20 · 커밋 `e1c127c`~`1c33575`

## 대상 / 경계

어느 작업 대상(위키·화면·키)에도 속하지 않는 **시스템 자체**의 변경이 여기 기록된다.

| 포함 | 제외 |
|---|---|
`HANDOFF.md` 계층 구조 · `handoff/**` · `.claude/**`(훅·스킬·에이전트) · `md/**` 절차 문서 · `scripts/**` · `.gitignore` | 가이드 **콘텐츠**·용어집 → `projects/glossary-guide.md` · IA 정본 → `projects/ia-monitor.md` |

## 현재 상태

**3계층 핸드오프 가동 중**(2026-08-20 전환) — 공유 정본 1 + 프로젝트 11 + 사람 1 + 레인(락).

- 훅 주입 **8.7KB**(이전 단일 파일 28KB) · 프로젝트 본문 미주입 · 활성 인덱스 11행
- 분량: `HANDOFF.md` 인덱스 제외 100줄 · 프로젝트 120줄 · 사람 40줄 · 레인 20줄
- 담당자 식별: `git config --global handoff.person` (PC마다 동일 값) — 기획자 가이드 v28 「사용자 설정」 섹션이 안내 창구
- 온보딩 판별: **증거 우선순위 5단계**(확정 키 → hostname → 이메일 → 추정 키 일치 → 신규). 추정 키가 실제 파일명과 다르면 훅이 경고하고 **새 사람 파일 생성을 금지**한다 — 이 iMac은 현재 **상태 A**

## 진행 중 작업(WIP)

없음.

## 다음 할 일

**대기 항목 없음.**

- (관측 대기) 활성 프로젝트가 **30개를 넘으면** 인덱스가 다시 무거워진다 → 그때 `handoff/INDEX.md` 분리 + 훅의 담당자별 필터 주입을 검토한다. 지금(11개) 하면 과설계
- (관측 대기) **한 프로젝트를 2명이 공동 담당**하면 그 파일의 「현재 상태」가 충돌 핫스팟이 된다 → 실제로 발생하면 세션 단위 블록으로 쪼갠다

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-20 | **온보딩 판별은 「확정 키 → 증거 역추적」 우선순위로** — `user.email`에서 딴 키는 **추정**으로 표시하고, 실제 파일명과 다르면 새 사람 파일을 만들지 않는다 | 추정 키가 채워져 있으면 **hostname 증거가 이미 있어도 `C` 분기가 걸리지 않아** `E`로 오판하고 **중복 사람 파일 생성을 지시**했다(이 iMac 실측). 신호를 늘릴 때 우선순위와 「추정」 표시를 같이 넣지 않으면 추정이 확정을 가린다. 커밋 `cf44ac6`~ |
| 2026-08-20 | **핸드오프를 사람이 아니라 3계층으로 분리** — 공유 정본 + 프로젝트 + 사람 + 레인 | 사용자별 분할은 실제 사고를 못 막는다. 세션 #20↔#21 덮어쓰기는 **같은 사람·같은 PC**였고, 사고 원인은 전부 **공유 산출물 동시 쓰기**였다. 사용자별로 나누면 남의 진행을 못 봐 위험이 오히려 커진다. 축은 **작업 대상(프로젝트)** 과 **점유(레인)** 다 |
| 2026-08-20 | **레인은 단일 표가 아니라 파일 단위** — 파일 존재 = 점유 | 8머신이 같은 표 줄을 고치면 그 표가 충돌 핫스팟이 된다. 파일 생성/삭제는 **머지 충돌이 원리적으로 발생하지 않고** 현황 파악이 `ls` 한 줄이다 |
| 2026-08-20 | **인덱스는 활성 프로젝트만** — 종료분은 `projects/_archive/`로 이동 | 인덱스는 매 세션 주입된다. 활성만 유지하면 시간이 지나도 주입량이 무한 증가하지 않는다(유일한 선형 증가축) |
| 2026-08-20 | **hostname은 식별자가 아니다** — 담당자는 git identity(`handoff.person`) | 한 사람이 PC 2대를 쓰고 한 PC를 여러 사람이 쓸 수 있다. hostname은 `people/*.md` 안의 세부 정보로 내려갔다 |
| 2026-08-20 | **`.claude/hooks`를 `.gitignore` 화이트리스트에 추가** | `.claude/*`가 기본 차단이라 훅 스크립트가 커밋되지 않고 **`settings.json`만 전파돼 다른 PC에서 훅이 조용히 깨진다**. 커밋 직전 `git add`가 이를 잡았다 |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지)

- **`이름 <본인@example.com>` author로 푸시된 5커밋(`e1c127c`~`3215088`)은 그대로 둔다**(2026-08-20 사용자 결정) — `main`은 4명이 공유하는 브랜치라 히스토리 재작성 + force-push가 다른 PC·다른 사람의 로컬 `main`을 전부 어긋나게 만든다. author 메타데이터를 위해 감당할 리스크가 아니다. **재작성 제안 금지.** git config는 `Hogeun Kim <hogeun.kim.lnxt@gmail.com>`으로 정정 완료
- **Figma MCP 서버 인증 폐기**(2026-08-20 사용자 결정) — `plugin:figma:figma`는 미인증 상태로 둔다. 비대화형 세션은 OAuth를 진행할 수 없어 대화형 `/mcp`가 필요했으나 **인증하지 않기로 결정**했다. **폐기가 안전한 근거**: Figma 작업은 **REST + PAT 폴백으로 전부 대체된다** — 이번 세션에 4프레임 노드 조회·코멘트 스레드(답글 포함)·어노테이션 렌더·이미지 추출을 전부 REST로 처리했다(`scripts/collect_frames.py`·`fetch_comments.py`). **MCP 인증 재제안 금지.**
- **`dooboo` 플러그인 도입 폐기**(2026-08-20 사용자 결정) — 사내 마켓플레이스 `git.linecorp.com/BlockchainLab/dooboo.git`. 이 PC에 git 인증이 없어(SSH publickey 거부 · osxkeychain 미저장) 설치 전 단계에서 중단했고, **사용자가 인증을 폐기 결정**했다. 마켓플레이스는 **추가되지 않았다**(`known_marketplaces.json`에 `claude-plugins-official`만 · 정리할 로컬 상태 없음). **재설치·재제안 금지.**
  - (재사용 가능한 실측) 다른 플러그인을 팀에 공유할 때는 `claude plugin {marketplace add,install} --scope project` — `.claude/settings.json`(git 추적)의 `extraKnownMarketplaces`·`enabledPlugins`에 기록돼 `git pull`로 전파된다. CLI **v2.1.235**에서 `--scope user|project|local`·`/reload-plugins` 존재 확인. `--scope user`는 `~/.claude/`에만 남는다
- **사용자별 핸드오프 분할안 종결** — 위 3계층 결정으로 대체. 담당 영역이 실제로 갈리면 재검토할 수 있으나 현재 구조에서 재제안하지 않는다

## 세션 기록

### 2026-08-20 — 온보딩 판별 버그 수정: 추정 키가 확정 키를 가린다 (`cf44ac6`~)

- **증상** — 이 iMac에서 훅이 **E**(「사람키 `hogeun.kim.lnxt` 있으나 파일 없음」)로 판정해 `people/hogeun.kim.lnxt.md` 생성을 지시했다. 실제로는 `people/hogeun.md`가 있고 **이 PC도 그 파일에 등록**돼 있었다 — 지시를 따르면 사람 파일이 둘로 갈려 담당자 식별이 깨진다
- **원인** — `user.email`에서 딴 사람키(**추정**)를 `handoff.person`(**확정**)과 같은 권위로 썼다. `C` 분기 조건이 `not person`이라 **추정 키가 채워져 있으면 hostname 증거가 있어도 C가 걸리지 않고 E로 떨어진다**
- **수정** — 증거 우선순위 5단계 도입(① 확정 키 → ② hostname 등록 파일 → ③ `user.email`이 본문에 있는 파일 → ④ 추정 키=파일명 일치 → ⑤ 신규 D). ②~④는 확정 키가 없을 때만 쓰이고, 추정 키가 어긋나면 **경고 + 새 파일 생성 금지 + 확인된 값으로 완성된 `handoff.person` 명령**을 준다
- **검증** — hostname 위조 + `GIT_CONFIG_GLOBAL` 주입으로 **7시나리오 실측**: A · B · **C-1**(미설정+hostname, 원래 버그) · **C-2**(미설정+이메일+PC 미등록) · D · E. 사용자가 `handoff.person`을 설정한 뒤 실제 훅 재실행으로 **A 확인**
- **동반 갱신** — `SKILL.md` 온보딩 표 4행 + 추정 키 주의 1줄 · `handoff/README.md` 판별 순서 · `HANDOFF.md` 인덱스 misc-wikis stale 1줄 · `people/hogeun.md` iMac 경로 확정·세팅 ✅ · **`lanes/README.md` 락 대상에 `.claude/**`(git 추적분) 추가** — 훅·스킬·에이전트·`settings.json`은 전 세션 동작을 바꾸는데 락 대상이 아니었다(기기별 `settings.local.json`·`launch.json`은 제외) · **`people/hogeun.md` 44→32줄 압축**(obsolete 「iMac 최초 세팅」 섹션 제거, 이메일은 훅 근거라 헤더로 이동·회귀 테스트) · **`_TEMPLATE.md`에 identity 줄·`git 세팅` 컬럼 추가** — 템플릿에 이메일 자리가 없어 **새 사람 파일은 훅 근거 ③이 동작하지 않았고**, `SKILL.md`가 지시하는 「세팅 열 ✅」의 컬럼 자체가 없었다
- **배운 것**: **판별 신호에 「확정」과 「추정」이 섞이면 추정이 확정을 가린다** — 신호를 늘릴 때 우선순위와 출처 표시를 함께 넣어야 한다. 그리고 **훅이 낸 「조치」도 실측으로 검증해야 한다** — 이번엔 조치를 그대로 따르면 중복 파일이 생겼다

### 2026-08-20 — 온보딩 자동 판별(A~E) + 가이드 v30 (`e59bcb4`~)

- **훅에 상태 판별 추가** — `handoff.person` · `user.email` · **이 PC의 hostname이 어느 `people/*.md`에 등록됐는지** · 저장소 커밋 author 이력을 근거로 A~E 판정 후 「조치」와 「물어볼 것」을 주입. 주입 마지막에 `⛔ 조치에 없는 설정을 묻지 말 것`
  - A 기존·등록 PC = **질문 0** / B 새 PC = 경로 1 / C 설정 누락·PC 등록됨 = 확인 1회 / D 신규 = 3개 / E 키만 있음 = 2개
  - ④에 **「내 담당」** 프로젝트 목록 추가
- **검증** — hostname 위조 + `GIT_CONFIG_GLOBAL=/dev/null`로 5개 상태 전부 재현, 마지막은 **원격 클린 클론 + 가상 신규 PC**로 D 확인
- **가이드 v30**(2026-08-20 게시 완료 · 태그 `guide-v30`) — 「사용자 설정」 맨 앞에 **「AI가 먼저 확인하는 것」** 표(4가지 상황) 신설, 직접 설정 절차는 그 아래로. 기획자는 답만 하면 된다
- **배운 것**: **온보딩은 「설정 안내」가 아니라 「상태 판별」 문제다** — 안내문을 잘 쓰는 대신 판별 신호(hostname ↔ people 파일)를 만들면 질문이 0개로 줄어든다. hostname은 **식별자로는 부적합하지만 판별 힌트로는 유용**하다(같은 사람의 PC를 잇는 단서)

### 2026-08-20 — 3계층 전환 + 가이드 v28 + 잔여 전건 종결 (5커밋 `e1c127c`·`51d0e8f`·`9fe27bb`·`0fe871b`·`3215088`)

- **구조 전환** — `HANDOFF.md` 243줄 → 공유 정본 101줄 + `projects/` 11종 + `people/hogeun.md` + `lanes/`. 훅을 계층 주입으로 교체(`.claude/hooks/handoff-start.sh`), `SKILL.md` 재작성(종료 0단계 담당자 식별 · `git add -A` 금지 · `git diff` 기반 갱신 대상 판별 · `_archive/`)
- **무손실 검증** — 위키 ID 16/16 · 커밋 해시 29/29 · 코드 토큰 225개 중 실질 누락 0(1차에서 **환경 노트 12줄·시즌3 근거 3건 유실을 발견해 복구**)
- **가이드 v28 게시** — 「프로젝트 받기·업데이트」 다음에 「사용자 설정」 섹션. v27은 용어집 v4.8과 함께 철회돼 건너뜀. 태그 `guide-v28`
- **미추적 게이트 리포트 3건 정리** — `projects/unifi-app-xlt.md`·`projects/kaiawallet-transition.md` 신설 후 커밋
- **kaiawallet 위키 반영 실측** — v12 · 34키/170셀 전건 · 검사기 pre/post exit 0
- **잔여 전건 종결**(사용자 지시) — XLT 업로드 3건 · Figma 원문 요청 6건 · 용어집 보완 권장 5건
- **가이드 v29**(2026-08-20 게시 완료 · 태그 `guide-v29`) — 구조 변경을 기획자 문서에 반영: ⑤ 문서 지도에 `handoff/projects`·`people`·`lanes` 3행 추가 + `HANDOFF.md` 행을 「공유 정본 + 인덱스」로 정정 · ② 흐름 지도 세션 시작 설명(계층 4가지) · ③ 라우팅 갱신 · ⑦ 확장 규칙에 「새 작업 대상」 카드 · **「시스템 가이드」 탭의 ⑦ 중복 번호를 ⑦/⑧로 정정**
- **배운 것**
  - **구조를 바꾸면 그 구조를 설명하는 문서도 같이 낡는다** — 가이드 ⑤ 문서 지도가 `HANDOFF.md`를 「현재 상태·다음 할 일」의 정본으로 설명하고 있었는데 그 내용은 이미 `projects/`로 내려간 상태였다. **CLAUDE.md 「가이드 최신화 제안 규칙」의 트리거 ⓐ(규칙·절차 변경)에 해당**하며, 사용자 지적으로 발견했다 — 구조 변경 시 가이드 점검을 체크리스트에 넣어야 한다
  - **플레이스홀더는 실행된다** — 채팅에 `user.name "이름"` · `본인@example.com`을 복사 가능한 명령 블록으로 제시했고 그대로 실행돼 **5커밋이 잘못된 author로 푸시됐다.** 실행용 블록에는 예시 값을 넣지 말고, 넣어야 하면 값이 비어 있어 **실행하면 실패하는 형태**로 준다
  - **`.claude/*`는 기본 차단이다** — 훅을 만들 때 확인하지 않아 `settings.json`만 전파될 뻔했다. **새 경로를 규칙에 엮으면 `git check-ignore`로 먼저 확인**한다
  - **분할의 축을 잘못 잡으면 사고가 그대로 남는다** — "사용자별로 나누면 명확해지지 않나"에서 출발했지만, 실제 사고는 같은 사람·같은 PC의 병렬 세션이었다. **사고 기록이 축을 결정한다**
  - **무손실은 선언이 아니라 검증이다** — 토큰 대조 없이 끝냈으면 환경 노트 12줄이 조용히 사라졌다

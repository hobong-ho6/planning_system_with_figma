# clinic (클리닉 예약 동선 · K-뷰티 광고 퍼널)

> 담당자: hogeun · 마지막 갱신: 2026-09-04 · 세션 #5 · 마지막 커밋 `(이 커밋)`
> ⚠️ **병렬 세션 사고 2건 기록** — ⓐ `ff46985`(guide v33)에 이 프로젝트 스테이징(핸드오프·landpress·게이트 4건)이 함께 담겨 푸시됐다(내용 온전·메시지만 무관). ⓑ 위키 v109 PUT이 자동화 스크립트의 본문 파일 경로 미갱신으로 **직전 본문을 올려 v108 사용자 추가 1행이 일시 유실** → v110에서 신규 키로 복원, v108↔v109 전문 대조로 그 1행 외 영향 없음 확인

## 대상 / 링크

- 위키: `4667512757` 「클리닉 예약 동선 개선」 — **현재 v123** (부모 `3910828993`)
  - **하위 `4686692164` 「클리닉 LPC(Landpress) 관리 영역 정리」**(2026-09-03 신설 · **v14**) — LPC 영역 10곳의 컬렉션·필드·구조·**실등록 JSON** + 크롭 이미지 10장
  - **하위 `4685320765` 「클리닉 일본어 문구 검수 (日本語レビュー)」**(2026-09-04 신설 · **v9**) — XLT 87건 + Landpress **11필드** ko↔ja 대조표 · **검수 완료·전건 반영**(비고 ✅) · 베타 `https://unifi-web.line-apps-beta.com/k-pick/clinic`
- Jira: `UNIFY-10552`(특정상거래법 표기 링크) · `UNIFY-10554`(환율 안내·환불조건 동의) — ⚠️ **Jira는 별도 PAT 필요**
- Figma: `GOCHAYBS7hIrmWRGNuJOKV`
  - `(Web) 브릿지 페이지` `69630:13293` — 코멘트 20건
  - `(Unifi mini) 상세페이지` `69637:14698` — 코멘트 **54건**(8건 삭제 후)
  - `OA 영역` `69826:10046`
  - **(2026-09-03 추가)** `(Unifi mini) 상세페이지 - 개선` `71241:9438` — 코멘트 **42건**
  - **(2026-09-03 추가)** `병원 링크 저장` `71339:15430` — 코멘트 4건
  - **(2026-09-03 추가)** `(Unifi mini) 상세페이지 - 즐겨찾기 안내` `71417:17333` — 코멘트 2건
  - **(2026-09-03 세션 #3)** `상단 롤링 배너` `71543:3307`(코멘트 0) · `(Unifi mini) 브릿지페이지` `71241:9154`(25건) · `(Unifi mini) 상세페이지 - 전체 리뷰` `71543:3322`(1건) · `(Unifi mini) 상세페이지 - 개별리뷰` `71417:17133`(1건)
  - ⚠️ `(Unifi mini) 상세페이지` `69637:14698`는 **노드 삭제됨**(API `null`)
- XLT: **Unifi**(정본) · 프리픽스 `UF_`(**UIT** · `{{0}}`) + `mini_guidekim_`(**LV** · `{0}`) 혼재 — 키를 사용자가 직접 기재해 프리픽스로 팀 판별
- 엑셀: `xlt/xlt_clinic_screen_7keys_20260901.xlsx`(7키·**업로드 확인됨**) · `xlt/registry_fix/xlt_fix_main_faq_title_ja_1key_20260901.xlsx`(1키·**미반영**)
  - ⭐ **`xlt_clinic_all_86keys_20260903.xlsx`(86키) = 업로드 정본** — 위키 첨부(같은 파일명으로 **첨부 v2 갱신** — 본문 링크 그대로 최신본). 개별 엑셀·85키판은 **전부 여기 흡수**됐다(따로 올리지 않는다). ⚠️ `xlt/`는 `.gitignore`라 위키 첨부·게이트 리포트가 정본
- **Landpress 읽기 API**(2026-09-03 확보 · 무인증) — 프로젝트 `n7nuefo6t491uc9cp863lgyq` · 컬렉션 `k_pick_clinic_product`(병원별) · `k_pick_clinic_common_info`(공통)
  🔴 **`?locale=ko_KR` 필수** — 없으면 `primaryLocale`(**`en_US`**) 항목만 나온다. 로케일별 item id가 다르다 — **en_US 1·11·21 ↔ ko_KR 2·12·22**. CMS URL의 `?_locale=`과 이름이 다르며 `_locale=`은 API에서 무시된다
  `curl "https://landpress-content.line-scdn.net/contents/v2/projects/n7nuefo6t491uc9cp863lgyq/collections/{컬렉션}/items?page=1&limit=100"`
  CMS: `https://landpress-content-v2.linecorp.com/projects/n7nuefo6t491uc9cp863lgyq/content/collections/{컬렉션}/items/1`
- **Landpress 병원별 item 3건 등록 완료**(2026-09-03) — `uid`: **`tiana-ps`**(item 11) · **`da-ps`**(item 1) · **`healing-eye`**(item 21). API 재조회로 **9필드 전건 일치** 확인(uid는 당일 `tiana`→`tiana-ps`·`healing`→`healing-eye`로 변경됨)
- Landpress 콘텐츠 JSON: `landpress/` — **병원별 9파일**(`{bridge_info,detail_info,menu}_{da-ps,tiana-ps,healing-eye}_ko_KR.json` — **파일명 = CMS `uid`**) + `aftercare_ko_KR.json`(보류) · (기존 24파일 구성) 공통 4필드 × 5개 언어 + 병원별 3필드 × ko + **`voucher_ko_KR.json`**(추천 바우처 4종 · ko만)
- 게이트 리포트: `reports/gate/gate_report_clinic_*.md` · `gate_report_landpress_clinic_4sections_2026-09-01.md`
  - 2026-09-03 세션 #2: `gate_report_clinic_detail_improved_8keys_*` · `_oa_save_improved_3keys_*` · `_favorite_guide_2keys_*` · 2026-09-03 세션 #3: `gate_report_clinic_service_no_price_menu_*` · `_bridge_rolling_banner_10keys_*` · `_mini_bridge_19keys_*` · **`_consolidated_85keys_*`**(전수 425셀) · `_hospital_products_*`(병원 3곳) · `_aftercare_faq_*` · **`_review_verified_*`**(86키 전수 430셀 · 띄어쓰기 교정 후속) · **`_unify_notation_*`**(표기 통일 5종) · **`_upload_verify_2026-09-04`**(업로드·반영 검증)

## 현재 상태

위키 **v123**(세션 #5에서 v117→v123). 하위 LPC **v14** · 일본어 검수 **v9**. 게이트 **17건 누적 전부 P0=0** · `check_gate_report.py`·`check_wiki_storage.py` 전건 exit 0.

**세션 #5에서 파이프라인이 한 바퀴 닫혔다** — 다국어 5개 언어 등록 → 일본어 현지 검수 → 검수 반영 → API 검증까지.

- **Landpress 다국어 완료** — 공통 5필드 + 병원별 3필드 × 2곳이 **5개 언어 전부 등록**. `?locale=`별 재조회로 **70/70 전건 일치** 확인. 「다국어는 베타 확인 후」 보류 종결
- **일본어 현지 검수 1라운드 완료** — 수정안 32건(XLT 10 · LP 22) + 검수자가 「현행」 칸을 직접 고친 1건을 전건 반영. 재조회 결과 **XLT 11/11 · Landpress 9/9 일치**. 검수 페이지는 「현행」 칸을 적용값으로 갱신하고 비고에 ✅ 34행 기록
- **캐시백 ja `還元` 전면 통일** — 용어집 **v5.0**(캐시백 ja 교체 + deprecated #20) · XLT 17키 재업로드. 등재 2,461키에서 `キャッシュバック` **잔존 0**
- **용어집 v5.1** — 실사용 0건 등재값 2건 교정(`가격` zh `售價`→`價格` · `유의사항` th `หมายเหตุ`→`ข้อควรระวัง`). A/B 실측 **P1 2350→2339(−11)** · P0·P2 불변. 가이드 zip 동반 갱신 → 태그 `guide-v33.2`·`guide-v33.3`
- **캐시백률 병원별 가변화** — B/E `cashbackRate`가 **5·8·10·15**로 다름을 확인(da-ps 8 · tiana 15 · healing 15). `faq`·`process`의 `15%` 고정을 비율 미명시 문장으로 5개 언어 교체 → **3중 불일치 종결**
- **`bridge_info.tag` 정본을 B/E `tags`로 일원화** — 병원당 1개 → 4·4·5개. `clinic_detail`의 `tags`·`recommendPoints`와 3자 동기화
- **`da-ps` 비공개**(사용자 결정) — 컬렉션 조회 `total=2`가 정상. 로컬에 3필드 × 5개 언어 보관, 재게시 즉시 가능
- **B/E 파일 → Landpress 이관 진행** — `clinic-detail.{tiana-ps,healing-eye}.json`을 언어별로 펼친 10파일 산출(`landpress/be/`). `reviews`는 작성자 원문이라 5개 파일에 동일

## 진행 중 작업(WIP)

없음.

## 다음 할 일

- [x] ~~P0 XLT 업로드~~ — **2026-09-04 완료 확인**(레지스트리 v1.7.4→**v1.7.5** · 2,431→**2,461키** · 86키 전건 등재)
- [x] ~~P0 XLT 7키 재업로드~~ — **2026-09-04 완료 확인**(위키 86키 × 5개 언어 430셀 전건 대조 · 상이 0)
- [x] ~~P0 Landpress 반영 5필드~~ — **2026-09-04 완료 확인**(`?locale=ko_KR` 조회 · 공통 5 + 병원별 9 = **14필드 전건 일치**)
- [x] ~~P0 일본어 검수 1라운드~~ — **2026-09-04 완료**(수정안 32 + 현행칸 직접수정 1 전건 반영 · XLT 11/11 · LP 9/9 API 확인 · 검수페이지 v9 비고 ✅ 34행)
- [x] ~~P1 ja 미번역 3종~~ — **2026-09-04 완료**(5개 언어 전건 등록 · 검수까지 완료)
- [x] ~~P1 Landpress ja `口コミ` 잔존~~ — **2026-09-04 해소**(`ユーザーレビュー`). ⚠️ 다만 검수 결과 `UF_clinic_mini_bridge_section_review`에 `口コミ`가 **새로 들어갔다**(사용자 승인 예외) — 아래 신규 항목 참조
- [x] ~~P1 `k_pick_clinic_product` en_US 한국어 구값~~ — **2026-09-04 해소**(실제 영어 등록). 옛 설명: 🔴 — ko_KR 항목(2·12·22)은 최신인데 primaryLocale인 en_US가 **표기 통일 이전 한국어**다(`리쥬란힐러`·`프리미엄 LASIK`·`http://` 등). FE가 non-ko 로케일에서 이걸 서빙하면 **구 문구가 노출**된다. 다국어 확장 시 en_US에 실제 영어를 넣거나, 그전까지는 ko 폴백 정책을 확정해야 한다
- [ ] **P1 `menu.items[].description` 1건 잔존** — 디에이 `프리미엄 리프트 패키지`. 티아나 3 + 힐링안과 3은 **2026-09-04 A안(시술 설명형)으로 작성 완료**(5개 언어 · 의료광고 금지어 스캔 0건). ⚠️ **디에이 2건은 B안(의료진 강조형)이라 형식이 갈린다** — A안 통일 여부 결정 필요
- [x] ~~공통 `faq` 9항목 반영~~ — **2026-09-04 완료 확인**(`updatedAt` 09-03 14:36 · `id` 9건 일치)
- [ ] **P1 `aftercare` 신규 필드 보류**(사용자 결정) — 「시술 후 주의사항」 9항목(LINE 챗 원문 2,300자 → 1,150자 축약 · 수치 18개 전건 보존). ⛔ 내용이 **티아나 코수술 전용**인데 `common_info`는 공통 컬렉션이라 배치 부적합 — 다른 병원에 노출되면 의료적 오안내
- [ ] 🔴 **P1 `ラセック`→`ラシック` 적용의 파급** — 일본어 검수 요청으로 `menu_healing-eye` ja를 바꿨는데 `ラセック`=LASEK(라섹) / `ラシック`=LASIK(라식)로 **다른 수술**이다. ja 화면에서 라섹이 사라지고 `スマイルラシック`과 라식 계열 2개가 되며, **ko·en·th·zh는 라섹 유지**라 언어별로 시술이 갈린다. 또 B/E 원문인 `clinic_detail.services[].name`은 `スマイルラシック · ラセック · ICLレンズ挿入術`이라 **같은 화면에 두 표기 공존**. B/E 원본 수정 여부 결정 필요
- [ ] **P1 `口コミ` 용어집 예외 1키** — `UF_clinic_mini_bridge_section_review` ja가 검수 요청으로 `口コミ`가 됐다(사용자 승인). 용어집 v5.1 `리뷰` ja=`レビュー`와 이탈해 검증기 P1 상시 1건. 같은 화면의 `UF_clinic_detail_review_source`는 `レビュー`라 **두 표기 공존**
- [ ] **P1 B/E `tags`의 구분자 오류** — B/E가 **zh·th 칸에 일본어 전용 중점 `・`(U+30FB)** 를 쓴다(da-ps zh 2·th 1 · healing zh 1). Landpress·`clinic_detail`은 `·`(U+00B7)로 교정했지만 **B/E 원본을 고치지 않으면 다음 동기화 때 되돌아온다**. QA에서 확인 예정(사용자)
- [ ] **P1 `bridge_info.tag` 개수 증가 UI 확인** — 병원당 1개 → 4·4·5개. 브릿지 카드 노출 개수 확인 필요(FE 전달 완료)
- [ ] **P1 의료광고 법무 검토** — `menu.items[].description` 6건은 금지어 사전 5개 언어 스캔을 통과했으나 뉘앙스는 미보증. 특히 `남성 얼굴 비율에 맞춰 설계하는`
- [ ] **P2 `da-ps` 재게시** — 사용자가 비공개 전환(2026-09-04). 로컬에 3필드 × 5개 언어 보관. 재게시 전 `프리미엄 리프트 패키지` description·A/B안 형식 정리 권장
- [ ] **P2 `clinic_detail` Landpress 이관 마무리** — 언어별 10파일 산출 완료(`landpress/be/`). `reviews`를 언어별로 나눌지 · 컬렉션·필드 설계 미정
- [ ] **P1 병원별 단일본 3파일 정리** — 파일명 규칙은 **`{필드}_{uid}_{locale}.json`으로 확정**했다(CMS 편집 단위 = 필드). 다만 구 단일본 `menu_ko_KR.json`·`bridge_info_ko_KR.json`·`detail_info_ko_KR.json`이 남아 **데이터가 중복**된다(단일본은 티아나 `bridge_info` + 디에이 `menu`가 **섞인 실등록 초기값**). 삭제 여부 결정 필요
- [x] ~~P1 다국어 베타 확인 후 진행~~ — **2026-09-04 완료**(70/70 전건 일치). 잔여: `voucher` 카드 아트가 일본어라 **이미지 로케일 분기**는 미결
- [x] ~~P1 `k_pick_clinic_product` locale 결함~~ — **2026-09-04 해소**(로케일별 item 생성 · `postId`로 묶임. 매핑표는 LPC 문서 v14 §1)
- [ ] **P1 `UF_clinic_detail_benefit_prefix` 구조** — `결제금액의` / `8% 캐시백 혜택`이 2개 텍스트로 분리돼 **en·th 조합 시 어순이 뒤집힌다**. 등재값 선례(`UF_clinic_bridge_card_cashback` = `결제금의 캐시백 {{0}}%`)처럼 **단일 키 병합** 권장 — FE·디자인 결정 필요
- [x] ~~P1 용어집 보완 5건~~ — **v4.9에서 반영 완료**. 옛 내용:  — ⓐ 혜택 en `perks`→**`benefit`**(29 vs 6 · **5세션 반복**) ⓑ 더보기 ja `すべて見る`→**`もっと見る`**(⚠️ **근거 정정**: `すべて見る`는 0건이 아니라 **6건**이나 전부 ko 「전체보기」 번역 · ko 「더보기」는 `もっと見る` 5건) ⓒ 더보기 zh `查看更多`→**`更多` 병기**(22 vs 5 · 문맥 분기) ⓓ 결제 zh `結帳`→**`付款` 병기**(25건 · `結帳`은 체크아웃 한정) ⓔ **교환 en `swap`·ja `スワップ` 문맥 분기** — 토큰 스왑 전용 등재값이 「포인트→JPYC 전환」에도 매칭돼 6건 오탐. ⛔ 버전 상승 시 **기획자 가이드 zip 동반 갱신 필수**
- [x] ~~P2 `menu.price` 구조~~ — **오브젝트 `{from, amount, currency}`로 확정·등록됐다**(2026-09-03 13:12 실측). 잔여 논점: `from`이 예약어라 `is_from` 등 대안 검토는 FE 협의 사항으로 남는다
- [ ] **P1 기획·법무 검토 3건** — ⓐ FAQ 답변 3건이 **Claude 작성 샘플**(Figma엔 1개만) — 확정 시 5개 언어 재생성 ⓑ `kpick_clinic_oa_save_01_03` CTA `LINE 친구 추가하고 상담하기` — 저장(찜) 플로우인데 「상담」이라 의도 확인 권장 ⓒ 개선판에서 `UF_clinic_oa_save_note`(친구 추가 목적 고지)가 삭제됐다 — 개인정보 이용 목적 고지 성격이라 **법무 확인**
- [ ] P2 **`voucher` JSON 미확정 3건** — ⓐ `price`(문자열)/`normalPriceJpy`(숫자) 비대칭(`priceJpy` 추가로 완화) ⓑ `targetUrl` 표기 불일치(`oliveyoung-` vs `olive-young-`) ⓒ `sortOrder` 채택 여부 · 아울러 실등록에 `title`·`moreLabel`·`moreTargetUrl`이 추가돼 있다(2026-09-03 실측)
- [x] ~~P2 병원별 3필드 다국어 확장~~ — **2026-09-04 완료**. `tag`는 **B/E `tags`(5개 언어 완비)를 그대로 복사**하는 방식으로 정리했다
- [x] ~~P2 캐시백률 3중 불일치~~ — **2026-09-04 종결**. B/E `cashbackRate`가 병원별 5·8·10·15로 다름을 확인하고 `faq`·`process`를 **비율 미명시 공통 문장**으로 5개 언어 교체했다
- [ ] P2 **`UF_clinic_oa_*` 키 XLT 미등재 + 다국어 부재** — `_title`·`_desc`·`_desc_terms`·`_note`·`_btn`·`_agree_*`가 위키에만 있고, `_01_03` 외 기존 행은 5개 언어 번역 행이 없다
- [x] ~~P2 GA Event 미정의~~ — **2026-09-03 세션 #4(GA)에서 정의**(v103·v106). `XLT`→`XLT & GA` 열, view 6·click 18(`#`=Description 번호), 롤링 배너·OA 3행 「정의 대상 아님」. **`click_faq`는 Landpress `faq.items[].id`(4건) 파라미터** — 5개 언어 JSON `id` 신설 + **CMS 반영 완료**(LPC 문서 v4). 상세-개선 XLT No 6 이후 +2 정정. 규칙 `md/GA.md`
- [ ] P2 **`click_review_card` `#`이 `-`** — 개별리뷰 카드에 Figma 코멘트가 없다. 디자이너 코멘트 추가 후 번호 기입 · **FE 협의**: 이벤트 전송 + FAQ 펼침 시 `items[].id`를 `faq_id`로
- [ ] **P1 코치마크 미결 3건** — ⓐ 즐겨찾기 배너와 **메시지 중복**(중복 노출 여부·최초 1회 등 조건 정의) ⓑ 키 네임스페이스 `UF_clinic_detail_favorite_guide_*` — mini 공통이면 `UF_mini_favorite_guide_*`가 맞다(FE 협의) ⓒ 문구의 「더보기」는 **Unifi가 아니라 LINE 클라이언트 상단 우측 메뉴**다 — LINE 자체 UI의 언어별 실제 표기 확인 후 맞출지 결정
- [ ] P2 **Figma 원본 정리 5건** — 우상단 제목 `올리브영 1만원권`(→3만원권) · 좌상단 카드 이미지(1만→5만원권 아트) · `다이소 5만원권`(→3만원권) · `라인 무료 상담`(→`LINE`) · `더보기 를`(→`더보기를`) · **프레임 경계 밖 럭키드로우 유의사항 12건 잔존 노드**

## 주요 결정 사항 (이 프로젝트 한정)

| 날짜 | 결정 | 근거 / 커밋 |
|---|---|---|
| 2026-09-04 | **ja 캐시백을 `還元`으로 전면 통일** — 09-01·09-03의 `キャッシュバック` 결정 2건을 **반전**(그 2행은 삭제) | 사용자 결정. 등재 실측은 교체 직전 `キャッシュバック` 17 : `還元` 1이었으나 **실측 다수를 결정으로 뒤집었다**. 용어집 v5.0 + XLT 17키 재업로드로 동시 전환해 P1 중립(−1) |
| 2026-09-04 | **캐시백률을 문구에서 제거** — `faq`·`process`를 비율 미명시 공통 문장으로 | B/E `cashbackRate`가 병원별 **5·8·10·15**로 다름을 확인. 15% 고정은 명백한 오류였고 3중 불일치도 함께 종결 |
| 2026-09-04 | **`bridge_info.tag` = B/E `tags` 그대로 복사** · `clinic_detail`의 `tags`·`recommendPoints`도 3자 동기화 | B/E가 5개 언어를 이미 갖고 있어 번역 중복이 사라진다. 「추천 포인트는 B/E라 LPC가 아니다」는 혼동도 해소 |
| 2026-09-04 | **`menu.items[].description`은 A안(시술 설명형)** — 티아나·힐링안과 6건 | 사용자 선택. 의료법 제약상 효과 단정·최상급·`부작용 없음`을 쓰지 않고 시술 내용만 기술. **디에이는 사용자 지시로 제외**(B안 유지) |
| 2026-09-04 | **일본어 검수 결과 전건 적용** — 보류 3건(`ラシック` 2 · `口コミ` 1) 포함 | 우려를 보고했으나 사용자가 「모두 변경」으로 재확인. 파급은 「다음 할 일」에 남긴다 |
| 2026-09-04 | **`da-ps` 비공개 전환** — 컬렉션 조회 `total=2`가 정상 | 사용자 결정. 데이터 소실 아님 · 필요 시 재게시 |
| 2026-09-03 | **개선 프레임은 새 Screen 행으로 추가**(기존 행 보존) — 3프레임 모두 동일 적용 | 개선 전/후 대조 목적. 단 사용자가 이후 구 `(Unifi mini) 상세페이지` 행을 직접 삭제해 개선판으로 통합했다 |
| 2026-09-03 | **문구가 바뀐 기존 키는 값 변경**(신규 키 미생성) — `UF_voucher_mini_certified`·`UF_clinic_detail_benefit_sub`·`UF_clinic_oa_save_title`·`_desc` | 사용자 결정. 공유 키 파급을 감수. **단 `UF_clinic_oa_save_btn`은 예외** — `kpick_clinic_oa_save_01_02`(약관 미동의)와 공유해 CTA 의미가 갈리므로 신규 키 `UF_clinic_oa_save_btn_line` 부여 |
| 2026-09-03 | **캐시백 8%는 이 프레임에만 적용** — 위키 Policy(20% 2곳)·`landpress/process`·`faq`(15%) 미변경 | 사용자 결정. 8%는 법무 안전 범위(5~15%) 내 |
| 2026-09-03 | **`cancel_1~3`·`notice_1~3` 6키는 위키 XLT ↔ Landpress 병존 유지** | 사용자 결정. 상담·시술과정 8키 제거 선례를 따르지 않는다. **FE에 어느 쪽을 읽는지 명시 필요** |
| 2026-09-03 | **추천 바우처를 Landpress `voucher` 필드로 이관** — 고정 4종(올리브영 5만·3만 / 다이소 1만·3만) · 필드명 **camelCase**(상품 API와 1:1) | 카드 내부 라벨 3키(`_price`·`_origin_price`·`_cashback`)는 XLT 유지. 타이틀·더보기 라벨은 **XLT와 JSON 병존**(사용자 지시) |
| 2026-09-03 | **`더보기`·`혜택` 용어집 권장값 미채택** — 등재 실사용 실측 우선 | `benefit` 29 vs `perk` 6 · `更多` 22 vs `查看更多` 5. ⚠️ **근거 정정**: `すべて見る`를 「0건」으로 적었으나 실측 **6건**이다(전부 ko 「전체보기」). 「더보기」는 `もっと見る` 5건이 정본 — 문제는 실사용이 아니라 **등재 의미 오류**다 |
| 2026-09-03 | **곡선 따옴표 `‘즐겨찾기’` 유지** | 등재값 선례 2건(`UF_asset_purchase_price_edit_desc4`·`UF_boost_claim_complete_desc`)이 한국어 강조 인용에 `‘’`를 쓴다 |
| 2026-09-01 | **Landpress 필드 7종 확정** — 공통 `cautions`·`change_cancel`·`process`·`faq` / 병원별 `bridge_info`·`detail_info`·`menu`. 리스트형은 `{"items":[…]}`, 오브젝트형은 필드 직접 노출 | 병원 식별자는 **API의 `uid`** 로 처리하므로 JSON에 넣지 않는다 |
| 2026-09-01 | **`cautions`·`change_cancel`만 `title`을 JSON에 포함**, `process`·`faq`는 XLT 유지 | 타이틀 값은 등재값 `UF_voucher_detail_caution`·`_cancel` 5개 언어 재사용. 두 키는 Unifi에 살아 있어 **XLT와 JSON 양쪽 병존을 사용자가 승인** |
| 2026-09-01 | **`tag`는 양쪽 모두 배열** (`bridge_info`·`detail_info`) · 값은 **자유 텍스트** | 같은 이름이 타입이 갈리면 FE 사고. 고정 코드(enum) 제안은 반려 |
| 2026-09-01 | **번역값·규격 결정 5건(압축)** — ⓐ `mini_guidekim_cat_dematology` ko=`피부과·성형외과`(Figma 기준 · 3중 불일치 해소) ⓑ `UF_voucher_recommend_origin_price`=`정가 <span>{{0}}</span>`(span이 **가격 변수만** 감싼다 — 라벨까지 감싸면 취소선이 번진다) ⓒ `mini_guidekim_cat_eye` th=`จักษุวิทยา`(`จักษุ`는 결합형이라 단독 불가) ⓓ 엑셀 포함 기준 = **「Unifi 미등재」**(`mini_guidekim_cat_*` 3키는 Dapp Portal에만 있어 7키 전량 포함) ⓔ **캐시백률은 리터럴 `15%`**(FE API 조달 불가 → 수기 텍스트) | 상세 근거는 세션 #1 게이트 리포트 |
| 2026-09-03 | **XLT 네임스페이스 `UF_clinic_mini_bridge_` 신설** — mini 브릿지 문구를 Web(`UF_clinic_bridge_`)과 분리. 롤링 배너 10키도 리네임 | 실측 — 기존 `UF_clinic_bridge_*` **10키가 전부 (Web) 브릿지 문구**였고 두 화면은 카피가 완전히 다르다(Web `좋은 후기로 주목받는…` / mini `한국 인기 클리닉 더 좋은 혜택과 함께`). 후보 선례 `UF_clinic_mini_bridge_` 0건 · `UF_mini_guidekim_bridge_` 3건(guidekim 상품 브릿지). **업로드 전이 유일한 정정 시점**이었다 |
| 2026-09-03 | **코멘트 선별 모드 운용 결정 3건** — ⓐ `xlt` 마커는 **「본문에 `xlt` 한 줄이 독립적으로 있으면」**으로 판정(CLAUDE.md는 「XLT로 시작」인데 설명 뒤 끝줄에 단 것이 4건 있었고 `collect_frames.py`는 「정확히 xlt」만 봐 6건만 잡았다 — 규칙대로 10건 처리) ⓑ `(Unifi mini) 상세페이지 - 개선`을 **새 Screen 행으로 재추가**(신규 키 0 · 코멘트 42→44 재조회본을 별도 보존) ⓒ **리뷰 2프레임은 마커가 없어 키 부여 0** — 화면·정책만 반영, `영수증 인증`·`VIP`·`공식 홈페이지`는 「키 부여 검토 대상」으로 위키에 남김 | 사용자 결정 |
| 2026-09-03 | **브릿지 「시술 인증」은 별도 키** — `UF_clinic_mini_bridge_review_verified` 신설, 기존 `UF_clinic_detail_review_verified`(`영수증 인증`)는 유지 | 한 키에 두 문구였고 **Figma 원문도 실제로 다르다**(브릿지 `71241-9154` y=17372 `시술인증` / 상세·리뷰 `영수증 인증`). 어휘는 형제 키 어형 승계 — 「시술」 등재 실측 **11/11 만장일치**(`施術`·`treatment`·`ทรีตเมนต์`·`療程`) |
| 2026-09-03 | **띄어쓰기는 올바른 표기로 교정** — `시술인증`→`시술 인증` · `리쥬란힐러`→`리쥬란 힐러` · `리프팅패키지`→`리프팅 패키지` · `찾고있다면`→`찾고 있다면` | 사용자 지시. **시술 상품명은 관행상 붙여쓰기 유지**(`코성형`·`렌즈삽입술`·`정밀검사` 등) · `의사소통`은 한 단어(오탐) · `시력교정`은 전문용어라 미교정. Figma 원본 수정 요청 대상 4건 |
| 2026-09-03 | **병원별 Landpress 파일명 = `{필드}_{uid}_{locale}.json`** · `uid`는 `tiana-ps`·`da-ps`·`healing-eye` | CMS는 필드를 하나씩 편집하므로(`?activeField=menu`) 필드 단위가 붙여넣기에 맞다. `uid`는 당일 `tiana`·`healing`에서 개명됐다 |
| 2026-09-03 | **힐링안과 `menu.tag` 3번째는 `정밀검사`** (Figma 원문의 `라식` 중복 대신) · **homepage는 `https://www.healingeye.co.kr/`** | 사용자 결정 + 실측(`https` **200 OK** · `www` 없는 주소는 301 리다이렉트). Figma 원본 수정 요청 대상 |
| 2026-09-04 | **`UF_clinic_detail_review_source` ja `口コミ` → `レビュー`** | 용어집 v4.9의 `리뷰` 등재가 잡아낸 **실제 일관성 위반**. 실측 ja `レビュー` **8건** vs `口コミ` **1건**(이 키뿐) · 같은 문맥의 `UF_clinic_mini_bridge_section_review_note`도 `レビュー`. 사용자 승인 |
| 2026-09-04 | **`MINI App`이 공식 표기** — LINE Developers 문서가 제품명을 **`LINE MINI App`**(`MINI` 전부 대문자)로 일관 표기하고 `Mini App`은 쓰지 않는다 | 1차 출처 확인 — [문서 허브](https://developers.line.biz/en/docs/line-mini-app/) · [Introducing](https://developers.line.biz/en/docs/line-mini-app/discover/introduction/) 제목·헤딩·본문·API 레퍼런스 전건 동일. 09-03 표기 통일 방향이 옳았음을 확정. ※ `/ko/`·인증 가이드 페이지는 **403**이라 영문+검색으로 교차 확인 |
| 2026-09-03 | **표기 통일 5종** — `시력교정`→`시력 교정` · ko `후기`→**`리뷰`** · `Mini App`→`MINI App` · `프리미엄 LASIK`→`프리미엄 라식` · `프리미엄 리프트 PKG`→`프리미엄 리프트 패키지` · 병원명 붙여쓰기 | 사용자 지시. 「리뷰」를 고른 근거 = **4개 언어가 이미 전부 review 계열**이라 ko만 맞추면 번역이 한 셀도 안 바뀐다. 치환은 **Screen~병원데이터·다국어 구간 41곳에 한정**(History·법무 섹션 제외 — 전역 치환은 과거 이력을 훼손) |
| 2026-09-03 | **`faq` 4 → 9항목** — 화면 구성 근거로 신규 5건(`japanese_consultation`·`booking_flow`·`cashback_usage`·`info_review_source`·`change_or_cancel`) | 사용자 승인. 5건 전부 화면·실등재값 근거가 있다(세션 #1의 「샘플」 답변과 다름). `items[].id`는 GA `faq_id` 파라미터와 연결 |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지 — 이 프로젝트 한정)

- **Figma 원본 수정 요청 전건 무시** — 표기 통일 5종 + `시술인증`·`리쥬란힐러`·`리프팅패키지`·`찾고있다면`·`라인 무료 상담`·`더보기 를`·`무료 LINE상담`·`還元特典`·`最大15%還元`·`편한 의사 소통` 등 **누적 15건**. Figma는 참고용이라 원본을 고치지 않는다 — **목록화·재제안도 하지 않는다** (2026-09-04, 세션 #4)
- **`프리미엄 라식`·`프리미엄 리프트 패키지`가 공식 상품명** — 위키 「병원 데이터」 표의 `프리미엄 LASIK`·`프리미엄 리프트 PKG`는 폐기. 병원 확인 항목 종결 (2026-09-04, 세션 #4)

- **Figma 원문 수정 요청 8건**(구분자 `･`→`·` 5건 · `무료 LINE상담` 띄어쓰기 · `change_cancel` 문체·마침표 2건) — **Figma는 참고용**이라 원본을 고치지 않는다 (2026-09-01, 세션 #1)
- **등재값 U+FF65(`･`) 4건**(`UF_login` 등) · **Dapp Portal `mini_guidekim_cat_dematology` en `Surge` 잘림** — 그대로 둔다. Unifi 신규 등록분만 `Surgery`로 교정 (2026-09-01)
- **`UF_voucher_detail_caution`·`_cancel` 2키를 위키 XLT 표에서 제거하지 않는다** — 타이틀이 JSON으로 가도 XLT와 병존 (2026-09-01)
- **어노테이션 이미지 핀 3·5 색상(빨강)** — Figma 코멘트 본문이 `LPC로 관리`/`Admin에…`으로 시작해 파랑이 안 된다. 코멘트를 고치지 않으므로 그대로 (2026-09-01)
- **법무 준수 체크 섹션의 서식**(정렬 스타일·파란 텍스트) — 사용자 작성 영역이라 손대지 않는다 (2026-09-01)
- **`LINE 무료 상담` 키 중복을 통합하지 않는다** — `UF_clinic_detail_btn_consult_line`(상세 CTA)과 `UF_clinic_mini_bridge_badge_consult_title`(브릿지 배지)의 5개 언어 값이 완전히 같지만 **그대로 둔다** (2026-09-03, 세션 #3)
- **`UF_main_faq_more` ja 일관성은 다루지 않는다** — 제목이 `FAQ`인데 더보기가 `よくある質問をもっと見る`로 갈리지만 **그대로 둔다**(이 키는 클리닉 위키 범위 밖) (2026-09-03, 세션 #3)

## 세션 기록 (최신 위, 최대 5개)

### 2026-09-04 — 세션 #5: 다국어 완성 + 일본어 검수 반영 + 용어집 v5.1 (v117→v123 · LPC v13→v14 · 검수 v1→v9)

- 완료: **Landpress 다국어 5개 언어 70/70** · **일본어 검수 1라운드 전건 반영**(XLT 11/11 · LP 9/9 API 확인) · 용어집 **v5.0→v5.1**(가이드 zip 2회 · 태그 `guide-v33.2`·`guide-v33.3`) · 캐시백 ja `還元` 전면 통일(17키) · 캐시백률 병원별 가변화 · `menu` description A안 6건 · 전체 리뷰 CTA 변수화(`{{0}} 상담신청`) · `clinic_detail` 언어별 10파일 · 게이트 **6건 P0=0**
- 결정: `還元` 전면 통일(실측 다수 반전) · 캐시백률 문구 제거 · `bridge_info.tag`=B/E `tags` · description A안 · **검수 전건 적용**(보류 3건 포함) · `da-ps` 비공개
- 🔴 교훈 **표 파서는 열 수를 고정하지 않는다** — 검수 페이지가 XLT **6열**·Landpress **5열**이라 5열만 훑어 **XLT 수정안 10건을 통째로 놓쳤다**. 사용자가 `UF_clinic_detail_notice_2`를 짚어 발견. 표마다 헤더를 먼저 읽고 열 수를 판정한다
- 🔴 교훈 **검수자는 「수정안」 칸이 아니라 「현행」 칸을 고치기도 한다** — `voucher.title`이 그랬다. **수정안 기입 행만 보지 말고 「현행 ↔ 현재 값」을 전건 대조**해야 누락이 없다. 이 대조로 `UF_clinic_detail_review_source`의 **등재값 드리프트**(`口コミ` 잔존)도 함께 발견했다
- 🔴 교훈 **의미가 바뀌는 검수안은 적용 전에 반드시 짚는다** — `ラセック`(LASEK)→`ラシック`(LASIK)은 **다른 수술**이다. 보류·보고 후 사용자 재확인을 받아 적용했다. 언어 검수라도 **도메인 사실은 별도로 검증**한다
- 교훈 **B/E 데이터도 로케일 오염이 있다** — zh·th 칸에 일본어 전용 중점 `・`(U+30FB)를 써 게이트 P0가 났다. 외부 데이터를 그대로 복사할 때 **문자체계 검사를 반드시 통과시킨다**
- 교훈 **범위를 넓게 잡으면 되돌리는 비용이 든다** — clinic-detail 2개만 대상인데 `hospital be whole`까지 변환해 「빈 값 30건」 보고로 혼선을 만들었다. 첨부 파일 목록 = 범위다
- 교훈 **중간 산출물을 임의로 지우지 않는다** — `be/`를 「중복」으로 판단해 삭제했으나 원본(Downloads)도 이미 사라져 언어별 파일에서 **역합성으로 복원**했다(왕복 검증 ✅)

### 2026-09-04 — 세션 #4: 업로드·반영 검증 + 결정 2건 (v113→v114 · LPC v10→v11)

- 완료: XLT 레지스트리 재조회로 **86키 전건 등재 확인**(v1.7.4→**v1.7.5** · 2,431→**2,461키**) · Landpress 두 컬렉션 재조회 · 게이트 1건 P0=0
- 🔴 **업로드된 것이 표기 통일 이전 값(첨부 v2)** — 7키 10셀이 어긋나 `xlt_clinic_unify_fix_7keys_20260904.xlsx` 생성·첨부(재업로드 대기)
- ✅ `faq` 9항목 반영 확인 / ❌ Landpress **5필드 미반영**(공통 `cautions` + `da-ps` `menu` + `healing-eye` 3필드)
- 결정: **`프리미엄 라식`·`프리미엄 리프트 패키지` = 공식 상품명** · **Figma 원본 수정 15건 무시**(목록화도 안 함) · **`MINI App` 공식 표기 확정**(LINE Developers 1차 출처)
- 🔴 교훈 **`glob(...)[-1]` 같은 암묵적 파일 선택을 검증에 쓰지 않는다** — 측정 시점에 대상 엑셀이 이미 삭제돼(`xlt/`는 `.gitignore`) glob이 **다른 파일(1키 faq)** 을 집었고 「P1 4→1」로 **잘못 보고**했다. 실제는 P1 5·P2 2(전건 오탐). 경로를 명시하고 없으면 **실패하게** 한다
- 교훈 **값 치환은 태그 포함 좁은 패턴으로 하면 서술문을 놓친다** — 09-03 통일에서 `<td>Mini App</td>`만 치환해 Screen Description 1건이 남았다. 같은 문구가 **표 셀·Description 서술·다국어 표** 3곳에 나오므로 **문자열 자체로 치환하되 구간(History·법무 제외)을 한정**한다
- 교훈 **「업로드했다」는 보고를 값 대조 없이 완료로 적지 않는다** — 등재 **여부**는 100% 맞았지만 **값**이 구버전이었다. 첨부를 같은 파일명으로 갱신(v2→v3)하면 **어느 버전을 받아 갔는지 알 수 없다** — 업로드 확인은 반드시 **레지스트리 값 전건 대조**로 한다

### 2026-09-03 — 세션 #4(GA) · #3 · #1·#2 *(압축 병합)*

- **#4(GA)** `md/GA.md` 신설 후 첫 적용 — 헤더 `XLT & GA` · view 6 · click 18 · `click_faq`용 `faq.items[].id` 4건(CMS 반영). 페이지가 계속 바뀌어 **PUT 직전 라이브 재조회 + 앵커 assert**로만 넣었다
- **#3** 5프레임 반영 · **LPC 하위 페이지 신설** · 병원 3곳 등록 검증 · `faq` 4→9항목 · 표기 통일 5종 · **통합 엑셀 86키** · 게이트 8건 P0=0
- **#1·#2** 위키 신규 생성(Jira 2건) · 62키 번역 · OA Flex JSON · **Landpress 7필드 23파일 + `voucher`** · 개선 프레임 3개 반영
- 🔴 교훈 **본문 생성과 PUT을 다른 파일로 나누면 조용히 구버전을 올린다**(v109 사고 — 사용자 추가 1행 유실 후 v110 복원). 생성 직후 **같은 프로세스에서 버전 가드 후 PUT**
- 🔴 교훈 **`glob(...)[-1]` 같은 암묵적 파일 선택을 검증에 쓰지 않는다**(삭제된 엑셀 대신 다른 파일을 집어 오보고). 경로를 명시하고 없으면 실패하게 한다
- 교훈 **로컬 산출물보다 실등록값 API가 정본이고 세션 중에도 바뀐다** · **프리픽스는 Web/mini까지 쪼개 확인** · **정규식이 `status` 매크로 셀을 조용히 빼먹는다**(셀 깊이 추적) · **중첩표에서 비탐욕 정규식이 끊긴다**(HTTP 400 3회) · **자체 점검 스크립트의 리터럴도 검증 대상**(nbsp·U+2028) · **`code` 매크로 `language=json` 미지원** · **사용자가 같은 페이지를 동시 편집**(PUT 직전 버전 가드) · 값 치환은 **구간 한정**(History·법무 제외)

# clinic (클리닉 예약 동선 · K-뷰티 광고 퍼널)

> 담당자: hogeun · 마지막 갱신: 2026-09-03 · 세션 #2 · 마지막 커밋 `8637bb9`(세션 #2)

## 대상 / 링크

- 위키: `4667512757` 「클리닉 예약 동선 개선」 — **현재 v95** (부모 `3910828993`)
- Jira: `UNIFY-10552`(특정상거래법 표기 링크) · `UNIFY-10554`(환율 안내·환불조건 동의) — ⚠️ **Jira는 별도 PAT 필요**
- Figma: `GOCHAYBS7hIrmWRGNuJOKV`
  - `(Web) 브릿지 페이지` `69630:13293` — 코멘트 20건
  - `(Unifi mini) 상세페이지` `69637:14698` — 코멘트 **54건**(8건 삭제 후)
  - `OA 영역` `69826:10046`
  - **(2026-09-03 추가)** `(Unifi mini) 상세페이지 - 개선` `71241:9438` — 코멘트 **42건**
  - **(2026-09-03 추가)** `병원 링크 저장` `71339:15430` — 코멘트 4건
  - **(2026-09-03 추가)** `(Unifi mini) 상세페이지 - 즐겨찾기 안내` `71417:17333` — 코멘트 2건
- XLT: **Unifi**(정본) · 프리픽스 `UF_`(**UIT** · `{{0}}`) + `mini_guidekim_`(**LV** · `{0}`) 혼재 — 키를 사용자가 직접 기재해 프리픽스로 팀 판별
- 엑셀: `xlt/xlt_clinic_screen_7keys_20260901.xlsx`(7키·**업로드 확인됨**) · `xlt/registry_fix/xlt_fix_main_faq_title_ja_1key_20260901.xlsx`(1키·**미반영**)
  - 2026-09-03: `xlt_output_20260903171314.xlsx`(8키) · `xlt_output_20260903175353.xlsx`(3키) · `xlt_output_20260903182643.xlsx`(2키) — **셋 다 업로드 대기(사용자)**. ⚠️ `xlt/`는 `.gitignore`라 위키 첨부·게이트 리포트가 정본
- Landpress 콘텐츠 JSON: `landpress/` **24파일** — 공통 4필드 × 5개 언어 + 병원별 3필드 × ko + **`voucher_ko_KR.json`**(추천 바우처 4종 · ko만)
- 게이트 리포트: `reports/gate/gate_report_clinic_*.md` · `gate_report_landpress_clinic_4sections_2026-09-01.md`
  - 2026-09-03: `gate_report_clinic_detail_improved_8keys_2026-09-03.md` · `gate_report_clinic_oa_save_improved_3keys_2026-09-03.md` · `gate_report_clinic_favorite_guide_2keys_2026-09-03.md`

## 현재 상태

위키 **v95**. 2026-09-03 세션에서 **개선 프레임 3개**를 반영해 Screen 표가 6행이 됐다 — `(Web) 브릿지 페이지` · `(Unifi mini) 상세페이지 - 개선` · `(Unifi mini) 상세페이지 - 즐겨찾기 안내` · `kpick_clinic_oa_save_01_03` · `kpick_clinic_oa_msg_save_01` · `kpick_clinic_oa_msg_consult_01`. 게이트 **7건 전부 P0=0** + `check_gate_report.py` exit 0 · `check_wiki_storage.py` pre/post exit 0.

누적 번역 **75키**(62 + 8 + 3 + 2) · UIT `UF_*`.

- **`(Unifi mini) 상세페이지 - 개선`**(`71241:9438`) — 코멘트 42건. 탭(병원정보·이용안내·FAQ) 구조·영수증 인증 배지·즐겨찾기 배너 신설. 신규 6키 + 문구 변경 2키. **기존 44행 × 5개 언어(220셀) 등재값 재대조 — 43행 일치**
- **`kpick_clinic_oa_save_01_03`**(`71339:15430`) — 친구추가 바텀시트 개선판. 3키(신규 1 + 값 변경 2). 기존 `_01_01`·`_01_02`는 보존
- **`(Unifi mini) 상세페이지 - 즐겨찾기 안내`**(`71417:17333`) — 코치마크 2키 신규
- **사용자가 세션 중 위키를 직접 여러 번 편집했다**(v89·v94). 구 `(Unifi mini) 상세페이지` 행과 OA 모달 4행을 삭제해 개선판으로 통합했고, `kpick_clinic_oa_msg_consult_01`은 실수로 지워져 **v91 원문에서 복원**했다

## 진행 중 작업(WIP)

없음.

## 다음 할 일

- [ ] **P0 XLT 업로드(사용자)** — 2026-09-03 신규·변경 **13키**(8+3+2). 쓰기 API 없음. 업로드 전까지 위키 값과 등재값이 갈린다
- [ ] **P1 `UF_main_faq_title` ja 1키 미반영** — 위키 `FAQ` ↔ 등재값 `よくある質問`. 2026-09-01 수정 엑셀이 업로드되지 않았다(같은 배치 7키는 반영 확인됨)
- [ ] **P1 Landpress 등록(사용자)** — `landpress/` 24파일. `voucher_ko_KR.json`은 **ko만** 있어 5개 언어 확장 시 이미지 로케일 분기 결정 필요(카드 아트가 일본어)
- [ ] **P1 `UF_clinic_detail_benefit_prefix` 구조** — `결제금액의` / `8% 캐시백 혜택`이 2개 텍스트로 분리돼 **en·th 조합 시 어순이 뒤집힌다**. 등재값 선례(`UF_clinic_bridge_card_cashback` = `결제금의 캐시백 {{0}}%`)처럼 **단일 키 병합** 권장 — FE·디자인 결정 필요
- [ ] **P1 용어집 보완 3건(실측 근거 확보)** — 혜택 en `perks`→**`benefit`**(29 vs 6) · 더보기 ja `すべて見る`→**`もっと見る`**(2 vs **0**) · 더보기 zh `查看更多`→**`更多`**(3 vs 1). 같은 오탐이 3회 반복. ⛔ 버전 상승 시 **기획자 가이드 zip 동반 갱신 필수**
- [ ] **P1 FAQ 답변 3건 기획 검토** — Figma에 답변이 1개만 있어 나머지 3건은 Claude가 정책 근거로 작성한 **샘플**. 확정 시 5개 언어 재생성
- [ ] **P1 「더보기」가 LINE 클라이언트 UI** — 코치마크 문구가 가리키는 것은 Unifi 요소가 아니라 LINE 앱 상단 우측 메뉴다. **LINE 자체 UI의 언어별 실제 표기** 확인 후 맞출지 결정
- [ ] **P1 CTA 「상담하기」 확인** — `kpick_clinic_oa_save_01_03`은 저장(찜) 플로우인데 CTA가 `LINE 친구 추가하고 상담하기`. 사용자가 Figma 그대로 확정했으나 「저장하기/링크 받기」 의도 여부 확인 권장
- [ ] **P1 주석 삭제 법무 확인** — 개선판에서 `UF_clinic_oa_save_note`(`*친구 추가는 캐시백 등 혜택 안내 목적으로만 이용돼요.`)가 사라졌다. 개인정보 이용 목적 고지 성격
- [ ] P2 **`menu.price` 구조 FE 협의** — `{amount, currency, from}` 채택 여부 · `from`이 예약어라 `is_from` 등 대안
- [ ] P2 **`voucher` JSON 미확정 3건** — ⓐ `price`(문자열)/`normalPriceJpy`(숫자) 비대칭 → `priceJpy` 추가로 완화했으나 통일 권장 ⓑ `targetUrl` 표기 불일치(`oliveyoung-` vs `olive-young-`) ⓒ `sortOrder` 채택 여부(현재는 배열 순서)
- [ ] P2 **병원별 3필드 다국어 확장** — `bridge_info`·`detail_info`·`menu`는 ko만. tag가 자유 텍스트라 병원 수 × 5개 언어 발생 → ⓐ 사람 입력 ⓑ ko 폴백 결정 필요
- [ ] P2 **`landpress/process`·`faq` 캐시백 15% ↔ 프레임 8%** — 사용자 결정으로 「8%는 이 프레임만」이라 미변경. 8% 확정 시 **2종 × 5개 언어** 갱신 필요. 위키 Policy에도 `20%` 2곳 잔존
- [ ] P2 **GA Event 미정의** — 개선판·바텀시트·코치마크 3행 모두 「미정의」로 표기. 스펙 확정 후 추가
- [ ] P2 **Figma 원본 정리 5건** — 우상단 제목 `올리브영 1만원권`(→3만원권) · 좌상단 카드 이미지(1만→5만원권 아트) · `다이소 5만원권`(→3만원권) · `라인 무료 상담`(→`LINE`) · `더보기 를`(→`더보기를`) · **프레임 경계 밖 럭키드로우 유의사항 12건 잔존 노드**
- [ ] P2 **코치마크 ↔ 배너 메시지 중복** — 즐겨찾기 배너와 코치마크가 같은 얘기를 한다. 중복 노출 여부·노출 조건(최초 1회 등) 정책 정의 권장
- [ ] P2 **코치마크 키 네임스페이스** — `UF_clinic_detail_favorite_guide_*`로 부여. mini 공통이면 `UF_mini_favorite_guide_*`가 맞다 — FE 협의
- [ ] P2 **`UF_clinic_oa_*` 키 전부 XLT 미등재 + 다국어 부재** — `_title`·`_desc`·`_desc_terms`·`_note`·`_btn`·`_agree_*`가 위키에만 있다. `_01_03` 외 기존 행들은 **5개 언어 번역 행이 없다**

## 주요 결정 사항 (이 프로젝트 한정)

| 날짜 | 결정 | 근거 / 커밋 |
|---|---|---|
| 2026-09-03 | **개선 프레임은 새 Screen 행으로 추가**(기존 행 보존) — 3프레임 모두 동일 적용 | 개선 전/후 대조 목적. 단 사용자가 이후 구 `(Unifi mini) 상세페이지` 행을 직접 삭제해 개선판으로 통합했다 |
| 2026-09-03 | **문구가 바뀐 기존 키는 값 변경**(신규 키 미생성) — `UF_voucher_mini_certified`·`UF_clinic_detail_benefit_sub`·`UF_clinic_oa_save_title`·`_desc` | 사용자 결정. 공유 키 파급을 감수. **단 `UF_clinic_oa_save_btn`은 예외** — `kpick_clinic_oa_save_01_02`(약관 미동의)와 공유해 CTA 의미가 갈리므로 신규 키 `UF_clinic_oa_save_btn_line` 부여 |
| 2026-09-03 | **캐시백 8%는 이 프레임에만 적용** — 위키 Policy(20% 2곳)·`landpress/process`·`faq`(15%) 미변경 | 사용자 결정. 8%는 법무 안전 범위(5~15%) 내 |
| 2026-09-03 | **`cancel_1~3`·`notice_1~3` 6키는 위키 XLT ↔ Landpress 병존 유지** | 사용자 결정. 상담·시술과정 8키 제거 선례를 따르지 않는다. **FE에 어느 쪽을 읽는지 명시 필요** |
| 2026-09-03 | **추천 바우처를 Landpress `voucher` 필드로 이관** — 고정 4종(올리브영 5만·3만 / 다이소 1만·3만) · 필드명 **camelCase**(상품 API와 1:1) | 카드 내부 라벨 3키(`_price`·`_origin_price`·`_cashback`)는 XLT 유지. 타이틀·더보기 라벨은 **XLT와 JSON 병존**(사용자 지시) |
| 2026-09-03 | **`더보기`·`혜택` 용어집 권장값 미채택** — 등재 실사용 실측 우선 | `すべて見る` **0건**(`md/check.md` 함정 2-1) · `benefit` 29 vs `perk` 6 · `更多` 3 vs `查看更多` 1 |
| 2026-09-03 | **곡선 따옴표 `‘즐겨찾기’` 유지** | 등재값 선례 2건(`UF_asset_purchase_price_edit_desc4`·`UF_boost_claim_complete_desc`)이 한국어 강조 인용에 `‘’`를 쓴다 |
| 2026-09-01 | **Landpress 필드 7종 확정** — 공통 `cautions`·`change_cancel`·`process`·`faq` / 병원별 `bridge_info`·`detail_info`·`menu`. 리스트형은 `{"items":[…]}`, 오브젝트형은 필드 직접 노출 | 병원 식별자는 **API의 `uid`** 로 처리하므로 JSON에 넣지 않는다 |
| 2026-09-01 | **`cautions`·`change_cancel`만 `title`을 JSON에 포함**, `process`·`faq`는 XLT 유지 | 타이틀 값은 등재값 `UF_voucher_detail_caution`·`_cancel` 5개 언어 재사용. 두 키는 Unifi에 살아 있어 **XLT와 JSON 양쪽 병존을 사용자가 승인** |
| 2026-09-01 | **`tag`는 양쪽 모두 배열** (`bridge_info`·`detail_info`) · 값은 **자유 텍스트** | 같은 이름이 타입이 갈리면 FE 사고. 고정 코드(enum) 제안은 반려 |
| 2026-09-01 | **캐시백률은 리터럴 `15%`** (치환자 미사용) | FE API 조달 불가 → **수기 텍스트 대응**으로 확정 |
| 2026-09-01 | **`mini_guidekim_cat_dematology` ko = `피부과·성형외과`** | Figma 표기 기준. 위키 `피부과`·등재값 `피부·성형외과`와 3중 불일치였다 |
| 2026-09-01 | **`UF_voucher_recommend_origin_price` = `정가 <span>{{0}}</span>`** — span이 **가격 변수만** 감싼다 | 첨부 화면에서 취소선이 `¥5,654`에만 걸려 있고 라벨엔 없다. 사용자 제안(`<span>정가 {{0}}</span>`)은 라벨까지 감싸 취소선이 번진다 |
| 2026-09-01 | **ja 「캐시백」은 `キャッシュバック`** (`還元` 미채택) | 등재값 실측 — 바우처 계열 **3/3**, Unifi 전체 **13:1**. `還元`은 guidekim mini 배지 2키뿐 |
| 2026-09-01 | **`mini_guidekim_cat_eye` th = `จักษุวิทยา`** | `จักษุ`는 단독 사용이 어려운 결합형. 형제 키(치과 `ทันตกรรม` 등)는 전부 완전한 명사 |
| 2026-09-01 | **엑셀 포함 기준 = 「Unifi 미등재」** | `mini_guidekim_cat_*` 3키는 Dapp Portal엔 있고 Unifi엔 없어 **7키 전량 포함** |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지 — 이 프로젝트 한정)

- **Figma 원문 수정 요청 8건**(구분자 `･`→`·` 5건 · `무료 LINE상담` 띄어쓰기 · `change_cancel` 문체·마침표 2건) — **Figma는 참고용**이라 원본을 고치지 않는다 (2026-09-01, 세션 #1)
- **등재값 U+FF65(`･`) 4건**(`UF_login` 등) · **Dapp Portal `mini_guidekim_cat_dematology` en `Surge` 잘림** — 그대로 둔다. Unifi 신규 등록분만 `Surgery`로 교정 (2026-09-01)
- **`UF_voucher_detail_caution`·`_cancel` 2키를 위키 XLT 표에서 제거하지 않는다** — 타이틀이 JSON으로 가도 XLT와 병존 (2026-09-01)
- **어노테이션 이미지 핀 3·5 색상(빨강)** — Figma 코멘트 본문이 `LPC로 관리`/`Admin에…`으로 시작해 파랑이 안 된다. 코멘트를 고치지 않으므로 그대로 (2026-09-01)
- **법무 준수 체크 섹션의 서식**(정렬 스타일·파란 텍스트) — 사용자 작성 영역이라 손대지 않는다 (2026-09-01)

## 세션 기록 (최신 위, 최대 5개)

### 2026-09-03 — 세션 #2: 개선 프레임 3개 반영 + 추천 바우처 Landpress 이관 (v86→v95)

- 완료: **Landpress `voucher` JSON 신설**(고정 4종 · 이미지 4장 실물 대조로 액면가 검증) · 개선 프레임 3개 위키 반영(Screen 3행 + 다국어 3섹션) · 게이트 3건 P0=0 · 삭제된 `kpick_clinic_oa_msg_consult_01` 행 v91에서 복원
- 교훈 ⓐ **사용자가 같은 페이지를 동시에 편집한다** — PUT 직전 라이브가 v88→v89로 바뀌어 있었고, 그대로 올리면 사용자 편집 4항목을 되돌렸을 것이다. **PUT 직전 버전 재확인 → 어긋나면 최신을 다시 받아 그 위에 재작성**을 3회 수행(v89·v94 기준 rebase). 「기준 버전 == 라이브」 가드를 PUT 스크립트에 넣었다
- 교훈 ⓑ **`<tr class="">`를 정규식이 놓친다** — 위키 에디터로 편집된 표는 `<tr class="">`가 되어 `r'</?tr>'`(정확 일치)로는 여는 태그가 안 잡히고 **행이 통째로 사라진 것처럼 오판**했다. `r'<tr[\s>]|</tr\s*>'`로 고쳐야 한다. 하마터면 「OA 행 6개 삭제됨」으로 잘못 보고할 뻔했다
- 교훈 ⓒ **오버레이 프레임은 코멘트 핀이 배경 텍스트로 오매칭된다** — 바텀시트·코치마크 2프레임에서 각 1건 발생(`B/E API - name`→배경 태그 / 코치마크 설명→배경 `4.9`). 두 bbox가 모두 핀을 포함해 좌표만으로는 못 가린다. **렌더 후 육안 검증 + 수동 교정**이 유일한 방어
- 교훈 ⓓ **정책 핀도 글자를 가린다** — 순수 정책 핀을 「핀 위치」에 찍으니 ③⑤④⑦이 글자를 덮었다. 정책 핀도 **매칭 텍스트 왼쪽(mx-10)** 으로 옮기고 같은 텍스트에 몰린 핀은 계단식으로 내리는 방식으로 해소
- 교훈 ⓔ **오탐이 반복되면 용어집을 고칠 신호다** — `혜택`→`perks` 오탐이 3세션 연속 나왔다. 게이트 (d-1)에 실측 건수를 붙여 보완 권장으로 정리했다
- 교훈 ⓕ **프레임 경계 밖 텍스트를 대상에 넣지 않는다** — 코치마크 프레임(높이 812)에 y2091~2626 럭키드로우 유의사항 12건이 붙어 있었다. `y >= frame.height` 필터로 걸러 오번역을 막았다

### 2026-09-01 — 세션 #1: 위키 신설 → Landpress 이관 → 서식 원복 (v1→v86 · `88d4074`)

- 완료: 위키 신규 생성(Jira 2건 기반) · 브릿지·상세·OA 3영역 반영 · **62키 번역**(54+1+7) · OA 2메시지 5개 언어 Flex JSON + zip · 일본어 현지인 검수 반영 · **Landpress 7필드 23파일** 신설 · 상담·시술 과정 XLT 8키 제거 + 번호 재산출 · Screen 표 서식 원복(하이라이트 7 · 회색 텍스트 2 · `<code title="">` 4 · 정렬 9)
- 교훈 ⓐ **위키 템플릿은 형제 1건만 보고 일반화하면 안 된다** — 형제 `4620240898` 하나만 확인해 Schedule 기반 구조를 썼고, 정본(`3751027285`)과 달라 재작성했다. `md/wiki.md`에 실측 오류 정정 반영
- 교훈 ⓑ **어노테이션 번호 규칙에 예외를 만들지 않는다** — "As-is/To-be 비교 그래픽이니 생략"은 규칙에 없는 자체 판단이었다. 코멘트가 있으면 전 프레임에 번호를 붙인다
- 교훈 ⓒ **변수화는 사용자 확인 후에만** — 브릿지 부제를 `최대 {{0}}%`로 임의 변수화했다가 Figma 리터럴 `최대 15%`로 되돌렸다
- 교훈 ⓓ **검수 반영은 생성 경로 전체에 넣는다** — 일본어 검수본을 엑셀에만 적용해 Landpress JSON이 검수 전 값(`change_cancel` ja 3건)으로 나갔고, 재생성 검증에서 잡았다. 패치는 **정본 데이터 구조**에 넣고 모든 산출물을 다시 뽑는다
- 교훈 ⓔ **셀 경계 파싱은 깊이 추적으로** — `<td>…</td>` 비탐욕 매칭이 중첩표에서 끊겨 **HTTP 400 3회**. `cell_end()`/`tr_bounds()` 깊이 추적 + PUT 직전 14태그 균형 assert로 고정
- 교훈 ⓕ **인라인 코멘트 마커를 보존한다** — Description 항목 앞에 텍스트를 덧붙일 때 `<li>` 여는 태그 직후에만 삽입하고, `ac:inline-comment-marker` 개수를 편집 전후로 비교한다

# glossary-guide — 용어집 + 기획자 가이드 (⚠️ 전역 자원 · 락 필요)

> 담당자: `hogeun` · 마지막 갱신: 2026-09-16 · 커밋 `a15b5bc` · 라이브 태그 **`guide-v43`**(2026-09-16 **Claude가 직접 게시** · `get_site`·라이브 `<title>` 대조 확인)
>
> ⛔ **2026-09-16 — 게시 주체가 바뀌었다: Claude가 직접 올린다**(사용자 결정 · 전역 결정 표). 업로드는 **REST `PUT /api/sites/ab3ffda0`**(MCP 토큰 · `file`+`name`)이며 **MCP `update_site`는 못 쓴다**(`file_path`가 DropWeb **서버** FS 경로 — 로컬 zip 절대·상대·URL 3형태 모두 「파일을 찾을 수 없습니다」). 검증은 MCP `get_site`(원본 파일명·수정일·배포 횟수) + 라이브 `<title>`. 정본 `md/dropweb-guide.md` §8
>
> 🔄 **2026-09-15 실측 재확인** — 용어집 API **v5.5**(118 terms · `last_updated` 2026-09-12) · 최신 태그 **`guide-v42`** · `dropweb/` zip **v42까지** · `md/glossary-backlog.md` 대기 **21건**(2026-09-15). 이때 「대상/링크」의 가이드 표기가 **v39**, 「현재 상태」가 **용어집 v4.9 / 가이드 v33**으로 낡아 있어 **실측값으로 덮어썼다**.
>
> ⛔ **2026-09-15 — 기획자 가이드 자체도 「모아서 한 번에」로 전환**(사용자 결정). 푸시할 때마다 「가이드를 업데이트할까요?」를 **묻지 않는다** — `md/guide-backlog.md`에 **등재만** 하고, 사용자가 「가이드 업데이트」를 요청한 시점에 대기 전건을 제시해 일괄 반영한다. 용어집은 예외가 아니라 **같은 흐름**이다(용어집 반영 작업에서 가이드 zip을 함께 갱신 — 「필수 동반」은 유효). 기준선 **`guide-v42`** 이후 미반영 **6건 등재**(LPC 쓰기 규칙 · `md/decisions.md` 신설 · glossary-backlog 18→21건 · 신규 스크립트 2종 · OA 3-1 ⓖ 강화 · OA 발송 시간 제한).
> 📌 **v42 — 용어집 보완을 「모아서 한 번에」로 전환**(`md/glossary-backlog.md` 신설 · `e92eac7`). 발견 즉시 여쭙던 것을 대기 목록에 쌓아 두었다가 「용어집 리뷰」 요청 시 일괄 검토한다. 가이드 5곳 갱신(용어집 섹션 흐름 6단계 · 문서 지도 3곳 · 이력 · 작업 모드 카드 · 요청 예시). **대기 8건**(→ 2026-09-15 실측 **18건**).
>
> ⚠️ **v41은 라이브 게시 없이 v42에 흡수됐다** — `guide-v41` 태그는 없다(IA 주간 점검 #7 내용은 v42 zip에 포함).
> ⚠️ **v5.2~v5.5 · 가이드 v34~v38은 이 파일에 기록되지 않은 다른 세션들이 했다** — 상세는 `md/glossary-changelog.md`(용어집)와 `guide/index.html` 업데이트 이력 탭이 정본이다. 아래 v5.0·v5.1 서술은 그 시점 기록이며 최신 상태는 아니다
> ⚠️ 이 자원은 **모든 프로젝트가 공유**한다. 편집 전 `handoff/lanes/`에 락 파일을 만들 것(`handoff/lanes/README.md`).

## 대상 / 링크

- **용어집**: Landpress `web3_xlt_json` — 라이브 **v5.5** · **118 terms · 11 exceptions · 22 deprecated · 2 oa_variables**(2026-09-15 API 실조회 재확인 · `last_updated` 2026-09-12)
  - 갱신 방식은 **사용자에게 묻는다** — ① Claude가 CMS API로 직접 PUT ② 사용자가 전체 JSON 붙여넣기(`md/landpress.md` §10-5)
  - **버전별 변경 이력은 `md/glossary-changelog.md`가 정본** · 절차는 `md/landpress.md`
  - ⚠️ **4.8은 재사용 금지 번호** — 2026-08-12 시도 후 revert(팀 합의 미도출). 다음은 그 번호를 건너뛴다
  - 커밋된 산출 JSON: `reports/glossary_v5.0_full.json`·`v5.1_full.json`·`v4.7_for_cms.json`(라이브 동치 확인분)
- **기획자 가이드**: 라이브 **v43**(태그 **`guide-v43`** · 2026-09-16 게시 — `dropweb/web3_planning_v43.zip` · `guide/index.html` 내부 표기 v43 · DropWeb site `ab3ffda0` 배포 11회차)
  - ⛔ **소스 정본은 git 추적 `guide/`** — zip은 게시용 빌드 산출물이다(직접 편집 금지)
  - 최신 판별 = `git log -- guide/` · **라이브 표식 = `guide-v*` 태그**(사용자 게시 확인 후 부여)
  - 절차: `md/landpress.md` §5-1 0단계 · 갱신 대상 5곳은 CLAUDE.md 「기획자 가이드 최신화」
  - ⚠️ **v27은 철회됨**(용어집 v4.8 revert에 연동)
- 에이전트: `glossary-guide-updater`(용어집 버전 상승 시 가이드 용어집 탭 갱신)

## 필수 동반 규칙

**용어집 버전이 오르면 같은 작업에서 가이드 zip도 갱신·전달한다**(승인 불필요 — CLAUDE.md 예외 조항). 사용자가 **가이드의 용어집 탭에서 전체 JSON을 복사해 CMS에 붙여넣는 것이 반영 창구**다. 갱신 대상 4곳(이력 표·전체 용어 표·수치 표기·임베드 JSON) + **본문 하드코딩**(히어로 chip 등 — 세션 #18에 v20까지 방치된 실측) → `md/landpress.md` §5-1. **게시는 Claude가 직접 수행**(2026-09-16 변경 · 승인 후 REST · `md/dropweb-guide.md` §8).

## 현재 상태

> **2026-09-15 실측 기준.** 과거 버전별 서술은 누적하지 않는다 — **용어집 이력은 `md/glossary-changelog.md`, 가이드 이력은 `guide/index.html` 업데이트 이력 탭이 정본**이다.

| 항목 | 라이브 | 근거 |
|---|---|---|
| 용어집 | **v5.5** · 118 terms · 11 exceptions · 22 deprecated · 2 oa_variables | `fetch_glossary.py` API 실조회(2026-09-15) · `last_updated` 2026-09-12 |
| 기획자 가이드 | **v43** (태그 `guide-v43` · 2026-09-16 **Claude 직접 게시**) | `git tag -l 'guide-*'` 최신 · `dropweb/web3_planning_v43.zip` · 라이브 `<title>` v43 · `get_site` 원본 파일명 |
| 용어집 보완 대기 | **18건** | `md/glossary-backlog.md` |

- **보완은 「모아서 한 번에」다**(2026-09-14 사용자 결정) — 게이트 (d-1)에서 발견하면 **대기 목록에 등재만** 하고 그 자리에서 반영 여부를 묻지 않는다. 사용자가 **「용어집 리뷰」를 요청한 시점**에 전건을 제시해 일괄 결정한다
- ⚠️ **v41은 라이브 게시 없이 v42에 흡수**됐다 — `guide-v41` 태그는 없고 `dropweb/web3_planning_v41.zip`만 남아 있다
- ⚠️ **v5.2~v5.5 · 가이드 v34~v42는 이 파일에 기록되지 않은 다른 세션들이 수행**했다. 세부 경위가 필요하면 위 두 정본과 `reports/gate/`를 본다

## 진행 중 작업(WIP)

없음(미확인 — 위 revert의 소관 세션이 HANDOFF를 갱신하지 않았다).

## 다음 할 일

> 완료·종결 항목은 남기지 않는다 — **용어집 이력은 `md/glossary-changelog.md`, 가이드 이력은 `guide/index.html` 이력 탭, 종결은 아래 「⛔ 종결」이 정본**이다(2026-09-15 정리: 완료된 `[x]` 8건 제거).

- [ ] **P1 — `UF_clinic_detail_review_source` ja `口コミ` → `レビュー` 검토**: v4.9의 「리뷰」 등재로 드러난 **실제 일관성 위반 1건**. 실측 ja `レビュー` **8건** vs `口コミ` **1건**(이 키뿐). 같은 문맥의 `UF_clinic_mini_bridge_section_review_note`는 `レビュー`를 쓴다 — clinic 위키·XLT 재업로드 대상에 넣을지 결정 필요
- [ ] **P2 — 용어집 보완 대기 18건 일괄 리뷰**: `md/glossary-backlog.md`. ⛔ **발견 시점에 묻지 않고 모아서 한 번에**(2026-09-14 결정) — 사용자가 「용어집 리뷰」를 요청하면 **전건을 제시**한다. 값이 `실측 필요`로 비어 있는 항목은 `fetch_xlt_registry.py`로 **실사용 건수를 먼저 실측**한다(추측 금지 · 실사용 0건 표기 권장 금지)
- [ ] **P3 — 용어집 버전이 오르면 가이드 zip 동반 갱신**(승인 불필요 · 위 「필수 동반 규칙」). 게시는 **Claude가 직접**(사용자 승인 후 REST) 하고, `get_site` 대조 후 `git tag guide-vN`

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-10 | **가이드 소스를 git 추적 `guide/`로 전환** — zip은 빌드 산출물, 게시본은 `guide-vN` 태그 | 병렬 세션이 같은 파일명 zip을 **무음으로 덮어써** 용어집 v4.7 반영본이 유실됐다. git 안에서는 같은 상황이 **push 충돌로 드러나** 두 변경이 합쳐진다. 최신 판별도 zip 3값 포렌식 → `git log` 한 줄로 단순화. 커밋 `7d1bd3d`·`3075eb6`·`b84bfe5` |
| 2026-08-05 | **용어집에 `oa_variables` 영역 신설**(4번째 최상위 키) · `{{amount}}`→**`{{total_amount}}`** | OA는 XLT 키가 없어 **변수명이 정본으로 남지 않았고** 캠페인마다 이름을 다시 정하는 왕복이 반복됐다. 등재하면 문의 없이 재사용. `validate_translation.py`가 `terminology`/`exceptions`만 읽어 신규 키는 무해(실측). 커밋 `21fc1fa` |
| 2026-08-14 | **용어집은 교체하지 않고 우선 그대로 둔다**(사용자 지시) | 게이트 3건에서 보완 권장이 올라왔지만 v4.7 유지. `채우기` 건은 시스템 등록값 정리까지 묶이는 사안이라 단독 교체가 위험하다 |
| 2026-08-05 | **용어집 등재 판정은 추정이 아니라 A/B 실측으로** | 2차 검토가 `캠페인` 등재를 "+3 신규 P1"로 추정해 보류를 권고했으나 **실측 0건**. 검증기는 **ko에 그 용어가 든 행만** 검사한다. 커밋 `fe67b80` |
| 2026-08-04 | **`채널 팔로우`·`팔로우` → `공식 계정 친구 추가`**(5개 언어 정본) · 용어집 등재는 보류 | 실측 일치율 32%로 `md/landpress.md` §4-3 미충족 → 규칙 문서로 강제 후 시스템 정리·재측정. 커밋 `4b186d6` |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지)

- **게이트에서 올라온 용어집 보완 권장 5건 종결**(2026-08-20 사용자 지시 「남은 건 폐기 처리」) — `이자 내역` 등재 · `프로모션` th 정본(`โปรโมชั่น`/`โปรโมชัน`) · **`채우기` 4개 언어 교체**(등재값 `Receive` 계열이 실사용과 어긋남) · `대표 지갑` 등재 · `전송`/`송금` 정리. **용어집은 v4.7 그대로 유지하며 이 5건을 재제안하지 않는다.** 근거·권장값은 각 게이트 리포트(`apppush_permission_desc` · `ms_popup_twa_policy` · `kaiawallet_unifi_transition`)에 보존돼 있어 필요할 때만 참조한다
  - ⚠️ 남는 사실: `채우기` 관련 시스템 등록값 불일치(`UF_asset_token_detail_btn_deposit`·`UF_asset_token_list_btn_topup`는 `Receive` 계열, `UF_interest_main_accumulated_reward_btn`은 ja만 `チャージする`)는 **여전히 존재**한다. 종결은 「지금 고치지 않는다」이며 「불일치가 없다」가 아니다

- **`선물` 용어집 등재 보류**(2026-07-30) — `선물하기`(v3.6)로 커버 + zh·th 명사/동사 분기
- **가이드 용어집 검증 버튼 현행 유지**(2026-07-30) — 드랍웹이 정적 호스팅이고 용어집 API가 CORS 헤더를 주지 않아 자동 대조 불가, 수동 폴백 동작. **사용자 결정**

## 세션 기록

### 2026-09-16 — 가이드 v43 게시(대기 7건 전건 반영) + 드랍웹 직접 게시 전환 (`a15b5bc`)

- **v42 → v43 · 대기 7건 전건 반영** — ① LPC 쓰기 규칙(차단 게이트 **4대→5대**) ② `md/decisions.md` ③ 용어집 대기 23건 ④ 스크립트 2종 ⑤ OA LIAM 스냅샷 ⑥ OA 발송 시간(22~08시 · 확정 T0 예외) ⑦ `put_wiki_storage.py`. **5곳 갱신 규칙 전부 충족** — 네비는 기존 「LPC 반영」에 흡수해 **신규 항목 0**(2줄 접힘 위험 회피)
- **게시 주체 전환** — 사용자 결정으로 Claude가 직접 게시. **MCP `update_site` 실패 → REST PUT로 성공**(위 헤더). ⚠️ REST에는 `change_summary`가 없어 **배포 이력에 변경 요약이 안 남는다** — 커밋·`guide-backlog` 반영 이력이 대체
- **검증** — 태그 균형 15종 일치 · 임베드 용어집 JSON 파싱 OK(v5.5 · 118용어) · headless 1920px 헤더 **한 줄** · 라이브 `<title>` v43
- ⚠️ **교훈 — 「MCP가 있다」가 「파일을 올릴 수 있다」는 뜻은 아니다.** 원격 HTTP MCP는 클라이언트 PC의 파일을 읽지 못한다. 도구 설명(「서버가 파일을 직접 읽어 배포」)만 보고 가능하다고 적었다가 실패했고, **실측 후 규칙 4개 문서를 정정**했다
- ⚠️ **락 없이 전역 자원을 만졌다** — `guide/`·`md/**`·`dropweb/*.zip`은 락 대상인데 `handoff/lanes/`에 파일을 만들지 않았다. 병렬 세션이 있었으나(그 세션은 `handoff/`·`reports/`만) 충돌은 없었다. 다음 가이드 작업은 **락 먼저**

### 2026-09-03 — GA Event 안내 v32→v33 (`7e8a9ea`·`ff46985`·`a364c84`)

- v32: 이력 카드 2·작업 모드 카드·요청 예시 4 → 게시 후 **사용자 지적**: 본문 섹션과 시스템 가이드 문서 지도가 없다 → v33: `#ga` 전용 섹션(규칙 카드·표 예시·확인 3장·제외 노트) + 문서 지도 3곳 + 네비 2곳. **교훈: 새 규칙 문서는 5곳을 한 번에**(CLAUDE.md 3-ⓐ에 체크리스트로 승격) · 정적 스냅숏(`file://`)에서는 탭 JS가 안 돌아 find/read_page로 DOM만 확인했다

### 2026-08-06 ~ 08-20 — 용어집 v4.4~v4.7 · 가이드 git 관리 체계 전환 (`081c725`·`7d1bd3d`~`80097c8`)

- **가이드 소스를 git으로 전환** — zip 포렌식(푸터·임베드·mtime 3값 대조)을 폐지하고 `guide/`를 정본으로. **베이스 오판 사고**(v4.zip mtime이 최신이라 실제 최신 v6.zip 대신 베이스로 삼음)가 계기
- 용어집 v4.4·v4.7 반영 · 3계층 전환에 맞춰 가이드 v28 갱신
- **교훈**: **zip은 git 밖이라 덮어써도 무음이다** — 병렬 세션이 같은 파일명으로 만들면 복구가 불가능하다(2026-08-10 실측, v4.7 반영본 유실 → v25로 재적용). 「쓰기 직전 최신성 재확인」 규칙의 계기

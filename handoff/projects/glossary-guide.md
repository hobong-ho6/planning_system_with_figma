# glossary-guide — 용어집 + 기획자 가이드 (⚠️ 전역 자원 · 락 필요)

> 담당자: `hogeun` · 마지막 갱신: 2026-09-04 · 커밋 `(이 커밋)` · 라이브 태그 **`guide-v33.3`**
> ⚠️ 이 자원은 **모든 프로젝트가 공유**한다. 편집 전 `handoff/lanes/`에 락 파일을 만들 것(`handoff/lanes/README.md`).

## 대상 / 링크

- 용어집: Landpress `web3_xlt_json` — **API 읽기 전용**, 갱신은 전체 JSON을 **사용자가 CMS에 붙여넣기**
  - 라이브 **v5.1**(2026-09-04 사용자 CMS 반영 완료 · **API 재조회로 산출 JSON과 완전 동치 확인**) — **117 terms · 11 exceptions · 20 deprecated · 2 OA variables**
  - **v5.0**: 캐시백 ja `キャッシュバック`→**`還元`** + deprecated #20 신설. XLT **17키 재업로드 동반**. A/B(2,461키): 용어집만 P1 2350→2383(+33) · **17키 업로드 후 2349(−1)** · P0·P2 불변. ⚠️ 등재 실사용은 `キャッシュバック` 17 : `還元` 1이었고 **실측 다수를 사용자 결정으로 뒤집은 건**이다
  - **v5.1**: 실사용 **0건** 등재값 2건 교정 — 가격 zh `售價`→**`價格`**(실측 9:0) · 유의사항 th `หมายเหตุ`→**`ข้อควรระวัง`**(2:0). A/B **P1 2350→2339(−11)** · 신규 P1 0
  - 정본 JSON: `reports/glossary_v5.0_full.json` · `reports/glossary_v5.1_full.json`
  - ⚠️ **4.8은 재사용 금지 번호** — 2026-08-12 시도 후 revert(팀 합의 미도출). 그래서 4.7 다음이 **4.9**다
  - 이력 정본: `md/glossary-changelog.md` · 절차 정본: `md/landpress.md`
  - 커밋된 산출 JSON: `reports/glossary_v4.7_for_cms.json`(라이브 반영본과 동치 확인)
- 기획자 가이드: 라이브 **v33**(2026-09-03 게시 완료, 태그 **`guide-v33`** · `guide-v32`도 태그됨 · **v31은 태그 없음** — zip만 발행·소스 미커밋이라 게시 여부 미확인)
  - ⚠️ **v27은 철회됨** — 용어집 v4.8 커밋(`39303c1`)이 revert(`3a5a4a3`)돼 유효하지 않다. iMac에 다른 내용의 v27 zip이 남아 있을 수 있어 **v28로 건너뛰었다**(사용자 결정 2026-08-20)
  - ⛔ **소스 정본은 git 추적 `guide/`** (2026-08-10 신설 — 커밋 `7d1bd3d`·`3075eb6`). zip은 게시용 빌드 산출물(`dropweb/`은 `.gitignore` — **`git add` 금지**)
  - 최신 판별 = `git log -- guide/` · 라이브 표식 = **`guide-v*` 태그**(사용자 게시 확인 후 `git tag guide-vN`) · 다음 버전 = 내부 버전 +1
  - 절차: `md/landpress.md` §5-1 0단계(git 기반)
  - 버전 연혁: **v33=GA Event 전용 섹션+시스템 가이드 문서 지도 3곳에 `md/GA.md`** · **v32=GA Event 기능 안내(이력 카드·모드·예시)** · **v31=IA #5(소스 누락 → `b49b1a1` 복원)** · **v30=「사용자 설정」에 AI 자동 판별 안내(질문 최소화)** · **v29=문서 지도에 인수인계 3계층 반영(+⑦ 중복 번호 ⑦/⑧ 정정)** · **v28=「사용자 설정」 섹션 신설** · ~~v27 철회~~ · v26=⑦ 가이드 버전 관리 탭 · v25=v24+용어집 v4.7 병합 · v22~v24=IA #4 3회 발행 · v21=용어집 v4.6 · v20=「🧭 시스템 가이드」 탭
- 에이전트: `glossary-guide-updater`(용어집 버전 상승 시 가이드 용어집 탭 갱신)

## 필수 동반 규칙

**용어집 버전이 오르면 같은 작업에서 가이드 zip도 갱신·전달한다**(승인 불필요 — CLAUDE.md 예외 조항). 사용자가 **가이드의 용어집 탭에서 전체 JSON을 복사해 CMS에 붙여넣는 것이 반영 창구**다. 갱신 대상 4곳(이력 표·전체 용어 표·수치 표기·임베드 JSON) + **본문 하드코딩**(히어로 chip 등 — 세션 #18에 v20까지 방치된 실측) → `md/landpress.md` §5-1. **게시는 사용자가 수행.**

## 현재 상태

- **용어집 라이브 = v4.9**(2026-09-04 반영 완료 · API 동치 확인). 가이드 v33 zip 임베드 JSON도 동일본(에이전트 검증 8종 PASS)
- **반영 효과 실측**: 등재값 전체 2,461키 **P1 2,437 → 2,357(−80)** · P0 102·P2 534 불변. 클리닉 통합 86키 **P1 59 → 55** · 7키 재업로드 엑셀 **P1 4 → 1 · P2 2 → 0**
- **v4.9 근거**: `LINE MINI App`은 LINE Developers **공식 문서 1차 출처**로 확정(`Mini App` 미사용) · terminology 5건은 **등재 실사용 실측**(교환 ja 48:5 · 혜택 en 18:6 · 결제 zh 26:3 등) · **교환 en `swap`은 반려**(`exchange`가 A/B P1 +24 악화)
- **v4.8은 철회됐다 — 팀 내 합의 미도출**(사용자 확인 2026-08-20). `39303c1`(「마켓플레이스」 등재 + 「NEXT Bay」 예외) → `3a5a4a3` revert. ⚠️ **재추진은 팀 합의 후에만** — 합의 없이 같은 등재를 재제안하지 않는다. 상세 `md/glossary-changelog.md` v4.8
- **가이드 라이브 = v33**(2026-09-03 게시 완료, 태그 `guide-v33`). ⚠️ **v31은 `guide/`에 커밋되지 않은 채 zip으로만 발행**됐었다(ia-monitor 세션 #22) — 09-03 GA 세션이 zip을 소스로 복원(`b49b1a1`)한 뒤 v32·v33을 쌓았다. **zip을 만들었으면 같은 커밋에 `guide/`를 넣는다**(§5-1 0단계 4항)

## 진행 중 작업(WIP)

없음(미확인 — 위 revert의 소관 세션이 HANDOFF를 갱신하지 않았다).

## 다음 할 일

- [x] ~~용어집 v4.9 CMS 반영~~ — **2026-09-04 완료**(API 재조회 완전 동치)
- [ ] **P1 `UF_clinic_detail_review_source` ja `口コミ` → `レビュー` 검토** — v4.9의 `리뷰` 등재로 드러난 **실제 일관성 위반 1건**. 실측 ja `レビュー` **8건** vs `口コミ` **1건**(이 키뿐). clinic 위키·XLT 재업로드 대상에 포함할지 결정 필요(같은 문맥의 `UF_clinic_mini_bridge_section_review_note`는 `レビュー`를 쓴다)
- [ ] 🔴 **P0 가이드 v33 zip 재게시(사용자)** — `dropweb/web3_planning_v33.zip`(2,903,843 bytes · 임베드 v4.9). **같은 버전 번호로 내용만 교체**했다(§5-1 「용어집 갱신만이면 현행 버전 갱신」) — 게시 확인 후 태그는 이미 `guide-v33`이 있어 **재태그 불필요**
- [ ] P1 **가이드 업데이트 이력 탭 미기재** — 과거엔 용어집 갱신마다 가이드 버전을 올려 이력 카드를 남겼다(v25=용어집 v4.7 · v21=v4.6). 이번엔 v33 유지라 카드가 없다 — 관례를 바꿀지 결정 필요

- (완료) v30 게시 → 태그 `guide-v30` 기록
- (완료) v29 게시 → 태그 `guide-v29` 기록
- 용어집 관련 대기 항목 없음 — 2026-08-20 사용자 지시로 보완 권장 5건 종결(아래 ⛔).

- (완료) v28 게시 → 태그 `guide-v28` 기록
- (해소) v4.8 revert 경위 = 팀 내 합의 미도출 → `md/glossary-changelog.md` v4.8에 철회 기록
- (종결) 용어집 보완 2건(`확인` 다의어 · `・` 구분자) — 2026-08-10 미착수 종결
- (종결) 가이드 이력 탭 접이식 — 2026-08-10 미착수 종결

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-10 | **가이드 소스를 git 추적 `guide/`로 전환** — zip은 빌드 산출물, 게시본은 `guide-vN` 태그 | 병렬 세션이 같은 파일명 zip을 **무음으로 덮어써** 용어집 v4.7 반영본이 유실됐다. git 안에서는 같은 상황이 **push 충돌로 드러나** 두 변경이 합쳐진다. 최신 판별도 zip 3값 포렌식 → `git log` 한 줄로 단순화. 커밋 `7d1bd3d`·`3075eb6`·`b84bfe5` |
| 2026-08-05 | **용어집에 `oa_variables` 영역 신설**(4번째 최상위 키) · `{{amount}}`→**`{{total_amount}}`** | OA는 XLT 키가 없어 **변수명이 정본으로 남지 않았고** 캠페인마다 이름을 다시 정하는 왕복이 반복됐다. 등재하면 문의 없이 재사용. `validate_translation.py`가 `terminology`/`exceptions`만 읽어 신규 키는 무해(실측). 커밋 `21fc1fa` |
| 2026-08-14 | **용어집은 교체하지 않고 우선 그대로 둔다**(사용자 지시) | 게이트 3건에서 보완 권장이 올라왔지만 v4.7 유지. `채우기` 건은 시스템 등록값 정리까지 묶이는 사안이라 단독 교체가 위험하다 |
| 2026-08-05 | **용어집 등재 판정은 추정이 아니라 A/B 실측으로** | 2차 검토가 `캠페인` 등재를 "+3 신규 P1"로 추정해 보류를 권고했으나 **실측 0건**. 검증기는 **ko에 그 용어가 든 행만** 검사한다. 커밋 `fe67b80` |
| 2026-08-04 | **`채널 팔로우`·`팔로우` → `공식 계정 친구 추가`**(5개 언어 정본) · 용어집 등재는 보류 | 실측 일치율 32%로 `md/landpress.md` §4-3 미충족 → 규칙 문서로 강제 후 시스템 정리·재측정. 커밋 `4b186d6` |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지)

- **게이트에서 올라온 용어집 보완 권장 5건 종결**(2026-08-20 사용자 지시 「남은 건 폐기 처리」) — `이자 내역` 등재 · `프로모션` th 정본(`โปรโมชั่น`/`โปรโมชัน`) · **`채우기` 4개 언어 교체**(등재값 `Receive` 계열이 실사용과 어긋남) · `대표 지갑` 등재 · `전송`/`송금` 정리. **용어집은 v4.7 그대로 유지하며 이 5건을 재제안하지 않는다.** 근거·권장값은 각 게이트 리포트(`apppush_permission_desc` · `ms_popup_twa_policy` · `kaiawallet_unifi_transition`)에 보존돼 있어 필요할 때만 참조한다
  - ⚠️ 남는 사실: `채우기` 관련 시스템 등록값 불일치(`UF_asset_token_detail_btn_deposit`·`UF_asset_token_list_btn_topup`는 `Receive` 계열, `UF_interest_main_accumulated_reward_btn`은 ja만 `チャージする`)는 **여전히 존재**한다. 종결은 「지금 고치지 않는다」이며 「불일치가 없다」가 아니다

- **`선물` 용어집 등재 보류**(2026-07-30) — `선물하기`(v3.6)로 커버 + zh·th 명사/동사 분기
- **가이드 용어집 검증 버튼 현행 유지**(2026-07-30) — 드랍웹이 정적 호스팅이고 용어집 API가 CORS 헤더를 주지 않아 자동 대조 불가, 수동 폴백 동작. **사용자 결정**

## 세션 기록

### 2026-09-03 — GA Event 안내 v32→v33 (`7e8a9ea`·`ff46985`·`a364c84`)

- v32: 이력 카드 2·작업 모드 카드·요청 예시 4 → 게시 후 **사용자 지적**: 본문 섹션과 시스템 가이드 문서 지도가 없다 → v33: `#ga` 전용 섹션(규칙 카드·표 예시·확인 3장·제외 노트) + 문서 지도 3곳 + 네비 2곳. **교훈: 새 규칙 문서는 5곳을 한 번에**(CLAUDE.md 3-ⓐ에 체크리스트로 승격) · 정적 스냅숏(`file://`)에서는 탭 JS가 안 돌아 find/read_page로 DOM만 확인했다

### 2026-08-20 — 핸드오프 3계층 전환에 따른 가이드 v28 (게시 대기)

- 「프로젝트 받기·업데이트」 **다음에 「사용자 설정」 섹션 신설**(`#whoami`) — `user.name`·`user.email`·`handoff.person` 3줄, PC마다 1회. 컴퓨터 이름으로 사람을 구분할 수 없다는 근거 포함
- 내비게이션: 데스크톱 라벨 `설치` → `설치·설정`(항목 수 유지 — flex-wrap 없어 오버플로 위험), 모바일에 `사용자 설정` 항목 추가
- 검증: 태그 균형 OK · 섹션 순서 `clone < whoami < tokens` · 깨진 앵커 0 · 버전 4곳 v28 · 임베드 용어집 JSON 파싱 OK(v4.7·116 terms·14 deprecated) · 전체 용어 표 117행 = 116+헤더
- ⚠️ **브라우저 육안 확인 미수행** — 이 세션에서 localhost 내비게이션이 정책으로 차단됐다(구조 검증으로 대체). 게시 전 사용자 육안 확인 권장

### 2026-08-10 — 세션 #20(용어집·가이드 분): v4.7 + 가이드 git 관리 체계 (`7d1bd3d`·`3075eb6`·`b84bfe5`·`6e40091`·`80097c8`)

- **용어집 v4.7** — A/B 3개 모집단 실측 **감소만·신규 0**. 사용자 CMS 반영 후 API 재조회로 동치 확인
- **`guide/` git 관리 전환** — `guide/`는 2026-07-27 v1 시절 커밋 후 **23개 버전 방치**돼 있었다. v24로 교체해 베이스라인 커밋
- **v25 병합본** — 세션 #21의 덮어쓰기로 유실된 v4.7 반영분을 커밋된 원본 JSON으로 v24 베이스에 **15곳 재적용**(검증: 임베드 JSON 동치·용어 표 117행·태그 균형·IA #4 내용 보존 + 브라우저 육안) → 게시 `guide-v25`
- **절차 개정** — `md/landpress.md` §5-1 0단계를 zip 포렌식 → `git pull` 후 `guide/` HEAD로 전면 교체(+ 에이전트 동일 반영). **v26** = ⑦ 가이드 버전 관리 탭 신설 → `guide-v26`
- **배운 것**
  - **등재값이 실사용 0건이면 등재값이 틀린 것이다** — `點數活動`은 3,730키 어디에도 없었다. 「번역이 용어집을 안 지킨다」가 아니라 「용어집이 현실을 모른다」였다
  - **산출물이 git 밖에 있으면 덮어쓰기가 무음이다** — git 안으로 들이면 **push 충돌로 가시화**되고 늦게 push하는 쪽이 합치는 역할을 맡는다. 반대로 **추적만 하고 절차가 없으면 낡는다**(23개 버전 방치)
  - **검사 코드의 앵커도 틀릴 수 있다**(세션 #18) — `구 표기 금지 목록` 문자열이 업데이트 이력 탭에도 있어 `find()`가 다른 표를 잡아 가이드 검증 ④가 오탐 실패했다. 앵커에 버전을 붙여 해결
  - **정본은 재생성 가능한 형태로 커밋해 둘 것** — 유실된 v23을 15분 만에 복원한 것은 `reports/glossary_v4.7_for_cms.json`이 커밋돼 있었기 때문이다
  - **가이드 본문의 하드코딩은 조용히 낡는다** — 히어로 chip이 `112 용어 (v3.9)`로 v20까지 방치(라이브 v4.5). §5-1 갱신 4곳은 지켰는데 「본문 하드코딩」이 빠져서다. **브라우저 육안 확인이 이걸 잡았다**

### 2026-08-06 — 세션 #12: 용어집 v4.4 (`081c725`, 병렬 세션·HANDOFF 미갱신)

- `deprecated_terms` 신규 2종(ja `友達`→`友だち` · ko `공식계정`→`공식 계정`) + **`context` 필드 신설**로 오탐 8건 제거(zh `關注`/`追蹤`·th `ติดตาม`·en `follow`는 같은 셀에 지정 문자열이 있을 때만 판정)
- 계기는 사용자가 제공한 XLT export 전수 검토 — v4.3 목록 21건 중 8건이 오탐이고 잡아야 할 2종이 누락돼 있었다. 상세 `md/glossary-changelog.md` v4.4

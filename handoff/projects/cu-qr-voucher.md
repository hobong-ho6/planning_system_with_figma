# cu-qr-voucher — CU 상품권 전용 동선 (QR 특가 · Unifi mini)

> 담당자: `hogeun` · 마지막 갱신: 2026-09-22 · 세션 #2 · 마지막 커밋 `e7be615` · 위키 **v4**

## 대상 / 링크

- 위키: [pageId 4774235795](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4774235795) `CU 상품권 전용 동선` — **현재 v4** (부모 `[Hogeun]` 3910828993 · 2026-09-21 생성)
- Figma: `Web3` `GOCHAYBS7hIrmWRGNuJOKV`
  - `CU QR Flow` `75611:4511` — 페이지 생성 시 참조(세션 #1)
  - **`화면정의` `75648:8390`** — 직속 3프레임: `Frame 2085674810`(팝업 · 내부 `75648:8233`) · `결제 페이지 - 바텀시트` `75648:8250` · `voucher Detail` `75648:7935`
  - **`CU Detail` `75685:2394`** — `voucher Detail`을 감싼 새 부모(2026-09-21 오후 갱신 · 노드 id는 그대로 `75648:7935`)
- Screen ID(사용자 승인 2026-09-21): `voucher_cu_detail_qr_01`(상세) · `voucher_cu_detail_qr_01_02`(쿠폰 사용 팝업) · `voucher_cu_payment_done_01_01`(결제 완료 바텀시트)
- XLT: **LV** · 프리픽스 `mini_` · 치환자 `{0}`(현재 키 전부 변수 없음) — ⚠️ **결제 바텀시트는 팀 판정이 갈린다(아래 P0)**
- 엑셀: `xlt/xlt_output_20260921223728.xlsx`(7키) — 위키 첨부 정본. 파일명 **고정 아님**(갱신 시 새 파일 첨부 + 본문 링크 교체 + 구버전 삭제)
- 게이트 리포트: `reports/gate/gate_report_cu_qr_voucher_2026-09-21.md`(세션 #2 · 1차+2차) · `reports/gate/gate_report_cu_qr_flow_2026-09-21.md`(세션 #1)

## 현재 상태

**위키 v4까지 반영 완료 · Screen 3화면 · XLT 7키 · GA Event 11건(view 3 · click 8).** **XLT 7키 전건 시스템 등록 완료**(2026-09-22 10:05 실측 — Dapp Portal v2.6.2 **1,727키**). 검사 전건 통과 — `check_wiki_storage pre/post` exit 0 · 버전 가드 PUT(v2→v3→v4) · `check_gate_report` exit 0 · 자동 검증 **P0 0건**(전체 7행 전수 수동 검토 동반).

첨부는 **4건만** 남기고 정리했다(구버전 엑셀 2 + 미참조 이미지 3 삭제 · 전부 본문 참조 중). 이미지는 어노테이션 정본이며 **같은 파일명으로 버전 갱신**해 본문 링크를 유지한다.

⚠️ **결제 바텀시트(`75648:8250`)는 다른 세션 결과와 겹친다 — 2026-09-21 사용자 결정으로 「키는 그대로 두고 재발 방지로 종결」했다(아래 ⛔종결).** 같은 프레임·같은 4문구가 **두 위키에 서로 다른 키로** 들어가 있고, 그중 이 페이지 쪽 `mini_` 4키는 **이미 XLT 시스템에 등록됐다**(2026-09-21 22:40 실측 Unifi 2,615 · Dapp Portal 1,726 — 내가 업로드한 것이 아니라 첨부 엑셀을 누군가 올린 것으로 보인다).

## 진행 중 작업(WIP)

없음. 위키·첨부는 정합 상태이고 P0 2건은 사용자 결정으로 종결됐다.

## 다음 할 일

- [x] ~~🔴 P0 — 결제 바텀시트 키 중복 해소~~ → **종결(2026-09-21 사용자 결정): 두 키를 그대로 둔다.** 재작업·재제안 금지(아래 ⛔종결)
- [x] ~~🔴 P0 — 화면 중복 문서화~~ → **종결(같은 결정)**. 두 위키 모두 현행 유지
- [x] ~~P1 — `mini_voucher_cu_qr_coupon_applied` 미등록~~ → **해소(2026-09-22)**. 1키 전용 엑셀로 재업로드해 Dapp Portal 1,726→**1,727키** · 5개 언어 5셀 전건 일치 확인. ⚠️ Unifi 쪽에는 없다 — `mini_` 키는 **Dapp Portal이 정본 키스페이스**라 정상이다(2026-09-21 4키가 양쪽에 들어간 쪽이 예외).
- [ ] **P1 — `1,000엔 쿠폰 적용 중` 문구 분기**: Unifi `UF_100yen_coupon_applied`(`1,000엔 쿠폰 적용 중`) ↔ mini `mini_voucher_cu_qr_coupon_applied`(`1,000엔 할인 쿠폰 적용 중`). 같은 기능이므로 통일 여부 결정(통일 시 기존 키 재업로드 동반). `100yen-deal`과 함께 본다
- [ ] **P2 — Description ①의 변경 이력 성격**: 코멘트 ①은 「[CU상품권 전용 UI 변경 사항]」 5개 항목(정책이 아니라 디자인 변경 목록)이다. 그대로 실었으며 정책 문장으로 다듬을지는 사용자 판단
- [ ] **P2 — GA `#` 대부분 `-`**: 클릭 요소(바로 구매하기·공유·리뷰 더보기·상품 카드·팝업 버튼)에 Figma 코멘트가 없어 어노테이션 번호를 못 붙였다. 디자이너에게 코멘트 추가 요청 시 `#` 채운다

## 주요 결정 사항 (이 프로젝트 한정)

| 날짜 | 결정 | 근거 / 커밋 |
|---|---|---|
| 2026-09-21 | Screen ID 3종 승인 — `voucher_cu_detail_qr_01` · `_01_02`(기존 유지) · `voucher_cu_payment_done_01_01` | 사용자 선택(매핑 표 검토 게이트) |
| 2026-09-21 | GA Event 정의 추가(view 3 · click 8) — 초안 그대로 승인 | 사용자 선택 · `md/GA.md` §2 구조 |
| 2026-09-21 | 원문 교정 2건 적용 — `추가구매하기`→`추가 구매하기` · `적용중`→`적용 중` | 의존명사 띄어쓰기 · 기존 등록값 `UF_100yen_coupon_applied` 선례. alias는 게이트 리포트에 기록 |
| 2026-09-21 | `mini_voucher_cu_qr_coupon_applied` 신규 등록(재사용 안 함) | 최근접 `UF_100yen_coupon_applied`는 **Unifi 키스페이스에만** 있어 mini(Dapp Portal)에서 재사용 불가 + 문구도 다르다 |
| 2026-09-21 | 미참조 첨부 5건 삭제(엑셀 2 · 이미지 3) | 사용자 요청 · 삭제 직전 본문 참조 재확인 후 실행 |
| 2026-09-21 | **결제 바텀시트 중복 키를 되돌리지 않는다 — 두 키 유지 + 재발 방지 장치로 대응** | 사용자 결정. `mini_` 4키는 이미 시스템 등록됐고 `UF_` 4키는 타 위키 정본이라 어느 쪽을 지워도 다른 문서·FE가 깨진다 |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지 — 이 프로젝트 한정)

- **결제 바텀시트 4문구의 키 중복** — `mini_voucher_cu_payment_done_benefit_*`(이 페이지·등록됨)와 `UF_voucher_pay_more_cu_*`(`4704515582`·미등록)를 **둘 다 유지**한다. 통합·폐기를 다시 제안하지 않는다. 대신 재발 방지로 `scripts/check_pending_keys.py` + `md/translate.md` §2-1-c를 신설했다(2026-09-21, 세션 #2)
- **Screen ID 체계** — 위 3종으로 확정. `_01_02`가 `_01_01`보다 먼저 존재하는 비대칭은 세션 #1 기존값을 보존한 결과이며 되돌리지 않는다(2026-09-21, 세션 #2)

## 세션 기록 (최신 위, 최대 5개)

### 2026-09-22 — 세션 #2(이어짐): 키 등록 확인 + 저장소 산출물 정리

- 완료: `mini_voucher_cu_qr_coupon_applied` **등록 확인**(1키 전용 엑셀로 재업로드 → Dapp Portal 1,727키 · 5셀 일치). 6키 묶음 업로드가 두 번 반영되지 않아 **1키 파일로 분리**한 것이 주효했다
- 완료: **7일 초과 미추적 산출물 664개 삭제**(약 64MB) — `assets` 286 · `landpress` 228 · `oa` 103 · `reports/gate` 40 · `xlt` 7. 삭제 전 **SHA-256 per-file 사본 대조 371건 불일치 0**(Auto-react `reports/{gate,landpress,oa}`) · `xlt` 엑셀은 위키 첨부 존재 확인 · **git 추적 파일은 제외**(사용자 결정: 커밋된 `reports/` 기록물 21건 유지)
- 교훈: **정리 기준을 「오래됨」이 아니라 「사본 있음」으로 검증**한다 — `reports/gate/`는 구값 복원의 유일 자산이라(md/translate.md 2-1-b) 날짜만 보고 지우면 롤백 경로가 사라진다. Auto-react 사본이 전건 일치할 때만 삭제했다

### 2026-09-21 — 세션 #2: `화면정의` 3프레임 + `CU Detail` 갱신 반영 (위키 v2→v4)

- 완료: Screen 2행 추가(상세·결제완료 바텀시트) · XLT 5키 신규(`payment_done_benefit_*` 4 + `qr_coupon_applied` 1) · GA 11건 · 어노테이션 이미지 3종 · 엑셀 7키 재첨부 · 미참조 첨부 5건 정리 · 용어집 대기 #28 `할인`·#29 `모바일 상품권` 등재
- 실측 함정: **`collect_frames.py` 좌표 매칭이 2건 오매칭**(바텀시트에 가려진 아래 화면의 `¥4,500`·`구매한 바우처 확인하기`를 집었다). z-order + 어노테이션 이미지 육안 확인으로 교정 — `md/wiki.md` 함정 ⓑ·ⓓ가 그대로 재현됐다
- 실측: 같은 프레임(`75648:7935`)이 세션 중 **새 부모 `CU Detail`로 이동**하고 코멘트가 0→4건으로 늘었다. 노드 id는 유지됐다
- 재발 방지(사용자 지시): **`scripts/check_pending_keys.py` 신설**(게이트 리포트 + 위키 CQL 전문 검색 · 읽기 전용 · `test_validation.py` [7] 회귀 6건) · `md/translate.md` **§2-1-c** 신설 · `CLAUDE.md` 게이트 **1-2** 신설 · 스크립트 표 등재 · `md/guide-backlog.md` #1 등재
- 🔴 교훈: **레지스트리 대조만으로는 중복을 못 막는다** — 같은 날 다른 세션이 **위키에만 올리고 아직 등록하지 않은 키**는 레지스트리에 없어 「중복 없음」으로 통과한다(이번 P0의 원인). 신규 키 전에 **`reports/gate/` 당일 리포트와 형제 위키도 문구로 대조**해야 한다
- 토큰: 키체인 5종 갱신(`figma-pat`·`confluence-pat`·`jira-pat`·`github-token`·`dropweb-token`) — 종전 `confluence-pat`은 만료돼 익명으로 떨어지고 있었다

### 2026-09-21 — 세션 #1: 페이지 생성 + QR 쿠폰 사용 팝업 (위키 v1→v2)

- 완료: `[Hogeun]` 하위에 페이지 생성 · `voucher_cu_detail_qr_01_02` 1화면 · `mini_voucher_cu_qr_coupon_used_{title,desc}` 2키(Dapp Portal 등록 완료)
- 근거: `reports/gate/gate_report_cu_qr_flow_2026-09-21.md` · 담당 팀 **LV**를 사용자 확인으로 확정

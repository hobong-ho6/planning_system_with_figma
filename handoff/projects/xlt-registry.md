# xlt-registry — XLT 시스템 등록값 검증

> 담당자: `hogeun` · 마지막 갱신: 2026-08-10 · 세션 #20(API 동작 확인) · 체계 신설은 세션 #17

## 대상 / 링크

- 절차 정본: **`md/xlt-verify.md`** (엔드포인트·타겟 확정·용어집 대조·업로드 엑셀)
- 스크립트: `fetch_xlt_registry.py --list-targets`(유효 조합 프로브) → `python3 scripts/fetch_xlt_registry.py --service <S>`(조달) · `verify_xlt_glossary.py`(대조·`--apply`로 엑셀) · `compare_wiki_xlt.py`(위키 대조)
- 감사 정본: `reports/audit/xlt_system_glossary_audit_2026-08-07.md` (3서비스 4,092키 전수)
- 수정 산출물: `reports/xlt/`(git 미추적) · `xlt/registry_fix/`
- 감사 사이트: `xlt_audit_site/`

## 서비스별 키스페이스 (⛔ 대조 전 타겟 확정 필수)

| 서비스 | 버전 | 키 수 | 정본 프리픽스 |
|---|---|---|---|
| `Dapp Portal` | v2.6.0 | 1,596 | `mini_` · `unifi_promotion_` (LV) |
| `Unifi` | v1.6.7 | **2,134** | `UF_` (UIT) |
| `Kaia Wallet` | v1.4.3 | 366 | — |

- 공통 335키 중 **151키(45%)가 값 상이** — ⛔ **레지스트리 병합 금지**(지정 순서에 따라 결론이 뒤집힌다)
- 디바이스는 `WEB BROWSER` 하나뿐(다른 값은 빈 배열) · 언어 5개 외에는 HTTP 500 · **쓰기 API 없음**(업로드는 사용자)
- 목록 API가 없어 유효 조합은 후보 프로브로 판별(무효 조합도 200 + `result: []`)
- **API 1차 조달**(세션 #17부터) — export 수동 요청은 폐지, VPN 미연결·API 장애 시에만 폴백

## 현재 상태

세션 #20에서 3서비스 프로브 + `Unifi` 2,134키 × 5개 언어 실조회로 **API 정상 확인**. 미결 없음.

## 진행 중 작업(WIP)

없음.

## 다음 할 일

- (추적 종료) `xlt/registry_fix/xlt_fix_Unifi_20260810.xlsx`(1키, zh `網址`→`URL`) 업로드는 사용자 수행 → 이후 `fetch_xlt_registry.py` 재조회로 반영 확인(세션 #18 미반영 실측 전례)

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-07 | **XLT 등록값은 API 1차 조달** — export 수동 요청 폐지 | 세션 #16이 타당성만 검증하고 멈춘 것을 구현. export 없이 0.5초에 4,092키를 받았고 사람이 손으로 센 구 표기 수치를 전건 재현했다. 커밋 `847e2de` |
| 2026-08-07 | **서비스별로 각각 대조하고 레지스트리를 병합하지 않는다** | 초기 구현은 "먼저 지정한 쪽 우선"으로 합쳤는데 **시즌3에서 Unifi를 앞에 두자 10키가 「값 상이」로 오보**됐다(실제로는 전부 Dapp Portal 정본과 일치). **지정 순서에 따라 결론이 뒤집혔다.** 공통 335키 중 45%가 값이 갈리는 구조라 병합 자체가 틀린 모델이다 |
| 2026-08-07 | **검증 로직을 새로 만들지 않고 `validate_translation.py`를 재사용** — 레지스트리를 엑셀 규격 DataFrame으로 변환 | 등급 의미가 갈리면 과거 게이트 리포트와 A/B 비교가 불가능해진다. 「등록값 유래 P1 강등 기각」도 자동으로 지켜진다 |
| 2026-08-07 | **전수 스윕의 자동 등급은 그대로 읽지 않는다** — 전건 사람 판정 후 실제 위반만 보고 | 4,092키에서 P1 4,173건 중 3,954건이 `용어 불일치` 오탐. 등급이 아니라 **용어집 매핑이 좁다는 신호**로 읽어야 한다 |
| 2026-08-04 | **기존 키 값의 정본 = XLT 시스템 등록값** | FE가 실제 사용하는 값. 기획 문서가 구버전인 항목 7건 확인. 커밋 `b656873` |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지)

- **XLT 구 표기 49건** — 2026-08-10 미착수 종결
- **용어집 예외 위반 #8~#11** — 팀 리뷰 시 전체 XLT 조회로 이관(2026-08-10)
- **`mini_luckyball_invite_banner_desc` 서비스 간 값 분기** — 미착수 종결

## 세션 기록

### 2026-08-07 — 세션 #17: 검증 체계 신설 + 전수 감사 (`847e2de`·`a4139eb`·`1ba57bd`·`a48ddfb`)

- 스크립트 3종 + `md/xlt-verify.md` 신설 · 3서비스 4,092키 전수 감사(확정 위반 69 · 치환 제안 49 · **오탐 4,975건 분리**) · 교차 오염 매트릭스 20조합 중 15개만 탐지 → `md/check.md` v3.4
- **배운 것**: **「어느 서비스를 보는가」가 검증의 전제**(공통 335키 중 151키 값 상이 — 병합하면 결론이 뒤집힌다) · **자동 검사의 "통과"에는 종류가 있다**(P0=0 = "이 검사가 볼 수 있는 범위에 문제 없음") · **표는 인덱스가 아니라 헤더 시그니처로 찾는다**

### 2026-08-07 — 세션 #16: API 타당성 검토 (`07b0d72`, **코드 미작성**)

- 크롬 확장 `~/Documents/dec/` 역공학으로 **무인증 읽기 REST API 발견 + 실호출 검증**. 보고서 `reports/research/xlt_api_feasibility_2026-08-07.md`(327줄)
- API 명세는 세션 #17이 `md/xlt-verify.md`로 정본화. 이 세션의 「UIT 치환자 불일치」 발견은 #17에서 **조회 대상 오류로 원인 규명**(문서 규칙이 맞았다). 남은 참조 가치는 §5-2 「등록값 유래 P1 강등 기각」 근거 4가지

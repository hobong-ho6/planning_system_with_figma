# guidekim-banner — Unifi Mini 홈 배너 문구 교체 (K-Pick 홈 통일)

> 담당자: `hogeun` · 마지막 갱신: 2026-08-24 · 세션 #2 · 마지막 커밋 `058b737`

## 대상 / 링크

- 위키: [pageId 4637821463](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4637821463) `[Unifi Mini] 홈을 LIFF K-Pick 홈과 통일 + 배너 문구 변경` — **현재 v11** · Jira `UNIFY-10315`
- Figma: 파일 `GOCHAYBS7hIrmWRGNuJOKV` / 프레임 **`To-be`**(`68044:20741`, 375×813)
  - ⚠️ `clipsContent: false`라 **콘텐츠가 프레임 높이 밖(y=877)까지 이어진다** — 이미지 렌더·좌표 계산 시 프레임 높이만 믿지 않는다
  - ⚠️ **Figma 배너는 여전히 구 문구**(`From K-beauty to K-culture` / `All Things Korea, Unifi`). 코멘트가 XLT 교체를 지시하는 구조이고 위키 XLT 표가 `현재 ko_KR | 변경 ko_KR (확정)` 2열이라 **의도된 As-is 화면 — 원본 수정 요청 대상 아님**
- XLT: **2키** `mini_guidekim_kbeauty_to_kculture`(`최대 15% 캐시백!`) · `mini_guidekim_all_korea_experience`(`올리브영, 다이소 바우처부터 뷰티케어 시술까지`) · 담당 FE **LV**(프리픽스 `mini_guidekim_`로 판별 · 치환자 `{0}` — 이번 2키는 변수 없음)
- 엑셀: `xlt/xlt_fix_guidekim_banner_2keys_20260824.xlsx` — **위키 첨부와 같은 파일명 고정**(재첨부 시 본문 링크 그대로 최신본)
- 게이트 리포트 prefix: `reports/gate/gate_report_guidekim_banner_*.md`

## 현재 상태

위키 **v10 → v11 반영 완료**(세션 #2). Description 정책 3건 추가(Figma 코멘트 6건 중 3건 누락분) · 통합 번호를 **y순 + 같은 행은 좌→우**로 재정렬 · `To-be.png`를 어노테이션(정책 빨강 4 / xlt 파랑 2, 겹침 0, scale 2) 버전으로 같은 파일명 재첨부 · 엑셀 ja 정합 재생성·재첨부. **게이트 P0 = 0**(자동 + 수동 10셀 전수) · `check_wiki_storage.py` pre/post exit 0.

**ja_JP 정본 = 시스템 등록값 = 위키 v9** — `最大15%還元！` / `オリヤン、ダイソーのバウチャーからビューティー施術まで`. 세션 #1 엑셀(`キャッシュバック`·`オリーブヤング`·`ビューティーケア`)이 낡은 값이었고, 그대로 재업로드하면 **등록된 정본을 되돌리는 사고**가 된다.

## 진행 중 작업(WIP)

- **미커밋 파일 1건 — 손대지 않는다**: `reports/gate/gate_report_guidekim_banner_2keys_2026-08-24.md`(3판, 세션 #1 산출물). **다른 세션 소관**이라 세션 #2도 커밋하지 않았다. 그 세션이 정리할 때까지 `git status`에 `??`로 남는다

## 다음 할 일

- [ ] **🔴 P0 — 두 서비스의 값이 갈렸다(업로드 범위 확정 필요)**: `kbeauty_to_kculture`는 Dapp Portal v2.6.1 = 신 문구 / **Unifi v1.6.7.1 = 구 문구**(`K-뷰티부터 K-컬처까지`·`韓国肌管理も、Kカルチャーも！`)로 **ko·ja 모두 갈렸고**, `all_korea_experience`는 **Unifi 미등록**. 세션 #1이 경고한 「양쪽 업로드 필요」가 한쪽만 올라가 현실화됐다. → **화면이 Unifi 키스페이스를 참조하는지 FE·기획 확인** 후 양쪽 업로드 / 또는 Unifi 구 문구 정리(삭제·동기화) 결정. 쓰기 API 없음 → **업로드는 사용자 수행**
- [ ] **P1 — `JP UX 피드백`(pageId `4611882417`) 이슈 미해소**: 「같은 키인데 Dapp/Unifi ja 값 상이」는 세션 #1이 「해소된다」고 봤으나 **해소되지 않았다**(ja 여전히 상이 + ko까지 갈림). 위 P0 처리 후 상태 갱신
- [ ] **P1 — 다른 위키의 구 문구**: `Unifi mini - Guide Kim`(pageId `4339334947`)의 다국어 표·Screen XLT에 두 키의 **구 문구 잔존**. 시스템 업로드 후 정합이 깨진다 → 키 단위 패치 모드로 함께 갱신할지 확인
- [ ] **P1 — 위키 History의 도구 진단이 틀렸다(정정 필요)**: History에 「`fetch_comments.py`가 이 파일에서 **roots=0 반환(버그)**」로 적혀 있으나 세션 #2가 같은 파일·프레임에서 **`roots=6` 정상 수신**했다. 호출 인자 문제(`node_ids` 자손 집합 · `collect_node_boxes`는 튜플 반환 → 언패킹)로 보인다. **잘못된 도구 진단이 남으면 다음 세션이 공용 페처를 우회해 인라인 재구현**(`md/wiki.md` 금지 안티패턴)한다 → 이력 훼손 우려로 미처리
- [ ] **P2 — 용어집 보완 권장**(전역 자원 → `projects/glossary-guide.md`와 함께): **캐시백 ja 분기**(등재 `キャッシュバック` 단일 매핑이 짧은 배너·배지의 `還元`을 못 덮어 P1 오탐 — 형제 `還元` 2건 vs `キャッシュバック` 1건) · **올리브영 `オリヤン`**(세션 #1 권장 `オリーブヤング`는 실사용 **0건** → 정정) · **뷰티케어 `ビューティー`**(`ビューティーケア` 실사용 0건 → 정정) · **다이소 `ダイソー`** · 세션 #1 권장 중 **최대(문장형)·바우처·시술 미해소 이월**(바우처는 `kpick-oa-consent` P2와 동일 건)
- [ ] **P2 — 고아 첨부 2건 정리**: `annotated_68044-20741.png` · `frame_68044-20708_tobe.png`(본문 미참조 · 현재 `To-be.png`만 참조). **첨부 삭제는 되돌릴 수 없어 사용자 확인 후에만**
- [ ] **P2 — Screen 표 첫 행 Screen ID 공란**: 둘째 행은 `Reservation Case`(프레임명 기반 구 방식 · 소급 전환 금지)인데 첫 행은 비어 있어 식별자가 없다. `md/IA.md` 어휘로 부여하려면 **매핑 표 사용자 승인** 필요
- [ ] **P2 — `15%` 변수화**(PM 판단): `md/IA.md` 「캐시백률은 고정값이 아니다」(환경별 상이). LV 적용안 `최대 {0} 캐시백!`. **미해소 이월**
- [ ] **P2 — 디자이너 확인**: `My Reservation` 아이콘에만 이동 정책 코멘트가 없다(탭 3개는 있음) · `mini_luckyball_kbeauty_kculture`(Dapp Portal)에 구 문구 `K-뷰티부터 K-컬처까지` 잔존(럭키볼 영역 — 대상 외)

## 주요 결정 사항 (이 프로젝트 한정)

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-08-24 | **ja_JP 정본 = 시스템 등록값(위키 v9)** — 엑셀을 그 값으로 재생성 | 엑셀·위키·시스템 3자 불일치 실측에서 **엑셀만 낡았다**. 사용자가 이미 Dapp Portal에 업로드하고 위키를 맞춘 상태 |
| 2026-08-24 | **ja `캐시백` = `還元` 유지**(P1 등재값 권고 기각) | 등록 정본 + `mini_guidekim_` 형제 `還元` 2건 vs `キャッシュバック` 1건(배지 `還元{0}%`가 같은 계열) + 배너 길이 제약. **HANDOFF 전역 결정(형제 키 우선)에 부합** |
| 2026-08-24 | **통합 번호 = y순 + 같은 행은 좌→우** | 탭 3개가 y=407·412·416으로 9px 차라 사실상 같은 행 — 엄격 y순이면 화면 좌우 순서와 어긋난다(사용자 결정) |
| 2026-08-24 | **1키 2행 → 2키로 분리**(각 1행, 개행 제거) | 사용자 정정 — 원래 2개 키였다. 세션 #1 3판에서 엑셀·검증·리포트 전부 재실행 |
| 2026-08-24 | **용어 `상품권` → `바우처` 통일** · `상품권 부터` → `바우처부터` | 2차 검토가 서비스 내 `바우처` 계열과의 분기를 P2 제기 → 사용자 결정. 위키 Policy에 원안(`김민정 안`)은 이력으로 보존하고 「최종 확정 문구(교정 반영)」 항목 추가 |
| 2026-08-24 | **`15%` 리터럴로 등록**(변수화 안 함) | 가변 여부는 PM 판단 — `md/guide.md` §5-2 「변수화는 사용자 확인 후에만」 |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지 — 이 프로젝트 한정)

- **용어 `바우처` 통일** — 3판에서 재확인 완료. `상품권` 재제안 금지(기존 키의 th·zh 표기 이탈은 용어집 등재 과제로 이월)
- **키 개명하지 않음** — 키 이름과 내용이 괴리돼 있으나(`_kbeauty_to_kculture` → 내용은 캐시백 / `_all_korea_experience` → 내용은 바우처·시술) **키는 FE 전달 후 변경 불가**. 위키에 교체 이력을 남기는 것으로 갈음

## 세션 기록 (최신 위, 최대 5개)

### 2026-08-24 — 세션 #2: ja 정합 패치 + To-be 프레임 갱신, 위키 v10→v11 (`058b737`)

- **완료**: 키 단위 번역 패치 모드 (B) lang-specific — ja 2셀만 등록값 정본으로 교체(`patch_translation` 컬럼 정렬 가드 1g 통과) + 위키 Mode B 프레임 갱신. 리포트 `reports/gate/gate_report_guidekim_banner_ja_patch_2026-08-24.md`
- **교훈 ① — 단일 언어 패치도 10셀 전수 대조가 먼저다**: 「ja만 바꾼다」고 ja diff만 보면 **컬럼이 회전 어긋난 키의 다른 언어 유일 원본이 소실**된다(`mini_guidekim_cat_eye`류 실측). 대상 2키 5개 언어 전부를 위키 정본과 대조해 misalignment 0건 확인 후 진행
- **교훈 ② — 편집 중 라이브 버전이 올랐다(rebase 실측)**: 최초 조회 v9 → dry-run 시점 **v10**(사용자가 History 행 직접 추가). 그 행을 보존 가드에 넣고 v10 기준 재작성. **버전 가드가 없었으면 사용자 행을 덮어썼다**
- **교훈 ③ — 전역 금지 가드 ↔ 이력 기록 충돌 재발**: 「다국어 표에 구 ja 값 금지」를 전역으로 걸었더니 **History에 이력으로 인용한 `オリーブヤング…`** 를 잡아 중단. 가드 범위를 `<h2>다국어 번역` 이후로 한정해 해결(세션 #1의 `상품권` 가드와 동형 — HANDOFF·`md/wiki.md` 673행에 이미 기록된 함정이 두 세션 연속 재발)
- **교훈 ④ — 세션 #1 (d-1) 권장 2건이 실사용과 반대였다**: `オリーブヤング`(실측 0건) · `ビューティーケア`(실측 0건). **용어집 권장 표기는 레지스트리 실측 후에 낸다**

### 2026-08-24 — 세션 #1: 배너 2키 문구 교체 + 위키 v4→v5 (리포트 미커밋)

- **완료**: 키 단위 번역 패치 모드 (A) ko-source — ko 변경 → 5개 언어 전면 재번역, 게이트 P0=0(2차 독립 검토 5개 언어 전부 「정확」). 위키에 다국어 번역 섹션 신설(6컬럼 2키) + 엑셀 첨부 + XLT 정리 섹션 「미진행」→「완료」
- **3판까지 재작업** — 초판 1키 2행(용어 `상품권`) → 2판 `바우처` 통일 → **3판 2키 분리**(사용자 정정). 폐기된 1키 통합 엑셀은 **오업로드 방지 목적으로 삭제**
- **경고를 남겼으나 반영되지 않았다**: (d-2) #12 「키 ①은 **양 서비스 업로드 필요**」가 Dapp Portal만 업로드돼 세션 #2 시점에 ko·ja 분기로 확대됐다 → 위 P0

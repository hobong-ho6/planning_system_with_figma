# kaiawallet-transition — Kaia Wallet > Unifi 전환 유도

> 담당자: `hogeun` · 마지막 갱신: 2026-08-24 · 마지막 커밋 `a0d2a5a` · 게이트 P0=0 · **37키** · **위키 v18 반영 완료(실측 확인)**

## 대상 / 링크

- 위키: `4515188069` [Kaia Wallet > Unifi 전환 유도] — **현재 v18**
- Figma 섹션 `68082:21171`(화면정의) · 파일 `GOCHAYBS7hIrmWRGNuJOKV` — **8프레임**(캔버스 읽기 순서)

  | # | node-id | 프레임명 | Screen ID | 크기 |
  |---|---|---|---|---|
  | 1 | `67372:8517` | Unifi 지갑 상태 | `kaiawallet_unifi_walletstate_01` | 375×570 |
  | 2 | `67935:5288` | Unifi 연결 전 | `kaiawallet_unifi_transition_01` | 375×1442 |
  | 3 | `67935:5542` | Unifi 연결 후 | `kaiawallet_unifi_transition_02` | 375×1471 |
  | 4 | `67935:6681` | (EXB) Unifi Bridge to Kaia Wallet | `kaiawallet_unifi_bridge_01` | 375×812 |
  | 5 | `67935:6442` | 자산전송 팝업 | `kaiawallet_unifi_transition_01_01` | 375×812 |
  | 6 | `67935:5007` | 전송완료 팝업 | `kaiawallet_unifi_transition_01_02` | 375×812 |
  | 7 | `65740:26924` | 전송 버튼 상태 | `kaiawallet_unifi_transferstate_01` | 375×671 |
  | 8 | `67935:5858` | 트랜젝션 화면 | `kaiawallet_unifi_transaction_01` | 375×949 |

- 산출: `xlt/xlt_output_kaiawallet_unifi_transition_20260824.xlsx` (**37키**) — 구 `..._20260814.xlsx`(34키)는 위키 첨부에 남아 있으나 본문 참조 없음
- 게이트 리포트: `reports/gate/gate_report_kaiawallet_unifi_transition_8frames_2026-08-24.md`
- 성격: Kaia Wallet 앱 실행 시 **메인 위에 노출**되는 자산 이전 안내 페이지 → surface `main_`

## 담당 FE · 키 작명

- **Kaia Wallet 기존 규칙**(UIT/LV 미적용 — 2026-08-19 확정, 재편 후에도 유지). 프리픽스 `main_unifi_transition_` · 변수 치환자 없음
- **예외 2키** — 트랜잭션 서명 화면(8번 프레임)은 시스템에 이미 등록된 **`send_asset_transaction_check_`** 계열을 쓴다(사용자 선택 2026-08-24). 형제 키 `_send_address`·`_receive_address`가 v1.4.3에 등재돼 있다

## 2026-08-24 재편 — 34키 → 37키

화면이 전면 변경돼 사용자가 위키 Screen 표를 비웠다(v16 `Screen(TBU)` → v17). 지시: 문구 같은 것은 기존 키·번역 재사용 / 화면에서 사라진 키는 표·엑셀에서 제거.

| 구분 | 키 수 | 처리 |
|---|---:|---|
| 유지(문구 동일) | 11 | 기존 키 + 기존 번역 **그대로**(재번역 없음) |
| 갱신(슬롯 동일·문구 리라이팅) | 9 | **기존 키 이름 유지** + 5개 언어 재번역 ← 사용자 선택 A안 |
| 신규 | 17 | |
| **제거** | 14 | `signup_*` 3 · `mainwallet_*` 2 · `wallet_sns/mnemonic_desc` 2 · `guide_nowallet_*` 6 · `guide_step3_title` |

- `guide_step3_title`은 문구가 `Unifi 바로가기`로 바뀌어 기존 `link_btn`과 같아졌다 → **같은 문구=같은 키** 원칙으로 키를 신설하지 않고 `link_btn` 재사용, 이 키는 제거
- 「연결 후」 프레임은 **xlt 마커가 0개**인데 가이드 1단계 문구가 연결 전과 다르다 → 사용자 승인으로 `guide_linked_step1_title`/`_desc` 2키 부여(마커 없이 화면 실재 기준)

## 현재 상태

**위키 v18 반영 완료 — 라이브 실측 확인**

| 검증 | 결과 |
|---|---|
| 라이브 버전 | **v17 → v18** |
| Screen 표 | **8행 신규**(비어 있던 표) · Screen ID 8/8 · 어노테이션 이미지 8장 첨부 |
| 키 대조 | 엑셀 37키 → 위키 **미발견 0** · **제거 14키 잔존 0** |
| 번역값 대조 | **185셀**(37키×5개 언어, `<br />`↔개행 정규화) → **미발견 0** |
| 첨부 참조 | 구 이미지 2장·구 엑셀 참조 **제거 확인** · 신 엑셀 링크 존재 |
| `check_wiki_storage.py` | `pre` **exit 0** · `post` **exit 0** |
| `check_gate_report.py` | **exit 0**(필수 요소 6종) |

게이트: **P0 = 0**(자동 + **수동 전수 185셀**) · P1 24건·P2 10건 전건 판정 · ko 원문 교정 9건 · 코멘트→텍스트 좌표 오매칭 3건 교정.

## 진행 중 작업(WIP)

없음.

## 다음 할 일 (사용자 결정 대기)

1. **용어집 보완 3건**(게이트 d-1) — ⓐ **「연동」 미등재** ⓑ **「전송」 미등재**(37키 중 12키가 사용) ⓒ **「보내기」 등재값이 시스템 실사용과 어긋남**(등재 ja `出金`·zh `轉帳` / Kaia Wallet v1.4.3 실사용은 `送信`·`發送`, `出金`은 **0건**). 승인 시 전체 JSON 산출 + **기획자 가이드 zip 필수 동반 갱신**(`md/landpress.md` §5-1)
2. **Figma 원문 개선 — 신규 6건만**(아래 ⛔ 종결과 겹치는 3건은 요청하지 않는다): `연동`↔`연결` 혼용 · `Kaia wallet`→`Kaia Wallet`(3곳) · `지금바로`→`지금 바로` · `전송중`→`전송 중` · `트랜젝션`→`트랜잭션`(시스템 등록값 `wallet_request_sign_tx_title`이 정본) · `Imported wallet` 한국어 표기 여부(시스템에 ko=영어 선례 20/366건이 있어 유지도 가능)

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지)

> 2026-08-20 사용자 지시 「이 프로젝트에 남은 건 폐기 처리」. **2026-08-24 재편 후에도 그대로 유효하다.**

- **XLT 시스템 업로드 추적 종료** — 산출 엑셀(37키)은 그대로 두고 **업로드 여부를 이 문서에서 추적하지 않는다.** ⚠️ 업로드하지 않으면 시스템에 반영되지 않는다(사용자 인지 사항 · v1.4.3 366키에 이 프로젝트 키는 **0건**). 재개 시 대상은 `Kaia Wallet` / `WEB BROWSER`
- **Figma 원문 개선 요청 중 3건은 종결 유지** — `대표지갑`(띄어쓰기) · `코인만 합산`↔`코인에 한해 합산` 프레임 간 불일치 · 보조용언 띄어쓰기 정책. **2026-08-24 신규 프레임에서 재발했으나 종결 판정을 그대로 적용해 원문 유지**(XLT ko는 교정본을 쓰므로 결과에 영향 없음). 디자이너 요청도 하지 않는다
- **P2 마침표·느낌표 정책 보류**(원문 유지) — 재편분 10건도 동일 판정. 재보고 금지

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-24 | **문구가 리라이팅된 슬롯은 기존 키 이름을 유지하고 값만 갱신**(A안) | 기존 34키가 XLT 시스템에 **미업로드**라 키 교체 비용이 0이고, 화면 슬롯(가이드 3스텝·연동 카드·전송 카드)이 그대로여서 FE가 키를 다시 갈아끼울 필요가 없다 |
| 2026-08-24 | **트랜잭션 서명 화면 2키는 `send_asset_transaction_check_` 계열** | 그 화면의 형제 키가 이미 시스템에 등재돼 있다. 페이지 내 프리픽스 통일보다 **시스템 키스페이스 정합**이 우선 |
| 2026-08-24 | **용어집 등재값과 시스템 등록값이 충돌하면 「같은 키스페이스 형제 키」를 정본으로** | 「보내기」 ja 등재값 `出金`은 Kaia Wallet 실사용 0건, 실제는 `送信`. 형제 키 `_send_address`가 `送信元`이라 그쪽에 맞췄다 |
| 2026-08-24 | **키 제거 판정은 「정본 화면」 2종 기준 + 이미지 육안 확인** | 팝업 프레임의 **배경이 구버전 페이지**라 사라진 문구가 노드로 남아 있다. 노드 존재만으로 판정하면 제거 대상을 놓친다 |
| 2026-08-19 | Kaia Wallet 기존 키 체계를 따른다(UIT/LV 미적용) | 기존 366키가 어느 팀 프리픽스도 쓰지 않는다 |
| 2026-08-19 | 기존 키가 있는 라벨은 신규 키를 만들지 않는다 | `보내기`·`편집`·`코인/토큰 추가하기` 3건이 이미 등록돼 있었다 |
| 2026-08-19 | **`대표 지갑`(띄어쓰기)** + 5개 언어 통일 | 신규 개념이라 시스템 실사용 0회 = 표기 정본이 없었다. en `main wallet` / ja `代表ウォレット` / zh `主要錢包` / th `กระเป๋าหลัก` |

## 세션 기록

### 2026-08-24 — 8프레임 재편 37키 + 위키 v18 (게이트 P0=0)

- 섹션 `68082:21171` 8프레임 `collect_frames.py` 1회 수집(코멘트 55루트·답글 19) → 기존 34키 대조 → 유지 11·갱신 9·신규 17·제거 14 → 게이트 → 위키 Screen 8행·다국어 37키·History 1행 PUT → 185셀 실측 0건 미발견
- **배운 것 ①** `collect_frames.py`의 좌표 매칭이 **더미 텍스트를 잡는다** — 겹친 박스에서 `cypress`(네트워크값)·`KLAY`(코인심볼)가 선택돼 **3건 오매칭**. 어노테이션 이미지 **육안 확인으로만** 잡혔다. 매칭 결과는 번역 전 반드시 이미지와 대조한다
- **배운 것 ②** **팝업 프레임의 배경은 구버전 화면일 수 있다** — 「전송완료/자산전송 팝업」 배경이 옛 페이지라 삭제된 문구가 노드로 살아 있었고, 배경 상단 문구도 `Unifi` vs `Unifi App 출시`로 갈렸다. 키 제거 판정의 정본은 **전체 페이지 프레임**이다
- **배운 것 ③** **용어집이 아니라 시스템 등록값이 정본일 때가 있다** — 「보내기」 ja `出金`(등재) vs `送信`(실사용 전건). 레지스트리 조회 없이 용어집만 믿으면 시스템과 어긋난 번역을 넣는다
- **배운 것 ④** 자동 검증기 P0=0·P1 24건 중 **실제 수정 대상은 0건**이었지만, **수동 전수에서 언어 내 표기 혼용 5건**(th `ส่ง`↔`โอน` · zh `傳送`/`連結` · en 아포스트로피)을 새로 찾았다. 수동 검토는 형식이 아니다

### 2026-08-20 — 위키 반영 검증(구 4프레임 34키)

- 사용자 「반영했어」 보고를 실측으로 확인 — v11→v12 · 프레임 4/4 · 34키/170셀 미발견 0 · 검사기 pre/post exit 0. 전건 일치였지만 확인 비용이 낮고(2분) 틀렸을 때 비용이 크다

### 2026-08-19 — 4프레임 신규 번역 34키

- 추출 62 → 분류(신규 35·더미 18·기존 9) → 통합 1쌍 → 34키 · ko 교정 11건 · 수동 170셀 전수. **더미·기존 라벨을 먼저 걷어내야 키가 부풀지 않는다**(62 중 27이 대상 아님)

# kaiawallet-transition — Kaia Wallet > Unifi Mobile 전환 유도

> 담당자: `hogeun` · 마지막 갱신: 2026-08-19 · 게이트 P0=0 · 34키

## 대상 / 링크

- 위키: `4515188069` [Kaia Wallet > Unifi Mobile 전환 유도] — 게이트 시점 **v11**(Screen 섹션 공란)
- Figma 4프레임 (`GOCHAYBS7hIrmWRGNuJOKV`)

  | node-id | 프레임명 | 크기 |
  |---|---|---|
  | `67372:7491` | Unifi 지갑이 있는 경우 | 375×1726 |
  | `65740:26199` | Unifi 지갑이 없는 경우 | 375×1516 |
  | `67372:8517` | Unifi 지갑 상태 | 375×1071 |
  | `65740:26924` | 전송 버튼 상태 | 375×671 |

- 산출: `xlt/xlt_output_kaiawallet_unifi_transition_20260814.xlsx` (**34키**)
- 게이트 리포트: `reports/gate/gate_report_kaiawallet_unifi_transition_2026-08-19.md`
- 성격: Kaia Wallet 앱 실행 시 **메인 위에 노출**되는 자산 이전 안내 페이지 → surface `main_`

## 담당 FE · 키 작명 (사용자 확정 2026-08-19)

- 위키에 UIT/LV 구분이 없어 질의 → **Kaia Wallet 기존 규칙을 따른다**(기존 366키가 `main_`·`web_`·`kaiawallet_`처럼 **UIT/LV 어느 프리픽스도 쓰지 않는다**)
- 작명 체계: **`{surface}_{section}_{element}_{type}`** (1st: `web` 109 · `kaiawallet` 71 · `payment` 48 · `main` 44 · `wallet` 40 · `Minidapp` 20 / last: `title` 103 · `btn` 39 · `desc` 36)

## 추출·분류 (신규 문구만 — 사용자 선택)

4프레임 TEXT 노드 → 고유 텍스트 **62개** = 신규 35 + 더미 18 + 기존 라벨 9 → near-duplicate 1쌍 통합 → **최종 34키**

- 제외 더미 18: 상태바 `9:27` · 스텝 번호 · 금액 4종 · 지갑 주소 6종 · 계정 3종
- 제외 기존 라벨 9 중 **3개는 Kaia Wallet에 기존 키가 있어 신규 키를 만들지 않았다**(중복 방지) — `보내기`(`main_balance_send_btn` 외 3키) · `편집`(`main_balance_manage_btn`) · `코인/토큰 추가하기`(`main_addtoken_title`)

## 현재 상태

**게이트 P0 = 0**(자동 P1도 0 · 수동 전수 34키 × 5개 언어 = **170셀**) · ko 교정 **11건** + 통합 1쌍 · P2 10건은 전건 **정책 보류(원문 유지)** 판정 · 엑셀 생성 완료.

- ⚠️ **위키 반영 여부는 (미확인)** — 게이트는 「위키 반영으로 진행 가능」에서 끝났고, 이후 PUT 기록이 없다. **라이브 버전 재조회로 확인이 필요하다**(Confluence PAT 필요)

## 진행 중 작업(WIP)

없음.

## 다음 할 일

- [ ] P1 **위키 반영 여부 확인** — 라이브 재조회로 Screen 섹션·번역표 반영 상태 확인 후 미반영이면 PUT(`check_wiki_storage.py` pre/post exit 0)
- [ ] P1 **XLT 시스템 업로드(사용자 수행)** — 대상 서비스 **`Kaia Wallet`**. 앱 키스페이스가 별도라면 **업로드 타겟(디바이스) 확인 필요**(읽기 API에는 `WEB BROWSER`만 노출)
- [ ] P2 **Figma 원문 개선 요청(디자이너)** — 6건: `코인만 합산`/`코인에 한해 합산` 표현 통일 · `'전송하기' 를` 따옴표 짝·조사 앞 공백 · `계정으로  매일` 이중 공백 · `대표지갑`/`대표 지갑` 혼용 · `이자혜택`·`지갑생성`·`SNS계정`·`여러개`·`Kaia wallet` 띄어쓰기·브랜드 표기 · 마침표/느낌표 정책 확정
- (권장 이관) 용어집 보완 2건(`대표 지갑`·`전송/송금`)은 `projects/glossary-guide.md`로 모았다

## 주요 결정 사항

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-19 | **Kaia Wallet 기존 키 체계를 따른다**(UIT/LV 규칙 미적용) | 기존 366키가 어느 팀 프리픽스도 쓰지 않는다. 위키에 팀 구분이 없어 사용자에게 질의해 확정 |
| 2026-08-19 | **기존 키가 있는 라벨은 신규 키를 만들지 않는다** | `보내기`·`편집`·`코인/토큰 추가하기` 3건이 이미 등록돼 있었다. 같은 문구=같은 키 재사용 원칙 |
| 2026-08-19 | **`대표 지갑`(띄어쓰기)으로 표기 확정** + 5개 언어 통일 | 신규 개념이라 시스템 전체 실사용 0회 = 표기 정본이 없었다. en `main wallet` / ja `代表ウォレット` / zh `主要錢包` / th `กระเป๋าหลัก` |
| 2026-08-19 | **P2 10건 전건 원문 유지(정책 보류)** | 마침표·느낌표 정책이 확정되지 않아 임의 적용하지 않는다(사용자 결정 사안) |

## 세션 기록

### 2026-08-19 — 4프레임 신규 번역 34키 (게이트 P0=0)

- 추출 62 → 분류(신규 35·더미 18·기존 9) → 통합 1쌍 → 34키 · ko 교정 11건 · 5개 언어 · 수동 170셀 전수
- **배운 것**: **더미와 기존 라벨을 먼저 걷어내야 키가 부풀지 않는다**(62개 중 27개가 대상 아님) · **기존 366키의 작명 통계를 먼저 뽑아야 새 키가 체계에 맞는다**(1st/last 토큰 빈도로 `main_` surface 결정) · **신규 개념은 실사용 0회라 정본이 없다** — 이번에 정한 표기를 용어집에 올려두지 않으면 다음 화면에서 흔들린다

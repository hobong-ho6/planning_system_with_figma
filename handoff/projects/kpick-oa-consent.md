# kpick-oa-consent — K-Pick 바우처 구매 후 Unifi OA 알림 동의

> 담당자: `hogeun` · 마지막 갱신: 2026-08-24 · 세션 #1 · 마지막 커밋 `04cc57a`

## 대상 / 링크

- 위키: [pageId 4637821100](https://wiki.workers-hub.com/pages/viewpage.action?pageId=4637821100) `K-Pick 바우처 구매 후 Unifi OA 친구추가 및 Unifi OA 발송` — **현재 v3** · Jira `UNIFY-10309`
- Figma: 파일 `GOCHAYBS7hIrmWRGNuJOKV` / 페이지 `K-pick(상세페이지, My)`(`67735:11021`) / 프레임 **`Unfi OA 친구추가 동의 팝업`**(`68080:20746`, 375×812)
  - ⚠️ 프레임명에 오타 — `Unfi`(`i` 누락). 첨부 파일명·Description 병기가 원본 이름을 따랐다
  - ⚠️ 이 프레임은 위키 Design 링크·Flow 임베드가 가리키는 SECTION `바우처 구매 후 Unifi OA 친구추가`(`67971:11654`) **안이 아니라 같은 페이지의 형제 노드**다(실측)
- XLT: 프리픽스 **`mini_guidekim_kpick_oa_consent_`** · 담당 FE **LV**(치환자 `{0}` — 이번 4키는 변수 없음) · **등록 서비스 미정**(아래 다음 할 일)
- 엑셀: `xlt/xlt_output_20260824181908.xlsx` — 위키 첨부 id `4637823639`. **파일명 고정 아님**(타임스탬프) → 갱신 시 본문 `ri:filename`도 함께 바꾼다
- 이미지: 위키 첨부 `Unfi_OA_친구추가_동의_팝업.png`(id `4639688389`)가 정본 — 로컬 보관 안 함
- 게이트 리포트 prefix: `reports/gate/gate_report_kpick_oa_consent_*.md`

## 현재 상태

위키 **v2 → v3 반영 완료.** 비어 있던 Screen 섹션에 4컬럼 표(Screen ID `kpick_voucher_oa_01_01`)를 신설하고, 다국어 번역(XLT Full Translation) 섹션을 신설해 **4키 × 5개 언어 = 20셀**을 채웠다. 어노테이션 이미지·XLT 엑셀 첨부 완료. **게이트 P0 = 0**(자동 + 수동 20셀 전수) · `check_gate_report.py` exit 0 · `check_wiki_storage.py` pre/post exit 0. **미결 3건**(등록 서비스 확정 · 프리픽스 잠정 · 위키 History 불일치)이 아래에 남아 있다.

**확정 4키** — `_purchase_complete`(`구매가 완료되었어요!`) · `_title`(`LINE으로\n바우처를 보내드릴까요?`) · `_desc`(`Unifi 알림 수신에 동의하시면\n바우처와 쏠쏠한 캐시백 포인트 정보도 알려드려요.`) · `_agree_btn`(`동의하고 알림 받기`)

## 진행 중 작업(WIP)

없음.

## 다음 할 일

- [ ] **P1 — XLT 시스템 업로드 서비스 확정(FE 확인)**: 4키 전부 **신규 등록**이다(Unifi v1.6.7.1 2,143키 + Dapp Portal v2.6.1 1,599키 = 3,742키 전수에서 동일·정규화 동일 **0건**, 2026-08-24 16:58 실측). 기존 `mini_guidekim_kpick_*` 4키가 **Dapp Portal**에 있어 1차 후보이나(Unifi에는 K-Pick 관련 `mini_` 키 0건), 화면이 참조하는 키스페이스는 FE 확인이 필요하다. 쓰기 API 없음 → **업로드는 사용자 수행**
- [ ] **P1 — 위키 History ↔ 본문 Policy 불일치 (이번 작업 전부터 존재 · 손대지 않음)**: History 2번째 행은 「Policy에 합의 사항 **5개 구분**, **후속 액션·확인 필요 4건 분리**」로 적혀 있으나 현재 본문 Policy는 **2행**이고 후속 액션 섹션이 없다. **v1 실측 확인**: v1에는 Policy 5행(우선순위 과제 / 9·11 타겟 과제 / GuideKim 결제 화면 / 복합결제 / 바우처 구매 후 Unifi OA 동선) + 후속 액션 섹션이 모두 있었고 **v2(사용자 직접 편집)에서 3행과 해당 섹션이 삭제**됐다. 의도적 축약이면 History 문구를 맞추고, 실수면 v1에서 복원 — **판단 사안이라 미처리**
- [ ] **P2 — `kpick_` 프리픽스 확정 대기**: Screen ID `kpick_voucher_oa_01_01`이 `md/IA.md` §2-7의 **잠정** 어휘(IA 미결 #496 승인 대기)에 의존한다. 프리픽스 확정 시 Screen ID 재확인. **사용자 결정으로 IA.md는 이번에 갱신하지 않았다**
- [ ] **P2 — Figma 원본 교정 요청(디자이너)**: ⓐ `구매 완료 되었어요!` → `구매가 완료되었어요!` ⓑ 개행 앞 공백 제거(`동의하시면 \n`) ⓒ 프레임명 오타 `Unfi` → `Unifi`
- [ ] **P2 — 동의 범위 고지(기획·법무)**: 화면 목적은 「친구추가 + 알림 수신 동의」인데 문구는 알림 수신만 말한다. 5개 언어가 ko를 일관되게 따르므로 **번역 결함은 아님** — 원문 정책 사안
- [ ] **P2 — 용어집 보완 권장 3건**(전역 자원 → `projects/glossary-guide.md`와 함께 처리): **바우처**(등록값 정본은 있으나 미등재 · th `เวาเชอร์`↔`บัตรกำนัล` 2:2·zh `優惠券`↔`憑證` 3:1 이탈 잔존 — **2026-08-24 guidekim 배너 리포트에서도 동일 권장, 미해소 이월**) · **포인트**(36키 표기 일관인데 미등재) · **구매 완료**(문장형 — 라벨형 매핑이 P1 오탐 유발)

## 주요 결정 사항 (이 프로젝트 한정)

| 날짜 | 결정 | 근거 / 커밋 |
|---|---|---|
| 2026-08-24 | **담당 FE 팀 = LV** | 위키에 UIT/LV 구분이 없어 사용자에게 질문 → LV 확정. 규칙(프리픽스=프로젝트 약어, 치환자 `{0}`) 설명·적용 확인 완료 |
| 2026-08-24 | **XLT Key 네임스페이스 = `mini_guidekim_kpick_oa_consent_*`** | 초판 `mini_kpick_*`는 3,742키 전수에서 **선례 0건**. K-Pick 탭 기존 키가 전부 `mini_guidekim_kpick_*`(`_tab`·`_label`·`_jp_banner_title`·`_jp_banner_desc`)라 같은 기능 영역이 두 네임스페이스로 갈리는 것을 방지. **트레이드오프**: 화면은 K-Pick 탭인데 키에 `guidekim`이 붙고 최장 키가 48자 |
| 2026-08-24 | **Screen ID = `kpick_voucher_oa_01_01`** · IA.md 미갱신 | 사용자 승인. 부기능 `voucher`는 IA 기재 「한국 여행 필수 바우처」 어휘. IA 갱신(OA 바텀시트 항목 신설 · 탭 구성 K-POP 차이)은 **사용자가 「하지 않음」 결정** |
| 2026-08-24 | **키① ko 주격 조사 보완** `구매 완료되었어요!` → `구매가 완료되었어요!` | 2차 검토 제기 → 사용자 결정. 등록값 문장형 선례가 조사를 쓴다(`연결이 완료되었어요.`) |
| 2026-08-24 | **키② th 물음표 추가** `ไหม` → `ไหม?` | ko가 `?`로 끝나는 55키 중 th도 `?`가 **48건(87%)**. 「th 문장 끝 마침표 미사용」 관례는 마침표 사안이고 물음표는 대상 아님 |
| 2026-08-24 | **키③ ja `ご同意いただくと`** | 레지스트리 `ご同意` 1건 / `同意いただく` 0건 |

## ⛔ 사용자 결정으로 종결 (재작업·재제안 금지 — 이 프로젝트 한정)

- **IA.md 갱신 안 함** — OA 동의 바텀시트 항목 신설·K-Pick 탭 구성 차이(Figma는 `K-뷰티·K-쇼핑·K-POP` 3종 / IA는 `K-뷰티·K-쇼핑·K-컬처·내 예약` 4종) 둘 다 **미반영 확정**(2026-08-24, 세션 #1). IA는 실제 서버 실측 기반이고 Figma는 디자인 상 구성이라 별개다 — 재제안 금지
- **`캐시백 10%`·`캐시백 5%` 배지 12개 미처리** — `xlt` 마커가 없어 대상 외. 이미 `mini_guidekim_cashback`(ko `캐시백 {0}%`)로 등록돼 있다. ⚠️ IA §2-7 「**캐시백률은 고정값이 아니다**」(환경별 상이) — 수치를 상수로 인용하지 않는다

## 세션 기록 (최신 위, 최대 5개)

### 2026-08-24 — 세션 #1: 위키 v2→v3 Screen·다국어 섹션 신설 + 4키 번역 (`04cc57a`)

- **완료**: 코멘트 선별 모드(`XLT` 마커) → 위키 Mode B. 미해결 루트 5건을 통합 번호화(정책 1 + `xlt` 마커 4). 4키 × 5개 언어 번역 → 게이트 P0=0 → 위키 v3 반영 + 이미지·엑셀 첨부. 게이트 리포트 `reports/gate/gate_report_kpick_oa_consent_4keys_2026-08-24.md`
- **교훈 ①(중요) — 신규 키 프리픽스는 물어보기 전에 선례를 실측한다**: 프리픽스 선택지를 제시할 때 「K-Pick 기존 키 = `mini_guidekim_kpick_*`」를 조사하지 않아 사용자가 정보 없이 `mini_kpick_`을 골랐고, 2차 검토 지적으로 **정정 보고 후 재결정**을 받았다. `md/translate.md` Step 2-1은 「신규 키 후보 **문구**마다 유사 키 검색」만 요구하고 **프리픽스(네임스페이스) 선례 조사는 규칙에 없다** → **Step 2-1 보강 필요**(HANDOFF 전역 컨텍스트에 등록)
- **교훈 ② — 자동 `P0=0`에서 2차 검토가 실제 결함 2건을 찾았다**: th `ยินยอมรับ`(`ยินยอม`+`รับ` 붙은 결합 · 레지스트리 3,742키 중 0건 · `ยอมรับ`와 시각 충돌) · en 「쏠쏠한」 수식어 누락(ja `お得な`·th `สุดคุ้ม`·zh `超值`는 반영 — 언어 간 의미 비대칭) + `and…and` 이중 접속. **둘 다 자동 검증기의 구조적 사각지대**이며, `translation-reviewer` 팬아웃이 값을 한 실증 사례다
- **교훈 ③ — 팝업 겹침 오매칭 재발(규칙대로 육안 교정)**: `xlt` 마커 No 4의 핀이 팝업 뒤 배경 바우처 카드의 `5%`(bbox 22×17)를 「포함 중 최소 bbox」로 잡았다. 의도한 문구는 팝업 본문(327×32). 어노테이션 렌더 → 육안 확인 → 수동 교정으로 해소
- **주의 — 2차 검토 에이전트가 세션 한도로 1회 중단**됐다(`session limit · resets 6pm`). 같은 프롬프트로 재시도해 정상 완료. 장문 프롬프트 팬아웃은 한도에 걸릴 수 있으니 결과를 받기 전 완료로 넘기지 않는다

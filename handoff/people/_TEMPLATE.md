# {person}

> 담당 프로젝트: `projects/{a}.md`, `projects/{b}.md`

## PC

| hostname | 저장소 경로 | 비고 |
|---|---|---|
| `{hostname}` | `/Users/{u}/Documents/planning_system_with_figma` | 사내망·VPN 가능 여부 |

## 환경 복구

```bash
pip3 install --break-system-packages -r scripts/requirements.txt
```

## 개인 노트

- {이 사람의 PC에서만 발생하는 함정 · 선호하는 작업 방식}

# 번역 품질 게이트 리포트 — 치환자+통화 표기 `{0}JPYC` → `{0} JPYC` + 용어집 v3.7

- **대상**: 위키 pageId `4479306980`(Screen 표 2곳 + 전역 다국어 표 1행) / `xlt/xlt_output_LV_20260727.xlsx`(73키) / 용어집 v3.6 → **v3.7**
- **작업일**: 2026-07-28
- **담당 FE 팀**: **LV** (치환자 `{0}` 단일 중괄호)
- **사용자 지시**: `{0}JPYC`가 포함된 키를 `{0} JPYC`(공백 1칸)로 바꿔 전체 문구·다국어 번역표·엑셀에 반영하고, `{0} JPYC`를 용어집에 등재해 규칙이 강제되게 할 것

---

## (1a) 번역 전 — 한국어 원문 교정

| 대상 | 변경 전 | 변경 후 | 판정 |
|---|---|---|---|
| `mini_luckyball_prize_won` ko_KR | `{0}JPYC 당첨됐어요!` | `{0} JPYC 당첨됐어요!` | **표기 정책 결정 반영**(사용자 지시). **alias 기록**: `{ {0}JPYC → {0} JPYC }` — Figma 원문·기존 산출물의 붙임 표기는 이 정본으로 대체 |

- 영향 범위 조사 결과 **붙임 표기를 가진 키는 `mini_luckyball_prize_won` 1개뿐**이며, 이미 공백을 쓰던 `mini_luckyball_congrats_won`(`{0} JPYC에 당첨됐어요!`)·`mini_luckyball_reward_paid_toast`(`{0} JPYC가 지급되었어요🎉`)와 **표기가 일치하게 됐다**(교정 방향이 다수 표기와 일치).
- **ja_JP·zh_TW도 동일하게 공백 적용** — 두 언어의 조판 관용은 숫자+라틴문자 붙여쓰기이나, 런타임 치환값이 통화에 달라붙어 보이는 것을 막기 위한 **서비스 표기 결정**(사용자 지시). `md/guide.md` '치환자 보존'에 규칙으로 명문화했다.

---

## (a) 자동 검증 요약 — `scripts/validate_translation.py` 실제 실행 (2회)

**① 라이브 용어집(v3.5) 기준 — 변경 전후 비교**
```
변경 전 : P0 0 / P1 110 / P2 26
변경 후 : P0 0 / P1 110 / P2 26   ← 증감 0
```

**② 신규 용어집(v3.7) 기준 — 등재 효과 확인**
```
P0 0 / P1 124 / P2 26
  · `{0} JPYC` term 관련 P1 : 0건  → 5개 언어 전부 공백형으로 일치함을 역검증
  · 증가한 14건은 v3.6 신규 term(친구·초대·선물하기) 매칭분 — 아래 (c)에서 전건 오탐 판정
```

**placeholder 무결성**: 전 73키에서 언어별 `{n}` 개수 불일치 **0건**. 붙임 `{0}JPYC` 잔존 **0건**.
**P0 = 0 — 통과 기준 충족.**

---

## (b) 수동 3단계 체크표 (md/check.md) — 변경 키 전 컬럼

> 변경은 1개 키의 공백 1칸이지만, 자동 검증은 **엑셀 전체 행(73키) 전수**로 실행했고 (c)에서 **전체 파일**의 P1/P2 전건 처리 판정을 남긴다. 추가로 `JPYC`가 들어간 **전 행(10키)을 눈으로 훑어** 다른 붙임 표기가 없는지 확인했다.

### 1단계: 한국어

| 키 | ko_KR | 판정 |
|---|---|---|
| `prize_won` | `{0} JPYC 당첨됐어요!` | ✅ 치환자 원형 `{0}` 보존, 공백 1칸, 조사(`당첨됐어요`) 정상 |

### 2단계: 용어집 (v3.7 · 112 terms)

| 용어 | 등재값 | 적용 | 판정 |
|---|---|---|---|
| `{0} JPYC` | 5개 언어 모두 `{0} JPYC` | ko/ja/en/th/zh 전부 공백형 | ✅ (신규 등재 term 역검증 통과) |
| 당첨 / 당첨금 | win·当選·中獎·ถูกรางวัล / prize·当選金·獎金·เงินรางวัล | `당첨됐어요`→`当選しました`·`中獎`·`ถูกรางวัล` | ✅ |
| JPYC | exceptions(미번역) | 5개 언어 원문 유지 | ✅ |

### 3단계: 다른 언어

| 언어 | 변경 후 | 판정 |
|---|---|---|
| ja_JP | `{0} JPYCに当選しました！` | ✅ 조사 `に` 접속 정상, 공백은 변수 경계에만 |
| en_US | `You won {0} JPYC!` | ✅ 영어는 원래 공백이 표준 |
| th_TH | `คุณถูกรางวัล {0} JPYC!` | ✅ 태국어는 원래 공백 |
| zh_TW | `中獎 {0} JPYC！` | ✅ 번체 정자, 문말 전각 `！` 유지 |

- 컬럼 정렬 검증(check.md 4-1): 5개 컬럼 회전 어긋남·swap 없음. 이질 문자체계 검출 0건.
- **반영 위치 3면 일치 확인**: 위키 Screen 표 2곳(Unifi mini No.2 · 초대자 프로모션 페이지 No.1) / 전역 다국어 표 1행 5개 언어 / 엑셀 1행 5개 언어 — 재조회로 붙임 잔존 0건 확인(History 설명문 1건은 변경 내용을 기술한 문장이라 의도적 유지).

---

## (c) 검증기 보고 전건 처리 판정 (v3.7 용어집 기준, 전체 파일 73키)

| 심각도 | 건수 | 처리 판정 |
|---|---|---|
| P0 | 0 | 해당 없음 |
| P1 | 124 | **전건 오탐(정상 의역)**. 내역 ⓐ 기존 110건 — 당첨·최대·확인·완료·뽑기·가입 등 용어집 대표 표기 1개와의 문자열 비교에서 나오는 문맥 활용형·대소문자·버튼 관용역. ⓑ **신규 14건** — v3.6에 추가한 `친구`·`초대`·`선물하기` 매칭분으로, 전부 **대소문자(`Invite`/`Friends`/`Gift`)·활용형(`invited`)·수동형(zh `受邀`)·허용 변형(th `ชวน`)**. 실제 오류 0건. ⓒ `{0} JPYC` term 관련 **0건**. |
| P2 | 26 | **전건 오탐/정책 유지** — [마침표 스타일]. 설명문=마침표 / 버튼·타이틀=마침표 없음 정책에 일치. |

**통과 판정: P0 = 0 (자동·수동 모두) → 위키·엑셀·용어집 반영 완료.**

---

## (d) 추가 개선·제안

### (d-1) 용어집 — **v3.6 → v3.7 진행 완료(산출물 전달)**

`{0} JPYC`(5개 언어 동일) 1종을 추가한 **전체 JSON**을 산출했습니다(`glossary_v3.7_full.json`, **112 terms**).
편집 URL: `https://landpress-content-v2.linecorp.com/projects/wdmwbfuv10x39bukv58ocevp/content/collections/web3_xlt_json/items?env=main`

> ⚠️ **CMS는 아직 v3.5입니다**(v3.6 미반영 상태). **v3.7 전체 JSON은 v3.6의 4종을 포함한 누적본**이므로, **v3.7 하나만 붙여넣으면** 링크·친구·선물하기·초대 + `{0} JPYC`가 한 번에 반영됩니다.

새 규칙(`md/landpress.md` §5-1)에 따라 **기획자 가이드도 같은 작업에서 갱신**했습니다 — 미게시 상태이던 v5를 제자리 갱신(용어집 탭 이력 표·전체 용어 표 112행·수치·임베드 JSON + 치환자 공백 규칙 카드).

### (d-2) 확인 권장

1. **리터럴 금액에도 같은 공백 규칙을 적용할지** — 이번 지시는 치환자(`{0}`) 기준이라 리터럴은 손대지 않았는데, 현재 리터럴 표기가 **엇갈려 있습니다**:
   - `mini_luckyball_invite_hero_highlight` = `최대 50,000JPYC` (**붙임**)
   - `mini_luckyball_invite_banner_desc` = `럭키볼 1개에 최대 50,000 JPYC 당첨!` (**공백**)
   같은 원칙이면 `50,000 JPYC`로 통일하는 게 맞습니다. 지시하시면 hero_highlight를 5개 언어로 교정하겠습니다(디자인 원문도 함께 수정 요청 필요).
2. **검증기 대소문자 구분 개선** — 2단계 용어집 검사가 **대소문자를 구분하는 완전일치 부분문자열 비교**라, 라틴 문자 용어(`invite`/`friend`/`gift`/`copy`)는 문두 대문자·복수형만으로도 P1이 됩니다. v3.6 추가만으로 오탐이 14건 늘었습니다. `scripts/validate_translation.py`에서 **en_US 비교를 casefold 후 수행**하고 간단한 어미(`-s`/`-d`/`-ed`) 허용을 넣으면 이 계열 오탐이 대부분 사라집니다. 진행할까요?
3. **th `초대` 표기(`เชิญ`/`ชวน`)** — 앞선 리포트에서 안내드린 대로 **품사·격에 따른 정상 구분**이라 통일 불필요로 판단했습니다(버튼=`ชวน`, 명사·수동=`เชิญ`). 별도 조치 없음.
4. **`친구한테` vs `친구에게`** — 미결(캠페인 전반 `친구한테`로 통일 중).

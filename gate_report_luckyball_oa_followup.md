# 번역 검증 게이트 리포트 — 럭키볼 OA 팔로우 후속 (37141·37584·Mini-OA추가)

- 대상 위키: pageId 4479306980 (럭키볼 친구에게 선물하기 캠페인) / 팀 **LV** (`mini_luckyball_` · `{0}`)
- 대상 엑셀: `xlt/xlt_output_20260724115459.xlsx` (73키) — **번역 변경 없음(재검증)**
- 검증 성격: 사용자 요청 "37141·37584 이미지·번역까지 **전체 재검증**" → 신규/변경 키 한정이 아니라 **전체 파일(73키) 전수** 재검증.
- 용어집: API v2.6 (용어 35 / 예외 9) 새로 조회.

## 0. 한국어 원문 교정 (게이트 1단계)

- 이번 작업은 **신규 번역이 없다** — 3개 대상 프레임(37141·37584·Mini-OA추가)의 표시 문구는 모두 **기존 키 재사용**이며 한국어 원문 변경이 없다.
- `translation_data.json`에 기존 교정 이력이 **alias 80건**으로 보존돼 있음(과거 run의 Figma 원문→XLT Key 매핑). 이번 재검증에서 추가 교정 필요 항목 **없음**.
- 대상 3키 원문 재점검: `럭키볼이 1개 더 도착했어요` / `Unifi 공식채널 팔로우를 하셨군요!` / `Unifi 공식채널을 팔로우 하고 열어보세요.` — 맞춤법·띄어쓰기·표기 이상 없음.

## Executive Summary (자동 검증 — `scripts/validate_translation.py`)

| 심각도 | 건수 | 카테고리 |
|--------|------|----------|
| 🔴 P0 (Critical) | **0건** | 빈칸, 오타, 언어 혼입, 용어집 표기 위반 |
| 🟡 P1 (Medium) | 25건 | 용어 불일치(용어집 협소 매핑) |
| 🟢 P2 (Low) | 26건 | 마침표 스타일 |

→ **P0 = 0 (자동)**. 통과 기준 충족. P1/P2는 아래 각 건 처리 판정 참조.

## 1단계: 한국어 맞춤법·띄어쓰기 검토 (수동, 전수)

전체 73키의 `ko_KR`을 전체 행 기준으로 검토. 발견 P0 = 0.

| 키 | ko_KR | 판정 |
|----|-------|------|
| mini_luckyball_more_arrived | 럭키볼이 1개 더 도착했어요 | ✅ 정상 (알림 문구, 마침표 없음 정상) |
| mini_luckyball_oa_followed | Unifi 공식채널 팔로우를 하셨군요! | ✅ 정상 (`!` 종결) |
| mini_luckyball_oa_follow_open | Unifi 공식채널을 팔로우 하고 열어보세요. | ✅ 정상 (마침표 종결) |
| (그 외 70키) | — | ✅ 오타·띄어쓰기·비표준 공백·`/n` 오타 없음. 외래어 음차 오염·단어 붙은 띄어쓰기 누락 없음 |

- 자동 도구가 못 잡는 부류(외래어 음차 `포이가츠`·단어 붙은 띄어쓰기 `누리고다양한`/`perksand`·다의어 모호 `피부결과`·표현 어색 `미션하고`)를 **수동 확인** — 해당 사례 없음.

## 2단계: 용어집 위반 확인 (수동, 전수)

- 예외 패턴(USDT, JPYC, PIN, API, URL, Apple, Google, IDRP) 표기 통일 확인 — 이상 없음.
- zh_TW 번체 정자(`產`/`臺` 등) 확인 — 간체 혼입 없음.
- th_TH 자산 표기 일관성 확인 — 이상 없음.
- 자동 P1 25건은 모두 **용어집 협소 매핑에 의한 오탐**(아래 처리 판정).

## 3단계: 다른 언어 번역 검토 (수동, 전수)

- 빈칸: 없음 (5개 언어 모든 셀 채워짐, 73×5).
- 언어 혼입(en_US 셀 한글 등): 없음. 컬럼 swap: 없음.
- placeholder(`{0}`) 일치: `prize_won`·`draw_remaining`·`event_period`·`congrats_won` 등 5개 언어 동일 인덱스 유지 확인.
- ja_JP 한국어 직역 패턴: 없음 (`出席`→`チェックイン`류 없음).
- 대상 3키 5개 언어 재검토: 자연스러움·용어 일관 확인 (`公式チャンネル`/`official channel`/`ช่องทางการอย่างเป็นทางการ`/`官方頻道` 일관).

## 각 건 처리 판정 (자동 검증기 보고분 전건)

### P1 (25건) — 전건 **오탐 (용어집 협소 매핑)**
- `확인`→check/查看/ตรวจสอบ 권장(confirm/notice/missions_promo 등): 실제 문맥은 "확인(Confirm/OK)"·"확인해 보세요(check out)"로, 현재 번역(Confirm/確認/ยืนยัน 등)이 문맥상 정확. 용어집이 `확인`을 단일 매핑해 발생 → **오탐**.
- `변경`→更換(zh) / `미션`→mission / `복사`→copy / `로그인`→log in / `송금`→send/轉帳/ส่ง / `예치`→預入(ja): 모두 현재 번역이 문맥상 적절하며, 용어집의 단일 표기 강제로 인한 **오탐**. 실제 위반 아님 → 수정하지 않음.

### P2 (26건) — 전건 **오탐 (마침표 스타일)**
- 배너·타이틀·알림 문구(예: `친구 초대하고\n럭키볼 받기`, `럭키볼이 1개 더 도착했어요`, `{0}JPYC 당첨됐어요!`)는 UI 관습상 마침표를 넣지 않거나 `!`로 종결. 문장형 소수 스타일로 잡혔으나 **의도된 스타일** → 정책 유지, 수정하지 않음.

## (d) 추가 개선·제안 (권장 — 사용자 결정 사안)

- **(d-1) 용어집 보완 권장**: `확인`·`변경`·`송금`·`미션`·`복사` 등 **다의어가 단일 표기로 매핑**돼 정상 의역이 P1로 반복 오탐된다(25건 대부분). 문맥 변형(예: 확인 = Confirm/Check 구분)을 용어집에 추가하면 오탐이 줄어든다. 용어집은 API 읽기전용이라 Claude가 직접 못 쓰며, `md/landpress.md` 절차로 전체 JSON 산출 → 사용자가 Landpress CMS에 붙여넣어 반영. **이번엔 번역 변경이 없어 보류**, 권장만 기록.
- **(d-2) 마침표 정책**: 배너/타이틀 무마침표를 전역 정책으로 고정할지(현행 유지 권장). 임의 적용하지 않음.
- **(d-3) Mini- OA 추가 정책 출처**: 이 프레임에는 Figma 코멘트가 없어, Description 정책은 형제 프레임 `초대자 친구 추가`(63509:41915, "OA 팔로우 안된 경우 친구 추가 바텀시트 노출")와 `피초대자 OA 팔로우 X`(37584)의 "선택 시 OA 페이지 바텀시트 노출" 플로우에서 **도출**했다. 원 코멘트 정책이 아니므로 사용자 확인 대상.

## 통과 판정

- 자동 **P0 = 0** + 수동 3단계 **P0 = 0** → **통과**.
- P1/P2 전건 오탐 처리(수정 없음), (d) 권장은 사용자 결정 전 미반영.
- **번역 변경 없음** → 엑셀 재생성·다국어 표 변경 없음. 위키 변경은 Screen 표(Description 답글 보정 2건 + 신규 행 1건)에 한정.
